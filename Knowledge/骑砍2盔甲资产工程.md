# 骑砍2 盔甲资产工程（Armor Project）

> 状态：**2026-09-14 全链路跑通并实机验证** —— 真田幸村当世具足（战国无双2 解包件）已重定向到
> `human_skeleton`、6 级 LOD 齐、三族贴图程序生成、编辑器 Publish、进游戏能穿能看。
> **工具链**：[tools/armor-pipeline/](../tools/armor-pipeline/README.md)（怎么跑 / 参数 / 坑表 / 编辑器步骤）。
> **本文档** = 知识归档（结论 + 凭据 + 未决）；README = 操作手册。**要做下一件甲，先读 README 再读本文档 §0。**
> 🔴 **先读三节**：[§2 参考物裁定](#2-参考物裁定原版当基准织丰当看样2026-09-14-实测)、
> [§3 结构规格](#3-盔甲结构规格引擎认什么实测)、[§7 坑点表](#7-坑点表)。
> **前置**：换头工程里与"是不是头"无关的通用结论仍然适用 —— 见 [蒂法换头工程.md](蒂法换头工程.md)
> §13.1（FBX 导出规格）、§13.2（引擎凭什么决定要蒙皮）、§13.3（坐标可纯离线量）、§13.5b（ModKit 会删源 FBX）。
> **目标**：把 Taikou 现在**借用原版欧洲/草原装备**的盔甲，换成自建日式甲胄（tpac）。

---

## 0. 第一件甲：真田幸村 当世具足（2026-09-14，全链路实机验证通过）

### 0.1 成品规格

| 项 | 值 |
|---|---|
| 物品 id | `taikou_yukimura_do_a` |
| **内容** | 胴（含**肩帯**）+ 草摺 + 佩楯 + 袴 + 脛当 + 靴 + 内衬着物 + **袖(sode)** + **籠手(kote)** |
| 网格 | 1692 顶点，**6 级 LOD**：1686 / 1529 / 1202 / 675 / 383 / 193 |
| 贴图 | `_d` / `_n` / `_s` 各 **512×780**（原图集 512×1024，裁掉空白的 24%，无分辨率损失） |
| 材质 | `taikou_yukimura_do_a`，`vertexLayoutFlags=[bumpmap,skinning]`，三槽 `tex[0]/[2]/[4]` |
| 装机 | `Taikou/AssetPackages/pack0.tpac`（30.8 MB；Taikou 双客户端 junction 同源，放一处两边生效） |
| 属性 | `body_armor=42 / leg_armor=12 / arm_armor=12`，`Civilian="true"` |

### 0.2 源件 → 子网格对照（**换源件时照这张表重新认件**）

源件 `L00_yukimura.fbx` 共 16 网格 = 3 布料驱动 + 2 武器 + 11 角色件。**认件方法**：逐件单显渲染 + UV 采样贴图颜色聚类（程序判定，不靠肉眼）。

| 子网格 | 顶点 | 部位 | 用途 |
|---|---|---|---|
| 0 | 44 | 兜的系带（两条细带） | 兜件 |
| 1 | 535 | **着物内衬**（含头/手的皮） | 收（须配 `--kimono-torso-only` + `--no-hands`） |
| 2 | 576 | **胴 + 袴 + 佩楯 + 脛当 + 靴** | ✓ |
| **3** | 263 | **左臂袖子 + 左手**（230/263 顶点在左手/手指骨上） | 收 + `--no-hands` |
| 4 | 494 | **袖 + 籠手**（両腕，本来就对称） | ✓ |
| 5 / 6 | 460 / 38 | 脸 / 眼 | 不收 |
| 7 / 8 | 32 / 32 | 草摺 前 / 后 | ✓ |
| 9 | 128 | 草摺 （环） | ✓ |
| 10 | 380 | **兜** | 兜件 |
| 0.001 / 1.001 | 43 / 170 | 武器（矛刃 / 矛柄） | 不收 |
| driver_0/1/2 | 9/10/9 | 布料驱动件 | 不收 |

**排掉武器件不能只按子网格号**：`submesh_1`（着物）与 `submesh_1.001`（矛柄）解析出同一个号 → 判据改用**材质名 `mat_w_*` + 有无 UV**。

### 0.3 管线 = 两条命令

```bash
# 1. 网格：选件 → 重定向骨架 → 6 级 LOD → 导出 FBX
build_armor.py    --src <源件.fbx> --skel <human_skeleton.fbx> --out <out> --name <名>                   --parts body_kimono_arms --kimono-torso-only --no-hands                   --r 0.0120 --r-arms 0.0160

# 2. 贴图：裁图集 + 生成 _n/_s + 重映射 UV（🔴 不幂等，重跑必须先重跑第 1 步）
build_textures.py --armor <上一步的.fbx> --src <源件.fbx> --diffuse <原始图集.png>                   --out <out> --name <名> --parts body_kimono_arms --ao 0.35
```

编辑器那一段（人做）：导入 FBX（unit=m / 不勾 Z-up / **只勾 Import meshes**）→ 材质勾 **`Bumpmap`+`Skinning`** → 挂三张贴图 → Model Viewer 验（`Add Entity→Human` → 挂 **`Body parts`** → 放 `lord_walk` 看跟不跟动）→ Publish（**目标选模块外**）→ tpac 拷进 `Taikou/AssetPackages/`。

### 0.4 关键发现（**写下一件的代码前必读**）

| # | 发现 | 凭据 |
|---|---|---|
| 1 | **权重用源件自带的，不从原版身体抄**（推翻 §2.3 原裁定） | 源件是绑定过的，权重是美术做的。从身体最近点抄的话，草摺会被"就近"吸到单侧大腿，走路左右撕扯 |
| 2 | 🔴 **两侧"正面"可能相反，必须 Y 轴镜像** | 源件正面 = −Y（脚趾/面部骨/前垂草摺全在负侧）；骑砍 = **+Y**（渲染身体可见胸肌朝 +Y）。**不能用 180° 绕 Z 旋转**（会连左右一起翻）；镜像是反射，**做完必须反转面绕序** |
| 3 | 🔴 **骑砍 `bone.matrix_local` 的骨轴不沿肢体** | `spine_9` 头 z=1.0064、尾 y=+0.157，指向正前方。拿它当旋转 → 甲整个转歪 |
| 4 | 🔴 **旋转只能从关节位置推，且只给手臂链推** | 两侧都有"朝向信息是垃圾"的骨：源件骨盆骨 `bone_1` 的最长子骨是腿根（头→子骨得到"朝下"，与朝上的 `spine_9` 差 147.7°）。给每根骨都推会互相打架而切变。手臂 T-pose→A-pose 差 **33.5°** |
| 5 | 🔴 **手臂缩放必须拆「径向 / 沿骨轴」两个方向** | 各向同性放 `--r-arms` 会把手臂**同时拉长**：前臂 25.97cm × 0.0165 = 0.429m，而骑砍肘→手腕只有 0.267m → 籠手末端超出拳头 16cm。沿轴改成按**解剖段长**（两边关节坐标实时求比值）|
| 6 | **比例差用「躯干一个 R + 手臂一个 R」吸收** | 源件腿长约长 10%、躯干约短 1%。躯干 `--r 0.0120` 是**下限**（实测躯干只差 0.3cm，再收身体就顶出来）；手臂 `--r-arms 0.0160` |

### 0.5 ⚠️ 未根治

| 问题 | 现状 | 干净的解法 |
|---|---|---|
| **上臂中段露身体** | 源件那里本来是**着物的布袖子**盖的；着物紧贴胳膊，骑砍身体更粗 → 着物陷在身体里。想靠袖/籠手盖住只能**径向猛撑**，撑到能盖住（≈0.0185）手臂变香肠，不撑（≈0.0130）露一大块。**当前取中间 0.0160** | 加一段**程序生成的布袖**（绕上臂骨的筒，权重给 `upperarm_twist_15`，UV 抄最近着物顶点）。尚未做 |
| 时装穿模 | 6 个 LOD 位置重合是正常（引擎按距离切），但编辑器里要**藏起非目标 LOD** 才能预览 | 已写进两个渲染脚本 |
| 发布包混入无关资产 | `pack0.tpac` 里同时有蒂法/萨菲罗斯的全部头部资产（发布时工程里有的全打进） | 铠甲单独一个工程，或发布前移出无关资产 |

### 0.6 已知可优化

- 贴图可**按 UV 岛重新排布**（现在只做了矩形裁切，图集利用 76%）
- `_s` 的分区阈值可细调（现在亮金只占 0.2%）
- 兜（submesh_10）与靴子的形状还可以打磨（源件是 PS2 时代低模）

---

## 1. 骨架映射：源件 `bone_N` → 骑砍 `bip01_*`

**骑砍侧共 28 根真骨**，骨名后缀就是引擎骨索引（`bip01_pelvis_0` … `bip01_r_finger0_27`）。完整清单见 [蒂法换头工程.md](蒂法换头工程.md) §11。

### 1.1 主映射（按**关节坐标**对，不按名字）

| 源件骨 | 源件 z(cm) | → 骑砍骨 | 骑砍 z(m) |
|---|---|---|---|
| `bone_0` | 0（根） | `bip01_pelvis_0` | 0.9127 |
| `bone_1` / `bone_8` | 117.47 | `bip01_spine_9` | 1.0064 |
| `bone_9` | 132.71 | `bip01_spine1_10` | 1.1630 |
| `bone_10` | 158.18 | `bip01_neck_12` | 1.4623 |
| `bone_11` | 165.87 | `bip01_head_13` | 1.5690 |
| `bone_2` / `bone_4` / `bone_6` / `bone_24` | 102 / 57.57 / 10.57 / 3.17 | `bip01_l_thigh_1` / `_calf_2` / `_foot_3` / `_toe0_4` | 0.9127 / 0.4977 / 0.0928 / −0.002 |
| `bone_3` / `bone_5` / `bone_7` / `bone_25` | 同上（+x 侧） | `bip01_r_thigh_5` / `_calf_6` / `_foot_7` / `_toe0_8` | 同上 |
| `bone_12` / `bone_14` / `bone_16` / `bone_18` | 152.4 / 156.39 / 156.27 / 156.17 | `bip01_l_clavicle_14` / `_upperarm_twist_15` / `_foretwist_17` / `_hand_19` | 1.4104 / 1.4104 / 1.2518 / 1.1052 |
| `bone_13` / `bone_15` / `bone_17` / `bone_19` | 同上（+x 侧） | `bip01_r_clavicle_21` / `_upperarm_twist_22` / `_foretwist_24` / `_hand_26` | 同上 |
| `bone_32~45`（左手指） | — | `bip01_l_finger0_20` | — |
| `bone_26~31, 38~41`（右手指） | — | `bip01_r_finger0_27` | — |
| `bone_46 / 59~62 / 68 / 69`（脸） | — | `bip01_head_13` | — |
| `nuno1_*`（布料骨，约 36 根） | — | **走父链兜底**：往上找到第一根已映射的骨 | — |

**左右按 x 符号对齐**：源件 −x 侧配骑砍 `l_*`。

### 1.2 骑砍侧的三个坑（照抄就对了）

1. **骑砍把上臂/前臂各拆成两根**（`upperarm_twist_15`+`twist1_16`；`foretwist_17`+`foretwist1_18`），源件只有一根 → **映射到前一根即可**（两根同轴，位置正确，只是肘部关节细分少一点）
2. **不要用 `bone.matrix_local` 的旋转**（骨轴不沿肢体，见 §0.4 #3）
3. **不要给每根骨都推旋转**（见 §0.4 #4）

---

## 2. 参考物裁定：原版当基准，织丰当看样（2026-09-14 实测）

### 2.1 先纠一个错判：两个身体其实一样大

初判是「织丰用自建身体（`body_male_japanese`）、我们用原版身体 → 织丰甲不能当基准」。**实测推翻了**：

| 男体（三件拼装后） | 宽 | 高 | 深 |
|---|---|---|---|
| 原版 `body_male_a` + `feet_male_a` + `hands_male_a` | 1.5996 | 1.5438 | 0.2975 |
| 织丰 `body_male_japanese` + `feet_male_japanese` + `hands_male_japanese` | 1.5973 | 1.5202 | 0.2866 |
| **差** | **−2.3 mm（0.14%）** | **−23.6 mm（1.53%）** | −10.9 mm（3.7%） |

**同尺度。** 织丰身体不是"另一个人种体型"，是拿原版改的。

真正不一样的是**分件切法**：

| | 原版 | 织丰 |
|---|---|---|
| 切法 | `body_male_a` **一件**包住 躯干 + 双臂 + 腿 | 拆**三件**：`body_*`(纯躯干) / `hands_*`(整条手臂) / `feet_*`(小腿) |
| LOD | **6 层**（0..5） | **0 层** |
| 原版各件 bbox | 躯干+臂+腿 `x±0.5990 z[0.381,1.544]`；肩 `body_male_a_sh x±0.2083 z[1.285,1.544]`；脚 `feet_male_a z[0.001,0.399]`；手 `hands_male_a x±0.7998 z[0.967,1.190]` | 躯干 `x±0.2891 z[0.803,1.523]`；肩 `x±0.2891 z[1.180,1.523]`；脚 `z[0.003,0.851]`；臂 `x±0.7986 z[0.969,1.398]` |

> ⚠️ 拿织丰身体做参照时**不能按同名部位直接对 bbox** —— 必须按三件拼装后的整体比。

### 2.2 三件甲的实测结构对照

| | 原版 `aserai_cavalry_armor` | 织丰 `sho_new_maru_do` | 织丰 `sho_okegawa_do_a_1_kimono_a` |
|---|---|---|---|
| 来源包 | `Native/AssetPackages/core_game.tpac` | `Shokuho/AssetPackages/pack4.tpac` | 同左 |
| 子网格 | **6 件 = LOD0..5** | 5 件，**全 Lod=0** | 13 件 = LOD0..5 × 2 材质 + 1 件 `.001` |
| 材质 | **1 个贯穿全部 LOD** | 4 个 | 2 个 |
| LOD0 顶点 | **1962**（面 2591） | **13191**（面 20469，**6.7×**） | 2494 + 10910 |
| 顶点合计 | 5605 | 23176 | — |
| 骨骼（B1~B4 去重） | **20**（全 6 层一致） | 逐件 15/16/16/7/5 | LOD0 主件 **20** |
| `UnknownInt2` | 20 | 28 | 28 |
| 逐层顶点 | 1962 / 1636 / 1104 / 488 / 274 / 141 | — | LOD0~5 各 2 件 |
| bbox（LOD0） | x±0.6896 y[−0.2035, 0.2353] z[0.2919, 1.6011] | 整体 x[−0.4460, 0.4363] z[0.3670, 1.5906] | x[−0.6029, 0.6085] z[0.3670, 1.5811] |
| `BoundingSphereRadius` | 0.9758 @LOD0 → 0.9490 @LOD5 | — | — |

**原版甲的权重是多骨混合**（LOD0 的 1962 顶点里 1812 个 W2~W4 非零）—— **不是刚性绑**。

### 2.3 裁定

| 参考物 | 干什么 | 为什么是它 |
|---|---|---|
| **原版甲** `aserai_cavalry_armor` | **结构模板 + 尺寸基准** | 结构最干净：**1 材质贯穿 6 层 LOD**，顶点量级合理（LOD0 1962），正是"做对"的最简范式 |
| **原版身体** `body_male_a` / `body_female_a` | **权重来源** | 甲贴在身体表面，**身体那个位置的顶点权重就是一手答案**（从另一件甲抄是二手）。已 dump 在 `Debug/offline/core_game/out/body/` |
| **织丰 `sho_okegawa_do_a_1_kimono_a`** | **日式分件看样** | 甲 + 内衬双件、LOD0~5 齐全、被多支兵种装备的标准甲 |
| **素材模型** | **轮廓形状**（唯一真源） | 日式甲长什么样由下载到的模型决定，不从游戏资产抄 |

🔴 **不要拿 `sho_new_maru_do` 当范式** —— 它 **5 件全是 Lod=0、一层 LOD 都没有**，单件 13k 顶点。这是孤例（同包的 `okegawa` LOD 是全的），照抄会直接拖垮千人战性能。

---

## 3. 盔甲结构规格（引擎认什么）——实测

### 3.1 LOD 层数 = **0 ~ 5，不是 0~6**

社区文档写「LOD0→LOD6，210m+ 剔除」—— **实测原版全系列都是 0..5**。交叉验证 5 个：

```
aserai_cavalry_armor         6 件  0,1,2,3,4,5
empire_plate_armor           6 件  0,1,2,3,4,5
empire_plate_armor_gauntlets 6 件  0,1,2,3,4,5
empire_plate_armor_shoulder  6 件  0,1,2,3,4,5
body_male_a（身体）           6 件  0,1,2,3,4,5
```

LOD 切换距离（社区文档，未实测）：LOD0→1 **15m** / 1→2 **22.5m** / 2→3 **30m** / 3→4 **50m** / 4→5 **70m** / 5→剔除 **130m**。

### 3.2 子网格命名

- 原版方式：`<名字>`（LOD0）+ `<名字>.lod1` … `<名字>.lod5`
- 织丰方式：引擎按材质自动拆成 `.0/.1/.2/.3/.001`（Blender 导出痕迹）

### 3.3 蒙皮三条件（与头通用，见 [蒂法换头工程.md](蒂法换头工程.md) §13.2）

| 条件 | 在哪 | 缺了会怎样 |
|---|---|---|
| **`VertexLayoutFlags` 含 `skinning`** | **材质**上 | 网格钉死在绑定姿势、**完全不跟骨架动** |
| `SkinDataSize > 0` + `BoneIndices`/`BoneWeights` | 网格 + 顶点流 | native 越界（AccessViolation） |
| 绑 `human_skeleton` | FBX 里带骨架 | 编辑器把元数据全算错 |

🔴 第一条最易漏：**它是材质属性，FBX 不携带** → 换新工程 / 重导 FBX 后必须回编辑器重勾。
原版甲 `UnknownInt2` = **20**（网格用到的骨骼数）；织丰甲是 **28**。**别拿织丰的值去刷原版结构。**

### 3.4 男女版：原版**共用一件网格**

实测：`aserai_cavalry_armor` 标了 `has_gender_variations="true"`，但**只有一个网格名**，包里也没有 `aserai_cavalry_armor_fem`。
`core_game.tpac` 里 `_converted`（社区文档说的女性版）、`_slim`（手套瘦版）**命中数均为 0** —— 那两个是**改 mod 的人自己的命名习惯**，不是原版机制。

⇒ **做日式甲不必做男女两版。**

> ⚠️ 未确证的机制：`woman` 皮肤上有 `body_mesh_suffix="_fem"`，该字符串由 **`TaleWorlds.Native.dll`（C++ 层）**消费，反编译看不到。
> 已知边界：包里没有任何 `X_fem` 形式的网格；`body_meta_mesh` 是显式写死的 `body_female_a`。**真实作用待实机验证。**

### 3.5 头盔特有：跟着脸形参数缩放

原版 `deform_key` 条目带 `helmet_scaling_factor_min` / `helmet_scaling_factor_max`，例如：

```xml
<deform_key id="face_width" key_time_point="1" key_min="-0.3" key_max="0.9"
            helmet_scaling_factor_min="-0.1, 0.0, 0.0"
            helmet_scaling_factor_max="0.02, 0.0, 0.0" />
```

⇒ **头盔会随脸宽/脸深等参数被引擎自动缩放**，做头盔时要考虑这条（做身体甲不受影响）。

---

## 4. 物品注册链

```
FBX ──► [ModKit 编辑器工程] ──► Publish ──► AssetPackages/packN.tpac
                                                  │  mesh= 的扁平名 = 包内资源名
Item XML「id / name / mesh / weight / Type」 ───────┘
   └─► EquipmentRoster  id="Item.<item id>"  ──► NPCCharacter ──► 兵种
```

### 4.1 注册方式 = **目录级，不用改 SubModule.xml**

Taikou 已注册（`SubModule.xml:31-45`）：

```xml
<XmlNode>
    <XmlName id="Items" path="taikou_items"/>
    <IncludedGameTypes> … TaikouCampaign / Campaign / … </IncludedGameTypes>
</XmlNode>
```

`path` 是**目录名**（无 `.xml` 后缀）→ 引擎加载该目录下**全部** XML。**新丢一个 `.xml` 进去即生效。**
（织丰同理：`<XmlName id="Items" path="items"/>`。）

`<AssetPackages>` **不需要声明** —— 全 `Modules/` 下所有 XML grep 零命中，引擎按约定自动扫。

### 4.2 `mesh=` 是扁平字符串

- 不带路径、不带扩展名，**直接就是 tpac 内的资源名**
- 实证：`sho_new_maru_do` 二进制 grep 织丰 7 个包，**只在 pack4 命中**
- **`id` 与 `mesh` 允许不同**：`id="sho_nanban_do_a"` 的 `mesh="sho_nanban_do_1"`
- 下游引用一律用 **item `id`**（`id="Item.sho_bamboo_spear"`），不是 mesh

### 4.3 Item XML 字段（实测归纳）

```xml
<Item id="sho_new_maru_do" name="{=9vQ7nAuu}Maru Do" subtype="body_armor"
      mesh="sho_new_maru_do" weight="15" difficulty="0" appearance="1.6" Type="BodyArmor">
    <ItemComponent>
        <Armor body_armor="33" leg_armor="3" arm_armor="3"
               has_gender_variations="false" covers_body="true"
               modifier_group="plate" material_type="Plate"/>
    </ItemComponent>
    <Flags UseTeamColor="true"/>
</Item>
```

| 位置 | 字段 | 说明 |
|---|---|---|
| 属性·必备 | `id` / `name`（`{=8位ID}`）/ `mesh` / `weight` / `Type` | `Type` 实测：`BodyArmor` `HeadArmor` `HandArmor` `LegArmor` `Cape` |
| 属性·常用 | `subtype` / `difficulty` / `appearance` / `value` / `is_merchandise` | `subtype` 实测：`body_armor` `head_armor`（腿/手/披风常省略） |
| `<Armor>` | `body_armor` `head_armor` `arm_armor` `leg_armor` `covers_body` `covers_legs` `has_gender_variations` `modifier_group` `material_type` `hair_cover_type` | `modifier_group`：`plate`/`chain`/`cloth`/`cloth_unarmored`；`material_type`：`Plate`/`Chainmail`/`Cloth` |
| `<Flags>` | `UseTeamColor` / `Civilian` / `DoesNotHideChest` | 板甲用 `UseTeamColor`，便服用 `Civilian` |

### 4.4 🔴 `culture` 字段：Taikou 与织丰**做派相反**

| | 写法 |
|---|---|
| **织丰** | 5 个盔甲文件里 `culture` **出现 0 次** —— 势力归属完全由「哪个兵种的 EquipmentRoster 引用它」决定 |
| **Taikou** | **写** `culture="Culture.ikoku"`（`taikou_items/body_armors.xml` 现状，v0 拷贝官方物品库带进来的约定） |

**做日式甲时保持 Taikou 现状（写 `culture`）**，别跟着织丰改成不写。

---

## 5. 管线（**已落地，操作见 [tools/armor-pipeline/README.md](../tools/armor-pipeline/README.md)；命令见 §0.3**）

```
源模型 FBX（任意来源的日式甲，**须自带绑定**）
   |  ① 选件（按子网格号；排武器件看材质 mat_w_*）                    }
   |  ② Y 轴镜像到骑砍朝向（反射 -> 反转面绕序）                      } build_armor.py
   |  ③ 骨架重定向：旋转按关节位置推（只手臂）+ 缩放拆径向/沿骨轴       } 一条命令
   |  ④ 分 LOD0..5（减面比抄原版 0.834/0.563/0.249/0.140/0.072）      }
   v
甲 FBX --> build_textures.py：裁图集（_d）+ 程序生成 _n/_s + 重映射 UV
   |
   v [关卡 1] fbx_probe.py 验导出规格（USF=100 / UpAxis=2 / 网格节点零变换）
   v ModKit 编辑器（人做）：导入 -> 材质勾 Bumpmap+Skinning -> 挂三贴图 -> Publish（目标选模块外）
   v tpac 内资源名 --> Item XML 的 mesh= --> 进游戏
```

**关键约定**：`_s` 通道 = R 金属度 / G 255−粗糙度 / B 环境光遮蔽。

🔴 **没有"后处理补丁"这一步** —— 换头工程的 `morphfix` / `skinfix --fullmat` 是**脸部专用**（变形帧 + 脸部角色标记），**盔甲不打**。

---

## 6. 可复用 / 不可复用（换头工程资产）

### 6.1 直接复用

| 资产 | 用途 |
|---|---|
| `tools/face-pipeline/scripts/fbx_probe.py` | **关卡 1**：进编辑器前查 FBX 规格（USF=100 / UpAxis=2 / 网格节点零变换） |
| `tpaccli` 通用命令 | `dump`（导 OBJ/贴图）、`metaparts`（读子网格/LOD）、`morphinfo`（骨骼数/包围盒）、`meshdiff`（逐字段差分）、`assetclone`、`makepack`、`texreplace`、`inspect`、`segs` |
| **硬链接单文件加载**（见 §6.3） | 从 2.8GB / 15GB 的包里读单个资产 |
| **Assets / Assets_disabled 两态 bat** | 编辑器工程不能留给游戏读（见 §7 坑 7） |
| **权威备份 + recipe 脚本**纪律 | 源 FBX 会被 ModKit 删（见 §7 坑 3） |
| **坐标可离线量** | tpac 不存绑定矩阵，静止姿态顶点渲染在自己坐标上 → 写"关卡 2"式离线体检，别烧实机启动 |
| `--weights-from` **思路** | `build_head.py` 里"从原版同类件最近点抄权重" —— 盔甲版改成**从原版身体抄** |

### 6.2 不可复用（脸部专用）

`morphfix` / `skinfix --fullmat` / `transfer_channels.py` / `check_head_space.py` / `build_head.py` 的脸部相关部分
（4 件子网格顺序、59 条变形通道、帧序 Basis→占位→KeyTime —— 盔甲全不需要）

### 6.3 硬链接单文件加载（实测做法）

`AssetManager.Load(dir)` 会把目录下**所有** tpac 载入，所以大包要先隔离：

```powershell
# 建一个只含目标 tpac 硬链接的目录，把它当 --packdir（不额外占盘）
New-Item -ItemType HardLink -Path "...\armor_probe\sho_pack4\pack4.tpac" `
         -Target "...\Modules\Shokuho\AssetPackages\pack4.tpac"
```

实测 **2.3 GB 包加载 0.2 秒**（元数据惰性载入）。

### 6.4 资源定位：先二进制 grep，后开工具

```
grep -c -a "<网格名>" <包>.tpac        # 0 命中 = 肯定不在；≥1 = 在
```

实测：`sho_new_maru_do` → pack4（4 次，其余 6 包 0）；`body_male_japanese` → pack0（12 次，其余 0）。

---

## 7. 坑点表

| # | 坑 | 症状 / 后果 | 规避 |
|---|---|---|---|
| 1 | **官方骨架 FBX 单位标厘米、数值其实是米** | 头骨落 z≈0.0157 而网格在 z≈1.47 → **差 100 倍** | 绑骨架前把骨架缩放到与网格同空间（换头 §11.3） |
| 2 | **FBX 导出规格** | 节点 scale=100 被烘进顶点 / 绕 X 翻 180°（脸朝后） | `FBX_SCALE_UNITS` + `axis_forward='Y'` + `axis_up='Z'` + 导出前重设场景单位（换头 §13.1） |
| 3 | **ModKit 删资产会连带删源 FBX** | 迭代几轮 `AssetSources/` 就空了 | 权威备份放模块外；生成脚本读备份不读 AssetSources（换头 §13.5b） |
| 4 | **Publish 目标目录不能选模块内** | 得到 `Modules/X/X/` 嵌套（140MB） | 固定发到模块外，再拷 `pack0.tpac` 进来 |
| 5 | **材质手工配置会被 FBX 重导入刷回默认** | shader / flags / **VertexLayout** 全丢 → 网格不跟骨架动 | 导完 FBX 后回看三个材质的 Vertex Layout 还在不在 |
| 6 | **Publish 会清空 `AssetPackages/`** | 放在里面的备份被清掉 | 备份一律放模块外 |
| 7 | **`Assets/` 遮蔽 `AssetPackages/`** | 引擎按 `Assets`→`AssetPackages`→… 取**第一个存在的**；`Assets/` 是编辑器半成品（缺顶点流、贴图无像素）→ 游戏读半成品 | 游戏模式必须让 `Assets` 改名 `Assets_disabled`（bat 化） |
| 8 | **织丰甲顶点量级是原版的 4~12 倍** | 照搬精度拖垮千人战 | 自己控面数，按原版量级（LOD0 约 2000 顶点）设预算 |
| 9 | **`sho_new_maru_do` 无 LOD 是孤例** | 照它做 = 没有 LOD 链 | 参照物选 `sho_okegawa_do_a_1_kimono_a` |
| 10 | **`UnknownInt2` 两家不同**（原版甲 20 / 织丰甲 28） | 拿织丰的值刷原版结构 = 错 | 网格用到的骨骼数由编辑器按 FBX 自己算，**别手工覆盖** |
| 11 | **甲只能穿在战斗装里，平民装栏穿不上** | 骑砍装备栏分**平民装/战斗装**两套；物品要带 `Civilian="true"` 才能进平民装栏。**跟负重/力量无关**（甲没有属性门槛）。原版惯例：甲只进战斗装，轻装才带 Civilian（Taikou 里 lamellar / cavalry armor 全都不带） | 想两栏都能穿 → `<Flags UseTeamColor="true" Civilian="true" />` |
| 12 | 🔴 **`<Item>` 内部禁塞注释**（`ItemComponent` 与 `Flags` 之间也不行） | 引擎对这两个子节点是**按下标盲读**的，插一条注释会让它读到注释节点 → 解析失败。与雷 52（`clan_names`）同族 | 注释写到 `<Item>` **外面**（同级两个 `<Item>` 之间）；写完用 `re.finditer(r'<Item [^>]*>(.*?)</Item>')` 自查内部有无 `<!--` |

---

## 8. 已知未知（要实机验证的）

| # | 问题 | 怎么验 |
|---|---|---|
| 1 | **`body_mesh_suffix="_fem"` 到底加在哪** | 由 native 引擎消费，反编译不可见。做法：做一件甲，让男女角色都穿，看是否解析到不同网格 |
| 2 | **LOD 切换距离**（15/22.5/30/50/70/130m） | 社区文档值，未实测；影响不大，可后验 |
| 5 | **`Civilian` 标记对 NPC 的影响** | 玩家侧已验能两栏穿；NPC 的平民装是否也挑这件，未验（本件未挂 EquipmentRoster）|
| 3 | **头盔 `helmet_scaling_factor` 的实际表现** | 做头盔时实机看会不会跟着脸形走形 |
| 4 | **原版 `sturgia_cavalry_armor` / `armor_leather_iron_plate_a` 拿不到** | 名字在 `SandBoxCore/ModuleData/items/body_armors.xml` 里有，但 `core_game.tpac` 里 0 命中 —— 可能在别的包。做参照时换一件能 dump 的 |

---

## 9. 开工清单

| # | 事项 | 谁 | 状态 |
|---|---|---|---|
| 1 | 参考物裁定（原版当基准 / 织丰当看样） | — | ✅ 完成（§2） |
| 2 | 结构规格实测（LOD 0..5 / 单材质 / 骨骼 20 / 男女共用一件） | — | ✅ 完成（§3） |
| 3 | 物品注册链摸清（目录级注册 / mesh 扁平名 / culture 写不写） | — | ✅ 完成（§4） |
| 4 | 工具链盘点（可复用 / 不可复用 / 硬链接加载） | — | ✅ 完成（§6） |
| 5 | 素材模型（真田幸村，战国无双2 解包件） | 用户 | ✅ 完成 |
| 6 | **第一件甲**：选件 → 重定向 → LOD → 导出 | Claude | ✅ 完成（§0；工具链见 [tools/armor-pipeline/](../tools/armor-pipeline/README.md)） |
| 7 | 编辑器：导入 → 勾 Skinning → Publish | 用户 | ✅ 完成（`vertexLayoutFlags=[bumpmap,skinning]` 已验）|
| 8 | 写 Item XML → 进游戏看 | Claude | ✅ 完成（含 `Civilian="true"`，两套装备栏都能穿）|
| 9 | 把 `body_male_a` 的权重 dump 出来当**校验基准** | Claude | ✅ 完成（`Debug/offline/core_game/fbx/body/`，`tpaccli dump --format fbx`） |
| 10 | **贴图三族**：`_d` 裁图集 / `_n` 法线 / `_s` 金属度·粗糙度·AO（程序生成） | Claude | ✅ 完成（`build_textures.py`）|
| 11 | **工具链固化**：`tools/armor-pipeline/`（README = 操作手册 + 坑表） | Claude | ✅ 完成 |

**下一件的建议顺序**：

1. **先跑第二步（换个角色/换个甲）** —— 现在管线是通的，`--parts body_kimono_arms --kimono-torso-only --no-hands --r 0.0120 --r-arms 0.0160` 基本可直接套
2. **补上臂布袖**（§0.5 那条未根治）—— 做第二件时顺手做，之后所有甲都受益
3. **拆件**：`--parts arms` 单独出 `hand_armor`、`--cut-z` 拆 `leg_armor`
4. **兜 → `head_armor`**：注意 §3.5 的"随脸形参数自动缩放"（未实测）

---

## 附：本轮实测数据来源

```
临时产物（不进库）  Debug/offline/armor_probe/
  sho_pack4/pack4.tpac, sho_pack0/pack0.tpac   硬链接
  vanilla_metaparts.txt / vanilla_morphinfo_aserai.txt / vanilla_meshdiff_aserai.txt
  sho4_metaparts.txt / sho4_metaparts_okegawa.txt / sho4_morphinfo_marudo.txt …
  objdump/ body_male_a.obj
命令  tpaccli metaparts|morphinfo|meshdiff --packdir <dir> --filter <网格名>
参照  core_game.tpac = Modules/Native/AssetPackages/core_game.tpac（2,820,936,030 B）
      织丰 7 包 = Modules/Shokuho/AssetPackages/pack0..6.tpac（合计 ~15 GB）
```

### 首件甲（真田幸村）生产期的数据来源

```
源件      D:\BrainMaker\战国无双2资产解包分析\exportbx\L00_yukimura.fbx
原版身体  Debug/offline/core_game/fbx/body/body/body_male_a.fbx（tpaccli dump --format fbx，含逐顶点权重）
原版甲    Debug/offline/armor_probe/vanilla_armor_fbx/*.fbx（7 件，做合身/材质对照）
原版材质  Debug/offline/armor_probe/vanilla_mat/*.mat.txt（vertexLayoutFlags 的实证来源）
已发布包  Modules/Taikou/AssetPackages/pack0.tpac（30.8 MB，含铠甲 + 蒂法/萨菲罗斯头）
产物      tools/armor-pipeline/out/（.fbx / .blend / _d/_n/_s.png / 各类验收渲染图）
诊断脚本  Debug/offline/armor_probe/_armzoom.py（怼近手臂：甲单独 vs 甲+身体）
          Debug/offline/armor_probe/_fitcheck.py（逐 z 带量甲/身体半径）
```
