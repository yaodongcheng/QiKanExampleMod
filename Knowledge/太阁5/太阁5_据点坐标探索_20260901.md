# 太阁5 据点坐标探索（2026-09-01 阶段首轮）

> 结论（先）：**地图规格已定 = 550×448 格**（MapLand_j.TR5 = 246400B = 550×448 地形网格，1B/格，0xFE~0xFF = 海；ASCII 渲染呈日本列岛轮廓）。
> 据点编号体系（官方/社区双确认）：**城 0-179 / 町 180-245（242-245 官方禁用区）/ 里 246-257 / 砦 258-273** —— 与 era_v2 cities.csv `city_idx` 完全一致（用户 2026-09-01 指认：据点编号 era_v2 本来就有）。
> **坐标表本身尚未定位**——本轮排除项见下。

## 已排除的候选
1. **城表 36B 记录**：tail8（0x1c-0x24 的 4 个 u16）实证 = 假名/杂项码（值域 0-25443，非 0-550/0-448 坐标域）→ 坐标**不在** Snr 城表内。
2. **MapFHTbl.tr5（16KB）** = 全 0x0000+0x0113 占位空表（不是坐标表）。
3. INIT.TG5 头后 4.4MB = 混淆/多层数据（1B/格探测失败；字节偏 0xFF/0xFE/0x3F… 疑似 LZ/变换,未解）。

## 已确认的地图资产
| 文件 | 内容 | 状态 |
|---|---|---|
| MapLand_j.TR5 | 550×448 地形格图（1B/格） | ✅ 已定位 |
| MapLand_n.TR5 | 825 u16（0x7E/0xFE 分布） | 小地图/关联层,待查 |
| MapRoute.tr5 | 16137 u16 = 路网表（884 唯一节点id） | 待解析（节点体系,≠坐标） |
| MapFHTbl.tr5 | 空占位表 | ✅ 排除 |
| JapanMap.tg5 / JapanMMap.tg5 / JMAPSTL.TG5 / INIT.TG5 | TK5G 资源包（头结构一致: Magic+TK5G + u32 块数 + u32 尺寸 + 校验） | **下一轮主战场**：写 TK5G 包解码器 |

## 第二轮结论（2026-09-01 深夜，点对点验证版）
1. **TK5G 包全家 = 图形包**：INIT.TG5 / JapanMap.tg5 / JapanMMap.tg5 / JMAPSTL.TG5 头同构（TK5G+u32×3），数据段 = **位平面压缩图像**（字节分布 0xFF/0xFE/0x3F/0x7F/0xFC/0xBF 高位族 = 位平面特征；S-box/XOR 双否定）→ **不在其中找坐标表。**
2. **CWTDAT.TR5** = 数值主表（头明文：u32 文件长/校验/0xE9），@0xB4 起 S-box 系加密（0xA4/0xA5 相间特征），S-box 6 表试解无有效信号 → **未破，代号「主数值表 X」**。
3. **MapLand_j.TR5** = 550×448 地形层（已确证）：**值分布 = 0x0F(41% 陆平) + 0xFE(36% 海) + 0x01/0x02/0x03/0x00 等小值格（0x20=123格 / 0x10=503格 / 0x0C=851格 / 0x00=2574格 / 0x01=12937格）= 网格属性类（路线/河流/城市标记候选，大小与 274 城 / 河路数量级可对应，但无法单层唯一确定）**
4. **MapRoute.tr5** = 16137 u16 路网表：884 唯一节点 id，前 40 值 0-105 = **小值节点域（不是坐标，是路网索引表）**
5. **MapFHTbl.tr5** = 全 0x0000+0x0113 占位空表（排除）

## 坐标在哪儿（最后判定）
- **不在** Snr（36B 城表尾=假名）、MapFHTbl、INIT/TG5 绘图包、MapRoute
- **只剩**：CWTDAT 主数值表（0xB4 起 S-box 系）或 **游戏运行时「地标记录」（=城 36B 内存记录邻域另一字段,社区 CWCheat 证实 36B 间隔;PSP/PC 一致）** —— **官方地图上画城 = 运行时从一个「坐标数组」读——该数组我还没静态找到,已圈定为两个候选：CWTDAT 主数值表 vs 内存地标记录**

## 第三轮结论（2026-09-01 夜，社区+文件交叉，坐标概念澄清）

### 重大情报（社区实证，日本/中文 mod 圈）
1. **Taikou5Editor（hermeskid）= 原版唯一地图编辑器**：可改「**名胜的名称、坐标（地图位置）、种类**」、城/町/里/砦名、地域名、商圈名、海域名——**坐标输入项实锤存在**。工具 = 贴吧 hermeskid 帖（+飞书教程，登录墙）。
2. **基准地图分辨率 = 14080×8960**（游戏「行走」基于此底图；自定义地图 MOD 以此作图）——**地图坐标 = 底图像素坐标系**，550×448 = 游戏内部格网，两者可换算（比例 14080/550 ≈ 25.6, 8960/448 = 20 —— 两轴比例不同！→ 实际为两套独立坐标）。
3. **千階堂ツール（日）属性文档**：类属性索引化（如「滞在座標」= **index 27** —— ⚠️ **该「座標」= 滞留据点编号（0-273），不是地图 x,y**）——属性 Dat 文件由工具内 `?????.Dat` 提供。
4. **FWarAttr.tr5 = 18000B = 180 城 × 100B 属性表**（顺带确定城战斗属性分发，坐标字段扫描未干净命中——**大概率不是坐标表**）。
5. 据点编号分配（与 era_v2 一致）再度被社区证实（城 0-179/町 180-241(+242-245 禁用)/里 246-257/砦 258-273）；**据点数量固定不可加**。

### 定性结论（说人话）
**「据点地图坐标」在游戏内的真实载体 = 「名胜（地标）坐标表」**——由 Taikou5Editor 编辑/导出，地图 = 14080×8960 底图像素坐标系。我们本地没有该编辑器文件；它编辑的**地图结构文件不在标准 TW/根目录清单**（探测完 MapFHTbl=空/FWarAttr=城属性/MapRoute=路网/MapLand=地形格）。**坐标表 = 隐藏于「编辑器专属地图结构文件」（可能是 00000000.016 / .256 / taikou5_template.bin / ali213.bin —— 本机根目录未探测的三个可疑文件！）**

### 下轮一击（已列案底）
- **00000000.016 / 00000000.256 / taikou5_template.bin / ali213.bin**（根目录 4 个未检文件名）→ 逐个体检——**编辑器/原版模板文件，坐标表候选！**

> 备注：社区（日本 Wiki/新浪修改器）确证据点间隔 0x24（36B）内存布局；坐标字段疑似在 36B 记录内但静态位置待定位——**动一把高亚男改坐标,比静态快**。
