# 蒂法换头工程（Tifa Face Project）知识总结

> 状态：**2026-09-13**。目标=在骑砍2 中做出"蒂法脸"可用的角色脸蛋。
> **Blender 侧 + 编辑器编译侧 + 皮肤接管侧已全部打通**：`AssetPackages\pack0.tpac` 已成包（8.99MB）、`skins.xslt` 已被引擎真实加载（日志实证）。
> **当前唯一阻塞 = 1.5.2 游戏在捏脸界面崩溃**，二分定位方案见 **§10.8**。
> ⚠️ 本文件第 **1~9 节写于 2026-09-03**，其中的"下一步/待办"已被 **第 10 节**覆盖——**最新状态一律以 §10 为准**（§10 是交接重点）。

## 1. 一句话现状

**网格与配件的"骨骼"已备齐**：`head_tifa_a.fbx`（蒂法头壳+附件+59 个脸形通道，FBX 7.4/7400，跑过 roundtrip 自检）。
**下一步**（编辑器里进行中）：FBX → 引擎 mesh/metamesh 编译 → 材质贴图打包 → skins.xslt 全量接管 → 新档验证拉杆/表情。

## 2. 关键机制事实（全部实证过）

| 事实 | 证据/来源 |
|---|---|
| FaceKey（128 hex）↔ `KeyWeights[320]`，编码/解码在**C++ 引擎层**（`MBBodyProperties.GetParamsFromKey / ProduceNumericKeyWithParams` → `MBAPI.IMBFaceGen`，无 C# 实现） | 反编译 MountAndBlade.dll 22320 行段 |
| CharacterObject **没有** BodyProperties 属性；某角色的脸 = `charObj.BodyPropertyRange.BodyPropertyMin`（定脸 NPC 的 min==max；随机范围脸才 min≠max） | 反编译 Core 2380-2490 + 编译期报错验证 |
| `KeyWeights[i]` ↔ 该 skin 的 **deform_keys 列表第 i 个（0 基）**。铁证：`KeyWeights[59]=weight 值`、`[60]=build 值`（dump 实测 0.5355/0.5015 精确命中 `StaticBodyProperties.WeightKeyNo=59 / BuildKeyNo=60` 常量） | skins.xml 男/女块各 63 key（顺序一致） |
| 63 键结构：**idx 0..58 = 脸形键**；59=weight 60=build 61=height 62=age（身体键，无网格位移语义） | 两 skin 块导出对照 |
| `deform_key.key_time_point` = **FBX morph 通道名 `KeyTime_{N}`**（xxFemale 子件各带 102 通道 = 基础+扩展） | FBX 导入 shape_keys 名单 |
| KeyWeights w → morph 实际值 = `key_min + w×(key_max−key_min)`。**直灌 w（0..1）会爆脸**，线性映射（B 型）正常 | 多轮渲染对照（暴脸 vs 瓜子脸） |
| KeyWeights 是 **15 档量化**（0, 1/15…1，观察值 0.1333·n）——观察，非结论 | dump 值分布 |
| 游戏脸贴图（白肤+五官）= FaceGen 合成层，源在**私有 `Bannerlord.gts`（103MB）解不出**；`IMBFaceGen` 也无贴图导出 API → Blender 侧只能用素材包 diffuse | 脸部系统分析 §三 + 接口清单 |
| 游戏进程内解码命令 `custom.dump_facekey`（MyCommands.cs）：BodyProperties 原文 + KeyWeights[320] + tattoo，写 `Debug/StoryEngine_RuntimeLog.txt`，**已实测（主角"坎纳尔"）** | 本工程实测 |

## 3. Blender 侧管线（headless bpy）— 坑与修法

- **TWT FBX 导入**：Blender 5.2 导入器对 TWT 的 morph 无 FullWeights 断言崩溃 → **内存级补丁**（不落盘改安装文件）：读 `io_scene_fbx/import_fbx.py` 源码 → assert 换 warning → `exec(compile(...), real_mod.__dict__)`。脚本模板 `%TEMP%\bpy_tifa_transfer2.py`。
- **脸对齐**：头壳两特征点（头部质心点 + 头顶质心）归一化，尺度比 `ratio`（Tifa/xxFemale ≈ 1.238）。
- **通道位移场移植**（把 xxFemale 的 59 个脸形通道搬到 Tifa 拓扑）：每个通道 ±0.3 世界位移 → **KDTree 3 近邻加权** → Tifa 顶点。× **1 近邻=硬贴=脸部起皱；3 近邻=平顺**（用户实测抓到皱纹并修复）。
- **mesh 烘焙**：世界矩阵（`obj.matrix_world @ v.co`）+ **绕 Z 180°**（Tifa 原脸朝 −Y，引擎侧 +Y）；附件（眼球/眉/睫/眼影/嘴）全部同样烘焙，再统一挂空节点。
- **Shape key 在 Object 上**：`obj.shape_key_add(...)`（不是 `obj.data.shape_key_add`——编译期报错的坑）。
- 默认 morph 值 = **0（蒂法原脸）**，通道只作"能力"；要塑形再灌 `min+w(max−min)`。

## 4. Tifa 素材资产（F:\下载\Tifa lockhart In Drees - FBX\）

| 对象 | 规模 | 骑砍2 对应 |
|---|---|---|
| `head` | 4435v / 8742f | 脸壳 `.0`（morph 通道全在它） |
| `Eyeballs` | 458v | 眼子件 `.3` |
| `Eyebrow` | 596v | `.3` |
| `eyelashes` / `eyelashes.2` / `eyeshadow` | 4234/1367/2031v | 睫子件 `.2` |
| `Mouth` | 1494v | 嘴子件 `.1` |
| `hair` | 115,294v | **独立发型件**（不进头壳，另立素材包/暂缓） |
| `root` 骨架 | 150 骨 | **删除/不导出**（引擎脸壳不绑骨） |

贴图全套：`Tifa_Head_D.jpg` + normal/roughness/metallic、`My Edit _Eye_C2/C3.jpg`、`Eyebrow.png`、`Tifa_Eyelashes_D.png`、`Hair_C7/PC0002_01_Hair_Cout`。
**授权**：粉丝二创，仅个人/非商（不能在公共发布物里带上这套模型贴图）。

## 5. 本工程产物（路径表）

**`C:\Users\yaodongcheng\AppData\Local\Temp\lwn_fbx_preview\tifa_fit\`**
- `head_tifa_a.fbx`（2.48MB）—— **编辑器编译主源**（FBX 7400；59 通道 KeyTime_1..59（Basis+59）；7 件（壳+附件）；脸朝 +Y；尺度米级 1.46~1.73；无骨架；空节点 `head_tifa_a_node`）
- `tifa_head_exported.blend` / `tifa_final_show.blend`（55MB 展示版：含发与骨架视口显示）
- `tifa_keyweights.json`（全形拟合 W）—— 已判"全形拟合=娃娃脸"**弃用**，改用"关键键手调"（`guazi_show.blend` / `manual_show.blend` = 蒂法版 KeyWeights 参考）
- PNG：`fit_/manual_/guazi_/tifabase_*`（各阶段对比图）

**源码侧**：`ExampleModVS/.../Debug/MyCommands.cs` 的 `custom.dump_facekey`（已编入，待下次编译）。

## 6. 游戏侧落地（两条路）

- **A 正轨（进行中）**：官方 **Module Editor** 编译 FBX+贴图 → 引擎 mesh/metamesh/材质 → `AssetPackages/*.tpac` → `skins.xslt 全量接管`（照抄模板：xxFemaleHead 的 `pack0.tpac + skins.xml 空壳 + skins.xslt + project.mbproj`）。
- **B 兜底**：不动网格（xxFemale 原网格）→ 蒂法 KeyWeights（手调 W）+ `bake_pool2` 池投影（贴图风）——半天级。
- **TpacTool CLI 能力边界**：贴图写回 ✅（`makepack` PNG→DXT5）、`assetclone` 改名克隆 ✅、**网格二进制写回 ❌**（源码 grep 证实无 Mesh 写入 API）——所以"真网格"必须走编辑器。

## 7. 编辑器环境（fact box）

- Steam 工具：**"Mount & Blade II Bannerlord - Modding Kit"**（appid 1393600，installdir=游戏本体目录；版本 **1.5.2**=游戏本体同版）
- 启动：`bin\Win64_Shipping_wEditor\TaleWorlds.MountAndBlade.Launcher.exe` → `SinglePlayer Play` → **主菜单按 `Ctrl + E`** → 模块编辑器
- **RGL 警告**（"Invalid submodule tag in…"）：点"确定"可继续进编辑器（第三方 mod 建议全部取消勾选）——不是致命错误
- `XmlEditor.exe`（XML 编辑器）；`modding_resources/skeletons/human_skeleton.fbx`（官方骨骼模板）
- VC2013 运行库已装（Steam 社区该情况标准解法）
- ⚠️ CLAUDE.md 里"本机 1.5.1"已是**旧记录**——本次确认客户端与 Modding Kit 均为 **1.5.2**

## 8. 下一步（editor 已开）

1. 编辑器新建/打开工程（建议 `xxFemaleHead` 克隆工程，或新建 `lwn_tifa`），导入 `head_tifa_a.fbx` 编译
2. 子件四件套归并（.0 壳 / .1 嘴 / .2 睫+影 / .3 眼+眉）——编辑器里对象选择顺序对应
3. 材质：照抄 xxFemale 配方（shader `8c88213c-ee55-491f-8523-1ebd5439bea6`，槽位 d,d,n,n,s）→ 蒂法贴图（D→diffuse、normal/roughness→n/s）
4. `skins.xslt` 照抄：头/眉/池/发/纹面/眼色/**deform_keys+constraints** 逐皮肤重写 → 资源指向 `lwn_tifa_*`
5. 打包 → 装游戏 → **新档**看脸；拉杆验证（59 通道在位）；表情 `morph_anims` face_01..12 抽查

---
关联：`Knowledge/脸部系统分析.md`（五官机制/五步战略）、`plans/rules/wheels.d/`（Blender/FaceKey 工作流若非运行时轮子不入 wheels）。本工程属工具链流，非 LivingWorldNpcs 运行时功能。

## 9. 待办总表（对照打的）

| # | 待办 | 状态 |
|---|---|---|
| 1 | FaceKey 解码链路（dump_facekey 命令 + 63 表映射） | ✅ 完成（实测"坎纳尔"） |
| 2 | 蒂法头壳 + 59 通道 + 附件装配（Blender） | ✅ 完成（roundtrip 自检过） |
| 3 | FBX 编译主源 `head_tifa_a.fbx` 导出 | ✅ 完成（2.48MB, FBX 7400） |
| 4 | 编辑器环境：Modding Kit 1.5.2 + Ctrl+E 开编辑器 | ✅ 完成（RGL 警告=可点确定） |
| 5 | 编辑器：新建/克隆工程（建议 xxFemaleHead 模板），导入 FBX 编译 | ⏳ 当前步骤 |
| 6 | 四件套子件归并（.0 壳/.1 嘴/.2 睫+影/.3 眼+眉） | ⏳ 编辑器内 |
| 7 | 材质 mat.txt（shader 8c88213c + 槽位 d,d,n,n,s）+ 蒂法贴图 | ⏳ 编辑器内 |
| 8 | skins.xslt 全量接管（照抄 xxFemaleHead，改名 lwn_tifa） | ⏳ |
| 9 | 打包 tpac → 装游戏 → 新档看脸 | ⏳ |
| 10 | 拉杆验证（59 通道）+ 表情 morph_anims face_01..12 抽查 | ⏳ |
| 11 | 反向 FaceKey 编码（build_facekey 命令）+ 游戏验证 | ⏳ 后续 |
| 12 | 发型素材包（Tifa 头发独立件，可选） | ⏳ 后续 |

---

# 10. 2026-09-13 大进展：FBX 编译成功 → 皮肤接管生效 → 崩溃定位（★交接重点，先看这节）

> 本节是**最新状态**，覆盖前 1~9 节中所有"待办/下一步"。前 1~9 节保留为历史与原理。

## 10.1 一句话现状

**Blender 侧、编辑器编译侧、皮肤接管侧全部打通**：
`head_tifa_a` MetaMesh + 6 材质 + 8 贴图已编译进 `AssetPackages/pack0.tpac`（8.99MB）；
`skins.xslt` 已被引擎**真实加载**（游戏日志实证 `opening .../TifaHead/ModuleData/skins.xml`）。
**当前唯一阻塞 = 游戏在捏脸界面崩溃**（1.5.2），怀疑"跨版本资源不兼容"或"xslt 改写过激"——二分方案见 §10.8。

## 10.2 两个安装的工程位置（★必须同步）

| 安装 | 游戏版本 | TifaHead 路径 |
|---|---|---|
| 默认（Steam） | **v1.5.2** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\TifaHead\` |
| 分版本 | **v1.2.12** | `H:\SteamLibrary\steamapps\common\MB2_Version\MB2_1.2.12\Mount & Blade II Bannerlord\Modules\TifaHead\` |

- 两份**除 `SubModule.xml` 的 `<Version>` 行外逐字节一致**（各 81 个文件，全量 md5 逐个比对：无独有文件，仅 1 个文件不同）。→ **改一份必须同步另一份**（Version 行除外）。
- 资源包由 **1.2.12 的编辑器**编译（源 `head_tifa_a_v3.fbx`）。
- 原 `tifaface` 空壳工程（1.5.2 编辑器建的）已被 `TifaHead` 取代。

## 10.3 编辑器导入 FBX 的正确设置（实证）

FBX：`AssetSources\head_tifa_a_v3.fbx`（2.49MB，md5 `ecae4ae4cc3756e4330eb013ef8cea77`；由 `head_tifa_a_v2.fbx` 清理而来：删 Cam/Sun、拆开 head↔eyelashes 共用材质、清悬空 ARMATURE 修改器与顶点组）。

FBX Import Settings 逐项：

| 项 | 值 | 说明 |
|---|---|---|
| Convert to unit | **m** | 默认 `mm` 会放大 10×；FBX 头声明 `Unit: centimeter`、几何值约 16~20 |
| Blend shape import | **as_vertex_anim** | 59 个 morph 存成 MetaMesh 顶点动画，供 `deform_key.key_time_point` 引用；选 `as_morph_anim`/`ignore`/`as_separate_meshes` 会丢通道或包体爆炸 |
| Convert to Z-up | ✘ 不勾 | FBX 头已声明 `Up axis: y` |
| Import meshes | ✔ **只勾这个** | |
| Remove physics shape type names / Order polygons by 2nd UV / Import skeletons / Import skeletal animations / Import morph animations / Import physics shapes | ✘ | — |

导入框自检统计：`Model(5)`=4 子网格+1 空节点、`Deformer(60)`=Basis+KeyTime_1..59、`Material(6)`。

## 10.4 编译产物结构（tpaccli 实测）

- Assets 编译出：`head_tifa_a` MetaMesh + 6 材质 + 8 贴图（`*_geo/_mtl/_tex.tpac`）；Publish 后汇总为 `AssetPackages/pack0.tpac`。
- v3 FBX 的 4 个对象因多材质被引擎拆成 **6 个子网格**（正常）：

| 子网格 | 材质 | 贴图 |
|---|---|---|
| `.0` 脸壳 | `head_tifa_a` | _d / _n / _s |
| `.1` 嘴 | `head_tifa_a_mouth` | _d |
| `.2.0` 睫 | `head_tifa_a_lash` | _d |
| `.2.1` 眼影 | `head_tifa_a_shadow` | _d |
| `.3.0` 眼 | `head_tifa_a_eye` | _d |
| `.3.1` 眉 | `head_tifa_a_brow` | _d |

- 发布包里 `default_material = None`（编辑器 Assets 里曾误指向源 FBX 的 Geometry，Publish 时自动清干净）。
- Publish 时**目标目录必须选模块外**，否则会把模块复制进自己里面（本次踩过：`Modules\TifaHead\TifaHead\`，138MB）。

## 10.5 贴图准备（`AssetSources\import_ready\`，文件名即资源名，导入即用）

| 文件 | 尺寸 | 来源 |
|---|---|---|
| `head_tifa_a_d.png` | 1024 | `Tifa_Head_D.jpg`（实测与 2048 的 `_2D.jpg` 是同一张图，平均像素差 1.3/255） |
| `head_tifa_a_n.png` | 512 | `Tifa_Head_n.png` |
| `head_tifa_a_s.png` | 512 | **合成**：R=metallic(`_m`)、G=255−roughness(`_r`)、B=255(AO) |
| `head_tifa_a_mouth_d.png` | 512 | `Common_Mouth_Player_C.png` |
| `head_tifa_a_eye_d.png` | 512 | `Tifa_Eye_C3.jpg` |
| `head_tifa_a_lash_d.png` | 512 RGBA | `Tifa_Eyelashes_D.png` |
| `head_tifa_a_brow_d.png` | 1024 RGBA | `Tifa_Eyebrow.png` |
| `head_tifa_a_shadow_d.png` | 128 RGBA | `Tifa_Eyeshadow_D.png`（原始素材仅 128²，偏糊） |

- **材质配方**：shader 选 `pbr_metallic` 即可——**已实证它就是 GUID `8c88213c-ee55-491f-8523-1ebd5439bea6`**（与 Native `body_female_a` 同）；槽位 DiffuseMap→`_d`、NormalMap→`_n`、SpecularMap→`_s`；Material Shader Flags 勾 `use_specular`(+`use_detailnormalmap`)；Vertex Layout 勾 `Bump Map`+`Skinning`。
- 官方文档确认 `pbr_metallic` 的 Specular = R 金属度 / G 光泽度(1−粗糙度) / B AO —— **与 `_s` 合成公式一致**。
- 透明件（lash / brow / shadow）需 `alpha_test`（阈值参考 `xxeyelash_mat` = 0.588）+ `two_sided` + `multi_pass_alpha`。

## 10.6 ★★★ 三个决定性坑（本次全踩过，务必先查）

**坑 1（"mod 不生效"的真根因）：`SubModule.xml` 的 `<Xmls>` 一旦显式列清单，引擎只加载清单内文件。**
- 现象：游戏日志里 `opening .../TifaHead/ModuleData/items.xml` 等 8 个都开了，**唯独没有 `skins.xml`** → xslt 不执行 → 女头不变。
- 对照：`xxFemaleHead`（能工作的同类 mod）与 `Native` 的 `<Xmls>` 都是**空的/不含 Skins**，引擎走默认扫描 → 读到 `skins.xml`。
- 修复：`<Xmls></Xmls>` 清空 → 修复后日志出现 `opening ..\..\Modules\TifaHead/ModuleData/skins.xml` ✅（实证）。

**坑 2：`project.mbproj` 要用 `<file .../>` 形式，不是 `<Module .../>`。**
- `Native` / `xxFemaleHead` 用 `<file id="soln_skins" name="ModuleData/skins.xml" type="skin" />`；1.5.2 编辑器生成的默认模板是 `<Module .../>`。已统一改为 `<file>`。

**坑 3：空壳 `skins.xml` 必须干净，不能留 `<replace_this_with_actual_nodes/>` 占位符。**
- 编辑器默认模板会写这个占位元素；`xxFemaleHead` / `ShokuhoTaikouExpansionPack` 的空壳都是纯 `<skins></skins>`。已改。

> **方法论沉淀（可复用）**：遇到"改名/配置类 mod 不生效"，**先去游戏日志搜 `opening <模块>/ModuleData/xxx.xml`，看引擎到底加载了哪些文件**；再拿"能工作的同类 mod"逐文件对照（本次靠 `xxFemaleHead` 定位）。

## 10.7 日志与崩溃定位法（可复用）

- **日志目录**：`C:\ProgramData\Mount and Blade II Bannerlord\logs\`
  - `rgl_log_<pid>.txt`（主日志，含 `opening ...` 加载记录）
  - `rgl_log_errors_<pid>.txt`、`watchdog_log_<pid>.txt`
- **崩溃记录**：`C:\ProgramData\Mount and Blade II Bannerlord\crashes\<日期>/`（`crash_tags.txt` + `dump.dmp`）
  - `crash_tags.txt` 里有 `Build Version` / `Used Modules` / `Has Installed Unofficial Modules`，**一眼可判"哪个版本 + 哪些模块 + 是否我们的 mod 引起"**。
- 本次实证：`Build Version = v1.5.2.121216`、`Used Modules` 含 `TifaHead`、`crashes\` 目录**史上仅此一条** → 崩溃确由本 mod 引起。

## 10.8 当前阻塞与下一步（交接）

**阻塞**：1.5.2 游戏在**捏脸界面**崩溃（`CharacterCreationScreen` → `FaceGen set character` 之后）。

两个嫌疑 + 二分方案：

1. **嫌疑 A：跨版本资源不兼容** —— `pack0.tpac` 由 **1.2.12 编辑器**编译，拿到 **1.5.2 游戏**跑。
   → **第 1 步：先在 1.2.12 里测**（同版本环境，什么都不用改）。
   - 不崩且女头变蒂法 → 坐实跨版本不兼容 → **用 1.5.2 编辑器重编资源**（FBX/贴图素材通用，只需重导重编）。
   - 仍崩 → 排除版本因素，进第 2 步。
2. **嫌疑 B：xslt 改写过激** —— 除换网格外还改了 `eyebrow_meshes`(清空) / `face_textures` / `mouth_textures`；**崩溃时屏幕上是男性角色**，而我们的 xslt 不碰男性皮肤却仍崩 → 可能整个皮肤库文档被改坏。
   → **第 2 步：换最小版 xslt**（已备好 `ModuleData\skins.xslt.minimal`，**只改 `face_meta_mesh`**）二分定位：
   ```
   skins.xslt          → 改名 skins.xslt.full
   skins.xslt.minimal  → 改名 skins.xslt
   ```
   - 最小版不崩 → 网格 OK，是某段改写把皮肤库改坏 → 逐个加回定位。
   - 最小版仍崩 → `head_tifa_a` 网格/材质在游戏里加载不了 → 从资源角度重做。

## 10.9 `skins.xslt` 现状（完整版做了什么）

对 `skin[@name='woman']` 做 4 处改写：
1. `@face_meta_mesh` → `head_tifa_a`（核心）
2. `eyebrow_meshes` → **清空**（蒂法眉毛烘在 `.3.1` 子网格里）
3. `face_textures` → 单条指向材质 `head_tifa_a`
4. `mouth_textures` → 单条指向材质 `head_tifa_a_mouth`

另附 **65 个 `deform_keys` + `constraints`**（抄自 1.2.12 `ShokuhoTaikouExpansionPack/ModuleData/skins.xslt` = xxFemale 版）。

**已实证的关键安全项**：
- 本 xslt 的 woman `deform_key` 前 **63 个 id 与 Native 逐位完全一致**（第 64 个 `skinkey_post_edit` 在被注释的块里，不生效）→ 顺序/KeyWeights 索引无风险；
- 1.2.12 与 1.5.2 的 Native woman `deform_keys` **也完全一致**（都是 63 个、逐位相同）→ **同一份 xslt 两版通用**，不用重做。

## 10.10 待办总表（更新版）

| # | 待办 | 状态 |
|---|---|---|
| 1 | FaceKey 解码链路（dump_facekey + 63 表映射） | ✅ |
| 2 | 蒂法头壳 + 59 通道 + 附件装配（Blender） | ✅ |
| 3 | FBX 导出（→ 已升级为 v3：清 Cam/Sun/悬空骨架/共用材质） | ✅ |
| 4 | 编辑器环境（Modding Kit + Ctrl+E） | ✅ |
| 5 | 编辑器新建工程 `TifaHead` + 导入 FBX 编译 | ✅ **完成** |
| 6 | 子件归并（.0 壳/.1 嘴/.2 睫+影/.3 眼+眉） | ✅ 完成（引擎按材质自动拆成 6 子网格） |
| 7 | 材质 ×6 + 贴图 ×8（含 `_s` 合成） | ✅ 完成 |
| 8 | `skins.xslt` 接管（并修复 `<Xmls>`/`project.mbproj`/空壳三坑） | ✅ **完成且已被引擎加载**（日志实证） |
| 9 | 打包 → 装游戏 | ✅ 完成（`pack0.tpac` 8.99MB） |
| — | **游戏内验证（捏脸看女头）** | ⏳ **当前阻塞：1.5.2 捏脸崩溃** |
| 9a | 二分定位崩溃（① 1.2.12 同版本测 ② 换最小 xslt） | ⏳ **下一步** |
| 9b | 若跨版本不兼容 → 用 1.5.2 编辑器重编资源 | ⏳ 备选 |
| 10 | 拉杆验证（59 通道）+ 表情 morph_anims 抽查 | ⏳ |
| 11 | 反向 FaceKey 编码（build_facekey） | ⏳ 后续 |
| 12 | 发型素材包（Tifa 头发独立件） | ⏳ 后续 |

## 10.11 关键路径速查

```
Modules\TifaHead\
├─ SubModule.xml                 ← <Xmls> 必须为空！
├─ AssetSources\
│   ├─ head_tifa_a_v3.fbx        ← 编辑器导入主源
│   └─ import_ready\             ← 8 张已按资源名命名的 PNG
├─ Assets\                       ← 编辑器可编辑 tpac（_geo/_mtl/_tex）
├─ AssetPackages\pack0.tpac      ← 游戏实际读的包
└─ ModuleData\
    ├─ skins.xml                 ← 纯空壳 <skins></skins>
    ├─ project.mbproj            ← <file ... type="skin"/> 形式
    ├─ skins.xslt                ← 完整接管版（20.9KB）
    └─ skins.xslt.minimal        ← 二分用最小版（备用）

```

**Blender 侧产物**（在 Temp，易失，建议迁移）：
`C:\Users\yaodongcheng\AppData\Local\Temp\lwn_fbx_preview\tifa_fit\`
（`head_tifa_a_v3.fbx`、`tifa_head_exported.blend`）
稳定备份已放：`D:\BrainMaker\blend_projects\tifa_export\head_tifa_a_v3.fbx`

**对照用的"能工作"参考 mod**：
`Modules\xxFemaleHead\`（`AssetPackages\pack0.tpac` + `ModuleData\{skins.xml 空壳, skins.xslt 178KB, project.mbproj <file>式}`）
