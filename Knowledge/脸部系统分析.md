# 骑砍2 脸部系统分析（含自定义换脸管线）

> 🔴🔴 **2026-09-02 认知升级——先看这里（覆盖旧结论）**：
> 1. **换脸正解 = 学 xxFemaleHead 的「自建素材包 + skins.xslt 全量接管」**（万能包：原版/织丰通吃）——
>    全文见 **§十 破译档案**、**§十一 五步换脸验证战略**（第一步已走完）。
> 2. 旧的「贴图替换不可达」结论**不推反、仅收窄**：对「只涂 diffuse 底色」依然成立（浅层无五官）；
>    对「整脸替换」已被推**翻**——正解是重新画「脸合成池」贴图 + 换头网格 + 全量接管皮肤引用。
> 3. 五步战略：①管线保真验证✅ → ②只换贴图不动名 → ③改资源名 → ④男头(信长脸) → ⑤每人面孔+防包膨胀。

> 状态：预研完成 + 管线工具链打通（2026-08-28/29，基于 v1.2.12 与 v1.5.2 双安装实测）。
> 工具实现见 `tools/face-pipeline/`（解包/打包/渲染/测量/拼脸全链路脚本）。
> 本文档沉淀"事实与结论"，实验过程不展开。

## 一、脸是怎么生成的（为什么丑）

1. **角色只存一串 FaceKey 数字**：`<BodyProperties key="...">` 含 320 个变形权重 + 年龄/体脂。
   JSON 里 `GetParamsFromKey` → `FaceGenerationParams`（KeyWeights[320]，最多 320），见 CSDN #16。
2. **运行时 FaceGen 生成头网格 + 合成脸贴图**：托管壳 `TaleWorlds.MountAndBlade.FaceGen : IFaceGen`
   （`TaleWorlds.Core.FaceGen` 静态门面 + `IFaceGen`），核心生成在 native 引擎（TaleWorlds.FaceGenImpl?）。
   `GetBodyPropertiesWithAge`（年龄变化）、`FaceGen.GetBaseMonsterFromRace`（怪物模板）等 API 见
   `TaleWorlds.MountAndBlade.dll` `FaceGen` 类。
3. **皮肤（skin）矩阵**：`Modules/Native/ModuleData/skins.xml` 按 race × gender × age 选 skin 节点，
   skin 节点指定 `face_meta_mesh`（头网格）、`body_meta_mesh`（身体）等 + 全部 deform_keys（320 权重的范围定义）。
4. **丑的根源**：① 大头网格全游戏就一个（男 `head_male_a` / 女 `head_female_a`），五官靠 320 个
   "顶点挤捏"往**统计平均骨架上套**；② 脸部贴图由 FaceGen 从样本脸库**混合合成**（`face_cache` 缓存），
   混合结果永远"平均脸"；③ 低模 + 护甲头盔/血污/变老共用同一管线，不为"好看"优化。
5. 对照实验：织丰（Shokuho）把男性 skin 换成 `sho_head_male_japanese`（自建网格+4096 贴图，
   `ModuleData/skins.xml` 覆盖），**证明 skin 矩阵可以被 mod 覆盖**（这正是"换头"mod 的机制）。

## 二、五官怎么会动（换脸不破坏动画的关键）

- **面部动画 = 顶点动画（morph）**，不是骨骼、不是贴图切换：
  `Modules/Native/AssetPackages/morph_anims.tpac` 含 `face_01~12`、`face_smile1`、`face_smiling_hard`、`face_cutscene` 等 clip。
- 播放入口：`Agent.SetAgentFacialAnimation(FacialAnimChannel.High/Low, "face_03", loop)` →
  native `IMBAgent.SetAgentFacialAnimation`；骨架级：`Skeleton.SetFacialAnimation(channel, name, playSound, loop)`
  （`TaleWorlds.MountAndBlade.dll` `Skeleton` 扩展，`AGENT` 也支持 `FacialAnimChannel`）。双通道（眉眼/嘴可分列）。
- **推论：换脸贴图不改动画**——morph 动的是网格顶点，贴图只是被拉着走的"皮"。
  新贴图只要与引擎 UV 布局对齐，动画照常；代价 = 表情时贴图 2D 形变（观感等价于女头美化类 mod 的效果）。

## 三、头部资源布局（换脸的直接目标）

| 资源（男/女一致） | 类型 | 说明 |
|---|---|---|
| `head_male_a`（metamesh） | 网格 | 头网格；男=全游戏公用 |
| `head_male_a_d/_n/_s` | 贴图 | diffuse/normal/specular；原版像素在 **私有的 `Bannerlord.gts`**（Graphine 图集，103MB，TpacTool 解不出）→ 换脸贴图必须**全新建** |
| `eye_a_d` | 贴图 | 眼球独立小贴图（左小岛）；眼睛/睫毛/嘴为 head 网格的子 mesh（`head_male_a.0/1/2/3`，CSDN #16） |
| `male_eyebrow_1..10` | 贴图/网格 | 选装眉毛（独立网格+纹理） |

**男头 UV 布局（head_male_a，v1.2.12 实测）**：
- 整脸挤在贴图**左下角 u 0.01–0.20、v 0.02–0.36** 的窄区：眼带 u[0.06,0.20] v~0.29（**左右镜像共用一小岛**），
  鼻尖 (0.012,0.235)，嘴 (0.020,0.184)，下巴 (0.047,0.114)；
  **眉毛在贴图中部 (0.50,0.17) 独立岛**（与其余五官不同层！）；耳朵岛 (0.114,0.208) 与嘴/眼带相邻。
- 女头布局完全不同（脸区居中）→ **锚点必须从"想换的那个头的网格"重新测量**（几何/渲染双轨校准，见工具）。

## 四、覆盖机制（换脸落地方式）

1. **引擎无"覆盖清单"**：同名资源后加载者覆盖先加载者。mod 的 `AssetPackages/*.tpac` 里放同 ID 资源
   （如 `head_male_a_d`），排 Native 之后即可。**SubModule.xml 只是注册 mod 名字/依赖，无覆盖规则**。
2. 范本：GT_Face（换女头，无任何 XML 覆盖）、WomeninCalradia（女性换头，自建 `head_malfoy_a_*`资源）。
3. **给特定角色换脸** = 拆 skin/race（`skins.xml` 加节点 + 角色 `face_meta_mesh` 引用），织丰自己的
   `sho_head_male_japanese` 就是完整先例。牵涉 `face_mesh_cache`（见下）。

## 五、脸缓存（NPC 为什么不刷新的机制）

- `face_mesh_cache="true"`（NPCCharacter 属性，织丰领主们都有）→ **该 NPC 的脸生成结果缓存**；
  `AgentBuildData.UseFaceCache/FaceCacheId`（`TaleWorlds.MountAndBlade.dll`，`AgentBuildData`
  `UseFaceCache`/`FaceCacheId`）+ `FlushFaceCache`（`FaceGen.FlushFaceCache`）。
- 实测：**旧存档里的 NPC 不受新贴图影响**（脸已固化）；主角（无此属性）实时生成。
- 处理：新档全生效；旧档需清缓存字段（存档手术，待实现）；或让 NPC 的 `face_mesh_cache=false`。

## 六、已验证的管线与探针法（2026-08-29 实机）

| 包 | 对方 | 结论 |
|---|---|---|
| A 包（孤儿资源 `test_face_d`，不覆盖任何东西） | mod 框架 | 引擎认，进游戏 → **tpac 段格式 OK** |
| B 包（覆盖 `head_male_a_d`） | 主角 | 主角脸生效（**红/黑/绿底色探针全部呈现**）→ **机制全通** |
| 红/黑/绿+蓝块探针 | 主角 | 底色通道 100% 生效；**五官区仍显原版** → **暂结论：脸部有独立"合成五官层"在基础贴图之上（FaceGen 合成层）**，仅纯色能透出；**待精确探针定案** |
| 标尺网格探针（0.1 网格+五官紫点） | 主角 | 校正 U V 坐标之（**测试中**） |
| v1-v17 拼脸包 | 主角 | 从"黑脸"→"五官对位"演进；用户端仅"下巴黑纹可见"→ 与合成层/UV 差都有关系（探针后定） |

- **视图分叉坑（重要）**：Bash（沙箱）产出的文件与宿主（IDE/游戏）不同视图 → **最终交付（dist/装游戏）必须走 PowerShell 或 Write/Read 工具**。
- **怪包大小正常**：tpac 段有压缩（均匀贴图 147KB 是压缩后，不是数据缺失；roundtrip 字节级一致已证）。

## 七、工具链（`tools/face-pipeline/`）

```
tpactool/            # TpacTool fork + TpacToolCLI（list/dump/inspect/roundtrip/makepack/assetclone）
scripts/             # render_uvview(正脸视口→UV色图) render_head(贴图预览)
                     # geo_anchor_v2/v3/v4(几何锚点) uv_pick(数值桥) uv_pick_sho(自适应)
                     # find_eyes(原版贴图抓瞳) warp_face/warp_sho_face(旧拼脸) bake_pool2(女池投影烘焙)
data/                # meshes/*.obj(解包导出)  + 关键对照图 + 输入/输出
                     # 旧 dist/FaceCustomLWN(贴图替换路线 mod 包)已于 2026-09-02 删除——路线判废
```

CLI 关键命令（`tools/face-pipeline/README.md` 有完整用法）：
`tpaccli list/dump/inspect/roundtrip/makepack/assetclone`（assetclone = 自包含克隆改名重指，
2026-09-02 新增）。

## 八、待解决（下一步）

> 🔴 **2026-09-02 更新**：本条多数遗留已由 §十/§十一 替代（旧路线=覆盖 diffuse=判废；合成层性质=已定案）。
> 保留仍有效的项 + 重定向：

1. ✅ ~~FaceGen 合成层确切性质~~ ——**已定案（§10.1/10.2）**：合成层 = FaceGen 从「脸池 diffuse」
   采样绘制到头 UV 上；因此换脸 = 画池贴图（五官画上），而不是涂头底 diffuse。
2. **旧档 NPC 清脸缓存**（仍有效）：face_mesh_cache 已固化；存档手术工具（待做）。
3. ~~织丰环境换脸（覆盖 2sho_head_male_japanese_d）~~ ——**废弃**：那是被判废的 diffuse 覆盖路线；
   织丰男头替换走 §十一 步骤 4（自建/克隆素材包 + skins.xslt 接管）。
4. ~~“仅信长专用” skin 拆分~~ ——并入 §十一 步骤 4/5（先全组统一，再拆 race/肤色差异）。

## 九、来源

- 反编译：`TaleWorlds.MountAndBlade.dll`（FaceGen/AgentBuildData/SetAgentFacialAnimation）、
  `TaleWorlds.Core.dll`（FaceGen/IFaceGen）、`TaleWorlds.Engine.dll`（MBAPI 桥）。
- 资源：`Modules/Native/ModuleData/skins.xml`、`morph_anims.tpac`、GT_Face/WomeninCalradia/Shokuho mod 实文件。
- 社区：TpacTool（MIT，szszss/hunharibo）、texconv（DirectXTex）、Real-ESRGAN x4plus（ncnn）；
  论坛资料 [steamcommunity face 讨论](https://steamcommunity.com/app/261550/discussions/0/3324240298477540552?l=french#1)、
  [Nexus Female Head Overhaul](https://www.nexusmods.com/mountandblade2bannerlord/mods/12331)、
  [中文站 贴图适配求助](https://bbs.mountblade.com.cn/thread-2064318-1-1.html)、
  [moddocs 资产类型](http://moddocs.bannerlord.com/ru/asset-management/asset-types/)。
- 预研（旧叙述段）原样保留在会话记录；此处为结论版。

## 十、xxFemaleHead 破译档案（2026-09-02，换脸的正解模板）

**一句话**：xxFemaleHead = 一个「万能包」——资源全部自建（不依赖/不修改任何来源 mod 的资产），
`skins.xslt` 对**合并后的全库 skin**（Native/织丰 都行）全量接管女性皮肤引用。加载顺序排后的
mod（我们的内容包），其 XSLT 无论来源是谁一律生效。

### 10.1 包的构成（20 项资产 = 一张脸的完整素材链）

| 资源 | 说明 |
|---|---|
| `head_xxfemale_a` 网格 | 自建女头（4 子网格：脸皮+眼+嘴+LOD；变形骨架与素材配套） |
| `female_xxeyebrow_2` 网格 | 眉附件网格 |
| `head_female_x1~x5` 材质 ×5 | **脸池材质 ×5 档**（相同配方：`shader 8c88213c-ee55-491f-8523-1ebd5439bea6`、槽位 `(d,d,n,n,s)`、blend=factor、use_specular+use_detailnormalmap、vertexLayout [bumpmap,skinning,doubleuv,skinning_precise]） |
| `head_female_x15/18/20/21/25_d` ×5 | **5 张池 diffuse（2048²）= 把整张脸（眼/眉/鼻/嘴/耳）直接画在 UV 图上**，脸区居中——这就是 FaceGen 合成层采样的"脸源" |
| `head_female_x10_n/s`、`x4_n` | 池法线/高光 |
| `xxeyelash_mat` + `xx_lashes12_d` | 睫毛材质/贴图 |
| `body_female_a/kid_female_a` 材质 | 身体皮肤材质（skin 用） |

### 10.2 XSLT 全量接管（8 组模板，skin[@name='woman'|kid_1/2/3_female|toddler_female]）

①`face_meta_mesh`→新头 ②`eyebrow_meshes`→新眉 ③`face_textures`→5 池（entry tags=face_texture2..5）
④⑤`hair_meshes`+`default_hair_meshes`→新发型列 ⑥`tattoo_materials`→Cleanface 系 ⑦`eye_color_gradient_points`
⑧**`deform_keys`+`constraints` 逐皮肤重写**（自己的网格配自己的变形口径——**换网格必须连变形键一起换**）。
空壳要点：同名 `skins.xml`（可空）+ `project.mbproj` 注册 `type="skin"`；XSLT 改写的是**前载模块合并后的文档**，
自己的空壳文件不被自己的 XSLT 修改。

### 10.3 管线代码现状（全部在 `tools/face-pipeline/`，2026-09-02 载入 git，不再被 ignore）

- **tpaccli**（TpacToolCLI 控制台工具，`dotnet build -c Release`，产物 bin/Release/net9.0/）：
  - `list`（列出包内资源）·`dump`（导出：obj/fbx/png/材质 .mat.txt）·`inspect`（纹理全字段）
  - `makepack --manifest`（PNG→DXT5 贴图→tpac，模板字段从织丰 pack6 实例）·`roundtrip`（解包→原样重打包→md5 对照）
  - **`assetclone`**（依赖图深拷贝改名重指：mesh+材质+贴图→lwn_* 自包含包；`--src/--newname/--extra/--packname`）
  - 库侧补丁：`Material.WriteMetadata`/`SubVersion`（上游缺失）、`RawMeta` 捕获+Save 原样直写、
    解析不匹配兜底（老纹理元数据 227 类越界→跳过解析、raw 保真）、`EditmodeMiscData` 剔除（库无写回）、
    BigGustave 类型 public 化、`WriteVec4`、Mesh 计数元数据 public 等。
- **scripts/**：`render_uvview`（正脸视口→UV 色图，轴模式参数化 男 xy/女 zx）、`render_head`（贴图预览）、
  `geo_anchor*`/`uv_pick*`/`find_eyes`/`warp_face`/`warp_sho_face`(旧拼脸)、`bake_pool2`（女池投影烘焙，第 2 步用）、
  `decode_miniface`（TK5 MINIFACE DDS→PNG）。
- **已删实验垃圾**（2026-09-02）：`scripts/warp_pool.py`（TPS 手点失败版）、`scripts/bake_pool.py`（轴错 v1）、
  `data/output/`(111M)、`tmp/`(687M)、`data/input_oda*.jpg`——按用户裁定清理。

## 十一、五步换脸验证战略（2026-09-02 用户制定）

| 步 | 内容 | 状态 |
|---|---|---|
| 1 | xxFemaleHead **原封不动**解包→重打包（资源名不动），验证解包-打包管线正确性 | ✅ **完成**（字节级：解拆→重组→再解出，20 项资源逐字节一致；md5 差异仅为包装层压缩；女包 `lwn_femhead_copy.tpac` 30.5MB 已装内容包 + **原样照抄**其 skins.xslt；待游戏内 30 秒确认——停用 xxFemaleHead、只装我方、新档看女头） |
| 2 | **只换贴图不动资源名**，重打包进游戏验证 | ⏳ 素材=我们 AI 生成脸；bake_pool2 管线 |
| 3 | **更改资源名**，重打包，证明自己管线跑通 | ⏳ assetclone 已具备 |
| 4 | 模仿女头经验，**完成男头替换**（信长脸：nobunaga.png 母版 + 织丰/原生男头网格） | ⏳ 素材与锚点在 hand |
| 5 | 每人有自己的面孔 + **包体不膨胀**：研究除贴图外是否需调整面部参数（deform_keys/重建池依赖） | ⏳ 设计预研（xxFemaleHead=5 池共用 1 母版+变体→包体上界=单套脸 + 每池 2048 d/n/s） |

### 11.1 第一步完成记录（管线保真证据）

- `tpaccli roundtrip`：原包 32,015,735B → 重组包同尺寸；解出资源全部逐一比对（fbx/obj/PNG/*.mat.txt **字节一致**）。
- md5 不同 = 段压缩字节/索引层（非内容层），引擎读内容语义等价——**管线写回无损原理级成立**。
- 历史教训（2026-08-31/09-02）：旧版本 md5 差异**是**内容损伤（库解析器对老纹理元数据读越界→写回坏结构→白脸），
  修复 = RawMeta 捕获 + Save 原样直写（10.3）。**今后任何新编译产物按 `roundtrip→解出比对` 做回归**。

## 十二、新 session 快速接续清单（2026-09-02 收尾）

> 断点审计完成（开新 session 前自查）：**第一步验收配置全部就位**，见下表。

### 12.1 磁盘现状核对表（第一步 = 解包-重打包管线验证）

| 部件 | 位置 | 状态 |
|---|---|---|
| 验收包（原封不动 20 项资源，30.5MB） | `ShokuhoTaikouExpansionPack/AssetPackages/lwn_femhead_copy.tpac` | ✅ roundtrip 产物（资源名未改；md5 差异仅包装层压缩，解出内容逐字节一致）。**gitignore 不入库（测试品，2026-09-02 用户裁定），游戏内照常加载** |
| skins.xslt（**原样照抄 xxFemaleHead**，6237 行/44 模板） | `ShokuhoTaikouExpansionPack/ModuleData/skins.xslt` | ✅ 引用 head_xxfemale_a / head_female_x1..5 / female_xxeyebrow_2 原资源名；hair/tattoo 引用 **Native 已有素材**（female_hair_l、tattoo_female_a_mat 已实测存在）→ 零缺资源 |
| skins.xml 空壳 + project.mbproj | 同 ModuleData/ | ✅ 触发引擎合并机制 |
| 女脸母版（换脸素材用） | `ArtSource/raw/oda_bigface_s2002.jpg`（s2003 备用） | ✅ s2002 = 正脸直视/对称/画风贴合（quick_bigface QUICK_FEMALE=1 产物） |
| 解包产物存档 | `ArtSource/xxFemaleHead_unpacked/`（20 项资源+对照） | ✅ |
| 工具链 | `tools/face-pipeline/`（tpaccli + scripts） | ✅ 已入库（含 assetclone/roundtrip/makepack/raw 直写补丁） |
| 旧实验路产物 | FaceCustomLWN ×3 处、data/output、tmp 等 | ✅ 已删；（游戏 Modules/FaceCustomLWN 旧 mod 待用户自行删除） |

### 12.2 第一步验收操作（30 秒）

1. 游戏启动器：**停用 xxFemaleHead**；确保启用 ShokuhoTaikouExpansionPack（其 AssetPackages 含验收包）；
2. **新开存档**（face_mesh_cache 只在旧档固化）；捏脸【女性】或查任意女性 NPC；
3. 预期：女头 = xxFemaleHead 原版女脸（因为包=原封不动素材；等效"原效果平移"）。**女头正常显示 = 第一步通过**。

### 12.3 已知挂起任务（下一步候选）

- `render_uvview.py` 女头 zx 轴渲染（女头 OBJ 用全身骨架坐标：z=宽 ±0.11、y=身高 1.4-1.8；zx 轴模式已实现**未验证**——bake_pool2 前置校准）；
- 第二步（只换贴图不动资源名）：bake_pool2（母版 s2002 → 5 池 d/n/s + 眉/嘴/眼）→ 同名单覆盖进包；
- 第三步（改资源名）：assetclone 命名链已就绪；
- 第四步（男头信长脸）：nobunaga.png 母版 + 织丰/原生男头；
- 第五步（人均面孔 + 包体上限）：5 池共用 1 母版变体 = 包体上界设计。

### 12.4 纪律提醒（续接时反复核对）

- tpac（无论立绘/换脸）一律**不入 git**（2026-09-02）；只有源码/脚本/配置/CSV 入库；
- 打包产物一律走 `roundtrip→解出比对` 回归（RawMeta 直写修复后写过；新改动必测）；
- 生成物改内容 = 改脚本重跑（铁律 22）；素材与生成器分仓（scripts/ 入库、ArtSource 素材忽略）。

## 十三、2026-09-02 晚班：gpt-UV 直出管线（当前主线）+ 本轮全部教训

> ⚠️ **认知翻转 1**：第一步"30 秒验收"（新档看女头）**从未真正在游戏内确认过**——roundtrip 保真只验证了管线读写字节点级，**游戏内表现（XSLT 头型链）从未实证**。"验证过"≠"通过"，本次实机证明头型一直是原版的。
> ⚠️ **认知翻转 2**：`render_head.py` 采样公式应为 `py = v*H`（vt v=0 对应贴图顶，xxFemaleHead 实测）——旧 `(1-v)*H` 版本做的所有"对位验证"是**镜像假象**。**任何渲染回显先做色带标定**（色带贴图→渲染→读段分布）。
> ⚠️ **认知翻转 3**：目标锚点表真值 = **原版画布位置**（直接用"原版贴图 render 对照"：渲染出正常脸=公式对）。OBJ 顶点带提取的 uv 均值为噪声大用，不可作锚。

### 13.1 当前主链路（女脸皮，已装游戏，头型待验证）

1. **gpt-image-2 直接文生 UV 展开图**（重大发现：模型能画标准 atlas——脸主体+4 角耳岛+唇岛）：
   - prompt 模板（见 `tools/face-pipeline/scripts/gen_uv.py`）：3D 女性面部头部 UV 纹理/平面投影/对称/闭口/无发/无妆/无阴影统一照明/PBR 哑光/超写实；**睁眼+深棕瞳+窄杏仁眼（眼宽≈头宽 1/8）**；变体=加 "slightly different face: ..."
   - API：网易端点（ArtSource/api_config.json，gpt-image-2）`images/generations`；**不接受 response_format**；**限流 429 → gen_uv.py 内置 8 轮退避 35s**
2. **mediapipe 自动锚（22 点）**：UV 大脸/无刘海 → 直接信眉眼角（33/133/362/263）；鼻嘴下巴=168/1/2/61/291/13/17/152/175；颏下=152+12px；额=眉线 y-60 三点
3. **一般仿射（3 对点）**：瞳 L/R + 下唇中 → LSQ 6 参数，`warpAffine` 上 2048 池坐标。**不用 TPS**——TPS 在锚区外线性项外推（实测 -720px 级旋转）→ 平带/白条。正脸对称素材仿射足够
4. **源域 mask**：与背景棕 (139,113,92) 距离 >90；**下缘截断 y<0.72H**（脸底=颈部带不进入）；bbox (470,430,1500,1760)；边缘 41blur
5. **眼带原版化（决定性）**：网格眼区尺寸=原版猫眼（AI 眼形贴不上，1/8 宽 prompt 仍偏宽）→ **EYE_BAND=(460,925,1530,1140) 直接取原版 base + 101px 高斯羽化**（硬边→柔和过渡）。**整带横向压缩方案证伪**（拉丝/眼位漂）
6. 产物走 texreplace（DXT1/1mip/原 GUID/RawMeta=null）→ roundtrip 回归 → 装机

### 13.2 本轮失败/教训清单（避免重踏）

| # | 经验 |
|---|---|
| 1 | 花屏（之前）根因 = **RawMeta 残留**（`Save` 走 `RawMeta ?? WriteMetadata()`）→ texreplace 必须置 `old.RawMeta=null`，否则引擎按 12-mip 老元数据读 1-mip 数据=越界碎块 |
| 2 | 渲染镜像假象：**先色带标定，再谈对位**（本会话标定图 10 分钟解决 1 天的疑虑） |
| 3 | mediapipe 眼角对小照片（刘海/小脸）检错（33/133/362/263 语义乱序）→ 瞳孔推导重建**仅限小脸**；大 UV 脸直接信任 |
| 4 | TPS `λ=1e5` 也没救锚区外旋转外推——**正脸线性素材用仿射，别用 TPS** |
| 5 | 母版有刘海=素材天花板（补全秃额/额帽均证伪）→ 无解即换素材（后梳露额版） |
| 6 | 5 池必须 5 张不同脸（用户裁定：同脸亮度变体=品质不合格）——gpt-UV 方案天然多张低成本 |
| 7 | FreeUV（CVPR2025）：环境 95% 装好（venv/torch cu121/nvdiffrast **[ComfyUI 0.3.3 py3-none-any wheel 可过 import+cudaTrue]**/SD+CLIP+FLAME 3 件/主权重 2bin）——**唯一缺口 albedo spec 1.84GB（GitHub 下载网络墙+HF 镜像间歇断）**；因 gpt-UV 更快（90s/张 vs 全套 1h+）被降级备胎。**SKIP：data-process 老 requirements（numpy1.23）需老 py；`sys.modules` 注意 vendored diffusers（FreeUV/diffusers）需 huggingface_hub 老 API 补丁（已打）** |
| 8 | 素材目录工作流（用户裁定）：`femhead_pack/*.png` = 图片源；骨架=原包；**改图=改目录→texreplace 一键打包**；`.obj/.mat.txt` 不进打包器（仅存档） |
| 9 | gpt-UV source 与目标画布**眼区宽度不可强解**——眼带原版化最稳（见 5） |

### 13.3 未决 / 下一步（user 已拍板）

- **已完成**：5 张 gpt-UV 脸皮已装（`AssetPackages/lwn_femhead_copy.tpac` 30.56MB，含 20 项+新 5 张皮）
- **待验证**：**XSLT 头型链**（face_meta_mesh→head_xxfemale_a / hair_meshes / deform_keys 重定向从未实证）。文件侧已与原版 0 diff（xslt 178,392B 相同；skins.xml/mbproj(filet=skin)/SubModule Xmls 空 均一致）。**mbproj 位置=ModuleData/**（原版实证，勿信"模块根"——2026-09-02 晚间猜测已证伪）
- **用户决定：复原第一步基线**——把 AssetPackages/lwn_femhead_copy.tpac 恢复为**原封 20 项**（=xxFemaleHead pack0.tpac 等价）→ 新档确认「女头=原版脸（第一步预期）」→ 锁定 XSLT 链 —→ 再换回 gpt-UV 皮做二段验证
- 后续按用户指令走；余项：旧档缓存清脸（存档手术）、紫眉/睫毛保持原版、FreeUV 备胎复活(需 albedo)、男头信长(第四步)


### 13.4 待办：照片 → 捏脸参数生成器（user 裁定 2026-09-02，仅记录勿开工）

**用户裁定**：只靠换脸部贴图实现织田信长**终究不可行**（贴图="固定长相的皮肤"，照片脸再贴也只是"膜"；信长脸的"像"必须几何层面）——**调整捏脸参数（320 键）实现形似，是以后的路**。当前仅记为待办。

**方案链**（调研已做，不作废，照抄）：
1. 度量：mediapipe 468 点 → 20-40 维几何比例（眼距/眼宽/鼻宽高/嘴宽/脸宽长/颧宽/下巴尖度/眉距…）——工具已有
2. 键词典：Native skins.xml 的 deform_keys（320 项，名字自描述 + min/max 范围）→ "键名↔几何量"映射表
3. 映射器：归一化几何量 → 各键权重（原型线性；校准集 + LSQ 精调）
4. 输出：`ProduceNumericKeyWithDefaultValues` + `EnforceConstraints`（[BodyGenerator 文档](http://apidoc.bannerlord.com/v/1.3.14/class_tale_worlds_1_1_mount_and_blade_1_1_body_generator.html)）；NPC 用 BodyProperties key 写 XML（sandbox lords.xml/NPCCharacter）
5. 精度预期：原型轮廓形似（细节差 20-40%）；校准闭环后追"像"；**第五条路=第五步"每人面孔+包体不膨胀"正解**（几十字节/人）

**能力确认**：key 可程序化生成/修改（MBBodyProperties API 全通）；社区仅有"捏脸界面 Ctrl+C 抄 key"手工流；无现成照片工具。

### 13.5 当前基线指针（2026-09-02 晚间收尾）

- **已验证**：第一步基线——`AssetPackages/lwn_femhead_copy.tpac`=原封 pack0（32,015,735B）+ XSLT 四件套（skins.xml 空壳/skins.xslt(=原版 0 diff)/ModuleData/project.mbproj(type=skin)/SubModule Xmls 空）——**新档实机：窄鹅蛋 xxFemaleHead 头型 + 5 池随机 + 头型多变 = XSLT 的 face_meta_mesh + deform_keys 链全通**；女性 skin 名=Native `woman`
- **gpt-UV 皮版本**（5 张 narrow 脸，30,559,723B）留档于 `ArtSource/femhead_pack/pack/`，未装机（复原基线时换下）
- **mbproj 位置=ModuleData/（原版实证）**——"必须模块根"是错误推测，勿重犯
- 下一步候选：①gpt 皮换回装机做二段（头型+皮）②照片→捏脸参数（未来）③信长男头（连同②一起）
