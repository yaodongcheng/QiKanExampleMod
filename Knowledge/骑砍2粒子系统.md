# 骑砍 2 粒子系统 —— XML 格式 + three.js 复现

> 来源：Native 1.2.12 反编译 + 41 个粒子材质实测 + 一整套 three.js 复现实践（2026-09-18）。
> 目标问题：**粒子特效怎么写成骑砍认的 XML？怎么在 three.js 里跑同一套规则来预览？**
> 配套：[自定义战斗.md](自定义战斗.md)（战斗动作/判定）、**工具链 `tools/particle-pipeline/`**（生成器 / 预览器 / 批次驱动 / 静帧渲染，2026-09-20 从 `Scripts/` + `Debug/offline/particle_demo/` 整合而来，数据在 `D:\BrainMaker\骑砍2粒子特效复刻\`，两边 junction 连接）。

---

## 0. 结论速览

| 问题 | 结论 |
|---|---|
| 粒子能纯文本写吗？ | ✅ **能**，全 XML，不用开编辑器 |
| 一个 emitter 多大？ | **固定 21 个 flag + 55 个 parameter**（2026-09-21 对原版 6 个已注册文件 **487 个 emitter** 复核：无一例外；唯一例外是 basic 里占位用的 `invalid_particle` 56 参数。死档 `particle_systems2.xml` 是旧格式 17/38，别当模板）→ **必须用生成器写** |
| 特效靠什么区分形态？ | `material`（贴图**且决定混合模式**）+ `emission_rate` / `velocity` / `gravity` / `damping` / 曲线 |
| 能做出"黑烟"吗？ | ✅ **能** —— 换 `modulate` 材质即可（不是叠加，是乘法压暗） |
| three.js 能复现吗？ | ✅ 能。粒子用 `Points` + 自定义 shader；混合模式用 `CustomBlending` 对齐 |
| three.js 预览 = 游戏效果吗？ | ❌ **不是**。它是**运动模拟器**：验证形状/密度/尺度/节奏，不验证最终外观（那取决于游戏 shader） |

---

## 🔴 交接（2026-09-24 收盘 · 粒子资产「离线编译」这条线）

**一句话现状**：`XML → tpac 里的二进制 Particle 资产` 这条路**已打通并验证**（编辑器里打开 `lwn_yinmo_trail`，
5 个 emitter 标签齐、参数正确）；游戏侧包已投 `Taikou/AssetPackages/`、`Spells.xml` 三个值已换回 `lwn_yinmo_*`
—— **只差一次游戏实机确认**。技术细节全在 **§十二**（那里是唯一正文，本节只是入口 + 待办）。

**这一轮做完了什么**

| 件 | 位置 |
|---|---|
| 写侧序列化器（读侧 95 个字段逐个镜像 + 原始字节保真） | `tools/tpactool/TpacTool.Lib/Particle/ParticleEffectData.cs` |
| 段数据校验 | `TpacTool.Lib/Util/XxHash64.cs` —— **段头 8 字节 = `XxHash64(未压缩数据)`** |
| 段 owner / 资产外壳 | `ParticleEffectData` + `AssetItem.CopyShellFrom` + `ExternalLoader.SaveTo`（owner==资产 guid、version+元数据+checksum 从模板抄） |
| 三个命令 | `tpaccli particleimport`（`--xml` / `--split` / `--clone` / `--probe` / `--mkdir`）· `prtdump`（可加 `--csv` 出机器可读全字段）· `prtroundtrip`（写侧闸门） |
| 字段映射表 | §12.4（手工） + §12.4.1（关联求解，405 样本） |
| 坑表（8 条） | §12.5 |
| 自检四件套 | §12.7 |

> 🔴 **另一条线（FCS 粒子复刻）的交接在** [FlexibleCombatSystem施法设计_UE实现分析.md](FlexibleCombatSystem施法设计_UE实现分析.md) **顶部的「🔴 交接（2026-09-25）」段** ——
> 99 个 FCS 特效已自动翻译 + 全部投进 ModKit；根因级缺陷修了 6 条（材质名/图集切法/元素配色/存活上限/尺寸上限/生成器编码崩）；
> **T1 待打通：`particle_size` 没从 UE 翻译**（685 个发射器清一色 0.25±0.25）。
> 本轮新增工具：`preview/sheet_stills.py`（批量分镜）· `Debug/offline/_mat_tex_survey.py`（材质贴图对照图）· `_grid_check.py`（图集切片校验）。

**下一步 TODO（按优先级）**

| # | 谁 | 做什么 | 判据 / 备注 |
|---|---|---|---|
| **T7** | ✅ **通过（2026-09-24 晚）** | 用户在编辑器里给 `lwn_savetest` 新建 emitter `savetest` 并按值表填了 17 个标签 → Save | **30/30 个值全部落盘、一个不少；6 条 emitter（5 旧 + 1 新）都在** ⇒ **编辑器写路径可信，路线 B（在编辑器里造/改粒子资产）复活**。⚠️ 但**编辑器写出来的布局是 `subVersion 3`**（原版 Native 及我们的产物都是 0）—— 详见 §12.8 |
| **T1** | ❌ **作废** | ~~指纹探针~~（探针值由我自造，两次踩非法 —— 见"别再犯" 2） | 这条线**停用**：自造值不可信，改走 T7，映射信息一模一样 |
| **T8** | 我 | 🔴 **读写器升到 `subVersion 3`**（§12.8） | 手法已验：拿同一个拖尾的两份（我们写的 = subVersion 0 / 编辑器存的 = subVersion 3）逐偏移对齐逆出新布局。升完 ⇒ Sphere radius / Radial Velocity / Local Force / Warmup… 这批新字段**能在 XML 里写、离线编译**，蓄力「真·径向内聚」才谈得上。**方向已由实机验收（§12.8 末）：游戏认得新格式，值得投** |
| **T9** | 🔄 进行中（2026-09-24 晚 已跑 2 轮）| 🔴 **照参考效果逐帧对齐**（用户指派）：目标 = `D:\BrainMaker\骑砍2粒子特效复刻\output\yinmo_ref`（从 `阴魔斩.mp4` 抽的参考帧 + flow 分析）。做法：改 spec → `gen_particle_effect.py` → `preview/sheet_stills.py`（本轮新增，一次渲多时刻拼 3×3 分镜）→ **我自己看图**与参考帧比 → 迭代 | 第 1 轮诊断：「太白、太干净、黑烟规模太小」；第 2 轮改完：白核缩小·绯红盘加大·蓄力段补远场暗丝·爆开段压亮壳抬黑烟·残迹段黑烟成规模+亮带加厚（emitter 15→16，校验 0 问题）。**产物**：`tools/particle-pipeline/out/sheet_p3_seq.png`（含替身网格）、`sheet_p2_particles.png`（纯粒子）。**资产已编译**：编辑器件写进 `TaikouAnim/Assets_disabled/particles/`、游戏包投 `Taikou/AssetPackages/lwn_yinmo_prt.tpac` |
| **T2** | 用户 | **游戏实机**：放一发阴魔斩 | 拖尾 / 爆散 / 蓄力是否是我们自己的效果；没出现就 `custom.ninja_report lwn_yinmo_trail` 定性（认得=效果问题；不认=包没加载） |
| **T3** | ✅ **定案（2026-09-24 晚）** | `emit_sphere_radius` | **Sphere radius → subVersion 3 的四连排新槽**（`Sphere / inner / Disc / Disc inner`，§12.8）。旧的 `F28` 猜测作废（那是 subVersion 0 的槽号，新版整体重排过） |
| **T4** | 我 | 蓄力「壳层接力」**换机制** | `activation_delay` 确认不在二进制里（§12.6）⇒ 改用发射窗错开 / C# 分两次 spawn |
| **T5** | 并入 **T8** | ~~`Curve15` / `S3` 定案~~ | 它俩同属"格式版本差异"（`Curve15` ≥1、`S3` ≥2 才在流里）⇒ 升到 subVersion 3 一并解决 |
| **T6** | 用户 | git 清理 | `git rm --cached tools/tpactool/out/lwn_yinmo_prt.tpac`（该目录已进 `.gitignore`） |

**⚠️ 下面这张探针值表已随 T1 停用**（留档：若以后要重新走 slot→label 方向，值照这个表；但**用之前先过 `Emitter.Validate()`**）

| 值 | 字段 | | 值 | 字段 | | 值 | 字段 |
|---|---|---|---|---|---|---|---|
| 11.25 | `F2` | | 17.75 | `F20` | | (31.25, 31.5, 31.75) | `V1` |
| 12.5 | `F19` | | 18.5 | `F21` | | (33.25, 33.5, 33.75) | `V2` |
| 13.75 | `F24` | | 19.25 | `F26` | | (35.25, 35.5, 35.75) | `V4` |
| 14.5 | `F25` | | 20.5 | `F27` | | 41.25 / 41.5 | `Curve1` 两个 float |
| 15.25 | `F29` | | 21.75 | `F31` | | 42.25 | `Curve7.CurveMultiplier` |
| 16.5 | `F3` | | **47.75** | **`F28`（疑似 `emit_sphere_radius`）** | | `lwn_probe_s11` | `S11` |

**四条"别再犯"（都是这一轮栽实了的）**

1. ⚠️ **【2026-09-24 晚更正：原结论已撤，反过来那半仍未验】** 原文写「编辑器按 Save 会把粒子资产写成**空壳**（`lwn_clone_blood1`：658 → 201 字节）」——
   用户当天确认：**那个 emitter 是他自己在编辑器里删掉的**。那个 201 字节文件（dump = 0 emitter / 16 字节桩）**如实记录了他清空后的状态**，不是保存丢数据。
   （`fight_tail` 的 195 字节空壳是另一回事 = 我们工具写侧老 bug，已修，见 §12.5 坑 4。）
   ⇒ **反方向仍未验**：**带数据的保存能不能原样往返**。判据 = **T7 存档测试**。验出「能」= 编辑器可直接造粒子资产（备选路线 B 复活）；
   验出「不能」= 恢复本禁令。**结论出来之前，改效果仍走**「改 spec → 重跑 XML → `particleimport` 重编译」。
2. 🔴 **别往未知字段填越界值 —— int 当枚举看、向量第 4 位永远是 1.0**（这一类栽了两次，都是"我随手填的数"炸编辑器）：
   · **int**（`U2~U8` / `I1~I5`）= 枚举/索引 —— 填 22~30 这种值 → ModKit 直接起不来
     （`rglAsset_package.h:589 Unable to serialize int value`，§12.5 坑 8）；
   · **`Vector4` 槽**（`V1/V2/V3/V4/V5/gravity/V7/fixedBillboardDir`）引擎按 **`rglVec3`** 读写 —— **w 必须 1.0**。
     给它填 32/34/36（想把 w 也当探针数字用）→ 编辑器一打开就断言崩：
     `rglBuffer.cpp:899  nearly_equals(vector->w, 1.0f)  "Potential read/write miss match for rglVec3"`（§12.5 坑 9）。
   ⇒ **写盘前过 `Emitter.Validate()`**（2026-09-24 已加闸门，`TpacTool.Lib/Particle/ParticleEffectData.cs`）；
   **更省事的路 = 让编辑器自己写**（T7）—— 编辑器写出来的必定是引擎认的值。
3. 🔴 **写模块资产前先解析两态目录**（`Assets` / `Assets_disabled` 是同一文件夹的两个名字，只能存在一个）。
   工具现在：目录不存在 → `[STOP]`；两态并存 → `[WARN]`（⚠️ 产物常写在 `<模块>/Assets/particles/`，检查要**回溯祖先**）。
4. 🔴 **数据段三件必须齐** —— owner guid == 资产 guid、`XxHash64` 校验、资产外壳从模板抄。
   **缺任何一个的症状都是同一个**："名字在、emitter 全空"（这也是排查这类问题时的第一顺位怀疑）。

---

## 一、XML 格式

### 1.1 文件结构

```xml
<particle_effects>
  <effect name="lwn_yinmo_charge" guid="{GUID}">
    <emitters>
      <emitter name="core" _index_="0">
        <flags>      …21 个 <flag name= value=/>…      </flags>
        <parameters> …55 个 <parameter …/>…            </parameters>
      </emitter>
    </emitters>
  </effect>
</particle_effects>
```

- 一个文件可放**多个 `<effect>`**（原版 `particle_systems_general.xml` 放了 27 个）
- 一个 effect = **一个发射原点**。多段式技能（蓄力→爆开→飞行）原点不同 → **必须拆成多个 effect**，由代码按时间线依次 Spawn
  - 引擎有 `activation_delay`，但那只延迟**同一原点**的 emitter，跨原点做不到

### 1.2 参数的四种类型（看属性就知道是哪种）

| 类型 | 写法 | 语义 |
|---|---|---|
| **常量** | `<parameter name="gravity" value="0,0,-1" />` | 固定值 |
| **随机** | `<parameter name="particle_life" base="1.3" bias="0.4" />` | 每颗粒子在 `base ± bias` 内随机 |
| **曲线** | `<parameter name="particle_size" base=".25" bias=".25"><curve …/></parameter>` | `base ± bias` **再叠一条生命曲线** |
| **颜色** | `<parameter name="particle_color"><color><keys/></color><alpha><keys/></alpha></parameter>` | 颜色 + 透明度各一条曲线 |

### 1.3 曲线（`<curve>` / `<keys>` / `<key>`）

```xml
<curve name="particle_life" version="1" default="1.000" curve_multiplier="0.300">
  <keys>
    <key time="0.000" value="1.000" tangent="0.193, 0.000" />
    <key time="1.000" value="0.667" tangent="-0.193, 0.000" />
  </keys>
</curve>
```

- `name` = **归一化基准**：`particle_life`（按粒子一生 0→1）或 `emitter_life`（按发射器一生）
- `time` ∈ [0,1]，`value` = 该时刻的输出，`tangent` = 切线（引擎用 Hermite 插值）
- `curve_multiplier` = 曲线输出再乘的系数
- `default` = 曲线缺省时的取值
- 尺寸走加法还是乘法看 `particle_size_curve_op`（`add` 624 处 / `multiply` 188 处）

### 1.4 完整字段表

**21 个 flag**（全部 `name= value="true|false"`）：
`emit_while_moving` / `dont_emit_while_moving` / `emit_at_once` / `local_emit_dir` / `loop_sprite` /
`uses_sprite_animation` / `scale_with_respect_to_emitter_velocity` / `skew_with_respect_to_particle_velocity` /
`spherical_normals` / `emit_on_terrain` / `select_random_sample_mesh` / `fixed_billboard_direction` /
`order_by_distance` / `select_random_sprite` / `create_decal_on_collision` / `create_decal_only_once` /
`randomize_collision_decal_rotation` / `permanent_collision_decals` / `collide_with_objects` /
`use_color_from_terrain` / `enable_collision`

**55 个 parameter**（**顺序即原版顺序，不要重排** —— 保持同序才能让 diff 只显示真正改动的行）。常用的：

| 参数 | 作用 |
|---|---|
| `emission_rate` | 每秒发射数（`emit_at_once=true` 时忽略） |
| `particle_life` / `emitter_life` | 粒子/发射器寿命（秒） |
| `emit_sphere_radius` + `emit_volume_type` | 发射体：球半径 / 盒体积。moduleData 里只出现过 `box`(106) 与 `sphere`，但**引擎还认 `disc`**（`Native/Prefabs/*.xml` 3 处写了 `emit_volume_type="disc"` + `emit_disc_radius`）⇒ 「只有两种」**不成立**（2026-09-22 更正） |
| `emit_velocity_x/y/z` | 初速分量（各向同性就三个填一样） |
| `gravity` | 重力向量 `x,y,z` |
| `damping` / `angular_damping` | 速度/角速度阻尼（每秒衰减） |
| `particle_size` | 尺寸（base±bias + 曲线） |
| `material` | **贴图 + 混合模式**（见 §二） |
| `inherit_emitter_velocity` | 粒子继承发射器速度的比例（**0~1，做尾迹的关键**） |
| `billboard_type` | `3d`(525) / `turn_to_velocity_side`(247) / `2d`(36) / `none`(4) |
| `texture_sprite_count` | 🔴 **图集行列数**（`"2, 2"` = 贴图里 4 个精灵，每颗随机取一格） |
| `max_alive_particle_count` | 上限（**整数**，别写成小数） |
| `particle_color` | 颜色 + 透明度曲线 |
| `emissive_multiplier` / `diffuse_multiplier` | 自发光/漫反射倍率（发光类调高） |
| `emission_turbulence_*` | 发射湍流（抖动） |
| `sample_mesh` + `select_random_sample_mesh` | **从任意网格表面发射** → 做球壳、月牙形粒子带的正解 |

### 1.5 注册（内容包怎么加自己的粒子）

`ModuleData/project.mbproj` 加一行（**走 soln 体系，不在 SubModule 的 XmlNode 体系里**）：

```xml
<file id="soln_particle_systems" name="ModuleData/particle_systems_mine.xml" type="particle_system" />
<!-- GPU 粒子用 type="gpu_particle_system" -->
```

⚠️ 同 `module_sounds` / `item_holsters` / `skins` 一样：**mbproj 不挂那一行 = 文件完全不加载**（这个坑项目踩过三次）。

#### 🔴 但 XML 不是唯一的路 —— tpac 里的粒子资产也是活的（2026-09-23 实证）

**引擎的粒子来源 = 注册的 XML ∪ AssetPackages 里的 `Particle` 资产。**

| 路径 | 做法 | 实证 |
|---|---|---|
| ① XML 注册（本节上面写的） | `particle_systems_*.xml` + mbproj `soln_particle_systems` | 原版 Native 挂了 **7 行**，218 个活体 effect |
| ② **tpac 粒子资产** | 粒子作为 `Particle` 资产编译进 `AssetPackages/*.tpac` | 🔴 **HikageRising**：**一个粒子 XML 都没有**，122 个粒子只以 tpac 资产存在，且确实生效 |

证据细节 + 对 ModKit 的影响，见 [HikageRising_忍者体系技术实现分析.md](HikageRising_忍者体系技术实现分析.md) **附录 C.7**。
一句话：**判断"某个 mod 有没有自定义粒子"不能只看粒子 XML，AssetPackages 里也要查**（HikageRising 就是没有 XML 的那种）。

🛠️ 工具侧：`tpaccli list --packdir <包> ` 能按类型 GUID 列出粒子资产（**Particle 的 GUID = `6de14d67-dd9a-45be-9463-0281c3d8dd51`**）；`tpaccli` **目前没有粒子导出命令**（库能读、没有出口）。

#### 🔴🔴 实机更正（2026-09-24）：内容包走 XML 注册**进不了游戏**，必须发布成包

我们自己那份粒子 XML（Taikou 上：文件在、mbproj 第 8 行 `soln_particle_systems` 在、启动参数里有 Taikou、引擎零报错）
实机结果是**运行期查不到名字**（`ParticleSystemManager.GetRuntimeIdByName` 返回 -1，LWN 的 `[Spell]` 日志会打"粒子查不到"）
—— 上面那条「∪」**只对编辑器成立**（原版 Native 的 XML 与它的 tpac 同时存在，所以看不出是哪边在起作用）。

| 判据 | 实测 |
|---|---|
| `tpaccli dump` 按名字捞 `psys_game_burning_jar_trail`（原版包） | 打出 **`META Particle`** ⇒ **运行期这份表来自包里的 `Particle` 资产**。⚠️ 它是**二进制**形态，拿 `<emitter` 文本去 grep tpac 搜不到，**别再用那个判据** |
| 同名对照 `waterfall_splash` | 包里只有 `material` + `texture`（没有 Particle）⇒ 引擎就报 `Unable to find particle system with name waterfall_splash` —— 与症状逐字吻合 |
| `particle_systems2.xml`（**没挂** mbproj） | 它的效果照样能在运行时用 ⇒ mbproj 不是运行期的登记处 |
| 外部反证 HikageRising | 没有粒子 XML、122 个粒子全是 tpac 资产、确实生效（§C.7） |

⇒ **给内容包加粒子的完整链路** = ① 写 XML + mbproj 注册（**这一步只服务于编辑器**）→ ② 编辑器里 **Publish** 发布成粒子包 →
③ 产物**改名**拷进内容包的 `AssetPackages/`（别覆盖内容包自有的 `pack0.tpac`）→ ④ 数据里才敢引用效果名。
本工程的做法：XML 挂在 `TaikouAnim` 沙箱上做前两步（Taikou 依赖太多、打不开 ModKit）。
详见 [../plans/法术体系-通用施法框架.md](../plans/法术体系-通用施法框架.md) §B。

> ✅ **同日晚已解决**：改成**纯离线**造资产（不打开编辑器）—— `tpaccli particleimport` 把我们的 XML 直接编成
> tpac 里的 `Particle` 资产。**完整链路 + 字段映射 + 七个坑见本节 §十二**。

### 1.6 原版规模（1.2.12）

| 文件 | effect 数 | 注册 |
|---|---|---|
| `particle_systems_hardcoded_misc1.xml` | 96 | ✓（`psys_*` 名字在这） |
| `particle_systems_hardcoded_misc2.xml` | 76 | ✓ |
| `particle_systems_general.xml` | 27 | ✓ |
| `particle_systems_basic.xml` | 11 | ✓ |
| `particle_systems_outdoor.xml` | 6 | ✓ |
| `particle_systems_map_icon.xml` | 1 | ✓ |
| `gpu_particle_systems.xml` | 1 | ✓（`gpu_particle_system`） |
| `particle_systems2.xml` | 141 | ✗ **未注册，死档** |
| `particle_systems_old.xml` | 144 | ✗ **未注册，死档** |

**活体 = 218 个 effect。**

### 1.7 🔴 编辑器面板上的每一项 = XML 里的哪个键（2026-09-23 实测）

**结论**：ModKit 粒子编辑器面板上能看到的每一项，**几乎都直接对应 XML 里的一个 flag 或一个 parameter**。
编辑器保存时**把仍是默认值的项省略不写** —— 所以一个 emitter 名义上「21 flag + 55 param」，
XML 里通常只写其中十几行，剩下的靠引擎默认值兜底。**改粒子 = 改这两张表，没有第三种存法。**

**实测方法**（可复现）：拿 `psys_game_blood_1` 的 5 张面板截图 + `psys_game_blood_4` 的 4 个发射器截图，
跟 XML 逐字段对值 —— 表里标 ✅ 的都是「截图显示值 == XML 存的值」。

**顺手证实了 §1.6 那条死档结论**（编辑器侧的第二重证据）：
截图里的 `psys_game_blood_4` 有 4 个发射器，名字是
`directional_splash / splatter / blood_dust / splatter_forward` —— 这四个名字**只在注册过的
`particle_systems_hardcoded_misc1.xml` 里存在**（死档 `particle_systems2.xml` 里那份是
`Emitter / New emitter / blood_dust`）。再拿一个数复核：`blood_dust` 的 skew 系数两份分别是
**200**（misc1）与 **17**（psys2），编辑器显示 **0.400** = 200 ÷ 500 ⇒ **编辑器读的就是 misc1**。
⇒ **编辑器也只认注册过的那几个文件**，死档连编辑器都不看。

#### 对照表（分区与面板顺序一致）

**Emission / Spawner Behavior**

| 编辑器标签 | XML 键 | 截图实测 |
|---|---|---|
| Emission rate | `emission_rate`（base / bias 两格） | ✅ 10.00 / 0.00 |
| Emit at once | flag `emit_at_once` | ✅ 勾选 = `true` |
| Emit while moving | flag `emit_while_moving` | ✅ 不勾 = `false` |
| Don't emit while moving | flag `dont_emit_while_moving` | ✅ 不勾 = `false` |
| Emit while moving speed threshold | ⚪ 无实例 | 显 0.000 |
| Emitter life | `emitter_life` | ✅ 0.000 |
| Activation delay | `activation_delay` | ✅ 0.00 / 0.00 |
| Max alive particle count | `max_alive_particle_count`（**整数**） | ✅ 0 |
| Emission loop / Warmup time | ⚪ 无实例 | 显 1.000 / 0.000 |

**Spawner Shape**

| 编辑器标签 | XML 键 | 截图实测 |
|---|---|---|
| Emit volume type | `emit_volume_type` | ✅ `box`（取值还有 `sphere`、引擎另认 `disc`） |
| Emit box size X/Y/Z | `emit_volume`（三个数） | ✅ 0.00 0.00 0.00 |
| Sphere radius | `emit_sphere_radius` | ✅ 1.000 |
| Sphere inner radius / Disc radius / Disc inner radius | ⚪ 无实例（引擎字段名 `emit_sphere_radius_inner` / `emit_disc_radius`） | 显 0.000 / 1.000 / 0.000 |
| Emission offset / Emission rotation | ⚪ 无实例 | 显 0 |
| Emit on terrain | flag `emit_on_terrain` | ✅ 不勾 |

**Initial velocity**

| 编辑器标签 | XML 键 | 截图实测 |
|---|---|---|
| Emit Velocity Model | `emission_velocity_model` | ✅ `random_velocity_components` |
| Cone Angle | `cone_emit_angle` | ✅ 0.000 |
| Emission Velocity（上排两格） | `cone_emit_velocity`（base / bias） | ✅ 1.00 / 0.00 |
| Emission Velocity Base（下排 X/Y/Z） | `emit_velocity_x` / `_y` / `_z` 的 **base** | ✅ 2.50 / 0.00 / 2.00 |
| Emission Velocity Bias（下排 X/Y/Z） | 同上三个键的 **bias** | ✅ 0.10 / 0.20 / 0.40 |
| Radial Velocity | `radial_velocity`（引擎字段，XML 无实例） | 显 0.00 / 0.00 |
| Emission speed limit | `emission_speed_limit` | ✅ 0.000 |
| Fixed particle initial speed | `fixed_particle_initial_speed` | ✅ 0.000 |
| Rotation speed | `emit_rotation_speed`（base / bias） | ✅ 0.00 / 286.48（存的是 286.479，**面板按两位小数显示**） |
| Radial initial rotation speed | ⚪ 无实例 | 不勾 |
| Local emit direction | flag `local_emit_dir` | ✅ 不勾 |
| Inherit emitter's velocity | `inherit_emitter_velocity` | ✅ 0.000 |
| Particle life | `particle_life`（base / bias） | ✅ 1.00 / 0.10 |
| Initial rotation | `initial_rotation`（base / bias） | ✅ 0.00 / 180.00 |

**Movement / Force**：Gravity `gravity` ✅ `0,0,-9.8` · Local Force ⚪ 无实例（引擎名 `local_force`）·
Wind effect `wind_effect`（base/bias）✅ 1.00 / 0.00 · Vector field `vector_field`（None）·
Damping `damping` ✅ · Turbulence strength `turbulence_strength` ✅ · Angular damping `angular_damping` ✅ 0.60 / 0.00 · Local simulation ⚪

**Collision / Decal**

| 编辑器标签 | XML 键 | 截图实测 |
|---|---|---|
| Enable | flag `enable_collision` | ✅ 不勾 |
| Collide with objects | flag `collide_with_objects` | ✅ 不勾 |
| Collision behavior | `collision_behaviour` | ✅ `normal` |
| Damping / Angular damping（碰撞区） | `collision_damping` / `collision_angular_damping` | ✅ 0.000 |
| Create decal on collision | flag `create_decal_on_collision` | ✅ 不勾 |
| Create decal on each collision | flag `create_decal_only_once`（**名字是反的，别按字面理解**） | ✅ 不勾 |
| Start index / End index | `skinned_decal_start_index` / `skinned_decal_end_index` | ✅ -1.000（**整数 -1 = 不用**） |
| Min scale / Max scale X,Y | `decal_min_scale` / `decal_max_scale` | ✅ 1.00 |
| Randomize rotation | flag `randomize_collision_decal_rotation` | ✅ 不勾 |
| Permanent decal | flag `permanent_collision_decals` | ✅ 不勾 |
| Decal Material 0…N | `decal_material`（**可多行**，原版 77 处在用） | 显 None |

**Visual / Orientation / Size / Render**

| 编辑器标签 | XML 键 | 截图实测 |
|---|---|---|
| Billboard type | `billboard_type` | ✅ `turn_to_velocity_side`（还有 `3d` / `2d` / `none`） |
| Use fixed billboard direction | flag `fixed_billboard_direction` | ✅ 不勾 |
| Fixed billboard direction | `fixed_billboard_direction`（**同名的参数**，存的是向量） | ✅ 0,0,1.00 |
| Quad scale / Quad bias | `quad_scale` / `quad_bias` | ✅ 1.00 / 0.00 |
| Particle size | `particle_size`（base / bias） | ✅ 0.15 / 0.15 |
| Particle size curve operation | `particle_size_curve_op` | ✅ `add`（另一种是 `multiply`） |
| Scale with respect to emitter velocity | flag `scale_with_respect_to_emitter_velocity` | ✅ 不勾 |
| Scale emitter velocity multiplier | `scale_with_emitter_velocity_coef` | ✅ 0.000 |
| Skew with respect to particle velocity | flag `skew_with_respect_to_particle_velocity` | ✅ 勾（原版最常用 flag，467 次） |
| Skew particle velocity multiplier | `skew_with_particle_velocity_coef` | ⚠️ **显示值 = 存值 ÷ 500**（见下） |
| Skew particle velocity limit | `skew_with_particle_velocity_limit` | ✅ 0.000 |
| Material | `material` | ✅ `prt_shd_blood_3`（**同时决定贴图与混合模式**，见 §二） |
| Particle color 渐变色条 | `particle_color` 的 `<color>` / `<alpha>` 关键帧 | ✅ |
| Color gradient behavior | `color_gradient_behavior` | ✅ `color_over_time` |
| Use color from terrain | flag `use_color_from_terrain` | ✅ 不勾 |
| Use exposure compensation / Exposure compensation | ⚪ 无实例 | 不勾 |
| Use spherical normals | flag `spherical_normals` | ✅ 不勾 |
| Enable point light shading / Enable sky occlusion / Soften alpha with water / Use specular | ⚪ 无实例 | 不勾 |
| Order by view distance | flag `order_by_distance` | ✅ 不勾 |
| Backlight / Diffuse / Emissive / Heatmap multiplier | `backlight_multiplier` / `diffuse_multiplier` / `emissive_multiplier` / `heatmap_multiplier` | ✅ 0.000 / 1.000 ×3 |
| Fadeout distance / Fadeout coefficient | `fadeout_distance` / `fadeout_coef` | ✅ 0.005 / 0.000 |
| Camera distance fadeout near coefficient | `camera_fadeout_coef` | ✅ 0.000 |
| Camera distance fadeout far coefficient | `camera_fadeout_far_coef`（Prefabs 里有实例） | 显 0.000 |
| Use Normal Vector As Alpha / …Coef | ⚪ 无实例 | 不勾 / 0.000 |
| Sample mesh / Random sample mesh | `sample_mesh` / flag `select_random_sample_mesh` | ✅ None / 不勾 |

**Texture sprite**：Type `animated` = flag `uses_sprite_animation` ✅ · Loop = flag `loop_sprite` ✅ 不勾 ·
Sprite count x / y = `texture_sprite_count`（一个键两个数）✅ 8 / 8 · Frame count = `texture_sprite_frame_count` ✅ 64 ·
Frame rate = `texture_sprite_frame_rate` ✅ 64.000 · Blend sprites / Random sprite ⚪ 无实例

**Sound**：Continuous sound / One shot sound / Size parameter ⚪ 全无实例（**音效不在粒子 XML 里**）

#### ⚠️ 全表唯一一个「显示值 ≠ 存储值」的字段：Skew 倍率

编辑器的 **Skew particle velocity multiplier** 显示的是 **XML 存值 ÷ 500**：

| 特效 | XML 里 `skew_with_particle_velocity_coef` | 编辑器显示 |
|---|---|---|
| `psys_game_blood_1` | 8.000 | **0.016** |
| `psys_game_blood_4/blood_dust` | 200.000 | **0.400** |
| （对照）死档 psys2 的同名发射器 | 17.000 | 若被读会显示 0.034 |

⇒ **写 XML 时用原值，别把面板上那个小数抄进去。**（为什么差 500 倍没查 —— 换算发生在 native 里。）

#### ⚪ 那一栏怎么理解（编辑器有、XML 里一个实例都没有）

面板上这一批字段，**引擎的结构体里确实有**（`TaleWorlds.Native.dll` 的参数字符串池能查到名字：
`radial_velocity` / `emit_sphere_radius_inner` / `emit_disc_radius` / `emit_box_size` / `local_force` / `warmup_time`…），
但**原版 491 个活体 emitter 清一色只写 21 flag + 55 param**，从没写过第 56 个 —— 所以：

- **要么**是「值是默认值 ⇒ 编辑器省略没写」；
- **要么**是「这些键写在 emitter 里引擎根本不读」（写在 `Native/Prefabs/*.xml` 的 `<emitter_overrides>` 里倒是有人用）。

**这条缝只能实测**（探针 `examples/probe_radial_spec.py` 就是干这个的）。
🔴 对蓄力段的直接影响：面板上那个 **Radial Velocity** 若真能被读到，「黑气向心聚拢」就能从
§10.3 的「壳层塌缩近似」换成**真·径向内飞**。

#### 1.7.1 「不写这个键会怎样」—— 默认值（2026-09-24 补）

🔴 **三个概念别混**（问到「默认值是多少」时先分清问的是哪个）：

| 概念 | 是什么 | 权威来源 |
|---|---|---|
| **引擎默认** | XML 里**根本不写**这个键时，引擎自己用的值 | 只能在编辑器里读：**键不在文件里时，面板显示的就是它** |
| **生成器模板值** | `gen_particle_effect.py` 的 `DEFAULT_FLAGS` / `DEFAULT_PARAMS` | `tools/particle-pipeline/gen_particle_effect.py`（下表是它的**只读快照**，以生成器为准） |
| **原版常见值** | 全库统计（决定「这个 flag 要不要开」时看它） | §4.2 那张统计表 |

⚠️ **生成器模板值 ≠ 引擎默认**：它逐字取自原版成品件 `prt_hit_dust`，里面 `damping 0.733`、
`particle_life 1.3±0.4`、`particle_size 0.25`、`material prt_shd_smoke_1` 这些明显是**调出来的**，不是引擎默认。
反过来 `emission_turbulence_interval = 3.4e38`（FLT_MAX）一眼就是引擎默认哨兵。**引用前先想清楚要哪个。**

**21 个 flag 的模板值**（未列出的都是 `false`）：`local_emit_dir` · `spherical_normals` · `select_random_sprite` = **true**。

**55 个 parameter 的模板值**（**顺序即原版顺序**，生成器按此顺序写出）：

| XML 字段 | 模板值 | | XML 字段 | 模板值 |
|---|---|---|---|---|
| `emitter_life` | 0.000 | | `emission_turbulence_interval` | 340282346638528859811704183484516925440.000（FLT_MAX） |
| `activation_delay` | 0.000 | | `emission_turbulence_strength` | 0.000 |
| `skew_with_particle_velocity_coef` | 10.000 | | `collision_damping` | 0.000 |
| `skew_with_particle_velocity_limit` | 0.000 | | `collision_angular_damping` | 0.000 |
| `scale_with_emitter_velocity_coef` | 0.000 | | `cone_emit_angle` | 0.000 |
| `inherit_emitter_velocity` | 0.000 | | `particle_size_curve_op` | `add` |
| `emit_volume` | 0.000, 0.000, 0.000 | | `emit_volume_type` | `box` |
| `emit_sphere_radius` | 1.000 | | `billboard_type` | `3d` |
| `gravity` | 0.000, 0.000, -1.000 | | `collision_behaviour` | `normal` |
| `fixed_billboard_direction` | 0.000, 0.000, 1.000 | | `emission_velocity_model` | `random_velocity_components` |
| `emission_speed_limit` | 0.000 | | `initial_rotation` | base 0.000 / bias 180.000 |
| `decal_min_scale` | 1.000, 1.000 | | `damping` | base 0.733 / bias 0.000 |
| `decal_max_scale` | 1.000, 1.000 | | `turbulence_strength` | base 0.000 / bias 0.000 |
| `skinned_decal_start_index` | -1 | | `angular_damping` | base 0.599 / bias 0.000 |
| `skinned_decal_end_index` | -1 | | `particle_life` | base 1.300 / bias 0.400 |
| `max_alive_particle_count` | 0 | | `emission_rate` | base 5.000 / bias 0.000 |
| `quad_scale` | 1.000, 1.000 | | `cone_emit_velocity` | base 1.000 / bias 0.000 + 曲线（emitter_life，mult 1.0） |
| `quad_bias` | 0.000, 0.000 | | `emit_velocity_x` | base 1.000 / bias 1.000 |
| `texture_sprite_count` | 2, 2 | | `emit_velocity_y` | base 0.000 / bias 1.000 |
| `texture_sprite_frame_count` | 1 | | `emit_velocity_z` | base 1.000 / bias 1.000 |
| `texture_sprite_frame_rate` | 1.000 | | `emit_rotation_speed` | base 0.000 / bias 171.887 |
| `fixed_particle_initial_speed` | 0.000 | | `wind_effect` | base 1.000 / bias 0.000 + 曲线（particle_life，mult 1.0） |
| `fadeout_distance` | 1.000 | | `particle_size` | base 0.250 / bias 0.250 + 曲线 1.0→0.667，mult 0.300 |
| `fadeout_coef` | 0.000 | | `material` | `prt_shd_smoke_1` |
| `camera_fadeout_coef` | 0.000 | | `sample_mesh` | （空） |
| `backlight_multiplier` | 0.000 | | `particle_color` | color 0.1→(0.307,0.255,0.220)、0.9→(0.349,0.284,0.231)；alpha 0→0、0.3→0.392、0.7→0.392、1→0 |
| `diffuse_multiplier` | 1.000 | | | |
| `emissive_multiplier` | 1.000 | | | |
| `heatmap_multiplier` | 1.000 | | | |

**已知的「引擎默认」**（来源 = 血包截图里**这些键不在 XML 中**时面板显示的值 —— 见 §1.7 的 ⚪ 标记）：

| 面板字段 | 引擎默认 | 对应 XML 键（名字来自 `Native.dll` 字符串池，**能否被读到仍未实测**） |
|---|---|---|
| Emit while moving speed threshold | 0.000 | ? |
| Emission loop | 1.000 | ? |
| Warmup time | 0.000 | `warmup_time` |
| Sphere inner radius | 0.000 | `emit_sphere_radius_inner` |
| Disc radius / Disc inner radius | 1.000 / 0.000 | `emit_disc_radius` / ? |
| Emission offset X/Y/Z · Emission rotation X/Y/Z | 0 / 0 / 0 | ? |
| Radial Velocity Base / Bias | 0.00 / 0.00 | `radial_velocity` |
| Local Force X/Y/Z | 0 / 0 / 0 | `local_force` |
| Camera distance fadeout far coefficient | 0.000 | `camera_fadeout_far_coef` |
| Use exposure compensation（及下面那格） | 不勾 / 灰 | ? |
| Enable point light shading · Enable sky occlusion · Soften alpha with water · Use specular | 都不勾 | ? |
| Use Normal Vector As Alpha / …Coef | 不勾 / 0.000 | ? |
| Blend sprites / Random sprite | 不勾 / 0.00 / 0.00 | ? |
| Continuous sound · One shot sound · Size parameter | 空 / 空 / 1.000 | **音效不在粒子 XML 里**（另一套管线） |

> 🔴 **55 个参数一个都读不到引擎默认** —— 因为血包那两份 XML **把 55 个参数全写满了**，编辑器没有"缺键"可显示。
> **要一次拿全引擎默认值，唯一干净的办法**：在 Particle Editor 里 **Add new** 一个空粒子 ——
> 它面板上显示的**全是引擎默认**，按 §1.7 那五个分区各截一张图（跟上次一样的 5 张）即可补满本表。

---

## 二、🔴 材质决定混合模式（最容易翻车的一条）

`material` 不只是贴图 —— **它同时决定这颗粒子怎么和背景合成**。2026-09-18 对 Native 41 个 `prt_shd_*` 材质全量实测：

| blend | 数量 | 代表材质 | 效果 |
|---|---|---|---|
| `add_modulate_combined` | 28 | `prt_shd_smoke_1` | 叠加 + 调制（发光） |
| **`modulate`** | **9** | **`prt_shd_haze_1`**（同为 smoke_d 贴图） | **乘法压暗 → 能出黑烟** |
| `add_alpha` | 4 | `prt_shd_steam_1` | 常规半透明 |

**同一个 `smoke_d` 贴图，换材质名就从「发光」变「黑烟」。** 不用自制材质。

`modulate` 族九个：`prt_shd_dust_1` / `prt_shd_fire_haze_1` / `prt_shd_fire_nm` / `prt_shd_haze_1` /
`prt_shd_rain_mud_1` / `prt_shd_rose_a` / `prt_shd_snow_dust_1` / `prt_shd_waterfall_dust` / `prt_shd_water_wave_1`

查看某材质的混合模式：
```bash
tpaccli dump --packdir <Native/AssetPackages> --filter prt_shd --format png --out <dir>
# 然后看 dumps 出的 *.mat.txt 里的 "blend = ..." 一行
```

---

## 三、three.js 复现

### 3.1 解析（`preview.template.html`）

`DOMParser` 按 `application/xml` 解析 → 遍历 `particle_effects > effect > emitters > emitter` →
按 §1.2 的四种类型分流成 `num` / `rnd` / `curves` / `color`+`alpha`。

曲线取样用 **smoothstep 近似**（引擎用切线 Hermite，视觉差别很小但不是逐位一致 —— 预览够用）。

### 3.2 模拟（每帧）

```
发射:   acc += emission_rate * dt
        当 acc >= 1 且 存活数 < maxAlive → 生成一颗粒子
生成:   位置 = 原点 + 球/盒体积内随机
        速度 = emit_velocity(±bias) + 发射器速度 × inherit
        寿命 = particle_life(base ± bias)
更新:   v *= exp(-damping*dt);  v += gravity*dt;  p += v*dt;  age += dt
        死亡 → 移除
```

**发射器速度** = 挂点（骨骼/飞行物）每帧的位移差 ÷ dt。`inherit_emitter_velocity` 就是粒子继承它的比例 —— **做尾迹的关键旋钮**（见 §四 坑 5）。

### 3.3 渲染：`THREE.Points` + 自定义 shader

```glsl
// vertex
attribute float aSize; attribute float aAlpha; attribute vec3 aColor;
uniform float uScale;                       // = (画布高/2) / tan(fov/2)
void main(){
  vec4 mv = modelViewMatrix * vec4(position, 1.0);
  gl_PointSize = max(1.0, aSize * uScale / max(0.001, -mv.z));   // aSize 是世界尺寸(米)
  gl_Position = projectionMatrix * mv;
}
// fragment
uniform sampler2D uTex;
void main(){ vec4 t = texture2D(uTex, gl_PointCoord);
  if (t.a < 0.004) discard;
  gl_FragColor = vec4(vColor, t.a * vAlpha); }
```

🔴 `uScale` 必须按画布高度算，否则粒径差 100 倍（踩过）。`aSize` 传**世界米数**，不要再乘系数。

**贴图**：`smoke_d` 是 **2×2 图集**（`texture_sprite_count="2, 2"`）—— 必须按格切，每颗随机取一格。整张当单颗用 = 画出一坨多瓣的灰块（踩过）。

### 3.4 混合模式映射（关键）

| XML 的 blend | three.js |
|---|---|
| `add` | `AdditiveBlending` |
| `alpha` | `NormalBlending` |
| **`modulate`** | `CustomBlending` + `blendSrc: ZERO, blendDst: SRC_COLOR`，片元输出 **`mix(vec3(1.0), color, a)`**（a=0 输出白=乘 1 不变；a=1 输出颜色=压暗） |
| `add_modulate_combined` | 近似 = **常规 alpha 混合 + 一份加法增益**（`region = region*(1-a) + color*a*1.55`） |

🔴 **`add_modulate_combined` 不能当成纯加法做**：纯加法在**亮背景**上几乎不可见（加法只能变亮，亮底已经亮了）——踩过，爆开阶段整段空白。

### 3.5 泛光后处理（决定"像不像游戏"）

three.js r128 的 UMD 包**不含 `EffectComposer`**，自己写三趟：

```
场景渲到离屏 RT
  → 亮部提取（阈值 ~0.86）
  → 三级降采样高斯模糊（1/2、1/4、1/8）
  → 加法合成 + 软性色调映射  c/(c+0.95)*1.22
```

参数经验：**阈值低了整个画面都在发光**（0.62 时地面全白过曝），**强度高了糊成一片**。起步用 `threshold 0.86 / strength 0.30`。

### 3.6 替身网格（XML 表达不了的）

粒子做不出来的几样，用 mesh + shader 代演，**并在文档里标明"实机需要网格/shader"**：

| 元素 | 做法 |
|---|---|
| 体积球壳 | 球体 mesh + 菲涅尔边缘光 + 经线流动 + 3D 噪声 |
| 暗色日冕 | 🔴 **朝向相机的径向渐变贴片**（不是球！球+菲涅尔会变成吞掉本体的灰球） |
| 月牙弧 | 环形扇几何 + 沿弧渐变 + 跨厚度软边 |
| 人形 | 圆柱/球的剪影，**只提供尺度感**（线宽要做投影换算，否则 1.8m 的人只剩 2px） |

---

## 四、坑点清单（全是本次踩出来的）

| # | 坑 | 症状 | 正解 |
|---|---|---|---|
| 1 | `texture_sprite_count` 是图集 | 多瓣灰块 | 按 `cols×rows` 切格，`select_random_sprite` 每颗随机取一格 |
| 2 | 纯加法在亮背景不可见 | 亮底上特效凭空消失 | 用 `add_modulate_combined` 的语义（alpha + 加法增益），别只做加法 |
| 3 | `modulate` 是乘法**连乘** | 多颗粒子重叠 → 饱和成死黑 | 压暗类 **alpha 峰值压到 0.4 上下**（发光类可到 0.9） |
| 4 | 尺度失控 | 烟团比人还高 | **以人形 1.8m 为基准**定 `particle_size`：核心≈头大(0.35m)、烟团≈半人高(0.7m)、球壳半径≈两人高(3.7m) |
| 5 | `inherit_emitter_velocity` 给太低 | 尾迹跟不上主体，堆成原地一坨 | 尾迹类给 **0.85~1.0** + 低阻尼(0.1~0.3) → 粒子跟着飞再慢慢落后 = 拉出痕迹 |
| 6 | 给剑气加重力/抛物线 | 像块石头飞出去 | **剑气/能量类零重力 + 直线弹道** |
| 7 | 几何体朝向 | 平面月牙侧对镜头 = 一条线 | 躺在 XY 平面的片**不要 `lookAt` 飞行方向**（那会让法线平行于视线）；要`lookAt` 就 `lookAt` 相机 |
| 8 | 相机跟飞行物太紧 | 特效整段飞出画面（投影坐标 1481 而画布才 1100 宽） | **固定取景**（人+特效同框），或大幅缩小飞行距离 |
| 9 | 过曝 | 整幅发白、主体被冲没 | 环境色别用近白；色调映射只压缩不提亮；bloom 阈值抬高 |
| 10 | `max_alive_particle_count` 写成小数 | 引擎按 int 解析可能失败 | 整数参数一律整数字面量 |
| 11 | 参数顺序被重排 | diff 全是噪音 | 生成器**按原版顺序**输出 |

---

## 五、自检闭环（能自己看图，效率差一个量级）

**没有截图通道时改这种视觉东西等于盲改。** 两条路：

1. **离屏静帧渲染** —— `tools/particle-pipeline/preview/render_still.py`
   用 numpy 重实现同一套规则，渲成 PNG。**不依赖浏览器**，但只画粒子和替身网格（无 shader/泛光）。
   阶段/锚点/相机这些 XML 里没有的演出参数走同名 sidecar（`<xml>.view.json`），没有就自动模式。
2. **浏览器 headless 截图**（推荐）
   ```bash
   chrome --headless=new --user-data-dir=<临时目录> --window-size=1280,720 \
          --hide-scrollbars --screenshot=out.png --virtual-time-budget=2500 \
          "file:///…/preview.html?t=3.4"
   ```
   - `--user-data-dir` 必须独立，否则会去开用户已运行的 Chrome
   - **虚拟时间推得比预期慢** → 页面加了 **`?t=` 参数直跳指定时刻**（加载后快进 + 暂停）

---

## 六、工具与文件

> 全部收在 **`tools/particle-pipeline/`**（代码进 git）；数据（288M T3D / 441M 贴图 / 产物）在
> `D:\BrainMaker\骑砍2粒子特效复刻\output\`，两边 junction 连接。**怎么跑见工具链 README**。

| 文件 | 作用 |
|---|---|
| `gen_particle_effect.py` | **生成器**：紧凑 spec → 完整 XML（补全 21 flag + 55 参数）。自检：参数名/flag 名写错报错、值没展开报错并指行、幂等 |
| `examples/yinmo_spec.py` | 样张 spec（阴魔斩三段式，13 emitter） |
| `examples/yinmo.view.json` | 阴魔斩的视图配置 sidecar（阶段 / 锚点 / 相机 / 替身网格） |
| `preview/preview.template.html` | three.js 预览器模板（本体仍是阴魔斩那页，靠 `make_preview.py` 打字符串补丁通用化） |
| `preview/make_preview.py` | **注入器**：填占位符 + 内联 three.js + 重建时间线/锚点/胶囊 + 轴向重映射，产出任意 XML 都能看的离线页 |
| `preview/build_preview_set.py` | **批次驱动**：一批 XML → N 页 + index.html（原来只有临时命令，2026-09-20 补成脚本） |
| `preview/render_still.py` | 离屏静帧渲染（numpy 版） |
| `preview/vendor/three.min.js` | three.js r160（入库，离线可开的保证） |
| `preview/smoke_d_256.png` | 原版 `smoke_d` 贴图（从 `particles.tpac` dump，缩到 256 便于内嵌） |
| `pipeline/mats/`（原 `tex/prt/`） | 41 个 `prt_shd_*` 材质 dump —— `MAT_BLEND` 混合模式表的证据 |

**生成物纪律**（铁律 22）：`yinmo_slash.xml` 是生成物，**禁止手改** —— 改 `yinmo_spec.py` 重跑生成器。

```bash
cd /d D:\BrainMaker\骑砍2粒子特效复刻
python <仓库>\tools\particle-pipeline\gen_particle_effect.py <仓库>\tools\particle-pipeline\examples\yinmo_spec.py -o output\yinmo_slash.xml
```

---

## 七、还没验证的

| 项 | 状态 |
|---|---|
| 内容包注册 `soln_particle_systems` 能否生效 | 🔴 **已实机证伪（2026-09-24）**：注册只对**编辑器**有效，运行期查不到名字 —— 运行期粒子来自包里的 `Particle` 资产，必须 Publish 成粒子包（见 §1.5 的实机更正） |
| 自造 effect 名会不会和原版撞名 | 用唯一前缀规避，未实测 |
| `add_modulate_combined` 的**确切**合成公式 | 反编译看不到 native 实现，预览里用的是近似（alpha + 加法增益） |
| 引擎的曲线插值 | 用的是切线 Hermite；预览用 smoothstep 近似 |
| 球壳的经线纹 / 体积感 | 引擎里是 **shader**，纯粒子做不出来 —— 需要自建网格 + shader，内容包能不能自定义 shader **未验证且倾向不能** |

---

## 八、跨引擎复刻：UE「FlexibleCombatSystem」粒子 → 骑砍 2 XML（2026-09-20 新增）

> 🔴 **§8.3/§8.4/§8.5 的「T3D 导出与解析」方法论已收编进工具链正文：**
> [tools/ue-dissect/README.md](../tools/ue-dissect/README.md)（唯一 UE 资产导出/解析底座，含可复用脚本与四条军规）。
> 本节保留的是**领域内容**：映射表（§8.6）、材质映射（§8.7）、不变量（§8.8）、产物清单（§8.9）。

> 来源工程：`D:/UEProjects/【UE5】FlexibleCombatSystem`（**UE 4.27**；就是 `骑砍2动画重定向/README.md`
> §7.2 **T6** 里提到的 "FCS"，那台工程本身带 `Magic/*` 法术与 `Assassinate_*` 成对处决动画）。
> 工程里的 `Content/FlexibleCombatSystem/VFX/` 有 **65 个 Niagara + 34 个 Cascade = 99 个粒子系统**，
> 外加 220 个材质/实例、262 张贴图、7 个向量场、38 个网格。
> 本节记录的是「**怎么把这 99 个特效的数值与结构挖出来、再自动写成骑砍认的 XML**」这条链路的实测经验。

### 8.1 结论速览

| 问题 | 结论 |
|---|---|
| 能解析吗？ | ✅ **能，而且是「拿全数值」级别**，不是只看个大概 |
| 靠什么解析？ | **UE4.27 编辑器无头跑 Python** → 用 `ObjectExporterT3D` 把资产导成 **T3D 文本** → 自己写解析器 |
| 解析规模 | 99/99 个粒子系统成功（**685 个 emitter**），0 失败 |
| 能自动转成骑砍 XML 吗？ | ✅ 99/99 生成；685 个 emitter **全部**通过「21 flag + 55 param」硬校验，材质 100% 落在原版白名单 |
| 复刻出来会一模一样吗？ | ❌ **不会**。数值/结构/曲线/材质语义可复刻；最终外观取决于游戏 shader 与自建网格（同 §七 的边界） |
| 单位要换吗？ | 🔴 **要**。UE 是**厘米**（cm），骑砍是**米**（m）→ 长度/速度/半径一律 **÷100** |

### 8.2 四条解析路线的实测对比（选型依据，别再重走）

| 路线 | 实测结果 | 原因 / 证据 |
|---|---|---|
| ① UE Python API 读 Niagara | ❌ **不可行** | 4.27 的 Niagara 运行期类**几乎没有脚本绑定**：`dir(unreal.load_asset("…NS_Fireball"))` 只有 24 个通用 UObject 方法；`get_editor_property("emitter_handles")` 直接报 `Failed to find property 'emitter_handles'`。`unreal` 模块里 Niagara* 有 133 个名字，但都是工厂/枚举/C++ 类型，不是可读句柄 |
| ② UE Python API 读 Cascade | ❌ **不可行** | `ParticleSystem.Emitters` 是 **protected**，Python 报 `Property 'Emitters' … is protected and cannot be read`（能调的只有 `contains_emitter_type()` 这种 helper） |
| ③ 直接读 `.uasset` 的 Names 表（`strings` / 二进制扫描） | ⚠️ **半可用** | 能拿到**模块配方**与材质引用，**但拿不到数值**。例：`/Niagara/Modules/Emitter/SpawnRate`、`/Niagara/Modules/Spawn/Location/V2/SphereLocation`、`ColorFromCurve`、`ENiagara_SpriteRotationMode`、`TilingCurl32`、`DefaultSpriteMaterial`。适合做「这是什么特效」的快速判读，不能做数值复刻 |
| ④ **`ObjectExporterT3D` 导出 T3D 文本** | ✅ **全可用（本方案）** | T3D 是 UE 原生**文本**序列化格式。一次导出同时拿到：节点图（`FunctionScript` / `PinName` / `DefaultValue`）+ **数值常量（含字节流）** + 渲染器（`Material` / `Alignment` / `SubImageSize`）+ **曲线关键帧**（`Time/Value/ArriveTangent/LeaveTangent`）+ Cascade 的全部 `Distribution*` 数值 |

**结论：想解析 UE 粒子，别碰 Python API，直接上 T3D 文本导出。**

### 8.3 怎么调起来（可复制的命令行）

```bash
# 1) 全量导出 T3D 文本（脚本：tools/ue-dissect/export_t3d.py —— 全仓唯一的 UE 导出器）
#    产物落 Debug/offline/fcs_dump/out/t3d/（含 vfx/ 150 个，是超集）
MSYS_NO_PATHCONV=1 "D:/UNREAL/UE_4.27/Engine/Binaries/Win64/UE4Editor-Cmd.exe" \
  "D:/UEProjects/【UE5】FlexibleCombatSystem/FlexibleCombatSystem.uproject" \
  -run=pythonscript -script="exec(open(r'<仓库>/tools/ue-dissect/export_t3d.py', encoding='utf-8-sig').read())" \
  -unattended -nosplash -nullrhi -stdout
```

导出器的核心三行（**Python 侧，不是 C++ 插件**）：

```python
exp = unreal.ObjectExporterT3D()          # 这个类在 unreal 模块里直接可用
task = unreal.AssetExportTask()
task.set_editor_property("object", asset)
task.set_editor_property("exporter", exp)
task.set_editor_property("filename", out_path)
unreal.Exporter.run_asset_export_task(task)
```

踩下来的 API 细节：

| 细节 | 实测 |
|---|---|
| `AssetExportTask` 有哪些可写属性 | `object / exporter / filename / prompt / replace_identical / use_file_archive / write_empty_files / automated / errors / ignore_object_list / selected / options`。**没有 `automated_export`**（写它会 `Failed to find property`） |
| 只设 `object/exporter/filename` 就够 | ✅ 是。多设反而容易撞属性名 |
| `format_extension` 能不能当扩展名用 | ❌ 它是 `Array` 对象，取出来可能为空 → **扩展名自己定**（用 `.t3d`） |
| 扩展名影响内容吗 | ❌ 不影响，`ObjectExporterT3D` 写的永远是 T3D 文本 |
| 一个 NiagaraSystem 导出来多大 | 平均 ~3 MB，最大 10 MB（T3D 会把同包内**所有**子对象都串出来，含贴图的 inline 数据）→ 99 个合计 **301 MB** |
| 退出码 | 命令结束 `EXIT=1/3` 都正常（引擎把 warning 计成 error 数）；**不看退出码，看产物文件在不在** |
| `-stdout` | 会把 `unreal.log()` 回显到 stdout，方便追踪 |

🔴 **AssetRegistry 两个必踩的坑（Python 侧）**：

```python
# 坑 1：千万别在命令行模式下调 scan_paths_synchronous(ForceRescan)
#        —— 会把注册表清空，随后 get_all_assets() 返回 0，命中资产 = 0
#        4.27 在 -run=pythonscript 启动时其实已经扫好了，直接 get_all_assets() 即可
arr = reg.get_all_assets()

# 坑 2：get_all_assets() 返回的是 unreal.Array，不是 python list
#        isinstance(arr, list) 恒为 False —— 用这个判空会得到「0 个资产」
#        正确姿势：list(arr) / len(arr) / 直接 for 迭代
```

### 8.4 T3D 的结构长什么样（解析器要按这个写）

```
Begin Object Class=/Script/Niagara.NiagaraSystem Name="NS_Fireball"
   Begin Object Class=/Script/Niagara.NiagaraEmitter Name="Embers001_10"
      Begin Object Class=/Script/NiagaraEditor.NiagaraScriptSource Name="NiagaraScriptSource_0"
         Begin Object Class=/Script/NiagaraEditor.NiagaraGraph Name="NiagaraGraph_0"
            Begin Object Class=/Script/NiagaraEditor.NiagaraNodeFunctionCall Name="NiagaraNodeFunctionCall_3"
               FunctionScript=NiagaraScript'"/Niagara/Modules/Emitter/SpawnRate.SpawnRate"'
               FunctionDisplayName="SpawnRate"
               CustomProperties Pin (PinId=…,PinName="Use Spawn Probability",…,DefaultValue="false",…)
            End Object
         End Object
      End Object
      Begin Object Class=/Script/Niagara.NiagaraScript Name="EmitterUpdateScript"
         RapidIterationParameters=(SortedParameterOffsets=(…),ParameterData=(0,0,0,0,…),DebugName="…")
      End Object
      Begin Object Class=/Script/Niagara.NiagaraSpriteRendererProperties Name="NiagaraSpriteRendererProperties_0"
         Material=MaterialInstanceConstant'"/Game/FlexibleCombatSystem/VFX/Materials/MI_Embers.MI_Embers"'
         Alignment=VelocityAligned
         SubImageSize=(X=2.000000,Y=2.000000)
      End Object
   End Object
End Object
```

🔴 **T3D 会把同一个对象序列化两次**：第一次是**空壳**（只有 `Begin/End Object`），第二次才带属性。
所以解析时必须**按对象名合并**，不能「取首现」——否则：
- 模块名会变成 `NiagaraNodeFunctionCall_3` 而不是 `SpawnRate`（因为 `FunctionScript` 在第二次出现里）
- Cascade 的 `Modules(0..N)` 全丢

🔴 **数组型属性必须保下标**：`Modules(0)=…` / `Modules(1)=…` 如果只用 `Modules` 当键，后面的会覆盖前面的。
解析时要存成 `Modules(0)`、`Modules(1)`…（键名带下标）。

### 8.5 🔴 数值藏在哪：`RapidIterationParameters` 的字节流解码

Niagara 的**数值字面量**不在图上的 pin 里，而在每个 `NiagaraScript` 的
`RapidIterationParameters` 里 —— 一份「命名常量 + 字节偏移 + 字节流」的表：

```
RapidIterationParameters=(
  SortedParameterOffsets=(
     (Offset=0 ,Name="Constants.Embers001.EmitterState.Loop Delay"   ,TypeDefHandle=(RegisteredTypeIndex=55)),
     (Offset=4 ,Name="Constants.Embers001.EmitterState.Loop Duration",TypeDefHandle=(RegisteredTypeIndex=55)),
     (Offset=8 ,Name="Constants.Embers001.EmitterState.MaxDistance"  ,TypeDefHandle=(RegisteredTypeIndex=55)),
     (Offset=12,Name="Constants.Embers001.EmitterState.MinDistance"  ,TypeDefHandle=(RegisteredTypeIndex=55)),
     (Offset=16,Name="Constants.Embers001.SpawnRate.Spawn Probability",TypeDefHandle=(RegisteredTypeIndex=55)),
     (Offset=20,Name="Constants.Embers001.SpawnRate.SpawnGroup"      ,TypeDefHandle=(RegisteredTypeIndex=57)),
     (Offset=24,Name="Constants.Embers001.SpawnRate.SpawnRate"       ,TypeDefHandle=(RegisteredTypeIndex=55))),
  ParameterData=(0,0,0,0, 0,0,128,63, 0,64,156,69, 0,0,0,0, 0,0,128,63, 0,0,0,0, 0,0,112,65),
  DebugName="NiagaraScript …:EmitterUpdateScript")
```

解码规则（**已验证**）：

1. `ParameterData` 是**十进制字节序列**（0~255），不是十六进制
2. 每项按 `Offset` 切片；**尺寸优先用「下一个 Offset − 当前 Offset」推断**，最后一项用类型表兜底
3. `RegisteredTypeIndex` → 类型：`55=float(4)` / `57=int32(4)` / `59=Vector2D(8)` / `60=Vector3(12)` / `62=LinearColor(16)` / `53,54=half(2)` / `58=bool(1)`
4. 小端 `float32` 解

上面那段的解出来是：`Loop Delay=0.0` / `Loop Duration=1.0` / `MaxDistance=5000` / `MinDistance=0` /
`Spawn Probability=1.0` / `SpawnGroup=0` / **`SpawnRate=15.0`**（`00 00 70 41` → 0x41700000 → 15.0f ✅）。

常量名是**三段式**：`Constants.<Emitter 基名>.<Module>.<Input>`。

🔴 **基名 ≠ 图里的节点名**：图里的 emitter 叫 `Embers001_10`，但常量前缀是 `Constants.Embers001.`。
按节点名拼前缀去匹配会**全部取不到值**（症状：所有参数都退回默认模板）。
正确姿势：**前缀无关匹配** —— 拿 `Constants` 与 emitter 段之后的 `Module.Input` 去比。

### 8.6 参数映射表：UE(Niagara / Cascade) → 骑砍 XML

| 骑砍 XML 参数 | Niagara 来源 | Cascade 来源 | 换算 |
|---|---|---|---|
| `emission_rate` | `SpawnRate.SpawnRate` | `ParticleModuleSpawn.Rate` | 直接（/s） |
| `particle_life` `base`/`bias` | `InitializeParticle.Lifetime Min/Max` | `ParticleModuleLifetime.Lifetime` | `base=min, bias=max-min` |
| `emitter_life` | `EmitterState.Loop Duration` | `ParticleModuleRequired.EmitterDuration` | 秒 |
| `emit_volume_type` + `emit_sphere_radius` | `SphereLocation` 存在 → sphere | `ParticleModuleLocationSphere` | 半径 ÷100 |
| `emit_velocity_x/y/z` | `AddVelocity.Velocity` | `ParticleModuleVelocity.StartVelocity` | ÷100，`bias≈|base|×0.2` |
| `emission_speed_limit` | `SolveForcesAndVelocity.Speed Limit` | — | ÷100 |
| `gravity` | `GravityForce.Gravity` | `ParticleModuleAcceleration.Acceleration` | **×1/980**（UE 惯用 −980 cm/s² ↔ 骑砍默认量级 −1） |
| `damping` | （无，留默认 0.733） | `ParticleModuleAccelerationDrag.DragCoefficient` | 直接 |
| `turbulence_strength` | `CurlNoiseForce.Noise Strength` | — | ÷100，**近似**（骑砍没有 curl noise） |
| `particle_size` / `size_curve` | `ScaleSpriteSize` + `Vector2DFromCurve` 的 `XCurve/YCurve` | `ParticleModuleSize.StartSize` | ÷100 |
| `particle_color`(color/alpha) | `NiagaraDataInterfaceColorCurve` 的 `Red/Green/Blue/AlphaCurve` | `ParticleModuleColor.StartColor` + `ColorOverLife/AlphaOverLife` | 曲线关键帧直接搬 |
| `material` | `NiagaraSpriteRendererProperties.Material` | `ParticleModuleRequired.Material` | 🔴 **必须换名**，见 §8.7 |
| `texture_sprite_count` | `SubImageSize=(X,Y)` | `SubImages_Horizontal/Vertical` | `(X,Y)` → `"X, Y"` |
| `billboard_type` | `Alignment` | `ParticleModuleRequired.ScreenAlignment` | 见下表 |
| `select_random_sprite`(flag) | 有图集时置 true | 同 | flag，**不是 parameter** |

朝向映射：

| UE | 骑砍 `billboard_type` |
|---|---|
| `VelocityAligned` / `PSA_Velocity` | `turn_to_velocity_side` |
| `ScreenAligned` / `PSA_Square` / `PSA_FacingCameraPosition` | `2d` |
| `Automatic` / `FaceCameraPlane` / `PSA_None` | `3d` |

曲线直接可搬：Niagara 的 `Keys=((InterpMode=RCIM_Cubic,Time=0.200000,Value=1.000000,ArriveTangent=0.625000,LeaveTangent=0.625000),…)`
→ 骑砍的 `<key time= value= tangent="Arrive,Leave"/>`（**切线语义天然对应**，这是最省事的一环）。

### 8.7 材质映射（决定成败的一环）

骑砍的 `material` **同时是贴图与混合模式**（§二），UE 的 `MI_*` 在骑砍里不存在，**必须按语义换名**。
原版白名单实测共 **45 个 `prt_shd_*`**（从 `Modules/Native/AssetPackages/*.tpac` 二进制扫出，存
`output/vanilla_prt_shd_materials_native.txt`）：

```
prt_shd_blood_1 / _blood_3 / _blood_4 / _blood_5
prt_shd_default_dirt / _dust / _dust_1 / _dust_2
prt_shd_fire_1 / _fire_2 / _fire_3 / _fire_haze_1 / _fire_haze_1y / _fire_nm / _flame_1
prt_shd_glow / _grass / _haze_1 / _lightning / _mist
prt_shd_rain_mud_1 / _rose_a / _ship_foam
prt_shd_smoke_1 / _smoke_2 / _smoke_3 / _smoke_4
prt_shd_snow_dust_1 / _snow_fall_1 / _sparks
prt_shd_steam_1 / _steam_2 / _stone_gravel / _straw_1 / _trail
prt_shd_water_dust / _water_dust_2 / _water_foam_circular / _water_splash2 / _water_splasho
prt_shd_water_splatter_vertical / _water_wave_1 / _waterfall_dust
prt_shd_wood_splinter / _wood_splinter_2
```

自动映射用的**顺序敏感**规则（先具体后笼统，命中即停；`__` 是实测例子）：

| UE 材质名含 | → 骑砍材质 | 实测例子 |
|---|---|---|
| `lightning` / `electric` / `thunder` / `chain` | `prt_shd_lightning` | `MI_Lightning` |
| `fire_haze` | `prt_shd_fire_haze_1` | — |
| `flame`/`fire`/`ember`/`burn`/`torch`/`meteor`/`lava` | `prt_shd_fire_1` | `MI_Fire` → fire_1；`M_Fire_Procedural_Round` → fire_1 |
| `spark` | `prt_shd_sparks` | `M_Hotdog_Spark_Vlerp_MasterKwang` → sparks |
| `snow`/`frost`/`ice`/`blizzard`/`crystal` | `prt_shd_snow_dust_1` | `MI_SnowFlake` |
| `water`/`bubble`/`splash`/`rain`/`foam`/`wave` | `prt_shd_water_splash2` | `MI_Splash_01` |
| `poison`/`acid`/`toxic`/`sludge` | `prt_shd_steam_2` | `NE_Poison_Smoke`(→smoke 先命中) |
| `steam`/`mist`/`fog`/`wind`/`noise`/`distort`/`refract` | `prt_shd_steam_1` | `MI_NoiseDistortion` |
| `blood`/`gore`/`slash` | `prt_shd_blood_1` | `MI_SlashLine_01` |
| `gravel`/`stone`/`rock`/`crack`/`ground` | `prt_shd_stone_gravel` | `MI_GroundCrack_02` |
| `wood`/`splinter` | `prt_shd_wood_splinter` | — |
| `dust`/`sand`/`dirt` | `prt_shd_dust_1` | — |
| `trail`/`ribbon` | `prt_shd_trail` | `M_ArrowTrail` → trail |
| `smoke`/`fume`/`ash` | `prt_shd_smoke_1` | `MI_Smoke`、`M_Plasma_Smoke_01` |
| `glow`/`aura`/`flash`/`ring`/`halo`/`flare`/`beam`/`laser`/`rune`/`shard`/`radial`/`gradient`/`thread`/`magic`/`plasma`/`light` | `prt_shd_glow` | `MI_Glow_Fire`、`M_Radial_Gradient_DepthFade`、`M_CleanseFlare` |
| `dissolv`/`fade`/`mask`/`default` | `prt_shd_haze_1` | `MI_Dissolver_03` |

🔴 **规则必须写成元组**：`(("spark"), "prt_shd_sparks")` 少了那个逗号会退化成**字符串按字符迭代**，
于是 `'s'` / `'p'` / `'a'` / `'r'` / `'k'` 全变成关键词，把任何含 s/p/a 的材质名都误判成 sparks
（本轮的「`~ s` / `~ p`」就是这个 bug）。正确：`(("spark",), "prt_shd_sparks")`。

🔴 **换材质 = 换混合模式**：`MI_Fire` 在 UE 是加法发光，换成 `prt_shd_smoke_1` 会变成灰烟；
压暗/黑烟要选 `prt_shd_haze_1`（`modulate`）。**这一层不能自动决定，必须人工确认**。

### 8.8 三条「不变量」（照抄会翻车）

| # | 不变量 | 症状 | 正解 |
|---|---|---|---|
| 1 | **UE 是 cm，骑砍是 m** | 烟团比人高 100 倍 | 长度/速度/半径 **÷100**；只有加速度类走 `×1/980` |
| 2 | **UE 颜色常是 HDR（>1）** | 闪电 `(0, 47.8, 50)` 直接写进 XML → 饱和成白团 | 取 `max(r,g,b) > 1` 时**除以该 max** 归一，再夹到 [0,1] |
| 3 | **Niagara 的「纯黑」= 颜色交给材质** | 余烬/火星被刷成黑炭 | `Color.Color ≈ (0,0,0)` 且无 `ColorFromCurve` 时**不要覆盖** `particle_color`，沿用默认 |

另外：`select_random_sprite` 是 **flag 不是 parameter**，写进参数区会被生成器直接 `KeyError` 拒绝
（`gen_particle_effect.py` 的 flag 白名单校验，这是它的优点，别绕过）。

### 8.9 工具与产物（数据根 `D:/BrainMaker/骑砍2粒子特效复刻/`）

> 2026-09-20 整合：**代码全部搬进仓库 `tools/particle-pipeline/`**（进 git），
> 数据留在 D 盘；D 盘的 `pipeline/` 与 `preview/` 现在是指向仓库的 **junction**（同一份代码）。
> 所以下表的相对路径依然成立 —— 从数据根跑，跑的就是仓库里那份代码。

| 文件 | 作用 |
|---|---|
| ~~`pipeline/export_t3d_all.py`~~ | 🔴 **已退役（2026-09-24 合并）**：T3D 导出统一到 `tools/ue-dissect/export_t3d.py`（超集，含 Cascade） |
| `pipeline/t3d_parse.py` | **领域投影层**：T3D → 本管线 JSON。**解析底座**（对象树 / 常量解码）在 `tools/ue-dissect/t3d_tools.py`，本文件 import 它 |
| `pipeline/ue2bannerlord.py` | **转换器**：解析 JSON → emitter spec → 调 `gen_particle_effect.py` 出 XML |
| `pipeline/validate_xml.py` | **自检**：XML 合法性 + 21 flag/55 param 硬约束 + 材质白名单 |
| `preview/build_preview_set.py` | **预览批次驱动**：一批 XML → N 页离线预览 + index.html |
| `output/t3d/` | ⚠️ 旧 T3D（288 MB，**已不是数据源**）：现由 `paths.t3d_src()` 读 `Debug/offline/fcs_dump/out/t3d/vfx/`（150 个，含 Cascade） |
| `output/parsed/` | 99 个结构化 JSON（31 MB）+ `_index.json` |
| `output/spec/` | 99 个 emitter spec（中间产物，可读可改） |
| `output/xml/` | **99 个骑砍 particle XML**（685 个 emitter，0 问题） |
| `output/tex/` | **262 张 UE VFX 贴图 PNG**（441 MB，供自建材质/贴图用） |
| `output/preview/` | 11 页预览 + 索引（three.js 内联，断网可开） |
| `output/vanilla_prt_shd_materials_native.txt` | 原版 45 个 `prt_shd_*` 白名单 |

一键复跑（改完任一层重跑）：

```bash
cd D:/BrainMaker/骑砍2粒子特效复刻
# 数据源：优先 ue-dissect 的 dump（超集，含 Cascade），没有才退回本管线 output/t3d；BM_T3D_SRC 可显式指定
python pipeline/t3d_parse.py                    # T3D  -> JSON
python pipeline/ue2bannerlord.py output/parsed/*.json   # JSON -> spec -> XML
python pipeline/validate_xml.py                 # 硬校验
# 贴图（可选）
MSYS_NO_PATHCONV=1 "D:/UEProjects/umodel_win32/umodel_64.exe" -game=ue4.27 \
  -path="D:/UEProjects/【UE5】FlexibleCombatSystem/Content" \
  -export -out="D:/BrainMaker/骑砍2粒子特效复刻/output/tex" -png -nooverwrite \
  "FlexibleCombatSystem/VFX/Textures/*"
```

> 贴图导出还有一个**捷径**：T3D 里本来就带了贴图的 `Source=(Id=…,SizeX=…,bPNGCompressed=True,Format=TSF_BGRA8)`，
> 想省一步可以用 `umodel` 按包名批量出 PNG（上面那条命令即是，实测 262/262 成功）。

### 8.10 这次实测到的规模（供排期参考）

| 维度 | 数量 |
|---|---|
| Niagara 粒子系统 / Cascade 粒子系统 | **65 / 34 = 99** |
| emitter（转换后） | **685** |
| 原版材质白名单 | 45 |
| 自动映射到 `prt_shd_glow`（发光类）的比例 | 最高，因为 UE 特效大量用 `MI_Glow_*` / `MI_Flash` / `MI_Aura_*` |
| 生成 XML 总行数 | 约 13 万行（21 flag + 55 param 定长样板所致，**必须用生成器**） |

### 8.11 还没验证的（交接）

| 项 | 状态 |
|---|---|
| 这批 XML 在**游戏里实机播放**是否正常 | **未验证**。只做了「格式合规 + 材质白名单」自检 |
| 内容包注册 `soln_particle_systems` 指向这批 XML 能否生效 | 🔴 **已实机证伪（2026-09-24）**：见 §1.5 实机更正 —— 照这个做法引用效果名会静默查不到 |
| UE 坐标 → 骑砍坐标的**方向约定** | **未验证**。UE 是 Z-up（X 前 / Y 右 / Z 上），骑砍粒子局部也是 Z-up，**但两者 Y/X 的手性是否镜像没实测** → 目前向量**原样搬**，可能出现左右镜像 |
| 曲线语义 | Niagara `particle_life` / `emitter_life` 的归一化基准与骑砍是否一致，**未逐位比对** |
| Cascade 的 `DistributionVectorConstantCurve`（矢量曲线） | 解析器目前只吃**标量**关键帧；矢量曲线（`Value=(R=…,G=…,B=…,A=…)`）**没解**，会退回常量/默认 → 少数 `ColorOverLife` 特效颜色不还原 |
| `ShaderLUT` 采样表 | Niagara 曲线接口同时带一张 91 点采样表（`ShaderLUT(0..181)` + `LUTNumSamplesMinusOne=90`），**比 XCurve 关键帧更精确**，本方案用的是关键帧，未用采样表 |
| 继承 `DefaultEmitter` 的 emitter 材质 | 部分 emitter 的 `Material` 为空（继承自默认发射器），当前退回 `prt_shd_smoke_1`；**应改为读它的父级默认材质** |
| 复刻后的**外观相似度** | 未做（无法在本机看到 UE 原效果，只有工程资产）。建议下一步：先用 UE 编辑器把 99 个特效各渲一张静帧，再与 `output/preview/` 的 three.js 预览页并排比对 |

---

## 九、从录像反推特效参数（2026-09-21，`阴魔斩.mp4`）

### 9.1 为什么要立这一条

手上有 UE 工程时可以走 §8 那条路（T3D → parsed → spec → XML）；**没有工程、只有一段录像**时，
录像反而是最可靠的参考源——尤其当它是「同一技能多次释放 + 多机位」的演示片：
多机位能互相补全被遮挡的结构，多次释放能看出哪些是随机、哪些是固定。

### 9.2 三条可复现的方法（别凭肉眼估）

| 方法 | 怎么做 | 本次实测 |
|---|---|---|
| ① **尺子 = 角色** | 挑一帧角色全身清晰、效果也在同框的帧，量「头顶→脚底」像素数，除角色身高（UE5 小白人 ≈ 1.8 m），得到 **px/m**；此后所有尺寸都换算成米 | 12.00s 帧：头顶 252 → 脚底 657 = **405 px = 1.8 m → 225 px/m（1 px = 4.44 mm）** |
| ② **取色 = 掩膜** | 对每帧做「高亮红」「品红」掩膜（如 `R>225 & R-G>60 & R-B>55`），取**最大连通块**的均值与 p95；比肉眼/取色器可靠，也能顺带量出 bbox | 白热核 (246,150,135)/p95 (255,194,185)；绯红亮盘 (241,142,143)；品红飘带 (202,146,185)/p95 (255,181,216) |
| ③ **先粗后密抽帧** | 先 1 fps 抽满全片 → 算**帧差能量曲线**找"动作窗口"→ 再在窗口内按 0.25 s 补密看分解 | 45.1 s 里 **约 10 次施法**（1.2 / 6.5 / 12.4 / 22.0 / 25.0 / 27.4 / 32.2 / 34.0 / 37.0 / 43.0 s），单次循环 ≈ 1.5 s 蓄力 + 0.6~0.9 s 掷出飞行 + 1.5~2.6 s 余韵 |

> 用 `cv2.VideoCapture.set(CAP_PROP_POS_FRAMES, t*fps)` 定点抽帧最省事（不用装 ffmpeg）。
> 合成"九宫格 + 时间戳"的对照图再交给视觉模型，比逐帧单看快一个量级。

### 9.3 阴魔斩实测表（写进 `examples/yinmo_spec.py` 的那一组数）

| 元素 | 实测 | 依据帧 |
|---|---|---|
| 蓄力球·白热亮盘 | **0.27–0.31 m** ⌀ | 12.00s（60–70 px） |
| 蓄力球·白热内核 | 0.13 m | 12.00s（~30 px） |
| 蓄力球·暗紫晕圈 | **0.52 m** ⌀（= 亮盘的 1.7 倍） | 12.00s（~117 px） |
| 蓄力球·悬停高度 | 指尖上方 **0.37 m** | 12.00s（83 px） |
| 蓄力时长 | **≈ 1.5 s**（满蓄 → 释放） | 0.00→1.25s；11.0→12.25s 复现 |
| 月牙弧带厚 | **0.6–0.85 m** | 7.00s（130–190 px）/ 43.25s |
| 月牙弧半径 | **1.6–2.2 m**（受 `初速 / 阻尼` 控制） | 7.00s / 43.25s |
| 月牙整体跨度 | 3–4 m（≈两人高） | 7.00s |
| 弧尖亮珠 | ⌀ 0.20–0.25 m | 43.25s（~50 px） |
| 品红飘带 | 长 **2–4 m**、宽 0.03–0.06 m、2–3 根成组、存活 **1.5–2.6 s**（比月牙活得久） | 8.00s / 28.0–29.1s |
| 黑烟 | 暗紫红 → 近黑（≈ 0.20,0.09,0.13 → 0.06,0.03,0.05） | 1.00s / 12.25s / 27.6s |

**🔴 结构性结论**：阴魔斩 = **白热红球（加法发光）＋ 月牙弧（发光）＋ 黑烟（乘法压暗）＋ 品红飘带（发光细带）** 四件套。
红与暗**必须同时由两类材质承担**：只做加法没有"阴"的黑，只做乘法没有"斩"的亮。

### 9.4 🔴 骑砍粒子做不出「月牙」——本次最重要的边界认识

录像里的月牙在 UE 侧是 **Mesh（月牙片）/ Flipbook ribbon**；骑砍没有 mesh 渲染器、也没有 ribbon emitter
（已实证：`bin` 全量 grep 无 `ParticleMesh` / `RibbonRenderer` 命中；`billboard_type` / `sample_mesh`
只在 `TaleWorlds.Native.dll` 里解析）。所以**常规粒子方案**只能做出「弧形冲击波 / 彗尾」，
**读作斩击波，读不出月牙**（已用静帧自检确认：`out/final_*.png` 里残迹段是一团"亮核 + 拖烟 + 余烬"的彗尾）。

> ⚠️ **别把这条说满（README §4.2 已更正）**：发射体除 box / sphere 外**还有第三种 `sample_mesh`**
> ——「从任意网格表面采样发射」，原版自己在用（`prt_pot_pile` 10 处 · `prt_wood_parts2` 9 ·
> 攻城塔残骸 6 · `prt_foam_a` 2 · `generic_particle_3d` 2）。即**结论对、理由错**：
> 做不出来的只是**参考图那种薄、有厚度、边界锐利的立体月牙**，而"**月牙形粒子带**"是能做到的
> ⇒ 路线 C 优先级最高。完整判据表见 **README §4.2**。

三条路线（由易到难，**都没实机验证**）：

| 路线 | 做法 | 代价 / 风险 |
|---|---|---|
| **A 纯粒子近似**（已做） | `crescent_edge`：`billboard_type=turn_to_velocity_side` + `inherit_emitter_velocity≈0.94`，让粒子沿速度铺成 0.3 m 宽的带 | 只能得到"弧形彗尾"，不是月牙；优点是零新增资产、立刻能进内容包 |
| **B 自制月牙贴图 + 单 quad** | 生成一张 256² RGBA 月牙（RGB 恒白 + alpha 载形状，见 §四 坑 2），配 `prt_shd_glow`/自建材质，用 1 个大粒子（`particle_size ≈ 3.6 m`）承担整条月牙，靠 `fixed_billboard_direction` 永远正对相机 | 需要能加材质（`soln_particle_systems` 注册本身就没验证过，见 §8.11）；单 quad 无厚度，侧面看会"翻片" |
| **C `sample_mesh` 撒点成弧** | 引擎有 `sample_mesh` 参数 + `select_random_sample_mesh` flag —— 可以拿一个**月牙 mesh 当发射形状模板**，把粒子撒在月牙面上，得到真正的月牙形粒子阵（mesh 本身不渲染） | 要能给内容包塞 mesh 资产并让粒子系统引用；这条**最接近原效果**，优先级最高去试 |

> 顺带：录像里月牙四周那层**半透明粉紫"面"**同样是 shader/mesh 属性，粒子只能靠
> `prt_shd_steam_1`（add_alpha）大面积低 alpha 近似 —— 本次 13 emitter 里没塞它，需要时再加一个。

### 9.5 复刻后的验证链路（照抄即可）

```bash
cd H:/SteamLibrary/.../tools/particle-pipeline
PY=/c/Users/yaodongcheng/AppData/Local/Programs/Python/Python312/python.exe
$PY gen_particle_effect.py examples/yinmo_spec.py -o out/yinmo_slash.xml   # 生成
$PY pipeline/validate_xml.py out                                          # 硬校验：应得 emitters=13 / 问题 0 条
$PY preview/render_still.py --xml out/yinmo_slash.xml \
      --view out/yinmo_particles_only.view.json --t 1.20 --out out/x.png   # 静帧（关替身网格=只看粒子本体）
$PY preview/make_preview.py --xml out/yinmo_slash.xml -o out/yinmo_slash.html --title 阴魔斩
```

### 9.6 本次新踩的坑

| # | 坑 | 症状 | 正解 |
|---|---|---|---|
| 1 | `render_still.py` 的 `draw_shell()` 引用了**不存在的 `CENTER`** | 渲染 `爆开` 阶段（1.50–2.40 s）时 `NameError: name 'CENTER' is not defined`，静帧直接不出来 | 改成 `cam.project(anchor(1, t))`（`fx=1` 就是爆开阶段）。**只在渲染爆开那一刻才触发**，所以之前一直没暴露 |
| 2 | `render_still.py` **一次只模拟"当前阶段"的那一个 effect** | 1.80 s 的静帧里看不到同帧还在飞的爆开壳 → 误判"效果没做出来" | 静帧只用来量**尺寸/形态/密度**；想看"三段连播"必须用 `make_preview.py` 出的 HTML 页 |
| 3 | 预览器**不吃 `emissive_multiplier`** | 预览里过曝成白团 / 偏暗，改了 emissive 没反应 | 亮度以**材质 + emissive** 在游戏里的表现为准，**别照预览调亮度**；预览只用来看尺寸、形态、颜色色相、时序 |
| 4 | 九宫格对照图必须带**时间戳水印** | 两张表连看时分不清哪张是哪一秒 | 拼图时在每格左上角写死 `t=xx.xxs` |
| 5 | 🔴 **「原版 XML 没用到」≠「引擎不支持」——别用使用面推断能力面** | 我据「`emission_velocity_model` 全库只有 `random_velocity_components`」断言骑砍做不了朝心/引力；用户质疑「一般粒子都能做引力，难道骑砍不支持？」后改用 native DLL 重查，发现参数表里其实有 `radial_velocity` / `radial_emission_velocity` / `radial_rotation_speed` / `emit_sphere_radius_inner` / `emit_disc_radius` / `emit_box_size` / `warmup_time` 等字段 | 判能力边界要看**引擎侧证据**：① `bin/Win64_Shipping_wEditor/TaleWorlds.Native.dll` 的参数字符串池（按文件偏移 dump，能看到结构体字段顺序）② `Modules/Native/Prefabs/*.xml`（**真的写过 55 个以外的参数**：`emit_disc_radius` / `emit_box_size` / `decal_material` / `camera_fadeout_far_coef`）③ `Modules/Native/ModuleData/Layouts/*.xml`（编辑器面板字段）。结论必须区分**「原版没用」与「引擎没有」**，前者不是证据 |
| 6 | `cv2.imwrite` 遇**中文路径会静默失败** | 抽帧/拼图脚本跑到「-> 输出路径」看着成功，文件却没生成 | 用 `cv2.imencode(".png", img)[1].tofile(path)` 写盘（本工程数据根含中文，必踩） |
| 7 | 拼九宫格前**定好物理视场再比** | 拿两张不同 px/m 的图并排比黑气范围，结论全错 | 先各自标定 px/m（视频 = 角色 1.8 m 反推；静帧 = `f=(H/2)/tan(FOV/2)`、`z≈dist` 反推），裁**相同物理边长**再缩放对齐，并在图内画比例尺 |

---

## 十、实例：阴魔斩「蓄力段」怎么配（2026-09-23）

> **现状**：已经配好了 —— spec `tools/particle-pipeline/examples/yinmo_spec.py` 的 `lwn_yinmo_charge`（6 个发射器），
> 生成物 `Modules/TaikouAnim/ModuleData/particles/yinmo_slash.xml`。**还没进过游戏。**
> 面板上每个字段对应哪个 XML 键，查 **§1.7**；本节的数是**实测标定过的**（方法见 §9.2）。

### 10.1 思路（三句话）

1. 蓄力 = **掌上一颗白热红球 + 四周黑气往核里收**（1.5 秒攒满）。
2. 亮和暗**必须由两种材质分开承担**：加法发光类（`prt_shd_glow`）出「白热」，乘法压暗类（`prt_shd_haze_1`）出「阴气」。
   只做加法 = 没有阴；只做乘法 = 没有蓄力的亮光。**这是成败线**（§二 + §9.3）。
3. **一个发射器只干一件事**，六件事 = 六个发射器（别指望一根发射器里调出两种观感）。

### 10.2 六个发射器的分工表（照抄即可）

| # | 发射器名 | 职责（实测尺寸） | material | 关键字段（值就是 XML 里该写的数） |
|---|---|---|---|---|
| 1 | `core_whitehot` | 白热内核，视觉球 ≈ **0.30 m** | `prt_shd_glow` | `emission_rate` 420 · `particle_life` 0.30±0.08 · `emit_volume_type` sphere + `emit_sphere_radius` 0.05 · `damping` 3.0 · `emit_velocity_x/y/z` 0.30 · `particle_size` 0.150±0.045 + 尺寸曲线(1.0→0.45，乘 0.160) · `emissive_multiplier` 5.0 · `max_alive_particle_count` 400 · 颜色 白热粉(1.0,0.70,0.62)→(1.0,0.34,0.22) · alpha 峰值 0.92 |
| 2 | `crimson_disk` | 包着核的绯红亮盘 ≈ **0.45 m** | `prt_shd_glow` | rate 260 · life 0.42±0.12 · sphere 0.09 · damping 2.0 · vel 0.45 · size 0.360±0.080 + 曲线(0.50→1.40，0.160) · emissive 1.8 · 颜色 (0.98,0.55,0.52)→(0.62,0.10,0.12) · alpha 峰值 0.50 |
| 3 | `dark_halo_flat` | 紧贴亮盘的**平滑**暗晕 ⌀ **0.52 m** | `prt_shd_haze_1` | **rate 只要 6**（单颗大面片顶替几百颗小烟团）· life 1.20±0.30 · size 0.520±0.060 · 曲线基本平 · `diffuse_multiplier` 0.45 · `emissive_multiplier` **0** · `max_alive_particle_count` **8** · alpha 峰值 0.22 |
| 4 | `dark_shell_outer` | 外层黑气壳（半径 **0.45 m**）往小瘪 | `prt_shd_haze_1` | **`emitter_life` 0.10** · rate 120 · life 0.90±0.20 · sphere 0.45 · size 0.160±0.050 + 曲线(1.0→0.25，0.18) · diffuse 0.40 · emissive 0 · alpha 峰值 **0.06** · flag `order_by_distance` |
| 5 | `dark_shell_inner` | 内层黑气壳（**0.22 m**）**延后 0.35 s** | `prt_shd_haze_1` | **`activation_delay` 0.35** · rate 80 · sphere 0.22 · size 0.140±0.040 · alpha 峰值 0.08 · 其余同 4 |
| 6 | `dark_inflow_streaks` | 细长暗气流条（"往心里钻"的观感） | `prt_shd_haze_1` | `billboard_type` **turn_to_velocity_side** · rate 40 · sphere 0.90 · damping 0.25 · size 0.050±0.020 + 曲线(1.0→0.25，0.030) · **`skew_with_particle_velocity_coef` 10 · `skew_with_particle_velocity_limit` 0.350** · flag `skew_with_respect_to_particle_velocity` · alpha 峰值 0.10 |

> 上面每条的 `emission_rate` 都是 `(base, bias)`，XML 里就是 `base=` / `bias=` 两个属性（§1.2）。
> 球体积发射靠 `emit_volume_type="sphere"` + `emit_sphere_radius` —— 原版几乎不用（§4.2 统计：box 812 / sphere 1），
> 但**能用**，蓄力这种「一团包一颗核」的结构正好用它。

### 10.3 四条硬规矩（都是踩出来的，别重犯）

| # | 规矩 | 为什么 |
|---|---|---|
| 1 | 🔴 **浓度铁律：压暗类粒子要小、要少、alpha 峰值压在 0.1 以下** | `modulate` 是**乘法**叠加，一团叠一团会连乘到纯黑。第一版 90 颗 0.85 m 黑烟**把白热核整个吃掉了**。要「一层平滑的暗」就用**单颗大面片**（第 3 条 rate=6），不是几百颗小烟团 |
| 2 | 🔴 **「聚拢」目前是近似，不是真·径向** | 引擎没有吸引子。做法 = **壳层塌缩三件套**：每个壳一个发射器 + 固定半径 + `activation_delay` 错开 + `size_curve` 让雾团自己往小瘪 ⇒ 看上去像一圈黑气往核里收。<br>🔴 真·向心要看 `radial_velocity`（面板上叫 **Radial Velocity**）能不能被读到 —— **未实测**，探针已备（§1.7 末） |
| 3 | **一次发完一层：用「短 `emitter_life` × 高 `emission_rate`」代替 `emit_at_once`** | `emitter_life 0.10 × rate 120` ≈ 0.1 秒内发完一层，行为可预测；`emit_at_once` 语义不明、不碰 |
| 4 | **尺寸先问「站在 1.8 米的人旁边多大」** | 尺子 = 角色身高（§9.2 方法①）。初版把烟团算到 3.17 m（比人还高），叠加后糊成一坨死黑 |

### 10.4 改哪、怎么验

```bash
cd tools/particle-pipeline
python gen_particle_effect.py examples/yinmo_spec.py -o out/yinmo_slash.xml   # 改 spec → 重生成（铁律 22：别手改 XML）
python pipeline/validate_xml.py out                                          # 硬闸门：应得 15 emitter / 问题 0 条
python preview/make_preview.py --xml out/yinmo_slash.xml -o out/yinmo_slash.html --title 阴魔斩
```

- **看形态**：上面的 HTML 页（三段连播）或 `render_still.py` 静帧（只看尺寸/形态/密度）。
  ⚠️ 静帧一次只模拟「当前阶段」一个 effect；**亮度别照预览调**（预览不吃 `emissive_multiplier`，§9.6 坑 3）。
- **在编辑器里看**：`Add Component → Particle → 搜 lwn_yinmo_charge`（注册在
  `Modules/TaikouAnim/ModuleData/project.mbproj`；编辑器开之前文件要先落盘 —— 它是文件监视，铁律 31）。
- **游戏里触发**：`Scene.CreateBurstParticle(ParticleSystemManager.GetRuntimeIdByName("lwn_yinmo_charge"), pos)`
  —— 粒子 XML 是惰性数据，没人 spawn 就永远不出现。
- **要动「一段一段的节奏」**（发射点在哪、什么时候切下一段）改 sidecar `examples/yinmo.view.json`；
  **XML 里没有演出参数**（§2.2 通道 B）。

---

## 十一、Cascade 支线：解析、映射与交接规范（2026-09-24 新增）

> **为什么要单开一节**：UE 有**两代**粒子系统 —— 老的 **Cascade**（`UParticleSystem`，资产类 `ParticleSystem`）与新的 **Niagara**（`NiagaraSystem`）。§八 那条链路主攻 Niagara；Cascade 的结构**比 Niagara 更接近骑砍**，是更好的模仿蓝本，所以单独写清楚，便于"之后拿一批 Cascade 来模仿"。

### 11.1 一句话结论

**Cascade 与骑砍的抽象几乎一一对应**：都是「一串模块 + 分布/曲线」，值全是**具名属性**（不用像 Niagara 那样解字节流）。所以：**结构可直译、数值可直搬，损耗只在材质语义与渲染类型上。**

### 11.2 结构（一屏看懂）

```
ParticleSystem                                  ← 一个特效 = 一个资产
  Emitters(n)  → ParticleSpriteEmitter / ParticleMeshEmitter / …      ← 一个 emitter 一条
      EmitterName="SparkCoreLinger"
      LODLevels(n) → ParticleLODLevel                                  ← ⚠️ 每个 LOD 一套独立模块
          RequiredModule → ParticleModuleRequired   （材质 / 时长 / 延迟 / 图集 / 朝向）
          SpawnModule    → ParticleModuleSpawn      （Rate / RateScale / BurstList）
          Modules(n)     → ParticleModule*          （Lifetime / Size / Color / Location / Velocity / …）
          TypeDataModule → ParticleModuleTypeData*  （Sprite / Mesh / Ribbon / Beam / AnimTrail / GPU）
```

**值长这样**（直接可读，这就是它好解析的原因）：

```
Rate        = (MinValue=20.000000, MaxValue=20.000000, Distribution=DistributionFloatConstant'"RequiredDistributionSpawnRate"')
Lifetime    = (MinValue=0.300000, MaxValue=0.300000, ...)
StartSize   = (MinValue=150.0, MaxValue=150.0, MinValueVec=(X=150,Y=150,Z=150), MaxValueVec=(...))
StartVelocity = (MinValue=-250, MaxValue=1500, MinValueVec=(100,-250,-250), MaxValueVec=(1500,250,250))
AlphaOverLife = (… Distribution=DistributionFloatConstantCurve'"DistributionFloatConstantCurve_0"')
ConstantCurve = (Points=((OutVal=1.0,ArriveTangent=-1.0,LeaveTangent=-1.0),(InVal=1.0)))
Material    = Material'"/Game/FlexibleCombatSystem/VFX/RangedVFX/ArrowTrails/M_ArrowTrail.M_ArrowTrail"'   ← 在 RequiredModule 里
SubImages_Horizontal=3  SubImages_Vertical=3                                                                ← 图集（3×3）
```

### 11.3 本工程实测规模（34 个 Cascade，已全量解出）

| 维度 | 数量 |
|---|---|
| 系统 / emitter / LOD 层 / 模块实例 | **34 / 198 / 633 / 2381** |
| 不同模块类型 | **52 种** |
| 分布类型 | 8 种（`FloatConstant` 393 · `VectorConstant` 148 · `VectorUniform` 115 · `FloatConstantCurve` 106 · `VectorConstantCurve` 93 · `FloatUniform` 87 …） |
| 分布位置 | **全在近战/弓箭**（`VFX/MeleeVFX` 5 + `VFX/RangedVFX` 29）；**法术那边 0 个 Cascade**（法术表引用：Niagara 132 次、Cascade 0 次） |

**产物与工具**：

| 件 | 位置 |
|---|---|
| 逐系统拆解（人读） | `Debug/offline/fcs_dump/out/digest/CASCADE_BREAKDOWN.md` |
| 机读 + 普查 | `Debug/offline/fcs_dump/out/digest/cascade_census.json` |
| 🔴 **解析器（新）** | `Debug/offline/fcs_dump/cascade_breakdown.py` —— 换任意工程改 `VFX` 常量即可 |
| 转换器（已有） | `tools/particle-pipeline/pipeline/ue2bannerlord.py`（含 Cascade 分支） |

### 11.4 与 Niagara 的对照（选型用）

| | **Cascade**（老） | **Niagara**（新） |
|---|---|---|
| 结构 | 扁平模块列表 | 模块图（节点连线） |
| 参数位置 | **具名属性，直读** | `RapidIterationParameters` 字节流，**要按偏移+类型索引解码** |
| 表达力 | 只有累加式模块（**没有力场/求解器**） | 有 Curl Noise 力场、`SolveForcesAndVelocity` 积分器 |
| 材质 | `RequiredModule.Material` | 渲染器对象的 `Material` |
| 渲染类型 | `ParticleModuleTypeData*`（Sprite/Mesh/Ribbon/Beam/AnimTrail/Gpu） | Renderer 对象类型 |
| **→ 翻骑砍** | 🟢 **近似同构**（模块↔参数、分布↔base/bias、曲线↔key） | 🟡 **降维**（力场/积分器没有对应物，卷曲感会丢） |

**⇒ 结论：想学"怎么在骑砍里做出效果"，Cascade 是更好的蓝本；想复刻某个具体法术的观感，那只能是 Niagara。**

### 11.5 模块清单（本工程实测 52 种，按用途分组 + 骑砍落点）

| 分组 | 模块（实测出现次数） | 骑砍 XML 落点 |
|---|---|---|
| **必需** | `Required`(198) `Spawn`(224) `Lifetime`(196) `Size`(199) `Color`(230) | `material` / `emission_rate` / `particle_life` / `particle_size` / `particle_color` |
| 位置 | `Location`(103) `LocationPrimitiveCylinder`(59) `LocationPrimitiveSphere`(21) `LocationWorldOffset`(20) `LocationEmitter`(14) `LocationDirect`(1) `LocationBoneSocket`(8) | `emit_volume_type` + 半径/尺寸（**÷100**） |
| 速度与力 | `Velocity`(60) `VelocityInheritParent`(8) `AccelerationConstant`(50) `Acceleration`(7) `AccelerationDrag`(53) `SourceMovement`(1) `Orbit`(1) | `emit_velocity_*` / `gravity`（**×1/980**）/ `damping` |
| 尺寸演化 | `SizeMultiplyLife`(189) `SizeScaleBySpeed`(35) `PivotOffset`(2) | `size_curve`（曲线直搬） |
| 颜色演化 | `ColorOverLife`(11) `ColorScaleOverLife`(174) | `particle_color` + alpha 曲线 |
| 图集/朝向 | `SubUV`(20) · `RequiredModule.SubImages_*` · `OrientationAxisLock`(6) | `texture_sprite_count` / `billboard_type` |
| 渲染类型 | `TypeDataGpu`(55) `TypeDataMesh`(42) `TypeDataRibbon`(13) `TypeDataAnimTrail`(8) `TypeDataBeam2`(5) | ⚠️ **骑砍无直接对应**，见 §11.8 |
| 附加 | `MeshMaterial`(41) `MeshRotation`(28) `Rotation`(53) `RotationRate`(5) `Light`(19) `CameraOffset`(14) `VectorFieldLocal`(20) `ParameterDynamic`(58) `EventReceiverSpawn`(66) `EventGenerator`(2) `Collision`(1) `CollisionGPU`(18) `SpawnPerUnit`(2) `TrailSource`(1) | 多数**丢弃或用替代做法**（碰撞/事件/矢量场在骑砍粒子层没有等价物） |

> 分布三态 → 骑砍的对应：`DistributionFloatConstant`（固定值）→ 直接填；`DistributionFloatUniform`（min~max）→ `base=min` + `bias=max-min`；`DistributionFloatConstantCurve`（关键帧）→ 骑砍 `<curve>/<keys>`，**切线语义天然对应**（§8.6 末）。
> `DistributionVectorUniform` 的 `MinValueVec/MaxValueVec` → 三轴各自的 base/bias。

### 11.6 三个坑（实测踩出来的）

1. 🔴 **LOD 层会给你"另一套参数"**：同一个 emitter 的 `LODLevels(0/1/2)` 各挂**一整套独立模块对象**，高层通常是降级版且 `bEnabled=False`。**解析必须取 `Level=0`（或第一个启用层）**，否则会拿到"远处才用"的稀疏参数 —— 症状：效果比原作稀。
2. **`EmitterName` 会重名**：本工程 `P_FireBurst` 里有 **4 个 emitter 都叫 `dustring`**（靠对象名 `ParticleSpriteEmitter_0/1/2/3` 区分）。**别用 EmitterName 当主键**。
3. **材质在 `RequiredModule` 里，而且 UE 材质是自定义 shader**：映射到骑砍那 45 个 `prt_shd_*` 时，**换材质 = 换混合模式语义**（发光↔压暗），必须人工确认，不能全自动（§8.7）。

### 11.7 🔴 交接规范（之后给我 Cascade 素材时，按这个给）

**你给什么**

| # | 项 | 说明 |
|---|---|---|
| ① | **能打开的 UE 工程路径** | 我必须能在无头编辑器里加载它（`-run=pythonscript` 跑导出）。**散装 `.uasset` 没有工程 = 加载不了**，所以要工程 + 资产清单 |
| ② | **要模仿的资产清单** | 一个 txt 列出资产路径或名字即可（如 `VFX/Fire/PS_Explosion`），不用全工程 |
| ③ | **目标用途** | 哪个技能 / 什么场合 / 多大尺度 —— 决定材质语义与单位换算 |
| ④ | **相似度期望** | 三档任选：**结构像**（模块与数值一致）/ **数值像**（参数与曲线一致）/ **观感像**（连材质观感一起调，人工量最大） |
| ⑤ | （可选）参考视频或静帧 | 有的话我做 A/B 对照 |

**我产出什么**

1. `CASCADE_BREAKDOWN.md` 风格**逐 emitter 数值表**（模块 / min~max / 曲线关键帧 / 图集 / 材质）
2. **骑砍粒子 XML**（走现成 `gen_particle_effect.py` + `validate_xml.py` 硬校验）
3. **three.js 预览静帧**（`preview/`，与 XML 同源，用来对照形状/密度/节奏）
4. **差异说明**：哪些模块我丢了、为什么丢、丢了会怎样

**验收三档（先讲好，避免"以为成了"）**

| 档 | 判据 |
|---|---|
| ① 格式合规 | `validate_xml.py` 全过（21 flag + 55 param + 材质白名单） |
| ② 结构/数值一致 | 拆解表与 XML 逐项对得上（发射率/寿命/尺寸/速度/曲线） |
| ③ 观感 | **必须实机看**，且只承诺"骑砍能做到的最好版本"，不是 UE 原样（见 §11.8） |

### 11.8 先讲清楚：这几类 Cascade 复刻不了 / 要换做法

| 项 | 本工程实例数 | 结论 |
|---|---|---|
| **GPU 粒子**（`TypeDataGpu`） | **55**（占比很高） | 骑砍只有原版 1 个 `gpu_particle_system`；**自定义 GPU 粒子能否进内容包未验证** → 默认按 CPU 精灵粒子近似 |
| 网格渲染（`TypeDataMesh`） | 42 | 骑砍粒子层没有网格渲染器 → 用**替身网格**（mesh + shader）另做（§3.6） |
| 丝带 / 光束 / 动画拖尾（`Ribbon`/`Beam2`/`AnimTrail`） | 13 / 5 / 8 | 骑砍无对应 → 只能用**串联精灵**或替身网格近似 |
| 矢量场（`VectorFieldLocal`） | 20 | 骑砍粒子层无矢量场 → 丢弃，用湍流/初速近似 |
| 碰撞 / 事件（`Collision`/`EventGenerator`） | 19 / 2 | 丢弃（骑砍粒子不做碰撞与粒子间事件） |
| **材质观感**（自定义 shader / HDR / 泛光） | 全部 | ❌ **不可复刻**（§三、§8.7、§八末）—— 这是"像不像"的最大瓶颈，不是参数问题 |

### 11.9 我这边的就绪状态

| 件 | 状态 |
|---|---|
| Cascade 解析器 | ✅ 新写 `cascade_breakdown.py`，本工程 34 个跑通（198 emitter / 2381 模块 / 0 失败） |
| 转换器（Cascade 分支） | ✅ 已有 `pipeline/ue2bannerlord.py` |
| 骑砍 XML 生成 + 校验 | ✅ 已有 `gen_particle_effect.py` + `validate_xml.py` |
| 离线预览 | ✅ 已有 `preview/`（three.js，可出 PNG） |
| 🔴 **实机验证** | ❌ **一次都没做过**（§8.11 第一条仍未销）—— 拿新素材之前，建议先花一次把已有产物播一个进游戏，确认"这条路是活的" |

---

## 十二、🔴 离线编译粒子资产 —— 不开编辑器也能造出引擎认的粒子（2026-09-24 打通）

**一句话**：粒子的**运行期形态**是 tpac 里的二进制 `Particle` 资产。现在有一条**纯离线**的路把它造出来：
`XML` →（`tpaccli particleimport`）→ `<名字>_psys.tpac` → 丢进 `Assets/particles/`（编辑器看）或 `AssetPackages/`（游戏用）。
**已验**：编辑器里打开 `lwn_yinmo_trail` → **5 个 emitter 标签齐**（`crescent_edge`/`core_bead`/`embers`/`smoke_puffs`/`magenta_ribbons`）、
Inspector 参数正确、预览在跑；游戏侧包也已投进 `Taikou/AssetPackages/`。

### 12.1 为什么必须离线造（三条结论，别再重推）

| # | 事实 | 证据 |
|---|---|---|
| 1 | 引擎**运行期**的粒子表**只来自 tpac 里的 `Particle` 资产**；`ModuleData` 的 XML + mbproj 注册**只服务编辑器** | 注册得好好的 XML，实机 `GetRuntimeIdByName` = **-1**（零报错、静默失败）；原版包 **208 个粒子资产 ↔ XML 218 个 effect** |
| 2 | 编辑器里「Source particle system」下拉 / 资源浏览器能看到的，就是**已编译成资产**的粒子 | §1.5 实测小节；编辑器日志 `Loading packages …/Assets…` |
| 3 | 所以"写 XML 就完事"不成立 —— 中间必须有一道**编译成资产**的工序 | 本节 |

### 12.2 链路与命令

```bash
# ① 出【编辑器】用的（一个粒子一个文件；目录必须是当前态的 Assets/particles/）
tpaccli particleimport --xml <我们的XML> --out <模块>/Assets/particles \
        --packdir <原版包目录> --split

# ② 出【游戏】用的（一个包装多个 effect）
tpaccli particleimport --xml <我们的XML> --out <出包目录> \
        --packname lwn_yinmo_prt.tpac --packdir <原版包目录>
#   → 产物拷进 Modules/<内容包>/AssetPackages/（🔴 别覆盖内容包自有的 pack0.tpac）

# ③ 对照/二分用：把原版粒子**原样**搬运成我们的名字（数据一字节不改）
tpaccli particleimport --clone psys_game_blood_1 --newname lwn_clone_blood1 --out <目录> --packdir <原版包目录>

# 读回对账 / 写侧闸门
tpaccli prtdump     --packdir <目录> --filter lwn_yinmo
tpaccli prtroundtrip --packdir <原版包目录> --filter psys_
```

- `--template <原版粒子名>`（默认 `psys_game_blood_1`）= **骨架**：XML 里没映射到的字段**全部继承它**。
- 🔴 **输出目录闸门**：目录不存在就 `[STOP]`（要新建必须显式 `--mkdir`）—— 因为模块资产有
  **`Assets`（编辑器态）/ `Assets_disabled`（游戏态）两态**，写错名字以前会"悄悄新建一个没人读的目录"。

### 12.3 资产长什么样（结构 + 校验）

```
tpac 包 = 头(36B) + 资产记录… + 数据段…
  头         : magic "TPAC" / 版本(2) / 包 guid / 资产数 / 数据段区偏移 / 保留
  资产记录   : type guid | guid | version | name(sized string) | 元数据(长度+字节)
               | 8 字节 checksum | 段数 | 段头…
  段头       : offset | actualSize | storageSize | **owner guid** | type guid
               | **XxHash64(未压缩数据)** | **1**（常规段） | 压缩格式(LZ4HC=1)
  数据段     : 粒子效果数据（emitter 列表，见 §12.4）
```

- **`owner guid` 必须 == 资产自己的 guid**（原版实证）—— 空 = 引擎找得到资产、**读不到数据**。
- **8 字节校验 = `XxHash64(未压缩数据)`，种子 0**（实测原版段算出来逐位相同；实现见 `TpacTool.Lib/Util/XxHash64.cs`）。
- 资产生成后**外观三件**要从模板抄：`version` / 元数据字节 / 那个 8 字节 checksum（`AssetItem.CopyShellFrom`）。

### 12.4 XML 参数 → 二进制字段（已钉死的映射）

**emitter 内的曲线槽位**（`EmitterParameter` 的 `UnknownFloat1/2` = 该参数的 **base / bias**，`Curve` = 它的曲线）：

| XML 参数 | 二进制位置 | 证据（多样本对上） |
|---|---|---|
| `emitter_life` | `F1` | 1.000 / 1.5 / 0.1 |
| `skew_with_particle_velocity_coef` | `F4` = **XML 值 ÷ 500** | 8→0.016、100→0.2、200→0.4、400→0.8 |
| `skew_with_particle_velocity_limit` | `F5` | 2 / 9 / 0 |
| `inherit_emitter_velocity` | `F7` | 1 / 1 / 0 |
| `fadeout_distance` | `F9` | 0.005 / 1.0 / 0.05 |
| `initial_rotation` base / bias | `F22` / `F23` | 0/180、0/360、0/30 |
| `backlight/diffuse/emissive/heatmap_multiplier` | 四个同名字段 | 0.35 / 0.5 / 60 |
| `particle_size` base / bias | `ParticleSizeBase` / `ParticleSizeBias` | 0.9/0.8、1.0/0.15 |
| `particle_size` 的曲线 | **`curve7`**（`name="emitter_life"`）/**`curve8`**（`name="particle_life"`，blood_1 走这条） | mult 0.2/0.6/1.0 + keys 逐点 |
| `cone_emit_velocity` | `curve3` | base 1.0/0.4 |
| `damping` | `curve4` | 1.0/0.62/0.5/0.72 + 曲线 |
| `angular_damping` | `curve5` | 0.599 / 0.5+0.3 / 0.78 |
| `particle_life` | `curve6` | 1.0+0.1 / 2.4+0.9 |
| `emission_rate` | `curve9` | 10 / 1 / 3 / 25 / 50 |
| `emit_velocity_x/y/z` | `curve10/11/12` | 2.5+0.1 / 0+0.2 / 2.0+0.4 |
| `emit_rotation_speed` | `curve13` | 286.479 / 57.296 / 30 |
| `wind_effect` | `curve14` | base 1.0/0.0 + keys |
| `material` | **`G4`**（按名字查原版材质资产的 GUID） | 解析成 `prt_shd_blood_4` / `prt_blood_splatter` / `prt_shd_dust` |
| `gravity` / `fixed_billboard_direction` | 两个 vec4 字段（**存整 4 分量**，第 4 位常是 1.0） | (0,0,-9.8) / (0,0,1) |
| `texture_sprite_count`(x,y) / `frame_count` / `frame_rate` | 三个同名字段 | 8,8 / 64 / 64 |
| `decal_min/max_scale`、`skinned_decal_start/end_index`、`max_alive_particle_count`、`quad_scale/bias` | 同名 | ✅ |
| `particle_size_curve_op`、`emit_volume_type`、`billboard_type`、`collision_behaviour`、`emission_velocity_model` | 同名字段 | ✅ |
| `particle_color` 的 color/alpha 关键帧 | 同名结构（`<key>` 在 `<keys>` **里面**，取键要用 `Descendants`） | 逐点对上（含 0.392 这种尾数） |
| **flags** | 只存**值为 true** 的那几个名字（字符串表） | blood_1 三个 flag 完全对上 |
| `S4`（编辑器里灰掉的 "Parent activation event"） | `S4` | 继承骨架的 `particle_spawn` |

**曲线编码**：`<key time value tangent="x, y"/>` → 二进制里**一个 vec4 = (time, value, tanX, tanY)**；
计数 = **vec4 数 ÷ 2**（引擎按"每两个 vec4 一段"写）⇒ 奇数个键要补一个尾巴（工具已处理）。

#### 12.4.1 🔴 关联求解的补全（2026-09-24 · 405 个 emitter 全样本比对）

方法：把原版 **208 个粒子的全部二进制字段** 与 **218 个 effect 的 XML 参数** 做全样本同值比对
（按 emitter **名字**对位，跳过带子发射器的 effect —— 那种 XML 文档序 ≠ 二进制序）。得出的新增映射：

| 二进制字段 | XML 参数 | 命中率 |
|---|---|---|
| `F2` / `F8` / `F29` | **`fixed_particle_initial_speed`** / 同上 / 同上（三个槽都指向它，多半是同一语义的三个副本） | 401~405/405 |
| `F6` | **`scale_with_emitter_velocity_coef`** | **405/405** |
| `F10` / `F11` | **`fadeout_coef`** / **`camera_fadeout_coef`** | 401 / 398 /405 |
| `F16` / `F17` | **`collision_damping`** / **`collision_angular_damping`** | 399 / 398 /405 |
| `F32` | **`emission_speed_limit`** | 403/405 |
| **`V3`** | **`emit_volume`**（三个 vec 槽里就是它） | 381/405 |
| `F4` | **`skew_with_particle_velocity_coef ÷ 500`**（用数据坐实了那条 ÷500） | 358/405 |
| `Curve8.CurveMultiplier` | `particle_size` 曲线的倍率（curve8 = 尺寸曲线 ✓） | 334/405 |
| `QuadScale` / `QuadBias` / `DecalMinScale` / `DecalMaxScale` | 同名 | 394~404/405 |
| `BillboardType` / `EmitVolumeType` / `CollisionBehaviour` / `ParticleSizeCurveOp` / `EmissionVelocityModel` | 同名（字符串直比） | 369~405/405 |
| `TextureSpriteCountX` / `Y` / `FrameCount` | `texture_sprite_count` 的第 1/2 个数、`texture_sprite_frame_count` | 384~390/405 |
| `S3` / `S4` | **XML 里没有对应项**（`S4` 恒为 `particle_spawn` —— 编辑器里那个灰掉的 "Parent activation event"，属内部/继承） | — |

> 命中率不满 100% 的原因：**ModuleData 的 XML 与发布包不是同一版**（实测 `psys_game_metal_metal_coll`
> XML 写 `damping 0.780` 而包里是 `0.8`）—— 少数样本天然对不上，不影响唯一性判定。

**还剩这些关联法解不出**（原版里**恒为 0**，没有非默认样本可比）：`F3` `F19` `F20` `F21` `F24` `F25` `F26` `F27` `F31`
`U2~U8` `I1~I5` `V1` `V2` `V4` `Curve1` 两个 float `Curve7.CurveMultiplier`。
🔴 **指纹探针**（`particleimport --probe <名字>`）：给每个这类字段填一个**独一无二**的值造一个粒子，
在编辑器里打开 —— **哪个面板标签显示我填的那个数，那个字段就是谁**（编辑器即解码器）。
探针值表在命令输出里；`Curve15` / `S3` 在 `subVersion=0` 的布局里不存在，探针测不到（要另换 subVersion≥1 的骨架）。

### 12.5 九个坑（按踩到的顺序）

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 1 | 名字**在**、**emitter 全空**（真凶一） | 段的 `owner guid` 是空 —— 库新建 loader 时置 `Guid.Empty`（作者注释"随机 guid 没意义"），而原版是 **owner == 资产 guid** | 建段时 `loader.OwnerGuid = asset.Guid` |
| 2 | 同上症状（真凶二） | 段头 8 字节**数据校验** = `XxHash64(未压缩数据)`，新建时是 0 → 引擎校验不过 | `SaveTo` 里算（`XxHash64.cs`）；判据 = 真包重存**逐字节一致** |
| 3 | `Save` 抛 `NotImplementedException` | `AssetItem.WriteMetadata()` 对"新建且没元数据"的资产没实现 | `CopyShellFrom(模板)` 抄 version + 元数据字节 + checksum |
| 4 | 克隆/另存粒子得到 **195 字节空壳**（`fight_tail` 的来历） | `ParticleEffectData` **重写了读侧却没写侧** → 基类写 `_unknownRawData`（空） | 读时留存 `RawData`，`WriteData` 优先原样写回 |
| 5 | 🔴 写进去"没人读"（我自己的蠢事） | `Assets` / `Assets_disabled` 是**同一个文件夹的两个名字**（编辑器态/游戏态，靠 bat 改名切换），我照抄了旧路径；`Directory.CreateDirectory` **静默建目录**把错误藏了 | 工具改成**目录不存在就 [STOP]**；写盘前**先解析当前态** |
| 6 | 改了同名文件，编辑器里还是旧的 | 编辑器**缓存**资产 | 重开编辑器 |
| 7 | （历史）拿 `particle_systems2.xml` 当模板 | 那是**未注册死档**，编辑器也不看 | 见 §1.6；只拿注册过的当骨架 |
| 8 | 🔴 **ModKit 直接起不来**：`Assertion Failed! rglAsset_package.h:589 Unable to serialize int value` | 指纹探针往**未解析的 int 字段**（`U2~U8`/`I1~I5`）填了**越界值**（22~30、-3/-4）—— 那些多半是枚举/索引，编辑器的包序列化器写不出来当场断言 | **int 字段不许乱填**：要探 int 必须一次一个、且只用原版出现过的取值。**救法 = 删掉那个探针 tpac**（`Assets/particles/<探针名>_psys.tpac`） |
| 9 | 🔴 **编辑器一打开就断言**：`Assertion Failed! C:\Develop\MB3\…\FairyTale.Library\rglBuffer.cpp:899` / `Expression: (rglMath::nearly_equals(vector->w, 1.0f)) && "Potential read/write miss match for rglVec3"` | 探针往**向量槽的第 4 位**填了非 1.0 的值（把 w 也当探针数字用了：V1.w=32、V2.w=34、V4.w=36）。引擎把这 8 个 `Vector4` 槽（`V1/V2/V3/V4/V5/gravity/V7/fixedBillboardDir`）按 **`rglVec3`** 读写，**w 恒为 1.0**（原版样本无一例外） | **向量只填 x/y/z，w 一律留 1.0**；写盘前过 `Emitter.Validate()`（2026-09-24 已加闸门）。**救法 = 删掉那个探针 tpac**，重开编辑器（缓存） |

### 12.6 还没钉死的三处（动手前先看这条）

| 项 | 现状 | 怎么办 |
|---|---|---|
| `activation_delay` | **不在二进制里** —— 拿"只差这一个参数"的一对发射器逐字段比过，**没有任何字段跟着变**；而 roundtrip 又证明解析器吃满了整个 blob | 蓄力「壳层接力」得换机制（改发射窗/多次 spawn） |
| `emit_sphere_radius` | ✅ **定案（2026-09-24 晚）**：Sphere radius 的值落在 `subVersion 3` 的**四连排新槽**（`Sphere radius / Sphere inner / Disc radius / Disc inner`，见 §12.8）。之前"所有原版样本都是默认值"的真因 = **原版那批文件是 subVersion 0，压根没这些槽**，不是我们没找对 | 要写它 ⇒ 我们的写出器得升到 subVersion 3（**T8**） |
| `emit_volume` | 同上口径：旧布局里 = `V3`（§12.4.1 已坐实）；新版里 Emission offset/rotation 另有槽（§12.8） | 同 T8 |
| 段头那个 4 字节 `uui` | 常规段恒为 **1**；另有一类段是 0（`actualSize` 固定 1048614，疑似大数据占位段） | 我们一律写 1 |

### 12.7 自检纪律（这套东西的正确性全靠它，别省）

| 检查 | 命令 | 合格线 |
|---|---|---|
| 写侧闸门 | `prtroundtrip --packdir <原版包> --filter psys_` | **187/208 逐字节一致**（21 个不一致全是新版天气/破坏类布局，与我们无关） |
| 包保真 | `roundtrip --packdir <原版包> --filter <某包>` + 人工 diff | 逐字节一致 |
| 内容对账 | `prtdump --packdir <产出目录>` | 材质 GUID / 发射率 / 尺寸 / 颜色 / 曲线**逐条与 XML 对上** |
| 负面测试 | `particleimport --out <不存在的目录>` | 必须 `[STOP]`、**不建目录、不写文件** |

---

### 12.8 🔴 编辑器写出来的是 `subVersion 3` —— 那批"面板有、原版没实例"的字段全在这个新布局里（2026-09-24 晚，用户存盘实证）

**怎么发现的**：让用户在编辑器里给 `lwn_savetest`（阴魔斩拖尾的原样克隆）**新建一条 emitter `savetest`**、按值表填 17 个标签、Save。然后看文件：

| 观测 | 结论 |
|---|---|
| 文件里 6 条 emitter 都在（5 旧 + 新 `savetest`），用户填的 **30 个值一个不少** | ✅ **编辑器写路径可信** —— 路线 B（在编辑器里改/造粒子资产）成立 |
| **所有 emitter 的 `subVersion` 都变成 3**（原版 Native、我们的产物都是 **0**） | 编辑器按**它自己的当前格式**整份重写；`subVersion` 就是格式版本号 |
| 用户填的那 17 个标签，**全部落在几个连续的 float 槽上**（下表） | 🔴 **`Sphere radius` / `Radial Velocity` / `Local Force` / `Warmup time` / 发射速度阈值 / `Turbulence strength` / `Camera far coef` / `Use Normal Vector As Alpha` 都是 subVersion 3 才有的新槽** |

⇒ **§12.6 那两条"钉不死"的真因**：不是没找对，是**原版那 208 个粒子全是 subVersion 0，这些槽根本不存在**（所以"原版样本恒为默认值"）。面板是编辑器的（新格式），XML/二进制是旧的（老格式），两边自然对不上。

**值 → 偏移（`lwn_savetest.raw`，解压后 7631 字节；偏移是 blob 内绝对偏移，全在最后那条 `savetest` emitter 里）**

| 面板标签 | 填的值 | 偏移 | 新旧 |
|---|---|---|---|
| Camera distance fadeout far coefficient | 45.5 | 6758 | 🆕 3 |
| Use Normal Vector As Alpha → 系数（另存 flag `normal_vector_z_as_alpha`） | 46.5 | 6762 | 🆕 3 |
| Emit while moving speed threshold | 40.5 | 6798 | 🆕 3 |
| Warmup time | 41.5 | 6802 | 🆕 3 |
| Turbulence strength（base/bias） | 36.5 / 36.75 | 6953 / 6957 | 🆕 3 |
| Damping（base/bias） | 26.5 / 26.75 | 7017 / 7021 | 旧（= curve4，位置没变） |
| Particle size（base/bias） | 14.5 / 14.75 | 7113 / 7117 | 旧（位置没变） |
| Radial Velocity（base/bias） | 35.5 / 35.75 | 7393 / 7397 | 🆕 3 |
| **Sphere radius** | 39.5 | **7455** | 🆕 3 **四连排** |
| Sphere inner radius | 42.5 | 7459 | 🆕 3 |
| Disc radius | 43.5 | 7463 | 🆕 3 |
| Disc inner radius | 44.5 | 7467 | 🆕 3 |
| Emission offset X / Y / Z | 51.5 / 52.5 / 53.5 | 7471 / 7475 / 7479 | 🆕 3 |
| Emission rotation X / Y / Z | 54.5 / 55.5 / 56.5 | 7487 / 7491 / 7495 | 🆕 3 |
| Gravity X / Y / Z | 28.5 / 29.5 / 30.5 | 7503 / 7507 / 7511 | 旧（位置后移） |
| Local Force X / Y / Z | 57.5 / 58.5 / 59.5 | 7519 / 7523 / 7527 | 🆕 3 |
| Quad scale X / Y | 32.5 / 32.75 | 7591 / 7595 | 旧 |

> 值的归属来自"我给的填值表"（每个标签一个独一无二的数），偏移是文件里的实测事实；截图核对见当次会话。

**复现手法（值得抄）**：`prtdump --filter <名>,--raw:<目录>` → 把**解压后的原始数据段**整个倒出来（`ParticleDump` 的 `--raw:` 开关，2026-09-24 加），然后用"扫 float 模式找偏移"的办法反查某个面板值落在哪 —— **不需要先读懂布局**。
🔴 **同一个 effect 有两份格式版本**（我们的 subVersion 0 vs 编辑器存的 3）时，逐偏移对齐就是**逆出新布局**的现成路子 —— 旧 5 条 emitter 两份里都有，是同源数据。

#### 老数据保真（同文件对照实测，2026-09-24 晚）

拿"拖尾原文（我们写的 subVersion 0）"与"编辑器重写后（subVersion 3）"对着扫：

| 查什么 | 结果 |
|---|---|
| 旧 5 条的标志值 | `F23=180` ×5（正好 5 条，新的 `savetest` 那条是 0 ✓）· `F8=0.94` ×1 · 曲线倍率 0.04 ×1 · `0.02/0.03/0.09/0.32` 命中数与原文一致 |
| 字符串（flags / 曲线操作） | 编辑器版 = 原文的 3 个 flag 串 **×5 一字不差** + 只为新 emitter **多出** `normal_vector_z_as_alpha` 与 `multiply` 两条；`add` ×5 不变 |

⇒ **编辑器的整份重写是"无损迁移"**（只换格式版本，值/字符串照搬）—— **在编辑器里改我们的粒子是安全的**（路线 B 的安全前提）。

#### 两条实操坑（"面板上找不到某个框"的两个真因）

| # | 症状 | 真因 |
|---|---|---|
| 1 | 按文档里的字段名找不到 | **面板标签被截断**：`Camera distance fadeout far coefficient` 显示成 `Camera distance fadeout far coeflic…` —— 按**截断后的前缀**找 |
| 2 | 框在那儿但是灰的 | **可用性被别的设置门控**：`Emit volume type = disc` ⇒ `Sphere radius / Sphere inner radius` 变灰（`box` ⇒ Disc 那两格灰）；`Cone Angle` / `Emission Velocity` 随 `Emit Velocity Model` 灰。**要填某一组就得先把它的门控开关拨过去**（用户实测：改成 disc 才能填 disc 参数） |

**新建 emitter 的编辑器默认值**（照抄参照用）：Emission rate 4.00 · Emission loop 1.000 · Emitter life 0 · Billboard type **3d** · Particle size curve operation **multiply** · Texture sprite type **none** · Material 空 · 无 flag · Collision 全关。

**下一步（T8）**：把读写器升到 **subVersion 3**。升完 ① 那批新字段（Sphere radius / Radial Velocity / Local Force…）就能在 XML 里写、离线编译 ② 蓄力的「真·径向内聚」才谈得上。

#### ✅ 运行时验收：游戏**接受**新格式（2026-09-24 晚，实机）

| 命令 | 返回 |
|---|---|
| `custom.ninja_report lwn_savetest` | `success [note: 'lwn_savetest' -> id 22]` —— **不是 -1** |
| `custom.ninja_report lwn_yinmo_trail` | `success [note: 'lwn_yinmo_trail' -> id 25]`（对照组：旧格式、已知能跑） |
| 画面 | 特效正常生成，与 ModKit 里看到的一致 |

⇒ **运行时认编辑器写的 subVersion 3 资产**。**T8 方向确认**（写新格式游戏吃得下），剩下的只是把布局反解出来。
⚠️ 注意区分"认得名字"与"数据解析成功"：名字认得 = 资产注册成功；数据能不能跑出来看画面（本条两者都过）。

**验收套路（可复用，以后验任何粒子资产是否进游戏）**：
① 把 `<名字>_psys.tpac` 拷进 `Modules/<内容包>/AssetPackages/`（**别覆盖同名自有包如 pack0.tpac**；模块必须是**游戏态** = 目录里没有 `Assets/` 而是 `Assets_disabled/`）
② 进战斗场景（命令只在 Mission 里生效）③ `custom.ninja_report <名字>` ④ **读返回文字里的 id**：`>= 0` = 认得；`-1` + `fell back to 'psys_game_boulder_stone_coll'` = 没认。
🔴 **`-1` 时会回落放一发"巨石撞石"（大白烟 + 碎石）—— 那是回落效果，别当成被测粒子**（2026-09-24 我就是这么误判过一轮）。

**顺带修的坑：`custom.ninja_report` 的参数位（2026-09-24）** —— 原实现 `if (strings.Count == 2) name = strings[1]`（要求**恰好 2 个**参数、名字放第 2 格），于是 `custom.ninja_report lwn_xxx`（1 个参数）**静默用默认效果**。已改成「首参可弃：取最后一个参数 + 把解析结果打进返回文字」（`Debug/MyCommands.cs`）。
⇒ **通用教训：`custom.*` 命令一律照 CLAUDE.md 的「首参可弃」办，禁止写成"恰好 N 个参数"** —— 那种写法在参数数不符时会静默走默认分支，调试时极难发现。
