# 太阁5 剧本初始化机制与 Snr 文件解码分析

> ⏮️ **数据引用导览（2026-09-01 新增：六剧本年代的人物/势力/城池归属去哪里查）** —— 六个剧本年代目录：`骑砍2织丰角色ID对应/_analysis/decoded/era_v2/{1554, 1560, 1568, 1575, 1582, 1598}/`（年代=剧本起始年）。

| 要查什么 | 路径 | 关键列 |
|---|---|---|
| 人物归属（某人当年属谁的势力、身份等级、上司、俸禄/野心/忠诚/家纹） | `{年代}/persons.csv` | person_id=人物号(character id)；force_id/force_name=所属势力；rank=身份等级；superior_id/superior_name=上司；salary/ambition/loyalty/kamon |
| 势力归属（某势力当主是谁、部众多少人） | `{年代}/forces.csv` | force_id/force_name=势力；lord_pid/lord_name=当主；member_count=成员数 |
| 城池归属（某城/町/砦当年城主、属哪家、兵粮金钱粮） | `{年代}/cities.csv` | city_idx=城序号(0-179城/180-245町/246-273里砦)；lord_name=城主；force_id/force_name=归属势力；soldiers/food/gold/train/morale |
| 人物号 ↔ 内存编辑器名单名（同号对照） | `_analysis/人物ID对照表_20260901.csv` | ID/character名单名/内存编辑器名单名 |
| 人物号 800-1056 三 AI 识别基准（claudecode 列为准） | `_analysis/人物ID_820_1056_三方识别_20260831.csv` | id/豆包/ds/claudecode |
| 800-1056 段 × 6 年代提取（person 一览） | `_analysis/decoded/era_v2/person_era_info_20260901.csv` | 截图id/基准名/年代/世界表pid/force/rank/superior/... |

> ⌛ **2026-08-31 破译会话总结（阶段性成果，写于用户提交前）** —— 本会话把"六剧本全量隶属关系"从猜测式解析升级为**确定性导出**，以下结论已被用户逐条校验/拍板，改数据=改事实表重跑，禁止回归到"投票/匹配"思路。

## 0.1 本会话裁定与结论速查（用户逐条拍板）

1. **城表结构（终版）**：180 条 × 36 字节等距连续区（记录 = 前2 类型 + 城主u16@+2 + 士兵u16@+4 + 兵粮u32 + 资金u32 + 训练u8 + 士气u8 + 城名编码4B@+0x18 + 元数据12B）。**禁止再全文数值扫描定位**（教训：v1 扫描 + 6B 指纹曾把 1568 骏府城误判为"不存在"）。
2. **城名 = 官方名单直取（0 偏移）**：`names_180.txt`（180 城，序号=城表序号）。**禁止投票/匹配/推测**（用户 2026-08-31 裁定：这些都是确定性事实，一次性读表即可）。
3. **名单真值修正（用户查证拍板）**：87=岩村城(远山景任) / 88=曾根城(稻叶一铁) / 89=松仓城(姊小路赖纲) / 90=安浓津城(神户具盛)。此前的"72..88 段 ±1 偏移规则"因抓取的论坛名单错位而生，**已删除**。
4. **1568 骏府城 = 武田信玄（9200 兵）**——用户游戏实机确认；全链：1549/1560 今川义元 → 1568/1575 武田信玄→胜赖 → 1582 穴山梅雪 → 1598 中村一氏。
5. **name_official = 该剧本的当代名**（随改名事件落代）：改名事件表 `RENAME_EPOCHS`（稻叶山→岐阜 1567；今滨→长滨 1573；曳马→滨松 1570；石山本愿寺→大坂 1583；观音寺→安土 1576→水口 1595；阪本→大津 1595；不来方→盛冈 1592；胜山馆→松前 1594）。
6. **name_history = 全量历史名集（含当前名，| 分隔）**：`稻叶山城|岐阜城`、`观音寺城|安土城|水口城`……出自 `RENAME_FACTS`（史料明确记载才收；删除了无把握条目）。
7. **势力**：force ID **跨时代复用**（25=斋藤(1560)→德川(1568+)；12=今川(~1575)→宇喜多(1582)）→ 势力名必须 per-era 命名（Kingdom 列多数票——⚠️ 该多数票是当前链条**唯一推断点**，待改)；当主 = 该 force 城表城主中主城兵最大者。
8. **输出产物**（全部为生成物，改=改脚本重跑，禁止手改）：`_analysis/decoded/era_v2/{1554,1560,1568,1575,1582,1598}/persons.csv | cities.csv | forces.csv`（cities 列：city_idx, kana4, name_official, name_history, lord_pid, lord_name, force_id, force_name, soldiers, food, gold, train, morale）+ `city_lore_match.csv`（城位→历史名集→六代城主链）+ `taikou5_eras.html`（浏览器可读成品，6 年代切换/势力筛选）。
9. **路径迁移（用户要求）**：`_analysis` 已从 `E:\taikou5\...\` **剪切**至 `Knowledge\骑砍2织丰角色ID对应\_analysis\`（原版游戏留在 E 盘未动）；脚本读游戏 `TW\Snr*.TR5` 的路径已改为 E 盘绝对路径，迁移后重跑验证通过。
10. **待办（未完成）**：① **町/里/砦表解析**——已定位城表后 0x3B88 起、184B/条 记录簇（町 84/里 216/砦），名单对接未做，用户已确认"继续做"；② kana4（城名编码 4B）语义未破；③ force 多数票替换为确定性方案。

## 0.2 生成链（单一入口）

```
build_v2_era.py（Snr 解码 → 人物/城表直读 + 官方名单/事件表 join → 6 年代 CSV）
build_viewer.py（CSV → 单文件 HTML 仪表盘）
```
事实表输入：`names_180.txt`（城名官方名单）、`RENAME_FACTS`（历史名集）、`RENAME_EPOCHS`（改名事件年份）——改这些表 = 改事实，重跑生效。

---

> 背景：剧本工程（scenario-campaign-mode / 06-玩家身份与时代重置）需要"不同年代下每个人的归属、每个城池的归属王国、人物出生情况"作为时代初始化数据。本文是太阁立志传5（2004 PC 原版，本文分析的为绿色版 `Taikou5脱壳原版.exe`）年代剧本初始化机制的完整逆向结论，含可复现的解码方法。
>
> **一句话结论**：TK5 的年代初始化 = **每年代一份"世界完整快照"二进制文件**；文件在字节级用**每年代一张 256 字节逐字节替换表（S-box）**混淆；解密表即社区编辑器（剑阁编辑器 / 3DM 综合编辑器 v1.24）附带的 `EnCode.rtg`/`DeCode.rtg`，已用它解出全部 6 个年代的初始状态数据。

---

## 1. 数据文件体系（`TW\` 目录）

| 文件 | 内容 |
|---|---|
| `TW/Snr0_TW.TR5` ~ `TW/Snr5_TW.TR5`（各约 153KB） | 6 个剧本（年代）的**初始状态快照**——本分析的核心 |
| `TW/TR5MSG00.TR5` ~ `TR5MSG05.TR5` | 与 6 剧本一一配套的文本包（章节名/开场） |
| `TW/tr5msg06-24.tr5` | 其他文本（人物列传 0-199/200-399/…、物品/卡/对话） |
| `SbCdInit.tr5` / `SpCdInit.tr5` | 初始卡分配 |
| `TW/INIT.TG5`、`Scenario.tg5`、`TITLE.TG5` 等 | 主数据/图形包（`.TG5` 格式，`TK5G` 魔数，另族混淆） |

文件名证据（反编译 exe 字符串，ASCII）：`TW\Snr%d_TW.TR5`、`TR5SCENARIO%05d`、`TAI5TAI5SCENARIO%d`、`TW\TR5MSG??.TR5`、`TW\CWTDAT.TR5`、`M_FC??.tr5`、`TW\*.TG5`、`*.TG5`。

**剧本 → 年代映射**（社区多方对照确认，攻略/DX 版资料）：

| 剧本号 | 年代 | 章节名 | 标志事件 |
|---|---|---|---|
| 0 | 1554 | 乱麻之章 | 三国同盟 |
| 1 | 1560 | 日轮之章 | 桶狭间 |
| 2 | 1568 | 升龙之章 | 上洛/天下布武 |
| 3 | 1575 | 霸道之章 | 长篠 |
| 4 | 1582 | 转变之章 | 本能寺 |
| 5 | 1598 | 太平之章 | 秀吉殁 |

## 2. 加载链（脱壳原版 exe 反编译结论，地址均 `Taikou5脱壳原版.exe` 内 VA）

```
[新游戏选剧本] → sprintf("TW\Snr%d_TW.TR5", 剧本号)
 → 打开文件 → 取 size → malloc → 全量 fread → [对象 vtable hook = 解码]
 → 流式读取，offset += 0x10（16 字节记录读取器）
```

关键函数（`_analysis/find_xrefs.py` / `xref.py` / `disasm_func.py` 输出，已确认）：

- `0x54b9a1` / `0x54bcc2` 附近：两个"加载剧本"包装函数，`sprintf("TW\Snr%d_TW.TR5", ...)` 后调用 `0x54b830`
- `0x54b830`：读 8 字节前缀（内层头）、`malloc(count*4+4)`、`0x54b730` 拷贝流数据
- `0x54b670`：打开(size/malloc/fread) + `call dword ptr [eax+4]`（vtable 钩子 = 解码入口；2004 年反调试特性，vtable 为运行时计算，静态搜不到）
- `0x54bad1` 附近：**头部校验函数**——读 16 字节 → `sprintf(%05d)` 比对 `TR5SCENARIO` → `offset += 0x10`

## 3. 加密算法 —— 单层 256 字节 S-box 逐字节替换（已完全破解）

### 3.1 判定过程（为什么不是压缩/异或）

1. 六个文件字节直方图**秩对齐**（最高频字节计数几乎相同：84402/84108/84056…，但**值不同**：0x49/0xC7/0x12/0xD4…）→ 排除固定异或/旋转/仿射（均保序），也非压缩（同一压缩器对近同数据产生近同 token）。
2. 全文件逐字节比对（Snr0 vs Snr1）几乎全不同，但六个文件**大小仅差 ±250 字节**、且 `EnCode` 表第 k 张的首字节恰好 == 第 k 剧本文件最高频字节（见下）→ 断定：**每年代一张字节置换表**，替换后"0x00=空值"频次反而均匀。
3. 十六进制观测细节：文件字节 `0x0F` 后第一区（第 2 层头）六文件完全相同或仅差 1 字节（场景号），证明非流式加密。

### 3.2 表来源与结构

剑阁编辑器（root重来作，3DM《太阁立志传5》综合编辑器 v1.24，下载 `https://dl1.wsyhn.com/tools/3DMGAME_《太阁立志传5》综合编辑器v1.24.rar`，56KB）附三个核心文件：

- `TK5SEditor.exe` —— VB6 编辑器本体
- `EnCode.rtg` / `DeCode.rtg` —— 各 **1536 字节 = 6 × 256 字节置换表**；**每张 256 字节是完整排列（全 256 值唯一），且 `DeCode[k][EnCode[k][x]] == x`（逆关系），即 chunk k = 剧本 k 的编码/解码 S-box 对**

判定铁证：`EnCode[k*256]`（即表 k 的 [] 0x00 项）== 剧本 k 原始文件的最高频字节：

| 剧本 | 文件冲线最高频字节 | EnCode 表 k[0x00] | 备注 |
|---|---|---|---|
| 0 | 0x49 | 0x49 | |
| 1 | 0xC7 | 0xC7 | |
| 2 / 4 | 0x12 | 0x12 | 表 2 ≡ 表 4 |
| 3 / 5 | 0xD4 | 0xD4 | 表 3 ≡ 表 5 |

### 3.3 解码方法（已验证，可复现）

```python
de  = open('DeCode.rtg','rb').read()          # 1536 字节
raw = open('Tw/Snr1_TW.TR5','rb').read()
# 剧本 1（1560）：
plain = bytes(de[1*256:(1+1)*256][b] for b in raw)   # 逐字节查表（注意可变大小文件）
```

验证：解码后最高频字节 = **0x00**（84,124 次）——"空值填充"回归，且文件头区域出现明文：

```
0x00: TR5SCENARIO00001        ← 明文外头（16 字节，未混淆）
0x10: TAI5SNR001 + 00 pad     ← 第 2 层头
0x29: TAI5TAI5SCENARIO1 + pad ← 第 2 层头
0x40+: 数据区（LE 16 位值表格：0x00a6/0x02b6/0x082e/… + 大量 0x00 填充 + SJIS/半角假名字符串）
```

产物已存：`E:\taikou5\Taikou5.Green.Edition-ALI213\Taikou5\_analysis\decoded\Snr0-5.plain`（每个约 153KB，全部 6 代已解码）。

### 3.4 存档无加密（2026-08-31 确认）

`SAVEDATA\SaveDat0.TR5`（399KB）**无 S-box、就是明文**（70% 字节为 0x00=空字段）。头部：`TAIKOU5WIN` 魔数 + [版本4][文件大小][校验][46]。`savedata.tr5`（24KB）与 exe 内嵌 24576 字节模板（VA 0x7018E0）**逐字节 24561/24576 一致**——存档 = 游戏内存状态块快照，模板即字段骨架。

### 3.5 人物记录 36 字节字段映射（三方验证：高亚男截图 × 存档 × Snr 快照）

人物属性表：每剧本一个定长数组，**记录 = 36 字节，基址 = 信长锚点反推**（Snr0=0x951F/Snr1=0x954C/Snr5=0x9540…）+ 36×人物ID；存档基址 0x6D8A。**字段**：

| 偏移 | 字段 | 验证值（1560 信长/秀吉） |
|---|---|---|
| +0x00~04 | 统率/武力/政治/智谋/魅力 | 96,70,97,91,99 / 80,56,88,96,93 |
| **+0x05** | **所属势力 ID**（19=织田；121=无势力/浪人/已死） | 19 / 19（1554 秀吉=121 浪人✓） |
| +0x06 | 直属编码（首领/直臣） | 0x42 信长 / 0xC2 秀吉 |
| +0x07 | 地位阶（随时代变，含义待定） | 0x10 / 0x11 |
| +0x09 | 身份类（实际枚举 ≠ 显示序号） | 信长 08 / 秀吉 02（家老长秀=02 说明更复杂） |
| +0x0A | 上司（1101=无） | ✓ |
| +0x0C | 俸禄（贯） | 0 / 5 ✓ |
| +0x0E | 野心/忠诚 | 100,100 / 98,100 ✓ |
| +0x10 | 家徽 | 39 / 41 ✓ |
| +0x11 | 常量 0x79 | — |
| +0x12-15 | 零 | — |
| +0x16-17 | 生没相关常量（信长0x065F/秀吉0x0649，跨时代不变；编码未破） | — |
| +0x1A-1B | 常量对（05 05 / 05 00…含义待定） | — |

**年龄公式**（游戏情报屏验证）：年龄 = 当前年 − 出生年 + 1（虚岁；信长 1534→1560 年 27 岁✓、秀吉 1536→25✓、长秀 1535→26✓）。

### 3.6 城池记录（城→城主映射，已验证）

城记录 = `[城主 u16 人物ID][士兵 u16][兵粮 u32][资金 u32][训练 u8][士气 u8]`……（城名/城源ID/铁炮/军马等字段随城记录类型变化）。例：1560 城主=信长 195 → 清洲（士兵7600/兵粮80000/资金17600/训50/士50）+ 一座 5800/60000/15800；城主=义元 110 → 骏府 7600/80000 + 冈崎 5800/60000（史实 ✓）。全代城表 6×~140 条。

### 3.7 势力表（势力ID→当主人物ID）

0~20 = 高亚男截图（0=赤松義祐8 / 12=今川義元110 / 19=織田信長195 …）；21+ 可由"城主都是各势力当主"顺推（城表城主即势力当主）。

**成品**：`_analysis/decoded/affiliations.csv`（8394 行）+ `tk5_era_init_v1.json`（6 时代 × 人/城/势力，写入脚本 `build_era_init_v1.py` / `extract_affiliations.py`）。

## 4. 数据内容语义（社区编辑器字段清单 = 独立第三方佐证）

剑阁编辑器"剧本文件编辑"界面可修改的字段（= 剧本文件的语义内容，社区十多年使用验证）：

- **人物**：姓名、编号、所属势力、上司、直属关系（首领/直臣/陪臣/寄骑/浪人）、身份（大名/城主/家老…足轻/浪人）、所在（城/町）、能力、情义/精神/主义/行为、初始卡
- **城**：大名当主、城主、城士兵数、防御
- **势力/外交**：外交关系、外交情感（停战/开战月数、感情）
- **经济**：商人（第一列=支店长，空则该支店不出现）、据点（其店所在）、御用商人、销售通路、物品所有者
- **其他**：官职及其所有者、武将流派、忍者众/海贼众名、家纹

→ **结论：Snr = 剧本的"世界完整初始快照"，非增量**。人物出生情况不逐代重列，而是通过**初始年龄/身份**随剧本变化实现（同一人物不同年代 = 快照不同）。

## 8. 城并合与映射数据工程（2026-08-31，已落地）

**规则（用户裁定，写死进生成脚本）**：

1. **N:1 并合必须过"地理邻接"检查**——同一织丰城承接多个 TK5 城时，各 TK5 城国名必须**同国或直接邻国**，否则报人工（2026-08-31 用户裁定；已验证 23 条 N:1 全部通过，唯一存疑 = 天雾城↔宇和岛城（赞歧↔伊予，织丰 nankai 无伊予实体城的无奈，IsMerge=1 可拆））。
2. **同名撞车优先级**：城名去"城"后相同 → 城/镇优先（大浦城(东北) vs 大浦(四国同名地)，正统城级优先承接桥）。
3. **文化片词典驱动**：`CultureCN/TK5Region` 一律查 `Culture.csv`（ChineseName=织丰文化名 ↔ OtherName=太阁大区名，9 主文化），查询缺失即报错。用户裁定三则：九州=西海(saikai)、四国=南海(nankai)、中国=山阳(sanyo)——与词典一致。
4. **名字查找契约**（CLAUDE.md 铁律 25）：`GetStringIdByName` 只查 `CNName+ScriptName+Alias`，不查 Name_YYYY；Alias 必须覆盖全部年代名（数据制备期完成）。

**成果**：`CityTaikou.csv` = 织丰主表（765 = Settlements 全量），桥列全保留（TK5_ID/TK5_Name/TK5_Country/IsMerge/Alias/TK5_merged）；1:1=131、N:1=23、纯织丰=611；孤儿 8 城已按建议并入住织丰城（出石→羽衣石城、白鹿→月山富田城、三原→吉田郡山城、山口→萩城、宇和岛→天雾城、久留米→柳川城、八代→岩尾城、佐伯→冈城）；母实体国名赋值 = 从 ID 前缀（castle_village_X / village_X）解析父级，子行国名继承父级 + Culture 一致性体检。生成链：`tk5_era_init_v1.json` + 旧表桥 → `rebuild_city_bridge.py` → CityTaikou.csv。

**🔴 遗留**：①`TK5_ID` 列目前是旧表自编号（1-180，非游戏城号；实证 = 高亚男截图 19 城顺序一致性仅 4/19）——待从 Snr 城表区提取真城号后替换，或按"解析城记录顺序"方案；②`Owner_YYYY/Soldiers_YYYY` 待"城配对"填（[城主名+规模档+文化片] 三键入）；③出生年精确编码（+0x16）未破——已用"虚岁公式+史实生年"闭合；④织丰表"今川义元/氏真"行 ID 疑似反了（lord_1_imagawa_katsumoto 挂在义元行）——待织丰侧勘误。

## 5. 待解 / 下一步

1. **数据区段级结构**（人物记录多少字节、字段偏移）——下一里程碑。三条路：
   - **存档对位法（推荐）**：游戏内 1560 开局即存档 → 用上表 S-box 试解（存档极可能同族）+ 用游戏内情报界面 2~3 条已知值锚定字段（如"织田信长：织田家/大名/居清洲"、"木下藤吉郎：织田家/足轻组头/清洲"）→ 锁定字段后套回全部 6 份 .plain。
   - **编辑器 UI**：运行 `TK5SEditor.exe` 看界面字段标签（第三方 exe 启动需用户授权；被 Claude Code 权限分类器拦截过一次）。
   - 内存导出（鸭神类修改器/游戏内 武将編集，存档改 `00000370 → FF` 解锁）。
2. **文本编码**：TK5 假名 1 字节化（"あいうえお"→ 单字节代码，假名 1 字节 + 汉字 2 字节 SJIS），名字等文本需五十音表才能转回可读——数值字段不受影响。社区已知此现象（"光荣把平假名替换为一字节的十六进制代码"）。
3. **其余文件的表分配**：chunk 0/2/3/5 分别对应哪类文件（tg5msg/列传/EXE 名称表等），以及 `TK5G` 魔数主数据包的另族格式——未解。
4. **"count*4+4" 前缀**：加载器 8 字节前缀读出的语义未定（0x54b830），不重要但不归零。

## 6. 对本 mod 剧本工程（LivingWorldNpcs scenario-campaign-mode）的意义

- **机制迁移**：年代初始化 = 每年代一份"快照数据包"（人归属 + 城归属 + 出生/年龄表），开局时整体覆盖运行时。TK5 用 6 独立文件；本 mod 拟用 JSON（每年代一包），引用一律 StringId（铁律 20）。
- **规模参考**：约 900 人物 + 全城归属 + 外交/官职/商人 ≈ 153KB/年代（二进制紧凑态）；JSON 态预计数百 KB/年代，可接受。
- **数据来源**：TK5 数据仅作机制与数量级参照；具体数值应取本工程 战国数据包（织丰主轴，StringId 体系），避免直接拷贝 TK5 数值（它含架空调味与史实偏差）。

## 7. 相关文件与工具

- 反编译工具脚本：`E:\taikou5\Taikou5.Green.Edition-ALI213\Taikou5\_analysis\`（find_xrefs.py / xref.py / xref_va.py / disasm_func.py / strings_scan.py）
- 编辑器包：`…\_analysis\editor124\`（TK5SEditor.exe + EnCode.rtg + DeCode.rtg + 说明文件.txt，GBK）
- 已解码数据：`…\_analysis\decoded\Snr0-5.plain`
- 对照产物：`…\_analysis\decoded\Snr%d.bin`（单层/双层变体，均为中间产物）

---

## 9. 数据资源总地图与解码胜利经验（2026-08-31 用户裁定：经验与结论固化于本文档）

> 全链路走通后的"哪张表以哪个为准 + 已验证结论"速查；细则互链：[07b 织丰表解读](../plans/scenario-campaign-mode/07b-织丰表解读-StringId覆盖率.md)（据点）、[08b 转化管线](../plans/scenario-campaign-mode/08b-转化器规格-自动化翻译流水线.md)。

### 9.1 数据资源总地图

| 表/产物 | 位置 | 存什么 | 纪律 |
|---|---|---|---|
| 织丰源 xlsx | `Knowledge/骑砍2织丰角色ID对应/骑砍2太阁Mod表_v1.xlsm.xlsx` | 上游（15+ 工作表） | 只作导入源；xlsx→csv 唯一解析器 = `tools/xlsx_to_csv.py` |
| csv/ 镜像 | `Knowledge/骑砍2织丰角色ID对应/csv/`（TaikouHero/item/ForceTaikou…） | 唯一读取源 | 禁手改；人填数据进生成器映射表 |
| Snr 解码产物 | `_analysis/decoded/era_v2/{1554,1560,1568}/` | 各年代快照（persons/settlements/force…） | 解码只读；人名列未接（§9.2） |
| 16a / entity_maps | `plans/scenario-campaign-mode/` | 词表翻译 / 名字→StringId（均生成物） | 改 = 生成器重跑（铁律 22） |

### 9.2 🔴 编号体系三套（禁混用——映射链只认第一套）

| 套 | 是什么 | 用途 | 警告 |
|---|---|---|---|
| **① 编辑器 TK5 编号** | `TaikouHero.csv` 的 `TK5编号` 列（编辑器红列表 996-1018…） | **唯一"人物书号"** | — |
| ② persons `person_id` | Snr 解码产物 | 只是**快照内存/存储偏移序** | **≠人物号**：拿它当 TK5 编号 = 全员错位（实证 2026-08-31：persons 996-999 = 熊野水军/甲贺众/户隐众，编辑器 996-1000 = 义元/家康/信长——同批 ID 完全不是一批人） |
| ③ 表行号 | CSV 第几行 | 无意义 | 不作标识 |

- **persons.csv 人名列 = `待替换(ID xxx)` 占位**（Snr 解码人名映射欠账）——禁止当人名用；persons 只配当"该时代有哪些人/在哪个势力"的快照户口。
- **验证法（编辑器对表法）**：编辑器截图（真实人名列表）↔ persons.csv 分段对照——错位即编号体系用错（本节就是这么抓出来的）。**同族适用于 §8 遗留①**（TK5_ID 列 = 旧表自编号 1-180 非游戏城号——同属编号体系混淆，待改提取真城号）。

### 9.3 据点/人物/组织的已验证结论（速查）

- **城**：`MatchSettlement` = 骑砍真城（99）；`NearSettlement` = 只有最近城（81）→ Match 命中用真 ID / 仅 Near 发布占位 `tk5_city_NNN` + 锚点（不得混用两列——08b 纪律 21）。后缀归一：城/之町/町/之砦/砦/館/港 全套注册同一 ID（冈崎城=冈崎之町）；判别 = 去后缀城名在不在表。成果：180 城 → 106 真 + 74 占位（8 座无锚点：出石/白鹿/三原/山口/宇和岛/久留米/八代/佐伯——§8 已并含）。
- **认错 ID 警示**：`lord_1_saito_dosan` = 太原雪斋（**不是**斋藤道三）；德川家康 = `lord_1_tokugawa`；今川氏真 = `lord_1_imagawa`；义元 = `lord_1_imagawa_katsumoto`（⚠️ §8 遗留④：织丰"义元/氏真"行疑似反挂，与本节一致）。
- **人物表**：李华梅 = TK5 编号 1290 = `lord_1_maria`（日文名 マリア，中文名只显示层）；`lord_t*` 前缀 = 模板 NPC（非真 Hero，铁律 8 平权）。**列错位事故**（2026-08-30）：镜像 CSV 曾被加列致数据右移（表头 128 vs 数据 126）——任何镜像字段读反常值，先验列数再推断语义；已修复 + 备份 `TaikouHero.csv.bak_colfix_20260830`。
- **组织**：`ForceTaikou` 202 条已分类（武家 132/商家 42/海贼 15/忍者 12）；用到 31 个组织 100% 命中；组织 StringId 列缺失 → 07 回填；Kingdom 表查无 = 组织 → `Org::` 占位（T3-预留）。

### 9.4 工程侧坑（全在 wheels 索引，此处只点名）

控制台指令签名 `List<string>`（string[] 启动崩）/ 指令返回值只准英文 / `Color` 属性不参与渲染（白屏坑，用 Brush）/ Gauntlet 无翻转 API（镜像 = Sprite UV 反转 + V 门面 2/3 参构造）/ `MB2_PATH` 以注册表为准（2026-08-31 实测 = 1.2.12 织丰环境）。活体验证法：`custom.*` 控制台驱动 + 编辑器截图对表——先单系统全绿再组合（12 分拆纪律）。
