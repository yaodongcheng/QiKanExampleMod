# 自定义世界内容包从零起步必备清单

> 🔴 **再做一个新世界观（新内容包）时，拿这份清单勾。** 全部条目 = Taikou 实机踩过的雷（雷 1~43）+ 织丰 65 条经验交叉对比。
>
> **文档分工（重要）**：本清单 = **导航式索引**（缺什么 → 症状信号 → 修法 → 深坑出处）。每条只留「够行动」的信息；
> 完整的反编译证据/调用链/踩坑过程在各自主档，遇疑先查它们：
> - 排雷全程实录 → `plans/太阁数据加载taikou-campaign-boot-20260907.md`（雷 1~36 全文，含证据行号）
> - 疑难杂症速查 → `plans/rules/pitfalls.md`（每坑：症状/根因/规避）
> - 已造轮子 → `plans/rules/wheels.md` + `wheels.d/`（terrain.md 地图资产 / campaign-mode.md 数据自给）
> - 织丰工程全清单 → `Knowledge/织丰自定义世界观经验.md`（65 条世界工程补丁 + 数据做派方法论）
> - 最小字段交集 → `Knowledge/内容包最小字段交集.md`
>
> 🔴 **维护纪律（CLAUDE.md「自定义世界坑点沉淀纪律」，2026-09-09 用户裁定）**：凡自定义世界相关新坑点/新雷，遇坑即登记——①雷对照总表追加一行（症状一句/根因一句/修法归属）②命中阶段清单 → 补「症状信号/勾选项」，让清单跟着经验升级；细节深挖写各自主档。**已用清单核查新世界时，新坑必须能挂到本清单某一条上——挂不上 = 清单有缺口 = 先补清单再写代码。**
>
> **总原则（动手前先记住三件事）**：
> 1. **三单元**：逻辑只写 LWN（通用基座），内容包 = 纯数据 + 一个 thin 适配类（`XxxCampaign : LivingWorldCampaign`，3 行）——引擎按战役类名匹配 GameType
> 2. **织丰对照法**：一切「缺 X」先反编译织丰/官方拿证据（做了/不做/怎么做）再动手，禁止凭名字猜
> 3. **每改数据必 parse XML + 跑 checker；每改 DLL 必查「LWN 已内置兜底表」（文末）**——很多通用兜底新内容包 0 代码获得

---

## 阶段 0：骨架（照抄 Taikou 模板改）

> **起手式 = 拷贝 Taikou ``Modules\Taikou\`` 整套 ModuleData + SubModule.xml 当样张**（junction 源；两个客户端同源）——Taikou 的全套 XML = 已跑通的最小可行样例参考（**不是那个世界的交付物，是骨架样张**），新世界动作 = 拷贝 → 全量替换 id（Culture.x/Kingdom./clan_/town_/Hero./TAIKOU_* 文本 key）→ 跑 checker。
> ⚠️ 两类文件**禁止手改**（铁律 22）：①生成物（spcultures/spnpccharacters/物品 = `Scripts/gen_taikou_*.py` / `prune_taikou_items.py` 生产——新世界复用生成器改名换映射）②9 个 sandbox 文本 = 官方原样拷贝（通用，直接复用）。

> ✅ 判定：能走到新游戏菜单、能建号、能进图不崩。信号 = 每个条目末尾的症状。

### 样张文件映射表（清单条目 → Taikou 样例文件 → 新世界动作）

| 清单条目 | 样张文件（Taikou/ModuleData/） | 性质 | 新世界动作 |
|---|---|---|---|
| SubModule 19+ 段 | `../SubModule.xml` | 模板 | 拷 → 逐段换路径（段 id 不变） |
| thin 类/出生点 | LWN `CampaignMode/` | 代码模板 | 拷 → 改类名/起点 |
| 文化必备组 | `spcultures.xml` | 生成物 | 复用生成器（gen_*.py 换文化 id） |
| 势力链 | `spkingdoms.xml` + `spclans.xml` | 生成物 | 同上（玩家族 is_minor_faction 参考值保留） |
| 据点 | `settlements.xml` | 生成物（T4 接入布局表后） | 换 id/owner/pos |
| 角色 | `spnpccharacters.xml` + `taikou_heroes.xml` + `spnpccharactertemplates.xml` | 生成物/自建 | 换 id；模板装备槽清空 |
| 物品 | `taikou_items/`(目录) + `items.xml` | 生成物 | 复用 prune 管线（`prune_taikou_items.py`；先 dry-run 看 deleted=0） |
| 基础八件套 | `taikou_bodyproperties` / `taikou_skill_sets` / `taikou_equipment_sets` / `taikou_concepts` / `taikou_crafting_pieces` / `taikou_music_instruments` / `taikou_music_tracks` / `taikou_location_complex_templates` | 自建（段照官方） | 拷 → 换内容 |
| 文本 | `taikou_module_strings.xml` + `taikou_world_lore_strings.xml` + `Languages/` | 自建 | 拷 → 换 TAIKOU_* key/翻译 |
| ⚠️ 9 文本段 | `module_strings/comment_strings/action_strings/wanderer_strings/companion_strings/comment_on_action_strings/trait_strings/voice_strings/world_lore_strings.xml` | **官方原样拷贝** | 直接复用，不换 |
| 其他引擎段 | `action_sets/action_types/collision_infos/combat_parameters/face_animations/item_holsters/module_sounds/native_parameters/physics_materials/skins.xml` + `project.mbproj` | 官方/工具产物 | 拷进即可 |
| 地图 | `SceneObj/Main_map/`（scene.xscene/terrain.bin/navmesh.bin/atmosphere.xml/flora.bin） | 本世界专属 | 换地形/实体（见 1.5） |
| 音乐 | `music/soundtrack.xml` + `ModuleSounds/` | 自建 | 换选曲 |

- [ ] **thin 适配类**：`XxxCampaign : LivingWorldCampaign`（含 StartingPosition）；LWN 侧 3 行接入
  - 信号：主菜单点 NewGame 无反应 / 进不了战役模式
- [ ] **SubModule.xml 完整段清单**：TaikouCampaign 段 = **19+ 段**（世界数据 8 + 基础 11：BodyProperties/SkillSets/EquipmentRosters/Concepts/CraftingPieces/MusicInstruments/MusicTracks/GameText×2 + Items/NPCCharacters×2/Kingdoms/Factions/spcultures/Settlements/WorkshopTypes/LocationComplexTemplates/partyTemplates…）——Copy Taikou 的 SubModule.xml 逐段替换路径
  - 信号：`CreateMergedXmlFile` 越界/etc 段缺失（雷 3/4/5——每少一段崩一次）
- [ ] **GameManager + CC**：`LivingWorldCampaignGameManager`（DoLoading 6 步）+ `LivingWorldCharacterCreationContent`（CC 2 阶段 + OnCharacterCreationFinalized 出生点）+ `CampaignModeActivator` + `ModuleActivationHelper`（内容包勾选判决据）——LWN 现成，内容包供数据
- [ ] **MBGlobals.InitializeReferences()**（DoLoading case0；信号：FaceGen 阶段 `MBGlobals._actionSets` 静态词典 NRE，雷 26）
- [ ] **SaveableTypeDefiner**：战役类注册（base 新编；信号：存档失败 "Could not find type definition of type: …Campaign"，雷 32）
- [ ] **CC 阶段链**：`[Culture, FaceGenerator, Generic, Review]`（信号：Generic 阶段 Tick NRE = 缺 FaceGen，雷 25）
- [ ] **CC 文化三件**：文化名/描述（`str_culture_rich_name.ikoku` 式 variation）+ 大图（brush Layer.1..4 Style 兜底官方 empire）+ 文化排序屏蔽（LWN 补丁现成，雷 23）
- [ ] **NPC 模板段**：spnpccharactertemplates 自备 + 自家 GameType 注册（信号：FaceGen 崩 `facgen_template_test_char_0` null，雷 27）；模板装备槽/升级线清空（模板 = 容貌样板，T4 挂了自家物品再配装）

## 阶段 1：数据最小自洽集（启动不崩底线）

### 1.1 文化（spcultures）——缺一条 = 一条 NRE 排队等
- [ ] `Culture.<本世界>` + **`neutral_culture`**（引擎 4 处 fallback 消费；信号：未知）
- [ ] 最小 7 字段（织丰最小集）：basic/elite 兵种、militia_template、encounter_mesh、roster×2、banner 武器
- [ ] **default_party_template**（信号：`CalculateAverageWage` NRE，雷 6）
- [ ] **notable_and_wanderer_templates**（信号：`InitializeCompanionTemplateList` NRE，雷 10/11）——引用必须指向世界内存在的角色
- [ ] lord_templates + rebellion_hero_templates（空节免炸）
- [ ] **basic_mercenary_troops**（信号：`FindTotalMercenaryProbability` NRE，雷 21）
- [ ] **名字池三池** clan_names/male_names/female_names（信号：`GenerateClanName` NRE，雷 22——织丰日式名池语义直接抄）
- [ ] ⚠️ **拷贝官方文件后必须做文化引用清洗**（1835 处 `Culture.<原版八文化>` 残留 = GetPresumedObject 裸文化桩 → 全链 NRE；sanitize 脚本，雷 11；**这是全工程最狠的一颗雷**——症状分散在对话/兵种/商队各处）
- [ ] N&W 池必须产得出来 **merchant / artisan**（信号：工坊 `ChooseWeighted(空)` NRE，雷 15）

### 1.2 势力（spkingdoms / spclans）
- [ ] Kingdom：owner（Hero）+ culture + banner + 名称（信号：`Kingdom.OnNewGameCreated` NRE，雷 13）
- [ ] RulingClan 链 = owner 英雄的 Clan（owner→Hero→Clan 三段必须通，雷 13/14）
- [ ] Clan：is_noble + owner + super_faction；玩家族参考织丰 `is_minor_faction="true"`（独立势力形态，雷 36 对照项）
- [ ] 王都/归属 partial-followup：Kingdom.InitialHomeLand + Clan.HomeSettlement，时点 = partial（OnInitialize 被引擎洗白；partial 时点 GetObjectTypeList 返回 null → 用 Campaign.Current.*，雷 13）

### 1.3 据点（settlements）
- [ ] 每个 Settlement：name + **owner** + culture + posX/posY + Components + Locations + CommonAreas（缺 owner = `.OwnerClan` 链 NRE；缺 CommonAreas = `i % 0`，雷 19）
- [ ] 城防 **level ≤ 3**（信号：`RefreshPartyIcon` KeyNotFound，雷 29——4 级城需自做第 4 层门旗实体）
- [ ] spworkshops + WorkshopTypes 段（雷 17）；occupation 值必须过引擎枚举全集（错 = NPCCharacters 段静默截断，雷 16）
- [ ] 黑巷 gang_leader（occupation=GangLeader，雷 19）
- [ ] **退休据点 `retirement_retreat`**（信号：每小时 tick NRE，雷 31——RetirementCampaignBehavior 硬编码查找）
- [ ] settlements.xml **必须闭合/parse 通过**（雷 8：未闭合 = 首发铁定 NRE 且错位迷惑，排查 15 轮）

### 1.4 角色与物品
- [ ] main_hero = 官方全量版（信号：`InitializeGamePlayReferences` NRE 链，雷 7）
- [ ] 英雄：faction（Clan）+ culture + occupation + 装备（雷 13）
- [ ] 物品体系：**仿织丰自建**（GameType 过滤 → 官方物品库不进本世界；merchandise + 工坊 Outputs 分类匹配，雷 18）；**勿恢复官方全量物品库**（雷 27 现场纠正）
- [ ] 兵种最小 4：guard/villager/townsman/townswoman（场景生成必撞）+ 商队组（caravan_master/armed_trader/caravan_guard 等触发面字段）
- [ ] caravan_guard 等级 1→26（`InitializeCaravanOnCreation` First 查询，雷 16）
- [ ] 🔴 **地痞三档 gangster_1/2/3**（雷 40：官方定义在 SandBoxCore 段带 GameType 白名单 → 自定义 GameType 下 GetObject null → **进城 TownCenter 就崩**，`DefaultAlleyModel` 巷战消费：AddToCounts(null) NRE）。做派：自家副本 + 自家段注册（织丰同款）；升级链/level 6/11/16/occupation=Gangster/skill_template 用自家 skill_sets；**物品只用自有物品集**（禁令：引用 vanilla Item.* 即悬空）
- [ ] 🔴 **场景马匹出生点马物品**（雷 41：官方 town 场景内 sp_horse_* prefab Tags[1] = vanilla 马物品 id → 未装载 → `SpawnHorses` NRE）。做派：官方马物品副本入自家物品库（aserai/battania/empire/khuzait/sturgia_horse 缺则补；体检走 `check_scene_consumables.py`，运行时兜底 2026-09-10 已删——数据优先）；**用官方场景 = 必须自备其 prefab 引用的全部物品**（排查法：`grep -o 'prefab="sp_horse[^"]*"' <scene>.xscene` + 读 Native/Prefabs/editor_spawnpoints.xml 对应 tags）
- [ ] 🔴 **场景动物出生点动物物品**（同雷 41 族：sp_sheep/sp_cow/sp_hog/sp_goose/sp_chicken → sheep/cow/hog/goose/chicken 物品消费，`MissionHelper.SpawnSheeps/Cows/Hogs/Geese/Chicken` 同款 GetObject(null) 会崩）——**物品集必须含这 5 只动物**（Taikou 物品集已含 ✓，新包照配）

### 1.4b 🔴 用官方场景 = 先扫「场景消费清单」（2026-09-09 两颗雷的总根，雷 40/41）

**规律**：场景文件本身不受 GameType 过滤（官方城市场景直接可用）——**但场景里的 prefab 实例引用原版物品/角色/兵种，那些被过滤**。所以「用官方场景」和「官方物品库进不来」是一对矛盾，矛盾的解法 = 白名单式补齐，**不是**不停等崩修崩。

**排查三步法**（一次补齐，不欠第二颗雷）：
1. `grep -o 'prefab="sp_[a-z]*"' <官方场景>.xscene`（场景用了哪些 spawn prefab）
2. 去 `Modules/Native/Prefabs/editor_spawnpoints.xml`（+ `Modules/SandBox/Prefabs/`）找对应 `<game_entity name="sp_xxx">` 看 `<tags>`——Tags[1] = 被引用的对象 id
3. 对象 id → 查自家物品库/角色库 → **缺失 = 拷官方定义入自家库**（物品/角色同法，culture 洗 ikoku）+ 跑 checker 0 悬空

🔴 **已脚本化（2026-09-10）**：以上三步 = `python Scripts/check_scene_consumables.py` 一键跑（自动读注册表 MB2_PATH 定位官方根；missing≠0 则 exit 1）。**新包/新场景无需手工排查——跑一次脚本即可**；手工三步法保留用于理解原理与脚本查不到时的兜底（已知边界：只查静态 prefab tags）。

**已知消费面**（已实锤，新包直接勾）：黑巷地痞 `gangster_1/2/3`（DefaultAlleyModel 进城消费）· 马 `sp_horse_*`（马物品 6 种）· 动物 5 只（sheep/cow/hog/goose/chicken）。**织丰从不用官方城市场景**（自有 sho_* 场景 + 自有 prefab + 自有物品）→ 天然免疫此族雷——不想补清单就学织丰做自有场景（成本高，v0 建议白名单补齐）。

### 1.5 地图场景（Main_map）——克隆大图时最难自查的两组
- [ ] 🔴 **border_min / border_max 实体**（相机边界 = 两个空实体；缺 = 空气墙静默兜底 900×900（1.2.12）/ 进图即崩（1.5.x）；取值 = terrain dimension×size，z = 织丰 1000；见 wheels.d/terrain.md）
- [ ] 🔴 **12 个官方地图脚本实体**：CampaignMapSiegePrefabEntityCache【无守卫 NRE 元凶】/MapColorGradeManager/SceneLeveler/SettlementPositionScript/Town Entity Manager/Town Scene Manager/river_generator/water_body/path_converger/sound_emitter/VolumeBox（信号：`MapScreen.OnInitialize` NRE，雷 28）
- [ ] 🔴 **定居点实体组必带 4 娃**（2026-09-09 京交互链实锤，雷 37——**缺 = 黄圈不显示 + 点击无反应 + 进不了城，且探针/寻路/可通行性全程"正确"极具迷惑性**）：
  1. **`bo_town` 只碰射线碰撞体**（`<physics shape="bo_sphere_collider"><body_flags><body_flag name="only_collide_with_raycast"/></body_flags></physics>`）——**地图拾取的前提**：`SelectEntitiesCollidedWith`/`GetCursorIntersectionPoint`（掩码 79617）只命中"仅碰射线"碰撞体，普通 mesh 不拾取。**这是整条交互链（hover 信息卡/点击移动/移动后黄圈反馈）的唯一入口**
  2. **`town_circle_decal`**（tag `map_settlement_circle`，decal_city_circle_a 黄色贴花 + physics mass）——黄圈视觉 + CircleLocalFrame 载体（PartyVisual.OnStartup 遍历 tag 设定帧）
  3. **`gate_position`**（tag `main_map_city_gate`）——城门口（PartyVisual 可通行校验/门定位）
  4. **`banner_pos`**（tag `map_banner_placeholder`）——旗帜占位 + 城墙等级 {1,2,3} 门旗组构建（雷 29 关联）
  - 结构 = 定居点实体（mobile + Town Entity Manager + tag town）的 **<children>** 下（官方 campaign_icon_capsule_36 同构；父级 scale 非 1 时子实体尺寸按父 scale 折算）
  - 织丰 Main_map **也有**（bo_town ×8 / bo_sphere_collider ×5）；我们 v0 曾 0 个 physics 全部裸奔 → 症状全中
  - 🔴 **第 5 条：圈/拾取两组件的 scale 是手调值**（雷 42，2026-09-10）：`town_circle_decal`（tag map_settlement_circle）= **视觉圈**；`bo_town`（bo_sphere_collider + only_collide_with_raycast）= **鼠标拾取范围**——**两者各配各的，官方也是场景作者手调**（官方 50 城抽样 local 1.7~9.1 无固定比例）。调法：进图目视 → 直接改两个 `<transform scale=...>`（纯 XML，无代码）。京定案参考：圈 0.936 / bo_town 1.0（父 scale 2.0）
- [ ] 城图标 = capsule 结构（`campaign_icon_capsule_NN` Z=20 + Town Entity Manager → 据点实体 `<tag name="town"/>`；子标签非属性）
- [ ] navmesh（navmesh.bin RNM1 或 auto_generated）；实体增删不影响 navmesh
- [ ] atmosphere.xml `time_of_day`（编辑器全黑坑：官方存 22:00；6~12 = 白昼）+ layer mask/tileset 黑白诊断（wheels.d/terrain.md）

### 1.6 🔴 SandBox 9 个 GameText 文本段全量拷贝（2026-09-09 盘底，雷 35 本体）
- [ ] **必须拷贝 + 自家注册**：`module_strings`/`world_lore_strings`/`companion_strings`/`wanderer_strings`/`comment_strings`/`comment_on_action_strings`/`trait_strings`/`voice_strings`/`action_strings`——SandBox 全部带 GameType 白名单（仅 Campaign/CampaignStoryMode），自定义 GameType 下**整文件被过滤**
- [ ] 信号：`ERROR: Text with id str_… doesn't exist!` 红字（= action_strings/其他）；`FindMatchingTextOrNull` null → NRE（= comment_strings 领主介绍，雷 35）
- [ ] Native 侧仅 multiplayer_strings 白名单，其余全量——**不用**拷贝
- [ ] 排查方法：扫描模块 SubModule.xml 中 `XmlName id="GameText"` 且带 `IncludedGameTypes` 的 path 列表 → 逐文件拷贝 + 注册

## 阶段 2：流程纪律（每次变动必跑）

> 🔴 **数据优先三原则（2026-09-10 全线复盘定案，写代码前先过）**：
> ①**能用 XML 表达的绝不用代码**（场景实体尺寸、物品/角色引用、段注册全是数据活；"运行时自动适配"方案已被用户否决）；
> ②**离线的能查的绝不留给运行期**（缺引用/缺物品/缺段 → 先扩 checker 脚本，别等进游戏崩）；
> ③**运行期的补丁只做兜底，且必须带退役条件**（新增任何 Harmony/加固补丁 → 在下方「LWN 内置兜底」表的台账列登记：治本侧 + 退役条件；每次数据补全后回查，满足即评审退役）。
> 三层防线 = ①数据断言（checker，改动即跑）②启动期加固（反射修补，跨包安全）③运行期兜底（拦截 NRE，带台账）。
> 现状比例（雷 1~43）：约 **7 成纯数据/离线**、1 成「数据治本+代码兜底」双保险、2 成必须代码（其中一半是功能而非补丁）。

- [ ] 🔴 **新增 .cs 文件必须登记 csproj，否则静默不编译**（2026-09-10 雷 40·流程）：本项目 csproj 为旧式**显式 `<Compile Include>` 清单（无通配）**——`dotnet build` 成功 **≠ 新文件被编译**（漏登记 = 编译器根本没看它，报 0 错是假象；实锤：两个补丁文件漏登记，"编译通过"后从未生效）。纪律：①新增 .cs → 登记 csproj → build 验证真编译（新文件里故意留个错也能验证）②删除 .cs → 同步删登记行。范本位置：`ExampleModVS/ExampleMod/ExampleMod/ExampleMod.csproj` Debug 区段。
- [ ] 🔴 **数据改动必跑四件套**（2026-09-10 更新）：①`check_taikou_xml_references.py`（0 悬空）②`check_taikou_field_coverage.py`（字段交集）③**`check_scene_consumables.py`（官方场景消费体检——场景 prefab 点名的物品本包齐不齐；雷 40/41 族离线防线）** ④改过的 XML 全 parse。**下线诊断补丁注意**：csproj 注释掉一个「看似诊断」的文件前，先 grep 全项目引用——`GameMenuLogger` 就是被 `UiFullScreenHelper` 当状态源用的"伪诊断"（2026-09-10 下线时被编译错误抓出）；文件直接删（**git 历史就是归档**，恢复 = `git checkout <commit> -- 原路径`，不留工作区副本）。

- [ ] **改 XML 必 parse**（雷 8 教训：未闭合 = 15 轮空跑；脚本/程序改完立即验）
- [ ] **checker 跑通**：`Scripts/check_taikou_xml_references.py`（0 悬空）+ `check_taikou_field_coverage.py`（七类交集 + 人工甄别）——新世界照抄改名复用
- [ ] **生成器唯一真源**：数据增补 = 改生成器 → 重跑 → checker；**禁止手改生成物**（铁律 22）；幂等判断覆盖全部新增 id（两踩）
- [ ] 枚举字段对照全集（Occupation=33 等）——错 = 段静默截断
- [ ] 编译：`MB2_PATH="…MB2_1.2.12…" dotnet build`（Bash 环境变量快照坑——默认对着 1.5.2 编！铁律 19，雷 36 当日在编）+ VS2022 最终；跨版本类型 = 接口 + 反射（禁引具体类）

## 阶段 3：启动链验证顺序 + 运行日志巡查

排队雷序：段缺失 → LoadBasicFiles 九件套 → Culture 必备组 → 王国 → 据点/工坊/黑巷/商队 → CC 各阶段 → 进图（地图实体/边界）→ 世界 tick（退休据点/墙旗）→ 存档/读档 → 对话/战斗。

**新建世界后扫一遍日志检查行**（缺失即对应坑未排）：
- `[MapBorder] … max=(2048.0,1280.0)` ✓ 边界实体
- `[LWN-dump]` Clan/Kingdom/Hero 链 ✓ 归属到位
- `ERROR: Text with id … doesn't exist!` ✗ 9 文本段漏拷贝
- `[BattlePowerGuard]`/`[LordIntroGuard]`/`[*NullFix]` 出现 = 兜底在工作但**世界数据还有缺口**，回清单补数据

---

## 雷 1~43 对照总表（排雷实录全索引 → 主档 `plans/太阁数据加载…md`）

> **解决层次速览（2026-09-10 复盘）**：约 26 颗 = 纯数据/离线（XML+checker+生成器）· 3 颗 = 数据治本+代码兜底双保险（11/23/35）· 8 颗 = 必须代码（其中 30/32 是功能代码；12/20 是拦原版硬编码，永久保留）· 其余 = 设计/通用加固。

| 雷 | 症状（一句话） | 根因一句话 | 修法归属 |
|---|---|---|---|
| 1 | 主菜单 NewGame 崩 | BodyProperties/SkillSets 段被 GameType 过滤 | 数据·SubModule 段 |
| 2 | 同点再崩 | CraftingPieces 0 段 | 数据 |
| 3 | 同点再崩 | Heroes/LocationComplexTemplates 0 段 | 数据 |
| 4 | 同点再崩 | MusicInstruments/MusicTracks 0 段 | 数据 |
| 5 | LoadBasicFiles 崩 | Items（沙盒库被过滤 + 空壳）→ 拷贝官方物品库 | 数据（后回滚部分，见 27） |
| 6 | CalculateAverageWage NRE | Culture 缺 default_party_template | 数据 |
| 7 | InitializeGamePlayReferences NRE | main_hero 半成品 → PlayerTroop=null | 数据·官方全量版 |
| 8 | ValidateInitialPosition NRE + 15 轮空跑 | 手改 settlements.xml 未闭合 → Settlement.All=0 | 流程纪律·parse 必验 |
| 9 | 误判今川 | 设计裁定今川全删（最小世界实验） | 设计保持 |
| 10 | InitializeCompanionTemplateList NRE | Culture 缺 N&W 模板 | 数据 |
| 11 | 同函数再崩（最狠） | 拷贝文件 1835 处原版文化引用 → 8 裸文化桩 + 真空引用/幽灵 id | 数据·sanitize + 代码·CultureTemplateNullFix |
| 12 | BackstoryCampaignBehavior NRE | 卡拉迪亚前史硬编码 8 领主+town_V6 | 代码·LWN 屏蔽补丁 |
| 13 | Kingdom.OnNewGameCreated NRE | Leader=null 链 + OnInitialize 洗白 | 数据 + partial-followup |
| 14 | SpawnMilitiaParty NRE | Culture.MilitiaPartyTemplate 空 | 数据·Party 引线 |
| 15 | BuildWorkshopForHeroAtGameStart NRE | N&W 池只有 Lord → 名流职业缺 merchant/artisan | 数据 |
| 16 | InitializeCaravanOnCreation 零命中 | occupation 非法 → NPCCharacters 段静默截断 | 数据·枚举全集 |
| 17 | Workshop.InitializeWorkshop type null | spworkshops 空壳 + 段未注册 | 数据 |
| 18 | TownMarketData.GetPrice NRE | 物品 merchandise=0 / Outputs 分类无匹配 | 数据 |
| 19 | AlleyCampaignBehavior DivideByZero | CommonAreas 缺失 → `i % 0` | 数据 |
| 20 | CC 崩 SortCultureList | 无原版六大文化 | 代码·LWN 补丁 |
| 21 | FindTotalMercenaryProbability NRE | Culture.BasicMercenaryTroops 空 | 数据 |
| 22 | GenerateClanName NRE | 名字池三池空 | 数据·织丰名池 |
| 23 | CC 文化界三问题 | 文本 variation / brush Style / 短名 | 数据 + 代码·LWN 兜底 |
| 24 | Generic 阶段越界 | 内容套餐菜单 0（未实现 OnInitialized） | 数据/CC 菜单（LWN CC 已建） |
| 25 | Generic Tick NRE | 缺 FaceGenerator 阶段 | CC 阶段链 |
| 26 | FaceGen MBGlobals NRE | `InitializeReferences()` 没人调 | 代码·case0 |
| 27 | FaceGen 模板 NRE | facgen_template 段被 GameType 过滤 | 数据·自备模板段 |
| 28 | MapScreen.OnInitialize NRE | 缺 12 个官方地图脚本实体 | 数据·场景实体 |
| 37 | 京点不开/无黄圈/进不了城（交互链全没） | 定居点实体组缺「bo_town 只碰射线碰撞体 + 圈/门/旗娃」——地图拾取只认 only_collide_with_raycast 碰撞体 | 数据·场景实体（清单 1.5） |
| 38 | 进城场景 MissionAgentHandler NRE | CommonAreaMarker.Tag 匹配不到巷=null → GetAllProps `Tag.Contains` NRE（旧档巷数据少） | 代码·LWN Tag 兜底 + 数据·CommonAreas 对齐 |
| 39 | 新开档 OnSessionStart 崩 | IssuesCampaignBehavior 对空据点集 `% 0`（Settlement.All 时点空集） | 代码·LWN 兜底 + 时点日志（数据侧待日志确认） |
| 29 | RefreshPartyIcon KeyNotFound | 城防 level=4 超官方上界 3 | 数据 |
| 30 | 相机看不到角色 | 大地图默认机位与出生点脱节 | 代码·LWN MapScreenCameraPatch |
| 31 | 每小时 tick NRE | retirement_retreat 缺失 | 数据 |
| 32 | 存档失败 | SaveableTypeDefiner 缺战役类注册 | 代码·LWN 定义器 |
| 33 | 相机不框玩家 | teleport 时序（CC 回调时 MapState 未推入） | 代码·LWN 补丁 + 出生点外移 |
| 34 | 相机空气墙（京都以东） | 场景缺 border_min/border_max → 兜底 900×900 | 数据（wheels.terrain） |
| 35 | 找信长对话 NRE | SandBox comment_strings 等 9 文本段 GameType 全过滤 | 数据（9 文件拷贝）+ 代码·LWN 兜底 |
| 36 | 战斗部署 KeyNotFound | 战力字典「按循环侧查桶 vs 按队 Side 登记」键错位 | 代码·LWN 兜底（数据对照项 = 玩家族 is_minor_faction） |
| 40 | 进城 TownCenter 场景加载 NRE（TroopRoster.AddToCountsAtIndex） | `DefaultAlleyModel._thug` GetObject("gangster_1/2/3")=null——官方定义在 SandBoxCore 段，IncludedGameTypes 只有 Campaign/StoryMode/CustomGame/EditorGame | 数据·自备地痞三档（taikou_gangsters.xml + 民用套装 + 自家段注册；织丰同款做派；物品只用自有集） |
| 41 | 进城场景 `SpawnHorses` NRE（ItemRosterElement(null)） | 官方 town 场景（empire_town_a 等）内 sp_horse_* prefab 的 Tags[1] 引用 vanilla 马物品 id → GameType 过滤未装载 → GetObject null | 数据·补 5 匹官方马（taikou_items/horses_and_others.xml，culture→ikoku）+ 代码·LWN 马出生点兜底（HorseSpawnNullGuardPatch） |
| 42 | 黄圈太大 / 圈外很远就触发 tooltip | 定居点**拾取与视觉是两个独立组件**（bo_town 射线碰撞体=拾取范围；map_settlement_circle 的 decal=视觉圈）——两个 scale 各配各的，官方也是场景作者手调（50 城抽样范围 1.7~9.1） | 数据·XML 手调两个 scale（京定案：圈 0.936 / bo 1.0）；运行时自动适配已否决 |
| 43 | 工坊/市场"没商品"疑云 + 僵尸修复文件 | ①`ItemObject.is_merchandise` **缺省即商品**（只显式 false 才排除）——"merchandise=0"是误诊，真根因 = **文化不匹配**（`IsItemPreferredForTown: item.Culture == town.Culture`）；②雷 18 的"造商品"备选方案产物未清理成僵尸（坏 XML+未注册） | 数据·删僵尸+生成器；**生成器判据补齐**（prune 脚本漏"场景消费物品/EquipmentSet 引用"→ 重跑会剪掉进场景所需物 + 地痞套装——改生成物前必 `--dry-run` 看 deleted=0） |

**状态说明**：43 项中 43 项已修/已兜；✅ 雷 40/41（进城）、42（圈/拾取）、43（工坊线结案）2026-09-09~10 实机/反编译验证；⚠️ 待用户终验：雷 32（存档 F5）、雷 34（复测东行）；雷 9 = 设计保持；雷 36 = 已修+兜底（兵源来源留 T4 复核）。

---

## 工具箱（验证/排查手段，命中即用）

| 工具 | 用法 | 解决什么 |
|---|---|---|
| `scripts/check_taikou_xml_references.py` | 数据改动后必跑；0 悬空 | 交叉引用完整性（图→字典判定） |
| `scripts/check_taikou_field_coverage.py` | 七类字段交集 | 最小字段覆盖（人工甄别误报） |
| 🔴 `scripts/check_scene_consumables.py` | 数据改动后必跑；missing=0（缺则 exit 1） | **官方场景消费体检**（场景 prefab 点名的物品/角色本包齐不齐——雷 40/41 族离线防线；自动读注册表 MB2_PATH 定位官方根，`--module`/`--official-root` 可覆盖） |
| `scripts/save_inspect.py` | `--dump`/`--strings` | 存档体检查（类型注册/字符串/定版） |
| `ilspycmd` | 反编译看实现 | 一切 API 行为验证（先二进制 grep 定位 DLL） |
| `custom.export_heightmap`（LWN 控制台） | 游戏内导出地形 | 关卡打不开/黑盒数据（wheels.terrain） |
| 运行日志 `Debug/StoryEngine_RuntimeLog.txt` | [标签] 速查 | 兜底/诊断行（见阶段 3 清单） |

---

## v0 边界（什么可以不做——织丰 65 条对照决策表机制）

> 织丰「做派」对照表 = 按 **A（必须做）/ B（织丰做+我们最小集不做，后续数据层）/ C（不做）** 三档裁定，先看织丰证据再定我们的动作。**例子**：路人 40 职业 = B（T4 再做，最小 4 个提前 A）；未成年变体/锦标赛师/舞女/乞丐 = C；日式素材 = 先抄官方值能跑、T5 日化。
> **v0 不做**：玩法扩展面（城镇体验/战争层/政治 UI/Encounter 战斗链定制——织丰同款参考 `Knowledge/织丰自定义世界观经验.md` §5.2-5.6，T3/T4 按需采纳）；「日式风格」面 = 官方 mesh/face/banner 先顶、T5 内容化。

## LWN 已内置的通用兜底 + 运行期补丁台账（新内容包 0 代码获得；退役看「台账」列）

> **怎么读**：前三列 = 这个兜底给你什么；「治本侧 / 退役条件」= 何时能拆（2026-09-10 复盘台账合并进本表——新增运行期补丁**必须**在此登记一行）。
> **退役验证成本**：🟢 日志观察（跑现有档看日志无修复行）· 🟡 现有存档复测（走进原崩溃场景一次）· 🔴 需重开档（新档时点/CC 阶段类）。**只有 🔴 才重开档。**

| 兜底 | 文件 | 效果 | 治本侧 / 退役条件 |
|---|---|---|---|
| Culture 模板列表 null→空 | Debug/CultureTemplateNullFix.cs | 文化模板缺席不崩（有日志） | sanitize 已治本 → 🟢 双版本实测无 `[CultureTemplateNullFix]` 修复日志即可退役 |
| 领主介绍句兜底 | Debug/LordIntroConditionGuardPatch.cs | 文本/数据缺失跳过不崩 | 9 个 GameText 段已拷贝 → 🟡 找织田对话一次无 `[LordIntroGuard]` 跳过即可退役 |
| 城镇区标记 Tag 兜底 | Debug/AreaMarkerTagGuard.cs | 老存档/缺巷城镇进场景不崩（Tag null 回退基类，雷 38） | 老档/旧巷数据 → 🟡 老档进城；⚠️ 当前被临时注释（读档崩二分排查中） |
| Issues 空列表兜底 | Debug/IssuesSettlementGuardPatch.cs | OnSessionStart 据点集空时跳过 Issues 初始化（%0 防崩，雷 39） | 时点空集（数据侧待日志确认）→ 🔴 新档 OnSessionStart；⚠️ 临时注释中 |
| 战斗战力计算兜底 | Debug/BattlePowerCalculationGuardPatch.cs | 键桶错位不崩 + 诊断 | T4 兵种数据时复核（玩家族 is_minor_faction）→ 🔴 遭遇战实测 |
| CC 文化排序/视觉兜底 | Debug/CharacterCreationCultureStageSortPatch / VisualFallbackPatch | 自定义文化 CC 界面 | 视觉兜底：自家文化图配齐后 🔴 新档 CC；排序：引擎硬编码六文化 = **不退役** |
| 地图相机对准 | Debug/MapScreenCameraPatch.cs | 进图相机 | 功能代码，**不退役** |
| 地图边界诊断 | Debug/MapBorderDiagnosticPatch.cs | [MapBorder] 一行验证 | 长期工具（造图必用），保留 |
| 伤害模型 Culture-null | Debug/AgentDamageModelCultureNullFix.cs | 战斗伤害链 | 通用加固（织丰/原版共同隐患），**不退役** |
| 角色文化回填 | Debug/CharacterCultureBackfill.cs | 生成期按地点补 culture | 通用加固，**不退役** |
| AiPatrolling 空保护 | Debug/AiPatrollingNullFix.cs | 原版行为 NRE 兜底 | 通用加固（原版隐患常在），**不退役** |
| Backstory 屏蔽 | Debug/BackstoryCampaignBehaviorPatch.cs | 卡拉迪亚前史不注入 | 设计拦截，**不退役** |
| 存档防线 | Debug/CrashLogHook.cs + Debug/SaveGuard.cs | 崩溃快照/存档三防 | 功能防线，**不退役** |
| 存档类型注册 | CampaignMode/LivingWorldSaveableTypeDefiner.cs | 战役类进存档 | 功能必需，**不退役** |
| ~~马出生点空物品兜底~~ | ~~Debug/HorseSpawnNullGuardPatch.cs~~ | ✅ **已删（2026-09-10）**——数据已治本（5 马入库），缺失改由 `check_scene_consumables.py` 离线抓 | — |

**诊断工具生命周期**（2026-09-10 第一批已执行）：9 个一次性诊断（AddQuickInformation/DisplayMessage/ShowInquiry/SceneNotification/AgentSetTeamLogger/ClickDiag/EncounterDiag/PartyVisualCircleDiag/TownEntryDiag）**已删除下线**（csproj 注释；恢复 = git 历史捞回——git 本身就是归档，不留工作区副本）；**NavMesh 五件套保留**（可视化未显示待最小复现，未结案）；`GameMenuLogger` 是 `UiFullScreenHelper` 状态源（**伪诊断**，归档时被编译错误抓出）—— **归档任何"看似诊断"的文件前先 grep 全项目引用**。

**数据侧待办（从台账导出）**：雷 36 兵源来源（T4 复核）· 雷 39 Issues 时点（日志确认）· 雷 38/39 两补丁恢复或退役（读档崩二分定案后）。

## 数据源纪律（太阁5 系专属，其他世界观换对应源）

- 织丰/太阁5 数据一律读 `Knowledge/太阁5/骑砍2织丰角色ID对应/csv/` 镜像 + `xlsx_to_csv.py` 唯一导入器；镜像 CSV 禁手改
- 实体 ID 一律 StringId（铁律 20）；查找走两轮策略（铁律 5）；名字查 ID 必过别名列（铁律 25）；引用禁用显示名（铁律 20）
