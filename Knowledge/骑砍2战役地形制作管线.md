# 骑砍2 战役地形制作管线：从一张图到 ModKit 大地图

> 2026-09-04/05 实测整理（日本列岛地图案例）。结论先行，坑点单列。
> 🔴 管线脚本现位于 `tools/ExportHeightMatMap/make_heightmap.py`（素材 `SourceMap/`、产物 `Output/`，两者已 gitignore，由脚本重跑生成）。
> v2（2026-09-05 晚）：补齐地形材质层清单、原版真实规格修正、水面参数、官方导入流程、terrain.bin 容器、flora.bin 植被格式、速度机制、（原版 vs 织丰 vs 日本建议）对标。**原版世界尺寸/顶点数据已修正**（见三点五）。
> 🔴 **v3（2026-09-06）：高度图细节达到 Native 卡拉迪亚水准**（band-40 std 0.032 vs 0.035、连通排水网、软润颗粒）——见**八**节；v1 的 σ 山影差分细节链已废弃。

## 管线一句话

`地图图(任意分辨率, 任一素材) → tools/ExportHeightMatMap/make_heightmap.py 一条命令 → 16bit 灰度高度图 PNG + 材质图 PNG → 场景编辑器 Terrain 面板 Import Heightmap / Import Materialmap；参数 = X/Y/Single Node Size/Single Node Dim 四件套`

```bash
python tools/ExportHeightMatMap/make_heightmap.py                # 主档 4096x2560（hires）
python tools/ExportHeightMatMap/make_heightmap.py 1024 640      # 任意档
python tools/ExportHeightMatMap/make_heightmap.py 1024 640 <src.png> <out_dir>   # 换素材/输出目录
```

---

## 🔴 研究环境与交付品质要求（2026-09-05 用户裁定）

**版本/路径（最常用，勿弄错）**：
- 研究版本 = **1.2.12**（`H:\SteamLibrary\steamapps\common\MB2_Version\MB2_1.2.12\Mount & Blade II Bannerlord\`）
- **Taikou mod（工作场景）** = `H:\SteamLibrary\steamapps\common\MB2_Version\MB2_1.2.12\Mount & Blade II Bannerlord\Modules\Taikou`
- **原版 bigmap 参照** = `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\SandBox\SceneObj\Main_map`（**主安装**的 SandBox，非 1.2.12 目录）——与原版一切对照用它

**🔴 品质硬指标**：我方输出的高度图/材质图必须保持与**原版同等品质细节**——高低差形态（山体坡度/峰值量级）、海岸线形状与过渡（滩→浅水→深水）都要到原版水准，逐项核对后才算可交付。对照样本 = `tools/ExportHeightMatMap/NativeExample/`（用户从原版 Main_map 导出的原版高度图 + 8 层权重，16bit 4097×4097）。

---

## 一、编辑器里没有"PNG 导入"这种单独功能——在 Terrain 面板，参数是四件套

已核实：引擎 DLL 里没有 ImportHeight/TerrainTools 字符串；导入功能藏在场景编辑器 Terrain Properties 面板。

| 面板字段 | 含义 | 换算关系 |
|---|---|---|
| **X** | 横节点数 | 世界宽(m) = X × Size |
| **Y** | 纵节点数 | 世界高(m) = Y × Size |
| **Single Node Size** | 每节点边长(米) | 世界尺寸的真正控制者 |
| **Single Node Dim** | 每节点分辨率 | 总顶点 = X×Dim × Y×Dim；**PNG 必须与之 1:1** |

**三条铁律**：
1. 世界尺寸（米）**只由 X×Size / Y×Size 决定**，与 PNG 分辨率无关
2. **PNG 像素数 ≡ 地形顶点数**（1:1 强制，错像素=错位/形变）
3. 节点数与 Dim 可互换：X=16, Size=128, Dim=256 与 X=32, Size=64, Dim=128 等价（同世界体、同顶点、不同单节点分辨率；原版可参考量≈4096×4096 顶点）

按钮（同一面板，自上而下）：`Import Heightmap` / `Import Materialmap` / `Import Normalmap from Heightmap`（可选）/ `Export Heightmap`（导出核对）。

### 🔴 官方导入流程（2026-09-05 moddocs 原文确认，与你实测的"直接成功"无矛盾）

1. **先有图层**：Terrain Paint 面板"Add Layer"建一个图层 → **选中该图层** → 属性面板 → `CTRL+A` 选中全部节点 → 点导入按钮
2. **新场景自带 `default` 图层**（`terrain_materials.xml` 第一个条目）且默认选中 → 所以导入高度图/材质图**不需要手动 Add Layer**，直接成功（2026-09-05 用户确认："因为有 default，所以高度图直接成功了"）
3. 材质精度官方原文：高度图 **8/16 位（PNG 或 raw）**；材质图 **8bit**
4. 官方"四件" = **四类图层**（碎屑图 debris / 水流图 flowmap / 植被图 vegetation / 基岩图 bedrock）—— ⚠️ 与我们文档用语"四件套"（X/Y/Size/Dim）撞名，是两码事

---

## 二、Python 管线（2026-09-05 版，替换旧「亮度差分吃整图」）

旧版被废弃的原因：道路黄线/河蓝线被抬成山脊与刻槽、海上航路白点线变成小岛、雪山(灰白/蓝白)混进滩、平原亮度方向搞反。**现管线 = 元素语义分层**：

| 元素 | 判据（源图色域采样） | 高度 |
|---|---|---|
| 海（深浅湾） | blueish & lum≤165 | 0（统一） |
| 湖 | 陆上大块蓝 | 0（=海） |
| 航路/浪花 | 海上低饱和亮色 + 开运算线状/点状 | 归海 → 0 |
| 滩/岩墙/雪 | 粗白 & 距海 d 连续映射（灰白/淡蓝白/淡绿白）| 0.05+d/1200（贴海0.05→岩0.14→雪0.55+雪脊差分，上限0.95） |
| 平原 | 绿区亮度分段低位（Koei 画法：平原亮黄绿、山地暗绿 → 亮度与山高**反向**）| ~0.2 |
| 山地 | 绿区亮度分段高位 + 山影高斯差分（亮度门控增益）| 0.25~0.5+细节 |
| 富士蓝白雪 | 蓝判 + 最大白点簇 → 雪帽；PEAKS 地标锥体豁免海清零 | 0.6-0.88 |
| 道路/河线 | 黄色判/细蓝判 + 开运算线状 | 中值替换底色（不抬山不刻槽） |

**⚠️ 关键裁定（用户亲报 2026-09-05）**：富士山 == 源图坐标 (10500,7000)（山顶白点=格线交点）。
- 富士雪是「蓝白 + 中心白点 + 绿地包围」——**蓝白雪≠水**（与海同色系），曾被三次当湖泊判 0 → 黑斑/蓝湖 bug
- 修复 = 雪帽检测（**最大白点簇**判定：全图最大白点簇=雪顶；浪花/礁石白点都是小簇自动排除）+ **PEAKS 地标区豁免海清零**（高度图、材质图两边同源，禁止分叉）

**材质图四通道**（RGBA 256，与高度图同尺度 1:1）—— ⚠️ 见「三·七官方流程」：**官方做法 = 每图层一张 8bit 灰度 mask**；下图 RGBA 拼图层是我们当时实测的**捷径形式**（引擎侧存在整图导入路径，具体以"三·八 待验证"为准）：
- R 草（平原档，h_base<0.30）/ G 林（山地档）/ B 沙土（白系离海≤400）/ A 雪（白系离海>400 且 **hm≥0.42**（高度先决，防海岸白带误判）+ 雪帽 + 地标峰锥）
- 海/湖 = 全 0（引擎材质层 water/河层覆盖，并非"引擎自动铺水面"——见「三·六水系统」）
- ⚠️ 查看器不认 RGBA 四通道（A=雪 被当 alpha → 变半透明黑）；**用 `matpreview_*.png` 人看**

**脚本内部五段**：①语义分类（海陆/白/黄/河 + 雪帽）②高度合成（分层 + 山影）③地标层（`PEAKS` 表 =(cx, cy, 锥半径, 峰高, 山体基底高, 山体半径)）④材质派生 ⑤预览/QA。改素材/改参数 → 重跑一条命令。

> 🔴 2026-09-05 晚（用户裁定「普通地表压平、富士独大」）：`HEIGHT_GAMMA=1.4` 压平曲线（平 0.2→0.105 / 普通山 0.45→0.30 / 顶值保留）+ PEAKS 富士峰高 `0.88→1.00` 顶格；场景 min/max 定稿 **0 / 21**（富士≈19.6m=3.1× 普通山；平滑中值把尖顶磨 1.0→0.933，正常）。雪 mask 0.3%→0.1%（γ 后 0.5~0.55 白脊降破 0.42 阈值转沙，属预期）。

---

## 🔴 三、素材分辨率 × Terrain 四件套：不同分辨率的图适配同一世界的方法论

**问题**：为什么给两张分辨率不同的日本地图（japanmap_hires 15840×10080 / TaikouMap2 704×448），都能生成同一世界尺寸、同一语义内容的地形，精细度如何换算？

**回答（三层）**：

### 第一层：世界尺寸只由四件套决定
```
世界宽(m) = X × Size；世界高(m) = Y × Size    ← 与 PNG 分辨率无关
总顶点    = X×Dim × Y×Dim                      ← PNG 必须 1:1 等于顶点数
```

### 第二层：PNG 分辨率只决定"网格能装多细"，不决定世界有多大
| 管线输出 PNG | X | Y | Size | Dim | 顶点 | 世界尺寸 | 每顶点代表 |
|---|---|---|---|---|---|---|---|
| 4096×2560（hires 主档） | 16 | 10 | 128m | **256** | 4096×2560 | **2048×1280m** | 0.5m |
| 1024×640（小素材推荐档） | 16 | 10 | 128m | **64** | 1024×640 | **2048×1280m** | 2.0m |
| 704×440（Taikou 原生近似） | 16 | 10 | 128m | **44** | 704×440 | 2048×1280m | ≈2.9m |
| **4096×4096（原版规格档，2026-09-05 新）** | 16 | 16 | **53m** | **256** | 4096×4096 | **848×848m** | 0.207m |

**规律**：Size 与节点数不动 ⇒ **世界尺寸不变**；Dim × 节点数 = PNG 分辨率；**PNG 越高 ⇒ Dim 可以越大 ⇒ 同世界体网格越细**。

### 第三层：素材分辨率如何换算成"精细度上限"
```
素材有效像素率 = 素材宽 / 世界宽(m)     ← 素材能支撑多少"真细节"
精细度上限 = min(素材像素率, 引擎顶点预算)
```
- japanmap_hires 15840 宽 / 2048m ≈ 7.7 源px/m → 撑得起 4096 顶点网格（0.5m/顶点），细节是真材实料
- TaikouMap2 704 宽 / 2048m ≈ 0.34 源px/m → **只能撑 1024 档（2m/顶点）**；强行 4096 = 顶点全插值 → "假细节"（碎斑振铃），观感反而劣化
- **"同一世界"的保证**：管线输出是**语义图层**（海/陆/山影/雪），语义与分辨率无关；素材经裁剪→语义→按目标顶点重采样，所以两素材输出**图上每个像素映射到世界上同一点**（山脉/海岸/富士同位置），仅网格粗细不同

### 实践规约
1. 正式产出用 **hires 4096 主档**（Dim=256）；小素材（704 级）只配 **1024 档预览**（Dim=64），两档可共用同一场景在世界坐标逐位置对齐
2. **不要**给小素材强行套大 Dim（顶点=插值假纹）
3. 换素材后**四件套只需要改 Dim**（节点数/Size 不动），世界尺寸自动一致
4. 素材原生分辨率不整除不是问题（如 704×448→440）：管线以"世界比例裁切后重采样到目标顶点"输出任意尺寸 PNG

### 直接参数表（照抄用，2026-09-05 实测）

**当前 Output/ 两档现成图 → 参数**：

| 要导入的 PNG（tools/ExportHeightMatMap/Output/） | X | Y | Single Node Size | Single Node Dim | 顶点 = PNG | 世界尺寸 |
|---|---|---|---|---|---|---|
| `hm_japanmap_hires_4096x2560_16bit.png` + `mat_japanmap_hires_4096x2560.png` | 16 | 10 | 128.000 | **256x256** | 4096×2560 ✓ | 2048m × 1280m |
| `hm_TaikouMap2_1024x640_16bit.png` + `mat_TaikouMap2_1024x640.png` | 16 | 10 | 128.000 | **64x64** | 1024×640 ✓ | 2048m × 1280m（同） |

**脚本输出档 → 四件套通用表**：

| 脚本输出的 PNG | 四件套（X / Y / Size / Dim） |
|---|---|
| 4096×2560 | 16 / 10 / 128 / **256x256** |
| 2048×1280 | 16 / 10 / 128 / **128x128** |
| 1024×640 | 16 / 10 / 128 / **64x64** |
| 704×440（Taikou 原生比例） | 16 / 10 / 128 / **44x44** |
| **4096×4096（原版规格档）** | **16 / 16 / 53 / 256x256** |

**核验公式（防手滑）**：`PNG 宽 = X × Dim 数`，`PNG 高 = Y × Dim 数`；`世界宽(m) = X × Size`。

---

## 三点五、原版 main_map 真实规格（2026-09-05 修正，旧数据已作废）

| 度量 | 原版实测（scene.xscene + terrain.bin） | 备注 |
|---|---|---|
| **世界尺寸** | **16×16 节点 × 53m = 848×848m** | ⚠️ 旧稿"≈1821×1785m"**错**。那 1821m 是**实体装饰环跨度**（背景山/图标散布到地形网格外 ±430m 当视觉边界），不是地形网格 |
| **地形顶点** | **4096×4096**（16 节点 × 256/节点 = 0.207m/顶点） | ⚠️ 旧稿"≈3000×3000 顶点"**错**（错从"17.9MB÷2B"线性推，那 17.9MB 是段长不是原始数据） |
| 高度图位深 | **8bit 灰度**（terrain.bin HGHT 段 PNG） | 16bit 可选（管线用）；8bit 每步 ≈0.093m |
| 高度 min/max | **-0.723 / 23.052**（总高差 ≈24m） | 原版"卡通风大陆"——山是背景装饰实体，地形本体贴地 |
| 实体规模 | 37,918 game_entity（据点/城堡/门楼/桥/图标 LOD…） | **做自己的图别超此量级**（织丰 41,581 曾让编辑器全机崩） |
| 植被 | flora.bin **75,618** 条（FLR2 格式，见三·十一） | — |
| tileset | `vista_tileset="WorldMap"`（= Native/TileSets/Bannerlord.gts 117MB + pages/ 分页贴图库） | 无公开 tileset 编辑工具（见三·十二） |
| 动态天气 | `use_dynamic_weather_effects=true` + 雪流图 `main_map_snow_flowmap` | 日本地图可换名（织丰：`shokuho_main_map_flowmap`） |
| 其他 | lod_multiplier=3、colormap_detail_level=0、ao_intensity=4、outer_mesh 未启用 | — |

---

## 三·六、水系统（原版 vs 织丰 vs 你的日本地图）

| 参数 | 原版（卡拉迪亚大陆） | 织丰（日本题材 mod） |
|---|---|---|
| `water_exists` | **false**（不开水面！视觉海 = 材质层 water_ground 画出来的） | **true** |
| `water_level` | -100（哨兵） | **1.650** |
| `water_material` | `wat_main_map_ocean_g` | 同（直接引用原版资源） |
| 浅/深色 | (0.006,0.013,0.006)/(0.000,0.007,0.017) | (0.012,0.085,0.046)/(0.004,0.018,0.032) |

> ⚠️ 表中原版列 = 1.5.x 实读。**🔴 版本差异（2026-09-05 补，1.2.12 实读）：1.2.12 原版 = `water_exists=true` / `water_level=1.800`**（真水面 1.8m）；1.5.x 原版才改为 false/-100 哨兵（视觉海走材质层 water_ground）。**本研究版本（1.2.12）以 1.800/true 为准**。

**对你（日本列岛 = 满图海）的定稿建议**：`water_exists=true`、`water_level=1.65`（原版 1.2.12 = 1.8，织丰 = 1.65，任选，审美差异）、`water_material=wat_main_map_ocean_g`、浅深色用织丰（青绿浅海）。

⚠️ **管线文档旧语误**：~~"海/湖 = 全 0（引擎自动铺水面）"~~ —— 引擎**不会自动铺水面**，材质图"全 0"只相当于在地表画了水色材质层；真水面 = water_properties 单独开关。岛国必须开。

---

## 三·七、原版地形材质层清单（SandBox `SceneObj/Main_map/scene.xscene` `<layers>` 段实录）

**激活材质层 = 8 层**（对 WorldMap tileset；每层有夏/秋/冬三季变体，落表为夏季）。**这就是"大地图里有哪些材质层记录"的官方答案**，也是 Terrain Paint 面板层列表的真相：

| # | 层名 | physics | diffuse（夏） | normal | scale | mesh_blend | 语义 |
|---|---|---|---|---|---|---|---|
| 1 | **default** | grass | ground_grass_b_d_mainmap | desert_floor_b_n | 5,5 | true | 兜底草地 |
| 2 | **flora_forest** | grass | ground_forest_b_d_main_map | ground_forest_c_n | 1,1 | true | 森林（草地物理+林纹） |
| 3 | **rock** | stone | main_map_mountains_d | stone_desert_a_n | 4,4 | true | 山岩（six_side_uv=true） |
| 4 | **grass** | grass | ground_grass_b_d_mainmap | ground_drygrass_a_n | 16,16 | false | 细草地（16×高频小格） |
| 5 | **dirt** | soil | ground_soil_a_d_mainmap | ground_soil_b_n | 4,4 | true | 土路/土坡 |
| 6 | **dirt2** | soil | ground_soil_a_d_mainmap | ground_earth_n | 5,5 | true | 土变体 |
| 7 | **forest2** | soil | ground_forest_b_d_main_map | ground_forest_c_n | 5,5 | true | 森林变体 |
| 8 | **river** | soil | water_ground | 无 | 5,5 | false | 河/水 |

**配套表（物理层池，`Native/ModuleData/terrain_materials.xml`，全场景共用）**：default(soil) / rock_cliff(stone) / desert_a(sand) / desert_b(sand) / desert_cliff(stone) / soil_b(soil) / flora_habitat_a(grass) / em f sum(soil) / summerFpreset(soil) / mud_cliff(stone)。→ 场景层的 physics 字段只是从这里**引用**（grass/stone/soil），不是 1:1。

⚠️ **通道映射依此定稿（管线材质图 ↔ 场景层）**：草区 = `grass`/`default` 层、林区 = `flora_forest`/`forest2` 层、山地岩 = `rock` 层、土路 = `dirt`/`dirt2`、河流 = `river` 层、雪 = 无独立层（原版雪依赖 tileset 材质层组+雪流图实现，雪区实质上复用 rock 或需要 tileset 自定义）。

**🔴 层序与覆盖规则（2026-09-06 用户实机制定）**：
- 层序号 **1 起**：第 1 层 = `default`（最底·兜底草）；**序号越大 = 越上层 = 覆盖优先级越高**（权重重叠时大号压小号）
- 原版 8 层按此设计（default 垫底，river/forest2 偏上）；**整块照抄原版 XML = 顺序天然正确**，只有编辑器 Move Layer Up/Down 才会打破
- 混合主导 = 各层权重（某格权重 0 = 该层对该格无作用）；仅有权重重叠时"谁压谁"由序号决定
- **对导入无影响**：Import Materialmap 按"当前选中层"落权、与层序无关；注意导出文件名是引擎**零基**（第 N 层 ←─ `layer(N-1)`），面板是 **1 基**——以"选中为准"不数编号

---

## 三·八、官方导入流程与"RGBA 四通道"之谜（待实机定案）

- **官方文档（[Terrain Creation](https://moddocs.bannerlord.com/zh_cn/editor/scene-editor/terrain_creation/)）**：Import Materialmap = 每图层一张 **8bit** 图 + Add Layer/选中/CTRL+A；Import Heightmap 同流程（8/16bit PNG 或 raw）
- **我们管线现状** = 一张 RGBA 4 通道整图 → 导入成功 + "换 R/G/B/A 次序重导"调色（用户实测 2026-09-04）→ 与官方文档说法存在 **A) 引擎隐藏整图导入路径** vs **B) 当时其实每一步都是对 default 层单层导入（碰巧）** 两种解释
- **待实机定案（2026-09-05 计划）**：ModKit 打开 **Taikou Main_map**（三·十三）→ Terrain Paint 面板看**图层列表有几个层**：
  - 只有 default 一层 + 地表四种材质 → A（整图导入真实存在，管线零改动）
  - 已有草/林/沙/雪 4+ 个层 → B（管线改型：拆 4 张 8bit 灰度，每层一张）
- 改型影响：生成器去掉 RGBA 打包，输出 L1..L4 灰度 PNG（算法零改，代码更少）
- **2026-09-05 晚裁定（用户推理成立）**：**单层场景导入材质图 = 权重全灌唯一层 → 全图仍单材质，pixel 值不影响视觉**——故"丰富地形材质"必须先建层 → 逐层导入（官方流程），即 B 路线。生成器已改（保留 RGBA 供对照），另输出 **`mask_{ST}_L1_grass / L2_forest / L3_sand / L4_snow / L5_water_{W}x{H}.png`** 五张 8bit 灰度（与高度图同网格；L5 水 = 海/湖，河/路暂未分离，后续 R5 再做）。
- **ModKit 流程（B 路线）**：Paint 面板 **Add Layer**（草=模板 default / 林=flora_habitat_a / 沙=desert_b；雪=无模板暂缓，见三·七；水=自建 river 层可后置）→ **选中该层** → **CTRL+A 全选节点** → Properties 面板 Import Materialmap → 选对应 `mask_*` PNG。每层重复一遍。

**2026-09-05 实机补录（Paint 面板实测）**：
- ① 当前场景图层数 = **1**（`desert_b`）——但注意：这是 **Taikou 原图自带状态**（见三·十三），**不能就此判 A/B**；
- ② 画笔 `Select Template` 下拉 = 10 项（default/desert_a/desert_b/desert_cliff/em f sum/flora_habitat_a/mud_cliff/rock_cliff/soil_b/summerFpreset）——即 terrain_materials.xml **物理池**，与三·七配套表逐项吻合；层 = "模板引用物理 + 自选贴图组"，模板下拉不是"四类地表"的选择器；
- ③ **定案动作**：新场景 Properties 面板导入 `mat_japanmap_hires_4096x2560.png`（RGBA 整图）后数层数——仍单层 + 地表四色 → A 实锤（管线零改动）；变 4+ 层 → B（生成器出 L1..L4 灰度）。

---

## 三·九、terrain.bin 容器（ZGR6RTRN，只读解析）

```
0x00: 魔数 "ZGR6RTRN"
0x08: u32 version = 2
0x0C: 段目录（每段 20B 头）: MIDX | HGHT | NRML | WGHT | PHYM
      （段名 4B + u32 1 + 12B 元信息）
段数据 = 内嵌 PNG 流（引擎魔改容器，部分 IHDR 非标准/分块 tile 化）：
  4 张大参考图（4096 宽：高度 8bit 灰度等）
  + 181 张真 257px 分块 tile（每节点 256+1 边界）
```
- HGHT/WGHT/NRML/PHYM = 高度/权重/法线/**物理层（physics 索引图）**
- 物理层索引 → terrain_materials.xml 的 physics 值 → **party 地形类型采样基础之一**
- 工具：`tools/ExportHeightMatMap/terrain_probe.py`（段名/PNG 流定位）；内部结构未完全解开（魔改 PNG 的 IHDR 不标准），不影响管线（引擎从我们导入的 PNG 自己生成 bin）

**🔴 织丰 terrain.bin 解码进度快照（2026-09-06，逆向专项 [已放弃]）**：
- 目标：`Modules\Shokuho\SceneObj\Main_map\terrain.bin`（16,387,038B，与主安装版字节一致）→ 高度 PNG
- 已破：ZGR6RTRN v2 五段（HGHT@32）；大参考图骨架 = 签名 + IHDR（**W=4096、8bit 灰度**、字段位正确）+ pHYs(2835dpi) + IDAT(8192B) + **裸字节延续**；51 个 PNG 签名（1 大图 + 257px 分块 tile 群）；**zlib 头 78 9C 校验合法（0x789C%31==0）**
- 🔴 卡点：**deflate 位流解码恒失败（invalid distance）**——zlib 头合法但内容被引擎二次变换（推测：分段重排/字节变换/轻混淆）→ **下次逆向重点**：手解固定 huffman 起点 + 找变换；解法备选 = 手解位流逐步对账 257 块（每节点 256+1）→ 拼 4096 组装
- 参照：同样是"骨架+裸数据"的结构，注意 2 字节偏移教训（上次漏解全败在起点偏移 2）。**🔴 2026-09-06 用户裁定：本专项放弃**（解码成本 > 收益）——高度走自生成管线（v2/v3 已达成 Native 水准，见八节）；织丰素材只留观感参照：`Shokuho_V14_colourmap - VARIATION.png`（16,384² RGBA）+ `shokuho_main_map_flowmap.png`（雪流图），游戏内截图备选。

---

## 三·十、部队在坡地上能不能走（速度机制，2026-09-05 反编译结论）

**核心：大地图部队移动速度按"位置地形类型（TerrainType）"算，与高度坡度无关；高度图只管贴地/碰撞/相机。**

- 证据（TaleWorlds.CampaignSystem.dll 符号）：`currentPositionTerrainType` / `GetTerrainTypeAtPosition`（位置采样）、`GetTerrainTypeName`、`GetWeatherEffectOnTerrainForPosition`、`OnTraverseTerrain`
- 链条：**材质权重图（splat/层 mask）→ 地形物理层（terrain_materials.xml）→ TerrainType（草原/森林/沙漠/雪/水/岩）→ 速度倍率**（森林 ≈ -20%、沙漠 ≈ -30% 等，原版内置表）
- **水 = 陆军不可走**（TerrainType water）→ 原版桥梁实体（wooden_brige/map_bridge_b）做渡口；海上 = 海军机制（1.5.x 自带）
- 对你"山坡可走但影响速度"的实现 = **材质通道设计**（不是高度）：山坡给林通道（减速+植被匹配）、山顶裸岩（正常速）、河给 river 层（堵路走桥）、海全 0（海军）
- 坡度美术建议：峰 15~18m、山体底径 ≥60m → 坡 ≈25-28°（原版量级）；>40° 视觉贴墙出戏

---

## 三·十一、flora.bin 植被格式（2026-09-05 解析，生成器可直写）

**三要素之三：植被不是位图，是实例枚举二进制**（原版树/草/花全在这里，xscene 里反而几乎没树）。

```
0x00: 魔数 "FLR2"
0x04: u32 = 文件剩余字节（= 总长 − 8）
0x08: u32 = 实例数 N
0x0C: 记录流（变长步长 = 4 + 名字长 + 84）：
      [u32 名字长][树名 ASCII][payload 84B = 21×float32]
```
| payload 字段 | 语义 | 实测（原版首条棕榈） |
|---|---|---|
| f[0] | 恒 0 | 0 |
| f[1..10] | 朝向矩阵（内嵌旋转+缩放） | (-0.153, 0.592, 0, 0, -0.592, -0.153, 0, 0, 0, 0) |
| f[11] | 尺寸 | 0.611 |
| f[12] | 恒 0 | 0 |
| f[13..15] | **世界坐标 x/y/z（贴地）** | (457.26, 63.86, 4.34) |
| f[16..20] | 恒 1 | 1×5 |

- **树名注册表**：`Native/ModuleData/flora_kinds.xml`（每种植被 = flora_kind：夏/秋/冬季节变体 mesh + LOD 链 lod0 实体→lod5 billboard + 材质 + 碰撞体）。原版实盘 11 种：map_pine_a/b/c、map_tree_palm_a/b、worldmap_tree_acacia/beech/poplar/aspen/high_01、flora_grass_a_visualtest。
- **🔴 生成参数就在层 XML 的 `<meshes>` 段（2026-09-06 实机关联，面板标签即此数据）**——每层每个树种一行：`<mesh name="map_pine_a" index="0" density="6.000" seed_index="5" colony_radius="0.000" colony_threshold="0.000" size_min="0.600,0.600,0.600" size_max="0.800,0.800,0.800" albedo_multiplier="0.836,0.730,0.494" weight_offset="0.500"/>`。语义：`density`=单区生成数量（**面板层条标签 `层名 { 树种[density] }` 的 [] 数字**）、`size_min/max`=实例尺寸范围（×kind 基础模型；原版 0.6~0.8，OnlyTerrian flora.bin 实例 scale 均值 0.527 佐证）、`weight_offset`=权重≥该值才长树（0.5/0.67）、`seed_index`=分布种子、`colony_*`=聚落成团参数、`albedo_multiplier`=着色。**调密度 = 改 density/weight_offset；现生成的"小+稀疏" = 实时生成路径 ≠ 烘焙存档**（见下）。
- **🔴 生成因果链（2026-09-06 用户裁定，实机修正）**：`森林材质（判据）→ 种树动作 → flora.bin（存档）`——**材质图/权重是"哪里有树"的唯一判据**；flora.bin 只是实例存档（笔刷种的树保存后同样进 FLR2）。**导入材质权重会触发引擎现场生成**（实测长树——修正上句认知：重导权重长"小+稀疏"树）；但 **场景默认的封闭式 flora.bin 成品（原版 76,048 棵）才是茂密观感的来源——实时生成 ≠ 烘焙成果**。日本图 = `make_flora.py` 以自身 L2_forest mask 为判据撒点（设计已定），生成参数语义 = 上条 `<meshes>` 表。
- **织丰用法（日本地图范本）**：一种 `flora_plant__mix` 混合种刷全图 281,996 棵——混合种内部随机混合树种，一名字刷满多样性（搭配少量个别种 shō_lowpoly_black_pine 黑松等）。
- **量级**：原版 75,618 条 / 7.6MB；织丰 282,438 条 / 28.7MB（老版本编辑器"全机卡死"风险量级）。日本地图建议 6~7 万。
- 工具：`tools/ExportHeightMatMap/flora_probe.py`（解析验证）。生成器 = 按材质图林通道+高度约束撒点 → 写 FLR2（下一步 `make_flora.py`）。

---

## 三·十二、TileSets：官方无公开工具，两条路线

- **Native/TileSets/**：Bannerlord.gts（117MB 贴图集）+ pages/*.gtp（分页贴图库）+ ValidMaterials.xml（素材注册表）。gts 无字符串表（二进制），**官方 moddocs 亦无 tileset 编辑器/文档**——自建 tileset ≈ 无工具链，不建议。
- **路线 A（原版式 tileset 喷层）**：`vista_tileset="WorldMap"` + paint 层 + splat mask。原版系统。需要掌握层/编辑器笔刷。
- **路线 B（织丰式 colormap 整图，日本地图现成范本）**：`vista_tileset="none"` + `vista_diffuse_name="Shokuho_V14_colourmap"`（整张自绘彩图直接贴地形）+ `vista_detail_albedo_name="aomap"` + `colormap_detail_level="-1"` + 世界 16×16@132=**2112×2112m**。管线输出可直接当该图（零 tileset、零手工 paint）。
- 织丰 terrain 全参数（对标用）：min=-8.0 / max=20.968 / water_level=1.65 / water_exists=true / lod_multiplier=20 / dynamic_flowmap_name="shokuho_main_map_flowmap"。

## 三·十三、🔴 唯一合法工作场景：Taikou mod Main_map（1.2.12 机，2026-09-05 用户裁定）

🔴 **MyMapTest/JapanMap 已废弃**（2026-09-05 用户裁定）；唯一合法场景 = 下述 Taikou 路径。此前误打到 MyMapTest/JapanMap 的补丁（tileset/层/min-max）**作废**，可忽略/删除。

路径：`H:\SteamLibrary\steamapps\common\MB2_Version\MB2_1.2.12\Mount & Blade II Bannerlord\Modules\Taikou\SceneObj\Main_map\`

| 项 | 实测值 |
|---|---|
| terrain 节点 | `node_dimension_x="16" node_dimension_y="10" node_size="96.000"` = **1536×960m**（与本项目定稿推荐一致）|
| min/max_height | -2.821 / 19.618 |
| vista_tileset / vista_diffuse_name | 均为**空** + `colormap_detail_level="-1"`（不走 tileset 也不走 colormap，纯喷层路线）|
| 水 | `water_exists=true / water_level=1.000 / water_material=water_default` / 浅深色 (1,1,1)——未用织丰青绿 |
| 材质层 | **仅 1 层** `<layer name="desert_b">`：summer 变体 `physics_material="sand"`，textures = diffusemap `desert_floor_b` / areamap `desert_floor_a` / normalmap `desert_floor_b_n` / specularmap `desert_floor_b_s` / displacementmap `desert_floor_b_h`，scale (5,5) → **整图单材质** |
| terrain.bin | 5,534,974B，ZGR6RTRN v2，五段 MIDX/HGHT/NRML/WGHT/PHYM（`tools/ExportHeightMatMap/terrain_probe.py` 可读）|
| flora.bin | **12B（空，无植被记录）** |
| 其他 | lod_multiplier=1.0 / use_dynamic_weather_effects=0 / 无雪流图 |

**用处**：① 16×10@96 参数实锤与本项目推荐一致；② **唯一合法工作场景**——所有材质导入/层操作均在此场景进行；③ 图层定义完整格式（summer 变体文本块）可作手工加层/对位模板。

**2026-09-05 晚改造记录（用户指定：采用原版地形材质）**：
- 图层：原版 8 层（default/flora_forest/rock/grass/dirt/dirt2/forest2/river，取自 1.2.12 SandBox Main_map）+ 保留 Taikou 自带 `desert_b` = **9 层**（备份：`Taikou/SceneObj/Backups/scene.xscene.prelayers_20260905`）
- `vista_tileset=""` → `"WorldMap"`；`colormap_detail_level=-1` → `0`（跟原版一致）
- `min/max_height`：`-2.821/19.618` → `0.000/21.000`——🔴 已确认用户导入了本项目高度图（归一化：海 0 / 平 ~0.105 / 普通山 ~0.30 / 富士 0.933）；若沿用旧范围，平原 = -2.82+0.105×22.44 ≈ **-0.46m 会被水面 1.65 淹没**，故必须收敛到 0/21（平原 2.2m / 山 6.3m / 富士 19.6m）。导入实现是 C++ native 不可反编译，归一化数学未实证——**实机查看：平原被淹则再调**。
- mask 导入对照：default←L1 草 / flora_forest←L2 林 / desert_b←L3 沙 / rock←L4 雪（高山区）/ river←L5 水；grass/dirt/dirt2/forest2 不导入。

**🔴 2026-09-05 深夜转向（用户裁定，XML 层注入路线失败）**：
- 实机证据：①导入 L1 草 mask 后**地表无任何变化**（仍全白）——XML 注入的 9 层 + tileset 在本场景编辑器里**未生效**（原因未定案：编辑器内存态/层未重载/贴图未解析，ShaderCache 未清测试）；②新建 scene 实测**无原版层**（层列表只有模板，模板仅物理池项）
- **新路线 = 克隆原版 bigmap 基底**：`Modules\SandBox\SceneObj\Main_map\` 整目录复制到 Taikou `SceneObj\Main_map\`（保留原版全部内容：8 层/WorldMap tileset/水/氛围/背景实体）
- **克隆后必改 4 项**：①`node_dimension_y="10"` + `node_size="96.000"`（原版 16×16@53=848×848 是卡拉迪亚规格；我们 PNG=4096×2560 → 16×10@96=1536×960）→ ②min/max 原版 0/25 可直接用（平原 2.6m/山 7.5m/富士 23.3m，水面 1.65 不淹）→ ③水：原版 `water_exists=false`（视觉海=材质层）→ 改 true + level 1.65 + `wat_main_map_ocean_g` + 青绿双色（即 A 方案 4 项）→ ④重导高度图 + 逐层导 mask（default←L1/flora_forest←L2/rock←L4/L3(沙并入岩,原版无沙层)/river←L5，desert_b 沙层可后补）
- 风险提示：克隆会带入原版 ~37,918 个 game_entity（背景山/图标/桥，按卡拉迪亚位置摆放）——原版量级编辑器可承载；位置需要后续自己整（或保留装饰环）。
- 此前对 Taikou scene.xscene 的全部补丁（9 层/tileset/min-max/水 A）**均被克隆路线取代，视作废弃**；备份仍在 `Taikou/SceneObj/Backups/scene.xscene.prelayers_20260905`。

## 三·十四、先还原原版，再升级日本图（2026-09-05 深夜用户裁定）

1. **第一步：空 scene 还原原版**——用 `NativeExample/` 导出的原版 9 张图（1 高度图 + 8 层权重，16bit 4097×4097）在**空场景**逐项导入，与编辑器打开的原版 Main_map 逐项对照直至一致——用这一步验证我们对"导入流程/格式/四件套/参数"的全部理解
2. **第二步：日本地图升级到同品质**——流程验证通过后，再用日本管线产物（hm + masks）复刻同样流程，品质按上文硬指标（高低差/海岸线/过渡）对齐
3. 🔴 **Paint 与地形绑定坑（用户实测）**：TerrainPaint 面板与当前地形**强绑定**——Remove Terrain（删除当前地形）后 Paint 功能失效（删了就回不来，除非重建地形）。所以**克隆原版给 Taikou 必须整文件夹复制（含 terrain.bin），禁止"先删地形再重建"**；在克隆的现有地形上直接 Import 覆盖（高度图 Import 替换 HGHT，层权重用 Import Materialmap 逐层盖）

---

## 三·十五、原版剪枝对照与 copy1 差异定位（2026-09-05 午夜）

**新素材 `Main_map_onlyTerrian`**：原版 Main_map 删完场景实体后仅剩地形——16×16@53、8 层、原版水、atmosphere.xml、navmesh.bin、**flora.bin 保留（75,618 条植被实例）**、scene 里剩 6 个树 LOD mesh + 19 条河/海 path。

**A. 之后"要摆什么"清单（原版元素分类，2026-09-05 深度 diff：full vs onlyTerrian）**：

| 类别 | 原版数量 | 内容 | 日本图处理 |
|---|---|---|---|
| 地形底盘（已就绪） | — | terrain.bin + node 表 + 8 层 + 水 + tileset/colormap | 用我们的 hm+masks 覆盖即可 |
| 大气/雾 | — | atmosphere.xml + references.txt | 保留原版 |
| **① 据点外观群** | ~3.6 万 mesh（≈37,906 game_entity） | 城/堡/村外观 = mi_emp/mi_aserai/mi_khuz/mi_vla/mi_stu（城墙/塔/房屋/宅邸/广场/市场/水井/竞技场…每城一组，含 LOD 链）+ village_*（村群）+ worldmap_hideout_a/b（藏身处图标） | 🔴 **样式是卡拉迪亚文化建筑**——日本 mod 得换日式素材（织丰自带 sho_* 系列）；原版这套仅作**占位/体量参照**，数量用我们 300 城 |
| **② 功能实体** | 桥×36 / 码头/栈桥 mi_docks_*、mi_pier、mi_ship / emp_bridge_g | 渡口/封锁（河海阻挡）配装随行 | **必须**——按河流/渡口位置摆（日本图自绘位置） |
| **③ 背景装饰** | map_mountain_rock_* / map_rock / mainmap_cliff_c/f/g / waterfall_rock / map_rock_big / _snow 雪石变体 / empire_road_plaza | 山崖/岩石/瀑布/道路广场——外围视觉 | 按美术量级摆（≤原版 1.0×，织丰 4.1 万崩编辑器前科） |
| ④ 附属数据 | decal_component ×2,067（贴花）+ scripts ×2,594 + visibility_masks（LOD 遮罩）+ physics/body_flags → 全随实体走 | — | 随①③搬运 |
| ⑤ 编辑辅助 | editor_plane/cube/cone/cylinder ×17、selection_sets ×1,366（编辑选择集） | 编辑器用 | ❌ **不用补** |
| 线状路径 ×20 | 保留 19 条（river_*/ocean/mediterranean/volga/nile…）；**仅 lake_bat 被删** | 河/海定形曲线（水系效果用） | 日本图自绘 |
| 雪流图 main_map_snow_flowmap | — | 季节雪覆盖 | 日本图自画雪区（后期） |
| 植被 flora.bin | 7.9MB（75,618 条）**保留** | 实例枚举 | 按林通道重撒（make_flora.py） |

⚠️ 只留 6 个 mesh = map_pine_a/b/c + worldmap_tree_acacia/beech/high_01 —— 是 flora.bin 实例引用的 LOD mesh，**必须保留**（删了植被就断供）。navmesh.bin 保留（大地图导航，日图后续重算/重生成）。

**BigMapLearn 分析报告档案（2026-09-05 22:19）**——基准 = OnlyTerrian（原版参数全齐）：
- ✅ 已对齐（本次新建参数填正确）：terrain 标签 12 项（16×16@53 / min 0 / blend_type 1 / blend_amount 1 / layer_distance 10000 / albedo 0.67 / detail_tile 1.0 / **WorldMap tileset** / **colormap 0** / flowmap 已挂 / ao 4）+ 水 6 项（level 1.8 / exists true / 浅深色原版组）
- ❌ 待修 7 项：① layered 仅 1 层（rock_cliff，未注入 8 层）② node masks 全=1（1 层时代残留）③ `water_material=water_default`（应 ocean_g）④ `use_dynamic_weather_effects=0`（应 1）⑤ `lod_multiplier=1`（应 3）⑥ `max_height=24`（应 25）⑦ flora.bin 空（应重撒）/路径 0（应自绘）
- ⚠️ 即使 ①-⑥ 全修，**白问题不能保证解决**（copy1 同组修复已验证无效，白根因仍悬案）——BigMapLearn 的修复只是"逼近原版配置"。下一步：待白根因定位后再批量套。

**B. copy1 错误清单**（用户判定：高度图与水平面两者一致；其余差异即错误）：

| 项 | 原版/OnlyTerrian | copy1 | 嫌疑 |
|---|---|---|---|
| 🔴 **node masks**（每节点 × 4 季的"哪些层可用"掩码） | 各节点 1/61/255…按内容取值 | **全是 1（只认层0 = default）** | **头号嫌疑**：其他 7 层权重导了/刷了也不渲染；default 层冬季变体 = 雪白贴图 → 全图白、刷笔只变纹理 |
| 动态天气/雪流图 | 1 + main_map_snow_flowmap | 0 + 空 | 雪机制未开（与白的关系待验证） |
| min/max | 0 / 25 | 0 / 24 | 槽位小差 |

**修复清单（OnlyTerrian 验证为"正常原版观感"后执行）**：① node masks 4 季 × 256 节点 → **255**（全层可用，正确性优先）② max 24 → 25 ③ dynWeather 0→1 + flowmap → main_map_snow_flowmap。

**🔴 2026-09-05 晚 修复结果：失败，理论作废**——三项全部执行（1024 掩码→255、max 25、天气 1+流图）后重开 copy1 **仍全白**。记录：- ✅ 已确认的事实：OnlyTerrian（原版剪枝、含原版地形全参数+原版 terrain.bin）打开 = **正常彩色**；copy1（新场景 + 原版 8 层注入 + 原版高度图 4097 成功导入 + 上述修复）打开 = **全白**。差异面 = 层注入路线/terrain.bin 权重状态/node 表生成时机——**仍未定位（三候选：A 层注入时效问题 B copy1 terrain.bin 的 WGHT 权重数据无关渲染 C. editor 季节/配置另一属性——全部悬案）**
- 已试（全部失败/无变化）：XML 注入 8 层；挂 WorldMap tileset；colormap 0；水参数原版化；node masks 255；天气+雪流图；ShaderCache 改名；L1 权重导入
- **归档物（勿删，复检用）**：`Taikou/SceneObj/Backups/`：scene_copy1.prelayers_20260905、scene_copy1.premaskfix_20260905、Main_map_copy1_full_20260905（copy1 全量）· copy1 现行 scene.xscene（含所有修复，仍白）· OnlyTerrian = 白对照黑白基准（正常彩色）· 复盘入口 = 本节
- **下一步候选方向**：放弃"空场景重建"路线，直接**在 OnlyTerrian 上覆盖日本 hm + masks**（其原版参数/节点表/层全齐且验证正常——等于"还原原版"已由 OnlyTerrian 达成），即"剪枝原版 → 覆盖日本地形"路线；此路线恰好避开"删地形 Paint 失效"坑（不用删，直接 Import 覆盖）。

**🔴🔴 2026-09-06 白之谜闭环（BigMapLearn 单步控制实验，实锤）**：
- 实验链（每步只动一处、每步备份）：① 基线 desert_a 单层（普通贴图）→ 彩色 ② 掩码 1→255 → 无副作用 ③ **仅加原版 default 层（图集贴图 `ground_grass_b_d_mainmap`）→ 全白** ④ **仅挂 `vista_tileset="WorldMap"` → 全绿彩色**
- **根因（终审）**：**层引用 WorldMap 图集页贴图（`*_mainmap` / `main_map_*`）时，`vista_tileset` 必须 = `WorldMap`，否则引擎找不到贴图 → 整层渲染白/丢失**；`desert_floor_*` 类普通贴图不依赖 tileset（所以 desert 层一直正常）。
- 附带证实：terrain.bin 里旧权重的通道映射到**新层表的第一个层**（default 满权重 → 整图草绿）→ 正确流程 = 加完层后逐层 Import 各层权重覆盖。
- 复盘：此前的层层失败（taikou 9 层白/copy1 白）均与"tileset 与层贴图组合"耦合，当时混入了掩码/层序/引用等变量未能隔离——**单变量 + 逐步 + 每步备份**才是正解（经验已入 wheels）。

**🔴🔴🔴 2026-09-06 雪观感之谜闭环（BigMapLearn vs OnlyTerrian 实机对照）——"原版雪山白"不是雪，是 Vista Textures 段**：
- 现象：同配置（同流图/同 weather/同权重/同高度）下 OnlyTerrian 山是白雪山、BigMapLearn 是黄土木——面板参数一样，渲染不同
- 根因：**terrain 标签的 Vista Textures 段字段不同**——OnlyTerrian `vista_diffuse_blend_type=1 / vista_layer_detail_distance=10000 / vista_albedo_multiplier=0.67 / colormap_detail_level=0` vs BigMapLearn 半残值（blend 0 / layerdist 1 / albedo 1 / colormap -1）——**原版雪山白观感 = 白岩贴图 × Vista 冷色全局调 × 0.67 明度**，不是雪线/动态雪算法（`weather+flowmap` 只是季节雪覆盖层，编辑器里默认看不出差异）
- 对齐后 BigMapLearn 雪就出来了（实机 2026-09-06 验证）
- **排查口诀更新**：场景"同一份数据两个观感" → 先对 **Vista Textures 段字段组**（blend_type/blend_amount/layer_detail_distance/detail_tile/albedo_multiplier/colormap）→ 再对大气 → 最后才怀疑数据层（node/terrain.bin）

**🔴🔴🔴 2026-09-06 现状快照（还原原版 ≈ 完成，验收剩植被对照）**：
| 项 | 状态 |
|---|---|
| 高度图（4097）/四件套 16×16@53 / min-max 0-25 | ✅ |
| 8 层材质（注入原版层块、字节一致）+ tileset=WorldMap + 掩码 255 | ✅ |
| 8 张原生渐变权重逐层导入 | ✅ |
| 大气白天（time_of_day 对齐）/ Vista 段 / 动态雪流图 | ✅ |
| 水 1.8/true（`water_material` 待改 `wat_main_map_ocean_g` 一行） | ⏳ 一行 |
| **植被 = 拷贝 OnlyTerrian flora.bin（76,048 棵）已验证可行** | ✅（这是"还原原版"的最终手段；live-gen 复现密度属非必经研究） |
| 季节雪过渡 / path 河道曲线 / 背景装饰实体 / navmesh | ⏳ 后续 |
- 判定：**还原原版跑通 = BigMapLearn vs OnlyTerrian 渲染一致**；跑通后进入日本图阶段（v2/v3 生成器 + 软化 mask + 以上全部模板直接复用：四件套/导入流程/黑·白·雪排查/层序/flora 参数语义）。
- 带出成果：三大渲染谜（黑=time_of_day / 白=tileset / 雪山=Vista）全部闭环并入 wheels.d/terrain.md。**实验方法论**（单变量+逐步+每步备份+对照组场景 diff）为本轮最大收益。

---

## 四、ModKit 操作顺序

0. 🔴 **工作场景 = 三·十三 的 Taikou Main_map（1.2.12 机）**；MyMapTest/JapanMap 已废弃，勿用；**基底 = 克隆原版 `SandBox\SceneObj\Main_map\` 整目录**（XML 层注入路线已废，见三·十三深夜转向）
1. **新建自己的空场景**（不要克隆原版）
2. 右侧 Inspector → **Terrain 面板** → 按上表四件套填（hires 主档：X=16 Y=10 Size=128 Dim=256；或原版规格档 X=16 Y=16 Size=53 Dim=256）
3. `Import Heightmap` → 选 PNG（16bit  或 8bit）→ `Import Materialmap` → 选材质图（一键 Import；或官方流程：Add Layer→选中→CTRL+A→导入）
4. 若地表整体变色不对 = **图层/通道顺序与引擎材质层不匹配** → 交换 R/G/B/A 次序重导试 1-2 次（无需改图）
5. 布局据点 = 场景 `game_entity` 与 settlements.xml 同名即绑定（详见《定居点放置机制》章节）

---

## 五、定居点放置机制（已验证）

1. **战役层坐标**（玩法/寻路/部队用）= `Modules/<你的mod>/ModuleData/settlements.xml` 的 `posX/posY`（引擎读的值 = 地图场景里同名实体 transform 坐标）
2. **地图图标** = 场景 `game_entity`：`name` = 据点 StringId；`tag` = 类型（town/castle/village/hideout/…）；`transform` x,y = 地图坐标（z 贴地）
3. 让游戏知道这是你的地图 = 模块目录 `SceneObj/` 里场景文件夹与引擎默认名一致（原版 `main_map`；同名覆盖即替换大地图）

---

## 六、坑点清单（全踩过）

| 坑 | 症状 | 解法 |
|---|---|---|
| 富士蓝白雪被判湖泊 | 高度/材质图上富士=蓝湖/黑斑 | 最大白点簇雪帽检测 + PEAKS 地标豁免（前文§二） |
| 亮度映射方向反 | 平原发白、山地发低（Koei 平原亮/山地暗） | 亮度分段归位 + 山影由 detail 差分驱动 + 亮度门控增益 |
| detail 粒度判断 | 平原路网纹理 detail 比山地大（判不了山） | 弃用细节幅判据；山=uniform 暗绿、平原=细纹密 |
| σ 按输出分辨率等比放大 | 4096 档细节被 σ8 抹平（细节能量 ×2.39 差异） | σ 定值 2.0；基底 σ 按源图尺度 mb 换算 |
| 白判抓平原/城市亮色 | 平原进雪/岩道 | 白判加绿度排除（平原 g−b>10，雪/滩 g≈b） |
| 材质图海岸线白色 | 海岸白带被判雪 | 雪-高度交叉：hm≥0.42 才准当雪，不够高的白系降级沙 |
| 小素材管线失效 | 704 图河/路/滩爆量（river 8.5%/yline 13.8%） | **尺度标定**：小素材先 LANCZOS 放大到 MASTER_W=15840 再走管线（几何阈值按 15840 校准）；色判与分辨率无关 |
| LANCZOS 大倍数放大振铃 | 小素材出"碎斑假细节" | 此为插值伪影非真地；小素材只用于预览档 |
| matpreview 溢出回绕 | 双通道像素 uint8 回绕成品红 | clip 0..255 |
| 查看器不认 RGBA | 材质图黑+绿线 | 引擎版保持 RGBA；人看用 matpreview |
| 导入后地表整色不对 | 引擎图层/通道顺序不匹配 | 换 R/G/B/A 次序重导 1-2 次（无需改图）；严格做法 = 按三·七层序对位 |
| 大地图场景编辑器全机卡死 | 实体/植被超量 | 做自己的图别超原版规模；织丰 41,581 实体 / 28 万植被案例 |
| ~~"海=0 引擎自动铺水面"~~ | 岛国地图没水面 | 引擎不自动铺水；water_properties 单独开关（三·六） |
| Remove Terrain 后 Paint 失效 | 想"删了地形重做/换图" | Paint 与当前地形强绑定：**整目录克隆（含 terrain.bin），禁止删地形**；覆盖一律用 Import（三·十四） |

---

## 七、目录与复现

```
tools/ExportHeightMatMap/
├── make_heightmap.py        # 全管线脚本 v2（参数/裁定/坑点全注释）
├── proto_relief.py          # 细节调参实验沙盒（v6.x 迭代用；产物 Output/_probe/）
├── flora_probe.py           # flora.bin 解析探针（FLR2 格式）
├── terrain_probe.py         # terrain.bin 段结构探针（ZGR6RTRN）
├── SourceMap/               # 素材（gitignore）：japanmap_hires.png、TaikouMap2.png
└── Output/                  # 产物（gitignore）：hm_*_16bit.png / mat_*.png / matpreview_* / qa_* / _probe/
```
- 素材与产物不入库（gitignore）——产出靠脚本重跑
- 富士等固定地标：`PEAKS` 表（== (10500,7000) 等）——仅富士一行定了坐标；加别的高峰按同格式加行
- v2 后高度细节 = 结构感知排水网（见下节八）；v1 的「σ=60/150 山影基底」参数已废弃（被「整域山脊变换 + 半分辨软烤」替代）

---

## 八、高度图细节 v2→v3（2026-09-06，Native 卡拉迪亚水准）

**验收基准**（`NativeExample/terrain_heightmap.png` 4097² 实测）：
- 陆地 CDF：q01=0.013 / q05=0.025 / q50=0.255 / q95=0.567 / q99=0.698（大量黑谷+中灰山体+亮峰尾）
- 山窗（0.10-0.29 宽）：std=0.069~0.164；band(6-40px) std=0.035
- 空间形态：「云状亮块 + 深黑宽谷带 + 树状细支脉」，负偏斜（谷深>峰高）、暗占 66%

**v3 交付实测（同口径）**：land CDF 逐分位对齐（q05 0.033/q50 0.273/q95 0.560）、山窗 std=0.124 入 native 区间、黑谷带/亮峰块/微支脉齐全。

**v3 模型（make_heightmap.py 三段式）**：

| 段 | 做法 | 解决的问题（迭代实测） |
|---|---|---|
| 宏观 | 亮度先 σ=200源px 大平滑再分段 h_base（山档 0.38-0.74，γ1.4 后 0.26-0.65） | 画法斑点/林冠/云影≠海拔（v1 黑团 blob） |
| 细节 | 多倍频 fbm 整域山脊变换 `r=1-|2n-1|`（|n-0.5| 过零=连通排水网）+ 源图笔触方向场 + 半分辨软烤 + massif/宏脊/宽谷带三层 | 单层噪声=碎斑；高频超载=钢毛；黑虫洞（clip[-0.36,0.34]） |
| **分布匹配** | **陆地 rank 匹配 Native CDF**（native_quantiles.py 预计算 129 分位表；匹配前 core+3.6×massif 结构放大；**匹配必须放平滑之后**） | 局部参数调不出「全图层次」——分布整层对撞最有效；匹配前平滑=白谈 |

**🔴 三处必死坑（本轮全踩）**：
1. **评估口径**：只对 band(6-40) 局部纹理 → 全局还是平的（两轮用户打回）——正确口径 = **陆地 CDF 逐分位 + 山窗 std + 空间形态** 三者同验
2. **分布匹配的单调性**：rank 匹配不动空间形状（平台匹配后仍平台）——先「结构放大」（massif×3.6 注入 core）再匹配；且**细纹回叠破坏 CDF** → 必须「精排」收尾
3. **8bit/16bit 单位**：hmv 平滑产物是 8bit 量纲——排序输入 /255，写回 ×255，最后再 ×65535.0 转 16bit（写错两档数值全错；富士黑洞 = 单位写错 + 豁免区误清）

**其他裁定**：富士雪帽豁免区保留原裁定值（1.0 锥+平滑 0.86 雪顶），不参与匹配；匹配后微排水网（±0.03）铺细节补充「树状支脉」。种子 SEED=20260906 固定可复现；调参沙盒 `tools/ExportHeightMatMap/proto_relief.py`。

**调参信号**：黑谷不够深 → 检查重排（q05 应 ≈0.025）；山域太均质 → massif×3.6 注入量；「碎 vs 润」→ 微排水网 0.030 / 终平滑 σ2.2。

---

## 八·五、熵维度修复 v3G（2026-09-06，文件大小硬指标）

**用户硬指标**：PNG 文件大小（native 16.9MB vs 我们 2.83MB 差 6 倍——「细节不在一个维度」）。**拆解后真正对等的指标 = 每陆地像素字节 B/land-px**：
- native：16.8Mpx 总 / **12.8Mpx 陆地（76%）** / 17.9MB → **1.39 B/land-px**
- 我们（4096×2560）：10.5Mpx 总 / **1.75Mpx 陆地（17%）** / 17.9MB 同口径 → 3.27MB → **1.87 B/land-px（已超 native）**
- ⚠️ 总字节差 5 倍的主因 = **陆地占屏比 17% vs 76%**（海=0 → PNG 压到近 0 字节）+ 像素数 10.5M vs 16.8M（世界 2048×1280 vs 848×848，PNG 必须 1:1 顶点不能加水）

**v3G 三处修复**：
1. **float 域全链路**：旧链中段经 uint8 量化 → 16bit 图实际只有 8bit 信息（压缩率暴高）——平滑/重排/叠加全用 float，最后 ×65535 一次转
2. **低幅白噪熵钩子**：native band(2-6) std=0.006 是**空间不相关白噪**（Paeth 滤镜压不动→高熵）；我们细纹是平滑涟漪（压得动→熵低）。σ=0.006 白噪铺满陆（×4·h·(1-h) 两端保护窗——否则 q05 被压塌、保护 0.3+0.7·env 全陆铺）——B/land-px 从 0.47 → 1.87
3. **细节回补**：重排（CDF 钉死）后回补「平滑损失的 1-8px 细节」（fine2×0.9 + 微排水网 ±0.03）

**经验**：PNG 大小 = 信息熵的可视代理；「平滑涟漪」再多也压得动，「逐像素白噪」一点就压不动 —— 熵感来自不相关高频（native 雷达图/卫星 DEM 特征），涟漪式细节补不出熵。
