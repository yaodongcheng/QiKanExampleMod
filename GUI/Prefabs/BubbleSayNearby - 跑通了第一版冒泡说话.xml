<Prefab>
  <Window>
    <!-- 全屏容器，确保 ListPanel 覆盖整个屏幕 -->
    <Widget WidthSizePolicy="StretchToParent" HeightSizePolicy="StretchToParent" DoNotAcceptEvents="true">
      <Children>
      <!-- ListPanel: 负责循环生成，但不负责排版（因为子元素尺寸为0） -->
      <ListPanel DataSource="{Bubbles}" WidthSizePolicy="StretchToParent" HeightSizePolicy="StretchToParent" DoNotAcceptEvents="true">
        <ItemTemplate>
          
          <!-- 【核心技巧】根节点：尺寸设为 0 -->
          <!-- 作用：欺骗 ListPanel，让它以为这个控件不存在，从而所有气泡的起始点都在屏幕左上角 (0,0) -->
          <Widget WidthSizePolicy="Fixed" HeightSizePolicy="Fixed" SuggestedWidth="0" SuggestedHeight="0"
                  DoNotAcceptEvents="true">
            
            <Children>
              <!-- 实际内容节点：在这里进行坐标偏移 -->
              <!-- 注意：这里不需要 Stretch，给他一个具体的尺寸或者 CoverChildren -->
              <Widget WidthSizePolicy="Fixed" HeightSizePolicy="Fixed" SuggestedWidth="200" SuggestedHeight="100"
                      PositionXOffset="@PosX" 
                      PositionYOffset="@PosY"
                      Sprite="BlankWhiteSquare_9" 
                      Color="#00000088"> <!-- 半透明黑色背景 -->

                <Children>
                  <!-- 文本内容 -->
                  <RichTextWidget WidthSizePolicy="StretchToParent" HeightSizePolicy="StretchToParent" 
                                  Brush="Sho.Debug.Text" FontSize="@Scale"
                                  Text="@Text"
                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Children>
                
              </Widget>
            </Children>
          </Widget>
          
        </ItemTemplate>
      </ListPanel>
      </Children>
    </Widget>
  </Window>
</Prefab>