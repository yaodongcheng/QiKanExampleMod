# 玩家飞行 — 实施方案

> **状态：施工中**（2026-09-21 立）
> **一句话**：主角在场景里像超人一样飞 —— 脚底一块**看不见的木板**把人托起来，身上播飞行动画，方向跟镜头走。
> **本文与 [自定义移动管线.md](自定义移动管线.md) 的分工**：那份讲**原理与已验证的数据**（为什么能飞、9 条死路、实测数字）；本文讲**怎么落地**（状态机、操作键位、XML、资产清单、C# 文件、分步、验收、回退）。
> 🔴 **两份不要重复维护同一份事实** —— 新数据进那份，新步骤进这份。

> **三个词先说清楚**：
> · **载具** = 那块看不见的实心木板。玩家站上去，引擎就把他算在它表面上。
> · **逐帧瞬移** = 每帧直接把木板坐标改掉（不是施加物理速度）。**这是唯一能载人的移动方式。**
> · **原始按键** = 直接问引擎"这个键现在按着没有"，绕开项目的 `ModInput`（原因见 §四·1 挂载点）。

---

## 🔴 交接（2026-09-21 收盘 · 给下一个 session 先读这一节）

### 一句话现状

**✅ 核心玩法闭环已实机跑通**：二段跳起飞 → WASD 飞行（含姿态动画）→ 短按贴地落地 / 长按下降 → 撞地自动落。
2026-09-21 收盘状态：冻结（`aipause`）/ 机身朝向 / 二段跳起飞 / 落地手势 / 撞地自动落 —— **全部实机验证通过**。
剩下两件：**N1 法阵**（卡资产，要人导）· **N5 运动相机**（代码已落地，**4 个机位是猜测值，要人实机调**）。

### 已验证 / 未验证

| | 项 | 状态 |
|---|---|---|
| ✅ | **载具托人**（原方案唯一拦路虎） | **通过**。日志 `dz=0.37` 全程恒定，人从 `z=5.58` 被抬到 `11.58` |
| ✅ | **镜头方向解算** | **通过**。平视时 `look` 的 z ≈ 0（曾错取成相机上方向，导致一路朝天飞） |
| ✅ | **逐帧瞬移移动** | **通过**。用户实机确认"正常飞了、丝滑" |
| ✅ | 9 条飞行 TRF 重跑 | `--pelvis none`：位置轨全零、循环闭合 0.000° |
| ✅ | C# 代码 | `Flight/` 4 文件 + 命令 1 文件，编译通过，产物核对过 |
| ❌ | 冻结（第一版：Player + 清零移动输入） | **实机证伪**，见下节 |
| ❌ | 冻结（`IsDisabled`） | **实机证伪**：引擎 `UpdateCamera` 每帧冲它 |
| ✅ | **冻结定案（`aipause`）** | ✅ **2026-09-21 实机通过** —— `Controller=AI` + `SetIsAIPaused(true)`，已设为默认档 |
| ✅ | **WASD 驱动飞行** | ✅ **可用**（冻结定案后即成立） |
| ✅ | **动画可播** | ✅ **2026-09-21 实机通过** —— 7 条 clip 入包 + XML 接线，`custom.do_anim` 逐条验过 |
| ✅ | **二段跳起飞** | ✅ **2026-09-21 实机通过**（N2） |
| ✅ | **机身朝向（朝实际移动方向）** | ✅ **2026-09-21 实机通过**（N3）—— W 前 / A 左 / D 右 / S 转身朝镜头 |
| ✅ | **落地手势（短按贴地俯冲 / 长按下降 / 撞地自动）** | ✅ **2026-09-21 实机通过**（N2+N4） |
| ⬜ | **转向平滑渐变** | **待办**（现在是瞬时转向，观感生硬） |
| ⬜ | 法阵显示 | **未做**（N1）—— 网格还没进包 → 探针判定"没挂上"，只飞不显示法阵 |
| 🔶 | **运动相机** | **已实现**（N5）—— 4 机位（悬停/巡航/加速/瞄准）+ 渐变 + 右键瞄准；**参数是猜测值，待实机用 `custom.flight cam` 调** |

### 当前怎么操作（🔴 **2026-09-21 实机验证版，与正文旧方案不同，以本节为准**）

| 操作 | 手势 | 说明 |
|---|---|---|
| **起飞** | **跳跃中按空格**（二段跳） | `!IsOnLand()` + 空格按下沿。不要求"刚跳过"，所以**从高处坠落时按空格也能飞**（能自救） |
| 起飞（后备） | 站着长按空格 0.65s | `tune longpressjump 0` 可关 |
| **飞行方向** | **W A S D** | 方向 = 镜头方向（含俯仰）；**机身朝实际移动方向**（W 前 / A 左 / D 右 / S 转身朝镜头） |
| **冲刺** | 左 Shift | 26 m/s（巡航 9） |
| **落地 · 短按** | **贴地或俯冲时**按一下空格 | 贴地 = 板顶离地 ≤ 8m（≈ 刚二段跳进浮空的高度）；俯冲 = 镜头竖直分量 ≤ −0.42。**高空平飞短按不落地** |
| **落地 · 长按** | 按住空格 = **持续下降** | 垂直分量被 −6 m/s 接管（不看镜头俯仰），**松手停** |
| 落地（自动） | 撞地即落 | 板顶触地（容差 0.25m）→ 自动进落地 |
| 备用手感 | 小键盘 8 / 2 / 4 / 6 | 隔离对照组，见下 |

> **小键盘为什么留着**：它没绑任何游戏移动键。万一 WASD 表现不对，用它飞一圈就能判定问题出在
> 「冻结没生效」还是「板本身飞不动」—— 一个不用改代码的隔离对照组。

### ✅ T1b 定案（2026-09-21 实机验证通过）：`aipause`

**现行做法 = `Controller = AI` + `SetIsAIPaused(true)`**（已设为默认档，起飞自动冻、落地自动还）。

**它为什么是答案**：飞行要同时满足三件事，只有这一档全中 ——

| # | 要求 | aipause 为什么满足 |
|---|---|---|
| ① | 玩家 WASD 被拿掉 | 切 AI 就成立（用户在地上实测：AI 档 WASD 完全无效） |
| ② | 板照样托人 | 切 AI 不影响碰撞承载（日志 `player.z - board.z` 恒为 **0.37** = 板顶面高度） |
| ③ | **不让"脚下平台在动"干扰 AI** | 🔴 这是 `ai` 档失败的原因：AI 有自己的**平台自适应**，板一动它跟着调整 ⇒ 在板上乱走/滑移。**暂停 AI = 那套自适应整个不跑** |

**实机判据**：`[Flight-Diag]` 里板停之后人与板**双双静止、偏移恒定不漂**（若是 AI 在走路，会往回收敛——它没有）。
WASD 已可正常用于飞行。

### 🔴 三条已走过的死路（别重走）

| 做法 | 结果 |
|---|---|
| 保留 `Controller=Player`，每帧清零 `MovementInputVector`/`MovementFlags` | ❌ 无效。按 W 时 `engineMove` 恒为 `0x0`，人却比板快 2.25 m/s —— **玩家走路不看那两个量**（对玩家是被动镜像） |
| `MissionMainAgentController.IsDisabled = true` | ❌ 无效。反编译实锤：`MissionScreen.UpdateCamera`（每帧跑）先置 true 再置回 false，**我们的值活不过一帧** |
| 只切 `Controller=AI`、不暂停 | ❌ 输入冻住了，但 AI 跟移动中的板较劲（滑移/乱走） |

### 换档命令（飞行中热切，不用重编译）

```
custom.flight freeze                查当前档
custom.flight freeze aipause        现行做法（默认）
custom.flight freeze off            临时关掉冻结（按 WASD 角色会自己走下板）
custom.flight freeze ai | aidetach | none | flags | ctrloff   其它候选/对照
custom.set_controller               查控制权（Controller=... playerControlled=...）
custom.set_controller player|ai|none  手动切控制权（不冻输入，仅供排查）
```

**判据**（`[Flight-Diag]` 每 0.5 秒一行）：

```
ctrl=AI frozen=AiPaused        ← 现行档应有的样子
playerVel=(..)|n  boardVel=m   ← n ≈ m = 只是被板带着（跟随滞后，无害）；n 明显多出 2~3 = 他**自己在动**
offset=(x,y)                   ← 恒定 = 好；持续变大 = 出问题
z 差（player.z - board.z）      ← 恒为 0.37 = 板在托人；变大 = 板没托住
engDisabled=a/b                ← 引擎相机冲刷前/后的 IsDisabled（排查 IsDisabled 那条死路用）
```

> **一档不通就换下一档**，别同时改两个东西（方案 §交接「三条血泪」第 ① 条）。
> 全部不通 → 回到「Controller=Player + 逐帧把玩家 X/Y 拽回板心」这条路（知识文档已证 X/Y 可写、Z 不可写）。

### 🔴 `ctrloff` 档的来历（2026-09-21 二次反编译，用户提问逼出来的）

**问题**：「`Controller=Player` 的时候，就拦不住 WASD 了吗？」—— 不是拦不住，是我第一次只找了一条路。

**答案**：`TaleWorlds.MountAndBlade.View.MissionViews.MissionMainAgentController` 上有
**`public bool IsDisabled { get; set; }`**（公开可写），而玩家输入就写在这个守卫里面：

```csharp
public bool IsDisabled { get; set; }

// OnPreMissionTick 里：
if (... && !Mission.MainAgent.IsAIControlled && !IsDisabled && _activated)
{
    InteractionComponent.FocusTick();
    ControlTick();          // ← 玩家的移动输入就写在这两行里
    LookTick(dt);
}
else { LockedAgent = null; }
```

**为什么它是甜点区**（三个条件同时成立，这是前几档都做不到的）：

| | `Controller` | 引擎写玩家输入 | AI 会驱动他 | 板托人（已验证的那套） |
|---|---|---|---|---|
| 第一版 `flags` | Player | **写**（清不掉，实机证伪） | 否 | ✅ |
| `ai` / `aipause` / `aidetach` | **AI** | 不写 | **会**（除非暂停/摘编队生效） | ❓ 未知 |
| **`ctrloff`** | **Player（不动）** | **不写** | **否** | **✅ 同已验证的那套** |

**代价**：`LookTick` 一起停（角色不再自动转向镜头）—— 对飞行无碍，机身朝向本来就由 `TurnBody` 每帧写。
**相机不受影响**（走 `Mission.OnTick` 的 `_missionState.Handler.UpdateCamera`，另一条路）。
`InteractionComponent.FocusTick`（互动提示）也一起停，飞行中不需要。

⚠️ **落地必须还回去**：`ExitFreeze` 里对 CtrlOff 档单独还原 `IsDisabled`（不还 = 落地后玩家永久不能走）。
它不依赖 agent 存活（控制器是 MissionView，与 agent 无关），所以放在归还逻辑最前面。

### 🔴 T1 第一版为什么失败（2026-09-21 实机，必读，别再走一遍）

**做法**：保留 `Controller=Player`，每帧把引擎刚写进去的 `MovementInputVector` / `MovementFlags` 清零。

**反编译依据**（看起来无懈可击，但结论错了）：

```
① OnPreMissionTick → MissionMainAgentController.ControlTick   ← 唯一的写入者
② OnMissionTick                                                ← 我们在这里清零
③ TickAgentsAndTeams → Agent.Tick                              ← 唯一的读取者
```

时序**确实**卡在 ① 之后、③ 之前 —— 但**玩家走路根本不看这两个量**。

**实机证据**（`Debug/StoryEngine_RuntimeLog.txt`）：

| 事实 | 数字 |
|---|---|
| 按着 W | `engineMove=0x0`（**恒为 0**，MovementFlags 全程没被写过） |
| 板速 | 8.95 m/s（与 `vel=9.0` 吻合 ✓） |
| **人速** | **11.2 m/s** —— 比板快 **2.25 m/s**（≈ 步行速度）= 他确实在自己走 |
| 人板距离 | 0.12 → 1.17 → 2.18 持续变大 |

**结论**：`MovementFlags` / `MovementInputVector` 对玩家是**被动镜像**，不是驱动源。
**真开关是 `Controller`** —— 用户在地上实测：切成 AI 后 **WASD 完全无效**（冻结成立）。

**新的怀疑对象**（用户提出，待验）：AI 档会「乱走」，可能不是没冻住，而是
**AI 有自己的一套「脚下平台在动」的自适应机制** —— 板每帧把人挪走，AI 要把 agent
带回它认定的位置，于是自己走回来。对症档 = `aipause` / `aidetach`。

### TODO（按顺序，一次只推一项）

| # | 事项 | 依赖 | 谁做 |
|---|---|---|---|
| ✅ | ~~T1b 冻结档定案~~ → **`aipause` 实机通过**，已设为默认档 | — | 完成 |
| ✅ | **机身朝向（2026-09-21 用户裁定）** —— 见下节 | 无 | 完成 |
| ✅ | **T2 动画入包（2026-09-21）** —— 7 条 clip 已进 `Taikou\AssetPackages\lwn_taikou_anim.tpac`（176983 B）。🔴 **只导了 7 条**：`Pose_U`/`Pose_D` 用户判定属 FM 体系、**本轮不导**；**法阵仍未导** | — | 完成 |
| ✅ | **T4 XML 接线（2026-09-21）** —— `Taikou\ModuleData\action_types.xml` +4 行、`action_sets.xml` +4 条绑定（`act_fly_idle` 上一轮已接）。climb/dive **留空不接** | T2 | 完成 |
| ⬜ | **转向平滑渐变**：现在 `SetMovementDirection` 是**瞬时**转向，转镜头时人会「啪」地跟过去。要做成**每帧朝目标角度插值**（限速 / 最短弧），速率进 `FlightTuning` 可调 | 无 | 代码 |
| T3 | 法阵预制体拷回 `Modules\Taikou\Prefabs\lwn_flight_sigil.xml`（🔴 **必须等网格进包之后**，否则 native AV 崩 —— 雷 149） | T2 | 人 |
| T5 | 验证动画：`custom.do_anim act_fly_cruise` 逐条 → 实机看姿态切换 | T4 | 人 |
| T6 | 验证法阵：飞起来看到脚下法阵 → `custom.flight hide on` 隐藏木板 | T3 | 人 |
| T7 | 按 §七 验收表逐条过（含 3 条负面测试） | T5,T6 | 人 |
| T8 | 登记轮子进 `plans/rules/wheels.d/assets.md` §十五；更新知识文档的过期数字 | T7 | 代码 |

### 下一轮调整清单（2026-09-21 用户提出 · 动画已实机可播）

> **状态总表（2026-09-21 收盘）**：

| # | 事项 | 状态 | 卡在哪 |
|---|---|---|---|
| **N1** | 飞行法阵 | ⬜ **未做** | 卡**资产**：网格/贴图/加法材质还没导进包（人的活） |
| **N2** | 起飞改二段跳 + 落地重定义 | ✅ **实机验证通过** | — |
| **N3** | 机身朝向改「朝实际移动方向」 | ✅ **实机验证通过** | — |
| **N4** | 撞地自动落地 | ✅ **实机验证通过** | — |
| **N5** | 飞行运动相机 | 🔶 **已实现，参数待调** | 代码全落地；**4 个机位是猜测值，要你实机用 `custom.flight cam` 调** |

> **3 条实机验证通过 · 1 条已实现待调参（N5）· 1 条未做（N1 卡资产）**。
> **N3 是推翻前面裁定的修正**；**N5 最大**。

#### N1 · 飞行法阵（原 T3/T6，未做）

- **缺资产**：网格 `lwn_flight_sigil` + 贴图 + **加法/自发光档材质** —— 上一轮 Publish 产物里一件都没有
- 导完 → 预制体拷回 `Taikou\Prefabs\lwn_flight_sigil.xml`（🔴 **必须等网格进包之后**，否则 native AV 崩 —— 雷 149）
- 代码侧**已就绪**：`CarrierBoard.SpawnSigil` + `MetaMesh.GetCopy` 存在性探针 + `custom.flight hide on`（隐藏木板）

#### ✅ N2 · 起飞改「二段跳」+ 落地改「短按(贴地/俯冲) / 长按下降」（2026-09-21 **实机验证通过**）

**起飞与落地现在是不对称的，这是故意的** —— 起飞只要"在空中"就成立（跳一下按空格很简单）；落地是破坏性操作，给两道闸挡误触。

| 手势 | 行为 | 实现 |
|---|---|---|
| **起飞** | **跳跃中（不在地面）按空格** | `!main.IsOnLand()` + 空格**按下沿** ⇒ `BeginTakeoff` |
| 起飞（后备） | 站着长按空格 0.65s | `TakeoffByLongPress`（默认开） |
| **落地 · 短按** | **只有**两种情况落：① **冲向地面**（镜头前方向竖直分量 ≤ −0.42，≈低头25°）② **贴地**（板顶离地 ≤ **8 米** ≈ 刚二段跳进浮空的高度） | `LandByTap` / `LandTapWhileDiving` / `LandTapDivePitch` / `LandTapMaxHeight` |
| **落地 · 长按** | **持续下降**（垂直分量整个被接管 = `−DescendRate`，不看镜头俯仰），**松手停**；降到撞地由 N4 收尾 | `LandByLongPressDescend` / `DescendRate` |

> 🔴 **"高空平飞短按空格不落地"** 是刻意设计 —— 否则手一抖就从天上掉下来。
> 🔴 **按下沿只活一帧**（`FlightInput.Tick` 开头就清）：不清的话，"在地面按空格起跳"那一帧
> 按下沿没被消费 → **下一帧刚好离地 → 一跳就直接进飞行**。
> 🔴 起飞/落地的判据**分开两个常量**（`PitchThreshold` vs `LandTapDivePitch`）—— 姿态动画的阈值
> 和落地判定是两件事，别让它俩互相牵动。

**全部可热调，不用重编译**：
`custom.flight tune dbljump|longpressjump|landtap|landheight|landdive|divedepth|descend|descendrate|landtouch|landeps <值>`

- 实现：`TickGrounded` 的判断改成 `!main.IsOnLand() && 空格按下沿`
- ✅ **前提已澄清**：**玩家能跳**（空格即跳跃）。方案 §二 那句"跳跃对 human 无效"是文档误读，已订正
- ✅ **长按起飞默认保留**（`tune longpressjump 0` 可关）—— 长按现在专管"持续下降"，起飞那侧留着不碍事
- ⬜ **实机待验**：跳跃滞空窗口够不够按第二下；`LandTapMaxHeight=8` 这个闸合不合适
- `FlightTuning` 里加开关，两种触发方式可切换（避免又要重编译试）

#### ✅ N3 · 机身朝向改「朝实际移动方向」（2026-09-21 **实机验证通过**，🔴 推翻前面的"飞机式"裁定）

**已改**：`TurnBody(main, dir)` —— 一行（`dir` 换成 `forward` 即可改回飞机式）。

| | 现状（2026-09-21 早先裁定 = 飞机式） | **目标（本条为准）** |
|---|---|---|
| 有输入时朝向 | 朝**镜头前方**的水平投影（侧移不甩头） | **朝实际移动方向**：W 朝镜头前方 / **A 朝左** / **D 朝右** / **S 转身朝镜头** |
| 无输入时 | 保持最后朝向 | 不变 |

- 实现：`TurnBody(main, dir)` —— 把参数从 `forward` **改回 `dir`**（一行）
- 🔴 **这两条裁定是相反的**：早先那条走的是"像飞机/滑翔"，现在要的是"人跟着移动方向转身"。**以现在这条为准**，早先那条作废

#### ✅ N4 · 撞地 → 自动落地（2026-09-21 **实机验证通过**）

**已加**：`FlightTuning.LandOnGroundTouch` / `LandTouchEps`；`TickAirborne` 里检测板顶 vs 地形高。

- 现状：**落地只走手动**（长按空格）；飞行中完全不检测地面
- 目标：板顶触地 → 自动进 `Landing` 阶段
- 实现：**纯检测，不做位置修正** ——
  `scene.GetTerrainHeight()`（**查地形、不查物理体**，否则会查到自己那块板，见 `GetGroundZ` 的注释）
  算出地面高，若 `板原点 z + CarrierTopLocalZ >= 地面高` → `BeginLanding`
- 🔴 **只检测、不夹取**：方案开头那条血泪教训 —— 位置夹取会和"板的位置""玩家位置"两个回路耦合出正反馈

#### ✅ N5 · 飞行运动相机（2026-09-21 **已实现，参数待实机调**）🔴 最大的一项

**已落地**：`Flight/FlightCameraRig.cs`（4 机位 + 渐变）+ `Camera/SpringArmMath.cs`（算法抽出共用）
+ `FlightInput.AimHeld`（右键）+ `custom.flight cam` 热调命令。

##### 机位参数（第一版猜测值，**没实机调过**）

| 机位 | ArmLength | ArmPitch | SocketX | Fov | 设计意图 |
|---|---|---|---|---|---|
| **hover** 悬停 | 3.8 | 12 | 0 | 65 | 离得近 —— 悬停时会转镜头看角色 |
| **cruise** 巡航 | 5.5 | 14 | 0 | 70 | 标准跟随距离 |
| **boost** 加速 | **9.0** | 6 | 0 | **80** | 拉远 + 广角 ⇒ 地面景物掠过更快 = 速度感 |
| **aim** 瞄准 | 1.9 | 4 | **+0.75** | 55 | 过肩近景；**SocketX>0 ⇒ 人物偏左** |

- 🔴 **`SocketX` 的正负就是"人物偏左还是偏右"**：相机往右移(SocketX>0) ⇒ 角色在画面里偏左
- ⚠️ **`ArmPitch` 的正负可能反**（原实现自己注了"左右手定则需测试"）—— 用命令现调
- **瞄准只在悬停/巡航可用**（用户裁定）：优先级 `瞄准 > 加速 > 移动 > 悬停`，加速中不给进瞄准

##### 三个实现要点

| 要点 | 做法 |
|---|---|
| **算法只有一份** | 从 `SpringArmCameraView` 抽到 `SpringArmMath.ComputeFrame`（铁律 18 精神：同件事不许两份实现）；原主改成调它，**行为逐字节不变**（含那句"提前强制 `IsAnchorWorld=false`"，没顺手改别人的行为） |
| **渐变** | `SpringArmMath.Lerp` + `Ease`（smoothstep）；机位**真的变了才重启渐变**（每帧同机位不重置进度） |
| **相机与动画同时长** | `CamBlendIn` 与 `AnimBlendIn` **分开两个常量但取同一个值**（用户要求"一起做"）—— 改一个记得看另一个 |

##### 热调命令（我没法实机调，参数必须能现调）

```
custom.flight cam                                   列 4 个机位全部参数
custom.flight cam <机位> <参数> <值>                 改一个（飞行中立即生效）
custom.flight cam on|off                            开关"飞行时接管相机"
参数: arm pitch yaw pivotx pivoty pivotz socketx sockety socketz selfyaw selfpitch selfroll fov
```

🔴 **调好之后把数值报回来，我写死成默认** —— 现在是猜测值，实机大概率要改。

##### 已知风险（实机第一个要看）

1. **`CustomCamera = null` 能不能把相机还给引擎** —— 退出路径靠它，不行的话出飞行会黑屏/卡死
2. **`Camera.CreateCamera()` 的对象生命周期** —— 现在是懒创建、不销毁（跟 `SpringArmCameraView` 一致）
3. **接管失败要能优雅降级** —— `Enter()` 返回 false 就继续用引擎相机，不阻断飞行

**目标四条**：
1. 进飞行 → 切**运动机位**（不再是引擎默认跟随相机）
2. **不同机位之间渐变**，不硬切
3. **加速时拉远** ⇒ 速度感
4. **右键 → 人物偏左的瞄准视角**

**可复用的现成轮子**（`Camera/SpringArmCameraView.cs`，391 行，已实机在用）：

| 件 | 说明 |
|---|---|
| `SpringArmCameraParam` | 参数结构：`PivotX/Y/Z`（眼睛偏移）/ `ArmLength`（臂长）/ `ArmYaw`·`ArmPitch`（相对角色朝向）/ `SocketX/Y/Z` / `SelfYaw`·`SelfPitch`·`SelfRoll` / `Fov` |
| `ApplySpringArmCamera(p)` | 核心算法：锚点 = 角色 `LookFrame` 原点 + 眼高 → pivot → 臂旋转 → `相机位 = pivot − 臂前向 × 臂长 + socket 偏移` → `missionScreen.CustomCamera = _customCamera` |
| 模板系统 | `UseCameraTemlate(templateName, speakerAgent, listenerAgent, anchorWorldPos)` —— 已支持"按名字套一组机位参数" |
| 调试 UI | `custom.openSpringArmCamDebugger` —— 运行时拖滑杆调参，**定参数时用它，别盲填** |

**⚠️ 已知小坑**：`ApplySpringArmCamera` 里 `p.IsAnchorWorld = false;` 写在 `if (p.IsAnchorWorld)` **之前**
⇒ 世界模式分支是**死代码**（永远走角色朝向那一支）。做飞行相机别依赖世界模式。

**待你定（定了我才好动手）**：
- 机位清单：悬停 / 巡航 / 加速 / 瞄准，**各要几个**？
- 加速拉远：从多少米拉到多少米？（默认跟随机位差多少）
- 瞄准视角：**偏左多少**（横向偏移）、臂长/俯仰要不要跟着变
- 渐变时长：进/出飞行、机位互切各用多少秒
- 出飞行时怎么还回引擎相机（`missionScreen.CustomCamera = null`？）

---

### 已完成：冻结 / 朝向 / 接线

### ✅ 入包结果与真实 clip 名（2026-09-21 实测，tpaccli list）

🔴 **TRF 文件名 ≠ clip 名，接线必须用实测名**（写错 = 静默 `act_none`）：

| 动作名 | 源 TRF | **实测 clip 名** | XML |
|---|---|---|---|
| `act_fly_idle` | `fly_A_Flight_Idle_A` | `flight_idle_a` | 上一轮已接 |
| `act_fly_start` | `fly_A_Flight_Hover_Start_A` | `flight_hoverstart_a` | 本轮 +1 |
| `act_fly_cruise` | `fly_A_Flight_HoverMove_A` | `flight_hovermove_a` | 本轮 +1 |
| `act_fly_boost` | `fly_A_Flight_FastMove_A` | `flight_fastmove_a` | 本轮 +1 |
| `act_fly_land` | `fly_A_SuperheroLanding_A` | **`flight_superland_a`** ⚠️ 不叫 landing | 本轮 +1 |
| ~~`act_fly_climb`~~ | ~~`fly_A_FM_A_Pose_U`~~ | — | **本轮不导**，C# 里 `ActClimb` 留空 |
| ~~`act_fly_dive`~~ | ~~`fly_A_FM_A_Pose_D`~~ | — | **本轮不导**，C# 里 `ActDive` 留空 |

**`act_fly_idle` 之外全部裸声明**（不带 `type` / `action_stage`）—— 它们是表演类动作，由飞行代码
每帧 `SetActionChannel` 指定，**不进引擎走跑状态机**（引擎那 4 个移动模式名字写死在 native，表达不了俯仰）。

**climb/dive 留空的后果**：抬头/低头时**保持上一条姿态**（`SetAction` 遇空串直接返回），观感是"平板上升"。
将来要接：clip 进包 → `FlightTuning.ActClimb/ActDive` 填回动作名 → XML 各加一行。

### 🔴 源 TRF 的投放目录（2026-09-21 用户纠正）

| 资产 | 放哪 | 谁生成镜像 |
|---|---|---|
| 网格 / 贴图 | **镜像布局**（`AssetSources\<类>\<名>\`，与 `Assets\` 同目录同名） | 编辑器文件监视**自动同步** |
| **动画 TRF** | 🔴 **普通源目录** `TaikouAnim\AssetSources\Fly\` | **ModKit 的「导入」操作**（手动） |

**我把 TRF 放进 `AssetSources\animations\flight\`（镜像布局）是错的** —— 那是绕过了导入环节。
`Fly\` 里的**旧管线产出**同名 TRF（`fly_A_Flight_Idle_A.trf`，09-20 版，有"骨盆按绝对值写"缺陷）
已改名 `..._OLD_0920.trf.old` 搁置（ModKit 不认这个扩展名）——**判据用 md5，别只看文件名**。

### 机身朝向（🔴 **已被 N3 推翻，看 N3**）

> ⚠️ **下面这条裁定（飞机式）已于同日被用户推翻**，现行是 N3 的「朝实际移动方向」。
> 保留在此仅为记录当时的取舍理由（"侧移不甩头"）。**动手前看 N3。**

| 状态 | 机身朝向 | 实现 |
|---|---|---|
| **有 WASD 输入** | 朝**镜头看的方向**的水平投影 | `TurnBody(main, forward)` —— 注意传的是**镜头前方**，不是"实际移动方向" |
| **无 WASD 输入** | **保持最后朝向不动** | **一个字都不写**（`SetMovementDirection` 不写 = 保持）⇒ 镜头绕着转能看到各个面，转到正面就是正脸 |

🔴 **飞机式 = 侧移不甩头**：按 A/D 平移时身体**不转**，仍朝镜头前方（像飞机）。
若改成朝"实际移动方向"，一按侧移就甩头 —— 已否掉。

🔴 **别在"无输入"分支里补一个朝向**（比如"朝向镜头"/"归零"）—— 一旦每帧都写，
人就**被钉死在某个方向、再也转不动**，用户要的"转镜头看各个面"就没了。

### 🔴 转向接口：用 `SetMovementDirection`，**不是** `LookDirection`

**2026-09-21 用户纠正**：第一版用 `agent.LookDirection = 水平单位向量`，**转不动**（按 WASD 角色不转）。
本项目既有做法是：

```csharp
// Story/VisualCommands.cs:512 ——「强制说话者看向听者」
speakerAgent.SetMovementDirection(dirToListener.AsVec2);
// 本飞行代码同款：
agent.SetMovementDirection(new Vec2(dir.x, dir.y) 归一化);
```

| 接口 | 结果 |
|---|---|
| **`Agent.SetMovementDirection(in Vec2)`** | ✅ **现行**。对应 getter = `GetMovementDirection()` |
| `Agent.LookDirection`（Vec3 属性） | ❌ 实测转不动，别用 |
| `Agent.LookDirectionAsAngle`（单浮点） | ❌ 未证实（单位是度还是弧度都不知道），仅作对照 |

> 反编译层面 `LookDirection` 的 setter 没有守卫（就是裸调 `SetLookDirection`），
> 所以"转不动"是 native 行为，C# 层看不出来 —— **以实测为准，别以反编译为准**。

**验证判据**：`[Flight-Diag]` 里的 `body=(x,y)`，以及
`custom.turntodir` 回显的 `MoveDir=(x,y) yaw=…`（这才是 `SetMovementDirection` 的对应 getter）。
按 W/A/S/D 时 `body` 应当跟着**镜头**转（横移时不变），松手后**定住不动** = 对。

**隔离测法**（`custom.turntodir <角度>`，`0°=+X / 90°=+Y / 逆时针`）：

```
custom.turntodir               查（MoveDir / LookDir / LookDirAsAngle 三个读数）
custom.turntodir 90            SetMovementDirection 转 90°（现行接口）
custom.turntodir look 90       对照：LookDirection（已知转不动，复现用）
custom.turntodir angle 90      对照：LookDirectionAsAngle
custom.turntodir vec <x> <y>   直接给向量
```

地上拨一次 → 起飞拨一次 → `custom.flight freeze off` 再拨一次，
就能分清"接口选错"和"aipause 把转向也冻了"。

**验证判据**：`[Flight-Diag]` 里的 `body=(x,y)` = 机身当前水平朝向。
按 W/A/S/D 时它会跟着**镜头**转（横移时不变），松手后**定住不动** = 对。

### 🔴🔴 三条这次血泪换的（下一个 agent 必读）

**① 给已跑通的机制加层，一次只加一层、每加一层验一次。**
本次一次堆了五层（加速趋近 / 地形夹取 / 160 米高度钳 / 单帧位移钳 / 安全绳），
结果「板的位置」和「玩家的位置」两个回路互相耦合出**正反馈** —— 板 0.5 秒窜 42 米（84 m/s），
而速度变量还诚实显示 9。排了一整天。
**判据**：新加的东西**会不会读另一个也在被修正的量**？会 = 有耦合风险 = 必须单独验。

**② 移动逻辑保持"开环"形态：读回真实坐标 + 加增量 + 写回。**
```csharp
MatrixFrame f = _carrier.GetFrame();   // ① 读回【真实】坐标，不用自己记的值
f.origin += _velocity * dt;            // ② 加增量
_carrier.SetFrame(ref f);              // ③ 写回
```
自己记一份 `Origin` 再覆盖 = 引擎若动过板就会被拽回去 → 互相打架。

**③ 修 bug 前先取证，别先改代码。**
本次连着三轮"看现象 → 编原因 → 加补丁"，全猜错（UI 门控 / 键盘读不到 / 编队 AI 归位）。
后来加了诊断日志 `[Flight-Diag]`（控制权 / 两边坐标 / 原始按键 / 相机向量 / 速度），**一轮就定位**。
**先取证，再动手。**
> T1 沿用了这条：清零**之前**先把引擎写的值记下来打进 diag，这样"没冻结的话他会往哪走"是可见的
> —— 否则读到的永远是 0，分不清"冻结生效"和"根本没读到按键"。

### 日志标签速查

| 标签 | 打什么 | 什么时候看 |
|---|---|---|
| `[Flight]` | 起飞 / 落地 / 冻结 / 状态变化 | 常规 |
| `[Flight-Diag]` | 每 0.5 秒三行：控制权+坐标 / 原始按键 / 相机向量+速度 | **排查"飞不动 / 飞歪 / 摔下来"第一站** |
| `[Flight-Watch]` | 玩家单帧位移超出板能带出的量 | 有人/有事在动玩家时 |

> 三个标签全在 `Debug/StoryEngine_RuntimeLog.txt`。

---

## Context：为什么做、做到什么程度

**目标**：主角在**场景任务里**（战斗 / 据点 / 村庄）像超人一样飞 —— 脚底一块**看不见的木板**把人托起来，身上播飞行动画，方向跟镜头走。

**为什么要这么绕**：骑砍2 引擎**写不进 agent 的 Z**（实测 9 条路全死，见 [Knowledge/骑砍2Agent运动与位置机制.md](../Knowledge/骑砍2Agent运动与位置机制.md) §6）。唯一成立的办法是：**脚底放一块实心道具当"地面"，每帧瞬移它** —— 引擎自己会把玩家算在它表面上。这条已实测通过（人从 8 米被抬到 157 米，位置分毫不差）。

**本轮范围**（2026-09-21 已定）：完整飞行闭环 —— 起飞 / 悬停 / 空中移动 / 加速趴姿 / 俯仰姿态 / 落地，7 条动画全套接线。闪避、倾斜叠加（`Lean_*` 那套，见下）、特效留到下一轮。

**现状（已经有的）**：

| 已有 | 说明 |
|---|---|
| 载具机制 | 已验证。`FlySpike.cs` 的 `custom.prop` 命令族就是现成脚手架（生成 / 匀速瞬移 / 日志） |
| 1 条飞行动画 | `flight_idle_a` 已装进 `Taikou/AssetPackages/lwn_taikou_anim.tpac`，XML 也接好了 |
| 130 条飞行 TRF | `D:\BrainMaker\骑砍2动画重定向\output\trf\fly_*.trf` 已重定向完，**不用再跑 Blender** |

---

## 〇、结论速览

| 问题 | 答案 |
|---|---|
| 能飞吗 | ✅ 机制已实测通过，本轮是把它做成功能 |
| 要写多少 C# | **1 个新目录、5 个文件**（状态机 + 载具 + 输入 + 参数），另加一挂载点 |
| 要动多少 XML | **2 个文件各加 6 行**（`action_types.xml` / `action_sets.xml`） |
| 要重做多少动画 | **7 条**（6 条新导入 + 1 条重做）—— 🔴 **重做那 1 条是必须的，见 §五·1** |
| 要你手动做什么 | ModKit 里导入 7 条 clip + 建 1 个法阵材质（编辑器操作，脚本替代不了） |
| 视觉 | **分两步**：① 阶段 1 **木板照常显示**（不然没法用肉眼确认它真的在托人）② 法阵进包后再 `custom.flight hide on` 隐藏木板、由法阵接手 |
| 最大的未知 | **载具"直接出现在脚下"算不算站上去** —— 免费实验，10 分钟能出结论（§六 阶段 0） |
| 怎么回退 | XML 删行 / 代码摘挂载行 / 资产回滚 tpac，全部一行 |

---

## 一、先回答架构判断（原第 5 问）

> 问题原文：*"飞行过程中需要屏蔽玩家原生控制移动是吧，然后通过木板设置位移，通过其他方式来控制角色动画，是否是这样？"*

**判断：三条全对，只有"屏蔽"的手法要换一个。**

| 判断 | 结论 | 说明 |
|---|---|---|
| 要屏蔽原生移动 | ✅ **对** | 不屏蔽的话，按 W 角色就自己走下木板了 |
| 用木板设位移 | ✅ **对** | 更准确的说法是我们**每帧改木板的坐标**，人跟着走 |
| 用其他方式控制动画 | ✅ **对** | 引擎只会在"走/跑"里挑动画，飞行姿势不在那张表里，必须我们直接指定 |

**屏蔽的手段 —— ✅ 已按"最小侵入"落地（2026-09-21 T1）**：

| 做法 | 用不用 | 说明 |
|---|---|---|
| 每帧把 `MovementInputVector` / `MovementFlags` / `EventControlFlags` 写成 0（保留 `Controller = Player`） | ✅ **本方案采用** | 最小侵入：引擎侧一切不变，只是"替玩家一直松着手"。帧序上必定生效（见交接节 T1 说明） |
| 不冻结，用**小键盘 8/2/4/6** 控制飞行 | ✅ **保留作备用** | 没绑任何游戏移动键 ⇒ 按了只影响板。现为"不冻结也不会误触走路"的隔离对照组 |
| `V.SetPlayerControlFrozen(agent, true)`（切 Controller = **AI**） | ❌ **已否掉** | 🔴 它的语义是"**把 agent 交给引擎 AI 驾驶**"，**不等于"原地不动"** —— 实测表现为角色在木板上走来走去、甚至被挪到离板 **41 米**外。别再用 |

**摄像机 / 朝向照常归玩家**，镜头手感跟平时一样。

> 🔴 **订正一条旧结论**：[Knowledge/骑砍2Agent运动与位置机制.md](../Knowledge/骑砍2Agent运动与位置机制.md) §5 与 §6 表 #2 记着"切控制权会丢镜头、agent 被搬到兜底坐标 `(-100, 10385, 15.06)`"——**那条是错的**。该次实验同时在写位置，变量没分离（文档自己也注了"该观察变量未完全分离"）。**实际表现：切 AI 后角色原地待机，镜头仍在玩家身上。**
> 📌 待办：把这条错误结论从知识文档 §5 / §6 表 #2 / §八 里订正掉，`FlySpike.cs` 里那两处旧注释同批处理（见 §十）。

### 为什么不用原版的走 / 跑 / 蹲状态机（2026-09-21 裁定）

**问题**：骑砍2 本来就有一套移动状态机（走 / 跑 / 蹲走 / 蹲跑），[自定义移动管线.md](自定义移动管线.md) 第二部分也确立了"换 action set = 换整套走跑外观"这条零代码的路。飞行为什么不走它？

**两条硬边界**：

| # | 边界 | 事实 |
|---|---|---|
| 1 | **只有 4 个模式，且全是水平** | `walking` / `running` / `crouch_walking` / `crouch_running` 名字写死在 native 里，数据层加不了第 5 个。**表达不了"抬头爬升、低头俯冲"** |
| 2 | **移动集运行时换不了** | 移动集挂在**物品用法**上（`item_usage_sets.xml` 的 `<movement_set id="..."/>`），物品用法来自**身上的武器**。反编译 `AnimationSystemData` 核实过：里面只有 `ActionSet` 和一个 monster usage 索引，**没有物品用法** —— C# 只能切 action set（动作名→clip 的映射），切不了移动集 |

**裁定**：**走纯代码路线** —— 封住原生移动，木板自己飞，动画由我们直接指定。

**理由**：本作要的是"像超人一样飞"，**方向跟镜头含俯仰**是核心体验；引擎那 4 个水平模式从根上表达不了它。与其把飞行硬塞进走路模型再打补丁，不如整体接管。代价是**过渡与混合要自己写**（几十行，用 `blendInPeriod` 做交叉淡化，见 §三）。

**保留的后路**：将来若要做"铁炮走路也换外观"那类需求，[自定义移动管线.md](自定义移动管线.md) 第二部分那条路仍然成立，与本方案不冲突。

---

## 二、操作方案

> 已定：**空格只用来启动，飞的方向取决于镜头，不一定是水平面。**

| 操作 | 键 | 行为 |
|---|---|---|
| **起飞** | **长按空格** | 木板出现在脚下 → 抬升 → 进悬停 |
| **飞行方向** | **镜头** | 往哪看就往哪飞（**含俯仰**：抬头看天 + 前进键就是爬升） |
| 水平方向 | **W A S D** | 在镜头平面里给方向：前键 = 镜头正前方、后键 = 正后方、左右 = 左右 |
| **加速冲刺** | **左 Shift** | 切"超人趴姿"并提速（对应 UE 的 FastMove） |
| **落地** | **长按空格（再按一次）** | 自动落地本轮已随夹取一起删掉，落地只走手动 |

> **小键盘 8/2/4/6 同样可用**（前 / 左 / 后 / 右）。它不是历史残留 —— 它没绑任何游戏移动键，
> 是排查"角色自己走下板"时的隔离对照组：用它飞一圈就能判定问题出在「冻结没生效」还是「板本身飞不动」。

**为什么这么定**：空格不参与方向，就不会和"按住上升"打架；抬头 / 低头直接决定爬升下降，符合"像超人一样飞"的直觉，也正好对得上 UE 工程那套动画（它准备了 `Pose_U` / `Pose_D` 专门的抬头低头姿态）。

> ⚠️ **空格同时是原版的跳跃键。** 飞行期间连跳跃一起封掉，落地后自动恢复。
>
> 🔴 **订正（2026-09-21 用户当场反驳）**：这里原本写着「跳跃对 `human` 本来就是无效的 —— `jump_speed_limit = 0`」，
> **那是错的，已作废**。骑砍2 空格就是跳跃，**跳跃正常工作**。
> 错误来源：`Knowledge/骑砍2Agent运动与位置机制.md` §6.5 —— 那里的**字段值是真的**
> （`Native/ModuleData/monsters.xml` 的 `human` 确实是 `jump_speed_limit = 0`），
> 但**解读错了**，而且同一段文档自己就写着"⚠️ 玩家按空格是有效的……离地发生了"，
> 下面还有一句自曝"`custom.airhold` 每帧 `TeleportToPosition` 会把跳跃的上升速度清掉（现象：跳不起来了）"。
> ⇒ **当时看到的"跳不起来"是 airhold 自己清的**，跟 `jump_speed_limit` 无关。见 [[N2 二段跳]]。

---

## 三、飞行状态机

**4 个状态 + 7 条动作**（`animation=` 填的是**实测到的 clip 名**，见 §五·2）。

### 主状态

| # | 状态 | 什么时候进 | 播哪条 | UE 源动画 |
|---|---|---|---|---|
| 0 | **地面** | 默认（不飞） | 原版一套，我们不动；骑马时**禁止起飞** | — |
| 1 | **起飞** | 长按空格后的一小段 | `flight_hover_start_a` | `A_Flight_Hover_Start_A` |
| 2 | **空中** | 升到悬停高度后 | 见下表（按姿态再分四选一） | — |
| 3 | **落地** | 长按空格退出 / 贴地不动 0.45 秒 | `superhero_landing_a` | `A_SuperheroLanding_A` |

### 空中姿态（优先级：冲刺 &gt; 俯仰 &gt; 速度）

| 条件 | 播哪条 | UE 源动画 | 实测观感 |
|---|---|---|---|
| **完全悬停不动** | `flight_idle_a` ✅ **已有** | `A_Flight_Idle_A` | 悬浮待机（**不跟镜头翻**，站着发愣时头朝下会很怪） |
| 按住 **Shift** | `flight_fast_move_a` | `A_Flight_FastMove_A` | 水平趴姿 = 超人冲刺 |
| 有移动 + **抬头 &gt; 25°** | `flight_climb_a` | `A_FM_A_Pose_U` | 直立上升 |
| 有移动 + **低头 &gt; 25°** | `flight_dive_a` | `A_FM_A_Pose_D` | **头朝下俯冲** |
| 其余移动 | `flight_hover_move_a` | `A_Flight_HoverMove_A` | 巡航 |

> 🔴 **为什么要靠动画表现俯仰**：引擎的 agent **只能绕 Z 轴转，转不了俯仰**。所以"抬头飞"只能由我们挑一条抬头姿态的动画来做。阈值 `PitchThreshold = 0.42`（≈25°），控制台 `custom.flight tune pitch <值>` 可热调。
>
> 🔴 **姿态动画的名字会骗人，别按名字猜 —— 上面「实测观感」栏是渲染出来看过的**：
> `A_FM_A_Pose_U` 是直立、`A_FM_A_Pose_D` 是**头朝下垂直**。

#### 🔴 `Lean_*` **不是**俯仰姿态，是另一整套体系（2026-09-21 用户纠正，本轮不导）

**曾经写错的说法**：「`Lean_U` 是爬升备选、`Lean_D` 是俯冲备选」—— **错的**，已作废。

**事实**（TRF 源目录实测，`D:\BrainMaker\骑砍2动画重定向\output	rf\`）：

```
fly_A_FM_A_Lean_{L,R,U,D}.trf     ← A 套的 4 个方向
fly_A_FM_B_Lean_{L,R,U,D}.trf     ← B 套
fly_A_FM_C_Lean_{L,R,U,D}.trf     ← C 套
fly_A_FM_D_Lean_{L,R,U,D}.trf     ← D 套
fly_A_FM_E_Lean_{L,R,U,D}.trf     ← E 套        ⇒ FM 有 5 个变体，每套各带完整 4 方向
fly_A_Flight_HoverLean_{A,B,C,D}_{L,R}_Add.trf  ⇒ 悬停倾斜，而且是 **Additive（叠加）** 系列
```

**`Lean` = 转向/倾斜体系**（L 左倾 / R 右倾 / U / D），**不是"抬头/低头姿态"**。
从这套里抠 `_U` / `_D` 两块当竖直姿态用 = **类别错了**。

**归属**：它服务的是本方案开头写的「**倾斜叠加**」—— 那是**下一轮**的功能（飞起来转向时身体跟着压弯）。
**本轮的处理**：**一条都不导**。等做「倾斜」时**成套**导（先设计要用哪几个变体，别零敲碎打）。

**落位**（2026-09-21）：已从 `AssetSources\animations\flight\` 挪到
`AssetSources\animations\flight_lean_reserved\`（附 README 说明），
免得照着导入清单逐条导的时候顺手带上。**`flight\` 目录现在正好 7 条。**

> 抬头 / 低头姿态用的是**另一套** `Pose` 家族：`Pose`（中性）/ `Pose_U`（上）/ `Pose_D`（下）——
> 本轮只用 `Pose_U` / `Pose_D` 两条。✅

### 🔴 视觉分两步走（2026-09-21 定）

**验收"看不见的东西"是做不到的** —— 板要是从一开始就隐形，你没法分辨"真的被一块板托起来了"和"因为别的原因看起来像在飞"。
所以：

| 阶段 | 木板 | 怎么切 |
|---|---|---|
| **① 功能验收期**（现在） | **显示**（默认 `HideCarrier = false`） | 默认就是，不用做任何事 |
| ② 法阵进包后 | **隐藏**，由法阵接手 | `custom.flight hide on`（下次起飞生效） |

> 即使隐藏了，也能靠数字确认：飞行中每 0.5 秒打一行
> `[Flight] z: player=… board=… dz=… | |v|=… anim=…`
> —— `dz` 恒定 = 人确实站在板面上被托着。

### 两条节奏规则

**动画切换**：**状态变了才调一次** `SetActionChannel(0, 动作名, ignorePriority: true, blendInPeriod: 0.2)`；**每帧重复调会把动画卡在第 0 帧**。靠 `blendInPeriod` 做交叉淡化 —— 这就是骑砍2 里能做到的"混合空间"。
> 但 0 号通道引擎的 locomotion 系统也有权写，所以**每 0.5 秒核对一次**有没有被抢回去，抢走了才重设（`ActionRecheckSeconds`）。

**朝向**：机身水平朝向 = 镜头的水平朝向（每帧写 `Agent.LookDirection`）；**俯仰由动画表现**。

### 实测：TRF 体检（2026-09-21）

9 条 TRF 全部 `--pelvis none` 重跑后逐条量过（其中 **7 条入包**，2 条 `Lean_*` 见下节）：

| 项 | 结果 |
|---|---|
| **位置轨** | **全程全零**（不是只首帧）—— 确认 9 条都是**原地动作**，位移全由木板负责 ✅ |
| 骨骼数 | 28（= 骑砍 human_skeleton）✅ |
| **循环闭合** | 待机 / 巡航 / 冲刺 **首末帧差 0.000°** —— 循环不会跳帧 ✅ |
| 过渡片段 | 起飞 47.7° / 落地 116.7° 首末差 —— 预期内，它们本来就**不该**循环 |
| **衔接姿势差** | 起飞末帧→悬停首帧 **0.0°**、悬停→巡航 **0.0°** —— 完美衔接 ✅ |

**由实测数据改掉的两处代码**：

| # | 发现 | 处置 |
|---|---|---|
| 1 | 姿态之间是 **120°~180° 的大翻转**（巡航↔趴姿 120°、巡航↔头朝下 180°），0.2 秒淡化看着像猛翻跟头 | `AnimBlendIn` 0.2 → **0.3** |
| 2 | 单阈值下镜头停在 25° 附近会让动画来回翻 | 加**迟滞**：进 `PitchThreshold=0.42` / 出 `PitchExitThreshold=0.30` |

> 两个阈值都能热调：`custom.flight tune pitch <值>` / `custom.flight tune pitchout <值>`。

---

## 四、要动的东西

### 4.1 C#（LWN 侧，全部新代码）

**🔴 新建目录 `ExampleModVS/ExampleMod/ExampleMod/Flight/`** —— 按铁律 26，新目录已在方案里点名并获准。理由：现有目录没有一个能装下"玩家移动 / 飞行"（`Interaction` = NPC 互动、`Combat` = 战斗、`Stealth` = 偷窃、`Camera` = 相机），塞进任何一个都会让下一个人找错地方。

| 文件 | 职责 | 状态 |
|---|---|---|
| `Flight/FlightTuning.cs` | 可调参数集中一处（速度 / 升降 / 高度上下限 / 动作名） | ✅ 已写 |
| `Flight/FlightInput.cs` | 读原始按键（WASD / Shift / 空格长按）+ UI 门控 | ✅ 已写 |
| `Flight/CarrierBoard.cs` | 隐形载具 + 法阵挂点：生成 / 隐藏网格 / 每帧瞬移 / 销毁 | ✅ 已写 |
| `Flight/PlayerFlightBehavior.cs` | 状态机主体（`MissionBehavior`） | ✅ 已写 |
| `CampaignMode/Tools/FlightCommands.cs` | 调试命令 `custom.flight` | ✅ 已写 |

> 🔴 **5 个文件全部要登记进 `ExampleMod.csproj` 的 `<Compile Include>` 清单** —— 该工程是**显式清单**，漏登记 = 静默不编译且 `dotnet build` 照样报成功（雷 40）。已登记。
>
> **控制台命令为什么单独放在 `CampaignMode/Tools/`**：归口表规定「C# 命令实现 → `ExampleModVS/.../CampaignMode/Tools/`」，所以命令入口与玩法本体分开两处。

**挂载点**：`Core/MySubModule.cs` 的 `OnMissionBehaviorInitialize`，**必须挂在玩法闸门（`Settings.Instance.IsInteractionDisabled()`）之前** —— 和 `FlySpikeMissionView` 同一区。

> **为什么必须在前**：那道闸门的作用是"战场 / 竞技场不跑玩法逻辑"，而**战斗场景正好被它拦在外面**，挂在后面 = 战场里飞不起来。
> **为什么自己读按键**：项目的 `ModInput` 只在 `InteractionMissionView` 里 tick，那个视图同样在闸门之后 —— 战场上它根本不跑。所以飞行必须自己轮询原始按键。

**铁律遵守**：
- 类名中立 —— `Flight` / `Hover` / `Carrier`，不出现任何世界观词（铁律 3）
- **不动 `FlySpike.cs`**（它自己文件头写着"不要再往这里加新玩法代码"），新代码另立文件
- 与 LLM 无关，不涉及 `IsLLMConfigured`

### 4.2 XML（内容包侧，`Modules/Taikou/ModuleData/`）

| 文件 | 动作 | 内容 |
|---|---|---|
| `action_types.xml` | **加 6 行** | 6 个新动作**裸声明**（`<action name="act_fly_cruise" />`），照 `act_execution02_root` 的样式。`act_fly_idle` **已经在了**不用加 |
| `action_sets.xml` | **加 6 行** | 6 条 `type=动作名 → animation=clip 名` 绑定，加进现有 `as_human_warrior` 块 |

新增的 6 个动作名：`act_fly_start` · `act_fly_cruise` · `act_fly_boost` · `act_fly_land` · `act_fly_climb` · `act_fly_dive`

**不用碰的**：`project.mbproj`（`soln_action_sets` / `soln_action_types` 已经挂好了）、`item_usage_sets.xml`（那条是铁炮的活，与飞行无关）、`movement_sets.xml`（**本方案不用移动集** —— 动画由代码直接指定，不走引擎的走跑表）。

🔴 **每条 XML 注释里绝对不能出现连续两个减号**（两个字面减号连写）—— 会让整个文件解析失败、引擎启动即崩。现有文件里已经写着这条纪律，照抄。

### 4.3 资产（中转沙箱 → 内容包）

**新导入 6 条 + 重做 1 条 = 7 条**（重做的理由见 §五·1）：

| 我们的名字 | UE 源 | 帧数 | ModKit 填 Source 1 / 2 |
|---|---|---|---|
| `fly_A_Flight_Hover_Start_A` | `A_Flight_Hover_Start_A` | 46 | 2 / 47 |
| `fly_A_Flight_HoverMove_A` | `A_Flight_HoverMove_A` | 61 | 2 / 62 |
| `fly_A_Flight_FastMove_A` | `A_Flight_FastMove_A` | 31 | 2 / 32 |
| `fly_A_SuperheroLanding_A` | `A_SuperheroLanding_A` | 61 | 2 / 62 |
| `fly_A_FM_A_Pose_U` | `A_FM_A_Pose_U` | 2 | 2 / 3 |
| `fly_A_FM_A_Pose_D` | `A_FM_A_Pose_D` | 2 | 2 / 3 |
| `fly_A_Flight_Idle_A`（**重做**） | `A_Flight_Idle_A` | 91 | 2 / 92 |

> 帧范围以重跑时管线的实测输出为准，**导入前核对一遍**（上表已按实测更新）。

### 4.4 飞行法阵（脚下的视觉识别）

**作用**：木板网格要隐藏（它只是承载物），但玩家得**看得出自己踩在一个东西上** —— 所以脚下跟一张飞行法阵。

**🔴 不能用真 decal**：骑砍2 的 decal 是**投射到表面**上的（据点黄圈 `town_circle_decal` 就是投在地形上）。人在 50 米高空时**脚下没有面可投**，decal 会投到地面上、或者根本不显示。
**改用**：一块**贴了法阵贴图的平面网格**（静态图，不动的），跟木板锁在一起走。

**资产清单（内容包）**：

| 件 | 来源 | 说明 |
|---|---|---|
| 法阵贴图 | 一张圆形法阵图（**带透明通道**） | 走项目现成贴图管线；进编辑器前必须过 `png_for_editor.py`（否则编辑器会把源图连产物一起删） |
| 法阵材质 | ModKit 里建 | 建**自发光 / 无光照**档，夜里和暗处也看得见 |
| 平面网格 | 自建一个 quad（Blender 两分钟），或借现成平面预制体 | 走中转沙箱编译 → 内容包 |
| 预制体 | 一个 `<game_entity>`，只含网格组件，🔴 **不要 `<physics>`** | 承载物已经有了，法阵纯视觉 |

**C# 侧**：`CarrierBoard` 同时管**两个实体** —— 隐形的物理板（干活）+ 法阵平面（给人看）。每帧一起 `SetFrame`，两者差一个固定高度。法阵可做成**贴地朝向**（跟着板走，但绕 Z 自转留到下一轮再考虑）。

**尺寸建议**：法阵直径 ≈ 2 米（比玩家肩宽大一截，一眼能认出来）；具体值进 `FlightTuning` 可调。

---

## 五、三个必须提前知道的事

### 1. 🔴 现有那 130 条飞行 TRF 有已知缺陷，必须重跑

`output/trf/` 下所有 TRF 都是**旧版管线**产出的。旧版把"骨盆位置"按**绝对值**写进文件，而引擎读的是**增量** —— 症状是人整体被抬高。

**证据**：实测 `fly_A_Flight_Idle_A.trf` 的位置轨恒为 `0.020170 0.914520`（正常应是 ≈ 0）。而管线脚本 `pipeline/common/fbx_to_trf.py` 的修正时间是 **09-20 16:06**，这 130 条 TRF 的产出时间是 **09-20 15:25** —— 用的是有缺陷的旧版。

**修法**：重跑，并且**用 `--pelvis none`**：

```bash
cd /d D:\BrainMaker\骑砍2动画重定向
python pipeline/run_retarget.py --rig ue_mannequin \
  --clip A_Flight_HoverMove_A --name fly_A_Flight_HoverMove_A \
  --pelvis none --animdir input/source/ue_mannequin/clips_flight
```

**为什么是 `none`**：飞行动画必须**原地做**（位移由木板负责）。`--pelvis none` 的语义就是"不写位移轨"，正好 —— 而且它顺带**绕开了上面那个绝对值的坑**（不写就不会写错）。

**判据**：跑完看 `CHECK_POS` 一行，位置轨首帧应 ≈ 0。

> 管线**没有批量入口**，一条一条跑（9 条命令）。

### 2. 🔴 导入顺序不能反：先导入 → 读出真名 → 再写 XML

`animation=` 要填的是 **ModKit 给 clip 起的名字**，不是你给 TRF 起的名字，也不是 FBX 名字。

**上一轮的实测教训**：TRF 叫 `fly_A_Flight_Idle_A`，**clip 却叫 `flight_idle_a`**（ModKit 按源动画的 take 名自动生成、去了前缀）。**猜必错，写错 = 静默变成 `act_none`，不报任何错，只是播不出来。**

**纪律**：7 条全部导入完 → 用 `tpaccli clipinfo` / `list` 把真名抄下来 → 再写 `action_sets.xml`。

工具路径：`tools/tpactool/TpacToolCLI/bin/Release/net9.0/tpaccli.exe`

### 3. 🔴 别覆盖掉已有的装填动画

`Taikou\AssetPackages\lwn_taikou_anim.tpac` 里已经有三条**实机验证过的**资产：`gun_reload`（铁炮装填）、`flight_idle_a`、`execution_02`。

| | 做法 |
|---|---|
| ✅ **正确** | 飞行动画**加进同一个 TaikouAnim 工程**一起 Publish ⇒ 产物**改名**拷成 `lwn_taikou_anim.tpac` |
| ❌ **错误** | 另起干净工程只编飞行 ⇒ 覆盖后**装填回退成弩动作** |

---

## 六、执行步骤

### 阶段 0 · 先验三件事（便宜，做完再动代码）

| # | 验什么 | 怎么验 | 不过怎么办 |
|---|---|---|---|
| E1 | 🔴 **载具"直接出现在脚下"算不算站上去** | `custom.prop` 生成在脚下（偏移 0,0,0）然后立刻抬，看 `[PropMove]` 日志里 `playerZ` 跟不跟 | 不跟 ⇒ 改成"先瞬移到脚边半米、人走半步"或"先降低高度再抬" |
| E2 | **水平 / 斜向载人**（原计划里只是推断） | 把载具速度设成水平向量飞 8 秒，看 `rel`（人板水平偏移）是否恒定 | 不恒定 ⇒ 水平位移改由代码推玩家（走 A 路） |
| E3 | **动画能不能压住玩家待机** | `custom.do_anim act_fly_idle` 看动作出不出来；**再在冻结状态下（`custom.set_controller` 切 AI 后）播一次**，确认切 AI 不影响播放通道 | 出不来 ⇒ 查 `action_types` / `action_sets` 那两行；切 AI 后不出 ⇒ 冻结与动画要分两步做（先播动画再冻结，或反过来） |

> **E1 是整个方案唯一的拦路虎**（原计划 §四·1 就挂着这一条）。E2 / E3 是顺手确认。

### 阶段 1 · 资产（脚本跑 + 你在 ModKit 里操作）

**1A · 飞行动画（7 条）**

1. 重跑 9 条 TRF（§五·1 的命令，`--pelvis none`），看 `CHECK_POS ≈ 0`
2. 9 条 TRF 拷进 `Modules\TaikouAnim\AssetSources\animations\flight\`
   > ⚠️ 目录名是 **`flight`**（不是 `fly`）。同模块下还有个旧的 `AssetSources\Fly\`（大写）是历史残留，**别拷错地方**
3. 🔴 **你在 ModKit 里逐条**：导入 → 建 Animation Clip → 填 `Source 1 / 2` 和 `Duration > 0` → 编译 Publish（目标指向**模块外**，如 `Modules\Publish\TaikouAnim`）
4. 产物**改名**拷进 `Taikou\AssetPackages\lwn_taikou_anim.tpac`（**先备份现有的**）

**1B · 法阵（§4.4）**

5. 准备法阵贴图（圆形、带透明通道）→ 过 `png_for_editor.py`
6. 自建一个平面 quad（或借现成平面预制体）+ 一个只含网格、无物理的预制体
7. 与动画**同一个 TaikouAnim 工程**一起 Publish（这样一份产物同时含动画和法阵，**不会漏件**）
8. 法阵材质在 ModKit 里建（自发光档）

### 阶段 2 · XML 接线

5. `tpaccli clipinfo` 读出 7 条 clip 的真名
6. `action_types.xml` 加 6 行裸声明
7. `action_sets.xml` 加 6 行绑定
8. **验**：`custom.do_anim act_fly_cruise` 逐条播一遍（这一步过了 = 资产链通了，再写代码）

### 阶段 3 · 代码（✅ 2026-09-21 已写完并编译通过）

9. ✅ 建 `Flight/` 四个文件 + `CampaignMode/Tools/FlightCommands.cs`，挂进 `MySubModule`（闸门**之前**）
10. ✅ 调试命令 `custom.flight`（`start` / `stop` / `status` / `verbose` / `tune` / `reset`），首参可弃、返回纯英文
11. ✅ `dotnet build` 语法验证 0 错误；并核对过 DLL 里 5 个新类型与动作名常量确实在
    > 🔴 该工程是**显式编译清单**，漏登记 = 静默不编译且 build 照样报成功（雷 40）——所以"编译通过"必须再查一次产物。
    > 🔴 查产物时字符串常量要按 **UTF-16LE** 搜（.NET 字面量编码），按 ASCII 搜会全部搜不到。
    > **正式测试仍用 VS2022 编的 DLL**（既定工作流）。

### 阶段 4 · 实机

12. 按 §七 验收表逐条走

---

## 六·附 · ModKit 一次性操作清单（全部由人做，一次做完）

> 源文件**已经全部放好了**，你只需要在 ModKit 里导入 + 编译 + 发布。做完这一轮，剩下的 XML 接线和实机我来。

### 准备

- 跑 `Modules\TaikouAnim\to_editor_mode.bat`（把 `Assets_disabled` 改回 `Assets`——编辑器必须有这个目录才看得到工程）
- 开 ModKit，打开 **`TaikouAnim`** 模块（它只依赖 Native，能正常打开）

### A · 飞行动画（7 条）

源文件已就位：`TaikouAnim\AssetSources\animations\flight\`

| # | TRF | **Source 1** | **Source 2** | 用途 |
|---|---|---|---|---|
| 1 | `fly_A_Flight_Idle_A.trf` | 2 | 92 | 悬停待机 |
| 2 | `fly_A_Flight_Hover_Start_A.trf` | 2 | 47 | 起飞 |
| 3 | `fly_A_Flight_HoverMove_A.trf` | 2 | 62 | 巡航 |
| 4 | `fly_A_Flight_FastMove_A.trf` | 2 | 32 | 冲刺（趴姿） |
| 5 | `fly_A_SuperheroLanding_A.trf` | 2 | 62 | 落地 |
| 6 | `fly_A_FM_A_Pose_U.trf` | 2 | 3 | **爬升**（直立上升） |
| 7 | `fly_A_FM_A_Pose_D.trf` | 2 | 3 | **俯冲**（头朝下） |

> 🔴 **`Lean_*` 那两条本轮【不要导】** —— 它们不属于俯仰姿态，是「转向倾斜」那套体系的两块（详见 §三 的订正）。
> ✅ **已挪出导入目录**（2026-09-21）：`AssetSources\animations\flight_lean_reserved\` + 一份 README 说明来龙去脉。
> 所以 `flight\` 目录里现在**正好就是上面 7 条**，照单全导即可，不会多也不会少。
> 第 6、7 条只有 2 帧，是**定格姿态**——飞行没有脚部着地，定格姿态读起来就是"保持这个姿态在飞"，不是卡住。

每条：**导入 TRF → 建 Animation Clip → 填上表的 Source 1/2 → `Duration > 0` → 元数据整组抄原版同类 clip**。

> 🔴 **不填 Source 会报 `pos ipo[...] does not fit sources 0.00 and 0.00`，clip 存不下来。**
> 🔴 **元数据要整组抄**（`Blend in period` / `Priority` 等）—— 裸导会"能播但流程不认"。
> 🔴 目录名是 **`flight`**（不是 `fly`）。同模块下旧的 `AssetSources\Fly\` 是历史残留，**别弄混**。

### B · 飞行法阵（网格 + 贴图 + 材质）

源文件已就位：`TaikouAnim\AssetSources\sigil\lwn_flight_sigil\`

| # | 文件 | 怎么导 |
|---|---|---|
| 6 | `lwn_flight_sigil.fbx` | 导成**静态网格**：**不勾 Skinning、不要骨架**（跟武器的做法一致） |
| 7 | `lwn_flight_sigil_d.png` | 导成贴图（已经过 `png_for_editor` 洗过格式，直接导即可） |
| 8 | 材质 | 新建材质，贴 `lwn_flight_sigil_d` |
| 9 | 🔴 **把预制体拷回加载路径** | `TaikouAnim\AssetSources\sigil\lwn_flight_sigil\lwn_flight_sigil.prefab.xml` → 拷成 `Modules\Taikou\Prefabs\lwn_flight_sigil.xml`（**Publish 之后、装包之后**再做） |

> 🔴🔴 **第 9 步不能提前做**（2026-09-21 实机崩过）：预制体里写着 `<meta_mesh_component name="lwn_flight_sigil"/>`，
> 而那个网格**还没进任何包**时就去 `GameEntity.Instantiate` = **native 访问违例，游戏当场崩**，
> `try/catch` 拦不住（AV 属"损坏状态异常"，.NET 默认不允许捕获）。
> ⇒ 预制体现在**暂存在 AssetSources 里**，等网格进包了再放回去。
> 代码侧也加了双保险：实例化之前先用 `MetaMesh.GetCopy(name, false, mayReturnNull:true)` 探网格在不在，不在就跳过。
> （已登记为坑点清单 **雷 149**）

> 🔴 **材质必须是【加法 / 自发光】档**，不能是不透明档 —— 贴图是**黑底 RGB**（无 alpha，因为本工程贴图管线只吃 8-bit RGB）。
> 黑底 + 加法 = 黑的地方加零 = 视觉上等于透明，亮的地方自己发光。**建成不透明档就会看到一块黑方块。**
> 想改成"透明底 + alpha 混合"也行，但那要另做一张带 alpha 的贴图并确认编辑器接受——**加法档更省事且观感更好**。

### C · 编译发布

- **编译**（`Assets\` 里出现产物才算真的编了）
- **Publish，目标指向模块外**（例如 `Modules\Publish\TaikouAnim`）—— 🔴 **千万别指到 `Taikou\AssetPackages`，Publish 会清空目标目录**
- 产物**改名**拷进 `Modules\Taikou\AssetPackages\lwn_taikou_anim.tpac`
  > 🔴 **绝不能叫 `pack0.tpac`** —— 内容包自己那个 299 MB 的包会**被覆盖**（那是整个太阁的资产）。
  > 🔴 **导入前先备份** `lwn_taikou_anim.tpac`（里面已有实机验证过的 `gun_reload` / `flight_idle_a` / `execution_02`）。
  > ✅ **必须和已有的 gun / execute 放在同一个 TaikouAnim 工程里一起 Publish** —— 另起干净工程只编飞行 = 装填动画被冲掉、铁炮装填回退成弩动作。

### D · 收尾

- 跑 `Modules\TaikouAnim\to_game_mode.bat`（把 `Assets` 改名）—— **跑游戏前必做**，否则引擎读到编辑器的半成品
- **告诉我一声**，我接着做：用 `tpaccli` 读出 7 条 clip 的真实名字 → 写 `action_types.xml` / `action_sets.xml` → 核对法阵的 MetaMesh 名（确认 `Taikou\Prefabs\lwn_flight_sigil.xml` 里的 `name` 对不对）→ 重编译 → 你实机验收

---

## 七、验收

| # | 验什么 | 合格线 |
|---|---|---|
| 1 | 木板**看不见** | 起飞全程画面里没有任何木板 / 方块 |
| 2 | 脚下**有法阵** | 法阵贴着人一起飞，**任何高度都在**（不会掉在地面上或消失） |
| 3 | 法阵**暗处可见** | 夜里 / 阴影里也能一眼认出来（自发光档生效） |
| 4 | 人**站在**它上面 | 高度上升时脚底贴着平面，不是浮在半空 |
| 5 | 长按空格能起飞 | 到达时长才触发，不会误触 |
| 6 | **镜头方向 = 飞行方向** | 抬头看天 + W 会爬升；低头会俯冲 |
| 7 | 姿态随状态变 | 悬停 / 巡航 / 冲刺三种姿势能肉眼区分，切换是淡化不是硬切 |
| 8 | 冲刺是趴姿 | 按 Shift 明显变成超人前扑姿势且提速 |
| 9 | 落地动画播了 | 退出时播落地，而不是瞬间站回原版姿势 |
| 10 | 🔴 **负面：不飞的时候一模一样** | 走路 / 跑步 / 跳跃 / 攻击 / 骑马与改动前**完全一致** |
| 11 | 🔴 **负面：装填没被带坏** | 铁炮装填仍播 `gun_reload`（回归测试） |
| 12 | 🔴 **负面：其他角色不受影响** | 随从 / 士兵一切正常 |
| 13 | 退出干净 | 落地后控制权、跳跃、镜头全部恢复；法阵与木板一起消失；ESC 退场景不残留 |
| 14 | 🔴 **冻结生效**（T1） | 飞行中按 WASD，角色**原地不动**（判据：`[Flight-Diag]` 里 `frozen=1` 且 `engineMove` 有非零位）；板照常跟着 WASD 飞 |
| 15 | 🔴 **解冻干净**（T1） | 落地后 WASD **立刻**恢复走路（不用等、不用再按一次）；飞行中开 IM 面板打字**不会**带动飞行 |

---

## 八、回退（全部一行）

| 改动 | 回退 |
|---|---|
| XML 接线 | 删掉 `action_types.xml` / `action_sets.xml` 里那 8 行（文件留着不生效，无害） |
| 代码 | 删掉 `MySubModule.cs` 里 `AddMissionBehavior` 那一行 |
| 资产 | `lwn_taikou_anim.tpac` 回滚到备份（**导入前必须备份**） |
| 动画 TRF | 只管新产出的 9 条（其中 7 条入包），不碰其余 121 条 |

---

## 九、风险与未决

| # | 风险 | 处置 |
|---|---|---|
| 1 | **载具看不见但有碰撞体**（实测 5.29 × 5.16 米） | 飞行时在空中无影响；起降瞬间可能挡住 NPC / 箭。先观察，必要时换更小的物理体 |
| 2 | **掉出世界** | 实测从 169 米跳下后坐标归零。要做越界回收（高度上限 + 触底自动落） |
| 3 | **存档** | 飞行状态是否要存、载具怎么随读档恢复 —— 本轮**不做**，飞行视为场景内临时状态 |
| 4 | **骑马时起飞** | 行为未定义。本轮做法：骑马时**禁止起飞** |
| 5 | **女角色** | `as_human_female_warrior` 是另一个动作集，**大概率拿不到我们的动作**，要额外绑一份。本轮先只保证男性主角 |
| 6 | **角色相对木板的高度** | 趴姿 / 抬头姿态的"身体离板高度"可能要微调，属于导入后看效果的调参项 |
| 7 | **E1 不过** | 若"载具瞬移到脚下"不成立，退到"载具生成在脚边 + 人走半步再抬"，体验略差但可行 |
| 8 | **法阵平面起降时穿模** | 法阵是块平面，贴地瞬间可能插进地形。空中无影响；先观察，必要时按地形法线摆正（`FlightTuning` 里留角度参数） |

---

## 十、随之要更新的文档

| 文档 | 更新什么 | 时机 |
|---|---|---|
| [自定义移动管线.md](自定义移动管线.md) | ① 第一部分标题下加指向本文的链接（✅ 已加）② 订正 §二·5 那条"水平载人只是推断"（`FlySpike` 现已打 x/y 日志）③ 第一部分状态由「待实施」改为「施工中 · 见实施方案」 | 即刻 |
| [Knowledge/骑砍2Agent运动与位置机制.md](../Knowledge/骑砍2Agent运动与位置机制.md) | ① 🔴 **订正错误结论**：§5 / §6 表 #2 / §八 里"切控制权会丢镜头、agent 被搬到兜底坐标"是错的（变量未分离）——实际是**切 AI 后角色原地待机、镜头仍在玩家身上**，`SetPlayerControlFrozen` 是可用且推荐的默认做法 ② §八 指令表补 `custom.prop` / `custom.flight` ③ 订正"1842 行"等过期数字 | ①**即刻** ②③阶段 3 |
| `CampaignMode/Tools/FlySpike.cs` | 那两处旧注释（`FlySpike.cs:60-62` 的 `TickAsAI` 描述、`:2081` "输入失效、镜头丢失"）同批订正 —— **只改注释，不动代码**（该文件已封存不加新功能） | 即刻 |
| `Core/VersionCompat.cs:571-576` | 同一处错误结论也在**活代码的注释**里（`SetAgentControllerNone` 上方那段"AI = 会把 agent 当 AI 单位驱动 → 自己跑去了 z=15.06"）—— 后读代码的人会信它。**只改注释，不动逻辑** | 即刻 |
| [plans/rules/wheels.d/assets.md](rules/wheels.d/assets.md) §十五 | 登记"7 条飞行动画 + 飞行状态机接线"这组轮子 | 实机通过后 |
| [Knowledge/自定义世界内容包从零起步必备清单.md](../Knowledge/自定义世界内容包从零起步必备清单.md) | 实施中若踩到新雷，按纪律登记 | 实施中 |
