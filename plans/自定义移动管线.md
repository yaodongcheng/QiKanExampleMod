# 自定义移动管线

> **状态：施工中**（2026-09-20 立，由 `玩家飞行方案.md` + `移动模式补充.md` + `移动模式补充.plan.md` 三份合并）
> **一句话**：这一摊事本质都是「**改引擎的 movement**」—— 一是让**玩家本人**飞起来，二是让**铁炮兵**换一套走跑。
> **为什么合在一起写**：两者共用同一条资产管线（TRF 导入 → ModKit 编译 → 内容包接线），分开写会互相踩。

> **三部分**：
> · **第一部分 · 玩家飞行方案** ← 🔴 当前主攻。隐形载具 + 法阵特效，**核心机制已实机验证**。
> · **第二部分 · 铁炮持枪走跑（主计划）**：整套换装，要自制 10 条 clip。**未开工**。
> · **第三部分 · 阶段 1 执行版**：只换 idle 一个槽，用飞行动画占位先验管线。**从这里开工**。

---

# 第一部分 · 玩家飞行方案


> **状态：待实施**（2026-09-20 立）
> **一句话**：**玩家能飞，已经实测证明了。** 做法 = 一块**看不见的实心板**当"载具"，每帧改它的坐标把人抬走；视觉上用**飞行环 / 法阵特效**盖住那块板。
> **这是什么**：[Knowledge/骑砍2Agent运动与位置机制.md](../Knowledge/骑砍2Agent运动与位置机制.md) §6.6 的玩法化。那份是实验记录（含 9 条失败路），这份是"怎么把它做成功能"。

> **三个词先说清楚**：
> · **载具** = 那块看不见的实心场景道具。玩家站上去，引擎就把他算在它表面上。
> · **逐帧瞬移** = 每帧直接把载具的坐标改掉（不是施加物理速度）。**这是唯一能载人的移动方式，见 §三**。
> · **法阵** = 贴给玩家看的特效（光环 / 阵图）。它**不参与任何物理**，纯粹是"让玩家理解自己正踩在一个东西上"。

---

## 〇、结论速览

| 问题 | 答案 |
|---|---|
| 玩家能飞吗 | ✅ **能**。已实测：站在实心木板上被抬到 **157 米**，位置分毫不差 |
| 靠什么飞 | 一块**看不见的实心场景道具**当载具，**逐帧改它的坐标** |
| 要写 agent 自己的高度吗 | ❌ **不需要**（也写不进去，§6 结论 1）。引擎自己会把人算在载具表面上 |
| 视觉怎么做 | **飞行环 / 法阵特效**贴在载具上 |
| 现在就能做吗 | ⚠️ 只差一件：**载具"瞬移到脚下"算不算"站上去"未验证**（§四·1）。水平飞行**已验证可行**（§二·5） |
| 要写多少代码 | 载具生成 + 速度控制**已经有了**（`custom.prop` 命令族 = 现成脚手架），剩下的是把它从实验脚手架变成玩法 |
| 归属 | 逻辑进 **LWN**（通用基座）；特效资产进**内容包** |

---

## 一、原理

```
玩家
 └─ 站在 → 【载具】一块看不见的实心场景道具（有网格、有物理体，只是不显示）
              ↑ 每帧：坐标 += 速度 × dt
              └─ 挂 → 【法阵特效】飞行环 / 光环（纯视觉，不参与物理）
```

**为什么这样就飞起来了**：引擎把玩家脚下的实心道具当作"落脚面"，道具往上走，玩家的落点就跟着往上走 —— **不需要我们写玩家的高度**。

---

## 二、已验证的（实测数据，可直接依赖）

| # | 结论 | 数据 |
|---|---|---|
| 1 | ✅ **垂直载人成立** | `dz` 全程 **+0.36 分毫不差**（= 载具顶面），从 8 米抬到 157 米 |
| 2 | ✅ **只认"逐帧瞬移"** | 每帧 `SetFrame()` 改坐标；这是能载人的移动方式 |
| 3 | ✅ **载具的物理属性随便** | 静态体 / 动态体都照样托人（不用管 mobility） |
| 4 | ✅ **起步只有约 1 秒的视觉过渡** | 逻辑位置当场跟上，只有**画面**慢半拍（"板卡腰"）；停速即恢复 |
| 5 | ✅ **水平也能载人** | 载具 10 m/s 水平飞了 8 秒（80 米），玩家离板心的偏移 `rel` 全程恒定 ⇒ 人跟着飞 |

> ✅ **"卡腰"这件事不用治**（2026-09-20 定）：载具本来就要**隐藏**，玩家看到的是**飞行环 / 法阵特效**。
> 特效跟着载具走，起步那一秒顶多是"法阵在脚下浮了一下"，**不会出现"木板插进腰里"那种违和感**。
> ⇒ **零代码处理，直接接受。**（木板只是实验期的可视化替身，出货版本根本没有那块板。）

> ⚠️ **第 5 条的保留**：`rel` 恒定也可能是"两者都没动"。日志**从来没打过载具的 x/y**（只打了 z），
> 所以这条严格说是**推断**。做水平飞行之前，补一行 x/y 日志把它钉死（改一行的事）。

**载具尺寸参考**（`wooden_platform_a` 实测）：`5.29 × 5.16 × 0.40 m`，原点在中心底面、顶面在局部 `z≈0.37`。

---

## 三、🔴 两条不能走的岔路（都实测撞墙了）

| 岔路 | 结果 |
|---|---|
| **用物理速度驱动载具**（让它自己飞） | ❌ **完全不托人** —— 载具从玩家身体里穿过去 |
| **用加速度闭环校正速度** | ❌ **半秒内数值爆炸**（速度冲到 5.97e14），且不托人 |

⇒ **载具只能用"逐帧改坐标"这一种方式移动。** 想调速就改每帧位移量，别去碰物理。

---

## 四、现在挡路的一件事

### 🔴 载具"瞬移到脚下"算不算站上去（未验证）

现有实验里，玩家都是**自己走过去**站上载具的。但飞行玩法要的是"**法阵直接出现在脚下，人立刻起飞**"。
**这一步没测过** —— 引擎记"你站在哪块面上"可能只在**移动时**才更新（§7.1 的导航件就栽在这）。

**验证方法**：载具直接生成在玩家脚下，然后立刻抬 —— 看 `playerZ` 跟不跟。

> 顺带补一刀：日志目前**没打载具的 x/y**，水平载人只有推断（§二·5）。一起补掉。

---

## 五、要做的几件事

| # | 做什么 | 归属 |
|---|---|---|
| 1 | 解决 §四（载具瞬移到脚下）+ 补 x/y 日志钉死水平载人 | 实验，先做 |
| 2 | 载具实体：尺寸缩到**比玩家略大**（如 1.5 × 1.5 × 0.1），**不显示网格** | LWN |
| 3 | 速度控制：已经现成（`custom.prop speed` 的向量控制），搬成玩法接口 | LWN |
| 4 | 玩家上下载具的触发（技能 / 按键 / 剧情） | LWN |
| 5 | **飞行环 / 法阵特效** | 内容包（资产） |
| 6 | 女角色 / 骑马 / 进场景等边界 | LWN |
| 7 | **角色飞行姿势**（飘着时不能还是站着/走路的姿势） | 内容包（资产）—— 🔴 **接线方法见本文第三部分**（那份就是在做"把站着不动换成飞行动画"） |

**命名纪律**：LWN 是通用基座，类名一律中立 —— 用 `Hover` / `Levitate` / `Flight`，**不能出现世界观词**（铁律 3）。

---

## 六、回退 / 风险

| 风险 | 说明 |
|---|---|
| 载具有实体碰撞 | 看不见但有碰撞 ⇒ 可能卡住 NPC、挡住弹道。要评估是否加碰撞过滤 |
| 掉出世界 | 实测玩家从 169 米高空跳下去后 `z` 归 0、水平位置跑到 (-176,0) 这种默认点。需要越界回收 |
| 存档 | 飞行状态是否要存、载具怎么随存档恢复 —— 未定 |
| 与现有载具（马）冲突 | 骑马时再召唤载具的行为未定义 |

---

## 附：本文结论的取证来源

| 结论 | 出处 |
|---|---|
| 垂直载人成立 / 实测数据 | `Knowledge/骑砍2Agent运动与位置机制.md` §6.6 |
| 9 条失败路 | 同上 §6 |
| "Z 写不进去" | 同上 §二 |
| 拨掉控制权会丢镜头 | 同上 §5 · §6 表 #2 |
| 载具尺寸实测 | `tpaccli dump` 量 `wooden_platform_a` 包围盒（2026-09-20） |
| 实验脚手架 | `ExampleModVS/ExampleMod/ExampleMod/CampaignMode/Tools/FlySpike.cs` 的 `custom.prop` 命令族 |

---

# 第二部分 · 铁炮持枪走跑（主计划）


> **状态：待实施**（2026-09-19 立）
> **一句话**：让拿着铁炮的角色站着 / 走 / 跑 / 转身时用我们自己的动画。需要 **10 条自制 clip + 6 处 XML 改动**，**零 C#**。
> **这是什么**：[铁炮射击手感3C-实施计划.md](铁炮射击手感3C-实施计划.md) 的补充。那份管**一次性动作**（举枪 / 摇摆 / 装填 / 后坐），这份管**循环动作**（站着不动 / 走 / 跑 / 转身）。

> **本部（第二部分）覆盖 §〇~§十一** = 主计划：整套换装，**要自制 10 条 clip**。
> 分阶段执行在 **第三部分**（只换 `idle` 一个槽，用已重定向好的飞行动画当占位）。
> 🔴 **实际开工从第三部分开始** —— 它是性价比最高的探针：整条链任何一环断了，idle 都会第一个暴露。

> **三个词先说清楚**：
> · **移动集** = 一张「18 个槽位 → 动作名」的表，告诉引擎「往这个方向走的时候播哪条动画」。
> · **整套移动组** = 把「走 / 跑 / 蹲走 / 蹲跑」四档各指到一个移动集。
> · **追加层** = 叠在走路动画上的躯干修正，**不属于本次要改的东西**（见 §五）。

---

## 〇、结论速览

| 问题 | 答案 |
|---|---|
| 能新增一种移动模式吗 | ❌ **不能**。人形只有 4 个模式（走 / 跑 / 蹲走 / 蹲跑），名字写死在引擎里，数据层加不了第 5 个 |
| 那这次做什么 | **给这 4 个档换外观** —— 模式不变，每个模式指向我们的新动作 |
| 要提供多少条 clip | **一个档 = 10 条**（1 idle + 9 方向）。最小可用 = 只做「走」这一档 |
| 要改几处 XML | **6 处** = 2 个新文件 + 3 处追加 + 1 处改行 |
| 要不要写 C# | ❌ **零代码** |
| 影响谁 | **所有拿着铁炮的角色**（玩家 + 全部铁炮足轻）。数据层做不到「只影响玩家一个人」 |
| 怎么回退 | 一行：`item_usage_sets.xml` 里改回 `crossbow` |

**跟装填的关系**：装填（`reload_action`）已经走通过这条数据链，本次是同一个机制的另一种用法。装填踩过的坑（新 id 零交集、逐字抄原版、mbproj 必须挂 soln 行）**条条适用**。

---

## 一、引擎的边界

**人形只有 4 个运动模式，且不可扩展。**

- 模式名字（`walking` / `running` / `crouch_walking` / `crouch_running`）在 native 二进制里就是这四个字面量，C# 侧只能读（`Agent.WalkMode` / `Agent.CrouchMode`）。
- 数据层能做的只有一件事：**告诉引擎「这个模式下播哪条动画」**。
- 所以「加一种移动模式」的真实含义 = **加一套移动集**。走路还是走路，只是长得不一样了。

**走 / 跑 / 蹲 由谁切**：引擎的两把开关（`EventControlFlag.Walk/Run/Crouch/Stand`），玩家或 AI 拨；另有 `monsters.xml` 里的速度阈值 `walking_speed_limit="1.8"` / `crouch_walking_speed_limit="1.3"`。**两者的配合关系未验证**（见 §七）。

---

## 二、引用链

🔴 箭头方向 = 引用方向。改这套东西就是从下往上接线。

```
物品 item_usage="tk_firearm"
 └ item_usage_sets.xml   tk_firearm 的 <movement_sets>          ← 改 1 行（§四·6）
    └ full_movement_sets.xml  <full_movement_set id="taikou_gun">
       │                       四档各指一个移动集                ← 新增（§四·2）
       └ movement_sets.xml  <movement_set id="taikou_gun_walk">   ← 新增（§四·1）
          │                   18 个槽 → 动作名
          └ action_sets.xml   动作名 → clip                        ← 追加（§四·4）
             ↑
          action_types.xml   动作名声明                            ← 追加（§四·3）
```

**每个文件都要在 `project.mbproj` 挂 soln 行，但 `action_sets` / `action_types` 已经挂过了**，本次只需新挂 2 行（§四·5）。

---

## 三、要提供哪些 clip

### 3.1 一个移动集 = 18 个槽，一个都不能少

原版 103 条移动集**逐条数过，全部是满的 18 槽**（外加一个 id）。

| 类别 | 槽 | 要不要自制 |
|---|---|---|
| 静止 | `idle` | ✅ 要 |
| 9 个方向 | `forward` `backward` `left` `right` `left_back` `right_back` `left_to_right` `right_to_left` `rotate` | ✅ 要 |
| 8 个追加层 | `forward_adder` `backward_adder` `left_adder` `right_adder` `left_back_adder` `right_back_adder` `left_to_right_adder` `right_to_left_adder` | ❌ **照抄原版动作名** |

**→ 一个档 = 10 条自制 clip。**

> **追加层为什么不用做**：它们不分武器 —— 原版所有移动集共用同一条（如 `act_walk_forward_adder`），不存在 `_crossbow` 变体。槽里填原版名字即可。
>
> **18 个槽一个都不能省**：原版 103/103 全满。省了会怎样没验证过，别赌。

### 3.2 四档怎么分配

| 模式 | 建议 | 不做就指向 |
|---|---|---|
| `walking` | ✅ **做这一档** | — |
| `running` | 可选 | `run_crossbow` |
| `crouch_walking` | 不做 | `crouch_walk_crossbow` |
| `crouch_running` | 不做 | `crouch_run_crossbow` |
| `swimming` / `diving` | ❌ 不做 | `swim_unarmed` / `dive_unarmed`（原版弩用的也是 unarmed） |

**所以最小可用 = 10 条 clip**（只做「走」档，其余四档指向原版）。四档全做 = 40 条。

`left_stance`（左撇子）**不需要做**：铁炮的用法组带 `has_single_stance="true"`，只有 `False` 一份。

### 3.3 每条 clip 建的时候照抄谁

| 我们的槽 | 照抄的原版 clip |
|---|---|
| `idle` | `troop_stand_crossbow_1` |
| `forward` / `backward` / `left` / `right` | `walk_forward_crossbow` / `walk_backward_crossbow` / `walk_left_crossbow` / `walk_right_crossbow` |
| `left_back` / `right_back` | `walk_left_back_crossbow` / `walk_right_back_crossbow` |
| `left_to_right` / `right_to_left` | `walk_left_to_right_crossbow` / `walk_right_to_left_crossbow` |
| `rotate` | `turn_crossbow` |

⚠️ **动作名和 clip 名经常不同名** —— `act_idle_crossbow_1` 指向的 clip 叫 `troop_stand_crossbow_1`。**别按名字猜，照抄前先在 `action_sets.xml` 里查一遍。**

**为什么要抄元数据**：clip 的 `Blend in period` / `Blend out period` 决定「从站着到走」怎么混过去；`Bipedal movement and ik` 那一组决定脚步落地时机。**裸导 = 这两样都是空的 → 硬切 + 脚打滑。**（装填那颗雷的同款成因。）

### 3.4 减到多少算安全

| 做法 | 结果 |
|---|---|
| 只换 1~5 个方向槽，其余照抄 | ⚠️ 能跑，但**风格割裂**（前走是我们的、斜走是原版的）。**只适合验管线，不适合出货** |
| 换齐一个档的 10 条 | ✅ 出货标准 |
| 8 个追加层槽 | 永远照抄，不用自制 |

---

## 四、XML 配置（6 步）

全部落在 `Modules/Taikou/ModuleData/`。

### 1️⃣ 新增 `movement_sets.xml`

```xml
<movement_sets>
	<movement_set
		id="taikou_gun_walk"
		idle="act_gun_walk_idle"
		forward="act_gun_walk_forward"
		backward="act_gun_walk_backward"
		right="act_gun_walk_right"
		right_back="act_gun_walk_right_back"
		left="act_gun_walk_left"
		left_back="act_gun_walk_left_back"
		left_to_right="act_gun_walk_left_to_right"
		right_to_left="act_gun_walk_right_to_left"
		rotate="act_gun_turn"
		forward_adder="act_walk_forward_adder"
		backward_adder="act_walk_backward_adder"
		right_adder="act_walk_right_adder"
		right_back_adder="act_walk_right_back_adder"
		left_adder="act_walk_left_adder"
		left_back_adder="act_walk_left_back_adder"
		left_to_right_adder="act_walk_left_to_right_adder"
		right_to_left_adder="act_walk_right_to_left_adder" />
</movement_sets>
```

🔴 **id 必须全新。** 这张表**没有同名合并**（引擎原样直送 native），撞 id = 09-08 那次崩的形态。带 `taikou_` / `lwn_` 前缀最稳。

### 2️⃣ 新增 `full_movement_sets.xml`

```xml
<full_movement_sets>
	<full_movement_set id="taikou_gun">
		<movement_set id="taikou_gun_walk"      left_stance="False" movement_mode="walking" />
		<movement_set id="run_crossbow"         left_stance="False" movement_mode="running" />
		<movement_set id="crouch_walk_crossbow" left_stance="False" movement_mode="crouch_walking" />
		<movement_set id="crouch_run_crossbow"  left_stance="False" movement_mode="crouch_running" />
		<movement_set id="swim_unarmed"         left_stance="False" movement_mode="swimming" />
		<movement_set id="dive_unarmed"         left_stance="False" movement_mode="diving" />
	</full_movement_set>
</full_movement_sets>
```

> 这张表**引擎有同名合并**（同 id 的子节点并进第一个），用新 id 仍是纪律 —— 往原版 `crossbow` 里塞第二个 `walking`，谁生效是未定义的。
> 档位**可以缺**（原版 25 个里有 4 个缺档），但缺档时引擎怎么表现**没验证**，所以本次把六档填满、不做的指向原版。

### 3️⃣ `action_types.xml` 追加 10 行 —— 🔴 **两种声明方式不一样**

```xml
<action_types>
	<!-- 9 个方向 + 转身：裸声明，不带任何属性 -->
	<action name="act_gun_walk_forward" />
	<action name="act_gun_walk_backward" />
	<action name="act_gun_walk_left" />
	<action name="act_gun_walk_right" />
	<action name="act_gun_walk_left_back" />
	<action name="act_gun_walk_right_back" />
	<action name="act_gun_walk_left_to_right" />
	<action name="act_gun_walk_right_to_left" />
	<action name="act_gun_turn" />
	<!-- idle（移动集里的静止槽）：同样是裸声明 -->
	<action name="act_gun_walk_idle" />
</action_types>
```

**规则（原版数据实证）**：

| 动作种类 | 声明方式 | 实证 |
|---|---|---|
| 走 / 跑 / 转身（含移动集里的 `idle` 槽） | **裸声明** `<action name="X" />` | 590 / 590 条全裸 |
| 追加层 | **裸声明** | 41 / 41 条全裸 |
| **武器待机**（用法组的 `<idles>`，如 `act_idle_crossbow_1`） | 🔴 **必须带 `type="actt_idle"`** | 73 条里 71 条带 |
| 装填 | 带 `type="actt_reload"` + `action_stage` | 见 3C 计划 §〇 |

> ⚠️ **两个 idle 不是一回事**：移动集的 `idle` 槽指向 `act_walk_idle_crossbow` 这类（裸声明）；用法组的 `<idles>` 指向 `act_idle_crossbow_1`（带 `actt_idle`）。**本次只动前者**；后者要改就是 §六 的扩展项。

### 4️⃣ `action_sets.xml` 追加绑定

```xml
<action_set id="as_human_warrior" skeleton="human_skeleton" movement_system="bipedal">
	<action type="act_gun_walk_forward" animation="gun_walk_forward" />
	<!-- …其余 9 条同样式… -->
</action_set>
```

现有文件已经有 `act_gun_reload` 的绑定，**追加**即可。引擎按 id 把我们的块并进 Native 的同名块，原版几千条动作不会被挤掉。

🔴 `animation=` 填的是 **clip 名**，不是 FBX take 名、不是骨架动画名。写错 = 静默解析成 `act_none`，**没有任何报错**，只是播不出来。

### 5️⃣ `project.mbproj` 挂 2 行

```xml
<file id="soln_movement_sets"      name="ModuleData/movement_sets.xml"      type="movement_set"/>
<file id="soln_full_movement_sets" name="ModuleData/full_movement_sets.xml" type="full_movement_set"/>
```

**不挂这两行 = 两个文件从不解析。** id 名照抄 Native 的 mbproj（第 41 / 42 行）。

### 6️⃣ `item_usage_sets.xml` 改 1 行

```xml
<movement_sets>
	<movement_set id="taikou_gun" />     <!-- 原为 id="crossbow" -->
</movement_sets>
```

🔴 这里的 `<movement_set id="...">` 指的是**整套移动组**（`full_movement_set` 的 id），不是移动集 —— 原版 19 处引用全是这么对的（19/19 命中 `full_movement_set`，0 命中 `movement_sets`）。名字反直觉，别搞错。

**回退 = 这一行改回 `crossbow`。**

---

## 五、追加层（不用动，但得知道它是什么）

原版 `monsters.xml` 里 human 有这么一组：

```
move_adder_bone_0="spine"    move_adder_bone_4="r_clavicle"
move_adder_bone_1="spine1"   move_adder_bone_5="neck"
move_adder_bone_2="spine2"   move_adder_bone_6="head"
move_adder_bone_3="l_clavicle"
```

**7 根骨，正好等于引擎常量 `MoveAdderBoneMaxCount = 7`**，且全是脊柱链 + 锁骨 + 头颈。

→ 追加层是**只作用在脊柱链上的叠加动画**：基础走路动画驱动全身，追加层在上面加躯干修正。它们**不分武器**（只有 `act_walk_forward_adder`，没有 `_crossbow` 变体），所以本次直接沿用原版。

⚠️ 上面这句「脊柱叠加层」是基于骨表和「无武器变体」两条实证的**推断**，不是直接读到的实现。

---

## 六、四条纪律

1. **影响所有拿铁炮的角色** —— 玩家 + 全部铁炮足轻，跟装填一样。**只想改玩家一个人 → 数据层做不到**，得走运行时换整套动作集（LWN 侧代码），代价见 §八。
2. **女性角色大概率用不上** —— `as_human_female_warrior` 用 `base_set="as_human_warrior"` 继承，但它**明确覆盖了走跑全套**（换成 `female_walk_*`）。我们新声明的动作名它没覆盖 → **理论上会继承到我们这条**，但**没实测**。要女版就得在那个动作集里再绑一份。
3. **每条 clip 先单独验** —— `custom.do_anim act_gun_walk_forward`，命令现成，不用接任何线就能看姿势对不对。**这是最便宜的检查点。**
4. **顺序**：先 action_types + action_sets → `do_anim` 确认 10 条都能播 → 再上 movement_sets / full_movement_sets / usage → 最后实机走路看。

---

## 七、验收

| # | 验什么 | 怎么验 |
|---|---|---|
| 1 | 10 条动作都能播 | `custom.do_anim <动作名>` 逐条 |
| 2 | 走路的姿势换了 | 实机掏出铁炮向前走，对比改动前 |
| 3 | 🔴 **八个方向都对** | 前 / 后 / 左 / 右 / 斜向各走一遍，不该出现「前走是我们的、斜走是原版的」割裂 |
| 4 | 🔴 **脚不打滑** | 慢走时看脚有没有在地上搓 —— 打滑 = clip 的 `Bipedal movement and ik` 那组没抄 |
| 5 | 🔴 **站着不动没变呆** | idle 是我们的，且不是僵直姿势 |
| 6 | 转身正常 | 原地转圈看 `rotate` |
| 7 | 🔴 **负面测试：不带铁炮的角色不受影响** | 空手 / 拿刀走一遍，应与原版完全一致 |
| 8 | 🔴 **负面测试：收枪状态不受影响** | 铁炮收起时走路应还是原版（那走的是 `no_weapon` 那套） |
| 9 | 装填没被带坏 | 装填仍播我们的动画（回归） |

---

## 八、未验证 / 未决

| # | 事项 | 影响 |
|---|---|---|
| 1 | 移动集里的 `idle` 槽与用法组的 `<idles>` **谁在「站着不动」时生效** | 若发现站姿没变，说明是用法组那一条在管 → 需额外改 `<idles>`（带 `actt_idle` 声明） |
| 2 | 女角色到底拿不拿得到我们的新动作（`base_set` 继承） | 拿不到 = 女性铁炮兵还是原版走路；要一致就得加绑一份 |
| 3 | `walking_speed_limit`（1.8）与 Walk/Run 开关怎么配合 | 目前不改它，若走路速度观感不对再回来查 |
| 4 | clip 元数据里 `Pace switch limit` / `Snapping` / `Adjusting` 的**确切语义** | 名字是按上下文读的，抄的时候整组抄即可，不必逐个理解 |
| 5 | ModKit 的 `Create Override` / `Select base clip` 能不能对动画 clip 用 | 能用的话，「抄元数据」这步可能可以省掉（见 §九） |

---

## 九、可选的前置探查（便宜，建议先做）

**ModKit 里有 `Create Override` / `Select base clip` 这个入口**，很可能是「照抄某条已有 clip 建新 clip」的官方做法。**如果 AnimationClip 这个类型允许被覆盖**，那 §三·3 的「逐条抄元数据」就能省掉 —— 建覆盖时元数据天然继承。

⚠️ 编辑器里有一句提示 `This item type can not be overriden`，说明「覆盖」是**分类型的**，动画 clip 属不属于可覆盖类型未知。**开 ModKit 时花两分钟试一下**，值。

---

## 十、归属

| 东西 | 放哪 | 依据 |
|---|---|---|
| 动画资产 | `Modules/Taikou/AssetPackages/`（**改名拷入**，别覆盖 `pack0.tpac`） | 内容包 = 纯数据 |
| `movement_sets.xml` / `full_movement_sets.xml` / `action_sets.xml` / `action_types.xml` / `item_usage_sets.xml` / `project.mbproj` | `Modules/Taikou/ModuleData/` | 同上 |
| 动画源工程 | 中转沙箱模块（`TaikouAnim` 那类，运行时**不加载**） | 见 3C 计划 §〇 |
| C# | **本次零代码** | — |
| 备选路（只改玩家一个人） | 运行时 `Agent.SetActionSet` → 归 LWN | 三单元铁则 1 |

**LWN 侧类名一律中立** —— 不能出现铁炮 / 种子岛 / 火绳枪之类的世界观词汇（铁律 3）。

---

## 十一、交付后待办

| 待办 | 触发时机 |
|---|---|
| 登记轮子进 `plans/rules/wheels.d/assets.md` §十五（自制动画进游戏） | 实机验证通过之后 |
| 补坑点进 `Knowledge/自定义世界内容包从零起步必备清单.md` | 实施中踩到新雷时 |
| 更新 [Knowledge/骑砍2Agent运动与位置机制.md](../../Knowledge/骑砍2Agent运动与位置机制.md) §3 —— 那份现在写的是「只能换整套 action set」，**漏了「按武器换移动集」这条更轻的路** | 实施验证后 |

---

## 附：本文结论的取证来源

| 结论 | 出处 |
|---|---|
| 移动集 18 槽、103 条全满 | `Modules/Native/ModuleData/movement_sets.xml` 全量统计 |
| 用法组的 `<movement_set id>` 指整套移动组 | `item_usage_sets.xml` 19 处引用 × 对 `full_movement_sets.xml` 求交集 |
| `full_movement_sets` 有同名合并、`movement_sets` 没有 | 反编译 `TaleWorlds.MountAndBlade.Module.CreateProcessedModuleDataXMLForNative` |
| soln id 名 | `Modules/Native/ModuleData/project.mbproj` 第 41 / 42 行 |
| 走跑动作裸声明 / idle 带 `actt_idle` | `Modules/Native/ModuleData/action_types.xml` 全量统计 |
| 动作名 → clip 名映射 | `Modules/Native/ModuleData/action_sets.xml` |
| 追加层 7 根骨 | `Modules/Native/ModuleData/monsters.xml` human 块 |
| clip 元数据字段表 | 反编译编辑器 `TaleWorlds.Native.dll` 的 `AnimationClipInspector` |
| 女性动作集用 `base_set` 且覆盖走跑 | `Modules/Native/ModuleData/action_sets.xml` |

---

# 第三部分 · 阶段 1 执行版 —— 先用飞行动画把管线跑通

> 本节 = 原 `移动模式补充.plan.md` 全文并入（2026-09-20），内容一字未改。
> 定位：第二部分讲给铁炮换一整套持枪走跑；本节只做**第一步：把站着不动换成我们的飞行动画**。


> **状态：待实施**（2026-09-20 立）
> **这是什么**：**第二部分（主计划）**的分阶段执行版。第二部分讲"给铁炮换一整套持枪走跑"（10 clip + 6 处 XML）；本条只做**第一步：把"站着不动"换成我们的飞行动画**。
> **测试素材**：用**已重定向好的超人飞行动画**当占位，先把"TRF 导入 → 接线 → 实机生效"这条链跑通，再考虑补全整套走跑。

> **三个词先说清楚**：
> · **idle** = 角色站着不动时播的那条循环动画。本次要换的就是它。
> · **位置轨** = TRF 里存骨骼位移的那一段。**这条最容易翻车，见 §五·坑 1**。
> · **沙箱** = `TaikouAnim` 这个中转模块。Taikou 本身 ModKit 打不开，动画必须在沙箱里编译再搬过去。

---

### ★ 完整路径速查（下文相对路径的根都在这里）

| 代号 | 完整绝对路径 |
|---|---|
| **素材源工程** | `D:\BrainMaker\骑砍2动画重定向\` |
| **TRF 目录** | `D:\BrainMaker\骑砍2动画重定向\output\trf\` |
| **FBX 目录** | `D:\BrainMaker\骑砍2动画重定向\output\fbx\` |
| **MOD 根** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\` |
| **沙箱模块** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\TaikouAnim\` |
| **沙箱·源放置处**（要新建） | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\TaikouAnim\AssetSources\animations\fly\` |
| **沙箱·编译产物**（自动生成） | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\TaikouAnim\Assets\animations\fly\` |
| **Publish 目标** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\Publish\TaikouAnim\` |
| **内容包接线目录** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\Taikou\ModuleData\` |
| **内容包资产目录** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\Taikou\AssetPackages\` |
| **Native 参照** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\Native\ModuleData\` |
| **本文件** | `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\LivingWorldNpcs\plans\自定义移动管线.md` |
| **主计划** | 同文件 **第二部分**（原 `移动模式补充.md`） |

---

### 〇、结论速览

| 问题 | 答案 |
|---|---|
| 本阶段目标 | 掏出铁炮**站着不动**时，播我们的飞行待机（而不是原版弩的站姿） |
| 分几步 | **1A 先只验 clip 能播**（零风险）→ **1B 再替换站立** |
| 要自制动画吗 | ❌ **不用**。飞行动画已经重定向好了，**直接导入现成的 TRF** |
| 要写 C# 吗 | ❌ 零代码 |
| 1A 动几个文件 | **2 处追加**（`action_types.xml` + `action_sets.xml`） |
| 1B 动几个文件 | **1 处改行**（`item_usage_sets.xml` 的 `<idles>`）；不够再上移动集（3 处 + 2 新文件） |
| 影响谁 | **所有拿着铁炮的角色**（玩家 + 全部铁炮足轻）。数据层做不到只改玩家一个人 |
| 怎么回退 | 一行：`item_usage_sets.xml` 的 `<idle action=` 改回 `act_idle_crossbow_1` |

**为什么先做 idle**：idle 是 18 个槽里**最独立**的一个 —— 它不依赖方向切换、不依赖脚部 IK、不依赖走路速度。**整条链的任何一环断了，idle 会第一个暴露**，所以它是性价比最高的探针。

---

### 一、★ 素材在哪（你要导入的东西）

**全部已经重定向完成，不需要再跑 Blender。** 位置：

```
D:\BrainMaker\骑砍2动画重定向\output\trf\    ← ★ 最终交付物（要导入的就是这个）
D:\BrainMaker\骑砍2动画重定向\output\fbx\    ← 中间产物（ModKit 若要骨架，配它一起导）
```

#### 1.1 本阶段要用的两条

| 用途 | TRF（★ 导入这个） | 配套 FBX | 帧范围 | **ModKit 填值** | TRF 大小 |
|---|---|---|---|---|---|
| **主选 · idle** | `D:\BrainMaker\骑砍2动画重定向\output\trf\fly_A_Flight_Idle_A.trf` | `D:\BrainMaker\骑砍2动画重定向\output\fbx\fly_A_Flight_Idle_A.fbx` | 91 帧 `2..92` | **Source 1 = 2 · Source 2 = 92** | 111 KB |
| **备选 · 趴姿定格** | `D:\BrainMaker\骑砍2动画重定向\output\trf\fly_A_FM_A_Pose.trf` | `D:\BrainMaker\骑砍2动画重定向\output\fbx\fly_A_FM_A_Pose.fbx` | 2 帧 `2..3` | **Source 1 = 2 · Source 2 = 3** | 2.5 KB |

**选哪条**：

- **主选 `fly_A_Flight_Idle_A`** —— 91 帧，是**唯一真正的循环待机**（空中待机）。站着一动不动时观感自然。
- **备选 `fly_A_FM_A_Pose`** —— 2 帧的**趴姿定格**（水平趴着、双手前伸 = 最像"超人"的姿势）。优点是**一眼就能确认生效**；缺点是**不会动**（定格），站久了像卡住。**想"一眼看到超人"就用它。**

> 两条只是**换一行**的差别（clip 名 + Source 值）。建议主选跑通后，再拿备选做一次对照。

#### 1.2 完整池子（阶段 2 会用，先不动）

`D:\BrainMaker\骑砍2动画重定向\output\trf\` 下共 **130 条 `fly_*.trf`**，命名规则 `fly_A_<原资产名>`（`A` = 风格套系，B~E 是同族的其它风格）。真会动的只有这几族：

| 族 | 帧数 | 用途 |
|---|---|---|
| `fly_A_Flight_Idle_A~E` | 91 | 空中待机 ← **本阶段用它** |
| `fly_A_Flight_HoverMove_A~E` | 61 | 悬浮移动 |
| `fly_A_Flight_Hover_Start_A~E` | 46 | 悬浮起步（过渡动作，**骑砍2 没有它的位置**，见主计划讨论） |
| `fly_A_Flight_FastMove_A~E` | 31 | 快速飞行（★ 趴姿前飞） |
| `fly_A_Flight_Dodge_*` | 56 | 飞行闪避 |
| `fly_A_SuperheroLanding_*` | 61 | 落地 |
| `fly_A_FM_*_Pose` / `_Lean_*` | **2** | 静态姿态（趴姿 / 倾斜 / 升降） |
| `fly_A_Flight_HoverMove_*_*_Add` | **2** | 静态（方向叠加用） |

> ⚠️ **130 条里 80 条只有 2 帧**（静态姿态）。阶段 2 若拿它们填走路的 8 个方向槽，表现会是"定格滑行"。

---

### 二、导入（🔴 你在 ModKit 里做的部分）

**沙箱 = `Modules\TaikouAnim\`**（纯资产模块，运行时**不加载、不勾选**）。

#### 2.1 先把源文件放进去

```
Modules\TaikouAnim\
  ├─ AssetSources\animations\fly\fly_A_Flight_Idle_A.trf    ← ★ 新建 fly 子目录，TRF 拷这里
  └─ Assets\animations\fly\                                  ← 编辑器编译产物会落这里（自动生成）
```

> 现状：`AssetSources\animations\gun\sw2_gunner_p006_alig_abs.trf` —— 照着建 `fly\` 子目录即可。

#### 2.2 步骤（照《铁炮射击手感3C §〇》的配方）

| # | 做什么 | 关键点 |
|---|---|---|
| 1 | 跑 `TaikouAnim\to_editor_mode.bat`（切编辑器模式） | 🔴 `Assets\` 必须存在，否则 ModKit 看不到工程 |
| 2 | 开 ModKit，打开 `TaikouAnim` 模块 | 沙箱只依赖 Native，能正常打开 |
| 3 | 资源浏览器导入 TRF → 生成**骨骼动画** | 骨架应解析到 `human_skeleton` / 根名 `human_skeleton_notused` |
| 4 | 建 **Animation Clip** | clip 名 = **`flight_idle_a`**（🔴 2026-09-20 实测：ModKit 按 TRF 文件名自动给的，就是这个 —— 早期文档建议的 `fly_idle` 没用上，接线一律以 `flight_idle_a` 为准） |
| 5 | 🔴 填 `Source 1 = 2` / `Source 2 = 92`，`Duration > 0` | 不填 = 报 `pos ipo[2,92] does not fit sources 0.00 and 0.00`，clip 不保存 |
| 6 | 🔴 **元数据整组抄原版同类 clip** | 见 §2.3 |
| 7 | 编译 + Publish（目标指到**模块外**，如 `Modules\Publish\TaikouAnim`） | Publish 会**清空**目标目录，别指到 `Taikou\AssetPackages` |
| 8 | 产物 `pack0.tpac` **改名**拷进 `Taikou\AssetPackages\lwn_taikou_anim.tpac` | 🔴 见 §2.4 |

#### 2.3 🔴 clip 元数据抄谁（裸导 = 只有 Source 1/2，会"能播但流程不认"）

本次是**移动集/站姿**的 idle，不是装填，所以要抄的是**原版站姿**那两条：

| 我们抄的来源 | 原版 action | 原版 clip | 说明 |
|---|---|---|---|
| **主选参照** | `act_idle_crossbow_1` | **`troop_stand_crossbow_1`** | 用法组 `<idles>` 的站姿（带 `type="actt_idle"`） |
| 备选参照 | `act_walk_idle_crossbow` | **`stand_crossbow`** | 移动集的 idle 槽（**裸声明**，不带 type） |

**做法**：在 ModKit 里打开原版那条 clip，把它的元数据**整组**抄到我们的 clip 上（`Blend in / out period`、`Priority`、`Loading Type` 等）。装填那轮的教训：**裸导只填 Source 1/2 是不够的**。

> `Continue to action` / `Param 2` 是**装填两段式专用**（链到收尾段 + 弹药进膛时点）。**idle 是单段循环，不需要这两项。**

#### 2.4 🔴 别覆盖掉 `gun_reload`

`Taikou\AssetPackages\lwn_taikou_anim.tpac`（19 KB）里**已经有装填动画 `gun_reload`**，是实机验证过的资产。

**正确做法**：把飞行动画**加进同一个 TaikouAnim 工程**（和 `gun` 目录并列），一起 Publish ⇒ 产出的 `pack0.tpac` **同时含 gun_reload + flight_idle_a**，再改名覆盖过去。
**错误做法**：另起一个干净工程只编飞行动画 ⇒ 覆盖后 **gun_reload 消失，铁炮装填回退成弩动作**。

---

### 三、接线（XML，全在 `Modules\Taikou\ModuleData\`）

#### 3.1 阶段 1A · 先只验 clip（2 处追加，零新文件）

**① `action_types.xml` 追加**（🔴 用法组 `<idles>` 的动作**必须带 `type="actt_idle"`**）：

```xml
	<action name="act_fly_idle" type="actt_idle" />
```

**② `action_sets.xml` 追加**（在现有 `as_human_warrior` 块里）：

```xml
		<action type="act_fly_idle" animation="flight_idle_a" />
```

> 🔴 `animation=` 填的是 **ModKit 里建的那个 clip 名**（实测 = `flight_idle_a`），不是 TRF 文件名、不是 FBX take 名。**写错 = 静默解析成 `act_none`，没有任何报错，只是播不出来。**
>
> ✅ **2026-09-20 已落地**：`Taikou/ModuleData/action_types.xml` + `action_sets.xml` 两处已追加，
> 资产已装进 `Taikou/AssetPackages/lwn_taikou_anim.tpac`（`tpaccli list` 实测含 `flight_idle_a` + `gun_reload` 两条）。

**这两处不用碰 `project.mbproj`** —— `soln_action_types` / `soln_action_sets` **已经挂好了**（现有 7 行 soln 里的第 5、6 行）。

**验**：实机控制台 `custom.do_anim act_fly_idle` → 应看到飞行动画。**这一步过了 = TRF 导入成功 + 接线生效，整条链的地基就通了。**

#### 3.2 阶段 1B · 真正替换"站着不动"（🟢 推荐：只改 1 行）

**③ `item_usage_sets.xml` 改 1 行** —— 把 `tk_firearm` 的 `<idles>` 从原版指着我们的：

```xml
		<idles>
			<idle
				action="act_fly_idle"
				is_left_stance="False"
				require_free_left_hand="False" />
		</idles>
```

（原为 `action="act_idle_crossbow_1"`。回退 = 改回它。）

**为什么这条最省**：`soln_item_usage_sets` **已经在 mbproj 里挂着了**（第 7 行，当初的"探针"），而且**装填动画已经证明这条链是通的**（`reload_action` 走的就是同一个文件）。**零新文件、零新 soln 行。**

**验**：实机掏出铁炮站着不动 → 看站姿是否变了。

#### 3.3 阶段 1B 备选 · 若站着没变，再上移动集（3 处 + 2 新文件）

主计划 §八·1 的未决项：**"站着不动"到底由用法组的 `<idles>` 管，还是由移动集的 `idle` 槽管，未验证。**
若 §3.2 改完站姿没变 ⇒ 是移动集那条在管，按下面走（**这就是主计划的 6 步，本阶段只填 idle 一个槽**）：

| # | 文件 | 动作 |
|---|---|---|
| 1 | `movement_sets.xml` | **新建**：`<movement_set id="flytest_gun_walk" idle="act_fly_idle" …其余 17 槽逐字照抄 walk_crossbow…>` |
| 2 | `full_movement_sets.xml` | **新建**：`<full_movement_set id="flytest_gun">`，`walking` 指 `flytest_gun_walk`，其余 5 档指向原版 |
| 3 | `project.mbproj` | **挂 2 行**：`soln_movement_sets` / `soln_full_movement_sets`（id 名照抄 Native 第 41/42 行） |
| 4 | `item_usage_sets.xml` | 把 `<movement_set id="crossbow" />` 改成 `<movement_set id="flytest_gun" />` |

> 若同时做了 §3.2 和 §3.3，两条都指向 `act_fly_idle`，不冲突；若只生效一条，把没生效的那条回退即可（各一行）。

---

### 四、验证清单

| # | 验什么 | 怎么验 |
|---|---|---|
| 1 | **clip 能播** | `custom.do_anim act_fly_idle`（1A 的关卡） |
| 2 | **接线没写成 act_none** | 若第 1 条报了 `act_none` ⇒ `action_types` 名字或 `action_sets` 的 `animation=` 写错 |
| 3 | 🔴 **站着不动变了** | 实机掏铁炮站住，对比改动前（1B 的关卡） |
| 4 | 🔴 **人有没有被整体抬高 / 下沉** | **见 §五·坑 1** —— 这是本阶段**最可能翻车**的一项 |
| 5 | **不是僵住 / 卡住** | 用主选 `fly_A_Flight_Idle_A`（91 帧循环）时应能自然呼吸；若用 `FM_A_Pose` 则是定格（预期） |
| 6 | 🔴 **负面测试：空手 / 拿刀不受影响** | 空手站着、拿刀站着，应与原版完全一致（它们走 `unarmed` / `1h` 那套） |
| 7 | 🔴 **负面测试：装填没被带坏** | 掏铁炮装填，仍播 `gun_reload`（回归测试） |

---

### 五、🔴 三个必须提前知道的坑

#### 坑 1：TRF 的"位置轨语义"在两份文档里是**冲突的**（本阶段最可能翻车的一项）

我实测了三条 TRF 的位置轨首帧：

| 文件 | 位置轨首帧 | 语义 |
|---|---|---|
| `H:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\Modules\TaikouAnim\AssetSources\animations\gun\sw2_gunner_p006_alig_abs.trf`（**装填用，实机验证通过**） | `(0, 0, 0)` | **纯增量** ✅ |
| `骑砍2动画重定向\output\trf\sw2_gunner_p006_alig_abs.trf` | `(0, 0.0202, 0.8600)` | 绝对骨盆高度 ❌ |
| `骑砍2动画重定向\output\trf\fly_A_Flight_Idle_A.trf`（**本次要导的**） | `(0, 0.0202, **0.9145**)` | **绝对骨盆高度** ⚠️ |

**冲突**：

- 《铁炮射击手感3C §〇》坑 #2 + `plans/rules/wheels.d/assets.md` §15.3（**实机验证通过**）说：
  > 位置轨必须写**增量**（`rest_rot @ location`，**不叠加** `rest.translation`）。**判据：首帧应 ≈ 0（不是 0.9）。**写绝对 → **人整体抬高（实测 ≈ 6cm）**。
- 《骑砍2动画重定向 README §6 硬约束 5》+ `docs/TRF规范.md` 说：
  > TRF 存**绝对局部变换**，`CHECK_POS` 首帧应 ≈ **0.860**（≈0.003 才是错误）。

**谁对**：**已实机验证的那个（≈0）证据更强**——装填动画就是用它跑通的。所以**本次要导的飞行 TRF 用的是"有争议的那一版"**。

**实际影响**：按 §15.3 的量级，误差表现为**抬高约 6cm**——对视觉**几乎看不出**。
**所以：先照原样导入，专门看验证项 #4。**
**若确实抬高了** ⇒ 按 **附件 A** 把位置轨改成 ≈0（这几条飞行 clip 的位置轨是**常量**，改起来是一行命令）。

> 📌 这条同时是**主计划的一笔技术债**：`骑砍2动画重定向\pipeline\common\fbx_to_trf.py` 用的是绝对语义，与项目已验证的规则相反。**建议单独开一项确认，并把结论回填进 README。**（本次不处理，只记录。）

#### 坑 2：`<idles>` 与移动集 idle，**只有一个在管**

见 §3.2 / §3.3。判据 = 改完哪条生效。**别两条一起改完就收工** —— 那就分不清是哪条起的作用，下次复用会踩空。

#### 坑 3：别忘了从编辑器模式切回游戏模式

跑游戏前必须跑 `TaikouAnim\to_game_mode.bat`（把 `Assets\` 改名）。
模块里 `Assets\` 还在 ⇒ 引擎按 `Assets → AssetPackages` **取第一个存在的** ⇒ **整个 `AssetPackages\` 被绕过**，实机崩或资产不生效。

---

### 六、回退（全部一行）

| 改动 | 回退 |
|---|---|
| §3.2 用法组 `<idles>` | `action="act_fly_idle"` → 改回 `action="act_idle_crossbow_1"` |
| §3.3 移动集路线 | `item_usage_sets.xml` 的 `<movement_set id="flytest_gun" />` → 改回 `id="crossbow"` |
| §3.1 接线 | 删掉 `action_types` / `action_sets` 里那两行即可（文件留着不生效，无害） |
| 素材 | `Taikou\AssetPackages\lwn_taikou_anim.tpac` 回滚到备份（**导入前先备份**） |

---

### 七、未决 / 风险

| # | 事项 | 影响 |
|---|---|---|
| 1 | 位置轨语义（坑 1） | 抬高 ≈6cm；需实机确认 |
| 2 | `<idles>` vs 移动集 idle 谁生效（坑 2） | 决定 1B 走哪条；一次实验即可定性 |
| 3 | 女角色拿不拿得到（`as_human_female_warrior` 覆盖了走跑全套） | 拿不到 = 女性铁炮兵还是原版站姿；要一致就得在那个动作集里再绑一份 |
| 4 | ~~运行时不勾选 `TaikouAnim` 沙箱~~ | ✅ **2026-09-20 实查：勾着也无害** —— 沙箱的 `SubModule.xml` 是空 `<Xmls/>`、`project.mbproj` 里**一条 soln 都没挂**，所以它的 `ModuleData/*.xml` **从不解析**（文件头也自己写明了）。原判断过谨慎，已更正。<br>⚠️ 但**跑游戏前建议仍把沙箱切回游戏模式**（`to_game_mode.bat`）：它的 `Assets/` 里是同一套动画的编译中间产物，留着有同名资产的风险 |
| 5 | 引擎是否吃动画里的骨盆高度 | **飞行测试的核心未知**：视觉能不能"飘"起来，取决于此。见主计划讨论 |

---

### 附件 A：位置轨改成增量（仅当验证项 #4 发现"人被抬高"时用）

这几条飞行 clip 的位置轨是**常量**（首帧 == 末帧，实测都是 `… 0.020170 0.914520`），说明源动画**严格原地** ⇒ 增量应为 `(0, 0, 0)`。
换算规则：位置块每帧的 `x y z` **全置 0**，帧号保留。

```bash
cd "D:/BrainMaker/骑砍2动画重定向/output/trf"
cp fly_A_Flight_Idle_A.trf fly_A_Flight_Idle_A.trf.bak          # 先备份
awk '
NR==4 { nb=$1; b=0; s="bone"; print; next }
NR<4  { print; next }
s=="bone" { print; if (b>=nb) { pc=$1; pn=0; s="pos"; next } rl=$1; b++; s="rot"; next }
s=="rot"  { print; rl--; if (rl==0) s="bone"; next }
s=="pos"  { pn++; printf "%s 0.000000 0.000000 0.000000\n",$1; if (pn==pc) s="done"; next }
s=="done" { print }
' fly_A_Flight_Idle_A.trf.bak > fly_A_Flight_Idle_A.trf
```

**判据**：改完后用同一套逻辑反查，`POS_FIRST` / `POS_LAST` 都应是 `0.000000 0.000000 0.000000`；且**旋转块一字未动**（改前改后 diff 只应出现在位置块）。
改完再走 §2.2 导入（Source 1 / Source 2 不变，仍是 `2 / 92`）。

---

### 附：本文结论的取证来源

| 结论 | 出处 |
|---|---|
| 素材路径与帧范围 | `output/trf/*.trf` 实测（脚本读 bone0 帧数 + 位置块） |
| 位置轨首帧实测值 | 同上（awk 解析文件） |
| 位置轨必须 ≈0 | `plans/rules/wheels.d/assets.md` §15.3（实机验证通过，2026-09-19） |
| 位置轨应 ≈0.86（冲突说） | `骑砍2动画重定向/README.md` §6 硬约束 5 · `docs/TRF规范.md` §1 |
| 装填用哪条 TRF（≈0 那份） | `TaikouAnim/AssetSources/animations/gun/` md5 `b78076e3…` |
| 导入五环 + 元数据必抄 | `plans/铁炮射击手感3C-实施计划.md` §〇 · `plans/rules/wheels.d/assets.md` §十五 |
| 沙箱工作流 | `Knowledge/资产中转沙箱模块工作流.md` |
| 移动集 18 槽 / 103 条全满 | `Native/ModuleData/movement_sets.xml` 全量统计 |
| `act_idle_crossbow_1` 带 `actt_idle` / `act_walk_idle_crossbow` 裸声明 | `Native/ModuleData/action_types.xml` |
| `act_idle_crossbow_1 → troop_stand_crossbow_1` | `Native/ModuleData/action_sets.xml:4904` |
| `act_walk_idle_crossbow → stand_crossbow` | `Native/ModuleData/action_sets.xml:2692` |
| soln 行已挂好 | `Taikou/ModuleData/project.mbproj` 现存 7 行 |
