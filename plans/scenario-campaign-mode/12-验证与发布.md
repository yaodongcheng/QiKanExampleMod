# 12 — 验证与发布

> 阶段：全周期 ｜ 依赖：各阶段出口 ｜ 完成后：每阶段验收清单全绿 + 发布流程明确
>
> **审核状态：⏳ 未审核（禁止实施）** ｜ 审核人：用户 ｜ 审核日期：— ｜ 审核意见：—
> 通过后改为 ✅ 已审核（日期）+ 记录意见。

## 这一步做什么（一句话）

**怎么证明每一步做完了**——每个阶段有一张可操作的验收清单（进游戏能看到的、控制台能验证的）；外加贯穿始终的红线检查（代码里搜禁词/翻译表检查等）和发布流程。

## 各阶段验收清单

### Phase 0 骨架（01）
1. 新档 → 控制台开始演示剧本 → 手动改条件（设旗标/宣战）→ 事件触发 → 效果生效（能观察到宣战/停战/换城）→ 日志齐全
2. 存档 → 读档 → 进度保持
3. 坏剧本文件 / 找不到角色 / 战斗中触发 → 都不崩

### Phase 1 机制（02-06）
1. 事件强制触发带"锁军" → 部队集结成军团、只朝目标移动 → 到达 → 下一事件条件命中 → 释放后 AI 恢复
2. 背景剧本：玩家不动 → 世界按表走；玩家干预 → 引擎让路
3. 主命：节拍派发 → 任务面板可见 → 接/拒双路径（详见 13 验收）
4. **演示战斗**（原版场景）：程序化营寨（帐篷圈 + 篝火 + 主将中央 + 沉睡）→ 击杀主将 → 敌军溃散 → 胜利 → 战果旗标
5. 演出：样例剧本编译产物 dump 完整 → 播放无现场计算；多人对话轮转 + 旁听者；旧崩溃链已断
6. 开局：剧本下拉 → 世界拨到 1560 → 玩家是信长；缺失角色补造；玩家死亡换继承者
7. 没配 AI 全流程可玩

### Phase 2 战国数据 + 桶狭间（07-09）
1. 织丰战役可启动（我们的壳，不用织丰 DLL）；40 势力/848 城/兵种装备正常
2. 素材表落地 → 程序化营寨用织丰素材
3. 织丰战役新档 → 选桶狭间 → 全链走通（见 09 验收）
4. 存读档全程保持；角色 ID 两轮查找日志正常

### Phase 3 本能寺 + 转化管线（08-10）
1. 时间自然流：1560 桶狭间 → 1562 清洲同盟 → 1567 岐阜 → 1568 上洛…→ 1582 本能寺章节自然到来（事件按年份+条件触发，无跳跃）
2. 分阶段巷战 + 大火氛围 + 史实/IF 双结局 + 换扮演
3. 转化管线：事件子集 → 剧本/人物表/城池映射/编译产物/翻译 全链路产物，格式校验通过

## 红线检查（贯穿始终，每条都有对应命令）

```bash
# 1. 本 mod 代码零战国字串（数据包除外）
grep -ri "Shokuho\|织丰\|日本战国\|太阁\|主公" --include="*.cs" ExampleModVS/
# → LWN 核心零命中

# 2. 玩家可见文本必须走翻译表
# → 人工抽查（code review）：InformationManager 调用都带翻译表

# 3. 存档字段同步清理
grep -n "lwn_scenario" ExampleModVS/ExampleMod/ExampleMod/Core/MyBehavior.cs
# → 存档写入 + 新档清理两处都在

# 4. 旧崩溃链已断、确认弹窗不走新面板关闭接口
grep -rn "confirmFight" --include="*.cs" ExampleModVS/ | grep -i "vm.Close"
# → 零命中
```

## 没配 AI 也能玩（降级矩阵）

| 功能 | 有 AI | 没 AI |
|---|---|---|
| 剧情对话 | 预写文本 + AI 润色 | 预写文本直出 |
| 私信聊天 | AI 回复 | 模板回复 |
| 世界背景 | AI 生成 | 整段省略 |
| 演出 | 编译产物 + AI 扩展 | 编译产物兜底文本 |
| 主命/任务 | 正常 | 正常 |

## 版本兼容

- 官方开局选择器（06）：1.5.1 专属，老版本用设置菜单代替
- 开战斗场景（03）：1.5.1 新管道，老版本另一套（版本宏隔离）
- 指挥部队接口（02）：1.3.0 起签名变过 → 统一走版本兼容门面
- 织丰（07）：1.5.1 主方案；1.2.12 回退路线

## 发布流程

1. **机制层**（Phase 0/1）在 LWN 主仓库开发（卡拉迪亚演示验证）→ 独立发布（普通玩家不受影响——剧本模式默认关）
2. **数据包**（Phase 2/3）TaikouContent 独立工程发布（依赖 LWN + 织丰）
3. 🔴 **发布前跑 validator 自检查**（01「自检查脚本」）：事件脚本全部内容有正确定义（语法/注册表/引用 ID/本地化/资源引用/事件间引用），错误 = 阻断发布
4. 发布前用 Release 编译（版本自动检测本机游戏版本）；最终测试用 VS2022 手动编译的 DLL（项目约定）
5. 织丰资源安装：发布说明写明（解压织丰包或使用 1.2.12 客户端）

## 交付物清单（工程完成时）

- [ ] LWN 核心 Scenario/ 全套（01-06 系统）
- [ ] 卡拉迪亚演示剧本（机制验证）
- [ ] TaikouContent 数据包（07-10：克隆注册层/素材表/桶狭间/本能寺）
- [ ] 转化工具管线（08）
- [ ] 素材表 + 本地化文件
- [ ] 本文档验收清单全绿
