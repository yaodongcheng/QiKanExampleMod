# 00 — 开工评审（v3）：执行层核心 = 字段生命周期 + 方法全量实现

> 🔴 **v3（2026-08-30）**：JSON 设计（08b v6）已完成、数据层验收已过。下一步难点 = **在游戏里解析并执行 JSON**，按用户裁定拆成两件核心开工：①**字段**（剧本能问/能写的所有状态）——开局怎么初始化、随翻译总表的命令/函数怎么变、随原版真实世界状态怎么变；②**方法**（命令 + 函数 + **演绎演出**）——每条在骑砍2 里完整实现并保证真的能跑；**演绎演出（角色入场/退场/走位/相机）经实测 = 尚未注入 JSON，属方法的一部分，本轮一并实现**（实测与设计口径见 §0.5）。其余（场景类 trigger、02/03/13/17 真实现、07 数据包内容、05 体验大件）后置，清单见下。
> 依据：16a CSV（域 42 / 属性 217 / 命令 154 / 函数 10 / 域值 573 / 枚举值 453 / 语法 20 / 文本变量 12）+ 16b 逐条四档裁定（含「谁来写、写去哪、玩家看到什么」）+ 01（DSL/调度权威）+ 16（Ctx 三档/函数注册） + 11（存档键登记表已定）+ 06（时代重置链）。
> 产物实测（story_event_json/okehazama/）：events.jsonc = 39 perform / 436 effect / 102 if 步骤；story/ = 39 段（scene 15 / map_dialogue 3 / menu_dialogue 21）。
>
> **🔴 实施进度（2026-08-30，本会话）**：
> - ✅ **演绎层注入（stage_inject v2）已实施并验收**——显式 `actor_enter` 64 条 + `actor_leave` 54 条注入 15 个 scene 段（present 开场入场 / 首次发言前入场 / 段末收场；menu/map 24 段按 05 纪律 7 正确跳过）；39 段 JSON 合法、15 段「先入场后说话」0 问题、`verify_tk5_coverage` 重跑全绿（§0.5 实测行已按此更新）
> - ✅ **用户裁定（2026-08-30）**：演绎演出 = 方法的一部分；**scene 形态多 agent 对白 v1 UI = StoryDialogVM 升级版（05 大件，先做）→ 🔴 2026-08-30 再修正：StoryDialogVM 最后做**（「无非是显示立绘和谁在说话」），字段与方法（W1-W4 + 演绎非 UI 部分）先行——W5 完成标志按此更新
> - ✅ **W1 数据底座已实施**（见 T1 行）——Scenario/ 5 文件 + GlobalVariableBehavior 升级（键名对齐 11：lwn_scn_attr/lwn_scn_state、上限 500→10 万 + 淘汰改告警、全局状态读写、ClearAll 挂新档）；MySubModule/MyBehavior 注册与清档；csproj 登记。**验证**：dotnet build 0 错误；离线预演 22 事件 trigger/facility/step 对照注册表 **0 表外**
> - ✅ **W2 求值器已实施**——DslExpression（递归下降 Parser + AST + EvaluateDetailed 三档报告）/ AttributeResolver（T1 直读 + T2 换算 + T3 仓读 + 链尾空值 + 函数 15）/ ScenarioClock（CampaignTime 属性 API：GetYear/GetDayOfYear，30 天/月推算月）/ ScenarioContext（Ctx 槽）/ ScenarioDataPack（区域表+RankLadder+换算系数，反向索引 province）；新指令 custom.dsl_eval / custom.dsl_validate。**验证**：dotnet build 0 错误；离线预演 22 事件 condition = 函数 8 种（全实现）/域 11 种（全支持；预演抓出 Army::/superior/province 三缺 → 当场补齐）/属性 13 种全支持；诚实的已知降级 = identity 带序（缺 RankLadder 表 → 不可判定，17 接后恢复）、isAllied 非交战近似（引擎 API 待核实）、canMove 恒真（02 未接入）
> - ⏳ 其余（W3 初始化、W4 执行器、W5 运行侧、W6/W7）未动工——TODO 见文末「§3.5」

---

## §0.5 演绎演出注入实测（2026-08-30，用户点名核查）

| 字段 | 实测（39 段） | 谁负责（05 设计口径） | 状态 |
|---|---|---|---|
| `form`（scene/map/menu 三形态） | 15 / 3 / 21 | stage_inject（08b §四） | ✅ 已注入 |
| `actors[]`（heroId/agentId + slot 槽位） | scene 15 段全有；menu/map 按 05 纪律 7 正确无 | stage_inject | ✅ 已注入 |
| `present`（开场即在场/沉默观众位） | 3 处（Hero::MainHero×2、Ctx::hero_c） | stage_inject | ✅ 已注入 |
| `actor_enter` / `actor_move` / `actor_leave` / `camera` / `actor_action` | **39 段全 0——未注入** | 非生成器欠账（阶段设计）：①首次发言自动入场 + present 转化开场入场 = **编译器排演兜底**（05 纪律 6① / 05:163「编译期推断一次、运行期显式执行」）②中途入场/走位/提前离场 = 源格式显式指令（05 纪律 6③——TK5 源无走位信息，属 08b §七 agent 审核的演绎补写）③camera 默认中景跟说话人、不必每行写（05:351）；显式 camera/actor_action = 导演语言，同属 audit 补写 | ✅ **生成端已注入（stage_inject v2 完成，2026-08-30）**：enter 64 / leave 54 落 scene 15 段；运行时侧（编译器/播放器/相机/StoryDialogVM 升级）= W5 待做 |

**结论**：生成端（stage_inject）已把机械能推导的（actors/present/slot/form）注入完；未注入的 enter/leave/move/camera = **05 编译器 + 播放器的职责，且必须本轮做**——否则 39 个演绎分件没人演、演出类 23 条命令没执行者。显式 camera/actor_action 的**具体值**（哪个节点特写/谁拔刀）保留为 08b §七 agent 审核的演绎补写，缺失时执行器走默认（对话中景跟说话人、无动作、显式入场在段首）。

---

## 〇、结论先行（决定清单）

1. **决定：开工内容 = 两件核心——「字段的完整生命周期」+「方法（命令 154 + 函数）逐条实现并验证」。** 理由：跑一个事件 = 读字段（条件）、写字段（效果）、执行命令（世界变化）；这两件就是「解析并执行 JSON」的全部实质。
2. **决定：字段 = 三载体三仓，读链一条固定顺序。** 属性/域值/枚举/槽只落三处：①引擎原生（T1 直读 / T2 换算）②外置属性仓 `lwn_scn_attr` ③旗标计数仓 `lwn_scn_state`（Flag/计数器/Variable/GlobalSlot）；另有数据包只读表（定义，不进存档）。读取顺序固定 = 原生适配器 → 外置仓 → 空值；任何一步失败返回空值，**永不抛异常**（铁律 1/2）。
3. **决定：字段不镜像**（§1.4）——T1/T2 字段读引擎（换算），只有 T3 才进仓；**T1 禁止镜像进仓**（镜像 = 漂移源，两边不同步必错一个）。
4. **决定：方法 = 剧本命令 + 函数 + 演绎演出，按五组批次执行，但每条至少桩级一次到位**（§2.1）：A 机制族（代入 83 + 容器 8 + 状态动作家族）与 B 引擎直取（16b §5.2 全部 16 条 + im_message + 独立 T2 3 条）**本轮完整实现**；**C 演绎演出类（23 条：台词/选择/BGM/SE/图片/背景/换场/设施进出）本轮完整实现**（用户裁定：演绎演出是方法的一部分——编译器 + 播放器三形态 v1 + 舞台镜头指令，§0.5 实测断链需补）；D 依赖系统 9 条（军团/决斗/主命/会议）参数入仓 + 动作桩 + `[Scenario][TODO]` 日志，随 02/03/13/17 各自开工接真；E 特殊档 11 条（T3 新造 4 + T0 降级 3 + T3-预留 4）**本轮完整实现**（不依赖其他系统）。
5. **决定：函数 15 个实现点本轮全部进求值器**（§2.2）——引擎直取的（exists/atWar/isAllied）直接实现；需数据包/仓的（hasCard/allControlled/region_attr_1/isNeighbor 等）带空数据包实现（能算、无数据返回 false + 日志）；canPromote 桩（依赖 17）；unknown_2/8 = 恒假 + 生成期告警（16b 已裁定，无需再判）。
6. **决定：验证 = 控制台指令驱动（12 分拆纪律）**——每字段、每命令、每函数、每演绎指令都有 custom.* 对应；验收基线 = 桶狭间 22 事件全部「加载无错 + 条件可求值 + effect/if 步骤可执行」，选 1 个纯结算事件 + 1 个 scene 形态演绎段端到端跑通（触发→求值→执行→演出→存读档）。
7. **决定：实现位置**——LWN 核心新增 `Scenario/` 子系统（路径 `ExampleModVS/ExampleMod/ExampleMod/Scenario/`，命名空间 `LivingWorldNpcs.Scenario.*`，英文 token、零日战字串）；**旧 StoryEngine/CommandManager（中文 Cmd）管线本轮不碰**（归 05 链路 A 的地盘）。数据（区域表/卡表/身份链/换算系数）走补充包数据包，内容后置 07，机制先跑空表。

---

## 一、字段：读什么、存谁、怎么生老病死

### 1.1 总账与三载体（字段从哪来）

| 类别 | 行数（16b 裁定） | 载体 | 备注 |
|---|---|---|---|
| 属性 | 217 = T1 23 / T2 15 / T3 175 / T0 4 | 引擎原生 / 外置仓 | T2 量纲换算表 = 16b §4.4；T0 4 条 = 解析碎片只留注释 |
| 域值 | 573 | 外置仓 / 数据包 | 旗标 172 + 计数器 85 + 主命 85 + 数据包资源 253 等 |
| 枚举值 | 453 | 数据包资源 / 字面量 | 239 条数据包资源（卡/物品/CG/BGM/设施），其余 = 英文 token 字面量；带序枚举（身份/官位）序 = 17 RankLadder 数据包，**禁止按 CSV 行序**（16 §四） |
| 代入槽 | Ctx 命名槽 / Variable:: / GlobalSlot:: | Ctx（事件内）/ lwn_scn_state | 16 §一 三档作用域：事件内 / 持久数值 / 持久对象引用 |
| 域 | 42 | 引擎 + 域查询器 | 组织 3 域 = T3-预留（存得下读得出，无行为） |

**三仓定义**（16b §三 + 11 存档键登记表——四种数据结构均已注册进 `Story/StoryContext.cs` SaveDefiner，**本轮零新增存档结构**）：

| 仓 | 存什么 | 存档键 | C# 类型 |
|---|---|---|---|
| 外置属性仓 `ScenarioAttrStore` | 对象上骑砍2 没有的字段（功勋/技能/标志位/组织字段…），逐实体 key = `域:StringId`（铁律 20，禁显示名） | `lwn_scn_attr` | `Dictionary<string, Dictionary<string, string>>` |
| 旗标计数仓 `ScenarioStateStore` | Flag:: / Time::counter_N / Variable:: / GlobalSlot:: + 剧本进度段 | `lwn_scn_state` | `Dictionary<string, string>` |
| 数据包 `ScenarioDataPack` | 定义（区域表/卡表/身份链/官职链/换算系数/CG/BGM/设施） | 不进存档 | 加载期只读表 |
| （13 / 17 各自存档段） | 主命 / 功勋官职 | 13 / 17 | 见各 plan |

**关键纪律**：值只四型（数字/布尔/字符串/对象引用，引用存 `域:StringId` 字符串不存实例）；稀疏（只存非默认格）；孤儿键（对象不在 → 读空值**不删行**，对象回来状态自动接上，清理只走控制台）；16b §4.5 塌缩拆分**一位一字段**（flag_appeared / away_flag / flag_confined / flag_sortie / flag_executed / flag_sick / flag_missing、kokudaka_base vs kokudaka…）——resolver 与仓 key 全部按拆分后侧名注册，禁止再合并。

### 1.2 开局初始化（字段的出生）

| 谁来初始化 | 干什么 | 时机 |
|---|---|---|
| **ScenarioInit**（从 06 步骤 2 时代重置抽出，只做字段种子，不做 06 的选择器 UI） | ①人物池三态（活着/未出生/已死——改年龄/生死/技能，顺序：先年龄后技能）②**数据包 125 列默认值灌入外置仓**（五维/技能/功勋一开局就是真值，不是 0——16b §3.1「默认值来源」）③T1/T2 字段**不初始化**（引擎是事实源） | 新档一次，挂 `CampaignEvents.OnNewGameCreatedEvent` |
| **Ctx 事件上下文** | 触发时自动赋值 event_settlement（發生據點）/ event_hero（發生人物）；命名槽按需 | 每次事件触发时；结束清理 |
| 数据包对象种子 | 区域/卡/物品/组织定义加载进只读表（内容 = 07/补充包，本轮空表先跑机制） | 剧本加载时 |

**新档清仓红线**（11 已立）：`ScenarioAttrStore.ClearAll()` + `ScenarioStateStore.ClearAll()` 挂新档初始化（与 ResetAllCampaignState 同族，每加一行数据必须同步——存档纪律）。

### 1.3 随命令/函数变化（字段的成长——写入口）

| 命令（16a 侧名） | 落到哪 | 类型检查 |
|---|---|---|
| `update`（TK5 更新） | `set_attr(对象, 字段, 值)` —— **唯一写入口** | 加载期按 16a「值类型」列比对，不符 = 加载报错（不是运行期静默） |
| `set_flag` / `clear_flag` | 旗标仓，布尔 | — |
| `set_variable` / `global_set` | lwn_scn_state（持久数值 / 持久对象引用） | 值类型按用途（数字字符串 / 引用） |
| `assign_ctx`（代入 83 条） | Ctx 三档（事件内 → Ctx；跨事件数值 → Variable；跨事件引用 → GlobalSlot——08 b 纪律 4 v6 注的转作用域判定） | 槽类型 == 源值类型 |
| `container_*`（容器 8 条） | 运行期内存集合（容器域）+ 写回 Ctx 槽 | 元素类型 = 域类型（16 §四 对象子类型） |
| 计数器 | `Time::counter_N` 每日 +1（挂 Campaign 层日 tick，**不挂 OnApplicationTick**）；counter_reset 归零 | 数字 |
| `snapshot_push` / `snapshot_pop`（暫存人物屬性/所有變量） | 外置仓子集/全部压栈存 lwn_scn_state，pop 恢复 | — |
| 技能投影 | 仓 → 引擎技能，**单向**（仓是权威；骑砍2值 = 太阁值 × 3，系数进数据包禁硬编码） | — |

**资源归口**（铁律 4）：所持金变更 / 一切金额走 `AgentControlHelper`；业务层禁止裸调 `ChangeHeroGold`。

### 1.4 随原版真实世界状态变化（字段与世界的同步）

| 档 | 读 | 写 | 世界变了会怎样 |
|---|---|---|---|
| T1（23） | 直读引擎（hero.Clan / settlement.OwnerClan / hero.Age / AtWar…） | 引擎写 | **自动同步**——引擎是谁就是谁；纪律：**禁止镜像进仓**（镜像 = 漂移源） |
| T2（15） | 按量纲换算表（16b §4.4：石高 ↔ `town.Prosperity`、士气 ↔ `town.Loyalty`/部队 Morale、防御 = 墙级 x60% + 墙血 x40%…） | 换算式写回（忠诚度走 ChangeRelationAction 线性 0~100↔±100） | 同步 = 换算；「只读」的（支配力 / 所在地方）剧本写 = **报错**（防篡改） |
| T3（175） | 外置仓（唯一权威） | set_attr | **世界侧只靠单向投影**（技能→引擎技能 / 训练度→驻军升级 / 五维→战斗）；世界侧进展（晋升/战果/官职/意图）由 **16b 实现锚点列点名的系统**（02/03/13/17/数据包）写回仓——本轮把读链 + 写入口 + 投影落地，各系统的真写回随 D 组命令各自接真 |

**例外（自动变化的 T3）**：`Time::counter_N` 每日 +1（世界时间驱动）；`Event::<id>.done` 调度器自动记。

**求值兜底**（01 安全兜底）：引用求值失败（对象缺失/查不到）→ null → 参与比较一律假，记日志；未知域/属性/函数/语法不合法 → **加载报错并禁用剧本**（不是静默 false）。

### 1.5 实现件清单（字段侧）与验收

| # | 实现件 | 完成标志 |
|---|---|---|
| F1 | **ScenarioDataPack** 加载器（区域/卡/物品/身份链 RankLadder/官职链/换算系数——空表先跑） | 加载无错；缺文件 = 日志 + 禁用剧本，不崩 |
| F2 | **ScenarioAttrStore**（lwn_scn_attr 读写 + ClearAll + set_attr/get_attr） | `custom.scn_status` 全量 dump；存读档不丢；新档干净 |
| F3 | **ScenarioStateStore**（lwn_scn_state 读写 + 计数每日 +1 + 读档重建） | 计数器每日涨；存读档不丢 |
| F4 | **AttributeResolver**（T1 原生适配器 + T2 换算器 + T3 仓读 + 枚举带序解析（身份/官位查 RankLadder）+ 链尾空值兜底 + 类型系统） | `custom.dsl_eval` 30 条抽样（每档/每值类型/带序/孤儿键各若干）全部按预期；未注册 = 不可判定 + 日志 |
| F5 | **ScenarioInit**（三态 + 数据包默认值 seed，幂等，挂 OnNewGameCreatedEvent） | 新档：义元活 / 光秀尚幼 / 默认值在仓；同档重跑不重复灌（幂等） |

## 二、方法：命令 154 + 函数——逐条实现与验证

### 2.1 命令五组（实施批次）

| 组 | 内容 | 本轮做 | 接真时点 |
|---|---|---|---|
| **A 机制族** | 代入 83 + 容器 8 + 状态动作家族（set_flag/clear_flag/set_variable/global_set/计数器/快照×2/update→set_attr） | ✅ 完整实现 | — |
| **B 引擎直取** | 16b §5.2 全部 16 条（kill_hero/fire_hero/spawn_hero/destroy_faction/appoint_castellan/dismiss_castellan/change_clan_leader/independence/rename×3/teleport/gold_change/pause_time/personal_duel/create_order）+ im_message（现有 IM 管线）+ 独立 T2 3 条（set_away 外出 / change_home_settlement 居城 / force_weapon_swap 强制换武器） | ✅ 完整实现。API 走两轮查找（铁律 5）+ 铁律 4 归口；16b「待核实」清单（总督 API/墙血 API）先 `ilspycmd` 反编译确认，1.2.12/1.5.1 均无 = 按 16b 降级列走降级（如城主任免 = 只写归属 + 通知日志） | — |
| **C 演绎演出类（23）** | 16b §5.1：say/monologue/narrate/say_as/say_choice/monologue_choice/choice/choice_option_set/text_set/message_close/image_show/image_hide/bg_change/bg_restore/screen_effect/bgm_change/se_start/se_stop/se_loop/scene_next/facility_enter/facility_exit/branch | ✅ 完整实现（W5：StoryCompiler 排演 + 播放器三形态 v1 + 舞台/镜头指令——§2.3）。**生成端已注入 actors/present/slot/form（stage_inject）；enter/leave/move/camera 由编译器排演推导 + 显式指令透传（§0.5）** | —（本轮） |
| **D 依赖系统（9）** | army_command/army_form/army_form_best → 02；personal_duel → 03；quest_create/quest_update/quest_cancel → 13；council_config/council_open → 17 | 参数入仓 + 动作桩日志（存得下读得出，行为空） | 02 / 03 / 13 / 17 各自 plan（移交清单见 §五） |
| **E 特殊档（11）** | T3 新造 4（appoint/dismiss_provincial_lord 国主任免、change_stance 立場、become_purveyor 御用商人）+ T0 3（minigame/number_input/misc_branch）+ T3-预留 4（ninja/pirate_army_form×2） | ✅ 完整实现 | — |

**E 组 T0 明细**（16b §5.4）：迷你游戏 → `SingleRollResolver` d20 检定（铁律 17：掷点 ≥ 门槛；赢/平/输三分支原样保留）；数字输入 → 少/中/多三档选项写回原槽位；他歧 → 解析碎片只注释保留。**T0 降级纪律**：翻译器已生成 `note` 步骤 + 原文注释——执行器对 note = 日志 + 跳过（16b §5.4 末尾）。

### 2.2 函数（15 个实现点，全部进 DslEvaluator）

| 函数 | 实现 | 验证 |
|---|---|---|
| exists / atWar / isAllied / hasMet / hasRelation / relation / sameSettlement | 引擎直查（对象存在/交战/外交立场/是否认识/关系值比较/同据点）+ 量纲（relation 归一到太阁量纲，16b §六） | `custom.dsl_eval` 摆状态 fixture → 断言返回值 |
| isNeighbor / allControlled / region_attr_1 / canMove / canAttack | 数据包 + 引擎组合（据点相邻/距离阈值；区域据点归属多数派统计；区域支配大名家；目标可达 + 02 未锁（未锁先 = 恒可）；交战 + 可达 + 围城冷却） | 摆城归属两座 → 断言；空数据包 = false + 日志 |
| hasCard | 数据包卡表（空表）+ 外置仓持有关系 | false 路径 |
| canPromote | 桩（依赖 17 晋升链） | `[Scenario][TODO]` |
| unknown_2 / unknown_8 | 恒 false + 生成期告警（16b 已裁定，测试矩阵占位） | — |

**多返回值注意**：isAllied / relation = **数字**（!=0 即同盟；16b §六）——求值器按 16a 值类型列处理，禁止当布尔。

### 2.3 执行器（ScenarioExecutor）与演绎子引擎

**剧本执行器（主表）**：

- **新表，不塞旧管线**：旧 `CommandManager`（中文 Cmd）归演绎播放器麾下（本轮改造），剧本执行器 = 新分发表 `Dictionary<string, StepHandler>`，key = 01 步骤类型（effect/if/…）+ 16a 命令侧名（英文 token）；注册纪律 = wheels.d/story.md 同构（Register 一个 handler 三行，禁止改 Execute / if-else 链）。
- **步骤子集**（01 步骤类型表权威）：本轮实现 `effect` / `if`（嵌套 ≤ 2 层）/ `wait` / `note` / `perform` / `inquiry`（原版 quest 通知弹窗样式）/ `im_message`（现有 IM 管线）/ `cutscene`（SceneNotification 过场）/ `scene_enter` / `scene_exit`（場面 hop——01 反编译已验证：OpenIndoorMission/EndMission）；`bgm`/`se` 随 C 组。
- **执行语义照 01**：同步顺序（上一步完成才下一步）；演出截断 = 步骤完成；脚本执行中锁（`_isRunning` 时任何 trigger 跳过，防重入；脚本强开的 mission 不再检查）；`Event::<id>.done` 调度器自动记（三层纪律：Event::done / Ctx 事件内 / Flag 跨事件）。
- **执行器驻留**（01 已裁定）：外层全局单例 + `MBSubModuleBase.OnApplicationTick` 驱动，不继承 CampaignBehaviorBase/MissionBehavior——脚本状态跨层不丢。

**演绎子引擎（C 组 23 条的执行侧，用户裁定 = 方法一部分）**：

| 件 | 做什么 | 结果 |
|---|---|---|
| 台本校验脚本（A1） | 对 story/*.jsonc 跑语法/引用/时序三层检查（cmd ∈ 指令表、speaker ∈ actors、说话人必须先入场、choice→if 同构、形态约束 05 纪律 7） | 39 段全过，报告落 `report_okehazama.txt` 同级 |
| StoryCompiler（A3，05 步骤 2） | ①actors/present → 演出开头 enter 序列（present 先入、首次发言自动入场 05 纪律 6①）②显式 actor_enter/move/leave 透传 ③choice → 选项 + 写槽效果 ④if 与骨架同构 ⑤camera/actor_action 透传（无 = 执行器默认）⑥`{PH_N}` 注入表注册 | `custom.playback_dump`（05 验收）出完整台本 |
| PlaybackEngine（A4，05 步骤 3） | 播放器改造：从 ScriptNode（中文 Cmd）迁移到台本（英文 token）指令流；`when` 行级门控（==/not/and/or 子集）；`{PH_N}` 注入（首版最小集：玩家名/已注册 Hero 名/称呼表——缺失降级不崩）；note 跳过 | 三形态 v1：**menu_dialogue（21 段）** = 原版弹窗序列（inquiry 样式先行——05 铁律 16 旧对话 UI 禁用，立绘对话 UI 后置）；**map_dialogue（3 段）** = 大地图立绘/台词（无 3D）；**scene（15 段）** = Mission 内 Agent 编排——actor_enter/move/leave → 05:106-110 映射表（story_move=MoveToPositionAction、story_leave=离场藏身、story_anim=PlayAnimAction 复用 ForcePlayAction 切集管线、story_pose=姿态原子动作）+ camera（默认中景跟说话人，显式指令切 shot/target）。**🔴 2026-08-30 用户裁定：scene 形态多 agent 对白的 v1 UI = StoryDialogVM 升级版（05 §297 大件，先做），不用弹窗/气泡替代**——升级要点（05:297-314）：UI = 原版对话流视觉（说话人名+立绘头像槽位（数据包有图才显示，占位不塌）+文本+底部选项）；多 agent 轮转（说话人切换→切名字/头像/镜头）+ 旁听者+多方表态+镜头跟随说话人；玩家 Esc 中断 → 演出中止+渠道通知；**OnDialogClosed 取舍**（✅ 保留：掩码恢复/镜头恢复/状态复位/玩家控制恢复/演出流程继续；❌ 解除：GenerateEventAsync 旧协商链绑定）；🔴 **红线：IM confirmFight 禁止调重制版 Close()**；配套 DialogChoice.xml 重制为原版对话流布局 |
| 场景 ID 解析 | scene 形态的 sceneId 按 08b §四 4.2 查清单（查不到 = 停机报错，零兜底） | — |

**默认策略**（缺失不崩，铁律 1）：camera 未写 = 中景跟说话人；actor_action 未写 = 无动作；显式入场值缺失 = 段首默认（actors 表 slot 顺序入场）；`{PH_N}` 无数据 = 原文显示 + 日志点名（不比「裸 {PH_N}」静默）。

### 2.4 验证（保证真的能跑——三层，逐命令对号）

| 层 | 做法 | 入口 |
|---|---|---|
| 解析 | 加载期静态校验子集（语法/注册表/类型/两轮查找——01 检查项 1/2/3） | `custom.dsl_validate`（11 已有）/ 加载期日志 |
| 执行 | 摆状态 → 单动作落世界 → 断言 | `custom.scn_run_action <actionId>` / `custom.scn_set_flag` |
| 世界可见 | 每 B 组命令 = 一个「改世界 + 看得出来」断言（kill_hero → Hero.IsAlive=false + 死讯日志；gold_change → 钱袋数字；destroy_faction → 势力消失；rename → 地图名——12 A3 矩阵逐条对号） | 实机观察 + 日志 |
| 函数 | fixture 断言 | `custom.dsl_eval` |

**验收基线（W6 活体考题）**：①桶狭间 22 事件全量加载 0 错 ②condition 全可求值（每事件出一份「可判定/降级」清单）③全 script 无未注册步骤（含 perform 引用 = 演绎分件存在、可编译）④选 1 个纯结算事件端到端（触发 → 求值 → effect 落世界 → 存档 → 读档 → 进度不丢）⑤选 1 个 **scene 形态**演绎段端到端（EFF06E00_160_seg1 活体考题：present 沉默观众位开场入场 + 说话人自动入场 + 2 句对白 + 收场——台本校验/编译/播放全链）。

**诚实边界**：本轮演绎 =「**能演**」（三形态 v1：菜单弹窗序列 / 大地图立绘 / 场景 Agent 走位 + 默认镜头跟说话人），不是「演得好看」——多 agent 对话 UI、特写导演、Tableau、槽位系统 = 05 体验大件后置；显式 camera/actor_action **值** = agent 审核演绎补写（08b §七），缺失走执行器默认（中景跟说话人/无动作/段首入场），不崩。

---

## 三、工作包与顺序（W1~W6）

| # | 工作包 | 完成标志 |
|---|---|---|
| W1 | 数据加载器（events.jsonc 加载 + 加载期校验子集）+ 三仓 + 存档键 + 新档清仓 | `custom.scn_list` 列出 22 事件；存读档；新档干净 |
| W2 | AttributeResolver + DslEvaluator（属性读链 + 函数 15） | dsl_eval 30 条全过 |
| W3 | ScenarioInit（三态 + 默认值 seed + Ctx 上下文） | 新档种子正确；Ctx 事件触发赋值正确 |
| W4 | 剧本执行器（A + B + E 完整；D 桩）+ 调度器最小集（game_start / daily / monthly 无场景 trigger + 互斥选路 + once/priority + 执行中锁） | scn_run_action 逐条验收；桩日志格式统一 |
| W5 | **演绎执行**（数据侧 ✅ **stage_inject v2 已注入**（2026-08-30）；运行侧待做：台本校验 → StoryCompiler → 播放器三形态 v1 + **StoryDialogVM 升级版（05 大件：多 agent 轮转+立绘槽位+镜头跟随+OnDialogClosed 取舍+DialogChoice.xml 重制）** + 舞台/镜头/{PH_N} 注入；演出类 23 条接真） | playback_dump 全 39 段；EFF06E00_160_seg1 实机演完（文本/选项/入场走位正确）；story 面板多 agent 轮转正确、Esc 中断不崩、IM confirmFight 不碰重制版 Close（铁律 16 红线验收） |
| W6 | 活体考题（22 事件全链 + 纯结算事件端到端 + scene 段端到端 + 存读档） | 验收基线全绿；断点逐条记录归属（执行器 / 生成器 / 数据包） |
| W7 | 文档收口：16b 待核实清单刷新（本轮实测结论回填）、README 进度行更新 | 移交清单（§五）每条有主 |

**后置清单（明确不做，防战线展开）**：Tableau 战斗演出、槽位系统完整（语义位+情景模板+场景适配）、导演镜头库细节、立绘对话（ConversationManager 从 game menu 启动验证）；显式 camera/actor_action **值**的 agent 审核补写（08b §七）；场景类 trigger（settlement_enter/house_enter/council_start/siege_battle… 需 Mission/选址——本轮只挂 game_start/daily/monthly）；07 数据包内容（机制先跑空表）；06 选择器 UI、02/03/13/17 真实现（桩直通）；validator 全量（01 检查项 17 条）；多视角三通道；04/09/10/14/15。

### 3.5 最新 TODO 清单（2026-08-30 更新——实施进度回填）

> **🔴 进度总览（2026-08-30）**：**9 项 TODO 中 5 项推进 ✅（T1 W1 / T2 W2 / T3 W3 / T4 W4 / **T7 PlaybackDialog 面板骨架**（用户 2026-08-30 提前指令：DialogChoice 重制 = 文本框+立绘位+按钮，砍谈判三件套）——均为「代码完成、真机待验」）+ 演绎注入前置完成**；T5（PlaybackLoader+播放器）= **下一步**（含 T7 遗留的多 agent 轮转/镜头/立绘接入）；T5/T6/T7 三者实际 = 播放器一条线。真机清单随各 T 行标注。
> 🛡️ 仓库纪律自愈（2026-08-30）：`.gitignore` 追加 `tools/`（2720 张 round_full 图误提交事故 + tpactool 子模块 dirty，均按用户处理干净；故障= `git add -A` 手滑带 tmp/，防再犯 = 忽略已生效）。

> 顺序依据：**用户 2026-08-30 修正裁定：StoryDialogVM 最后做**（「无非是显示立绘和谁在说话，工作量简单」）——字段与方法（W1-W4 + 演绎运行侧非 UI 部分）先行；StoryDialogVM 升级作为 UI 收尾（scene 段对白 UI 临门一脚）。

| # | TODO | 完成标志 | 状态 |
|---|---|---|---|
| T1 | **W1 数据底座** —— ✅ **已实施（2026-08-30）**：Scenario/ 5 文件 + GlobalVariableBehavior 升级（键名对齐 11/上限 10 万告警不淘汰/全局状态接口/ClearAll）+ MySubModule/MyBehavior 注册与清档 + csproj 登记；build 0 错误、22 事件离线预演 0 表外。**遗留真机项**：控制台 `custom.scn_list` 实机跑 22 事件（真机列 0 或全列）；数据部署 = 发布流程拷贝 ModuleData/ScenarioData（07 补） | `custom.scn_list` 列出 22 事件（真机）；存读档 lwn_scn_attr/lwn_scn_state | ⚠️ 代码完成、真机待验 |
| T2 | **W2 求值器** —— ✅ **已实施（2026-08-30）**：DslParser/DslEvaluator/AttributeResolver/ScenarioClock/ScenarioContext/ScenarioDataPack + Arm::/superior/province 补缺 + custom.dsl_eval、dsl_validate；build 0 错误；预演 8 函数/11 域/13 属性全覆盖。**遗留**：真机 dsl_validate（identity 带序/CountRank 依赖数据包 RankLadder 表——17 接后恢复；isAllied 近似待核实 API） | `custom.dsl_validate` 22 事件三档报告（真机）；带序比较恢复 = 17 数据包 | ⚠️ 代码完成、真机待验 |
| T3 | **W3 初始化** —— ✅ **已实施（2026-08-30）**：ScenarioInit（三态：未出生=生日设未来 / 已死=仅日志（击杀流程留 06） / 活着=按剧本年代校年龄 + 数据包 heroes[].attrs 默认值 seed 外置仓 + 孤儿键照种）+ data pack heroes[] 解析 + 新档挂 OnNewGameCreatedEvent（清仓→种子顺序注释）+ `custom.scn_init` + 样例 pack.json（5 英雄/五维+功勋，注解 07 全量）+ Ctx.InitForEvent（W4 调用点）。**验证**：build 0 错误。**遗留**：真机 `custom.scn_init` 日志（英雄 5/属性 30/拨年龄 3/未出生 0/已死 1） | 新档种子正确；Ctx 触发赋值正确 | ⚠️ 代码完成、真机待验 |
| T4 | **W4 剧本执行器+调度器最小集** —— ✅ **已实施（2026-08-30）**：ScenarioSchema + JsonExtensionData（动态参数 actor/target/value/slot… 全收）+ ScenarioExecutor（effect/if≤2 层/note；perform/inquiry/演出/容器 = 桩 [Scenario][TODO]；单步异常跳过不崩）+ ScenarioActions（**A 组真实现**：update→set_attr（target 引用解析）、assign_ctx/assign_var（值 DSL 求值）、set_flag/clear_flag/set_variable/global_set/counter_reset；余 = 桩）+ ScenarioScheduler（trigger 分组按加载序/互斥选路（normal 优先 weak 兜底）/once→Event.done/执行中锁/懒加载）+ 挂接（game_start/daily/monthly 30 天钩子）+ `custom.scn_force_event` / `scn_run_action`。**验证**：build 0 错误。**遗留**：B 组引擎动作真实现（kill_hero 等 16+3 条——下轮 API 反编译逐个确认）；container_* 语义（读 16a 参数列下轮）；真机 force_event 跑 EFF0C300_159（update/assign 类真跑，kill_hero 类 = TODO 日志） | `custom.scn_run_action` 逐条验收；桩日志统一 | ⚠️ 代码完成、真机待验 |
| T5 | **PlaybackLoader + 播放器** —— ✅ **已实施（2026-08-30）**：ScenarioLoader.LoadPlaybacks（story/*.jsonc 单对象解析 + id 去重）+ PlaybackLine/PlaybackOption 数据类（cmd/speaker/textKey/text/when/options + JsonExtensionData）+ **PlaybackPlayer**（链式回调推进：dialogue/narrator → VM.Show（说话人名解析（Hero 引擎显示名/原样兜底）+ LWNTextHelper.ResolveText（textKey→本地化，fallback 中文原文））→ 点继续下一行；choice → VM.ShowOptions（StoryOptionVM 复用，选中 → Ctx::choice=optN 写槽）；行级 when 门控（DslEvaluator 同源）；effect/note 行直接执行；actor_*/camera/音频 = TODO 日志（T6）；演完 = 面板关闭+IsPlaying 复位）+ custom.playback_play/playback_dump + **数据部署**（39 段 jsonc → ModuleData/ScenarioData/story/、i18n → Languages/CNs/）。**验证**：build 0 错误。**遗留**：menu 形态真机 `custom.playback_play okehazama_ECF00000_159_seg1`（2 句 + 选项）；scene/map 形态 = T6 | playback_dump 全 39 段；实机弹窗播完；无裸 {PH_N} | ⚠️ 代码完成、真机待验 |
| T6 | **相机控制 + scene 段播放器**——SpringArmCameraView.UseCameraTemlate + shot 映射 + 默认「中景跟说话人」；舞台指令→StageDirector.ShowActor/HideActor/ChangeActorSlot + slot 语义映射（throne→Chair/side→Guest/gate→Gate） | scene 15 段实机演完；`custom.playback_play` 可驱动 | ⏳ |
| T7 | **🔴 StoryDialogVM 重制 = PlaybackDialog 面板（用户 2026-08-30 提前指令：基于 DialogChoice 砍谈判三件套，文本框+立绘位+按钮）** —— ✅ **骨架完成**：GUI/Prefabs/PlaybackDialog.xml（说话人名+立绘位（@PortraitTexture 空=隐藏占位不塌）+对白文本+选项按钮+继续按钮）+ Interaction/PlaybackDialogVM.cs（新链新类：无旧链 GenerateEventAsync 崩溃路径；OnPropertyChangedWithValue 惯例；Close→OnClosed 事件）+ Interaction/PlaybackDialogUI.cs（V.NewLayer(1000)+TopScreen.AddLayer（Mission/Map/Menu 三形态通用）+输入掩码开/复位+摘层守卫（HasLayer+try/catch，引擎无 IsFinalized）+幂等）+ custom.playback_show/playback_close；build 0 错误。**遗留**：多 agent 轮转+镜头跟随联动+立绘接入（SpriteAssetsManager，W5/T6 播放器接）；W5 播放器订阅 OnClosed 推进 | scene 段多 agent 对白全流程；Esc 中断不崩；confirmFight 不碰（新链不存在旧 VM 引用） | ⚠️ 骨架完成、真机待验 |
| T8 | **W6 活体考题**——22 事件全链 + 纯结算事件端到端 + EFF06E00_160_seg1 scene 段端到端 + 存读档 | 验收基线全绿；断点记录归属 | ⏳ |
| T9 | **W7 收口**——16b 待核实清单回填、README 进度行、移交清单有主 | 每移交项有接真 plan | ⏳ |

---

## 四、审批人四问（自查，铁律 21）

- **结论是什么**：执行层核心 = 字段生命周期（开局初始化 / 命令函数变化 / 原版世界同步）+ 方法（命令 + 函数）逐条实现与验证；分 F1~F5 字段件 + W1~W6 工作包；五组批次、桩级一次性到位。
- **凭什么**：16b 每张表都写明「谁来写、写去哪、玩家看到什么」；存档四键 11 已登记且 SaveDefiner 已注册（零新增存档结构）；函数/代入槽/触发语义 01/16 已全部定稿。
- **代价是什么**：本轮交付 =「引擎能跑 + 数据全落」，不是「玩家看戏」——演出类与依赖系统的命令桩期玩家无感；唯一技术风险 = 16b「待核实」API（总督/墙血/人物状态枚举），W4 前反编译确认，无解则按 16b 降级列走降级，不阻塞。
- **要做哪几件事**：W1~W6 + 每件验收 + §五 移交清单逐条有主。

---

## 五、移交清单（D/C 组命令接真归属）

| 命令（组） | 接真 plan |
|---|---|
| 军团指令/编成/编成最强 | 02 战略 AI 纠正器 |
| 个人战斗 | 03 程序化战斗编排器 |
| 事件主命作成/变更/解除 | 13 主命系统 |
| 会议设置/开始 | 17 功勋与晋升 |
| canPromote 函数 | 17 |
| 演出类 23 条 | ✅ 本轮 W5 实现（三形态 v1）；后续体验升级（多 agent UI/导演/Tableau/槽位系统）→ 05 演出系统（后置） |

---

# 00 — 开工评审（v2 版，完整存档，2026-08-30 v3 起按 v3 执行）

> 🔴 **v2（2026-08-30）方向修正**：v1（2026-08-28）是静态审计「21 份 plan 够不够写代码」，结论排「01 剧本引擎核心」为第一批——**错了**。v2 结论：第一步 = 验证已生成的故事事件 JSON（story_event_json/okehazama）能否被游戏执行、能否按 05-演出系统.md 演绎出来，为此改造已有的 StoryEngine 管线。其余一切为后话。v1 原文存档于文末附录。
> 审计对象：`plans/scenario-campaign-mode/` 全部 plan + 生成物 `story_event_json/okehazama/` + 现有管线 `ExampleModVS/ExampleMod/ExampleMod/Story/`

---

## 一、结论（v2）

**决定：第一步 = 端到端最小闭环——拿真实生成物（桶狭间 21 个事件）验证「能加载、能触发、能按 05 演绎」，为此改造已有 StoryEngine 管线。其他 plan（01/02/06/17…）全部后话。**

理由只有一句话：**生成物从未被任何游戏代码读取过**。"76 行覆盖全绿"只证明生成端自洽（太阁原文没丢、翻译没猜），不证明执行端能跑——而从 JSON 数据到游戏画面之间，**一件代码都不存在**。

### 为什么 v1 被判错（三条硬证据）

1. **现有管线吃的是另一套格式，和生成物结构对不上**：

   | 维度 | 已有管线（ExampleMod StoryEngine） | 生成物（story_event_json/okehazama） |
   |---|---|---|
   | 数据文件 | `ModuleData/StoryJson/*.json` = `List<StoryEvent>` | `events.jsonc`（单对象骨架）+ `story/*.jsonc`（演绎分件，文件头注明"机械翻译产物"） |
   | 指令语法 | **中文 Cmd**：`對話`/`分歧`/`代入ａ`/`變量賦值`…（CommandManager.cs:27-52 注册表） | **英文 token**：`dialogue`/`choice`/`narrator`/`effect`/`perform`… |
   | 角色引用 | ScriptNode 里裸中文名/params 字符串 | `Hero::lord_1_imagawa_katsumoto` 域前缀 + StringId（铁律 20） |
   | 条件 | `type:调查` + left/operator/right 字段 | `condition: "and(exists(...), ...)"` **DSL 文本表达式字符串**，求值器不存在 |
   | 文本 | node.Text 直接写中文 | **textKey → i18n XML**（铁律 13），文本里还有 `{PH_N}` 占位符需要运行时注入 |
   | 门控 | 无 | 行级 `when` DSL 表达式（05 源格式纪律 1） |

2. **整条「数据 → 画面」链路上缺 4 件代码，全只有文档**：①生成物加载器（StoryDataLoader 读不动 events.jsonc，字段名都不同）②DSL 求值器（condition/when 是字符串表达式，01 只写了语法没写代码）③StoryCompiler（05 步骤 2，把 lines[] 编成台本指令流——**0 行代码**）④播放器改造（05 步骤 3，现有 RunLoop/Stack 骨架保留但动得最深）。

3. **现有管线正是 05 要消灭的对象**：`GetCurrentSceneParticipants`（ReadStory 侧运行时向前扫描算"谁在场"）、`AssignSlots`、`CheckActorLeaving` 偷看下一幕——05 裁定全部移入编译期（05 "🔴 现状动态算的 4 个点"表）。所以第一步不是写新引擎，是**改造已有引擎**，让它从"运行时现算"变成"按编译器产出的台本播"——这本来就是 05 的 Phase 1。

### 验证可执行性时当场就能暴露的真问题（示例，执行侧验证返哺生成侧）

这些是在通读生成物时发现的，说明"跑一遍"值回票价（全跑通后另出验证报告清单）：

- 🔴 **生成器 bug（疑）**：EFF06E00_159_seg1 里 T23/T25 的 `when` 写成 `and(..., (Ctx::choice) == "opt0", (Ctx::choice) == "opt1")`——两个分支条件 AND 在一起**恒假**，对应台词永远不播。这正是执行侧验证才能照出来的病（生成端自检只查"行数对齐"，不查"逻辑可达"）。
- 🔴 **`{PH_N}` 占位符注入**：i18n 文本含 `{PH_N}`（208 条注入表，审记 §八），运行时没人注入 = 屏幕上直接显示 "{PH_1}" 穿帮。首版最小注入（玩家名/已注册 Hero 名），其余降级写死注释。
- 🔴 **`note` 步骤（BGM/SE/机制行）**：events.jsonc script[] 里大量 `{"step":"note","note":"🔴 ＢＧＭ變更 表现指令 → 05 承接"}`——执行器必须先能"跳过不崩"，能承接的再承接。
- 🔴 **演出形态待终裁**：159 两种分件 form = `menu_dialogue`，但事件 trigger 是 `house_enter`（室内画面）——机械默认的形态表与 trigger 是否匹配本来就留了 agent 复核（审记 §五.3）。
- 🔴 **`🔴待注册`/`🔴待07` 占位**：condition/effect 里还有待注册实体（如 `Clan::🔴待07今川氏真`）——执行器对未知 ID 必须"跳过并记日志"不崩（iron 1），但验证报告要逐条分清：执行器的锅 vs 数据包的锅 vs 生成器的锅。

## 二、第一步做什么（验证可执行性，两条链路）

> 🔴 **第 0 步（比链路 A 更前提，2026-08-30 用户裁定）**：先修生成器再谈执行——tk5_to_json.py 现为枚举式翻译（16a 有词条也漏翻：312 个 note/23 个 🔴 假 action/容器参数中文全角落产物），v6 数据驱动重构规格见 [08b §十五](08b-转化器规格-自动化翻译流水线.md)。**✅ 2026-08-30 完成（S1-S6 全绿）**：22 事件全绿、五道硬断言（含第五道新增）全过、v5 病征清零——**数据层验收通过，以下链路 A/B 为当前第一步**。证据链见 08b §15.2 + [审核记录 §〇.5](story_event_json/审核记录.md)。

> 纪律：用控制台指令驱动（12 分拆测试纪律），不碰拼装测试；先单事件、后全剧本。

### 链路 A：演绎链路（数据 → 游戏画面，主线）

| # | 做 | 完成标志 |
|---|---|---|
| A1 | **写台本校验脚本**（05 "台本校验脚本"节，内容包工具链）：对 `story/*.jsonc` 跑语法/引用/时序三层检查——cmd ∈ 指令表、speaker ∈ actors、说话人必须先入场、async/dialogue 纪律、choice→if 同构 | 18 个分件全部跑完，错误清单+警告清单落 `report_okehazama.txt` 同级新报告 |
| A2 | **生成物加载器**：读 events.jsonc + story/*.jsonc（文件头有 `//` 注释，解析注意 jsonc 兼容），映射为播放器可用的数据类 | 控制台指令 dump 一个分件完整内容无字段丢失 |
| A3 | **StoryCompiler**（05 步骤 2，先出 dump 后实播）：lines[] → 台本指令流（`dialogue`/`choice`/`narrator` 透传 + `when` 转条件指令 + 首次发言自动入场规则（05 源格式纪律 6））；scene 形态的 actor_enter/move/leave 生成（05 纪律 4-6） | `custom.playback_dump <playbackId>`（05 验收）输出完整台本；**先做 menu_dialogue 形态**（无 Agent 无走位，最小） |
| A4 | **播放器改造**（StoryEngine.cs）：①加载器/执行器数据类从 `ScriptNode`（中文 Cmd）迁移到台本（英文 token），CommandManager 加映射兼容层（05:160）②砍运行时现算（GetCurrentSceneParticipants/AssignSlots/CheckActorLeaving 全部退役）③`when` 门控（行级参与门控，先实现 ==/not/and/or 子集）④choice 写 Ctx 槽（`Ctx::choice`）⑤`{PH_N}` 注入器（先最小集）⑥`note` 步骤跳过；⑦menu_dialogue 形态的**弹窗式演绎路径**（据点菜单画面，无 Mission 无 Agent——现有 StoryEngine 是 Mission 内执行器，这条路径是新的，05 说 inquiry 弹窗序列或立绘对话待实测） | **A4 完成标志 = 真实分件（推荐 EFF06E00_159_seg1 评议会，含 dialogue+narrator+choice+when+占位符全要素）在游戏内完整演完**：文本正确渲染、选项弹得出、when 门控按玩家身份走对分支 |
| A5 | **{PH_N} 注入表落地**：角色名/称呼从哪来（07 数据包），玩家名/自称实时读 | 台词无裸 {PH_N}；缺数据的降级不崩 |

### 链路 B：事件触发链路（调度 → 条件 → 触发演绎，01 的战场）

| # | 做 | 完成标志 |
|---|---|---|
| B1 | **DSL 求值器**（01 正式格式子集先行）：and/or/not/`== != > >= < <=`/属性引用/函数 `exists` `isAllied` `atWar` `isNeighbor`；**答不上来的属性按"否"并记日志**（01 安全兜底，不崩） | 对 EFF0C300_159 的 condition（32 条子句）逐条求值，报告每条的"可判定/降级"状态 |
| B2 | **trigger 挂接**：`house_enter`/`settlement_enter` 等 09 用到的 trigger 挂到现成事件点（16 trigger 注册表 v1 对照） | 进据点/菜单画面 → 调度器检查事件 → 条件真 → 走触发 |
| B3 | **调度器 + perform**：事件触发 → `ctx_set`/`if`/`effect`（首版：旗标/本地化 declare_war/make_peace/set_owner 等 01 已定的可直接实现的）→ `perform` 引用演绎分件 → 链路 A 播 | `custom.event_test <eventId>`：一个真实事件从触发到演完，进度（once/flag）不丢 |

### 链路 A∩B 的最终验收（= "可执行性验证"完成）

- 桶狭间 21 个事件全部能在游戏内触发并演绎（或验证报告逐条记录"哪个类目断在哪"）
- 每一条断点记录归属：执行器（05）/ 生成器（08b）/ 数据包（07）——**生成器的锅回生成器修（铁律 22 纪律），不许在 JSON 产物上打补丁**
- 断点类目收敛后 → 回填 16 注册表/05 字段表（表外词条由执行侧验证反过来裁定，比纯读文档裁决实）

## 三、原审计结论怎么处理（v1 存档，不追改）

v1 的审计结果（21 份 plan 的评级/三处循环依赖/5 件前置）**不推翻，只是降级为后话**：

- 第一批排期改：**验证链路 A+B（本文档第二节）→ 修出断点清单 → 再按 v1 分批**。v1 说的"第一批 = 01 剧本引擎核心"搁置——01 的真身是 B 链路 + validator，现在被验证需求提前拉进来做（只做子集，不是整份 01）。
- v1 的 5 件前置（改闸门/循环依赖拆细/06 口径/场景库盘点/10 重写）**依然成立**，但都在验证闭环之后才挡路，暂不动。
- v1 的评级表（01✅/02✅/03⚠️…）在附录原样保留。

## 四、下一步动作（一句话）

**立即开始：链路 A 的 A1（台本校验脚本）→ A2/A3/A4（menu_dialogue 形态的编译+播放，用 EFF06E00_159_seg1 当第一份活体考题）** → 用验证报告决定 B 链路的范围与排期。

---

## 附录：v1 原文（2026-08-28，存档备查）

> ⚠️ 本附录 = 历史审计，结论按 v2 第一节修正执行。

### v1 一、结论（原文）

**能开工，但不能 21 份一起开。分三批，第一批今天就能动。**

- 第一批（立刻）：01 剧本引擎核心。Phase 0，无前置，9 个步骤 + 验收挂三条控制台指令，它要的「数据格式冻结」已经有现成事实源（16a 注册表 1481 行全部裁定完毕、机器自检全过）。
- 第二批（01 跑通后并行）：02 战略AI / 11 存档配置 / 13 主命与通用Quest / 16 注册表（已定稿）。这四份的依赖只有 01。
- 第三批（补完 5 件前置再开）：03 05 06 07 08 04 17 14 15 09 10。

开工前必须先补的东西：5 件，其中 3 件是十分钟的改字，1 件是要动手确认环境，1 件是要重写一份文档。

真正的拦路虎不是技术，是审批状态：README 的审核表里 21 份 plan 只有「07 附录」和「16b」打了 ✅，其余全 ⏳；而 README 自己写着「全部 plan 审核通过前禁止实施」。技术上第一批已经可以动，卡的是这条自设的闸门。建议把闸门从「全部通过」改成「按批次通过」。

### v1 二、必须先补的 5 件事（原文，仍成立，降级为后话）

1. 三处循环依赖 —— 依赖写太粗，排不出工期（十分钟改字）：03 依赖 07 素材表（仅 Phase 2 全量）/ 05 依赖 05 §63 字段表（已冻结）/ 06 依赖 tools/entity_maps.py（已生成）。
2. 运行环境不在开发机上（要动手确认）：织丰锁死 1.2.12；本机实测 1.5.2；1.2.12 备份客户端在 MB2_Version/MB2_1.2.12，在。一进 Phase 2（07 开始）必须切 1.2.12 客户端，切之前先查 junction。
3. 06 的开局选择器在 1.2.12 上必然走降级（[StartOptionsProvider] 是 1.5.1 新增），口径要改；数据源表名要更正为 骑砍2太阁Mod表.xlsx（1049×122）。
4. 03 的路线选择卡在一个没做的盘点上（场景库盘点 → 路线 A 现成场景参数化 or 路线 B 程序化搭景）。
5. 10 本能寺还是 55 行设计稿不是可执行定义（对照 09b 重写，排期按新写一份估）。

### v1 三、已经很扎实、不用再补的部分（原文，仍成立）

| 板块 | 扎实到什么程度 |
|---|---|
| DSL / 翻译链（16 / 16a / 16b / 08 / 08b） | 全工程最硬的一块。1481 行语料 100% 被裁定认领、零遗漏，六道机器自检全过；工具链闭环（生成器 → 翻译器 → 校验器），桶狭间 22 个事件已经产出实机 JSON 并 100% 通过覆盖率校验 |
| 05 演出系统 | 最厚的引擎件（426 行），复用现有 StoryEngine / StageDirector，把单一控制者纪律、语义槽位、朝向关系式、时间控制都写到了实现级；源格式字段表已冻结 |
| 02 战略AI纠正器 | 篇幅最短（113 行）但含金量高——写出了「保持交战 ≠ 改一次状态」这类反直觉的坑，并点名了 DeclareWarAction / SetPartyAiAction / DoNotMakeNewDecisions 等具体 API |
| 03 程序化战斗 | 字段冻结表 + archetype 注册表 + 两条管线的完整 API 签名都写全了，只差路线选择 |
| 12 验证与发布 | 有分拆测试总纲 + 四条红线检查的可直接执行的命令 |
| 结构完备度 | 21 份 plan 几乎每份都齐「分步骤 / 验收 / 坑」三件套 |
| 织丰数据源 | 07b 刚核完：事件里点名的 923 个对象查得到 758 个 = 82.1%，桶狭间 100% 落地。够用 |

### v1 四、逐份评级（原文，仍成立）

| plan | 阶段 | 评级 | 说明 |
|---|---|---|---|
| 01 剧本引擎核心 | Phase 0 | ✅ 能开工 | 无前置，格式冻结有事实源，验收挂三条控制台指令 |
| 16 DSL 注册表 | Phase 1 | ✅ 已定稿 | 配套 16a/16b 生成物 + 自检全过 |
| 02 战略AI纠正器 | Phase 1 | ✅ 能开工（01 后） | |
| 11 存档与配置 | 同步 | ✅ 能开工（01 后） | 存档键登记表已被 16b 自检⑥ 反查过 |
| 13 主命与通用Quest | Phase 1 | ✅ 能开工（01 后） | 现状盘点已读代码确认，迁移分三步 |
| 03 程序化战斗 | Phase 1 骨架 | ⚠️ 补前置 | 场景库盘点 |
| 05 演出系统 | Phase 1 | ⚠️ 补前置 | 依赖声明要拆细，本体已可执行 |
| 08 转化管线 | Phase 1~3 | ⚠️ 补前置 | 同上；工具链本身已跑通 |
| 06 玩家身份与时代重置 | Phase 1 | ⚠️ 补前置 | 降级口径 + 表名，且几乎没点名 API |
| 04 体验设计 | Phase 1 | ⚠️ 补前置 | 依赖 13/05/03；本身是体验规格不是引擎件 |
| 17 功勋与晋升 | Phase 1 | ⚠️ 补前置 | 依赖链最长，排在 Phase 1 末尾 |
| 14 历史时间轴 | Phase 1/2 | ⚠️ 补前置 | 机制部分可跟 02 一起做，数据部分等 Phase 2 |
| 07 织丰剧本层落地 | Phase 2 | ⚠️ 补前置 | 自己写明「01-06 未通过前禁止实施」；开工时必须切 1.2.12 客户端 |
| 09 / 09b / 09c 桶狭间 | Phase 2 | ✅ 内容就绪 | 103 处 🔴待07 占位是设计意图，不是缺陷 |
| 15 时间轴中间事件 | Phase 3 | ✅ 内容就绪 | 四个里程碑都有 condition + script 骨架 |
| 10 本能寺 | Phase 3 | 🔴 待重写 | 55 行设计稿，条件式留空 |
| 12 验证与发布 | 全周期 | ✅ 能开工 | 贯穿始终，随时可用 |

### v1 五、建议的动作（原文）

1. 改闸门：把「全部 plan 审核通过前禁止实施」改成按批次放行，先批 01。
2. 改字（十分钟）：拆细 03/05/06 的依赖声明；更正 06 的数据源表名。
3. 定口径：06 的开局选择器认下「1.2.12 上只有兜底路径」。
4. 开工第一批：01 剧本引擎核心。
5. 并行做场景库盘点，出结论后定 03 的 A/B 路线。
6. 进 Phase 2 之前：确认 1.2.12 备份客户端的 junction 是活的。
7. Phase 3 之前：把 10 本能寺按 09b 的规格重写一遍。

### v1 六、审批人四问（自查，铁律 21）

- 结论是什么？能开工，分三批；第一批（01）今天就能动；开工前补 5 件事，其中 3 件是改字。
- 凭什么？21 份 plan 全文实读；依赖图逐条对撞查出三处循环；环境版本、注册表自检、覆盖率数字均为脚本实测。
- 代价是什么？要放弃「全部审完再动手」的闸门，改成分批放行；10 本能寺要按 09b 规格重写一份。
- 要做哪几件事？v1 第五节的七步。

### v1 七、本轮已经做完的（2026-08-27 ~ 08-28）

1️⃣ 完善 16a DSL 翻译总表 ✅（1481 行 × 13 列，T0=24／T1=407／T2=162／T3=860／T3-预留=28，六道机器自检全过）
2️⃣ 优化 08b 转化管线 + 产出桶狭间 JSON ✅（修两个生成器缺陷并固化为纪律 24/25；产物 = events.jsonc 2379 行 / story.jsonc 1512 行 / story/ 38 分件 / i18n 561 行 / report 340 行；verify_tk5_coverage.py 通过（无缺失））
3️⃣ 解读织丰表、核对 StringId 覆盖率 ✅（923 具名 / 758 命中 = 82.1%；15 张工作表清单 + 3 个易错点）
4️⃣ 开工评审 ✅（本文档 v1）
5️⃣ 收尾修复：占位据点的锚点查不到 ✅（键覆盖 20.7% → 100%）

### v1 八、接下来要做的 TODO（原文，降级）

| 组 | 内容 |
|---|---|
| A 组（v1 认为开工前必须处理） | 改闸门 / 拆细三处循环依赖 / 定 06 降级口径 / 更正 06 表名 / 做场景库盘点 |
| B 组（数据补齐） | 地方名对照表（9 行）/ 组织 StringId（37 个）/ 8 座无锚点的城 / 3 个太阁独有角色 / 3 个 XML 查不到 ID / 35 条待注册裁决 |
| C 组（文档欠账） | 10 本能寺重写 / README 待办段三个过时数字 / 09b lines[] 实体化 / 动作统一管理 plan / 16b 六个待核实事项 / CLAUDE.md 版本记录 1.5.2 |
