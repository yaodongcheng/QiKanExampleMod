# 00 — 开工评审：这些 plan 够不够动手

> 审计对象：`plans/scenario-campaign-mode/` 全部 21 份 plan + 3 份附录（共 7666 行）
> 审计口径：一份 plan「可执行」= 有①点名的引擎 API / 自研类型 ②冻结的数据字段表 ③分步骤清单 ④验收标准 ⑤坑清单
> 审计方式：全文实读（不靠正则统计——02/03 的 API 写在散文和表格里、不带括号，正则会误判成「零 API」）

---

## 一、结论

**能开工，但不能 21 份一起开。分三批，第一批今天就能动。**

- **第一批（立刻）**：`01 剧本引擎核心`。Phase 0，无前置，9 个步骤 + 验收挂三条控制台指令，它要的「数据格式冻结」已经有现成事实源（16a 注册表 1481 行全部裁定完毕、机器自检全过）。
- **第二批（01 跑通后并行）**：`02 战略AI` / `11 存档配置` / `13 主命与通用Quest` / `16 注册表`（已定稿）。这四份的依赖只有 01。
- **第三批（补完 5 件前置再开）**：`03 05 06 07 08 04 17 14 15 09 10`。

**开工前必须先补的东西：5 件，其中 3 件是十分钟的改字，1 件是要动手确认环境，1 件是要重写一份文档。**

**真正的拦路虎不是技术，是审批状态**：README 的审核表里 21 份 plan 只有「07 附录」和「16b」打了 ✅，其余全 ⏳；而 README 自己写着「全部 plan 审核通过前禁止实施」。技术上第一批已经可以动，卡的是这条自设的闸门。**建议把闸门从「全部通过」改成「按批次通过」。**

---

## 二、必须先补的 5 件事

### 1. 三处循环依赖 —— 依赖写太粗，排不出工期（十分钟改字）

| 谁 | 声明依赖 | 对方又声明依赖 | 成环 |
|---|---|---|---|
| 03 程序化战斗 | 07（素材表/模板） | 07 依赖「01-06 机制层完成」= 含 03 | 🔴 |
| 05 演出系统 | 08（翻译产物作编译器输入） | 08 依赖 05（演出编译） | 🔴 |
| 06 玩家身份 | 07（角色映射） | 07 依赖「01-06」= 含 06 | 🔴 |

**不是真死锁，是依赖写得太粗**：

- 03 自己已经分了「Phase 1 骨架 / Phase 2 全量」，只有**全量**才要 07 的素材表；骨架不要。
- 05 已经在 §63「StoryJson 源格式补充」里把字段表冻结了，08 照这张表生成即可——**接口已经先落地了，依赖声明没跟着更新**。
- 06 要的只是「角色 → StringId」这一张映射，而这张表 `gen_entity_maps.py` 已经生成好了（07b 实测覆盖 82.1%），不必等整个 07。

**要做的事**：把这三条依赖改细——「依赖 07 的素材表（**仅 Phase 2 全量**）」「依赖 05 §63 的源格式字段表（**已冻结**）」「依赖 `tools/entity_maps.py`（**已生成**）」。不改，工期排不出来，且执行的人会以为要干等。

### 2. 运行环境不在开发机上（要动手确认）

- 织丰锁死 **1.2.12**（硬不兼容，README 裁定 7）。
- 本机 `MB2_PATH` 指向的游戏实测是 **v1.5.2**（`bin/Win64_Shipping_Client/Version.xml`，注意比 CLAUDE.md 里记的 1.5.1 又新了一档）。
- 1.2.12 备份客户端在 `MB2_Version/MB2_1.2.12`，在。

**结论**：

- 第一批、第二批（01/02/11/13/16）**不碰织丰**，在 1.5.2 上开发验证完全没问题。
- **一进 Phase 2（07 开始）必须切到 1.2.12 客户端**。切之前先查那台客户端的 `Modules\LivingWorldNpcs` junction 还活着没（CLAUDE.md 发布章节的必选检查项），死链会导致游戏根本加载不到 mod，白排查半天。

### 3. 06 的开局选择器在 1.2.12 上必然走降级（口径要改）

- 06 打算用官方扩展点做「选时代 → 选角色」的开局面板。那个扩展点（`[StartOptionsProvider]` / worldscenarios）是 **1.5.1 才新增的**（Knowledge 有专文分析）。
- 但剧本工程的运行环境锁死 1.2.12 → **这个扩展点在运行环境里根本不存在**。
- 06 现在写的是「按其自身版本支持选用——支持就直接用，不支持走设置菜单兜底」。**实际情况没有「选用」空间，是 100% 走兜底。**

**要做的事**：二选一——① 承认第一版只做设置菜单兜底路径，把官方扩展点那条降为「织丰上 1.5.x 之后再补」；② 明确写「等织丰升版才做 06 的第一部件」。现在这个写法会让执行的人先去实现一条永远不会走到的路径。

**顺带**：06 里的数据源还写着旧表名 `骑砍2太阁Mod表_v1.xlsm.xlsx`（1960 人 × 125 列），实际事实源是 `骑砍2太阁Mod表.xlsx`（1049 行 × 122 列，07b 实测）。要改，否则按旧表名找不到文件。

### 4. 03 的路线选择卡在一个没做的盘点上（建议列为第二批第一件事）

- 03 自己写着：**路线 A（用现成场景库 + 参数化）还是路线 B（程序化搭景）**，由「第 1 步：场景库盘点」的结论决定。**这个盘点没做。**
- 下游已经在等：09b 里 `🔴待07织丰平原战场` / `🔴待07织丰山城战场` 就是等这个答案。
- **工作量很小**（数一遍织丰带了哪些场景、对照剧本要哪几类），但不做就定不了 03 的骨架长什么样。

### 5. 10 本能寺还是设计稿，不是可执行定义（Phase 3，暂不拦路）

- 10 只有 **55 行**，条件式里还留着没填的空：`(Clan::<明智家 Clan ID，待 07 核实>)`、`<玩家在京都附近 50f…写法待核>`。
- 对比 09b 桶狭间的可执行定义有 **1568 行**——差一个数量级。README 自己也标着「10 本能寺事件链骨架化待做（对照 09b 重写）」。
- **不拦开工**（Phase 3 才用到），但排期时要按「从零写一份 09b 级别的文档」估工时，不能按「改改 55 行」估。

---

## 三、已经很扎实、不用再补的部分

写出来是为了让审批人知道钱花在哪了、哪些地方可以放心。

| 板块 | 扎实到什么程度 |
|---|---|
| **DSL / 翻译链**（16 / 16a / 16b / 08 / 08b） | 全工程最硬的一块。1481 行语料 **100% 被裁定认领、零遗漏**，六道机器自检全过；16b 有 187 条逐词裁定 + 103 条组裁定；工具链闭环（生成器 → 翻译器 → 校验器），桶狭间 22 个事件已经产出实机 JSON 并 100% 通过覆盖率校验 |
| **05 演出系统** | 最厚的引擎件（426 行），复用现有 `StoryEngine` / `StageDirector`，把单一控制者纪律、语义槽位、朝向关系式、时间控制都写到了实现级；源格式字段表已冻结 |
| **02 战略AI纠正器** | 篇幅最短（113 行）但含金量高——写出了「保持交战 ≠ 改一次状态」（家族层每天掷骰会自己和谈掉，必须每天重施加）这类反直觉的坑，并点名了 `DeclareWarAction` / `SetPartyAiAction` / `DoNotMakeNewDecisions` 等具体 API |
| **03 程序化战斗** | 字段冻结表 + archetype 注册表 + 两条管线的完整 API 签名（`GameEntity.Instantiate` / `Mission.GetAgentBuildDataToSpawnTroop` 全参数列表）都写全了，只差第 4 条说的路线选择 |
| **12 验证与发布** | 有分拆测试总纲（先分子系统、后整机拼装）+ 四条红线检查的**可直接执行的命令** |
| **结构完备度** | 21 份 plan 几乎每份都齐「分步骤 / 验收 / 坑」三件套 |
| **织丰数据源** | 07b 刚核完：事件里点名的 923 个对象查得到 758 个 = 82.1%，桶狭间 100% 落地。够用 |

---

## 四、逐份评级

「能开工」= 现在就能派人动手；「补前置」= 要先做第二节列的事；「待重写」= 文档本身还不到可执行级。

| plan | 阶段 | 评级 | 说明 |
|---|---|---|---|
| 01 剧本引擎核心 | Phase 0 | ✅ **能开工** | 无前置，格式冻结有事实源，验收挂三条控制台指令 |
| 16 DSL 注册表 | Phase 1 | ✅ **已定稿** | 配套 16a/16b 生成物 + 自检全过 |
| 02 战略AI纠正器 | Phase 1 | ✅ 能开工（01 后） | |
| 11 存档与配置 | 同步 | ✅ 能开工（01 后） | 存档键登记表已被 16b 自检⑥ 反查过 |
| 13 主命与通用Quest | Phase 1 | ✅ 能开工（01 后） | 现状盘点已读代码确认，迁移分三步 |
| 03 程序化战斗 | Phase 1 骨架 | ⚠️ 补前置 | 场景库盘点（第 4 条） |
| 05 演出系统 | Phase 1 | ⚠️ 补前置 | 依赖声明要拆细（第 1 条），本体已可执行 |
| 08 转化管线 | Phase 1~3 | ⚠️ 补前置 | 同上；工具链本身已跑通 |
| 06 玩家身份与时代重置 | Phase 1 | ⚠️ 补前置 | 降级口径 + 表名（第 3 条），且几乎没点名 API |
| 04 体验设计 | Phase 1 | ⚠️ 补前置 | 依赖 13/05/03；本身是体验规格不是引擎件，无需 API |
| 17 功勋与晋升 | Phase 1 | ⚠️ 补前置 | 依赖链最长（13/05/01/03/06/16/04），排在 Phase 1 末尾 |
| 14 历史时间轴 | Phase 1/2 | ⚠️ 补前置 | 机制部分可跟 02 一起做，数据部分等 Phase 2 |
| 07 织丰剧本层落地 | Phase 2 | ⚠️ 补前置 | 自己写明「01-06 未通过前禁止实施」；开工时必须切 1.2.12 客户端 |
| 09 / 09b / 09c 桶狭间 | Phase 2 | ✅ 内容就绪 | 09b 1568 行可执行定义 + 09c 859 行；103 处 `🔴待07` 占位是**设计意图**（07 落地前 validator 报错阻断），不是缺陷 |
| 15 时间轴中间事件 | Phase 3 | ✅ 内容就绪 | 四个里程碑都有 condition + script 骨架 |
| 10 本能寺 | Phase 3 | 🔴 **待重写** | 55 行设计稿，条件式留空（第 5 条） |
| 12 验证与发布 | 全周期 | ✅ 能开工 | 贯穿始终，随时可用 |

---

## 五、建议的动作（按顺序）

1. **改闸门**：把「全部 plan 审核通过前禁止实施」改成按批次放行，先批 01。否则技术已就绪、流程在空转。
2. **改字（十分钟）**：拆细 03/05/06 的依赖声明（第 1 条）；更正 06 的数据源表名（第 3 条）。
3. **定口径**：06 的开局选择器认下「1.2.12 上只有兜底路径」（第 3 条）。
4. **开工第一批**：01 剧本引擎核心。
5. **并行做场景库盘点**（第 4 条），出结论后定 03 的 A/B 路线。
6. **进 Phase 2 之前**：确认 1.2.12 备份客户端的 junction 是活的（第 2 条）。
7. **Phase 3 之前**：把 10 本能寺按 09b 的规格重写一遍。

---

## 六、审批人四问（自查，铁律 21）

- **结论是什么？** 能开工，分三批；第一批（01）今天就能动；开工前补 5 件事，其中 3 件是改字。
- **凭什么？** 21 份 plan 全文实读；依赖图逐条对撞查出三处循环；环境版本、注册表自检、覆盖率数字均为脚本实测。
- **代价是什么？** 要放弃「全部审完再动手」的闸门，改成分批放行；10 本能寺要按 09b 规格重写一份（工作量按新写一份文档估）。
- **要做哪几件事？** 第五节的七步。

---

## 七、本轮已经做完的（2026-08-27 ~ 08-28）

四件交付 + 一个收尾修复。**全部产物已落盘，数字均为脚本实测。**

### 1️⃣ 完善 16a DSL 翻译总表 ✅

- **产物**：`16a-DSL翻译总表.csv`，**1481 行 × 13 列**，重新生成（不是手改）。
- **口径**：太阁全量语料 1481 个词条 **认领 1481、未认领 0**——一条不漏。
- **分档**：T0=24 ／ T1=407 ／ T2=162 ／ T3=860 ／ T3-预留=28。
- **裁定来源**：`16b-落点裁定表.md` 的 187 条逐词裁定 + 103 条组裁定。
- **生成链**：`gen_registry_tables.py`（改映射/规则）→ `build_registry_csv.py`（重跑）→ CSV。**六道机器自检全过，不绿直接 exit(1)。**

### 2️⃣ 优化 08b 转化管线 + 产出桶狭间 JSON ✅

- **核实**：文中六个实测数字逐个复跑核对，属实。
- **修了两个生成器缺陷**（已写成纪律 24 / 25 固化）：
  - 纪律 24：生成器必须确定性——同输入两次重跑必须一字不差（原来 `set` 的迭代序会流进产物，重跑 diff 一片红，没法复核）。
  - 纪律 25：校验器必须能吃自己的生成物且跨文件合池（原来喂 `.jsonc` 会「声明源事件：空 → 通过」，**空转报绿**）。
- **更正**：§九 两条被数据推翻的旧表述（见 07b §六）。
- **实机产物**（`story_event_json/okehazama/`）：

  | 文件 | 规模 | 是什么 |
  |---|---:|---|
  | `events.jsonc` | 2379 行 | 事件骨架（触发/条件/步骤），引擎实读 |
  | `story.jsonc` | 1512 行 | 人读合并版台本（按历史聚类、时间序） |
  | `story/*.jsonc` | 38 个 | 演绎剧本分件 |
  | `i18n/std_scn_okehazama.xml` | 561 行 | 本地化 |
  | `report_okehazama.txt` | 340 行 | 覆盖报告 |

- **验收**：`verify_tk5_coverage.py` **通过（无缺失）**——21 个事件、源演出行全部有落点。
- **待注册 35 条**（去重）：28 条参数溢出（军团编成等动作的参数位超出注册表已定义范围 → 等 02 战略层）+ 7 条实体（茶屋 / 左大臣 / 從一位 → 等 07 数据包）。产物里 `🔴待注册` 计数 = 0，即没有靠占位凑数。

### 3️⃣ 解读织丰表、核对 StringId 覆盖 ✅

- **产物**：`07b-织丰表解读-StringId覆盖率.md`（126 行）。
- **结论**：事件里点名的 **923 个对象查得到 758 个 = 82.1%**，够开工。
- **修了两处测量法失真**：「势力 0%」是查错了表（该查 Kingdom 不是别处）→ 实为 82.1%；「地方 0%」是同层异名（太阁用现代地方名、织丰用古代八道）→ 不是缺数据。
- **清点 15 张工作表**，指出 3 张该用没用（ForceTaikou 204 条 / Culture 21 条 / BaseInfo）。
- **作废两条旧结论 + 一条证据不成立**（详见 07b §六）。

### 4️⃣ 开工评审 ✅

- 本文档（§一 ~ §六）+ README 文档索引补上 `00` 和 `07b` 两行。

### 5️⃣ 收尾修复：占位据点的锚点查不到

- **症状**：报告里鸣海馆 / 二條館显示「锚点（无）」——事件不知道该在地图哪一带发生。
- **根因**：据点表注册了变体名（鳴海城 / 鳴海館 / 鳴海之町），锚点表只注册正名。**实测键覆盖率仅 20.7%。**
- **修法**：改 `gen_entity_maps.py`，让两张表挂同一套查找键（铁律 22：改生成器不改产物）→ 重跑 → **键覆盖 100%**。
- **复核**：74 座占位城，66 座有锚点、8 座连锚点都没有（这 8 座进下面 TODO）。

---

## 八、接下来要做的 TODO

按「拦不拦开工」排序。**A 组不做排不出工期，B/C 组可以边做边补。**

### A 组：开工前必须处理（= 第二节的 5 件事）

| # | 做什么 | 工作量 | 卡住谁 |
|---|---|---|---|
| A1 | **改闸门**：把「全部 plan 审核通过前禁止实施」改成按批次放行，先批 01 | 一句话 | 全部——技术已就绪，卡在流程 |
| A2 | **拆细三处循环依赖**：03「依赖 07 素材表（仅 Phase 2 全量）」／05「依赖 05 §63 字段表（已冻结）」／06「依赖 `tools/entity_maps.py`（已生成）」 | 十分钟改字 | 03 / 05 / 06 排期 |
| A3 | **定 06 的降级口径**：`[StartOptionsProvider]` 是 1.5.1 新增、运行环境锁 1.2.12 → 100% 走设置菜单兜底。二选一：承认第一版只做兜底，或明确写「等织丰升版再做」 | 半小时 | 06；不改会让人去实现一条永远走不到的路径 |
| A4 | **更正 06 的数据源表名**：`骑砍2太阁Mod表_v1.xlsm.xlsx`（1960×125）→ `骑砍2太阁Mod表.xlsx`（1049×122） | 改字 | 06；按旧名找不到文件 |
| A5 | **做场景库盘点**：数一遍织丰带了哪些场景、对照剧本要哪几类 → 定 03 走路线 A（现成场景参数化）还是 B（程序化搭景） | 半天 | 03 骨架；09b 的 `🔴待07织丰平原战场` / `山城战场` 在等 |

### B 组：数据补齐（都是填表，不是找数据）

| # | 补什么 | 现状 | 工作量 |
|---|---|---|---|
| B1 | **地方名对照表**（9 行） | 太阁用现代地方名、织丰 Culture 用古代八道，两边都有数据只是叫法不同 | 十分钟 |
| B2 | **组织 StringId**（37 个） | ForceTaikou 表 204 条已 100% 覆盖，只缺 StringId 这一列 | 一小时 |
| B3 | **8 座无锚点的城** | 出石城 / 白鹿城 / 三原城 / 山口城 / 宇和岛城 / 久留米城 / 八代城 / 佐伯城——既没 Match 也没 Near，不知道摆地图哪 | 要查史料，07 数据包连位置一起定 |
| B4 | **3 个太阁独有角色** | 服部小平太 / 毛利新介 / 簗田政纲（桶狭间关键配角），现走 `TK5_ONLY_HERO` 占位 | 每人一条 Hero XML |
| B5 | **3 个 XML 里查不到的 ID** | `lord_1_kobayakawa_takakage` / `spc_suruga_shu_leader_1` / `spc_suruga_shu_leader_2` | 07 数据包 |
| B6 | **35 条待注册裁决** | 28 条参数溢出等 02 定义动作参数位；7 条实体等 07 数据包 | 逐条人工裁决，禁止猜译 |

> 注：报告里「势力 95 个 XML 查不到」**不是缺口**——那是 `IsShokuho=0` 的未启用预备数据，要用打开开关即可（07b §4.1）。

### C 组：文档欠账

| # | 改哪 | 改什么 |
|---|---|---|
| C1 | **10 本能寺** | 55 行设计稿 → 按 09b（1568 行）规格重写；条件式里还留着 `<明智家 Clan ID，待 07 核实>` 这类空。**排期按「新写一份文档」估，不是「改改 55 行」** |
| C2 | **README 待办段** | 三个数字已过时：`tk5_to_json.py v4.6` → **v5**；`572 演出行` → **573**；`待注册 269 条逐条裁决` → **去重 35 条**。另 README 审核表写「桶狭间 **22** 事件」→ 实际 **21** 个 |
| C3 | **09b lines[] 实体化 / 09c 八个事件 JSON 骨架** | README 自列待办，未动 |
| C4 | **动作统一管理独立 plan** | README 自列待办，未动 |
| C5 | **16b §十二 六个待核实事项** | 含 `spawn_clan` / `make_alliance` / `global_set` 三个动作的实现待核实 |
| C6 | **CLAUDE.md 版本记录** | 记的是 v1.5.1，本机实测 **v1.5.2** |

---

## 九、下一步动作（一句话版）

**批 01 开工** → 同时做 A2/A3/A4 的改字（半小时内可完）→ 并行做 A5 场景库盘点 → B 组填表随时可插 → Phase 2 之前确认 1.2.12 客户端 junction → Phase 3 之前重写 10。
