# 战国无双换装批量落地（28 张脸 + 28 套甲 + 28 件武器 ｜ 兵种线：23 套甲 + 15 顶笠 + 6 件通用武器）

> ## 🔴🔴 核心原则（2026-09-15 用户裁定，开工前必读）
>
> **① 1.2.12 的 `Modules\TifaHead2` = 编辑器导入 + tpac 导出，只此两用。**
> 源资产 `TifaHead2\AssetSources\sw2\<角色>\` → 编辑器导入 → Publish →
> **导出的包是白编译，必须二次处理**（`install_pack.py`：补 morph 帧 + 刷材质配方）→
> **处理完放进 `Modules\Taikou`**（游戏加载的是 Taikou）。判据：`morphinfo` 的 `VertexKeyCount`
> **101 = 已处理 / 60 = 白编译**。⚠️ `install_pack.py` 默认装到 TifaHead2，
> 装 Taikou 必须显式 `--clients "<...>Modules\Taikou"`。
>
> **② 换头照抄织田信长：每个换头角色一个独立 race（一人一 race）。**
> 新增角色 = `SPECIAL_RACE` 加一行，不需要重新论证设计。
>
> **③ 已经跑通的数据表不许乱改**（`csv/` 下那些）——改表先说清"改哪格、为什么、影响谁"，用户裁定后再动。

> ## 🔴🔴 2026-09-15 深夜（第二轮）会话交接 —— **验收入口，先读这一节**
>
> ### 一、本轮修了什么（都已 stage，等你 ModKit 重导）
>
> | # | 问题 | 根因 | 修法 | 判据 |
> |---|---|---|---|---|
> | 1 | **嘴巴开花 + 眼睛没有眼球**（28 人全中） | **件位顺序反了** | `parts_table`：`face,eye,mouth` → **`face,mouth,eye`** | 见下 §二① |
> | 2 | **下半身短、像小矮人**（28 套甲） | **沿骨轴缩放 λ 只用在手臂**，腿一根没管 | `build_armor.py` 加 `LEG_ALONG`（大腿 λ=0.762 / 小腿 λ=0.706）| 见下 §二② |
> | 3 | 颏带跑到后脑 / 兜钵缺失 / 金前立缺失 / 月牙背面没画 | 见 §一 各组条目 | 已修并重出（7 顶兜 + 长政 + 忠胜）| 各自条目里有 |
> | 4 | 资源进游戏 | — | `install_pack.py` 装进 `Taikou\AssetPackages\pack0.tpac`（121 MB）| `VertexKeyCount=101` |
> | 5 | race / 装备 / 登场 接线 | — | 28/28 全绿（见下表）| 对账脚本 |
>
> ### 二、两个根因（**已写进代码注释，别再踩**）
>
> **① 引擎按【子网格位置】认脸部件，不是按材质名**（`Knowledge/蒂法换头工程.md` §13.7 ①）
>
> | | 嘴 | 眼 | |
> |---|---|---|---|
> | 原版女头 `head_female_a` | `.2`(242v) | `.6`(98v) | ✅ 嘴在眼前 |
> | 蒂法 `head_tifa_a`（已知能用） | `.1`(1594v) | `.3` | ✅ 嘴在眼前 |
> | **战无2（原来）** | `.2`(137v) | **`.1`(38v)** | ❌ **反了** |
>
> ⇒ 眼球被当嘴、嘴被当眼 → 「嘴巴开花 + 没有眼球」。**与性别无关**（男头布局也救不了，28 人全中）。
> 女头完整布局是 4 件（脸/嘴/眼/睫），战无2 源模型没有独立睫毛件 → 仍出 3 件（脸/嘴/眼）。
>
> **② `build_armor.py` 注释第 14 行写着「比例差由 λ 逐骨承担」，但代码只对 4 根手臂骨应用**
>
> 大腿：源 45.63cm × R(0.0120) = **0.548m**，骑砍大腿只有 **0.417m** → 差 31% = 小矮人。
> 每根骨的 λ 代码早就算好了（`bl/(R*sl)` 钳 [0.5,2.0]），只是没用在腿上。
>
> ### 三、待办（按优先级）
>
> | # | 做什么 | 怎么做 |
> |---|---|---|
> | **N1** | 🔴 **ModKit 重导 + Publish** | 28 张脸 + 28 套甲 + 8 顶兜，全在 `TifaHead2\AssetSources\sw2\<角色>\` |
> | **N2** | 🔴 **补 morph 帧** | `python tools/face-pipeline/scripts/install_pack.py --module "<TifaHead2>" --filter head_ --clients "<Taikou>"` |
> | **N3** | 🔴 **蒂法 / 萨菲罗斯 形变通道重搬** | 帧数够（101）但**帧序错**：判据 = 逐帧位移签名 **f1 应纯 X（脸宽）、f2 应纯 Y（脸深）**；蒂法 f2 **全零** → 滑块一拉脸就乱。修法 = 重跑 `transfer_channels.py` |
> | **N4** | **谦信的金属兜**（旧 T7） | 静态网格，重定向压成尖刺；未解 |
> | **N5** | **对账 B 类 2802 条** | `Scripts/check_taikou_era_vs_log.py`：CSV 的立场/据点/组织与上级日志不一致（疑口径差异，如"家族居城 vs 本人所在地"）——**等用户定口径**，不许自己拍 |
> | **N6** | **「城 + 立场=其他」的身份词** | 阿市/归蝶/稻姬/訚千代/宁宁 这类"住在城里的女性"没有合法身份词；现按**留空**处理（checker 明确允许空身份），要补词再议 |
> | **N7** | 蒂法包仍是**白编译**（60 帧） | 只影响她的编辑器工程；游戏包里已是 101 |
>
> ### 四、本轮新增工具
>
> | 脚本 | 干什么 |
> |---|---|
> | `Scripts/gen_taikou_hero_era_cols.py` | **年代列写出方**（原仓库里缺失的那一环）：从上级日志填 `Appear_/Identity_/CareerStance_/Kingdom_/City_<年>` |
> | `Scripts/check_taikou_era_vs_log.py` | **CSV ↔ 上级日志对账**（常驻）：A 漏填 / B 冲突 / C 无行 / D 日志侧 PUA 缺字 |
>
> ### 五、本轮踩的坑（别重犯）
>
> 1. 🔴 **读派生数据当事实**：我曾说「阿国六代都不登场」，依据只是 **CSV 年代列空** —— 而**日志里她六代都在**。
>    ⇒ **任何"谁登场 / 谁在哪"的结论必须跑 `check_taikou_era_vs_log.py`，不许只读 CSV。**
> 2. 🔴 **把"装备档"当成"是否存在"**：`Identity_<年>` 只用来挑默认装备（`EQUIP_BY_IDENTITY.get(ident, EQUIP_DEFAULT)`），
>    **不决定建不建这个人**；checker 也明说「身份为空 = 合法，跳过检查」。推不出身份 → **留空**，绝不能连 `Appear_/City_` 一起不填。
> 3. 🔴 **编辑器/游戏模式 bat 在客户端根，不在游戏根**：`MB2_Version\MB2_1.2.12\to_game_mode.bat`（游戏目录的**上一层**）。
>    它一次管 **4 件**：`Bannerlord.MBOptionScreen` + `Bannerlord.UIExtenderEx` 的 `SubModule.xml`，以及 TifaHead/TifaHead2/Taikou 各自的 bat。
>    **只跑模块级那个会漏掉 MCM 那两项。**
> 4. 🔴 **空 `Assets\` 遮蔽**：模块里留一个空 `Assets\` 目录 → 引擎按 `Assets → AssetPackages → …` 取**第一个存在的** → 整个 `.tpac` 被绕过。
>    症状：跑了装机、游戏里什么都没变。
> 5. **install_pack 是整包替换**：装之前先备份目标模块的 `pack0.tpac`（**备份放模块外**，编辑器 Publish 会清 `AssetPackages\`）。

> ## 🔴 2026-09-15 深夜 会话交接（第一轮，**已被上一节取代**）
>
> ### 零、🔴 进度快照（**先看这张表再往下读**）
>
> **用户报的 24 项 → 已实机验收的进度**：
>
> | 人 | 报的问题 | 状态 | 实机验收 |
> |---|---|---|---|
> | 德川家康 | 下巴带子 | ✅ 修好 | ✅ 用户："下巴好了" |
> | 明智光秀 | 头发残缺 | ✅ 修好 | ✅ 用户："头发好了" |
> | 服部半藏 | 半张脸消失 | ✅ 修好 | ✅ 用户："自带面罩的脸好了" |
> | 佐佐木小次郎 | 发绳消失 | ✅ 修好（金发环 = `idx4`，**挑错过一次**）| ✅ 用户："佐佐木修好了" |
> | 稻姬 | 发绳消失 | ✅ 修好（发绳 `idx4` + 额环 `idx5`，**挑错过一次**）| ✅ 用户确认 |
> | 杂贺孙一 | 发绳消失 | ✅ 修好（**无独立发绳，是发尾根上的绿头绳**）+ 导入报错 + 单面板 + 无脖子 | ⏳ 待重导 |
> | 风魔小太郎 | 耳环挂坠消失 | ❓ **无进展 —— 缺线索**（见 T13）| ⏳ 用户报"头发没好"，但我量不出差异 |
> | 归蝶 / 小太郎 / 岛津 / 阿国 | 头饰/耳环消失 | ⏳ 归蝶·小太郎 已补件；**阿国未做**（T14）| —— |
> | 谦信 / 政宗 / 秀吉 / 訚千代 / 三成 / 长政 | 头盔连到铠甲 | ❌ **未做**（T3）| —— |
> | 家康之外的 5 人 | 下巴带子 | ❌ **方案试过并回退**（T4）| —— |
>
> **当前 staged、等用户重导的**：`L02_nobunaga` `L09_magoichi` `L13_hanzo`（三个人的头）。
> **本轮的通用改动**：`build_head.py` 1.2/1.3 判据（绑头骨豁免 + 逐碎片）+ **1.7 薄片补背面**（新增）。
>
> ---
>
> 🔴 **另有两条线并行，别和上面 28 武将混起来**：
> - **兵种线**（2026-09-15 晚新开）→ **见 §三之四**：23 套甲 + 15 顶笠 + 6 件通用武器，**资产已就绪、游戏数据零改动**，等 ModKit 导入
> - **英雄/势力数据线**（会话外进行中，非本次产物）：`TaikouHero.csv` / `spclans*.xml` / `taikou_heroes*.xml` / `taikou_lords*.xml` 有未提交改动，`run_all_checks` 有 **5 条红**（英雄文化/画像/选人目录系列）→ 重跑那几个英雄生成器即可
>
> ### 一、🔴 视觉验收结论：用户报的 **24 项问题全数属实**
>
> **验收方法**：不看代码猜——从**产物 FBX 本身**渲染（贴图版看颜色 / 纯灰版看几何），**源模型同机位对照**，
> 再用 28 份构建日志做顶点数对账。工具 `Debug/offline/_audit_batch.py` + `_audit_manifest.py`，产物 `_audit_*.png`，可重跑。
>
> #### 1. 头（15 项，全部属实）
>
> | 报的问题 | 人 | 硬证据 |
> |---|---|---|
> | 下巴带子需放头盔 | 家康 / 秀吉 / 兼续 / 政宗 / 长政 / 忠胜 | **秀吉挑件表里没有 `strap`**，从没处理过；其余 5 人只删了耳侧碎片（51/40/32/26/22 顶点），**下巴条几何不动、只改 UV** |
> | 半张脸消失 | 半藏 | `剔非头部 submesh_5 丢 165/320`（52%）。脸壳只剩 **202 面**（他人 500~900）；切嘴退化到"高度比例"兜底（18 面，正常 106~158）|
> | 发绳消失 | 稻姬 / 佐佐木 / 杂贺 | 源图有粉紫发绳 + 金额环、头顶金发环，产出头全无。佐佐木被删的 8 顶点 = 注释里那"2 个小金件" |
> | 耳环挂坠消失 | 风魔小太郎 | `剔非头部 submesh_0 丢 207/335`（62%）。源图双耳金钩 + 蓝流苏挂坠极醒目，产出头一个没有 |
> | 头发残缺 | 光秀 / 岛津 | 光秀 **687/801（86%）+ 216/324（67%）**；岛津 216/682（32%）|
> | 头饰消失 | 浓姬 / 阿国 | 浓姬 `idx11=蝴蝶+珠串+耳饰` **压根没进 `hair` 列**（表里只有 `hair=[6]`）；阿国 137/432（32%）+ 38/98（39%）|
>
> #### 2. 兜 / 甲（9 项，全部属实）
>
> | 报的问题 | 人 | 渲染所见 |
> |---|---|---|
> | 头盔被连到铠甲 | 谦信 / 政宗 / 秀吉 / 訚千代 / 半藏 / 三成 / 长政 | **7/7 全中**。政宗＝整顶兜 + 大金月牙；谦信＝银兜 + 白布；秀吉＝**日轮盘 + 双翼饰**；訚千代 / 三成＝金前立；半藏＝角刺；长政＝尖顶兜 |
> | 头饰被连到铠甲 | 稻姬 / 浓姬 | 稻姬肩上悬金环带；浓姬肩上悬**整团黑发髻**（`idx6`"身体 + 发髻"复合件被整块收进甲）|
>
> #### 3. 根因只有两个
>
> **根因 A ——「剔非头部」判据删过头（17/28 人触发）**
> 判据是"顶点主导骨离面部骨族中心远就删"。头发 / 耳环 / 发绳 / 头饰的件**主导骨本来就不在面部骨族里** → 被整块当杂质删掉。
> **9 人的"消失"类问题全落在这条上。**
>
> **根因 B —— 挑件表漏件 / `helmet` 列留空**
> - 浓姬 `idx11`、岛津 `idx4`（头顶毛簇）、稻姬 `idx10`（头后锥形条）—— **表里没列**
> - 秀吉 `strap` 缺失（同类 5 人都有）
> - 🔴 **谦信 `helmet=[]` 是空的**，但 [parts_table.py:5](../tools/sw2-pipeline/parts_table.py#L5) 自己的注释写着"9 人戴盔"含谦信 → 兜没标成兜 → 普查判"甲件" → 收进甲
>
> **甲侧机制**：普查把兜判成"甲件"，`build_armors.py` 的 `EXCLUDE` 只点名 3 人（秀吉 `idx0` / 兼续 / 光秀），**其余 6 人漏网**；
> 秀吉即使排除了 `idx0`，`idx7`（日轮盘 + 双翼饰）仍被判"甲件"留在甲上。
>
> #### 4. 顺手抓到、用户没报的 3 个
>
> 1. **长政 / 忠胜的兜：搬过去的耳侧颏带碎片悬空脱离兜体**（渲染图里兜下方飘着一个断开的 V 形环）
> 2. **稻姬 / 佐佐木头顶有游离碎块**（后侧视角可见脱离头发的黑色孤块）
> 3. 谦信与 parts_table 表头注释自相矛盾（见根因 B 最后一条）
>
> ### 二、上一轮（09-15 晚）做完的 4 件事
>
> | # | 做了什么 | 产物 | 判据 |
> |---|---|---|---|
> | 1 | **光秀头**：补漏件 + 封发块底口 | `Debug/offline/sw2_build/L03_mitsuhide/` | 面数 **1112** |
> | 2 | **慶次头**：清掉混进头上的甲片 | `.../L01_keiji/` | ✅ **2026-09-15 用户实机验收通过**。面数 **1074**；头包围盒 1.83m → **0.42m**。→ **这是 `hard=True` 路线（混装件走硬判据）的首个验收先例，后面同类照他做** |
> | 3 | **颏带 5 人**（家康/政宗/忠胜/长政/兼续）：耳侧碎片摘到兜、下巴条改 UV | 头 + `taikou_<名>_helmet_a` | 头删顶点数 = 兜加顶点数（32/51/40/26/22）|
> | 4 | **光秀甲**：剔掉混进去的头发/头 | `tools/armor-pipeline/out/taikou_mitsuhide_do_a.*` | 甲顶上不再有"假头" |
>
> **以上产物都已 stage 到 `MB2_1.2.12\...\TifaHead2\AssetSources\sw2\<角色>\`。**
> 🔴 **用户验收结果**：慶次头 ✅ 通过；**颏带 5 人的头 + 5 座兜 ❌ 否掉**（下巴上的带子照旧看得见，见 §一）。
>
> ### 三、待办（按优先级，照这个顺序做）
>
> | # | 做什么 | 怎么做 |
> |---|---|---|
> | ~~**T1**~~ | ~~修「剔非头部」判据~~ | ✅ **已做完**（2026-09-15 深夜）。实际做法见 §三之二 的三条判据改动 —— **不是**原计划的"hair 整件免检"，而是 ①绑头骨的碎片一律豁免 ②逐碎片判 ③有头骨保护时跳过 90% 安全网。全量 28 人重跑：**10 人改善 / 0 人回归**，慶次逐位不变 |
> | ~~**T2**~~ | ~~补挑件表 5 处~~ | ⚠️ **部分做完**：归蝶 `+[11]` ✅ / 稻姬 改 `[8,4,5]` ✅ / 佐佐木 `+[4]` ✅ / 小太郎 `+[10]` ✅。**谦信兜（`helmet=[]`）做不出来已回退**（他的兜是无蒙皮静态网格，见 T7）；**秀吉 `strap` 仍未补** |
> | **T3** | 🔴 **甲侧排除改自动** | `build_armors.py` 的 `EXCLUDE` 从"点名 3 人"改成**按普查 verdict 全自动**：命中兜 / 头饰关键词的件一律排除，废掉人名白名单 |
> | **T4** | **颏带归属重做** | 🔴 **2026-09-15 深夜试过"整条剪进兜"，实测失败已回退**。证据：① 剪掉当场在下巴开黑腔（`holes_fill` 补不上，那是下颌壳的背面开口）② 并进兜的那批顶点渲染出来是**肉色** —— `bone_59` 覆盖的是**下颌皮面本身**，不是带子。⇒ 这条带子是**画在下颌皮面上的贴图**，几何上它就是下巴；"搬到兜上"无解。**当前状态 = 只改 UV（几何不动）**，用户实机看着下巴是干净的 |
> | **T5** | **兜上带子悬空**（长政 / 忠胜）| 搬进兜的碎片没贴到兜体，见 §一.4 第 1 条 |
> | **T6** | **颈部侧面棋盘格**（仍未解）| 已证明**原构建就有**、非本轮引入。下一步：源模型同角度渲染对照 |
> | **T7** | **谦信的兜**（新，已定位做不出来）| 兜件 = `idx4`（submesh_0...0000.001，242 顶点，碗+双角+耳庇+垂带）。🔴 它是**无蒙皮的静态网格**（一个顶点组都没有），走兜管线连踩三坑：① `--keep-head-frags` 按主导骨留件 → 主导骨为空 → 整块删光 ② 清 `matrix_world` → 散架 ③ 补 bone_11 权重 + 烘到骨架空间后不再散架，**但重定向把它压成尖刺状**。⇒ 下一轮：查 retarget 对"整件单骨"的处理，或改走 head 管线 |
> | **T8** | ~~兜的贴图告警~~ | ✅ **已修**：`build_helmets.py` 的贴图调用把**脸壳件也算进 UV 范围**（颏带的 UV 画在脸壳图集区）。实测家康的兜带子原来是**肉色**，现在正常 |
> | **T9** | **武器 28 件**（未开工）| `TaikouHero.csv`「武器」列 + `gen_armor_items.py --kind weapon` |
> | T15 | **披风（D）/ 上臂布袖（C7）** | 上臂中段露身体是第一件就挂着的欠账，见本文 §四 阶段 3「已知欠账」|
>
> **T10~T14 见 §三之三**（本轮新增：双面重建 / 脖子普及 / build_neck 通用化 / 风魔 / 阿国）
>
> **作废（已被本轮验收取代，别再照做）**：
> - 旧 T1 "等用户验收 5 人的头 + 5 座兜" → 用户已验收，**产出否掉**（颏带仍在头上）
> - 旧 T4 "5 人甲片混进脸壳（秀吉/义弘/小太郎/武藏/半藏）" → 并入 **T1**（同一根因，同一处判据）
> - 旧 T5 "其余人有没有颏带/漏件" → 已全查完，结论见 §一
> - 旧 T7 "头盔返工：半藏/秀吉/政宗/忠胜" → 并入 **T3 + T2**（问题不在返工，在判据与挑件表）
>
> ### 三之二、🔴 缺件 / 小装饰的**通用查法**（2026-09-15 深夜杂贺复盘 —— 修其他人照这个查）
>
> **一句话**：战无2 的**发绳 / 发环 / 头绳 / 头饰这类小装饰，不是一块独立的件**，
> 而是**混在某个大复合件里的一小撮顶点**。所以"补件"这个动作本身就不成立 ——
> 正确动作是**先定位、再让判据把它留下、把同件的身体部分筛掉**。
>
> **代价**：本轮在稻姬、佐佐木、杂贺身上**各栽了一次**，三次都是拿"漏件图里看着像"直接挑件。
> 那个方法不可靠：稻姬挑成了"从胸口垂到头顶的长锥"（头变成 0.70 米高、跟头断开飘着）、
> 佐佐木挑成了"束发筒"（不是金环）。
>
> #### 五步查法（按顺序做，别跳）
>
> | # | 查什么 | 怎么做 | 看出来什么 |
> |---|---|---|---|
> | 1 | **装饰在哪块件里** | 逐件扫：**哪块件里有「绑 `bone_10`/`bone_11` 的小碎片」** | 一串 `bone_11×N(z=…)`，N 小、z 在头顶附近 = 候选 |
> | 2 | **确认身份** | 把该块**单独渲染**（`_audit_batch.py` 的 `only`）——**必须看图，不许猜** | 是绳 / 是环 / 是管，一眼可辨 |
> | 3 | **它有没有被删** | 看构建日志的 `剔非头部` / `清碎片` 行 | 「删掉的顶点数 ≈ 装饰的顶点数」= 中招 |
> | 4 | **同件混了什么** | 该件的碎片清单：逐块看骨名 + 尺寸 + 位置 | 手臂/身体/甲片各占多少 → 决定判据怎么收 |
> | 5 | **是普遍问题还是个例** | 换个量法**批量跑 28 人** | 见下面的实测表，一眼看出谁偏短 |
>
> #### 三条已固化的判据改动（都在 `build_head.py`，**通用**，不是给某人的补丁）
>
> | 改动 | 治什么 | 关键点 |
> |---|---|---|
> | **1.2 `prune_far` + 1.3 `keep_head_only`：绑头骨的碎片一律豁免** | "头发/装饰被当杂质删掉" | `keep_bones={bone_10,bone_11}`。已两次踩实：杂贺脸壳件被 `prune_far` 删 152 顶点、半藏的布头罩被 1.3 删 165 顶点 |
> | **1.3 逐碎片判，不再逐顶点判** | "整片头发被掏出洞" | 源模型是**碎片云**（不焊顶点），逐顶点会在头发中间开洞（光秀实测） |
> | **1.3 有头骨保护碎片时跳过 90% 安全网** | "判据明明对，却被比例阈值挡了" | 安全网是给"距离判据不可靠"设的；"绑头骨就是头"是确定事实 |
>
> #### 杂贺身上还捞出两个**管线级**的坑（已修，别人可能也中）
>
> | 坑 | 症状 | 修法 |
> |---|---|---|
> | **`.001` 后缀被吞** | `submesh_0` 与 `submesh_0.001` 解析成同一个号 → 选件选错 | `build_armor.py` / `build_textures.py` 加 `--parts-name`（按**精确网格名**选件）|
> | **`is_junk` 按材质名否决人工标注** | 材质名带 `mat_w_` 的件被当武器排掉（谦信的兜、杂贺的件都中过）| **精确点名的件不过 `is_junk`** —— 人已确认的东西，机器不该推翻 |
>
> #### 实测数据（批量跑出来的，修别人时直接查表）
>
> **脸壳件在「眼睛以下」延伸多长**（= 能做出来的脖子长度；短了就是个悬空的头）：
>
> | 够用 | 偏短 |
> |---|---|
> | 岛津 36.2 · 小太郎 22.9 · 半藏 22.6 · 佐佐木 20.8 · **信长 19.2（基准）** | 光秀 17.4 · 家康 16.1 · **杂贺 12.2（最短，已修）** |
>
> **修法**（杂贺已落地，别人照抄）：`parts_table` 加 `neck=[身体件idx]` + `neck_r`（排除肩甲/手臂）+ `neck_z0`
> （= **目标空间 z=1.44 换算回源坐标**的值，防止脖子拖到胸口穿进身体）。
> 换算：`neck_z0 = 眼源z − (1.6839 − 1.44) / 缩放`。
>
> #### ⚠️ 一条关于**验收手段**的硬教训
>
> **离线渲染只对「某块几何在不在」可信，对「像不像人」完全不可信。**
> 证据：**已通过实机验收的信长和慶次**，用同一套脚本渲出来**一样是歪脸、鼓眼、长鼻子的鬼样**。
> ⇒ 比例 / 美观 / 朝向类判断**只能实机**；别再拿离线渲染图当"视觉验收"报给用户。
>
> ### 三之三、本轮新增待办
>
> | # | 做什么 | 怎么做 |
> |---|---|---|
> | **T10** | 🔴 **双面改动后 28 人全部重建** | `build_head.py` 1.7「薄片补背面」是**通用**的（判据 = 连通域边界边比例 ≥ 0.5，复制+翻面）。已验三人（信长 +105 面 / 杂贺 +68 / 半藏 +14）。用户实机确认"有了"之后跑全量 |
> | **T11** | **脖子普及** | 家康 16.1、光秀 17.4 也偏短。杂贺那套 `neck=` 数据照抄一行即可 —— 但**先看杂贺实机效果对不对**再推 |
> | **T12** | **`build_neck.py` 通用化**（慢活）| 管线里有给蒂法做的"脖子+领口+抄权重"完整方案，**路径全写死不能直接复用**。要参数化成 `build_head.py` 的一步。**这是新功能，未验证不许塞进实机批次** |
> | **T13** | **风魔小太郎** | 用户报"头发没好"，但我量遍了他所有环节：脸壳被删的 152 顶点绑 `bone_18`、**离头 1 米**（是真该清的远处碎片，不是头发）；头发件 363 顶点没被误删；改前改后**逐项一样**。**缺线索，等用户指方向** |
> | **T14** | **补件类：阿国** | 她的头饰在 **`idx0`（1262 顶点的身体件）** 里 → 照三之二的五步查法做（要拆件，不能整块并入）|
>
> ### 三之四、🔴 兵种线（新开，2026-09-15 晚）—— 23 套甲 + 15 顶笠 + 6 件通用武器
>
> **一句话**：把战无2 的 17 个兵种 + 6 个护卫（23 个模型）做成 Taikou 兵种能穿的通用装备。
> **和 28 武将那批的最大区别**：**不需要新 race、不需要换头、不需要 morph 通道**，只走甲管线 —— 便宜得多。
>
> #### 资产已就绪（游戏数据零改动）
>
> | 产出 | 数量 | 命名 |
> |---|---|---|
> | 通用武器 长枪/薙刀/打刀/忍刀/弓/铁炮 | 6 | `taikou_troop_<x>_weapon_a` |
> | 甲 | 23 | `taikou_troop_<x>_do_a` |
> | 笠 / 兜 | 15 | `taikou_troop_<x>_helmet_a` |
>
> 全部已归拢到 `TifaHead2\AssetSources\sw2\troop\`（**44 件 × 4 文件 = 176 个**），**等 ModKit 导入 + Publish**。
>
> #### 工具（都已加 `--set troops`）
>
> | 工具 | 作用 |
> |---|---|
> | `run_census.py --set troops` | 23 个模型的骨普查 |
> | `troop_parts_table.py`（新） | 兵种挑件表：**兜在哪个对象**（逐对象渲染实测得出） |
> | `build_armors.py / build_helmets.py --set troops` | 出甲 / 出笠 |
> | `stage_for_import.py --set troops` | 归拢到 ModKit 导入根 |
> | `gen_troop_weapon_items.py`（新） | 武器物品登记；`--purge` 一键撤回 |
> | `Debug/offline/_troop_part_probe.py` | 逐对象头部取景（查"兜在哪个对象"专用） |
>
> #### 待办
>
> | # | 做什么 | 怎么做 |
> |---|---|---|
> | **T20** | **ModKit 导入 + 导出 44 件资产** | 导入 `sw2\troop\` → 资产名照文件名 → Publish。武器是**静态网格**，材质别勾 `Skinning`；甲/笠要勾 |
> | **T21** | 🔴 **甲里带着源模型的身体和头** | 普查把「身体件」判成甲件 → 整块收进甲（裸手臂/腿/皮肤）。试过剔掉身体件：**23 个里 10 个的甲和身体完全融在一个对象里**，剔掉=甲也没了 → 已回退。**先拿 1 件进游戏看穿模严重不严重**，再决定要不要做"碎片级皮肤过滤"（新工具） |
> | **T22** | **3 顶笠切得不完整** | `troop_guard3`（一块额当板）/ `troop_guard5`（一条窄片）/ `troop_jonin`（只有额当）—— 兜件和别的几何混得深，要单独查 |
> | **T23** | **6 个模型的笠（用户选了先跳过）** | 弓足轻 / 下忍 / 中忍 / 九州精锐 / 飞忍 / 护卫陆 —— 它们的兜和**脸在同一个对象**，`--keep-head-frags` 会把脸一起切进头盔。idx 记在 `troop_parts_table` 的 `pending_helmet` 列 |
> | **T24** | **头目 6 套（L200~L205）算不算** | 🔴 **待用户裁定**（问了三轮）。它们的甲/兜更华丽（金鹿角/双角/大金立），适合精英或亲卫兵种，但严格说不算"通用士兵" |
> | **T25** | **物品登记**（甲/笠/武器） | 🔴 **必须等 T20 导出完成之后**。武器那批已经踩过：网格只 stage 没导出就登记物品 = **能刷出来但拿在手里什么都没有的隐形物品**（用户当场抓包，已 `--purge` 撤回） |
> | **T26** | **挂进 `spnpccharacters.xml`** | 给 5 个现有兵种（枪/弓/精锐足轻、侍、骑马侍）配装 —— 这一步是用户后面要做的"配置战国兵种" |
>
> #### 两条教训（写进工具注释了，这里留索引）
>
> 1. 🔴 **「资产就绪」≠「可以登记物品」** —— 登记必须等网格真的导出进 Taikou 之后
> 2. 🔴 **普查的判据是按 28 武将调的，对兵种会误判**：兵种的「身体件」被判成「甲件」（武将判的是「内衬着物」），且 `--kimono-torso-only` 对兵种**无效**（只删 6 个顶点 —— 它的骨判据把大腿/肩也算作躯干）
>
> ### 四、脚本工作流总览（**三条工具链 + 关卡 + 人工步**）
>
> #### 4.1 全景（谁调谁）
>
> ```
> 源模型  D:\BrainMaker\战国无双2资产解包分析\export\fbx\L??_<名>.fbx
>    │
>    ├─[头]  parts_table.py ─► build_heads.py ─► build_head.py
>    │                                           ├─► transfer_channels.py   （蒂法 v10 搬 59 条形变通道）
>    │                                           ├─► make_sw2_textures.py   （源图集 → 5 张贴图）
>    │                                           └─► fbx_probe.py           （关卡 1：USF/UpAxis/节点零变换）
>    │        ⇒ Debug/offline/sw2_build/<角色>/head_*_a_v{1,2}.fbx + 5 张 png
>    │        ⇒ 关卡 2：check_materials.py（材质名必须正好 3 个且互不相同 —— 铁律 27 的闸门）
>    │        ⇒ 🧑 ModKit 导入 + Publish
>    │        ⇒ install_pack.py（补 morph 帧 + 刷材质配方 → 装进 Taikou）
>    │
>    ├─[兜]  parts_table.py ─► build_helmets.py ─► build_armor.py + build_textures.py
>    ├─[甲]  run_census.py ─► build_armors.py   ─► build_armor.py + build_textures.py
>    ├─[武器] parts_table.py ─► build_weapons.py
>    │        ⇒ 全部工具产物落 tools/armor-pipeline/out/（真源）
>    │        ⇒ stage_for_import.py 归拢到 AssetSources/sw2/<角色>/（导入副本，幂等）
>    │
>    └─[物品/登记] gen_armor_items.py · gen_weapon_items.py · gen_hero_wiring_cols.py
> ```
>
> #### 4.2 三处「改数据 / 改判据」的入口（**改动只碰这三个文件**）
>
> | 文件 | 管什么 | 什么时候改 |
> |---|---|---|
> | `tools/sw2-pipeline/parts_table.py` | **唯一改数据的地方**：每个角色挑哪几块零件（`face`/`eye`/`hair`/`helmet`/`strap`/`neck`/`weapons`/`hard`/`seal`/`head_k`）| 某人缺件 / 某块件挑错 |
> | `tools/face-pipeline/scripts/build_head.py` | **头的判据**：1.2 清碎片 / 1.3 剔非头部 / 1.4b 硬判据 / 1.7 薄片补背面 | 出现"整类问题"（如全部人被删过头）|
> | `tools/armor-pipeline/scripts/build_armor.py` | **甲/兜的判据**：选件 / 剔碎片 / 颏带并入 / 骨架重定向 | 同上，甲侧 |
>
> ⚠️ **`build_head.py` 是蒂法和战无2 共用的**。改判据要保证蒂法那条线不受影响（判据都带开关或按骨名限定）。
>
> #### 4.3 一次性排查工具（`Debug/offline/`，**不进 git**）
>
> | 脚本 | 用途 |
> |---|---|
> | **`_audit_batch.py` + `_audit_manifest.py`** | 🔴 **验收主力**：批量出「产出 vs 源模型」同机位多视角渲染。支持 `flat`（纯灰判几何）/ `only`（只渲某块）/ `exclude`（只渲"没挑中的"）/ `cull`（开背面剔除）|
> | **`_cmp_sheet.py`** | 三栏对照图「源模型 \| 改前 \| 改后」（改前取 AssetSources 的快照）|
> | **`_audit_cull.py` / `_audit_frag.py` / `_audit_sim.py`** | 判据诊断：逐骨/逐碎片统计删除量；全量 28 人模拟"旧判据 vs 新判据"找回归 |
> | **`_audit_logs.py`** | 解析 28 份构建日志 → 一张表看谁被删了多少顶点 |
> | **`_audit_leftcolor.py`** | 按子网格着色渲染"没被挑走的件" → 定位小装饰在哪块（见 §三之二）|
> | `_jit_*.py` 系列 | 单点排查（贴图渲染 / 只渲脸壳 / 按骨染色 / 按种子选碎片）|
>
> #### 4.4 旧版流程图（判据细节）
>
> ```
> parts_table.py  ──►  build_heads.py  ──►  build_head.py        （头）
>     │                                        ▲
>     └──────────►  build_helmets.py ──► build_armor.py          （兜，颏带并回来）
> ```
>
> **`tools/sw2-pipeline/parts_table.py`（唯一改数据的地方）** 每行的字段：
> | 字段 | 含义 |
> |---|---|
> | `face` / `eye` | 脸壳 / 眼球（零件表行号，不是子网格号）|
> | `hair` | 头发件。默认**并进 face**（走 1.3 离群判据）；标 `hard=True` 则**独立成 hair 角色**走 1.4b 硬判据（用在"1.3 会把整块头发判成杂质"的人身上，如慶次）|
> | `seal` | 发块底边有方形缺口 → 自动补面（光秀 idx6）|
> | `strap` | 颏带碎片种子点（源坐标）→ 头里摘掉 + 兜里并进来 |
> | `strap_bone` | 颏带里"长在脸壳主网格上、摘不掉"的那部分（如 `bone_59`）→ 现做法是**只改 UV 不删几何**。🔴 **该做法已被用户实机否掉**（改 UV ≠ 看不见），重做方向见待办 **T4** |
>
> 🔴 **挑件串只有一个来源** = `build_head_args()`。`build_heads.py` 里原来还有一份重复的 `pick_of()` —— **改一份不生效**（A2 两次没修上就是栽在这，已删）。
>
> **`tools/face-pipeline/scripts/build_head.py`** 本轮新增的步骤：
> | 步骤 | 参数 | 干什么 |
> |---|---|---|
> | 1.3c | `--strap-seed` | 按"碎片重心离种子最近"整块摘掉（**只用于独立碎片**）|
> | 1.3d | `--strap-bone` | 把这些骨骼的顶点**只改 UV**（指到最近肤色顶点）——**绝不删**（删=镂空）|
> | 1.4b | （`hair` 角色自动走）| 硬判据：碎片主导骨 ∉ `{bone_10, bone_11}` → 丢。白名单**只认头骨**，不收面部骨族 |
> | 1.4c | （`hair` 角色自动走）| 头发并进脸壳（必须在 1.3/1.4b **之后**）|
> | 1.6 | `--seal-bottom` + `--seal-atlas` | 发块开口底边补面；UV 从图集**采最暗的点**（头发是近黑）|
>
> **`tools/armor-pipeline/scripts/build_armor.py`**：`--strap-from <脸壳子网格号> --strap-seed "…"` → 把脸壳件里颏带碎片并进兜。
> **`tools/sw2-pipeline/build_helmets.py`**：自动从 parts_table 读 `strap` 传参，不用手传。
>
> ### 五、本轮踩的坑（都写进代码注释了，这里是索引）
>
> | 坑 | 症状 | 教训 |
> |---|---|---|
> | 判据定位错对象 | 光秀刘海整块没有 | 接触图把**裙子件**（内含前刘海框）看成纯裙子漏掉 → 挑件表漏件。**机器判 head_area 的件要人工复核** |
> | 离群判据吃头发 | 慶次头旁边飘甲片 | 1.3「离面部骨簇远就删」会把整块头发判成杂质；触发 90% 安全网后**整体跳过** = 什么都没滤 |
> | 删"长在脸上的部件" | 下巴镂空、脸上棋盘格 | **只保留半句**：删几何会镂空（下巴条确实连着下颌皮面）。~~⇒ 连在主网格上的一律只改 UV~~ 🔴 **2026-09-15 深夜被用户实机推翻**：改完 UV，下巴上的带子**照旧看得见**——形状还在，改色等于没改。**用户裁定：必须整条进兜**，见待办 T4 |
> | 整片一个 UV | 皮肤一块死板色 | 逐 loop 取**最近**肤色顶点的 UV |
> | UV 候选跨缝 | 脸上出现棋盘格 | 候选限制在**近邻半径内**（跨 UV 缝会取到图集占位区）|
> | 补洞的 UV | 补完像黑洞 | 别取洞边顶点的 UV（那是杂质区），取脸主干骨最近的 |
> | 顶点数对账 | —— | **头删掉的顶点数必须 = 兜加上的顶点数**（本轮的对照法，一眼看出搬没搬对）|
>
> ### 六、排查用的一次性工具（都在 `Debug/offline/`，不进 git）
>
> | 脚本 | 用途 |
> |---|---|
> | `_jit_textured.py` | 给任意 FBX 贴源图集渲染（复现编辑器里看到的样子）|
> | `_montage_face.py` | **只渲脸壳件**的 28 人拼图（查脸上多出来的东西 —— 颏带就是这么找出来的）|
> | `_montage.py` | 产出头拼图（多人同取景对照）|
> | `_jit_cut.py` | 复现 `keep_head_only` 删了哪些顶点（灰=留/红=删）|
> | `_jit_bonecolor.py` / `_jit_dim.py` | 按主导骨染色 / 打印骨骼分组 |
> | `_jit_inside.py` | 按种子点或盒子选出碎片单独渲染（**定种子点必用**）|
> | `_jit_small.py` | 只渲小碎片（在大件里找"带子"这类细件）|
> | `_jit_prov.py` / `_jit_provcolor.py` | 产出头顶点的溯源（来自源模型哪一块）|
> | 🔴 `_audit_manifest.py` + `_audit_batch.py` | **本轮验收主力**：一条命令批量出「产出 vs 源模型」同机位多视角渲染。`python _audit_manifest.py chin\|face\|hair\|armor\|helm\|zoom` 生成清单 → `blender -b --python _audit_batch.py -- --manifest <清单>`。支持 `flat`（纯灰判几何）、`only`（只渲指定网格）|
> | `_audit_logs.py` | 解析 28 份构建日志 → `_audit_logs.txt`（谁被删了多少顶点、出件面数），一眼看出异常人 |
>
> 全部用法见各脚本文件头的 docstring。
>
> ⚠️ **朝向陷阱**（踩过）：本管线源模型**面朝 +Y**，产出头已转向**面朝 -Y**。
> 所以同一份清单里，视角名对两侧含义相反——`_audit_manifest.py` 里用 `flip` 表自动转换，别手写。
>

> ## ~~本轮任务（2026-09-15 用户下令）~~ → 已完成，见上面交接
>
> | # | 做什么 | 状态 |
> |---|---|---|
> | 1 | 28 张脸：资产 + race + 接线 | ✅ |
> | 2 | **28 套甲** | ✅ 网格/物品/登记/接线全齐，待导入 |
> | 3 | **28 件武器** | ⏳ 未开工（交接表待办 7） |
> | 4 | 头盔 | ⚠️ 9 顶都做出来了，但**兜件归属没定干净**：谦信 `helmet` 列是空的（兜混进甲），另有 6 人的兜件普查误判成"甲件"→ 见待办 T2 / T3 |

> ## 🔴 2026-09-14 会话交接 → 见 [Knowledge/战国无双换装工程.md](../Knowledge/战国无双换装工程.md) §24
>
> **现状**：**28 张脸已全部产出**（`TifaHead2\AssetSources\sw2\`）+ **28 race / 140 Monster 已生成**
> + **`race="lwn_*"` 已挂到六代领主文件上**（35 项体检全绿）。
> **下一步**：① 用户进 ModKit 导入 28 个头（**先跑 `tools/sw2-pipeline/check_materials.py`**，28/28 已过）
> → ② 我：修渲染器看观感 + 收尾 2 个几何不合格的（改完要重新导入）→ ③ **甲 → ④ 武器**（本轮必须做完）。

> 目标：把《战国无双2》28 个有名武将的**脸**、**甲**、**武器**全部做进游戏。
> 现状：已完成 2 个（信长头、幸村甲）+ 两张默认脸（男萨菲罗斯 / 女蒂法）。
> 本计划要回答的是「剩下 27+27 个怎么批量化」，不是「第一个怎么做」——第一个已经做完了。

---

## 一、结论（这一轮要做五件事）

1. **先造一个零件识别工具**——28 个源模型的零件全是无名编号，必须先能自动认出「哪块是脸、哪块是眼、哪块是甲」。
2. **修一次男头领口**——领口几何破碎（拉丝/锯齿/穿出）是已登记的下一件事，27 张脸共用这一次修复。
3. **批量出 27 张脸**：一条命令跑完 → TifaHead2 工程导入 → Publish → 打补丁 → 进 Taikou。
4. **批量出 27 套甲**：同一个零件识别工具 → 重定向骨架 → 6 级 LOD → 贴图 → Item XML → 装备表。
5. **批量出 27 件武器**（2026-09-14 追加）：战无2 源模型自带每个角色的武器，做成资产挂到物品上——现在是原版欧洲剑。

---

## 二、现状盘点（谁做到哪了）

| 项 | 状态 | 证据 |
|---|---|---|
| 织田信长头 | ✅ 实机通过 | race `lwn_nobunaga`，§7.10/§7.12 |
| 真田幸村甲 | ✅ 实机通过 | item `taikou_yukimura_do_a` |
| 默认脸（男萨菲罗斯/女蒂法） | ✅ 实机通过 | `Taikou\ModuleData\skins.xslt` |
| 其余 27 人 × 脸 | ❌ 未开工 | —— |
| 其余 27 人 × 甲 | ❌ 未开工 | —— |

**能直接复用的现成管线**（这是本工程最大的本钱，别重写）：

| 用途 | 工具 | 位置 |
|---|---|---|
| 源模型 → 头部 FBX（挑件/标定/绑骨/导出） | `build_head.py` | `tools/face-pipeline/scripts/` |
| 59 条形变通道移植 | `transfer_channels.py` | 同上 |
| 头部贴图 5 张（脸/法线/高光/眼/嘴） | `make_head_textures.py` | 同上 |
| 源码模型 → 甲 FBX（重定向/6 级 LOD） | `build_armor.py` | `tools/armor-pipeline/scripts/` |
| 甲贴图（裁图集 + 生成 _n/_s + 重映射 UV） | `build_textures.py` | 同上 |
| 发布后补丁 + 装机 | `install_pack.py` | `tools/face-pipeline/scripts/` |
| tpac 读写全家桶 | `tpaccli`（morphfix / skinfix / texreplace / metaparts / meshdiff） | `tools/face-pipeline/tpactool/` |

---

## 三、本轮新查到的四个事实（它们改变了做法）

### 事实 1：一个 FBX 可以编译出多个网格（本次用不上，但记下来）

官方文档（`Knowledge/bannerlord_official_docs/Asset Management/Asset Types/asset_naming_conventions.md`）：

> All meshes imported from a single geometry file(e.g. fbx) are **grouped by their names**.

编辑器不是「一个 FBX 一个网格」，是按**名字前缀**分组的。
**本次已拍板仍走「一人一个 FBX」**（见第五节），所以这条只作记录——将来要一次导 50 个脸时它值一次导入。

### 事实 2：源模型的零件**全是无名编号** —— 现有工具的关键字/序号挑件完全失效

实测 28 个源 FBX（`Debug/offline/sw2_survey/sw2_survey.json`），零件名一律长这样：

```
model_0_submesh_6_noesis_meshnode_0006
```

没有 `head` / `eye` / `mouth` 这种字。而 `build_head.py` 的挑件靠关键字（`--pick face=head,...`）、`build_armor.py` 的选件靠**子网格序号**（`--parts body_kimono_arms`）——
**两个都不通用**：信长那次是人工看出「submesh_6 是脸」，幸村那次是人工看出「2/4/7/8/9 是甲」。
28 个模型 × 平均 13 块零件，人工认件不可行。

**⇒ 必须先造零件识别工具**（见阶段 0）。

### 事实 3：三个可程序判定的地标

| 地标 | 命中率 | 用途 |
|---|---|---|
| **眼球 = 38 顶点 / 60 面** | **27/28**（只有宫本武藏例外，他是 19v/30f 两块） | 定「眼」件，顺带定头的位置与朝向 |
| **武器件 = 材质名带 `mat_w_`** | 28/28 | 排除武器（盔甲工程已实证此法） |
| **每个模型都自带骨架**（96~142 骨） | 28/28 | 甲的重定向有绑定可用；头用不着 |

另外：**28 个模型都没有形变键**——59 条脸形通道全部要靠 `transfer_channels.py` 从蒂法那份移植过去（与萨菲罗斯同一条路）。

### 事实 4：编辑器里手配的材质**可以完全不打**

`skinfix --fullmat` 是**按材质名**（名字里含不含 mouth/eye/lash）在**发布后的包**上刷配方的，涵盖：
`VertexLayoutFlags`（含 `skinning`）、四角色 shader、`MaterialFlags`（脸/嘴/眼/睫标记）、纹理槽复制。
**⇒ 只要材质名规范（脸=裸名、其余加 `_eye`/`_mouth` 后缀，CLAUDE.md 铁律 27），编辑器里就不用逐个勾。**

唯一还没打通的一环：**材质挂哪张贴图**（`tex[0]`）——这一步现在靠编辑器里手挂。计划里给一条补丁命令补上（见阶段 2 的「材质接线」）。

---

### 事实 5：源模型的「一个对象」里住着**语义不同的几样东西**——取件要按骨骼分组

**先纠正一个容易走错的判断**：源模型的零件**全是碎片云**（实测：秀吉某件 736 顶点 = 66 个连通域、
归蝶某件 1241 顶点 = 86 个连通域）。PS2 时代的模型不焊顶点 —— 所以**「按连通域拆分」这条走不通**。

**真正的现象**是：一块件里混着语义不同的几样东西（逐件单独渲染实测）：

| 角色 | 件 | 里面混了什么 |
|---|---|---|
| 杂贺孙市 | 8 | 头发 **+ 长外套** |
| 武田信玄 | 10 | 红发 **+ 腰间红披** |
| 伊达政宗 | 7 | 身甲 **+ 大月牙前立** |
| 归蝶 | 6 | 身体 **+ 发髻** |
| 服部半藏 | 10 | 脸 **+ 布头罩** |
| 丰臣秀吉 | 0 | 甲 **+ 兜 + 日轮冠** |

**为什么必须处理**：换头/换甲是**整块拿走**的。整块拿 → 头发里会拖着一件外套飞到头上去。

**做法：按「碎片的主导骨骼」分组，只留目标那一组。**
碎片虽多，但每片绑在哪根骨头上是明确的——头发那几十片全绑头骨，外套那几十片绑脊柱/手臂。
判据沿用已有的那三条（主导骨骼 / 包围盒高度 / 主色），**不用新造**。

**⇒ 新增一个拆分工具**：输入源模型 + 目标（脸/头发/甲），输出「只剩目标那组碎片」的网格。

---

## 四、四阶段计划

### 阶段 0 —— 零件识别工具（本阶段是全部工作的前提，先做）

**要造的东西**：`identify_parts.py` —— 输入一个源 FBX，输出每块零件的「身份判定 + 依据」。

**判定依据**（三条并用，不靠肉眼）：

| 依据 | 怎么算 | 认出什么 |
|---|---|---|
| **顶点位置** | 世界坐标包围盒 + 顶点云重心 | 头（最高的那一簇）、脚、躯干 |
| **骨骼归属** | 逐顶点组的骨名 → 该件主导骨 | 脸/眼 → `head` 骨；腿 → `thigh/calf`；手 → `hand` |
| **贴图颜色** | UV 采样图集 → 主色聚类 | 肤色（脸/手）、甲色、金属色 |

**产出**：
1. 每人的一份**零件表**（CSV）：`序号, 顶点数, 材质, 主导骨, 包围盒, 主色, 判定身份, 置信度`
2. 每人的一张**接触图**（contact sheet）：每块零件单显渲染一格，带编号 —— **给人肉眼复核用**（机器判定 + 人抽查，不靠肉眼从零认）

**判据**：28 张接触图上，头和甲的零件都标对；错的人在表里手工写一行覆盖（覆盖表进 git，脚本读它）。

**产出目录**：`Debug/offline/sw2_parts/`（离线产物，不进 git）

---

### ~~阶段 1 —— 修一次男头领口~~ ❌ **取消**（2026-09-14 用户裁定：以信长/幸村为先例）

**为什么取消**：原计划是照**萨菲罗斯**那套（`--fit-rim` 硬收领口 → 领口破碎 → 要照 `build_neck.py` 重铺）。
但**萨菲罗斯不是战无2 的模型**——那几条（`--fit-rim` 收领口、`--weld-seam` 合缝、`--weights-from` 抄权重）
全是给他一个人的毛病打的补丁。

**信长那版的实际构建日志**（`work/out/build_head_L02.txt`，战无2 同类源模型）：

```
  收领口：收进 4 个顶点（最大收进 16.8mm）      ← 对比萨菲罗斯：118 个顶点 / 最大 6.2cm
  没有 weld-seam，没有 weights-from
  全头包围盒 x[-0.1321,0.1302] z[1.4644,1.9502]
```

**⇒ 战无2 的头只沾 4 个顶点、1.7cm，根本不是萨菲罗斯那种领口破碎。这条不成立，删掉。**

**⇒ 本轮的模板一律取**：
- **头 → 织田信长**（`--parts face,eye,mouth` + `--pick-idx` + `--cut-mouth` + `--fit-rim`，**不加** weld-seam / weights-from）
- **甲 → 真田幸村**（`build_armor_chain.py --parts body_kimono_arms --kimono-torso-only --no-hands --r 0.0120 --r-arms 0.0160`）
- **贴图 → 信长那版**（单张图集当三张 diffuse + `_n`/`_s` 纯色，见 `make_sw2_textures.py`）

**复现验证**（2026-09-14）：信长的头用全自动流程重跑 → face **818** 面（手工 816）、eye 60、mouth 140（手工 142）、
收领口 **4 顶点 / 16.8mm**（与手工日志相同）、包围盒**逐位相同**。

---

### 阶段 2 —— 批量出 27 张脸

**先补一个缺口：源模型没有「嘴」件，要自动切。**

`build_head.py` 的标定需要「嘴」件（用眼↔嘴的竖直距离算缩放），而战无2 的源模型**只有脸/眼/发**，
嘴是**从脸壳上切下来的一块**——信长当年是**手工切的**（`|x|≤3.2, z∈[170.6,174.4], y<0` → 142 面），
脚本里没有这一步。27 张脸不能手工切。

**补法**：给 `build_head.py` 加 `--cut-mouth`——按几何自动切唇周：
- 嘴中心估计 = 眼球中心往下 `0.20 × 脸壳高度`（实测信长：眼 179.3、嘴 172.5、脸高 34.1 → 6.8/34.1 = 0.20）
- 切盒 = `|x| ≤ 0.25×脸宽`、`z ∈ 嘴中心 ± 0.06×脸高`、`y` 取前半
- 切出来的面独立成件（`<名>.N`），与脸/眼一起走后面的标定
- **判据**：切完渲染一张脸，嘴的位置对得上（离线看，不用开游戏）

**每人一条命令**（新增一个批量驱动，把配方固化，照 `build_head_chain.py` 的样子）：

```
① build_head.py            源模型 → 头部 FBX（4 件或 3 件，按性别）
② transfer_channels.py     59 条形变通道移植
③ make_head_textures.py    源图集 → 5 张贴图（_d/_n/_s/_eye_d/_mouth_d）
④ fbx_probe.py             关卡 1：USF=100 / UpAxis=2 / 节点零变换
⑤ （不做合集 FBX —— 已拍板走「一人一个」）
⑥ 人：ModKit 导入 → Publish      ← 人工步骤，**每人一次**（27 次）
⑦ install_pack.py          两道补丁 + 关卡 2 + 装机（每张脸一次 --filter，脚本循环）
⑧ [补] 材质接线            把 tex[0]/[2]/[4] 指到我们的贴图上（新增 tpaccli 命令）
```

**接线（XML 侧）**：
- `Taikou\ModuleData\skins.xml` 每个头加一个 `<race id="lwn_<名>">`（生成器 `Scripts/gen_taikou_*.py` 出，禁手改）
- `taikou_lords*.xml` 对应行加 `race="lwn_<名>"`（生成器里的 `SPECIAL_RACE` 表加一行）

**判据**：进游戏，28 个领主各长各的脸；随便找个路人领主 —— 脸不变（隔离成功）。

---

### 阶段 3 —— 批量出 27 套甲

同一个零件识别工具输出「甲件清单」→ 走现成的 `build_armor_chain.py`：

```
① build_armor.py      选件 → Y 轴镜像 → 骨架重定向 → 6 级 LOD 0..5
② build_textures.py   裁图集 + 生成 _n/_s + 重映射 UV
③ fbx_probe.py        关卡 1
④ 人：ModKit 导入 → 材质勾 Bumpmap+Skinning → Publish
⑤ Item XML：taikou_items\body_armors.xml 加一件（id/mesh/属性）
⑥ EquipmentRoster：把甲挂到对应武将身上
```

**已知欠账（第一件就挂着，第二件顺手做掉）**：上臂中段露身体 —— 需要一段程序生成的布袖（绕上臂骨的筒，权重给 `upperarm_twist_15`）。做在 `build_armor.py` 里，之后所有甲都受益。

**判据**：28 个领主穿自己的甲，走路/战斗不穿模、不撕扯。

---

### 阶段 3.5 —— 批量出 27 件武器（2026-09-14 用户追加）

**为什么要做**：Taikou 现在**一件日式武器都没有**——28 个领主拿的全是原版欧洲剑
（实测统计：`short_sword_t3` 343 次 / `cleaver_sword_t3` 155 次 / `pugio` 136 次 / `ridged_sabre_sword_t4` 130 次 / 圆盾 287 次）。
织丰有整套日式武器（`sho_katana_1..31` 等），但**铁则 6 规定 Taikou 与织丰零依赖**，不能引它的 id。

**做法**：战无2 每个源模型**自带该角色的武器**（零件识别工具已按材质名 `mat_w_` 标出），
像幸村甲那样把它做成资产 → 挂到物品上。

| 比甲便宜的地方 | 说明 |
|---|---|
| 不用蒙皮 | 武器是刚体，直接跟手骨走 |
| 不用 6 级 LOD | 原版武器 LOD 少，先做一个 LOD 也能跑 |
| 不用后处理补丁 | 无 morph、无脸部标记 |

**流程**：零件识别工具挑出武器件 → 导出（朝向/尺度对标原版武器）→ 贴图 → 编辑器 Publish → Item XML。

**判据**：28 个领主手里拿的是自己的武器，不是欧洲剑。

---

### 阶段 4 —— 收尾

- `Inahime`（稻姬）**需要新增一行** `TaikouHero.csv`（全表 1440 行查无此人）——其余 27 人都在表里。
- 头资产精简：现在 `Taikou\AssetPackages\pack0.tpac` 会把「工程里有的全打进去」，28 张脸 + 28 套甲要留意包体（预估 100~200 MB）。
- 删掉早期误建的垃圾文件 `Taikou\AssetSources_disabled`（10,585 B，非目录）。

---

## 五、已拍板（2026-09-14 用户裁定）

1. **零件识别工具放 `tools/sw2-pipeline/`**（新建工具链）。
2. **编辑器导入走「一人一个 FBX」**——27 次导入，每次都是被验证过的路径，不赌「一个 FBX 出多个网格」。
3. **这一轮脸和甲一起做完**（27 张脸 + 27 套甲）。

---

## 六、风险

| # | 风险 | 兜底 |
|---|---|---|
| 1 | 零件识别工具认错（尤其头发与脸同一块、女性角色的发饰） | 接触图人工复核 + 覆盖表 |
| 2 | 材质 `tex[0]` 接线命令做不出来 | 退回「编辑器里手挂贴图」（每个头多 1 分钟） |
| 3 | 包体过大（28 脸 + 28 甲） | 实测后再决定要不要拆包 / 裁 morph 帧 |
| 4 | 甲的「上臂露身体」在 28 套上被放大 | 阶段 3 顺手做布袖（已挂在欠账里） |
| 5 | 授权 | 《战国无双2》形象版权归光荣特库摩，**仅个人非商**（与蒂法工程同性质） |

---

## 七、这一轮做完能交付什么

- 一个可复用的**零件识别工具**（28 人的零件表 + 接触图）——这是后面所有换装工作的地基
- 男头领口修好（27 张脸共用）
- 27 张脸 + 27 套甲做完并实机验证
- 一条**批量命令**，后面每加一个角色只需改一行配方

---

> 关联文档：`Knowledge/战国无双换装工程.md`（总纲 / 角色×StringId 对照表）、
> `Knowledge/蒂法换头工程.md`（头部工具链与硬规格）、`Knowledge/骑砍2盔甲资产工程.md`（甲工具链）。
