# 时代剧本切换 — 先行验证（spike）

> **状态：✅ 实机验证通过（2026-09-10）——多时代机制成立，T4 可据此铺开**
> 结论：一模块多 GameType + 每时代一套段**验通**；两时代互不污染（U1 过关）；选剧本走自建界面（绕开 MCM）；一键体检 `run_all_checks.py` 15 项（14 绿 / 1 既有红 = 雷 53）。
> **上游**：主 plan「四、」节（本文档从那里抽出）；剧本层路线见 [scenario-campaign-mode/06-玩家身份与时代重置](scenario-campaign-mode/06-玩家身份与时代重置.md)（该 plan 未审核，本 spike **不依赖**它）
> **一句话**：T4 要铺几百个据点、上千人物——**万一"多时代"这条路走不通，数据结构要推倒重来**。所以用最小成本先验一次。

---

## 要验的一件事

换了时代（如 1560 日轮 → 1582 转变），**据点归属 / 家族 / 王国 / 英雄**这四样能不能整套跟着变——世界建得出来、四项都对、两个时代互不污染。

## 候选机制（本次要验的就是它）

**一模块多 GameType + 每时代一套数据段**

- 引擎按 `<GameType value="…">` 过滤数据段（**现在就是这么工作的**：Taikou 的 30 个段挂在 `TaikouCampaign` 名下）→ 多时代 = 再注册一套段（换 GameType）+ 一个 thin 战役类 + 主菜单一个入口。
- **为什么不走官方 `worldscenarios` 扩展点**：那是 **1.5.1+** 的机制（06 号 plan 自己也标了"按其自身版本择用"），**我们的运行环境是 1.2.12**。

## 🔴 时序硬约束（决定了"选时代"只能放哪）

| 时序 | 发生什么 |
|---|---|
| ① 模块加载 | 只读 SubModule.xml 的段**清单**，数据还没加载 |
| ② **玩家点主菜单入口** | 启动对应 GameManager → new 出对应**战役类** |
| ③ 战役初始化 | 各段按 `IncludedGameTypes` **过滤加载**——过滤用的字符串 = **战役类名**（实证：官方 `Campaign` / 织丰 `ShokuhoCampaign` / 我们 `TaikouCampaign`） |
| ④ 建号流程 | 到这里数据早已加载完 |

**⇒ 两条推论**
- **「选时代」必须在主菜单层**（它决定加载哪套数据段，赶不上建号流程）。落地 = **两层菜单**（2026-09-10 用户裁定）：
  - **第一层**（主菜单）：一个「**剧本**」按钮，与"载入游戏/自定义战斗/Mod 选项/选项"平级
  - **第二层**：各时代剧本清单 + **返回**（回第一层）
  - 为什么不做成"各时代直接铺在主菜单"：时代会越来越多，主菜单的语义是"游戏功能入口"不是"内容清单"
- **「选角色」可以留在建号流程内**（它只是运行期注入玩家身份：改名/换阵营/传送/配部队，**不决定加载哪套数据**）。
- ⚠️ **06 号 plan 要改形态**：它把"时代 → 角色"两层都放在建号流程里——**1.2.12 上第一层必须上移到主菜单**。等 06 审核时一并提。

### 🔴 两层菜单 → 改为自建选剧本界面（2026-09-10 实机教训后的定案）

**先做的方案（已废弃）**：第一层「剧本」按钮换掉主菜单选项列表 → 点第二层 → 返回时还原快照。
**为什么废弃**：主菜单选项列表**被 MCM 等 mod 用 Harmony 盯着**——MCM 的 `RefreshMenuOptions` postfix
按固定下标往列表里插自己的按钮；我们把列表换短 → **它下标越界崩**
（实机：`ArgumentOutOfRangeException @ MCM.UI.Functionality.DefaultGameMenuScreenHandler.RefreshMenuOptionsPostfix`）。

**定案：自建选剧本界面（ScreenBase + prefab），完全绕开主菜单选项列表。**
用户裁定（2026-09-10）：反正以后要正式做 UI，不如现在就做成界面。
- 主菜单只加**一个**「剧本」入口按钮（`EraCatalog.BuildEntryOption`）
- 点它 → `ScreenManager.PushScreen(ScenarioSelectScreen)`（主菜单屏留在栈下，返回即还原）
- 界面布局对标太阁5「开始新游戏」：**左列剧本按钮 / 右侧大图 / 右下返回 / 左上标题**（"推荐"暂不做）
- 点剧本 → 先 `PopScreen()` 关界面 → 再 `StartNewGame`（顺序要紧：StartNewGame 会替换主菜单屏）

**顺带记下的引擎机制（改主菜单选项列表这条路以后若还要走，先看这里）**：
`Module.CurrentModule` 的 `InitialStateOption` 列表 = 主菜单数据源；改动后调
`InitialState.RefreshContentState()` → `OnGameContentUpdated` → `GauntletInitialScreen.OnGameContentUpdated`
→ `InitialMenuVM.RefreshMenuOptions()` 重建按钮 → UI 刷新（链路实锤于
`Native/.../TaleWorlds.MountAndBlade.GauntletUI.dll`）。
⚠️ 但**只要列表被 MCM 盯着，换短列表就不安全**——那条路只在"不改动列表长度/顺序"时才可用。

**为什么不省掉多余的战役类**：引擎的段过滤就是拿一个**字符串**比对 `IncludedGameTypes`；该字符串来自战役类名（`GameType.GameTypeStringId => GetType().Name`，虚属性）。理论上可让一个类按变量返回不同串，但省下的只是几个 thin 小类（每个 3 行），换来的是**碰引擎内部的未知风险**——不划算。除非 U1 验证发现"一模块多 GameType"本身有问题，届时再议。

## 命名规范（2026-09-10 用户裁定）

**每个时代一个 GameType，名字 = `TaikouCampaign<年份>`**

| 时代（TK5 六剧本） | GameType / 战役类 |
|---|---|
| 1554 乱麻 | `TaikouCampaign1554` |
| 1560 日轮 | `TaikouCampaign1560` |
| 1568 升龙 | `TaikouCampaign1568` |
| 1575 霸道 | `TaikouCampaign1575` |
| 1582 转变 | `TaikouCampaign1582` |
| 1598 太平 | `TaikouCampaign1598` |

**本 spike 取两个**：`TaikouCampaign1560`（= **现有 v0 世界改名**）与 `TaikouCampaign1582`（新增对照）。
⚠️ v0 目前是**占位世界**（京归织田，史实上更接近 1568 之后），年份眼下只是名字；T4 铺真数据时再对齐史实。

## 数据源结构（推荐 = 骨架 + 时代差异 → 生成器展开）

> **前提（用户给定）**：①**城池数量跨时代相同**（只是**改名 / 换归属**）②**人群是同一批**（一份人物池，各时代区别只在"未出生 / 已死 / 在世"与归属）。这两条成立 → **骨架可以共用**，每个时代只需要存"差异"。

**五张表（CSV，人维护 = 唯一真源）**

| 表 | 粒度 | 内容 |
|---|---|---|
| ① 据点骨架 | 每据点一行 | 据点 id / 地图场景 / 地图位置 / 组件类型 —— **跨时代不变** |
| ② 据点时代差异 | 据点 × 时代 | **显示名 / 归属家族** —— 每时代一行 |
| ③ 势力（家族·王国） | 势力 × 时代 | 是否存在 / 名称 / 统治家族 / 上级王国 —— 每时代一行 |
| ④ 人物池 | 每人一行 | 生年 / 卒年 / 属性技能 / 家系 —— **固有，跨时代不变** |
| ⑤ 人物时代差异 | 人物 × 时代 | **所属家族** —— 「在世 / 未出生 / 已死」**由生卒年自动推导，不用手填** |

> 🔴 **每个时代的 `heroes_<时代>.xml` = 那个时代开局「已存在」的人**（快照语义）——不是全时间轴人物池。
> 「1560 开局、玩到 1582 时该出生的人」不在这份文件里，走**出生队列**（见下方专节）。

**生成器（一次产出 = 引擎要的 XML，产出即每时代一套段）**
- 每个时代 → 一套 `settlements_<时代>.xml` / `spclans_<时代>.xml` / `spkingdoms_<时代>.xml` / `heroes_<时代>.xml`
- `SubModule.xml` 里按各时代 GameType 分别注册（引擎按 GameType 过滤，**运行期只加载所选时代那一套**）
- **「已死」怎么落地**：XML 表达不了"这人开局就已死" → 每个时代另生成一张**开局击杀名单**，由**一个**开局 behavior 走官方击杀流程（**运行期唯一要保留的一件事**）

**为什么推荐这个结构**
1. **差异集中在小表里**：几百据点 × 6 时代，每行只有"名字 + 归属"两个字段——改一处只改一处。
2. **符合数据优先三原则**：能 XML 不代码；**能离线查的绝不留给运行期**（每时代一套 XML → 十三件套能对**每套单独**跑）；运行期只剩"击杀已死"。
3. **绕开两个未知点**：运行期改据点归属、运行期改名——引擎接不接得住、UI 刷不刷新，**都不用验了**。

🔴 **一条关键纪律：据点 id 跨时代必须稳定（只改显示名，id 永不变）**——这就是铁律 20（引用一律用 StringId）。额外红利：**距离缓存天然可共用**（缓存按**据点 id** 存据点对）→ U4 风险**自动消解**。这正是"城池数量相同"这条前提的价值。（若某时代确有据点增减，表里加「启用」标记处理，缓存问题届时再议。）

**不推荐的两种**
- **每时代手写一套 XML**：6 倍手维护量、改一处要改 6 遍 —— 但**经上面生成器产出就没这个问题**（人只维护那 5 张表，别手写 XML）。
- **运行期覆盖 XML 设置**（自己搞 CSV、开新战役时改引擎已加载的数据）：违背"能离线查的别留给运行期"（离线查不了运行期改的结果），且引擎支持度未知——要额外验两件事：改归属接不接得住、改名 UI 刷不刷新。

**顺带：对 06 号 plan 的一处简化建议**
06 把「按时代改年龄、重算技能」放在**运行期**做；用生成期展开的话，每个时代的 `heroes_<时代>.xml` **直接写对生年**即可 → 06 那部分可简化为纯数据，运行期只剩"该出生的人到点出生 + 该退场的人到点退场"。
⚠️ **更正（2026-09-10）**：本行原写作"运行期只剩击杀已死"——**该说法不成立**，「击杀」会让那些人在连续时间线里永远出不来，正解见下方「出生队列」节。

**待核对一项**：「城池数量跨时代相同」需要在 TK5 布局表上核一遍（若不同，用「启用」标记处理——设计已留余地）。

## 🔴 出生队列（连续时间线：1560 开局玩到 1582，人自然出现）

> **用户 2026-09-10 提问引入**；本节是上面「数据源结构」的必要补充——那份只解决了**开局快照**，没解决**一条时间线走下去**。

### 先分清两个场景（解法完全不同，别混）

| 场景 | 是什么 | 解法 |
|---|---|---|
| **A. 选 1582 开局** | 开局那一刻世界就是 1582 年的样子 | 每时代一套段（**本 spike 已验通的就是这个**） |
| **B. 1560 开局，在游戏里一路玩到 1582** | 同一条时间线，人物随时间自然出现 | **本节**：出生队列 + 到点退场 |

### 决定：`未出生` 不能复用「击杀名单」机制

🔴 **这是原先设计里的一处死路**：原方案写"每个时代生成一张**开局击杀名单**，开局把还没出生的杀掉"——引擎里**死人不能复活**，1560 开局把信长杀进名单，玩到 1582 他也永远出不来。

**正解：未出生的人根本不创建，到点才建。**

### 决定：数据表不按时代存人，只存「生年 / 卒年 / 家族」

每个人物一行（设计文档第 ④ 张表长这样），**「在哪个时代活着」由生卒年推出来，不用手填**：

| 字段 | 用途 |
|---|---|
| 生年 | 开局已出生 → 开局就建（年龄 = 开局年 − 生年）；晚于开局年 → **进队列，不建** |
| 卒年 | 到点退场（引擎的"老死"未必符合史实：信长 49 岁殁） |
| 家族 | 出生时挂到哪个家族 |
| 属性/技能/家系 | 建人时用 |

**落地要点（已反编译核实，2026-09-10）**：
- `HeroCreator.CreateSpecialHero(模板, 出生地, 家族, null, age)` —— **支持指定年龄建人**（age 参数直接落地）
- `Hero.Age` = `BirthDay` 到当下的年数（死人则冻结在死亡时刻）
- **开局**：把"那时已出生"的人一次性建出来（用生成期算好的相对年龄）
- **运行期**（本方案唯一需要写的代码）：挂**每年检查**的监听 → 某人到生年就建出来（age 0），父母子女关系用引擎的 `DeliverOffSpring(mother, father, ...)` 或直接建立 `Father`/`Mother` 链
- **退场**：与出生同一张时间表的另一半——到卒年触发，机制同「击杀名单」，只是触发时点按卒年而非开局

**为什么用「相对开局年份的年龄」而不是绝对年份**：引擎 `CampaignTime.Year` 有自己的纪元，不必去对齐日本年号；相对年龄天然稳定。（若要显示"现在是永禄三年"，那是**另一层**年号映射，与本机制解耦。）

### 三个未验风险（动手前先验；都落在 T4，不在本 spike 范围）

1. **出生时家族可能已经变样**（灭亡 / 换家督）——出生队列挂到哪个家族需实测
2. **引擎"到年龄自然死"与史实卒年打架**——需要显式卒年控制，复杂度待评估
3. **`CampaignTime.Year` 纪元映射**——本方案已绕开；仅当要做年号显示时才需要

⚠️ **不现在做**：这三条全落在 T4 数据铺开里。先把本 spike 实机验通（U1–U4）——万一多时代机制本身走不通，出生队列就白设计了。

## 主菜单与读档（反编译 + 实测存档案查证）

**① 主菜单：要屏蔽原版、显示我们自己的——而且已经在这么做**
`CampaignModeActivator.WireMainMenu()` 现在就剔掉原版三个按钮（`SandBoxNewGame` / `StoryModeNewGame` / `ContinueCampaign`）再加我们自己的。多时代 = 多加入口。
⚠️ **一个漏网的**：原版的「**Saved Games**（存档列表）」按钮（`CampaignResumeGame`，SandBox.View 注册）**没被剔掉**——它点我们的存档会走 SandBox 的 GameManager（见 ②）→ 错路径。多时代前必须一并处理。

**② 读档：GameType 能自动对上，但载入入口必须我们自己的**
- **存档不记 GameType，记的是"对象类型"**：战役对象按类型序列化（`SaveableTypeDefiner` 注册过——雷 32 就是为这个"Could not find type definition"而加）→ 读档时恢复出**同一个战役类** → `GameTypeStringId`（=`GetType().Name`）自然是它 → **对应那套数据段正确加载** ✓ **自动对号入座**。
- ⚠️ **原版载入按钮写死了自家 GameManager**（反编译实锤）：
  ```csharp
  ContinueCampaign(): SandBoxSaveHelper.TryLoadSave(info, StartGame);
  StartGame(LoadResult r): MBGameManager.StartNewGame(new SandBoxGameManager(r));  // ← 写死 SandBox
  ```
  → 我们的读档必须换成 `new LivingWorldCampaignGameManager(loadResult)`。
- ⚠️ **存档元数据里没有"时代"字段**（实测 dump `save041.sav`：只有 `Modules` / `Module_<名>` / `CharacterName` / `MainHeroLevel` … **无 GameType**）→ 读档列表显示不出"这存档是哪个时代"。
  🔑 **但 `MetaData` 是自由键值表**（`Dictionary<string,string>`）→ **我们可以自己写一个键**（如 `LWN_Era=1560`），读档列表就能显示年代。
- **现状**：我们的「继续战役」按钮是**禁用**的（写死"Save-loading is not implemented yet"）——**读档链还没打通**；要多时代得先把这条链做完。

## 改名影响面（已核）

| 位置 | 数量/内容 | 处理 |
|---|---|---|
| `Taikou/SubModule.xml` | **33 处** `TaikouCampaign` 值 | 机械替换 |
| LWN 代码 | thin 类 `TaikouCampaign.cs` + 少数几处引用 | 类改名 + 引用同步；**`LivingWorldSaveableTypeDefiner` 要注册每个时代类**（否则存档报"Could not find type definition"，雷 32） |
| 🔴 **检查脚本** | `--game-type` 缺省 = 「模块目录名 + Campaign」→ 推断出 `TaikouCampaign`（**多时代后失效**） | **必须一起改**：改成显式传（每时代各跑一遍），或推断逻辑改为「读 SubModule.xml 里出现过的 GameType 全集」。不改 = 十三件套**静默查错段**（假绿） |
| 文档 | plan / 必备清单 / wheels 里的字样 | 随 spike 更新（历史记录段保持原样） |

## 执行步骤（5 步，做完就能跑）

> 思路：**共用的段照旧**（文化/物品/模板/文本…），**只给有差异的 4 个段各注册两份**（A 一份、B 一份）。`IncludedGameTypes` 本身就是**多值列表**（现有注册就是 `['Campaign','CampaignStoryMode','TaikouCampaign']`），共用的段把 `TaikouCampaign1582` 追加进去即可，不用复制。

| 步 | 做什么 | 产出 |
|---|---|---|
| 1 | **时代 B 的 4 份差异数据**（复制现文件 + 改）：据点归属（京 → `clan_g`）/ 家族（+`clan_g`）/ 王国（+`kingdom_g`）/ 英雄（+`lord_g`） | `settlements_1582.xml` · `spclans_1582.xml` · `spkingdoms_1582.xml` · `taikou_heroes_1582.xml` |
| 2 | **SubModule 注册**：4 个差异段各注册两份；其余共用段把新 GameType 追加进 `IncludedGameTypes` | `Taikou/SubModule.xml` |
| 3 | **时代 B 的 thin 战役类** + 现有类改名 | `CampaignMode/TaikouCampaign1582.cs` · `TaikouCampaign.cs`→`TaikouCampaign1560.cs` |
| 4 | **主菜单第二个入口** | `CampaignMode/CampaignModeActivator.cs` |
| 5 | **检查脚本 game-type 推断改对** + 编译 + 十三件套（两套数据各跑一遍） | `Scripts/*.py` + 验证报告 |

## 要一次验完的 5 个未知点

| # | 未知点 | 可能出什么问题 |
|---|---|---|
| U1 | **多 GameType 共存** | 一个模块里两套同名段（都叫 `Settlements`/`Factions`）按 GameType 分开注册——进时代 B 时时代 A 的段会不会漏进来（串档） |
| U2 | 据点归属差异生效 | 归属是 XML 属性，预期没问题——但要眼见为实 |
| U3 | 家族 / 王国 / 英雄差异生效 | 新建的家族/王国要过世界创建链的一堆假设 |
| U4 | 距离缓存 | **遵守「据点 id 跨时代稳定」纪律则自动消解**；spike 顺便验一遍确认无残留 |
| U5 | 存档 / 读档后归属保持 | 归属进存档；**但读档链目前未打通**，本 spike 先验前 4 条 |

## 验收（✅ 实机全过，2026-09-10）

> ⚠️ 第 1 条形态已变：**不是"主菜单两个入口"，而是"主菜单一个「剧本」入口 → 自建界面里选"**
> （原方案改主菜单列表被 MCM 的 Harmony 补丁打崩，已改自建 Screen，见上文定案）。

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 主菜单出现**剧本**入口 → 点进去是选剧本界面 | ✅ |
| 2 | 进 1582 → 建号 → 进图 → 点京 → **归属 clan_g** | ✅ |
| 3 | 氏族界面 clan_g / 王国界面 kingdom_g / 能找 lord_g 对话 | ✅ |
| 4 | 存档（F5）不报 `Could not find type definition` | ✅ |
| 5 | **切回 1560 → 京仍是织田家**（互不污染 = **U1 过关**） | ✅ |
| 6 | 检查套件对**两套数据分别**跑（各 0 错） | ✅（14 绿 / 1 既有红） |

⏭ **U5（读档后归属保持）仍跳过**——读档链未打通（「继续战役」按钮禁用），按设计随后单独补。

## 分叉 → **结论：走「每时代一套段」**

- ✅ **已验通** → T4 按「每时代一套段」铺开：生成器按时代批量产出数据（差异表 → `gen_taikou_era_diff.py` 同款），
  06 的选时代入口届时接上（**注意 06 要改形态**：选时代必须在主菜单层，见上文时序硬约束）。
- ~~没验通 → 回退方案（一份基线 + 运行期改朝换代）~~ **不需要了**，但保留备查：若将来某时代**据点数量本身不同**
  （超出「同骨架只换归属」的前提），届时再评估那条路（它自己要额外验"运行期改 `Settlement.OwnerClan` / 家族归属接不接得住"）。

## 与其它文档的关系

- **06 号 plan（未审核）**：解决「**英雄**按时代激活」（全时间轴人物池 + 开局一次时代重置），**不管据点归属**；本 spike 补的正是**据点/家族/王国**那块，且不依赖它。
- **主 plan（太阁数据加载…）**：「三、下一批待定」里有雷 53 收尾（距离缓存 + 家宅反射置位）等条目；本 spike 是其「四、」的展开。

---

## ✅ 实施记录（2026-09-10）

### 做了什么（5 步全落盘）

| 步 | 产出 | 位置 |
|---|---|---|
| 1 | 时代 B 四份差异数据 | `Taikou/ModuleData/{settlements,spclans,spkingdoms,taikou_heroes}_1582.xml`——**由生成器产出**（不手写） |
| — | 生成器（唯一真源） | `Scripts/gen_taikou_era_diff.py`（差异表在文件头 `DROP_*` / `NEW_*` / `SETTLEMENT_OWNER_OVERRIDE`；`--check` 校验产物是否最新） |
| 2 | SubModule 注册 | `Taikou/SubModule.xml`：4 个差异段**两份、GameType 互斥**；其余段共用 → 追加 `TaikouCampaign1582` |
| 3 | thin 类 | `CampaignMode/TaikouCampaign.cs` = 时代**基类**（`TaikouCampaignBase`）/ `TaikouCampaign1560.cs` / `TaikouCampaign1582.cs` + `LivingWorldSaveableTypeDefiner` 注册 3 类 |
| 4 | **选剧本界面**（自建 UI，绕开 MCM） | `CampaignMode/{EraCatalog,ScenarioSelectVM,ScenarioSelectScreen}.cs` + `GUI/Prefabs/ScenarioSelect.xml`：主菜单一个「剧本」入口 → 全屏选剧本界面（左列剧本 / 右大图 / 右下返回） |
| 5 | 检查脚本 | 5 个 checker 的 `--game-type` 推断改**多时代自动全跑**（`inferred_game_types`）+ 新增 `check_era_segments.py` + 生成器 `--check` 入 `run_all_checks.py` |

**顺带修正**：`LivingWorldCharacterCreationContent.OnCharacterCreationFinalized` 原硬编码 `TaikouCampaign.TaikouStartingPosition` + `_contentPack == "Taikou"` 判断 → 改为读**当前战役**的 `StartingPosition`（时代无关，加时代不用动这里）。

### 两个设计裁定（实施中发现，需知会）

1. **差异段 GameType 必须互斥**（设计文档只说"各注册两份"，未点明）——引擎对**同名 XmlName 的多段是合并加载**（铁证：官方 SandBox 自己就注册了 7 份 `NPCCharacters`，全部命中 Campaign 一起加载）。若两套段同时命中同一 GameType，同名据点/家族/英雄**会被定义两遍**。→ 落地为 `check_era_segments.py` 常驻守。
2. **`TaikouCampaign` 保留为基类**（评估过整体改名，结论 = 收益不值代价）——老存档里的对象类型仍能对上；`TaikouCampaign` 同时作为 1560 的**兼容 GameType** 留在 SubModule（旧档/旧入口不断）。代价：多一层继承（3 个类各 ~10 行）。

### 离线验证结果

`python Scripts/run_all_checks.py` → **13 绿 / 1 红**（唯一红 = **既有**雷 53 距离缓存，plan 已记「现正红 = 真问题」，等 T4）。

- **两套数据的 GameType 都在跑**：5 个 checker 输出确认 `TaikouCampaign1560` 与 `TaikouCampaign1582` 各跑一遍。
- **负面测试通过**：故意把京的归属指向不存在的 `clan_g_ghost` → `check_taikou_xml_references` 报 `[DANGLING] Clan.clan_g_ghost (settlements_1582.xml)` + exit 1；随后从生成器还原、复验转绿。
- **据点 id 跨时代一致**（`town_kyoto` / `retirement_retreat`）→ 距离缓存可共用，U4 按设计自动消解。

### 🔴 顺带查出的既有问题（**不是本次引入**，git 已确认 HEAD 里就存在）
`check_era_segments.py` 首次跑出两条「同一对象被定义两遍」（判 **WARN**，不阻塞）：

| 冲突 | 实情 | 影响 |
|---|---|---|
| `NPCCharacter.guard` 同时定义在 `spnpccharactertemplates.xml`（**等级 11**、heavyinfantry_level11 技能模板）与 `spnpccharacters.xml`（**等级 4**、level1 技能模板） | 引擎合并加载 → **后加载者静默覆盖前者** | 卫兵实际等级/技能由加载顺序决定（当前 4），与"模板"那份（11）不一致 |
| `taikou_world_lore_strings.xml` 与 `world_lore_strings.xml` 的 **50 个键完全重复** | 两份文件近似互为拷贝 | 文本取哪份取决于加载顺序 |

→ **处置建议**：归 T4 数据整理（合并 `world_lore` 两份；`guard` 二选一并从另一份删掉）。**修之前先确认哪一份是"想要的"**，属数据决策不是机械去重。

### 🔴 实机排雷：1582 开局崩（2026-09-10 当晚定位并修复）

**症状**：选「1582 转变之卷」→ 新战役崩，堆栈**全指向引擎**：
`Kingdom.OnNewGameCreated`（`InitialHomeLand = Leader.HomeSettlement`）NRE；
补上一处守卫后**挪到** `HeroSpawnCampaignBehavior.GetBestAvailableCommander` NRE。

**诊断过程**（用临时 `[EraDiag]` 日志打出世界实况，一次定案）：
```
Kingdom kingdom_g: RulingClan=NULL Leader=NULL Clans=1
Clan clan_g:       Leader=lord_g  Lords=1    ← 家族以为有领主
Hero lord_g:       不存在                     ← ★ 但英雄根本没被创建
Hero 数=1（只有 main_hero）
```

**根因（雷 54，已入必备清单）**：`<Heroes>` 段里的 `<Hero id="lord_g"/>`
**没有同名 `<NPCCharacter id="lord_g">` 模板** → 该条**被引擎静默吞掉**
（语法合法、交叉引用也解析得了，属「生效条件」缺失而非「引用悬空」）。
→ 家族没有可用的领主 → 引擎给家族 spawn 领主部队时 `GetBestAvailableCommander` NRE。

**为什么之前没查到**：这条规则（英雄需配模板）**当时不存在于任何检查里**——
不是脚本失效，是「`<Hero>` 的生效条件」从来没被写成规则。这一次靠诊断日志才撞出来。

**修复三件**：
1. **数据**：`Taikou/ModuleData/spnpccharacters.xml` 补 `lord_g` 模板
   （照 `lord_oda_nobunaga` 那份，`is_hero="true" occupation="Lord"`）
2. **离线防线**：新增 `Scripts/check_hero_templates.py`（逐 GameType 查，缺即 exit 1 并点名）→ 已进 `run_all_checks.py`
   - 规则**先用现存数据反证**：Shokuho 1382 / ShokuhoTaikouExpansionPack 753 / SandBox 399 个英雄**全部配对、零例外**
     （唯一 `main_hero` 缺模板 = 建号自造，豁免）
   - **负面测试通过**（摘掉 `lord_g` 模板 → 报红 → 装回复绿）
3. **引擎裸解引用守卫**：`CampaignMode/KingdomOnNewGameCreatedGuardPatch.cs`
   （`Leader` 为 null 时跳过引擎那行，交给家宅置位段处理）——伴生崩点，一并堵上

### 实机验证清单（待用户执行；对应上文「验收」6 条）

前置：编译产物已直出游戏目录；启动器勾选 LivingWorldNpcs + Taikou。

1. 主菜单出现**两个**时代入口（`1560 · 日轮之卷` / `1582 · 转变之卷`）
2. 进 **1582** → 建号 → 进图 → 点京 → **归属显示 G 家**（`clan_g`）
3. 氏族界面见 G 家、王国界面见 G 家（`kingdom_g`）、能找 **G 家当主**（`lord_g`）对话
4. 存档（F5）不报 `Could not find type definition` ← 验证新战役类已注册
5. **切回 1560** → 建号 → 点京 → **归属仍是织田家**（互不污染 = **U1 过关**）
6. （可选）1260 那侧确认京无 G 家痕迹

⏭ **U5（读档后归属保持）本轮跳过**——读档链未打通（「继续战役」按钮仍禁用），按设计随后单独补。
