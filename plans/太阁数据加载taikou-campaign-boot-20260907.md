# 太阁战役 — 项目主交接（2026-09-12 更新）

> 文件名保留原样（`太阁数据加载taikou-campaign-boot-20260907.md`，历史链接不破）；内容已从"启动链排雷"长成**项目主交接**。
>
> 🔴 **本 plan 的雷 1~53 已沉淀为可复用的「自定义世界从零起步必备清单」**：`Knowledge/自定义世界内容包从零起步必备清单.md`——新内容包立项直接照那份清单勾选，本文件只保留 Taikou 上下文与排雷实录。
>
> 🔴🔴 **执行纪律（2026-09-10 用户裁定）——每执行完一步，必须停下来对照三处，考虑是否修正「必备清单」：**
> ① **雷表**要不要加行/改行（症状 / 根因 / 修法归属）——凡本次产出"新症状→根因→修法"链条，即登记；
> ② **命中阶段清单的条目**（阶段 0/1/1.4b/1.5/1.6/2/3）→ 补勾选项或"症状信号"；
> ③ **兜底台账表**（清单「LWN 内置兜底 + 运行期补丁台账」节）→ 新增/退役/状态变更的运行期补丁。
> **判断标准**：这一步的结论，**如果新内容包不知道会再踩一次 → 必须回填清单**；只影响 Taikou 自身的（进度、临时验证、待办）→ 留在 plan 即可。清单是坑位地图（给下一个世界看），plan 是执行流水（给自己看）——两者不可互换。

---

## 🔴 当前 TODO（新 session 从这里开始；已完成项见下方「实机验证记录」与文末「📦 归档」）

**一句话现状**：**数据层已齐**（含**文化体系**：17 个文化全部自建）—— 英雄 **1440** 人 / 家族 **296** 家 / 势力当主 **553** 格 / **一键体检 23 绿 / 1 红**（唯一的红 = **雷 53** 距离缓存，等 T5 据点进场景才能收尾）。
**差的是 XML**：0.8 新加的 329 个泛用 hero 还没有 Hero 段和 CharacterObject 模板，所以**他们目前只活在 CSV 里、还没进游戏**。

---

### ⏭ 接下来的 TODO（按建议顺序）

| # | 做什么 | 为什么现在做 / 产出 |
|---|---|---|
| **T1** | ✅ **清数据层的小尾巴**（2026-09-12 完成，见「0.9」）—— 读音 9 条 · 家族名 16 家 · 据点归属整列 | 这些错误会**被铺进 XML**：`Kingdom_<年>` 决定英雄当年挂哪个势力，`Settlements.Owner_<年>` 决定据点归谁。**先清再铺** |
| **T2** | 🔴 **C1 算一次世界规模账**（不写代码）：每年代「王国 × 据点」的分摊（96/95/90/82/72/90 国 vs 274 据点 ≈ 2.9 据点/国）——**看世界碎不碎得离谱**，碎就回来调「哪些家该并进大名」（数据侧 = `TaikouForce.Owner_<年>` 的口径） | 一张「每代王国数 / 有据点的王国数 / 据点分布直方图」。**这是纯分析、零风险，且决定后面要不要返工** |
| **T3** | 🔴 **补那 329 人的 XML**：Hero 段 + CharacterObject 模板（+ 立绘接 Key） | **T4 的前置**。⚠️ `check_hero_templates.py` **只查 XML 里已有的 Hero**（现 8+2 个），查不出 CSV 里的新人；模板缺了会「静默被吞 → 新战役在 `OnNewGameCreated` 崩」 |
| **T4** | **C2 按年代生成世界段**（CSV → XML）：`spclans_<年>`（296 家的 `Owner_<年>`/`Kingdom_<年>`）· `spkingdoms_<年>`（势力表 `Owner_<年>` + Culture + banner）· `taikou_heroes_<年>`（`<Hero faction="Faction.<ClanID_<年>>">`）· `settlements_<年>`。**扩展现有 `gen_taikou_era_diff.py`**（现只做 1560/1582 → 做满六代） | 各年代互斥注册的差异段。🔴 校验：**每个王国必须有 clan（RulingClan 链）/ 每个 clan 有 owner / 每个据点有城主** |
| **T5** | **C3 据点批量进场景**（TK5 布局表 → `place_settlements.py` + 地图实体）→ 顺带收尾**雷 53**（距离缓存 + 家宅反射置位退役） | `settlements.xml` + `SceneObj/Main_map` 实体；`check_scene_entities` / `check_settlement_distance_cache` 转绿 |
| **T6** | **C4 英雄进世界**：1440 人按「生年/卒年/家族」建号 + **出生队列**（连续时间线的出生/退场） | `taikou_heroes_<年>` 全量 + 出生队列机制 |
| **T7** | **C5 兵种树 / 日式物品自建**（原料已备 112 SkillSet / 43 物品 / 25 BodyProperty；日式马/地痞外观留 T5） | 兵种/物品 XML |

| **T0** | 🔴 **下一回合（用户 2026-09-12 排定）**：① **CSV 内部引用全量体检**（每条边都有脚本 + 孤儿检查扩到全表）→ ② **照「最小太阁单元」铺六年代 XML** | 详见下方 **0.18 任务书**；② 的前置是 T3 |

> 📌 **T1 的三项已全部落地**（见 0.9）；**T2~T7 是原「T4-c 执行顺序」的 C1~C5**；本轮（0.9~0.17）清完数据层脏活，**下一回合看 0.18 任务书**。

### ✅ T1 之「待用户裁定」—— 已全部落地（2026-09-12，见「0.9」）

| # | 事项 | 结论 |
|---|---|---|
| 1 | **9 条读音抽查** | ✅ 定案：7 条按**同表内部一致性**判读（不改语义）· 1 条改正（泉识坊 senzuiboo→**senshikiboo**）· 2 条私用区前缀**读不出**，改纯 ASCII 占位 id |
| 2 | **16 个当主的家族名** | ✅ 改为**组织名**（透波 / 安东 / 坊津 / 钵屋 / 羽黑 / 轩猿 / 山潜 / 甲贺 / 丹后 / 土佐），家 id 同步（`clan_suppa_1` ↔ 透波家） |

### ⚠️ T1 之「待修的数据冲突」—— ①②③ 已修，④ 仍挂

| 位置 | 规模 | 状态 |
|---|---|---|
| `TaikouHero.City_<年>` | 4 格 | ⏭ 仍挂 —— **不用裁定**，按「上级日志为准」机械改即可 |
| `TaikouHero.Kingdom_<年>` | 24 格 | ⏭ 仍挂 —— 同上（含「追加段 2 人空列」需顺手看一眼） |
| `TaikouHero.ClanID_<年>` | 66 格 | ⏭ 仍挂 —— **要先查清**（疑 R5 亲属经**名字**解析）再改；另有 460 格「已死亡者仍挂家族」待判要不要清 |
| 🔴 `Settlements.Owner_<年>` | 整列 | ✅ **已接回「据点日志」**（2026-09-12，见「0.9」）——改 90 格 · **1644 格全覆盖、零兜底** · 新增常驻校验 |

> 🔴 **口径已定（用户裁定）：`据点日志.md` 是唯一信源**（按据点记、每格给当主）。
> ⚠️ **别再用「上级日志」推据点归属**——那份**按人记**（每人一个 `据点` 字段 = 当年在哪），
> 实测 1644 格里 **436 格当年无人驻守**（八户城全六代没人），拿它推必然有覆盖缺口。

### ❓ 待用户拍板（截至 2026-09-12 未做，逐条都能一句话回）

| # | 事项 | 要定什么 |
|---|---|---|
| 1 | **`std_scn_okehazama.xml` 处置** | 剧本中文文件三处不合规（未登记 / 结构错 / 键名不符）→ 建议先挪出 `Languages/`，等剧本工程接入时按规范放回 |
| 2 | **两条「同一对象定义两遍」** | ①`NPCCharacter.guard` 在 `spnpccharactertemplates.xml`（等级 11）与 `spnpccharacters.xml`（等级 4）各一份，**现生效 = 4**，取哪份 ②`taikou_world_lore_strings.xml` 与 `world_lore_strings.xml` **78 个键完全重复**，留哪份（已由 `check_era_segments.py` 常驻 WARN） |
| 3 | **四·补遗留 3 条**（复核后仍活） | ①`lord_tk5_579` 畠山义续 `EnglishName` 缺空格（`HatakeyamaYoshitsugu`）②`lord_tk5_740/741` 柳生 `FirstName` 仍是「柳生石」「柳生兵」③C 组 17 位女性身份未定 |
| 4 | **选人流程复刻太阁5 三处**（设计已写） | ①推荐人名单来源 ②详情页字段映射（官位/俸禄/功勋/流派我们无对应物）③立绘资产是否现在接 |

> ⏭ **待实机验证（不阻塞）**：两个内容包同开时 **kinai/kanto… 文化重复定义**（Taikou 单开无碍——按裁定它就该单开）。

### 🔴 新 session 先读这 5 条（能省你半天）

1. 🔴 **两份日志，先分清「按人记」还是「按据点记」**（这是本会话踩出来的最大教训）：
   · **上级日志** `Knowledge/太阁5/太阁日志/上级日志.md`（每年固定 1304 人）—— **按人记**（每人一个 `据点` 字段 = 当年在哪）。
     用途：**英雄/家族/势力的归属**（`立场/势力/组织/上司/当主/部下/陪臣`）。⚠️ **不要拿它推「据点归谁」**——没人驻守的城就没有行（实测 1644 格里 436 格没人）。
   · **据点日志** `Knowledge/太阁5/太阁日志/据点日志.md`（每据点每年一行）—— **按据点记**（直接给 `当主`）。用途：**据点归属**，274×6 = 1644 格**全覆盖**。
   任何冲突**以日志实际打印的为准**。⚠️ 解析前**必须过 `tk5_pua_names.restore()`**（私用区字符，不过这道必然对不上）。
2. **一条好用的判据**：日志里 `据点 = 胜山馆`（1598 年该槽改名 `松前城`）= **此人当年不在场（已死亡）**。
   实测「英雄表说已死亡」的 1145 格 **100% 落在这个槽**，0 反例 —— 这让「已死亡」从不可验证变成可验证。
3. **泛用 NPC 用「合成号」**（0.8 的关键机制）：那 329 个泛用 hero 的 ID 是名字式的（`lord_tk5_ninja_rokurooji`），
   且**每年占的槽号还不同**（仙左卫门 = 1108 / 1560 是 1120）。所以生成器内部给每人一个**合成号**（9000 起）当键，
   日志按年重键，**输出边界再换回真 ID**（`OUT_ID[键]`）。
   🔴 改生成器时**禁止**写 `"lord_tk5_" + 键` —— 那条只对真 DX 号成立。
4. **势力表只剩一张**：`TaikouForce.csv`。🔴 **`ForceTaikou.csv` 已于 2026-09-12 退役删除**（它的六列快照 `Owner_<年>`
   是**错误数据**——快照口径，编号还是废弃的织丰体系；用户裁定「以日志口径为准」）。
   生成器 `gen_taikou_force_csv.py` 现在**读自己、原地刷新**：6 列静态值（ID/太阁编号/势力名/别名/势力类型/Culture）
   是唯一真源，`Owner_<年>` 每次按上级日志重算。
5. **改数据一律改脚本再重跑**（铁律 22）—— `python Scripts/run_all_checks.py` 会抓出过期产物。
   ⚠️ `Debug/StoryEngine_RuntimeLog.txt` 是运行时日志，与这些数据检查无关。

### 已完成（详见下方 0.7~0.11）

| 步 | 内容 | 结果 |
|---|---|---|
| **T1–T3** | 建世界 → 建号 → 日本图 → 点京 → 进城中心 | 全通（2026-09-07~10） |
| **T4-a** | 英雄集合 | 1111 → **1440** 行（+329 泛用 hero） |
| **T4-b** | 四表定义与交叉引用 | 硬错误 0（常驻体检） |
| **0.7** | 势力源表合并（Kingdom→ForceTaikou）+ 家族表列裁剪（19→16 列） | 23 绿 |
| **0.8** | **泛用 NPC 提升为 hero（329 人）+ 按日志逐列纠错** | 实名替补 15→0 · 不出势力 28→0 · 假家族 5→0 |
| **0.8b** | **`ForceTaikou.csv` 退役删除**（快照口径=错误数据，日志口径为准） | 势力表只剩 `TaikouForce.csv`（读自己原地刷新） |
| **0.9** | ✅ **T1 清尾巴**（读音 9 条 · 16 家家族名 · **据点归属整列接回据点日志**） | 改 90 格（含 2 格确凿错）· 1644 格全覆盖零兜底 · 新增常驻校验 `gen_taikou_settlement_owner.py` |
| **0.10** | ✅ **文化体系定案**（16→17 个文化**全部自建** · 5+1 个身份文化 · 大航海联动/南蛮/游荡者归口 · `Culture.csv` 收敛为 `ID/Name/Alias`） | 「与织丰零依赖」写进 **CLAUDE.md 铁则 6** · 一键体检 **23 绿 / 1 红**（红 = 雷 53） |
| **0.11** | ✅ **河野父子（袭名）收口**（外观槽互换 · 据点归属的袭名钉死表 · 显示名留给 C2） | 连带修 `ProfileStages.csv`（儿子原先**一条立绘都没有**） |

---

### 零、✅ 家族按侍奉关系重建（T4-b 收尾，2026-09-11 完成）

#### 0.0 🔴 交接须知（新 session 先读这段）

**这一步做什么**：家族（clan）的定义**推倒重来**——✅ **已落地（2026-09-11）**。

- **旧定义**：按苗字（同姓即同族）+ 亲情。663 个家族，大量「同苗字硬拆家」的补丁。
- **新定义**：**只看「谁侍奉谁」**（详见 0.2）。一家 = 一个家头 + 直接侍奉他、且自己不带人的人。
- **旧 Clan.csv 已存档** `csv/_archive/Clan_织丰口径_20260911.csv`，新表由生成器从零产出（**296 行**，2026-09-12 0.8 后）。
- **六年代各算一套**（人物换主频繁：1304 人里 930 人跨年代换过所属组织、808 人换过立场）。

**改家族数据怎么做**（生成物，铁律 22）：
```bash
python Scripts/gen_taikou_clan_csv.py            # dry-run：逐年代家族数 / 无家人数 / 异常清单
python Scripts/gen_taikou_clan_csv.py --apply     # 落盘（自动备份 + 往返校验 + 幂等两跑）
python Scripts/run_all_checks.py                  # 收尾体检（应 **23 绿 / 1 红**，红 = 雷 53）
```

**基线**：`run_all_checks.py` 家族重建前 = 17 绿 / 3 红；**现在 = 23 绿 / 1 红**（2026-09-12，红 = **雷 53** 距离缓存）。⚠️ 曾有一段时间记录为「23 绿 / 0 红」——**那是错的**（雷 53 一直在红），2026-09-12 订正。

**六年代势力清单（要建时代段时用）** → [csv/_analysis/年代势力存在表_20260911.md](../Knowledge/太阁5/骑砍2织丰角色ID对应/csv/_analysis/年代势力存在表_20260911.md)

**生成物一览（都别手改）**：`Clan.csv` · `TaikouHero.csv`（`ClanID_<年>` / `CultureID`）· `Settlements.csv`（`Owner_<年>` + `Clan_<年>`）·
`TaikouForce.csv` · `Culture.csv`（**非生成物**，人维护）· 英雄画像表 · 选人目录 —— 改内容改对应生成器重跑。

**🔧 本会话新增的三个生成器（跑法，按依赖顺序）**：

| 生成器 | 管什么 | 命令 |
|---|---|---|
| `gen_taikou_settlement_owner.py` | `Settlements.Owner_<年>`（据点日志口径，1644 格全覆盖） | `--apply` / `--check`（已进一键体检） |
| `gen_taikou_clan_csv.py` | `Clan.csv` / `ClanID_<年>` / `Settlements.Clan_<年>`（**跟随 Owner**，必须在上一条之后） | `--apply` / `--check` |
| `gen_taikou_wanderer_culture.py` | `TaikouHero.CultureID`（游荡者=ronin / 大航海联动=pirate / 南蛮=namban / 非人物行按模板身份） | `--apply` / `--check`（**须在家族生成器之后**，它依赖 `ClanID_<年>`） |
| `gen_taikou_culture_full.py` | Taikou 的 `spcultures.xml`（**17 个文化全自建**）+ 40 个场景 NPC 模板 | 直接跑（无 --check，跑完必过 `check_taikou_culture_variants`） |
| `gen_taikou_english_strings.py` | 英文语言层（从数据 XML 的内联 fallback 抽取） | 跑 / `--check` |

> 🔴 **改生成器前先备份 + 跑 + `diff`**：实测生成器模板可能与现网分叉（模板里还是旧 id）→ 一重跑就**回退**改名脚本的成果（必备清单雷 95）。

**结论先行**：**家族 = 家臣团**（政治单位），不是血缘族；六个年代各一套；浪人无家（骑砍侧当游荡者）。

#### 0.1 ✅ 势力侧 —— 已定死（`TaikouForce.csv`，185 行）

| 项 | 结论 |
|---|---|
| 表 | `Kingdom.csv` × `ForceTaikou.csv` **合并为 `TaikouForce.csv`**（生成器 `Scripts/gen_taikou_force_csv.py`，铁律 22：改内容改脚本重跑） |
| 列序 | `势力类型 \| ID \| 势力名 \| 短名 \| 别名 \| Culture \| Owner_1554…1598 \| 太阁编号` |
| 🔴 **`Owner_<年>` 的语义** | **= 该年代此势力存不存在 + 当主是谁**（`-` = 不存在）。**生成某年代的 XML 时只看这一格** |
| 🔴 **存在性口径 = 上级日志**（2026-09-11 用户裁定） | **某家某年存在 ⟺ 上级日志里该年有人「组织」= 该家、且「立场」= 当主**。当主是泛用 NPC（透波众「六郎次」、轩猿众「道闲」…不在英雄表）→ 从同组织同年代成员里挑**实名替补**（部下多优先）。**`_analysis/decoded/era_v2` 快照口径作废、不再参考**。实测六年代存在格 = **96/95/90/82/72/90**（旧口径 87/89/84/76/66/83）；15 格走了实名替补（高坂甚内/加藤段藏/猿飞佐助/海雷丁/金光文右卫门） |
| 🔴 连带（用户裁定） | **忍者村/海盗众/商家各是一个势力**（日志里它们都有当主）——旧设计「商家/忍者/海贼 = 独立家族不建国」**作废** |
| ~~该年无人则不出~~ | ✅ **已作废（2026-09-12，见 0.8）**——原记录「6 家在某些年代成员全是泛用 NPC → 该年不出势力（28 格）」。**提升泛用 NPC 为 hero 后这些家都有当主了**，实测「该年无人挂它」= **0 格** |
| 值 | 全部换成可解析的 `lord_tk5_*`（改前 386 格是织丰编号，其中 103 个连织丰 XML 里都没有） |
| 归并 | `ikko_shu`（一向宗）→ `honganji`（本愿寺）。⚠️ 原来那句「`Clan.csv` 7 行已同步」**已作废**——Clan.csv 整表重建后不再引用 `ikko_shu`（见 0.3 退役行） |
| ⚠️ **旧「存在性三方对账」= 走错路的记录（留档防重踩）** | 曾拿**解码快照**逐格对，得出「势力表的 `-` 可信、不需要按日志重推」——**该结论已被用户推翻**（见上一行的日志口径）。错因：快照有一批槽位**名字没解出来**（显示「无」/空），比对时被当成不存在。**别再走这条路** |
| ⚠️ **快照的「势力代表」≠ 日志的「当主」（159 格，38%）** | 快照 `lord_pid` 记的是「这家派出的代表」（可能是家臣），DB 的 `立場` 记的是「谁是一家之主」。**现在统一按日志的当主**（快照口径已退役）|

#### 0.2 🔴 家族侧 —— 新规则（按侍奉关系重做，2026-09-11 用户裁定）

**结论**：家族的唯一定义 = 「谁侍奉谁」。一家 = 一个家头 + 直接侍奉他、且自己不带队的人。**苗字相同 ≠ 同族，血缘 ≠ 同族。**

**规则表**（落地时逐条对应，无例外）：

| # | 情况 | 归谁 | 例（1560 实测） |
|---|---|---|---|
| R1 | **有部下**（有人直接侍奉他） | **自己开一家**（他就是家头） | 北条氏政只是氏康的直臣，但北条氏直侍奉他 → 氏政自立一家 |
| R2 | 没部下、有上司 | 并入**直接上司**的家 | 北条氏直 → 北条氏政家 |
| R3 | 没部下、没上司 | **无家**（浪人 / 无所属） | 石田三成（1560 是浪人） |
| R4 | 妻子 | 并入**丈夫**的家 | 归蝶 → 织田家 |
| R5 | 其他亲人（父/母/祖父/亲戚）**既没上司也没部下** | 并入该人的家 | 武田信虎（隐居无主）→ 武田家 |
| ~~R6~~ | 🔴 **已删除（2026-09-12，见 0.8）**——原规则「势力该年存在却没人挂它 → 取势力表代表自立一家」。**它把直臣提升成家头，违反 R1**（造出 `clan_hairedin_1` 等 5 个假家族）。根治办法 = 把泛用 NPC 提升为 hero（0.8），**别再写回这条兜底** | — | — |

**为什么 R1 这么定**：一句话——**谁有人侍奉，谁就是一家之主**；「当主的家 = 自己 + 部下里不带人的那些」，所以带了人的部下自然另立一家。这正是太阁5「城主/国主有自己的家臣团」的形态。

**拆家后果（要接受的事实）**：大名家会拆成 2~6 家。1560 年北条家 → `clan_hojo_1`（氏康）/ `_2`（氏政）/ `_3`（高广）/ `_4`（氏邦）/ `_5`（氏繁）/ `_6`（纲成）。

**六个年代各算一套**（不共用一份）：

| 年代 | 1554 | 1560 | 1568 | 1575 | 1582 | 1598 |
|---|---|---|---|---|---|---|
| 存在的势力（王国） | 96 | 95 | 90 | 82 | 72 | 90 |
| 家族数 | 217 | 199 | 185 | 161 | 131 | 109 |
| 其中无王国的独立家族 | 0 | 0 | 0 | 0 | 0 | 0 |
| 有家英雄 | 857 | 866 | 838 | 786 | 704 | 522 |
| 无家者（浪人/无所属） | 180 | 171 | 199 | 251 | 333 | 515 |
| 妻子并入 / 亲人并入 | 22 / 23 | 23 / 40 | 22 / 68 | 19 / 88 | 18 / 119 | 14 / 162 |

- 理由：六份剧本快照里 **930/1304 人换过所属组织、808 人换过立场**——一套数据套六年代必错。
- 跨六年代合计 **296 个不同家族**（按 id 计；比单年代的 217~109 多，因为人会换家）。1598 家族少 = 大量人物已卒/隐居。
- 家族数与骑砍原版同量级（原版 8 文化约 100 家），274 个据点分摊得住。

**跨年代「同一个家」怎么认**（否则「织田信长 1554-1582 / 织田秀信 1598」会被拆成两个 id）：
家头同一个人 → 同一家；或两个家头是**直系血缘**（父/母链）**且从未在同一年代同时当家头**时合并，
且**按血缘距离从近到远并**（先父子、再祖孙，避免「先到先得」把孙子并到祖父那家）。
⚠️ 北条氏康与北条氏政是父子、但 1554~1582 **同时当家头**（各自有家臣团）→ **不并**，这是规则的正确结果。

**命名与 id**（用户裁定：**名字相同、id 不同**）：

| 项 | 规则 | 例 |
|---|---|---|
| id | `clan_<苗字罗马音>_<序号>`；序号按「首次出现年代 → 该年规模 → 历代最大 → 家头编号」排 | `clan_hojo_1` |
| 名字（苗字） | 取**规模最大那年**的家头 `FirstName`（改名家按最终形态命名：长尾政景→上杉景胜 = 上杉家）；同名不另起名，6 个「北条家」= `clan_hojo_1..6` | 北条家 |
| 英文 / 本地化键 | 苗字罗马音（= 家头 EnglishName 首块；家头没英文名走旧表兜底）/ `{=TAIKOU_clan_<id>}<罗马音>` | `{=TAIKOU_clan_hojo_1}Hojo` |
| 文化 | 该家成员多数文化 | `kanto` |
| 王国 | 家头该年代的组织 → `TaikouForce.id`（该组织当年存在 = 日志里它有当主）→ **每家都有王国**（实测六代「无王国」= 0） | 德川家 1560 → `imagawa`（今川的家臣）；赤松一门 → `akamatsu`（自己就是国） |
| 无家者 | **全部当游荡者 wanderer**（用户裁定）：无家族 + 酒馆可招募 | 石田三成 / 大谷吉继 / 公家 / 茶人 |

**🔴 每个家族都挂进一个王国（实测六年代「无王国」= 0 家）**：
家头的「组织」只要该年存在（= 日志里该组织当年有当主）就当王国；赤松一门这类**国主家**在日志里
本身就是「当主 + 上司=无」→ 它是**自己的一个国**（赤松家 1554-1575 存在），小寺/别所（组织=赤松家）自动挂它下面。
⚠️ 旧稿「这类家落成独立家族（无王国）」**已作废**（用户裁定：国主也是「对大名负责」的一层，不该悬空）。

**为什么同名不另起名**：骑砍 UI 里家族名旁边就是当主名，同名不会认错；编造「北条氏政家」这类叫法反而出戏。

**引擎事实（不变，反编译已核实）**：`Hero.MapFaction => Clan?.Kingdom ?? Clan` —— 同一家族所有人必然同属一个王国，所以「父子各为其主」只能靠**拆家**表达（新规则天然做到：各为其主的父子各自带队 → 各自一家）。

#### 0.3 连动面（改一处必须一起动）—— ✅ 本轮已全部落地

| 面 | 动作 |
|---|---|
| `Clan.csv` | ✅ **整表重建**（**296 行**，2026-09-12 0.8 后）：`ID \| Name \| Alias \| Culture \| Owner_<年>/Kingdom_<年> ×6`（2026-09-12 裁掉 4 个织丰遗留列，见 0.7）；旧表存档 `csv/_archive/Clan_织丰口径_20260911.csv` |
| `TaikouHero.csv` | ✅ `ClanID` 单列 → `ClanID_<年>` ×6（1037 人 × 6 格；空 = 该年无家＝浪人） |
| `Settlements.csv` | ✅ `Clan_<年>` 重算 = 城主（`Owner_<年>`）的家族；**302 格变化**；2 格例外（河野通直 1582 是浪人却当着城主）→ 沿用他有家的最近年代 |
| 画像 / 选人目录产物 | ✅ 重跑（`gen_taikou_hero_profiles.py` 的 `clan` 取 1560，1560 无家则退到最早有家的年代） |
| `TaikouForce.csv` | ✅ **存在性 + 当主口径整体换成上级日志**（用户裁定）：96/95/90/82/72/90 格存在；`@商人/@忍者/@海贼` 模板标记**退役**（改成实名当主）；15 格实名替补 |
| 旧脚本退役 | ✅ `gen_taikou_force_csv.py` 的 **Clan.csv 同步块退役**（新 Clan.csv 没有单列 `Kingdom`，也不再引用 `ikko_shu`——`honganji` 的别名已含「一向宗」）；`split_taikou_conflict_clans.py` / `split_taikou_duplicate_clans.py` 随「同名撞车」问题一起作废 |
| checker | ✅ `check_taikou_world_tables.py`：`Clan.Kingdom`→`Kingdom_<年>`、`Clan.Owner`→`Owner_<年>` 三态闸门、`TaikouHero.ClanID`→`ClanID_<年>`、**新增「`Owner_<年>` ↔ `ClanID_<年>` 互证」**、删「家族归属分歧」（40 格裁决作废）、空国警告改**逐年代**口径（只在「该年确实存在」的格上查——33 个永不建国的武家不再当噪声）；`check_englishname_clan_prefix.py` **重写**（旧规则「每个英雄 EnglishName 前缀 = 他的 ClanID」作废 → 新规则「家族 id = 家头 EnglishName 首块」） |
| `run_all_checks.py` | ✅ 接入 `gen_taikou_clan_csv.py --check` → **23 绿 / 0 红**（2026-09-12 0.8 后） |
| 冻结工程 | ⏭ `plans/scenario-campaign-mode/tools/GenerateXml.py:115` 读旧 Clan 列（`Kingdom`/`Owner` 单列）→ **解冻时指向 `_archive/` 快照或改列名**（本轮不动冻结工程） |

**遗留（不阻塞 T4-c，写清等什么）**：

| 项 | 说明 | 怎么办 |
|---|---|---|
| ~~6 家忍者/水军某些年代无实名成员~~ | ✅ **已解决（2026-09-12，见 0.8）**——原先这 6 家的成员全是泛用 NPC（英雄表 0 命中）→ 该年不出势力（28 格）。**现把泛用 NPC 提升为 hero**，它们都有当主了，实测「该年无人挂它」= 0 格 | — |
| 首领实名替补 | 15 格是「模板首领 → 同组织里部下最多的实名成员」（如 安东水军→海雷丁、土佐水军→金光文右卫门） | 语义准确（是那家的人），但未必是史实首领；要精修就登记 `FORCE_OWNER_OVERRIDE` |
| 势力槽快照（`era_v2`） | **已退役，不再作为任何判据**（保留在磁盘供考古） | 别再拿它做「存在性/宗主」的依据（本轮曾因此得出错误结论） |

**旧 TODO 1~6 的处置**：①加 `ClanID_<年>`×6 —— ✅ 做了（全员六格，生成器写）；②裁决 40 格 —— ❌ **作废**（新规则下没有「家族归属分歧」这个概念）；③`Kingdom` 列退役 —— ✅ **升级**为 `Kingdom_<年>`×6；④`Owner` 204 条织丰编号 —— ✅ **作废**（生成器直出 `lord_tk5_*`）；⑤`Kingdom.csv`/`ForceTaikou.csv` 去留 —— **不变**；⑥六时代段 —— **不变**（等家族表落地后由 `gen_taikou_era_diff.py` 派生）。

#### 0.4 本次会话（2026-09-11 下半场）产出

| 产出 | 脚本 |
|---|---|
| 🔴 **家族按侍奉关系重建**（Clan.csv **296 行** + `ClanID_<年>`×6 + 据点 `Clan_<年>`） | `Scripts/gen_taikou_clan_csv.py`（新；`--check` 进一键体检） |
| 🔴 **势力源表收编**（Kingdom.csv → ForceTaikou.csv，后者成唯一源表） | `Scripts/migrate_taikou_force_source.py`（一次性；`gen_taikou_force_csv.py --check` 现也进一键体检） |
| 势力表合并（135+185−1 → 185） | `Scripts/gen_taikou_force_csv.py` |
| 英雄表年代格修正（4 净改 + 2 登记例外） | `Scripts/fix_taikou_hero_kingdom.py` |
| 同姓家拆分（3 对手工 + **57 个自动**） | `Scripts/split_taikou_duplicate_clans.py` · `Scripts/split_taikou_conflict_clans.py`（**随新规则作废**） |
| macron 清除（166 格）+ 柳生家三合一 | `Scripts/convert_taikou_macrons.py` |
| 体检改造 | `check_taikou_world_tables.py`（列名换代 + 互证闸门）· `check_englishname_clan_prefix.py`（**重写**）· `run_all_checks.py`（+1 项） |

#### 0.5 本次的删列与边界（记录在案，免得后人问"这列去哪了"）

| 项 | 处置 |
|---|---|
| `TaikouForce` 的 `Is_Shokuho` | **删除**。消费者（`GenerateXml.py` / `gen_entity_maps.py`）在**已冻结的剧本工程**里、且读的是 Clan/Kingdom 表；**`Clan.csv` 与 `Culture.csv` 各自保留自己的 `Is_Shokuho`**，不受影响 |
| `TaikouForce` 的 `Is_1568` | **删除**。语义待考（与 `Owner_1568` 有 38 条不一致，试过「当主换人」「1568 新出现」两个假设都不成立），**无人消费** |
| `TaikouForce` 的 `LocozationName` | **删除**。134 个 `{=key}` 全悬空（95 个 `my_*` 在 Taikou 语言包 0 命中；39 个织丰随机键只存在于 `Shokuho/ModuleData/`，本模块不加载）。**势力英文名就用 ID**，真正的本地化留到后续 |
| `Kingdom.csv` 单列 `Owner` | **丢弃**（与 `Owner_<年>` 重复，且 40 条是织丰编号 —— 那条红的根源） |
| `BaseInfo.csv`（上游镜像） | 它的「罗马音名」列**已跟着去 macron**（它是英雄名的另一份表） |
| `shokuho_heroes_full.csv`（织丰原始转储） | **没改**（里面 `Sōma Takatane` 是织丰自己的数据，改了会跟源分叉） |

**🔴 连带重跑纪律**：改 `TaikouHero.csv` 的 `ClanID_<年>` 或改 `Clan.csv` / `TaikouForce.csv` → **`gen_taikou_hero_profiles.py` 的产物会过期**，
`run_all_checks.py` 的一致性检查会抓到（实测抓到过一次）。**改完记得重跑生成器。**

#### 0.6 🔴 经验（本次踩的坑，别再踩）

1. **拿英雄表的 `Kingdom_<年>` 推"有哪些势力" = 凭空造国**。实测 68 条口径差（赤松/虎屋/秋田/池田…）——英雄表记「当年跟着谁（含同族家名）」，快照记「势力槽」。**势力存在性只看 `TaikouForce.Owner_<年>` 的 `-`**。
2. **对账必须按编号 join，禁按人名**。快照的 `name` 列自己 README 就标了「勿再引用本行作权威」；按名字 join 会把明智光秀、柴田胜家都算成「芦名家」。
3. 🔴 **游戏数据在"过继/改仕"类年代边界上会错，必须查史料**（连踩两次）：上杉景虎被快照放进 1568 上杉家，实际 **1570 元龟元年**才过继；吉见正赖被标 1554 毛利家，实际 **1557** 大内灭亡后才从属。这两格登记进 `KNOWN_DEVIATIONS`——**故意与快照不一致**，白名单防被"对账"改回去。
4. **`CareerStance=首领` 不是"族长"，是"势力代表"**。家臣家族（下间/前波/七里）的成员标 `直臣`，本来就没有独立首领；"家族没有族长"是正常的。
5. **`Identity=大名` 会过度分配**：无主家的城主被标成大名 → 曾导致 9 格势力「多标为存在」（已回退）。**存在性与值要分开处理：只换值、别动标记。**
6. **同名撞车要用「归属集相交」聚类来发现**：内藤/津田/天野/加藤… 实测 65 个家族中招 → 拆出 57 个新家族。⚠️ **归属集为空的成员（女性/隐居/已故，如阿松/武田信虎）要并回最大一组**，否则被误拆。
7. **macron 规范在 `csv/ReadMe.csv` 里**（当年写的 VBA 规则原文）：`ō→oo`（**不是 `ou`**）、`ó→oo`（acute 与 macron 同等）。英雄英文名早就用 `oo`（`Katoo`/`Kyoogoku`/`Motoosa`），`check_englishname_clan_prefix.py` 的规范表也是 `oo`。
8. **生成器脚本别接 `head`**：`... --apply | head -6` 会 SIGPIPE 打断写盘，`--check` 随即报"产物不一致"。
9. **按首列去重要限定表**：对 `BaseInfo.csv` 去重会误删 459 行（它的「内置番号」天然不唯一）。
10. **改数据一律走脚本 + 备份 + 往返校验 + 幂等两跑**——本次靠这套回滚了三次（macron 转错两次、误删行一次），没有在错状态上打补丁。

**家族重建（2026-09-11 下半场续）新踩的六条**：

11. 🔴 **Python 的 `set` 迭代顺序不保证（`PYTHONHASHSEED` 随机化）→ 生成器会「每次跑出不同结果」**。
    症状：`--check` 永远红、三张表互相不一致（实测 `clan_araki_1` 的文化一次 `kinai` 一次 `nankai`）。
    **规矩**：凡 set 参与顺序（遍历、建组、取「第一个」）一律 `sorted(...)`；`Counter.most_common` 的并列要靠 `sorted(key=(-n, 名字))` 定死。**生成器不许有第二次结果不同的可能**。
12. 🔴 **合并类算法要「重验守卫」**：一轮里 A 吃掉 B 之后，A 的家头集合变了，但候选列表是吃掉之前算的 →
    另一条候选可能把「同年并存的两家」并进来（实测 `clan_oda_2` 1575 出现两个家头）。
    **两条防线**：①每轮合并时**重新验证**不变量；②生成器里加**自证断言**（「一家在任一年代只能有一个家头」）——本轮就是这条断言抓出来的。
13. 🔴 **合并/配对要按「距离」排，不能「先到先得」**：先到先得会让孙子并到祖父那家（实测长宗我部盛亲本该并元亲、却先并到了祖父国亲）。改成**按血缘距离从近到远**。
14. **外键映射要带「类型」消歧**：太阁5 的组织名里武家/商家/忍者/海贼会**同名**（`茶屋家` vs `茶屋`），
    只按名字建 `name→id` 会串。**规矩**：多类型同名时按源数据的类型列消歧（势力类型：大名家/商家/忍者众/海贼众）。
15. 🔴 **「组织/势力名」不等于「存在的势力」**：太阁5 的勢力槽每代只有 ~99 个，33 个武家（赤松/松平/池田…）
    六个年代都没有槽位——**引用一张表前先查它的「存在性列」**，否则会造出指向空王国的家族（本轮差点如此）。
16. 🔴 **「快照（decoded）」不能当权威——本轮被它带偏一次**：`era_v2/forces.csv` 有一批槽位的**名字解码失败**（显示成「无」/空），
    我据此得出「赤松没有势力槽」的结论并设计了「独立家族」方案；用户当场纠偏：**只看上级日志**。
    **规矩**：判「谁存在/谁归谁」只认**游戏运行时导出的日志**（上级日志/据点日志）；解码快照只作考古参考，
    且**引用前先查它有没有解码空洞**（空名/「无」占位、「待替换(ID N)」占位都是信号）。
17. **势力首领可能是泛用 NPC → 要一条「实名替补」规则**：忍者村/水军的头目在 DB 里常是 `六郎次`/`道闲`/`仙左卫门`
    这种**不在英雄表**的泛用槽位人。**规矩**：当主不在英雄表 → 从**同组织同年代**的成员里挑实名替补（部下多优先），
    并把「谁替了谁」打进生成报告（本轮 15 格：透波众→高坂甚内、轩猿众→加藤段藏、甲贺众→猿飞佐助…）。
18. **改名/继承类差异要用「最大那年」的形态命名**：同一家历代家头苗字不一（长尾政景→上杉景胜），
    按「第一个家头」命名会得到「长尾家」（而它在游戏里该叫上杉家）。**规矩**：取规模最大的那年的家头，并把这类家**列进报告**供人核。

#### 0.7 ✅ 表结构收尾（2026-09-12）：势力源表合并 + 家族表列裁剪

**一句话**：`Kingdom.csv` 退场归档（它的独有贡献只有 `Culture` 一列，并进 `ForceTaikou.csv`）；
`Clan.csv` 砍掉 4 个织丰遗留列。四表交叉引用体检**全绿，不动**。

##### 一、体检结论（诊断）

脚本 `Scripts/check_taikou_world_tables.py`（已挂 `run_all_checks.py`），覆盖
TaikouHero ↔ Clan ↔ TaikouForce ↔ Settlements 全部引用链 + 织丰编号闸门 + 据点名唯一性 +
身份↔据点类型对角线。**结果：硬错误 0。**警告 5 类逐条判读，只有一条要动：

| 警告 | 判读 |
|---|---|
| 🔴 「Settlements 在 1560 无指定当主（74 个）」 | **检查自身写死了年份**（遗留）。实测 1554 无当主 77 个、1560 74 个，只报了 1560。74 个里 **68 个六个年代全无当主**（62 町 + 6 砦 + 5 里 + 1 城 —— 太阁5 的町本就不设城主）→ 改成「六年代全无当主」口径 |
| 河野通直 1582 无家，town_tk145/146 沿用 1568 家族 | 生成器报告的预期行为，不是 bug |
| 33 个势力 / 11 个文化定义但无人引用 | 孤儿，信息性 |
| 无家英雄逐年递增 128→457 | 太阁5 真实形态（天下人收编），不是缺口 |
| 英雄表 `City_<年>` 地名查无 | **0 条** |

##### 二、`Kingdom.csv` 退场（实测依据）

| 项 | 实测 |
|---|---|
| 行数 | 135（133 与 ForceTaikou 共有 + 2 独有：`ikko_shu` / `noKingdom`） |
| **独有贡献** | **只有 `Culture`**（TaikouForce.csv 没这列，133 行的 Culture 全靠它）+ `noKingdom` 一行 |
| `短名` | 机械可推（实测 0/185 例外 = 势力名去掉尾部「家」），不算独有贡献 |
| 零消费者列 | `ScriptName`、`LocozationName`、`Is_1568` —— 全仓任何文件类型 0 命中 |
| `ScriptName` | 与 `ChineseName` **135/135 逐字相同** |
| 活代码消费者 | 只有 `gen_taikou_force_csv.py`（读 `ID`/`ChineseName`/`Culture`）+ `convert_taikou_macrons.py`（原地写） |

🔴 **所以「和 TaikouForce 重复」不成立** —— 它是**互补的另一半**。它的问题是**身份错位**：
已不是消费表，却还挂在生成器输入位、还带 5 个报废列（`Owner`/`Is_1568`/`IsShokuho`/`LocozationName`
合并时已丢，`ScriptName` 纯重复），所以看着像重复表。

**🔴 连带查出：`ForceTaikou.csv` 的 `Owner_<年>` 六列对产物零贡献**

实测这六列 396 格是织丰编号（`lord_1_*`）、89 格是 `@商人/@忍者/@海贼` 模板标记；
而生成器**每一格都从上级日志重算覆盖**，产物里 0 织丰编号、0 模板标记、525 个 `lord_tk5_*`。
→ 对输出**零贡献**（实证：拿 git HEAD 的旧脚本 + 旧源表重跑，产物与本轮**逐字节相同**）。

🔴 **但这六列必须留着——用户裁定「需要保留，并且要时刻保持交叉验证」**：
  它是**势力槽快照口径的唯一逐格记录**。`_analysis/` 下的派生表只是**中间状态文件**（不作留档依据），
  且实测有 **42 个取值**（忍者/商人/海贼头目，如 `lord_1_katoo_dankuroo` 加藤段藏）在别处**无留档**。
  ⚠️ 中途曾按「死输入、留着误导人」删掉，**当日即恢复**——错过一次，别再删。
  **两套口径留着互相对照才有价值**（快照有解码空洞 → 正是雷 85/87 换口径的原因，但快照本身仍是对账的基准）。

**落地**：`Scripts/restore_force_owner_columns.py` 从备份回填。
🔴 **列名保持 `Owner_<年>` 不动** —— 与产物 `TaikouForce.csv` 同名**不是问题**：两张表记的本来就是
同一概念的两种口径，区分靠**文件名**。
（中途我曾擅自把那六列改名为 `快照当主_<年>`/`SnapshotOwner_<年>`，**属未获授权的自作主张，已撤回**——
用户裁定：不许自己改列名、不许自己加「快照/snapshot」这种描述。）

##### 二·补、🔴 势力两张表的定位与生命周期（2026-09-12 用户明确）

| 表 | 定位 | 去留 |
|---|---|---|
| **`TaikouForce.csv`** | 🔴 **目标表**——进游戏的就是它 | **永久保留**；所有检查的主体都是它 |
| `ForceTaikou.csv` | **暂存源表**（TK5 官方口径的原始提取面） | 🔴 **数据提取完毕即删** |

⚠️ **本轮一度把两者搞反**（用户自述「这次的改动是我看错了」）：以为要保的是 `ForceTaikou`，
所以为它那六列 `Owner_<年>` 折腾了一轮。**实际要保的是 `TaikouForce`**。

**提取进度（实测，185 行逐格比对）**：

| `ForceTaikou` 的列 | 是否已进 `TaikouForce` |
|---|---|
| `太阁编号` / `势力名` / `别名` / `势力类型` / `Culture` | ✅ **185/185 逐格已提取** |
| `Owner_<年>` ×6（快照口径） | ✗ **未提取**（逐格重合 89 / 90 / 95 / 102 / 113 / 95） |

→ **`ForceTaikou` 里唯一还没交代完的就是那六列快照 Owner**。它的价值 = 给 `TaikouForce.Owner_<年>`
（日志口径）做**对账基准**——`check_force_owner_crossval.py` 就是这张进度表。
**判完即可删 `ForceTaikou`**；删掉之后交叉验证一并退役。

###### 🔴 那 194 格已判完：**0 异常**（2026-09-12，Claude 判读、给出结论不占用户时间）

| 类别 | 格数 | 原因 |
|---|---|---|
| 都有但人不同 | **63** | 快照记「这家派出的**代表**」，日志记「谁是一家**之主**」——口径差（高桥鉴种之于大友、北条高广之于上杉、一栗放牛之于大崎） |
| 日志有/快照无 · 商家/忍者/海贼 | **117** | 快照口径（织丰那张表）**只列武家**，没有这些家；新口径把它们也算势力（2026-09-11 裁定） |
| 日志有/快照无 · 武家 | **13** | 雷 85/87 的**快照解码空洞 / 无槽位**——赤松・有马・秋田・池田・少贰・十河・太田（**池田家 1598 正是雷 87 举的那个例子**）；另 2 格是上杉家 1554/1560，属「长尾家 → 上杉」的**别名合并** |
| 快照有/日志无 | **1** | 三好家 1575：快照记三好长治当主，但 1575 日志里三好众**全员浪人**（组织=无）→ 该年确不存在。史实吻合（三好長慶 1564 死后崩解） |

**桥的可靠性单独核过**：slug 桥能精确到人（`lord_1_katoo_yoshiaki` → 加藤嘉明），
不存在「家名指到同族另一人」的误桥（63 格里 19 格走转储、44 格走 slug，抽样逐个对得上）。

**结论：快照口径没有任何一格指向「日志口径错了」——目标表 `TaikouForce` 的当主数据可以放心。**
`ForceTaikou` 的使命到此完成：5 列已进目标表 + 剩余六列快照已判读。

**改动**：
1. `ForceTaikou.csv`：加 `Culture` 列（133 行取 Kingdom 值 / 52 个 `org_*` 填 `neutral_culture`）；
   加 `noKingdom` 行（`势力类型=Neutral`、`Culture=neutral_culture`）；
   `Owner_<年>` ×6 **保留**（列名不变，见上）
2. `Scripts/gen_taikou_force_csv.py`：去掉 Kingdom.csv 依赖与 `ikko_shu` 归并（源行没了归并自然消失，
   口径已在文件头留档）；`Culture` 改从 ForceTaikou.csv 直读；`Owner_<年>` 仍「完全按上级日志重算」
3. `Scripts/convert_taikou_macrons.py`：`TABLES` 去掉 `Kingdom.csv`
   —— 它是两表的**原地写入方**，不摘掉则归档后重跑会报缺文件
4. `Kingdom.csv` → `csv/_archive/Kingdom_织丰口径_20260912.csv`（归档不删，同 `Clan_织丰口径_20260911.csv` 之例）

##### 三、`Clan.csv` 列裁剪（实测，283 行）

| 列 | 实测 | 判定 |
|---|---|---|
| `ScriptName` | 与 `ChineseName` **283/283 逐字相同** | 纯重复 |
| `Surname` | 与 ID 罗马音块 **283/283 相同**（生成器就写 `g["rom"].capitalize()`） | 可从 ID 推 |
| `LocozationName` | 机械拼 `{=TAIKOU_<id>}<Surname>`；**282/283 的键在 Taikou 语言包不存在** | 悬空 + 可推 |
| `Is_Shokuho` | **283/283 恒为 "0"** | 零信息 |

**新列序**（19 → 16 列；`ChineseName` → `Name`，新增 `Alias`）：

```
ID, Name, Alias, Culture, Owner_1554, Kingdom_1554, …, Owner_1598, Kingdom_1598
```

**`Alias` 填法**（口径照 `TaikouForce.别名`：`|` 分隔、**不重复当前主名**）：

| 来源 | 落进 Alias |
|---|---|
| 生成器**已算出但只打印没落列**的历代苗字 | `clan_uesugi_1` → `长尾`；北畠家 → `木造` |
| 旧织丰表 `OtherName`（`_archive/Clan_织丰口径_20260911.csv`） | 木下家 → `羽柴\|丰臣` |

**改动**：
1. `Scripts/gen_taikou_clan_csv.py`：`CLAN_COLS` 换新列序；`render_clan` 写 `Name=zok` / `Alias=历代异名`
   （把已算出的 `names` 集合挂到 `g` 上供 render 用，按年代先后排）；
   往返校验的 `("ID","ScriptName","ChineseName")` 改 `("ID","Name")`；落盘前把现版 19 列 Clan.csv 存档
2. `Scripts/check_englishname_clan_prefix.py`：规则②（LocozationName 罗马音 ↔ id）**删除**
   ——列没了，且 id 本就由罗马音派生，该规则已成同义反复；报告里 `c.get("ScriptName")` → `c.get("Name")`
3. `Scripts/convert_taikou_macrons.py`：`ROW_MERGE_FILL` 的 Clan 条目 `ScriptName/ChineseName` → `Name`

##### 四、消费方实测清单（证明删列安全）

全仓扫描（含 `.gitignore` 屏蔽目录、Taikou / ShokuhoTaikouExpansionPack 模块）：

| 对象 | 实测消费者 |
|---|---|
| `Kingdom.csv` | 活代码只有 `gen_taikou_force_csv.py` + macron 工具 |
| `ForceTaikou.csv` | 活代码只有 `gen_taikou_force_csv.py` + macron 工具；**无冻结读取方** |
| `Clan.csv` 被删 4 列 | `check_englishname_clan_prefix.py`（改）+ 生成器自己（改）+ macron 工具（改）；`check_taikou_world_tables.py` **不读**这 4 列；罗马音兜底表读的是**归档文件**不是本表 |
| 模块 XML | **没有任何 XML 由 TaikouForce.csv 生成** —— `gen_taikou_era_diff.py` 是「diff 现有 XML」不读 CSV；CSV→`spkingdoms_<年>` 是**未实现的将来工作**（本文件 C2 项） |

冻结的 `plans/scenario-campaign-mode/tools/`（`GenerateXml.py`/`gen_entity_maps.py`）**指向的目录已不存在**
（`Knowledge/骑砍2织丰角色ID对应/csv`，缺 `太阁5` 段），**本来就跑不起来** → 删列对它们无语义影响。

##### 五、✅ 执行实录（2026-09-12）

| 动作 | 结果 |
|---|---|
| `migrate_taikou_force_source.py --apply` | ForceTaikou.csv 185 行；备份 `.bak_forcesrc_20260912_064545`（**快照列的取值来源**） |
| `gen_taikou_force_csv.py --check` | ✅ **产物 TaikouForce.csv 逐字节不变**（md5 `8ba718f8…`）——收编的是同一批数据、只是换了来源，最强的一条自证 |
| 🔴 `Owner_<年>` 六列**先删后恢复**（两次自作主张被纠） | 删除是我按「死输入」判的 → 用户裁定「**需要保留 + 时刻交叉验证**」→ 恢复；恢复时我又**擅自改名** `快照当主_<年>` → 用户再纠「**谁让你自己改列名的**」→ 撤回，列名保持 `Owner_<年>`。最终：`restore_force_owner_columns.py`（详见「二、」）。**实证无副作用**：拿 git HEAD 的旧脚本 + 旧源表重跑，产物与本轮逐字节相同 |
| `gen_taikou_clan_csv.py --apply` | Clan.csv 283 行 / 16 列；往返校验 + 幂等复跑通过；**TaikouHero.csv 与 Settlements.csv 逐字节未动**（md5 前后一致） |
| 现版 19 列 Clan.csv 存档 | `csv/_archive/Clan_19列_20260912.csv` |
| 新增常驻检查 | `check_force_owner_crossval.py`（双口径交叉验证）+ `gen_taikou_force_csv --check`，均进一键体检 |
| 🔴 **目标表补齐两道闸门**（2026-09-12） | `check_taikou_world_tables.py` 新增：**`势力类型` 枚举**（原先写错不报错——会静默排到末尾 + 跳过空国检查）+ **`太阁编号` 格式与跨行唯一**（空值合法：6 个织丰补造的家不在 TK5 势力表里）。**两道都做了负面测试**（故意写坏 → 报红 → 还原，`TaikouForce.csv` md5 不变） |
| `run_all_checks.py`（全量） | **22 绿 / 1 红** —— 唯一红 = 既有雷 53 距离缓存（与本次无关） |

**`Alias` 实测只填了 4 个家**（其余 279 个列空——本来就没有异名）：

| id | Name | Alias |
|---|---|---|
| `clan_kinoshita_1` | 丰臣 | `木下\|羽柴` |
| `clan_kitabatake_1` | 北畠 | `木造` |
| `clan_uesugi_1` / `_2` | 上杉 | `长尾` |

⚠️ 丰臣家这条要留个心眼：它 `Name=丰臣` 而 `id=clan_kinoshita_1`（id 取家头 `EnglishName`
"Kinoshita Hideyoshi" 首块、Name 取 `FirstName`）——**别名列反而把它救回来了**（木下/羽柴/丰臣 三个名字都能查到它）。
生成器找别名时**按「全部名字」反查旧表**，只按本名查会漏掉这个最典型的改名家。

顺带修掉的三处腐烂：
- `gen_taikou_force_csv.py --check` **挂进一键体检**（TaikouForce.csv 原先没有产物一致性守卫）
- `check_taikou_world_tables.py`：「1560 无指定当主」改「**六年代全无指定当主**」（原口径年份写死）、
  警告标题「在 ForceTaikou.csv 查无」→ 实际读 TaikouForce.csv、文件头列序描述更正
- 本文件里的「278 家」全部更正为 **283**（实际行数）

##### 五·补、🔴 势力当主双口径交叉验证（新增常驻检查，2026-09-12）

`Scripts/check_force_owner_crossval.py`——把「快照 × 日志」两套口径**逐格对照**，进一键体检。

🔴 **两张表的列名同为 `Owner_<年>`，区分靠文件名**（不许给列名加前缀/描述词）。

**桥**（两套编号不是一套，必须先桥）：`lord_1_amago_haruhisa` → 英雄表 `lord_tk5_*`
  ① slug 直桥（去掉前缀与尾序号 → 对 `EnglishName`）② 转储桥（`shokuho_heroes_full.csv` 的
  `StringId→NameRaw`）。**实测 188/188 全部可解析**——桥断了（英雄表或转储变了）直接报红。

**逐格判定（185 行 × 6 年代，共 526 格 = 快照非空 485 / 日志非空 525）**：

| 类别 | 格数 | 占比 | 含义 |
|---|---|---|---|
| 一致 | 332 | 63% | 两口径同一个人 |
| 日志有/快照无 | 130 | 25% | 日志发现了快照没有的当主 —— **雷 85/87 的主战场**（快照解码空洞） |
| 都有但人不同 | 63 | 12% | 口径差：快照记「这家派出的**代表**」，日志记「谁是一家**之主**」 |
| 快照有/日志无 | **1** | 0% | 唯一一格：**三好家 1575**（快照 `lord_1_miyoshi_nagaharu`，日志说该年无当主） |

**那唯一一格已经查明——正是这条检查该抓的东西**：1575 年日志里三好家全员都是**浪人**
（`三好长治` 立场=其他 / 势力=浪人 / 组织=无，其余三好众同），**没有任何人挂「三好家」当主**
→ 按口径该年三好家**不存在**。快照却记着「三好长治当主」。
史实也对得上：三好長慶 1564 死后三好家崩解，1575 年已是残党。**日志口径胜，快照那格是旧口径的错**。
→ 两套口径并排摆着的价值就在这里：**分歧不是噪声，是待判的名单**。

**判定口径**：❌ 硬错误 = 快照六列缺失/被清空、值非法、桥解析率 <100%；
❌ **漂移** = 四类计数 ≠ 脚本里的 `BASELINE`（**改任一源表 / 重导上级日志 / 改生成器都会动这个数**
→ 红了就是「有人动了其中一侧」→ 人判 → 判完把 `BASELINE` 更新成新值即为确认）。

##### 六、本次不动（记录在案）

- `TaikouForce.csv` 的 `太阁编号` 列**零消费者**，但**保留** —— 它是**正确**的数据（TK5 統一勢力番号
  = 对回官方数据的稳定键），与 `Owner_<年>` 那条**要删的理由不同**：那六列装的是**错的**数据
- `Culture.csv` 同一个毛病（21 行 `ScriptName ≡ ChineseName`、带 `LocozationName` + 恒 1 的 `IsShokuho`、
  表尾一串无名空列）—— 口径与 Clan.csv 相同，**待裁定**是否同批清
- `Debug/xlsx_dump/Kingdom.csv`、`ForceTaikou.csv` 是 gitignore 的孤儿转储，无人读取

#### 0.8 ✅ 泛用 NPC 提升为 hero + 日志口径纠错（2026-09-12 下半场）

**一句话**：给日志里出现过的忍者/海贼/商家泛用 NPC（**329 人**）建了稳定的 hero id 并取名；
连带把「势力/家族因为当主是泛用 NPC 而缺主」的问题**根治**（实名替补 15→0、该年不出势力 28→0）。
**四表体检 23 绿 / 0 红。**

##### 一、为什么要做

骑砍侧「家族要有家头、王国要有当主」。日志里忍者村/海盗众/商家的首领**大量是无卡泛用 NPC**
（「六郎次」「仙左卫门」），英雄表里没有 → 只能打两个补丁，**而两个都是错的**：

| 补丁 | 错在哪 |
|---|---|
| 势力表：从同组织挑**实名替补**当 Owner（15 格） | 权宜之计——当主换人后对不上，且替补本身在日志里是**别人的家臣** |
| 家族表：**R6 兜底**把「势力代表」提升成家头 | 把**直臣**（`立场=直臣`、`部下=0`）提升成家头，**违反 R1「谁有人侍奉谁就是一家之主」**——造出 `clan_hairedin_1` 等 **5 个假家族** |

##### 二、做了什么

**① 提升 329 个泛用 NPC 为 hero**

| 项 | 结果 |
|---|---|
| 识别键 | **（名前, 职业类型）** —— 同名同类型 = 同一个人（他不同年代换组织）。实测同一年代内 **0 冲突** |
| 规模 | 903 个 (名,组织,类型) 组合 → **329 人**（商家 120 / 海贼 110 / 忍者 99），其中 **16 个是当主** |
| ID 规则 | `lord_tk5_<职业>_<名字罗马音>`（用户裁定：职业+名字） |
| 罗马音 | **人工建表**（词干 240 + 后缀 40）。pykakasi **不可用**：327 个里 45 个输出空/截断，抽样 25 个里 8 个读错（`与平次→yoheitsugi`、`三郎左→saburouhidari`） |
| 统一写法 | macron 铁律：郎=roo / 次郎=jiroo / 兵衛=bee / 右衛門=uemon（既有 1037 条的写法有三套漂移，**未动**） |
| 立绘 | **不用新做**——`ProfileImage.csv` 里泛用槽的通用立绘早挂好了（`template_ninja_male_01` 55 槽 / `template_kaizoku_01` 55 槽 / `template_merchant_01` 63 槽；另有女忍者 2 / 女海贼 2——见 0.13），按类型沿用 |

**② 🔴 合成号机制（关键设计，改生成器的办法）**

全部生成器都假设「人物键 = 数字」（`DXRE = r"lord_tk5_(\d+)$"`、`sorted(key=int)`、`"unknown%d" % int(head)`），
而**泛用 NPC 每年占的槽号还不同**（仙左卫门 = 1108 / 1560 是 1120），ID 又是名字式的 → 靠 ID 认不出人。

解法：给每个泛用 hero 分一个**合成号**（9000 起，与真实号段不撞）当内部键——
① 日志按年重键：`(年, 槽号) → 合成号` ② **输出边界再换回真 ID**（`OUT_ID[键]`，**禁止**写 `"lord_tk5_" + 键`，那条只对真 DX 号成立）。
**改动面最小**：下游（`resolve` / `keep` / `clan`）一行不用改。

**③ 按上级日志逐列纠正（全表比对，日志为唯一信源）**

| 表 | 列 | 结果 |
|---|---|---|
| TaikouHero | `City_<年>` | 4615 格里 **4611 一致**；4 格冲突（须田满亲 1554/1560、十河存保 1575、武田信丰 1554）→ **待修** |
| TaikouHero | `Kingdom_<年>` | 6222 格里 6198 一致；**24 格冲突**（1575「三好家 vs 十河家」5 人 + 三好长治 + 天野隆重 1554 + 有马晴信 1560 + 太田氏资/三乐斋 1554 + 须田满亲 ×2 + 追加段 2 人空列）→ **待修** |
| TaikouHero | `Appear_<年>`=已死亡 | **1145 格 100% 有依据**——日志里这些人的据点在**收容槽**（`胜山馆`，1598 年叫 `松前城`）→ 无冲突 |
| TaikouHero | `ClanID_<年>` | 5598 一致；已死亡者仍挂家族 460 + 亲属并入 92 + **66 格待确认**（疑 R5 亲属经**名字**解析） |
| TaikouForce | `Owner_<年>` | 实名替补 **15 → 0**、「该年不出势力」**28 → 0**、存在格 525 → **553** |
| Clan | `Owner_<年>` | R6 假家族 **5 → 0**；「日志=其他(无所属)」= **0** |

##### 三、数字（做完之后）

| | 之前 | 之后 |
|---|---|---|
| `TaikouHero.csv` | 1111 行 | **1440 行** |
| `Clan.csv` | 283 家 | **296 家** |
| `TaikouForce` 该年无当主的格 | 28 | **0** |
| 实名替补 | 15 | **0** |
| 无王国的独立家族 | 5 | **0** |
| 一键体检 | 21 绿 / 1 红 | **23 绿 / 0 红** |

##### 四、本轮产出物（新增脚本与数据）

| 文件 | 说明 |
|---|---|
| `Scripts/gen_generic_hero_ids.py` | 泛用 NPC → ID 表 + 槽位对齐表（词干/后缀表可复现、可 `--check`） |
| `泛用heroID表_20260912.csv` | 329 人 ×（合成号 / ID / 名前 / 职业 / 罗马音 / **置信度** / 各年代槽号·组织 / 是否当主） |
| `泛用hero槽位对齐_20260912.csv` | **一人一行 × 六年代列**（`槽号_<年>` / `组织_<年>`）—— 生成器读它 |
| `Scripts/promote_generic_npcs.py` | 写 329 行英雄（`Appear/Identity/CareerStance/Kingdom/City` 全从日志推） |
| `Scripts/check_force_owner_crossval.py` | 势力当主**双口径交叉验证**（常驻体检；桥 188/188，四类计数对基线） |

##### 五、⏭ TODO

**A. 待用户裁定 —— ✅ 三条全部落地（2026-09-12，见「0.9」）**

1. ✅ **9 条读音** —— 7 条按同表内部一致性定案 · 1 条改正（泉识坊 → senshikiboo）· 2 条私用区改纯 ASCII id（`xe413bee` / `xe413uemon`）
2. ✅ **16 个当主的家族名** —— 改成**组织名**（透波 / 安东 / 坊津 / 钵屋 / 羽黑 / 轩猿 / 山潜 / 甲贺 / 丹后 / 土佐），家 id 同步（`clan_suppa_1` ↔ 透波家）
3. ✅ **`ForceTaikou.csv`** —— 已于 2026-09-12 退役删除（六列快照判读 0 异常）

**B. 待修的数据冲突**

4. `TaikouHero.City_<年>` **4 格**、`Kingdom_<年>` **24 格** —— ⏭ **仍挂**（按日志为准该改；**不需要裁定**，属未做完的活）
5. `TaikouHero.ClanID_<年>` **66 格** —— ⏭ **仍挂**（疑 R5 亲属经名字解析；要先查清再改）
6. ✅ `Settlements.Owner_<年>` —— **已接回据点日志**（2026-09-12，改 90 格、1644 格全覆盖零兜底；见「0.9」③）

**C. 那 329 人还没有 XML（属 T4-c）**

7. 加 **Hero 段 + CharacterObject 模板** —— ⚠️ `check_hero_templates.py` **只查 XML 里已有的 Hero**
   （现 8 + 2 个），**查不出 CSV 里的新人**；模板缺了会「静默被吞 → 新战役在 `OnNewGameCreated` 崩」
8. 接 `check_hero_profile_keys`（英雄 id 三处同键：模板 ↔ 画像表 ↔ 立绘表）

**D. 项目主线**

9. ⏭ **下一步仍是 T4-c 全量铺数据**（据点批量进场景 + 兵种树/日式物品自建 +
   按 `ClanID_<年>` / `Owner_<年>` 生成各年代的 `spclans` / `spnpccharacters` 段）。
   **本轮的 329 人 + 296 家 + 553 格当主就是 T4-c 的输入——数据层已齐。**

#### 0.9 ✅ T1 清尾巴（2026-09-12 下半场）—— 读音 / 家族名 / 据点归属

**一句话**：T1 的三件「动手前必须先定」的事全部落地；体检 **22 绿 / 1 红**（红 = 雷 53，等 T5）。

**① 9 条泛用 hero 读音**（`gen_generic_hero_ids.py`）

| 项 | 结论 |
|---|---|
| 依据 | **同表内部一致性**，不引外部词典：`〜三`=ぞう（同表 `祯三 teizoo`）· `〜太`=た（`平太 heita`）· `〜七`=しち（`佐七 sashichi`）· `〜坊`=ぼう |
| 7 条低置信 | 专次 senji · 闻太 kikuta · 传林坊 denrinboo · 岩蓦 ganbaku · 鸢七 tobishichi · 实三 sanezoo —— **全部维持原读**，置信度转「高」 |
| 1 条改正 | **泉识坊 senzuiboo → senshikiboo**（音读名：泉=せん、識=しき；旧读的「ずい」在「識」上无依据） |
| 2 条私用区 | `兵卫`/`右卫门` 的前缀是 **U+E413**，**读不出**（词库无同长度候选）→ id 改**纯 ASCII**：`lord_tk5_trader_xe413bee` / `..._xe413uemon`。旧 id 里带中文（违反铁律 20），已迁移 |

> 🔴 **为什么解不出**（试过、别再试）：拿**原版日志**（正常字、0 私用区）反查是死路——DX 人物池 1304 条 vs 原版 1100 条，**顺序不同**且 DX 有增补，同槽不同人（实测 0 命中）；唯一出路是**字体码表**（F_FONTT.tr5 的码→字面表，见 `太阁5_数据文件破解笔记_tr5msg与字体`）。

**② 16 个泛用当主的家族名**（`promote_generic_npcs.py`）

- 改前：`FirstName` 留空 → 家族生成器退回用**人名**当家族名（「六郎次」「仙左卫门」）。
- 改后：**苗字 = 组织名去掉类型尾缀**（`透波众`→透波、`安东水军`→安东），`EnglishName` 写成 `<苗字罗马音> <名罗马音>` ——
  罗马音**直接取 `TaikouForce` 的组织 id**（`org_ninja_suppa`→suppa、`org_pirate_antousuigun`→antou），**不另造读音**。
- 结果：**家 id ↔ 家名一致**（`clan_suppa_1` ↔ 透波家 / `clan_antou_1` ↔ 安东家 …），16 家全部改名，`check_englishname_clan_prefix` 保持绿。
- 脚本改为**幂等更新**（原先只新增不更新，已存在的行只改 `FirstName`/`EnglishName` 两列，其余列归别的生成器）。

**③ 据点归属 `Settlements.Owner_<年>`**（新脚本 `Scripts/gen_taikou_settlement_owner.py`）

> 🔴 **走了一趟弯路，结论记这里**：先按「上级日志（按人记）」推——那份日志每人一个 `据点` 字段，
> **没人驻守的城就没有行**（1644 格里 **436 格**当年无人驻守，八户城全六代没人），
> 只能沿用退役快照兜底，还得养一张例外表。**用户点破：直接用「据点日志」**——
> 它是**按据点记**的，每个据点每年一行、直接给 `当主`，**1644 格全覆盖、零兜底**。

| 项 | 结论 |
|---|---|
| **唯一信源（用户裁定）** | `Knowledge/太阁5/太阁日志/据点日志.md` 的 `Log: SET\|<年>\|<序号>\|<类型>\|<名>\|当主:<人>\|势力:<家>\|…` |
| **口径** | `Owner_<年>` = 该城当年 **`当主`**；当主 = `-` → 空（实测 398 格，与表里空值**完全吻合**） |
| **对位** | 日志 `序号` − 1 = 我们的 `tk` 编号（**274/274 名字前缀 0 处不符**，脚本内断言） |
| **改了多少** | **90 格**：其中 **56 格**是「大名 vs 实际城主」的层级差（北之庄=柴田胜家 / 佐伯=佐伯惟教 / 日野江=有马 / 久留米=少贰 / 四城=十河存保…），**34 格**是忍者里·海贼砦原先**空着**的城主（透波=六郎次 / 猿飞=… 直接填上）；连带 **`Clan_<年>` 改 79 格** |
| 🔴 **袭名歧义** | `河野通直` 父子同名（父 906 卒 1572 / 子 300 生 1564）——子是**袭父名**，1582 日志显示的都是「河野通直」，但当年在汤筑城当主的是**子**。名字解析**必须带年代过滤**（`Appear_<年>` = 已登场），并在 `NAME_AMBIGUOUS` 里**钉死并附证据**（不能用 Alias 解决：别名闸门明令「一个别名键只能映射一个 id」）。⚠️ 解析不出时**报硬错误**，不再静默退回（静默退回 = 让死人当城主） |
| 连带 | `Clan_<年>` 由 Owner 派生 → 改完**必须重跑** `gen_taikou_clan_csv.py --apply`（已跑） |
| 守卫 | `--check` 进 `run_all_checks`（**22 绿 / 1 红**，红 = 雷 53）· 幂等复跑必须归零 |

**④ 本轮踩的坑（已登记）**

1. 🔴 **`run_all_checks` 会给每个脚本追加 `--module <Taikou路径>`** —— 新脚本的 argparse **必须接这个参数**（哪怕忽略），否则当场 `exit 2` 报红。
2. 🔴 **生成器"定义了 apply 函数却没调用"**：写盘时直接写了没改过的 body，**幂等复跑才抓出来**（15 格原地不动）——「写入函数存在」≠「被调用」，幂等复跑是唯一防线。
3. 🔴 **名字式 id 的下游缺口**：0.8 提升的 329 个泛用 hero，id 是**名字式**（`lord_tk5_ninja_*`），而下游多处按 `lord_tk5_(\d+)$` 取号 → **平时不报错，直到泛用 hero 第一次当上城主**（忍者里/海贼砦填了 Owner）才炸；且生成器**报错即中止写入**，表面看像「数据没改」。改法 = 用 `OUT_ID` 逆表先换成内部键。
4. 🔴 **两个"唯一信源"要分清谁记什么**：上级日志**按人记**（谁在哪）· 据点日志**按据点记**（哪归谁）。
   要「某格归谁」就找**按格记**的那份——拿按人记的那份去凑，注定有覆盖缺口。



#### 0.10 ✅ 文化体系定案（2026-09-12，用户裁定）

**一句话**：文化表从「21 条、11 条没人用」收成 **15 条**；补上 4 个身份文化并**在 Taikou 自己包里定义**；329 个泛用 hero 补文化 → **69 个空文化家族归零**。

| 项 | 结论 |
|---|---|
| **定案（用户裁定）** | 身份文化 5 个：**商人 `trader`（新）· 忍者 `ninja` · 海贼 `pirate` · 强盗 `looters`（吸收 8 个）· 浪人 `ronin`（新，替 `ashigaru`）**；另有 9 个地域 + `neutral_culture`。`Culture.csv` **21 → 15 行** |
| **合并明细** | 并入 `looters` 并记进它的 `OtherName`（可追溯）：贼寇/野盗/倭寇/山贼/落武者狩（真同义）+ 决斗者/僧侣（用户裁定一并并入）+ 足轻（由浪人代替，名字一并记录） |
| 🔴 **关键发现 1 → 已按用户裁定修复** | `Culture.csv` 原来的 21 条里，**19 条的定义在织丰**（`Shokuho/.../shokuho_main_cultures.xml`）——Taikou **隐式依赖织丰**才拿得到文化；`ninja`/`pirate` 更极端：**谁都没定义**。**用户裁定「Taikou 与织丰零依赖，所有文化都自创」** → 生成器 `gen_taikou_culture_full.py` 扩成**产出本包全部 16 个文化**（ikoku + neutral_culture + 9 地域 + 5 身份），每个 76 字段全量块 + 名字池。**已写进 CLAUDE.md 三单元架构「铁则 6」** |
| 🔴 **关键发现 2** | 「自家文化」必须自备 **17 个文本变体族**（`check_culture_text_variants.py` 把守；缺 = 玩家可见 `ERROR: Text ... doesn't exist`）→ 16 文化 × 19 条（含 `_f`）变体**沿用 ikoku 已有键与措辞**（零新增本地化键，中文自动命中）+ 16 条文化名双语词条 |
| 🔴 **根因修复** | 69 个家族文化空的根因 = **329 个泛用 hero 全员没有 `CultureID`**（家族文化取成员多数 → 取到空）→ `promote_generic_npcs.py` 按职业补 `ninja`/`pirate`/`trader` → **空文化家族 69 → 0**（忍者 20 / 海贼 24 / 商家 25） |
| **`ronin` 给游荡者**（用户裁定） | `gen_taikou_wanderer_culture.py`：**六代全程无家**（`ClanID_1554…1598` 全空）= 游荡者 → `CultureID = ronin`，实测 **151 人**（原先散在 `neutral_culture`/地域文化/空值）。⚠️ 依赖 `ClanID_<年>` → 必须排在家族生成器之后（已进一键体检） |
| **大航海联动角色 → `pirate`**（用户裁定） | 同脚本规则二（6 人，名单带证据写在脚本里）：**海雷丁**（别名 `ハイレディン`；游戏里本就挂海贼众·安东水军）· **佐伯杏太郎** · **里璐**（项目史料提案记为联动）· **拉斐耶鲁**（`Raphael`）· **李华梅**（`マリア`）· **蒂雅**。⚠️ **不在此列**：`佛罗伊斯`（Frois 耶稣会士）/ `阿鲁梅达`（Almeida）= 史实南蛮人 |
| 踩的坑（已登记） | ①生成器模板比现网旧（`lord_oda_*` vs `lord_tk5_*`）→ 重跑会**回退**改名脚本的成果 ②生成器把 **LF 写成 CRLF** → 整份文件"逐行全变"（4510 行），diff 读不出来 → 两条都修进生成器（先备份 → 跑 → diff 只应出现预期行） |
| **南蛮 `namban`**（用户裁定） | 新增第 6 个身份文化「标记南蛮」→ **4 个外国人**：`佛罗伊斯`（Frois 耶稣会士）· `阿鲁梅达`（Almeida 葡萄牙人）· `弥助`（非洲出身，信长近臣）· `三浦按针`（William Adams 英国人）。⚠️ **日本人基督徒不在此列**（天正遣欧使節的 千千石米格尔/伊东满所/内田托马 = 日本人，文化不动） |
| **`Culture.csv` 结构（用户裁定）** | 列收敛为 **`ID / Name / Alias`**：三名（`ScriptName`/`ChineseName`/`OtherName`）合并后再拆——**主名进 `Name`，其余进 `Alias`**（`\|` 分隔，如 `西海` + `九州`、`强盗` + `追剥\|贼寇\|野盗\|倭寇\|山贼\|落武者狩\|决斗者\|僧侣\|足轻`）。**删四列**：`LocozationName`（键在 `spcultures.xml` 的 `name="{=…}"` 里 → **零信息损失**）· `IsShokuho`（所有文化已自建）· **`IsMainCulture`**（该信息在生成器 `CULTURES` 里）。**表尾 22 个无名空列已清**（用户裁量：三列有效列之外不留空列）→ 整表 **16 行 × 3 列**。消费方同步：`check_taikou_world_tables.py` 的必填校验改查 `Name` |
| ⏭ 遗留 | ①**`looters` 目前零引用**（体检 WARN）——它是**盗匪文化**，等 C2 生成 `spclans_<年>` 的盗匪家族时才会用上 ②**两个包同时启用时的重复定义**（kinai/kanto/… 两包都有）——Taikou 单开无碍，同开是否冲突待实机验证 |

**非人物行的文化口径（2026-09-12 用户裁定，已落地）**：`template_*`（68 个通用 NPC 模板）+ 变量行（6 个）= **74 行**。
规则：**身份明确的按身份给，其余一律 `neutral_culture`**（落在 `gen_taikou_wanderer_culture.py` 的 `TEMPLATE_IDENT` 表）——
`ninja` 6（忍者/上忍/中忍/下忍/女忍者/**头目**）· `pirate` 7（海贼/女海贼/头领/船大将/船头/水夫头/水夫）·
`trader` 14（商人/行商人/明朝·朝鲜·琉球商人/座掌柜/伙计/掌柜/马屋·米屋·酒场·宿屋·旅店·**当家**）· `ronin` 1（浪人模板）·
**`neutral_culture` 46**（足轻系/家臣系/剑术师范/农民·町民/天皇/僧侣/公家/公主… + 全部变量行）。
⚠️ 两处判断留痕：**头目**按本表 IDENT 口径（忍者众当主）给 `ninja`（它在模板清单里挨着海贼块，位置有歧义）；
**「贼」模板**按用户「其余都 neutral」的字面给了 `neutral_culture`（若要算盗匪文化，加 `template_yotto` 进表即可）。

### 0.18 📋 本轮复盘 + 下一回合任务书（2026-09-12）

**一句话**：本轮把**数据层最后一批脏活**清完（读音 / 家族名 / 据点归属 / 文化体系 / CSV 规范 / 列序），一键体检 **23 绿 / 1 红**（红 = 雷 53，等 T5）。
下一回合两件事：**① CSV 内部引用全量体检 → ② 照「最小太阁单元」铺六年代 XML**。

#### 本轮产出（0.9~0.17，每条都有小节）

| # | 做了什么 | 关键数字 |
|---|---|---|
| 0.9 | **据点归属整列接回「据点日志」**（按据点记的那份，不是按人记的上级日志） | 改 90 格 · 1644 格全覆盖零兜底 |
| 0.10 | **文化体系定案**：17 个文化**全部自建**（与织丰零依赖）、身份文化 5+1 个、`Culture.csv` 收敛 `ID/Name/Alias` | 69 个空文化家族 → 0 |
| 0.11 | **河野父子（袭名）收口** | 外观槽互换 + 袭名钉死表 |
| 0.12 | **畠山家别名混装人名**修正 + 新闸门 | 全表扫描只此 1 例 |
| 0.13 | **329 个泛用 hero 的立绘槽分配** | 177 槽 / 329 人，最大复用 2 人 |
| 0.14 | `TaikouForce.短名` 并入 `别名` | 别名不重复主名（过滤规则） |
| 0.15 | **CSV 表头规范**（两行表头：中文行 + 英文行，机器读第 2 行） | 5 张管线表全部合规 |
| 0.16 | **TaikouHero 年代列归位**（ClanID 并进各年代块） | 135 列只换序 |
| 0.17 | 前八个地区**名称/别名互换**（主名取真实地区名） | 换 5 行 |

#### 沉淀的纪律（写进了 CLAUDE.md / 必备清单）

- **CLAUDE.md 新增**：① 三单元架构**铁则 6**（内容包与织丰**零依赖**、所有内容自创、判据「引用闭合 ≠ 定义在本包」）② **CSV 表头规范**（两行表头，机器读第 2 行，读写走 `Scripts/csv_dual.py`）
- **必备清单新增雷 91~98**：按人记 vs 按格记的日志要分清 · `run_all_checks` 传 `--module` · 幂等复跑是唯一防线 · 名字式 id 的下游缺口 · 生成器模板与现网分叉（改前先备份跑 diff）· 引用闭合 ≠ 定义在本包 · 按位置取模板会把别人的条目一起复制 · 源表一格混装两类值

#### ⏭ 下一回合任务书

**① CSV 内部互相引用全量体检**（要求：每条检查都有脚本，负面测试过）
- 已有的 10 条边（2026-09-12 实测**悬空 0**）：英雄 `ClanID_<年>`→Clan · 英雄 `Kingdom_<年>`→势力名 · 英雄 `City_<年>`→据点名 · 英雄 `CultureID`→Culture · 家族 `Owner_<年>`→Hero · 家族 `Kingdom_<年>`→Force · 家族 `Culture`→Culture · 势力 `Owner_<年>`→Hero · 势力 `Culture`→Culture · 据点 `Owner_<年>`→Hero · 据点 `Clan_<年>`→Clan
- **要补的**：**孤儿检查扩到全表**（现在只有 `Culture.csv` 那一条 WARN；势力/家族/据点也要查「定义了但无人引用」）+ **非人物行不参与**的口径固定下来（`模板NPC` 非空 = 非人物行）+ XML 侧的引用也要纳入
- 口径提醒：**空值 ≠ 悬空**——语义空（浪人无家 / 已死亡 / 势力某年不存在 / 町无城主）是合法的，检查要能区分

**② 照「最小太阁单元」铺六年代 XML**（= 原 T4/C2）
- 模板 = **现有 Taikou 的最小单元**（`spcultures` / `spkingdoms` / `spclans` / `spnpccharacters` / `settlements` 现成样张）
- 产出：`spclans_<年>`（296 家的 `Owner_<年>`/`Kingdom_<年>`）· `spkingdoms_<年>`（势力 `Owner_<年>` + Culture + banner）· `taikou_heroes_<年>`（`<Hero faction="Faction.<ClanID_<年>>">`）· `settlements_<年>`（`Owner_<年>`/`Clan_<年>`）
- **前置**：329 个泛用 hero 的 Hero 段 + CharacterObject 模板（T3；模板缺了会「静默被吞 → 新战役崩」）
- **校验**：每个王国必须有 clan（RulingClan 链）/ 每个 clan 有 owner / 每个据点有城主

**③ 挂起项（用户已裁定「一起改」，逻辑下回合做）**：文化**显示名**——`Culture.csv` 前八个地区主名已换成真实地区名（九州/四国/中国/近畿/甲信），
但游戏内显示名走 `spcultures.xml` 的 `{=TAIKOU_culture_<id>}` + 语言文件 → 要改生成器 `CULTURES` 的名字 + 中英语言条目（另加 `ou` 奥羽→东北，用户已确认一起改）。

#### 0.17 ✅ 前八个地区的名称/别名互换（2026-09-12，用户裁定）

`Culture.csv` 前八个地区：**主名取真实地区名，TK5 的简称降为别名**（原为反着）。实测换 5 行（另 3 行本来就没别名）：

| ID | 改后 Name | 改后 Alias |
|---|---|---|
| `saikai` | **九州** | 西海 |
| `nankai` | **四国** | 南海 |
| `sanyo` | **中国** | 山阳 |
| `kinai` | **近畿** | 畿内 |
| `tosan` | **甲信** | 东山 |
| `hokuriku` / `tokai` / `kanto` | 北陆 / 东海 / 关东 | （本来就无别名，即真实地区名） |

⚠️ **这只是登记表的名称**——**游戏内显示名不从这里取**，走 `spcultures.xml` 的 `name="{=TAIKOU_culture_<id>}Xxx"` + 语言文件。
要让游戏里也显示「九州/四国/…」，得改生成器 `CULTURES` 的名字与语言条目（⏭ 待用户确认）。
⚠️ `ou`（第 9 行，奥羽/东北）不在「前八个」范围内，未动。

#### 0.16 ✅ TaikouHero 年代列归位（2026-09-12，用户抓出）

**问题**：`ClanID_<年>` ×6 孤零零待在 `EnglishName` 后面，而其他年代列（`Appear_<年>/Name_<年>/Identity_<年>/…`）
是按年代成块排的——查一个年代得横跨整张表。
**改法**：把 `ClanID_<年>` 并进各自的年代块，块内顺序定为
**`Appear_<年> → Name_<年> → Identity_<年> → CareerStance_<年> → Kingdom_<年> → ClanID_<年> → City_<年> → School_<年>`**（家族紧跟所属势力）。
年代块顺序 = 文件里的出现顺序（1554 1560 1568 1575 1582 1598 1549 1584 dream1560），插在属性区（`…EventGet`）之后 ✓ 135 列集合不变、只换序。
**连带**：`gen_taikou_clan_csv.py` 的 hero 写入原假设「6 个 ClanID 列连续」→ 改成**按列名定位**（`hdr.index` 逐个改值），列序怎么排都不受影响 ✓

#### 0.15 ✅ CSV 表头规范落地（2026-09-12 用户裁定）—— **两阶段都完成**

**规范**（已写进 CLAUDE.md「CSV 表头规范」）：数据表一律**两行表头**——第 1 行中文标签（人读）· 第 2 行英文键（**机器按第 2 行取键**）· 第 3 行起数据。
理由：中文标签会随内容改（`追剥`→`强盗`、`短名`→并入`别名`），**代码的键不能跟着动**。

**阶段一（已完成，23 绿 / 1 红）**：
- 新增共享读写器 `Scripts/csv_dual.py`（`read_table/dict_rows/write_table`，`head=2` 英文键（默认）/`head=1` 中文键/`head=0` 单行表头）
- **`Culture.csv` / `Clan.csv` / `Settlements.csv` 已转双行** ✓（数据行数不变：16 / 296 / 274）
- 读取方同步：`check_englishname_clan_prefix` · `check_taikou_world_tables`（`load()` 加 head 参数）· `gen_taikou_clan_csv`（`load_dict` 加 head；Clan.csv 写双行）· `gen_taikou_settlement_owner`（键取第 2 行 + 写回两行表头）
- 已合规（无需改）：`TaikouForce` · `Appearance` · `Facility` · `School`

**🔴 阶段一踩的坑（都已修，记下来）**：
1. `csv_dual` 原本想**自动识别**双行表头（判据：第 2 行全非空 + 两行无相同格子）→ 实测在「首行数据恰好全非空」的表上**误判**
   （`Culture.csv` 首行 `saikai,西海,九州`、`Settlements.csv` 首行 `town_tk000,城,…`）→ 改成**显式 `head` 参数**，禁止猜。
2. **单行表头的文件被默认 `head=2` 读** → 键变成首行数据 → `r.get(...)` 全 None。实测两次：`Clan_织丰口径` 存档（苗字罗马音兜底表）读空 →
   **`clan_okabe_1` 退化成 `clan_unknown176_1`**（真数据损坏，已还原）；往返校验的 TaikouHero 读取读空 → 误报「有家族的 0 行」。
   **教训：改读取器的默认值 = 全仓风险**，每个调用点都要显式声明 head。

**阶段二（已完成）**：`TaikouHero.csv`（135 列）已转双行 ✓
- 7 个中文列名改成英文键：`原版编号→OriginalID` · `外观ID→AppearanceID` · `模板NPC→TemplateNPC` ·
  `外观描述_光荣→KoeiAppearanceDesc` · `立绘阶段→ProfileStages` · `列传→Biography` · `列传原文→BiographyRaw`；
  代码 **36 处引用（15 个文件）同步改名** ✓
- 中文标签**抄上游 `BaseInfo.csv` 表头**（权威来源，不自编）：`生年/登场/终年/死因/父亲/祖父/亲族/仕官倾向/仕官相性/亲密相性/性情/精神/主义/行动/情义/野心/喜好/物欲/饮酒/出身/统率/武力/政务/智谋/魅力/足轻/骑马/火枪/水军/弓术/武艺/军学/忍术/建筑/开垦/矿山/算术/礼法/辩才/茶道/医术/称号卡/合战卡/秘技卡/亲密获取/组合获取/事件获取` + 年代列（`登场_<年>/身份_<年>/立场_<年>/所属势力_<年>/所属据点_<年>/所属流派_<年>/家族_<年>/姓名_<年>`）
- 读取方同步：`check_taikou_world_tables` · `gen_taikou_clan_csv`（hero 读写 + 往返）· `gen_taikou_hero_profiles` · `gen_taikou_hero_catalog` · `gen_taikou_wanderer_culture` · `gen_taikou_settlement_owner` · `promote_generic_npcs` ✓

⚠️ **遗留（下轮清）**：**一次性迁移脚本**（`fix_*` / `merge_*` / `renumber_*` / `reorder_*` / `restructure_*` / `import_taikou_hero_bios` / `sync_taikou_bio_cns` / `finalize_alias_and_ids` / `convert_taikou_macrons`）仍在用 `DictReader` 读 TaikouHero →
**重跑会读错**（键变成中文行）。它们的设计是一次性（多数带幂等守卫），但**要重跑就得先改成 `head=2`**。

#### 0.14 ✅ `TaikouForce.短名` 并入 `别名`（2026-09-12，用户裁定）

**结论**：`短名` 列去掉，值**并入 `别名`**。理由两条——①**机械可推**（= 势力名去掉尾部「家」，0/185 例外）；
②**但它是查找键**（名字→势力 id 的组织名解析会用到，`gen_taikou_clan_csv.py` / checker 都读）→ 不能丢，放别名里正合适。
列序变为 `势力类型 | ID | 势力名 | 别名 | Culture | Owner_<年>×6 | 太阁编号`（只剩 6 列静态值）。
消费方同步：`gen_taikou_clan_csv.py` 与 `check_taikou_world_tables.py` 的映射查找改为「势力名 + 别名」。
⚠️ 另加一条**过滤规则**：别名里**不得出现与势力名相同的项**（纪律同 Clan.csv「别名不重复主名」）——做成规则而非一次性清理，
因为静态列的真源是 CSV 自己，光靠生成期"不加"清不掉历史写入（实测 `org_ninja_suppa` 的 `透波众` 就是上一轮写进去的）。

#### 0.13 ✅ 329 个泛用 hero 的立绘槽分配（2026-09-12，用户裁定）

**口径**：从 5 个模板的槽池里按 **(职业, 性别)** 分配——池子**从模板行现读**（单一来源，不写死）：
`template_ninja_male_01` 55 槽 · `template_kunoichi_01` 2 · `template_kaizoku_01` 55 · `template_kaizoku_female_01` 2 · `template_merchant_01` 63 = **共 177 槽**（池子互不重叠 ✓）。
分配法 = **按 id 排序轮转**（第 n 人拿 池[n % 池长]）→ 均匀，不堆在同一个槽上。落在 `Scripts/promote_generic_npcs.py`（它本来就管这 329 行），`外观ID` 已加进「已存在行只更新本脚本负责的列」清单。

**实测分布（0 个槽超过 2 人）**：忍者 95 人/55 槽（15 槽 1 人 + 40 槽 2 人）· 海贼 107/55（3 + 52）· 商人 120/63（6 + 57）· 女忍者 4/2 · 女海贼 3/2。

⚠️ **性别数据里没有**（`Gender` 列全空、日志也没该字段）→ 女性池只按**名字判据** `FEMALE_NAMES` 分配，判成女性的 7 人（**人工判断、逐条可改**）：
**吹雪 · 枫 · 红叶 · 枣**（→ 女忍者池）· **泷 · 多津 · 鹤**（→ 女海贼池）。未收「鹤千代/虎千代/百千代」= 〜千代在江户庶民名里男女都用。

#### 0.12 ✅ 畠山家别名修正 + 新闸门（2026-09-12，用户抓出）

`TaikouForce.csv` 的 `hatakeyama`（畠山家）别名原是 `田山家|田山高政|田山義續|田山義綱`——后三个是**人名**（该家一门众，与 `太阁编号` `698|699|700` 同一批人）。
修正为 `田山家`（门户异写；日志里「组织」一律写 `畠山家`，故别名仅作异写留档）。
新增常驻闸门：**`TaikouForce.别名` 不得含英雄表人名**（硬错误，已做负面测试）→ 必备清单雷 98。

#### 0.11 ✅ 河野父子（袭名）收口（2026-09-12）

**父 `lord_tk5_906` 河野通直 / 子 `lord_tk5_300` 河野通直牛福丸**（同名袭名，两行独立不合并）。
- **外观槽互换**（用户裁定）：**子=300(通) / 父=1234(直)**（原 2026-09-11 裁定是父=300/子=1234，本次反转）。
  连带修 `AssetRegistry/ProfileStages.csv`：`通/300` 那行原先**错挂在父名下、子一行立绘都没有** → 已改挂 `lord_tk5_300`。
- **据点归属的袭名歧义**：据点日志 1582 汤筑城当主写「河野通直」（= 子袭父名），按名字精确匹配会落到**已死的父** →
  在 `gen_taikou_settlement_owner.py` 的 `NAME_AMBIGUOUS` 里**显式钉死** `("河野通直","1582") → lord_tk5_300`；
  同时把「名字匹配到的人当年已死/未登场」从**静默退回改成硬报错**（防同类袭名再静默出错）。
- ⏭ 留给 C2：子的 `CNName` 是「河野通直牛福丸」，但**游戏里 1582 显示为「河野通直」**（袭名）→ 铺 XML 时若要按年代显示名字，这行需按年代取显示名。

**「手工设定」是什么**（2026-09-12 查清）：上游 `BaseInfo.csv` 里数据准备者留下的**占位备注**（不是文化 id），字面意思「**这行的文化得人来定**」。
- **只在非人物行**：`template_*`（通用 NPC 模板 68 行：忍者/足轻/町人/保镖…）+ 变量行（主人公/主人公妻…）；**一个真实英雄都没有**。
- **看着像"文化"是因为这些行列用法不一致**：如 `template_ninja_male_01` 的 `EnglishName` 填的是 `clan_japanese_1`（**家 id**，不是英文名）——同一批行的 CultureID 格本就不表"文化"。
- **影响 = 零**：不是英雄、不进游戏；四表体检明确**跳过** `template_*`/`pronoun_*`。
- ⚠️ **本会话的手误（已修）**：游荡者规则按「六代无家」判定，**误把 40 格非人物行也判成了 `ronin`**（34 模板 + 6 变量）→ 已还原原值，并给规则加**人物行判据**（`模板NPC` 非空 = 非人物行）。现**人物行 1366 个的文化 100% 填满、零空值**；**非人物行 74 行也全部有了口径**（见上）。


> **设计全文 + 实施记录 + 实机排雷 + 验收表 → [plans/时代剧本切换-验证.md](时代剧本切换-验证.md)**

- **结论**：**一模块多 GameType + 每时代一套段**这条路**成立**——进 1582 归属换新家（`clan_g`）、氏族/王国界面对、存档不报错；**切回 1560 京仍是织田家 = 两时代互不污染（U1 过关）**。
- **机制**：每个时代一个 GameType（= 战役类名）；差异段（据点/家族/王国/英雄）两套**互斥注册**，共用段追加新 GameType 不复制文件；差异数据由 `gen_taikou_era_diff.py` 派生。
- **选剧本**：主菜单一个「剧本」入口 → **自建选剧本界面**（`ScenarioSelect` 族）。⚠️ 不是改主菜单列表——那条路被 MCM 的 Harmony 补丁打崩（详见验证文档）。
- **本次副产品**：新雷 **54**（英雄必须有同名 CharacterObject 模板，缺 = 静默被吞 → 新战役崩）+ 三个新 checker（`check_era_segments` / `check_hero_templates` / 生成器 `--check`）。
- ⏭ **读档链仍未打通**（「继续战役」按钮禁用）——U5（读档后归属保持）按设计随后单独补。

### 一·补、📋 **选人流程复刻太阁5（推荐人 + 角色详情页）——计划已写，待拍板**

> **设计全文 → [plans/选人流程复刻太阁5-设计.md](选人流程复刻太阁5-设计.md)**

- **要做什么**：选剧本页加「推荐」按钮（直达该时代 5 位推荐人物）；**点人名先看角色详情页**（立绘/基本情报/五维/16 技能/生卒年+列传），`[决定]` 才真正开局。
- **数据已核实**：五维 + **16 项技能** + 生卒 + 所属，全在 `Knowledge/太阁5/骑砍2织丰角色ID对应/csv/TaikouHero.csv`（**1112 行**：人物 1038 / 模板 68 / 变量 6；见「四、英雄集合已定」）；立绘 2720 张在归档包 `ShokuhoTaikouExpansionPack`。
- **三处待拍板**（见设计文档 §四）：①**推荐人名单来源**（现成数据里没有，需破解 TK5 或先自定占位）②详情页字段映射（官位/俸禄/功勋/流派我们无对应物）③立绘资产归属（是否现在接）。
- **建议先做**「详情页 + 流程改动」两步——纯展示、不依赖任何待定项。

### 二、其余待定（写清等什么，不占本步）

| 待定项 | 等什么 | 等到了怎么做 |
|---|---|---|
| 🔴 **雷 53 收尾**：距离缓存 + 家宅反射置位退役 | **T4 据点批量进场景**（需 ≥2 个「有场景实体」的据点**且距离算得出 > 0**） | ①补地图实体 ②重生成 `settlements_distance_cache.bin` ③复验日志不再出现置位行 → 删 `LivingWorldCampaign` 里的反射置位段。**离线防线已就位**（`check_settlement_distance_cache`，现正红 = 真问题） |
| `BattlePowerCalculationGuardPatch` | T4 自建兵种数据 | 复核兵源来源，定退役 |
| `CharacterCreationCultureVisualFallbackPatch` | 建号「仿太阁直接选人」改版 | 那时一起定（退役路线已取证备好：内容包加 2 个数据文件） |
| `AreaMarkerTagGuard` / `IssuesSettlementGuardPatch` | — | 🔴 **用户裁定不处理**（保留在磁盘、不编译；勘误：两者**从未编译过**） |
| 🔴 **剧本中文文件 `LivingWorldNpcs/ModuleData/Languages/CNs/std_scn_okehazama.xml` 处置** | **用户裁定**（未裁定前不动） | 三处不合规（未登记/结构错/键名不符）；建议先挪出 `Languages/`，等剧本工程接入时按规范放回 |
| 🔴 **两条「同一对象定义两遍」（2026-09-10 时代 spike 查出，既有问题非本次引入）** | **用户裁定"哪一份是对的"**（数据决策，不是机械去重） | ①`NPCCharacter.guard` 在 `spnpccharactertemplates.xml`（等级 11）与 `spnpccharacters.xml`（等级 4）各一份 → 后加载者静默覆盖（现为 4）；②`taikou_world_lore_strings.xml` 与 `world_lore_strings.xml` **78 个键完全重复**（2026-09-12 复核：两份各 78 键、交集 78）。现已由 `check_era_segments.py` 以 **WARN** 常驻可见（不阻塞），建议 T4 数据整理时一并清 |

### 三、主线

- [x] **T4-a：英雄集合已定（2026-09-11 完成）** —— 见下方「四、英雄集合已定」
- [x] **T4-a·补：英雄数据整备（亲属列 / 身份纠错 / 同人合并）** —— ✅ 完成（见「四·补」）。⚠️ 其中「柳生家三合一」**已随新家族规则作废**（家族 id 不再由 EnglishName 前缀生成）；剩余 6 项**待用户裁定**（见四·补表）。
      **2026-09-11 追加**：全表私用区字符就地还原 19 格（`fix_taikou_hero_pua.py`），列传里的 `U+F724`（=之）已不再漏进语言包
- [x] **T4-b：四表定义与交叉引用体检** —— ✅ 完成：据点表 ✅（见「五·补」）；**家族/势力表按侍奉关系 + 上级日志口径重建**（见「零、」，283 家 × 六年代 / 每家都有王国）；四表交叉闭合由 `check_taikou_world_tables.py` 常驻把守。
- [ ] **⏭ T4-c：全量铺数据（当前下一步）** —— 顺序见下方「T4-c 执行顺序」
  - 🔴 **人物数据按「生年 / 卒年 / 家族」存**（不按时代存人）——「哪个时代活着」由生卒年推导，连续时间线的出生/退场走**出生队列**机制，见 [plans/时代剧本切换-验证.md](时代剧本切换-验证.md)「🔴 出生队列」节（含 3 个未验风险）
- [ ] **T5：日式素材**（先 Native `mi_*`，织丰 `sho_*` 不可用——后续决断来源）
- [ ] **C 路线**：**自造名字池**替换织丰借用品（现 622 条逐字取自织丰、已标出处；发布前须取得授权）

### T4-c 执行顺序（2026-09-11 排定）

| # | 做什么 | 依据 / 产出 |
|---|---|---|
| **C1** | **先算一次世界规模账**（不写代码）：每年代「王国 × 据点」的分摊（96/95/90/82/72/90 国 vs 274 据点 ≈ 2.9 据点/国）——**看世界碎不碎得离谱**，碎就回来调「哪些家该并进大名」（数据侧：`TaikouForce.Owner_<年>` 的口径） | 一张「每代王国数 / 有据点的王国数 / 据点分布直方图」 |
| **C2** | **按年代生成世界段**（CSV → XML）：`spclans_<年>`（**296 家**的六代 `Owner_<年>`/`Kingdom_<年>`）· `spkingdoms_<年>`（势力表 `Owner_<年>` + Culture + banner）· `taikou_heroes_<年>`（`<Hero faction="Faction.<ClanID_<年>>">`）· `settlements_<年>`（`Owner_<年>`/`Clan_<年>`）。**扩展现有 `gen_taikou_era_diff.py`**（它现在只做 1560/1582 两代 → 做满六代） | 各年代互斥注册的差异段；🔴 校验：**每个王国必须有 clan（RulingClan 链）/ 每个 clan 有 owner / 每个据点有城主** |
| **C3** | **据点批量进场景**（TK5 布局表 → `place_settlements.py` + 地图实体）→ 顺带收尾**雷 53**（距离缓存 + 家宅反射置位退役） | `settlements.xml` + `SceneObj/Main_map` 实体；`check_scene_entities` / `check_settlement_distance_cache` 转绿 |
| **C4** | **英雄进世界**：**1440 人**（1037 有卡 + 329 泛用 + 70 模板/变量行）按「生年/卒年/家族」建号 + **出生队列**（连续时间线的出生/退场）。⚠️ **0.8 新加的 329 个泛用 hero 还没有 Hero 段与 CharacterObject 模板**，先补 XML | `taikou_heroes_<年>` 全量 + 出生队列机制 |
| **C5** | **兵种树 / 日式物品自建**（原料已备 112 SkillSet / 43 物品 / 25 BodyProperty；日式马/地痞外观留 T5） | 兵种/物品 XML |

---

## 四、🔴 英雄集合已定（2026-09-11）—— T4 的第一步

**结论：`TaikouHero.csv` 就是权威英雄清单。** ⚠️ **现行数值 = **1440 行 × 135 列**（2026-09-12 0.8 加了 329 个泛用 hero；此前 1111**（2026-09-11 亲属列改造 + 同人合并 + **家族列改年代制**：`ClanID` 单列 → `ClanID_<年>` ×6；改造前是 1112 × 128）。全量铺数据从这份清单出发，不再有「可能有谁」的悬念。

| 类别 | 行数 | 是什么 |
|---|---|---|
| `lord_tk5_<DX号>` | **1037** | 人物（DX 人物番号 0–1289，唯一、全表单调递增）——三法师并入织田秀信后由 1038 减 1 |
| `template_*` | 68 | 通用 NPC 模板（忍者/足轻/町人/保镖…）——**不是人物对象** |
| `pronoun_*` | 6 | 变量（主人公 / 主人公妻 / 發生人物 / 目標人物…） |

**前四列 = 三把钥匙 + 归类**：`ID`（DX 人物番号）｜`原版编号`｜`外观ID`｜`模板NPC`
- `ID`：主编号，**DX 人物番号**（用户 2026-09-11 裁定）
- `原版编号`：原版人物番号，849 行有值（其余是原版表里没有的追加段）
- `外观ID`：BUSTUP 立绘槽号 = 世界表 pid —— **join 六年代剧本数据用这把**
- `Alias`（第 6 列）：主名 + 繁体名 + 年代名（简繁两式）+ 别名，753 行非空

**已一并定稿的**：
- **列传 800 条**入库（= 1560 池全量），走 `TAIKOU_bio_<番号>` 本地化键，中英双层齐全
- **5 组「同一人两行」处理完**：4 组合并（重复行数据全空）+ 1 组确认为**父子**分号（河野通直/河野牛福丸）
- **归档**：`spnpccharacters.xml` / `taikou_heroes*.xml` / `ProfileStages.csv` 已同步到新 ID；`run_all_checks.py` 17 绿（唯一红 = 雷 53 距离缓存，与英雄线无关）

**纪律沉淀**（下次动英雄数据前必看）：编号基准、别名撞名闸门、私用区还原 —— 见
[plans/rules/wheels.d/data-registry.md](rules/wheels.d/data-registry.md) 与 memory `taikou5-three-id-systems`。

## 四·补、🔴 英雄数据整备：亲属列与身份（2026-09-11）

> **一句话**：英雄清单定了之后，把「亲属关系」和「身份元数据」两条线一起整干净——因为 T4-c 灌人要靠它们。

### 已做完（可交付）

| # | 事项 | 结果 |
|---|---|---|
| 1 | **列改造** | `FatherName/GrandFatherName/KinsName` → `FatherId/GrandFatherId/KinsId`（叫 Name 装的却是 ID）；新增 `MotherId`、`SpouseId`。列序：**父·母·配偶·祖父·亲戚**。128 → **130 列**（2026-09-11 家族列再改：`ClanID` → `ClanID_<年>`×6，**→ 135 列**） |
| 2 | **身份纠错 12 行** | 1180 段女性行的 `EnglishName/ClanID/CultureID` 当年是**按名字硬匹配织丰**填的（重名即错）。已按铁证纠正：阿松→前田家、阿梅→北条家、阿玉→细川家、相模→武田家、小松→真田家、千代→山内家、归蝶→织田家、濑名/德公主→德川家、菊公主→上杉家、南姫/早川夫人→今川家。**家族/文化跟丈夫走**（⚠️ 新规则下这件事**由 R4 妻子并入丈夫家自动完成**，那 12 格是旧单列的补丁）|
| 3 | **亲属补入** | TK5 脚本铁证：配偶 13 对（双向）+ 父 8 + 母 3 + 祖 1；织丰只补太阁空缺（父 6 / 母 2 / 配偶 2 对）。与太阁冲突的一律**太阁优先只报告**（9 条父冲突 = 生父 vs 养父） |
| 4 | **性别错位修正** | 丰臣秀吉/秀长的「父」位原填着他们的**母亲阿中** → 挪进「母」列 |
| 5 | **史实组 8 行** | 爱姬/义姬/豪姬/初姬/小督/三条/山手/彦鹤（用户批准的 A/B 组）。用独立的 `HIST_*` 表登记，报告里标「**史实来源，非游戏数据**」——与 TK5 铁证分开，不污染证据链 |
| 6 | **同人合并** | 三法师（`lord_tk5_1177`）并入 织田秀信（`lord_tk5_196`）——TK5 列传原文「信忠之嫡子。信長之孫。**幼名三法師**。」立绘两张都保留。**1112 → 1111 行** |
| 7 | **新校验器** | `check_englishname_clan_prefix.py`（⚠️ 2026-09-11 **已重写**：旧规则「每个英雄的 EnglishName 前缀 = 他的 ClanID」随新家族定义作废 → 现规则「**家族 id 的罗马音块 = 家头 EnglishName 首块**」）。已进 `run_all_checks` |

**产物**：生成器 `Scripts/fix_taikou_kinship.py`、`Scripts/merge_taikou_duplicate_person_rows.py`、校验器 `Scripts/check_englishname_clan_prefix.py`；
报告 `Knowledge/太阁5/骑砍2织丰角色ID对应/亲属补全报告_20260911.md`、提案 `…/未识别女性_史实提案_20260911.md`

### ⏭ 柳生家三合一 —— **已被新规则取代（2026-09-11）**

> 🔴 下面这条是**旧「同姓即同族」口径**下的待办：家族 id 由 EnglishName 前缀生成，前缀多打 u 就造出假家族。
> **新规则下不再需要**：家族 id / 归属全部由生成器 `gen_taikou_clan_csv.py` 从**上级日志的家头**派生
> （柳生石舟斋那一支是当主 → 自然成一家，742/741 按各自的上司链归属），**手工改 ClanID 的动作作废**。
> 保留本节仅供考古（说明「前缀错 → 假家族」的旧风险长什么样）。

#### 旧记录

**问题**：柳生石舟斋 / 柳生兵库助 / 柳生宗矩 被拆成**三个家族**。
**根因**：**家族 id 是从 EnglishName 前缀生成的**（`uu → ū`）——后两人前缀多写了 u，翻出来就成了假家族。

| 行 | EnglishName 前缀 | 现有家族 id | 应该是 |
|---|---|---|---|
| `lord_tk5_740` 柳生石舟斋 | `Yagyuu` ✓ | `clan_yagyū_1` ✓ | 不变 |
| `lord_tk5_741` 柳生兵库助 | `Yagyuuuu` ✗ | `clan_yagyūū_1` ✗ | `Yagyuu` / `clan_yagyū_1` |
| `lord_tk5_742` 柳生宗矩 | `Yagyuuuuuu` ✗ | `clan_yagyūūū_1` ✗ | `Yagyuu` / `clan_yagyū_1` |

**要改三处**（缺一不可）：① `TaikouHero.csv` 741/742 的 `ClanID` → `clan_yagyū_1` ② 同两行 `EnglishName` **前缀**去掉多余的 u（只动前缀、不动名字）③ `Clan.csv` 删 `clan_yagyūū_1` / `clan_yagyūūū_1` 两行。
**为什么必须连 EnglishName 一起改**：家族 id 由它生成，只改 ClanID 的话下次重跑生成器会再造出假家族。
**动前必做**：查这两个 id 还被谁引用（spclans / 生成器 / 剧本数据），确认爆炸半径。

### 待用户裁定（不动）

| # | 发现 | 要定什么 |
|---|---|---|
| 1 | `clan_didoji_1`（罗马音 `Daidoji`）、`clan_tatsuhara_1`（`Tachihara`）、`clan_kashuji_1`（`Kajuji`） | id 拼错还是罗马音写错 |
| 2 | `clan_misawa_1`（罗马音却写 `Mizawa`） | 同上（这条反过来，像罗马音错） |
| 3 | `lord_tk5_579` 畠山义续 的 `EnglishName` 缺空格（`HatakeyamaYoshitsugu`） | 补空格？ |
| 4 | `lord_tk5_740/741` 的 `FirstName` 切分错（`柳生石`/`柳生兵`，应是 `柳生`） | 一并修？ |
| 5 | C 组 17 位女性身份未定 + D/E 组未处理 | 见提案表 |
| 6 | `lord_tk5_958` 弥助（本无姓氏，挂 `clan_oda_1`） | 已列为校验器豁免，确认即可 |

### 判据备忘（下次别再踩）

- **检索一律用 `CNName` + `Alias` 全段**（铁律 25）——太阁脚本用日文/繁体写法（`歸蝶`/`瀬名`），只搜简体主名 = 0 命中，会把**有铁证的人误判成「无证据」**（本次实际漏掉归蝶·濑名·白枧·宁宁四人）
- **长音符写作双写（ō→oo）是设计预期** —— ASCII 名表示不了长音符，双写是为了**避免同音姓氏/名字相撞**。**禁止**把连续相同字母归一化成一个
- **同一人只能一行** —— 检测信号：同名多行 + 列传写死的幼名。袭名父子（生年差数十年，如河野通直 1500/1564）**不是**同一人
- **家族 id 由 EnglishName 前缀生成** —— 前缀写错 → 造出假家族
- **改数据前的安全动作**：①查引用（爆炸半径）②dry-run ③往返校验（写入 == 读回，本次因缺此校验写坏过 960 格）④幂等两跑（第二次必须空操作）⑤跑 `run_all_checks`

## 五、T4-b：四表定义与交叉引用体检（**四表已清完，2026-09-11**）

**要做什么**：`clan` / `kingdom` / `culture` / `settlement` 四张表，逐表查两件事——
① **自身定义是否自洽**（必填字段、枚举取值、id 命名规范）
② **两两交叉引用是否闭合**（引用方指向的 id 在不在；被引用的有没有孤儿）

**为什么现在做**：英雄清单定了，四表是英雄落地时的**归属载体**（英雄挂 clan、clan 挂 kingdom、据点挂 clan、全挂 culture）——
先把这层骨架验干净，再往里灌人；否则灌完再查等于返工。

**待办（勾选式）**：
- [x] **`Settlements.csv` ✅ 已完成（2026-09-11）** —— 见下面「五·补：据点表这一波做了什么」
- [x] **`Clan.csv` / 势力表 ✅ 已完成**（2026-09-11 下半场合并 → 2026-09-11 家族按侍奉关系重建，见「零、」）：
      **①势力表合并** → `TaikouForce.csv`（`Kingdom.csv` × `ForceTaikou.csv`）；
      **②~~同名撞车拆家~~**（旧口径产物，随新规则作废）；
      **③~~英雄表年代格修正~~**（旧口径，已被 `ClanID_<年>` 取代）；
      **④家族重建 + 势力口径换日志**（最终形态：283 家 × 六年代 / 96-95-90-82-72-90 个势力 / 每家都有王国）
- [x] `Culture.csv` → `spcultures.xml`：定义自洽 ✅（21 条 id 唯一、必填齐）+ 被引用全部命中 ✅
- [x] 势力表口径 ✅ **已定**：合并而非二选一 —— 见「零、0.1」
- [x] **交叉闭合矩阵** —— 已由 `Scripts/check_taikou_world_tables.py` 固化成常驻检查
- [x] 结论回填 —— checker 已进 `run_all_checks.py`；再加 O 列闸门（`Owner_<年>` 三态）

### 五·补、据点表这一波做了什么（2026-09-11 沉淀）

> 一句话：**名字全部换成「游戏自己说的」，并把三条不变量焊成硬断言。**

**① 名字来源定死为两个（别再发明第三个）**

| 来源 | 用途 | 为什么是它 |
|---|---|---|
| **简体版六份剧本日志**（`Knowledge/太阁5/据点日志/<年>剧本据点.log`） | `Name_<年>` 六个年代名 | **只有它记改名**（户石城→上田城、黑川城→会津若松城）；era_v2 的 `name_official` 永远是开局名 |
| **繁体原版转储**（`太阁5_据点坐标_20260901.md`，同槽号） | 只把**真异字**并进 `Name_All` | 繁体版与简体版对同一据点会用**不同的字**，剧本（事件包）引用的是繁体名 |

- `Name_All` = 该据点**全部称呼**（六年代名 + 繁体真异字别名），`|` 分隔，**第 1 段 = 开局名**（本地化键用它）
- 繁体名进 `Name_All` 前过滤：**两边都过 opencc `t2s` 再比**，相等 = 纯繁简字形（134 条丢弃）、不等 = 真异字（**6 条**：墨股/墨俣、二股/二俣、界/堺、阪本/坂本、安芸/安艺、御著/御着）
  - ⚠️ 必须**双向**归一化：opencc 把 `篠` 转 `筿`，游戏简体写 `篠`，只转一边会把 `長篠城` 误报成真异字
- **`era_v2` 的 `name_history` 整体退役**（它的差异大半是繁体写法，且它自己另有解码错字）

**② 三条不变量（生成器硬断言 + 体检脚本双查，都做过负面测试）**

1. 同一个名字不得属于两个据点（名字 = 身份，重名即匹配歧义）
2. 同一个据点内不得重名
3. 每个 `Name_<年>` 都必须在 `Name_All` 里

**③ 名字必须写全（用户裁定）** —— 「既然是叫之町那就是之町，不然不就和冈崎城混在一起了」
- 城和町会**共用前缀**（冈崎城 / 冈崎之町 是两个据点）→ **禁止**剥「城/町/之町」后缀再比
- 曾有三处「剥后缀兜底」全部拆除：`gen_settlements_csv.norm_place` / `check_taikou_world_tables` / `gen_taikou_hero_catalog.lookup_seat`

**④ 身份 → 据点类型（用户裁定：「浪人在町，武将在城」）** —— 一把硬尺子
- 武将（大名/城主/侍大将/家老/部将/足轻大将/足轻组头/国主）→ **城**名；町方（浪人/师范/当家/掌柜/医师/锻冶匠/见习/僧侣/茶人/伙计）→ **町**名；忍者系 → **里**名；海贼水军系 → **砦**名
- 实测全库对角线 0 违反；首次跑出的 11 组例外**全是我挂错的别名**（姬路城挂町槽 49 人次等）→ 已修
- 已固化为 `check_taikou_world_tables.py` 的硬错误

**⑤ 私用区（PUA）字符** —— 太阁5 自绘字形槽的字 dump 出来是 `U+E000–U+F8FF`
- 新脚本 `Scripts/fix_taikou_hero_pua.py`：全列扫 TaikouHero.csv → 还原 → 往返校验 + 幂等两跑 + 备份
- 还原表 `Scripts/tk5_pua_names.PUA_TO_CHAR` 补了 6 个**地名/列传**码点（原表只从人名词库推定）：
  `E40A=堺` / `E415=驒` / `E416=橡` / `E426=槻` / `F724=之`（F724 靠语料统计定：同批列传「之嫡子」26 次、「の」0 次）
- **已漏进语言包**：`TAIKOU_bio_704` 的列传带着 U+F724 出到了 `Languages/{std,CNs}/std_Taikou_strings.xml`（游戏里是豆腐块）→ 已修，两层现在都干净

**⑥ Owner 列 id 体系** —— ✅ 已随 2026-09-11 家族/势力重建收口（`Owner_<年>` 现由生成器直出 `lord_tk5_*`）
- `Settlements.csv` 的 `Owner_<年>` **306 个织丰编号 → `lord_tk5_*`**（重跑生成器；只动 Owner 列，坐标/名字/家族/兵员一字未动）
- 根因：`renumber_taikou_hero_ids.py` 的 `sync_chain` 只同步了 ModuleData 的 XML，**漏了这四张 CSV**
- 新 checker 加了 **id 体系闸门**（出现 `lord_1_*`/`dead_lord_*`/`spc_*` 即报错），防再分叉
- ~~🔴 `Clan.csv` 205 条 + `Kingdom.csv` 40 条 Owner 仍是织丰编号~~ → ✅ **已清**（2026-09-11：Clan.csv 整表重建 + 势力表口径换日志，两条红已消）

> DLL 状态：`dotnet build -c Debug` 产物已直出游戏目录，可直接跑验证；正式发布再走 VS2022 编译（铁律 19）。

## 一句话现状（2026-09-10 —— 交接口）

> 🔴 **本次复盘**（2026-09-10 用户提议）：数据优先三原则 + 全雷层次统计 + 运行期补丁台账（已合并进主清单）→ [Knowledge/自定义世界内容包从零起步必备清单.md](../Knowledge/自定义世界内容包从零起步必备清单.md) 阶段 2 总纪律 + 「LWN 内置兜底 + 运行期补丁台账」节。

**T1-T3 主线全通**：`建世界（雷 1-22）→ 建号 CC（雷 23-27）→ 进大地图（雷 28-34）→ 城镇交互+进城场景（雷 37-41）→ 圈/拾取手感（雷 39）` 全部实机验证。**当前可玩状态 = 开局 → 建号 → 日本图 → 点京 → 进城中心（有地痞/有马）**。收尾全清：存档 F5 ✅、相机全图 ✅、MCM 页 ✅、全中文 ✅。

🔴 **补丁与防线战线收官（2026-09-10）**：`CampaignMode/` 全部补丁逐条过完，结论 = **「能靠 XML + 离线检查替代的运行期补丁已清零」**：

| 处置 | 补丁 |
|---|---|
| **退役删除 2** | `MapScreenCameraPatch`（相机；正解 = 创建世界期写出生点）· `MapBorderDiagnosticPatch`（边界日志；被 `check_scene_entities` 取代） |
| **归位 `Core/` 2**（通用修复，**与太阁无关**） | `CharacterCultureBackfill` + `AgentDamageModelCultureNullFix`（服务织丰等"模板漏写 culture"的内容包，配套两半缺一不可） |
| **在役 3**（数据无解，必留） | `BackstoryCampaignBehaviorPatch`（拦卡拉迪亚前史）· `CharacterCreationCultureStageSortPatch`（拦原版文化排序）· `CharacterCreationCultureVisualFallbackPatch`（建号大图，等改版） |
| **不处理 2**（用户裁定，保留在磁盘不编译） | `AreaMarkerTagGuard` · `IssuesSettlementGuardPatch` |
| **等 T4 1** | `BattlePowerCalculationGuardPatch` |

逐条"为什么转不了"的论证 → `plans/rules/wheels.d/campaign-mode.md` **卷十**（**别再重复论证**）。
⚠️ **一次误判（已纠正）**：`CharacterCultureBackfill` 曾被我按"太阁侧 0 触发"误判退役，实为**通用修复** → 已恢复并移入 `Core/`。**教训：评审兜底补丁先问「它原本为谁写的」。**

**2026-09-10 当日增量（历史速览；细节见「📦 归档」与雷表）**：数据优先三原则定案；**兜底退役两批**（马补丁 / 9 诊断 / `CultureTemplateNullFix` / `LordIntroConditionGuardPatch`，全部改用**离线证据**退役——并揪出「读旧档看日志 = 假绿」这个坑）；**新雷 45~53**（城镇菜单印 ERROR 文本 / `Clan.HomeSettlement` 置位空转 / 语言文件清单式加载 / 自有键中文覆盖 / 官方拷贝与自家串分离 / 语言文件声明后夹注释 / C# 专有键从未进语言文件 / 雷 11 复发【名字池注释塞进元素里】/ **雷 53 距离缓存过期**）；**防线从四条扩到十三项**（`run_all_checks.py` 一键跑）；**名字池线**：622 条中文从织丰中文包补齐（键值与织丰 622/622 一致 → 用户裁定「先 A 保留 + 标出处」，发布前需授权，自造替换见「三、主线 C 路线」）。

**⚠️ 雷区备忘（已修但需知）**：
- **生成器重跑会覆盖手改**（铁律 22 现场教训——**改数据必须改生成器**；`gen_taikou_culture_full.py` 幂等已修）
- `gen_taikou_workshop_items.py` 输出 bug（2026-09-09 雷 37 同场查处）：模板函数拼 `${group(1)}` 缺空格 → 产出 `<Itemid=`（XML 非法，checker FILE-ERROR）；**2026-09-10 结案**：该生成器与产物 `taikou_produce_items.xml` 一并**已删**（详情见排雷链 41——方案被更省的路线取代后成僵尸，且"merch"路线经反编译证实从来不需要）。原"重跑会重复插入"的警告**不成立**（脚本是整体覆盖），已作废。
- 🔴 **`prune_taikou_items.py` 重跑安全（2026-09-10 修判据后）**：原判据只认"装备引用链/引擎必留"，**漏了场景消费物品（5 匹马）与 EquipmentSet 引用（地痞民用套装）** → 重跑会剪掉进场景所需物品。已补：`SCENE_CONSUMED_ITEMS` 常量（与 `check_scene_consumables.py` 体检对象同步）+ 泛化扫描（所有 NPCCharacters 文件的 `<equipment>` 与 `<EquipmentSet>` 引用）。**dry-run 全绿（43→43 / 11→11, deleted=0）**。今后加物品/套装：先 dry-run 看 deleted=0 再实跑。
- 数据改动后必跑（**一键：`python Scripts/run_all_checks.py`**，15 项 + 汇总约 5 秒）：交叉引用/列表污染 + 字段交集 + **引擎硬编码 38 条 id** + **必填字段/城防 level≤3/occupation 枚举/文化必备/商队护卫硬查询/势力 owner 链** + 文化悬空+角色模板文化属性 + 文化 variation 文本族 + 语言登记/自有键中文 + 官方场景消费 + **地图场景必备实体** + **段注册/孤儿数据文件/csproj 漏登记** + **距离缓存一致性** + **官方拷贝原样** + 改过的 XML 全 parse + **时代段注册互斥/据点 id 跨时代稳定** + **英雄↔模板配对（雷 54）** + **时代差异段产物与生成器一致**。逐项规则见 [必备清单「清单条目 ↔ 脚本对照表」](../Knowledge/自定义世界内容包从零起步必备清单.md)（**纪律：每条检查必须有脚本兜底，禁止只有文字**）

### 实机验证记录（2026-09-10 全清；证据已并入雷表 / 各批次记录）

| 项 | 结果 |
|---|---|
| ⓪ **建新档不崩**（最重要） | ✅ 15:04 —— 雷 52（名字池注释塞进 `<clan_names>` 元素）当场定位，**纯数据修** |
| ① 启动 + 读档 | ✅ 12:46 |
| ② 大地图手感（黄圈 / 鼠标拾取） | ✅ 用户确认 |
| ③ 交易界面 | ✅ 用户确认 |
| ④ 新开档一次 | ✅ 12:47 世界建成、无崩 |
| ⑤ 找领主对话 | ✅ 12:47 织田信长介绍句正常 |
| ⑥ 城镇菜单正文（雷 45） | ✅ 13:01 `ERROR: Text with id …` 消失 |
| ⑦ 新档日志（雷 46） | ✅ 13:01 置位行只出现 **1 次**（修前 100 次） |
| ⑧ 中文复核（雷 47/48/50/51） | ✅ 15:04 菜单/对白/名字/词条全中文，**英文残留 0** |
| ⑨ 相机线 | ✅ 16:19 镜头对准玩家、缩放正常、往东可拖全图 |
| ⑩ 第 4 批停用项 | ✅ 进城镇/村庄正常 |
| T3 尾项（存档 F5 / MCM 页 / 相机东移全图） | ✅ 全清 |

## T1 结案（2026-09-07 深夜，三颗雷合一）

**现象**：`CompanionsCampaignBehavior.InitializeCompanionTemplateList` NRE（GameManager case5）。

**根因三段链**（全部实锤）：
1. **入口**：官方 SPCultures 段被 GameType 白名单正确过滤 👌，但 **Taikou 自己拷贝的 13 个文件（物品/工艺件/音乐/装备模板）带 1835 处 `Culture.<原版八文化>` 引用** —— XML 引用解析经 `GetPresumedObject`（对象不存在时创建"裸对象"，只记 id 不 Deserialize）→ 内存里 8 个裸文化桩（模板列表 null）——验证脚本/证据：`Scripts/check_taikou_xml_references.py`
2. **引擎无防**：`InitializeCompanionTemplateList` 无 null 保护（`foreach culture.NotableAndWandererTemplates`）→ 迭代到桩文化 = NRE
3. **第二源**：spcultures 模板引用 5 个角色但 spnpccharacters 只定义 3 个（今川 2 真空引用）+ `basic_troop=peasant_farmer`/`lord_template_empire_*` 幽灵 id —— 同样的"引用不存在对象"模式

**修复清单（三层，全部已落盘）**：
| 层 | 内容 | 文件 |
|---|---|---|
| 数据·入口 | 13 文件 1835 处文化引用 → `Culture.ikoku`（脚本+parse 验证） | `Scripts/sanitize_taikou_cultures.py` |
| 数据·自洽 | spcultures 删今川 2 模板引用/lord 小节改空；taikou_heroes 删今川 2（"今川全删"裁定落地）；spnpccharacters 增 `peasant_farmer`（基础兵，全字段引用自有资源） | 3 个数据 XML |
| 代码·加固 | `CultureTemplateNullFix`：战役启动把所有文化的 N&W/LordT/RebelliousHeroT 模板列表 null→空/剔除 null 条目（反射，跨版本安全；任何内容包同问题兜底） | `Debug/CultureTemplateNullFix.cs` + LivingWorldCampaign.OnInitialize 调用 |
| 校验 | **数据改动后必跑** `python Scripts/check_taikou_xml_references.py`（0 悬空 = 入门条件） | `Scripts/check_taikou_xml_references.py` |

> ⚠️ **2026-09-10 更新**：上表「代码·加固」层的 `CultureTemplateNullFix` **已退役删除**（数据侧已治本 + 新防线 `Scripts/check_culture_references.py` 常驻守不变量）——文件与 `LivingWorldCampaign.OnInitialize` 调用都已不存在，别再去找；退役依据与恢复方式见文末「📦 归档 → 兜底治理第 2 批 ④」。

**1.5.x 对照**：1.5.1 DLL 中 `NotableAndWandererTemplates` 字符串 0 命中（属性名/归属已变）——加固代码设计为"属性存在才修"（GetProperty null 即跳过），天然兼容；1.5.x 机首测时留意 `[CultureTemplateNullFix] 属性 xxx 在本版本不存在` 日志。

## 📦 归档：T2/T3 完成记录 + 兜底治理第 1、2 批（2026-09-10）

> 归档 = 已完成的执行流水（复盘/交接查这里）；**当前要做的看开头「当前 TODO」**。

- [x] **T2（完成）**：CC 全链实机通过 = 文化 → FaceGen（捏脸）→ 命名/出身 → Review（雷 23-27 全修，探针全绿，用户截图确认——主英雄脸 10 号、原生捏脸界面出）
- [x] **T3（基本完成，2026-09-10）**：**大地图 + 城镇场景双通**——进图不崩（雷 28）✓ / 地图 tick（雷 29）✓ / 相机对准京（雷 30/33）✓ / 京图标可见可点（雷 37）✓ / **进城场景链（雷 40 地痞 + 雷 41 马）✓ 实机顺利进城镇中心** / **黄圈与拾取范围手感调好（雷 39：圈 0.936 / bo_town 1.0，纯 XML）✓**

### 兜底治理第 1、2 批（2026-09-10 复盘产物；原「兜底退役第一步 TODO」）

> 台账 = [必备清单「LWN 内置兜底 + 运行期补丁台账」节](../Knowledge/自定义世界内容包从零起步必备清单.md)。验证成本三档：🟢 日志观察 / 🟡 现有档复测 / 🔴 重开档（**本批全部无需重开档**）。
> 原则：**先建防线（防新增雷）→ 再清旧账（退役补丁）**；每项退役 = csproj 摘除登记行 + 台账行更新状态。

**第 1 批（本步就做，3 项）**
- [x] **① 场景消费脚本（新防线）** ✅ 2026-09-10 —— **是什么**：一个新的离线体检脚本 `Scripts/check_scene_consumables.py`。**体检什么**：太阁进城用的官方场景（如 `empire_town_a`）里藏着各种"出生点"（马厩、动物圈、货摊），每个出生点都**点名要某个物品**（例如马厩点名要 `aserai_horse` 这匹马——点名清单写在场景 prefab 文件里）；太阁的物品库有没有这些东西，引擎**直到进场景那一刻才检查，缺了就崩**（雷 41 马崩就是这样来的）。脚本把这道检查**提前到离线**：扫场景里所有出生点 → 读它们点名的物品/角色 → 对照太阁自己的清单 → 缺谁报谁（exit 1 阻断）。**实测（2026-09-10）**：Taikou 跑出 **missing=0 / satisfied=9**（9 处马消费全命中本包，反向验证 5 匹马修复）；顺带报出 4 个"找不到的场景名"（empire_hippodrome_a 等）= **官方自己也这么写**（官方场景文件实际叫 arena_empire_a，引擎侧有容错）→ 非问题。**已知边界**：只查静态可读的 prefab tags（运行时动态 tags 的情形查不到）。已列入「数据改动必跑」。
- [x] **② 马兜底补丁定案** ✅ 2026-09-10 —— **是哪个**：`Debug/HorseSpawnNullGuardPatch.cs`（2026-09-09 进城马崩当天写的"临时保险"）。**处置：已删**（数据已治本 + 从未编译生效；将来新包缺马交给 ① 脚本离线抓）。
- [x] **③ 诊断补丁归档** ✅ 2026-09-10 —— **下线 9 个**（文件直接删 + csproj 注释；恢复 = git 历史捞回 `git checkout HEAD -- 原路径`——git 本身就是归档，不留工作区副本）：AddQuickInformationLogger / DisplayMessageLogger / ShowInquiryLogger / SceneNotificationLogger / AgentSetTeamLogger / ClickDiag / EncounterDiag / PartyVisualCircleDiag / TownEntryDiag。**保留 6 个（未下线）**（其中 2 个是当时已移出又被编译错误抓回来的）：`GameMenuLogger`（**不是诊断**——是 `UiFullScreenHelper.IsGameMenuOpen` 的状态源，生产依赖）+ NavMesh 五件套（**未结案**的调试工具，memory 记"下一步=最小复现"，需要时再议）。编译 0 错 0 警。

**第 2 批（紧接第 1 批，2 项）** —— ✅ 2026-09-10 完成，**两项都改用「离线证据」退役**（见下：原计划的实机日志验证对 ④ 是坑）
- [x] **④ `CultureTemplateNullFix` 退役** ✅ —— **是什么**：战役启动时把「文化缺模板清单」的半成品对象修好的兜底（雷 11 遗留）。⚠️ **2026-09-10 14:50 退役后复发**：OnNewGameCreated → `InitializeCompanionTemplateList` NRE —— 真因**不是**退役错了，而是当天我给名字池加"来源标注"时把注释塞进了 `<clan_names>` **里面**（引擎盲读该元素所有子节点取 `Attributes["name"].Value` → 注释节点无 name → 反序列化中途 NRE → 该文化模板列表保持 null）＝**雷 52**。已修（注释挪到 `<Culture>` 直接子节点层）+ 新增防线「列表污染体检」（`check_taikou_xml_references.py`，用 `insert_comments=True` 解析器）。**退役前提（数据侧零悬空引用）仍成立** ✓。🔴 **原计划的验证方法是错的**：调用点在 `LivingWorldCampaign.OnInitialize` 的 `SavedCampaign` 早退之后（LivingWorldCampaign.cs:40）→ **读旧档根本不执行，日志永远没有修复行 = 假绿**。**改用三条离线证据（更硬）**：①**加载面枚举**：TaikouCampaign 下实际加载 **42 段**（官方常驻 12 + 本包 30），全文扫 `Culture.*` 引用 → **零悬空**；②**反编译实锤**：`CultureObject.Deserialize` 对三个模板列表**必赋非 null**（先 `new MBList<>()`、末尾整体赋值）——只有「裸文化桩」（引用不存在的对象时凭空造的壳）才会 null，而桩只由悬空引用产生 → ①已把它排除；③新增常驻防线脚本 `Scripts/check_culture_references.py` 把①的不变量固化，今后新内容包再犯 = 离线 exit 1。**动作**：摘调用 + 删 `Debug/CultureTemplateNullFix.cs` + csproj 摘行；`dotnet build -c Debug` **0 错 0 警**，DLL 按 UTF-16LE 搜 `[CultureTemplateNullFix]`=**0**（留存补丁正对照 >0，串搜法有效）。**待实机确认**：下次**新开档**世界能建出来 = 通过（不崩即证）。
- [x] **⑤ `LordIntroConditionGuardPatch` 退役** ✅ —— **是什么**：找领主对话时「自我介绍文本缺失就跳过、不崩」的兜底（雷 35 遗留）。**离线证据**：①Taikou 的 `comment_strings.xml` 与官方 SandBox **id 清单逐条一致**，5 个介绍句 id 全在（含 `.default` 兜底变体，引擎按文化取 variation 时缺 `.ikoku` 会落到 `.default`）；②该段注册在 `TaikouCampaign` 白名单内；③无主城镇 = 0（京 owner=`clan_oda`；`retirement_retreat` 非 Town）；④反编译证实**原版自身已前置校验** `Clan.MapFaction.IsKingdomFaction` 等条件（1.2.12 LordConversationsCampaignBehavior:1383），补丁的 clanBroken 分支本就冗余，真正兜的只有「文本缺失」与「无主城镇」两条 → 均已在数据层消除。**动作**：删 `Debug/LordIntroConditionGuardPatch.cs` + csproj 摘行（编译 0 错 0 警，DLL 串搜 `[LordIntroGuard]`=0）。**待实机确认**：现有档找织田信长对话一次，介绍句正常显示且不崩 = 通过。
- **恢复方式（两件同款）**：`git checkout <退役前 commit> -- 原路径`；csproj 登记行照抄回去。台账行已同步为「已退役」。

**第 3 批（2026-09-10 相机线，1 项退役 + 2 条旧结论更正）** —— ✅ 完成，实机验证通过
- [x] **⑥ `MapScreenCameraPatch` 退役** —— **是什么**：建号完成后拉相机对准玩家的补丁（雷 30/33 遗留）。
  - **真凶另有其人**：相机看不见玩家的**根因 = 场景缺 `border_min`/`border_max`**（雷 34）——相机目标每帧被钳进该盒子，1.2.12 缺实体时兜底成 `900×900`，而玩家在 x=973 = **永远在盒外，怎么拉都拉不过去**。border 补上后（2026-09-10 16:19 日志 `min=(0,0) max=(2048,1280)`）teleport 本来就生效。
  - **正解（已落盘）**：出生点改到**世界创建期**写入 —— `LivingWorldCampaign.StartingPosition`（内容包覆写，Taikou = 985,428）+ `OnNewGameCreatedPartialFollowUp` 的 `i==0` 写 `MobileParty.MainParty.Position2D`。机制依据（反编译实锤）：地图相机的初始目标 = 主队运行时坐标（`MapCameraView.Initialize` 读 `MainParty.Position2D`），该读取发生在「建号完成 → 推入地图状态」那一步，**比建号完成回调更早** → 只有创世界期写才赶得上。时序安全性：引擎 `InitializeMainParty`（放默认坐标）→ `OnNewGameCreated`（我们写）→ 建号；且 `LoadMapScene` 在更早的 `LoadVisualsThirdState`，所以写入时导航面能正常算出（日志时间戳已证）。
  - **动作**：删 `CampaignMode/MapScreenCameraPatch.cs` + csproj 摘行（编译 0 错 0 警，DLL 元数据串搜 `MapScreenCameraPatch`=0）。
  - **保留项（别一起删）**：建号内容 `LivingWorldCharacterCreationContent.OnCharacterCreationFinalized` 里的 `ResetCamera + TeleportCameraToMainParty` **必须留**——它负责把镜头缩放从构造默认 **2.5** 复位成正常的 **15**（反编译实锤），且原版 `SandboxCharacterCreationContent` 同款两句；删了进图就是贴脸画面。
- **两条旧结论更正（写进 wheels/pitfalls，防后人重挖）**：
  1. **雷 30/33「相机问题是 teleport 时序不生效」= 误判**——时序是同步的（`FinalizeCharacterCreation` 先 `CleanAndPushState` 推入 MapState、后调 Content 回调），当时的真凶是 border 缺实体。**教训：症状归因前先确认"有没有第二个更简单的解释"**（这里是数据缺口把正确代码的表现吃掉了）。
  2. **「默认机位来自场景实体 `camera_top`」= 错**——`camera_top` 全游戏 2 万个文件 **0 引用**（官方 Main_map 里有它，但无人读），是美术/编辑器标记；`Campaign.DefaultStartingPosition`（685.3,410.9，非 virtual，**1.3.15 起已移除**——基类引用它会挂 1.5.x 编译）才是"官方默认锚点"的真身。

**第 4 批（2026-09-10 数据化收口，3 项停用 + 1 块诊断清理 + 1 条新防线）** —— 🟡 已落盘，待实机一轮确认后删文件
- [x] **⑦ 新防线 `Scripts/check_scene_entities.py`（离线地图场景体检）** —— 替代运行期 `[MapBorder]` 日志，进游戏前就能查，覆盖更广：border 与地形规格对账 + 5 个引擎硬查询脚本实体 + navmesh + **据点同名场景实体** + 城镇交互链三件套。已进「数据改动必跑」清单（七件套 → **八件套**）。
  - ⚠️ **首跑差点误判（价值与教训各一）**：脚本第一版报 `retirement_retreat` 无场景实体 = 红线。**追查后推翻**——官方 493 据点实测：Town 120 / Village 273 / Hideout 99 **全有实体**，唯一无实体者就是 `retirement_retreat`（官方 Main_map 同样 0 命中）⇒ **服务性据点不在地图上是官方设计，不是缺陷**。规则已改为「只对带 `Town`/`Village`/`Castle`/`Hideout` 组件的据点要求实体，服务性据点打印理由后豁免」，脚本复绿（errors=0）。**教训：写检查规则前先拿官方数据跑一遍**——否则会把官方设计当缺陷，把"已知债"的错误结论写进清单（本轮已同步修正雷 53 的根因表述）。
- [x] **⑧ `CharacterCultureBackfill` —— ⚠️ 退役判定被推翻，已恢复并移入 `Core/`**（2026-09-10 用户纠正）—— 我按"太阁 233/233 模板全带文化、运行期 0 次触发"判它可退役并删除，**判定错了**：本类服务的是**织丰**（织丰 `spnpccharacters.xml` 漏写 culture 的模板，**实测至今仍缺 24 个**：`*_saikai` 系列 + 2 个 Special），太阁侧 0 触发恰恰说明它工作正常。**动作**：从 `a0f94a4^` 取回文件 → 移到 `Core/CharacterCultureBackfill.cs`（通用层，namespace 改 `LivingWorldNpcs`）→ 头注释写明「**通用修复，与太阁无关**」+ 附退役评审教训 → csproj 按 Core 区段登记 → `MySubModule` 重新挂载。编译 0 错 0 警；DLL 字节级搜到日志字串（`已按生成地点`）✓。**教训（已进必备清单台账）**：评审兜底补丁先问「**它原本为谁写的**」，别看"在我们自己的世界里触发了没有"。
- [x] **⑨ `MapBorderDiagnosticPatch` 退役删除** ✅ 2026-09-10 —— 纯日志补丁，被 ⑦ 的离线场景体检完全取代（离线查得更早、覆盖更广）。**动作**：删 `CampaignMode/MapBorderDiagnosticPatch.cs` + csproj 墓碑注释；DLL 串搜 `MapBorderDiagnosticPatch`=0 / `[MapBorder]`=0，编译 0 错 0 警。
- [x] **⑩ 战役启动链诊断块清理** —— 删 `LivingWorldCampaign.cs` 的 **`[LWN-dump]` 世界规模 dump（约 120 行）** + **「京名流兜底生成」**（根因已在数据侧治本、日志 0 次触发）。文件 314 → ~155 行。**保留的真修复（勿当遗留删）**：`EquipmentRosters` 补载、partial-follow-up 注册、家宅/王国家园反射置位、出生点置位。DLL 串搜验证：`LWN-dump`=0 / `京名流兜底生成`=0，保留项均 ≥1；编译 0 错 0 警。
- [x] **⑪ 两个「通用修复」移入 `Core/`**（2026-09-10 用户裁定）—— `CharacterCultureBackfill` + `AgentDamageModelCultureNullFix` 的共同特征：**与太阁无关**（服务对象是织丰等模板漏写 culture 的内容包），且后者在"纯功能包"模式跑原版战役时同样生效 → 从 `CampaignMode/`（战役层）移入 `Core/`（通用层），namespace 改 `LivingWorldNpcs`，头注释写明「🔴 通用修复，与太阁无关」+ 出处 + 评审教训。csproj 按 Core 区段登记 + 旧路径行清理；编译 0 错 0 警，DLL 字节级搜到两类的日志字串 ✓。
- **本批收口状态**：⑦ 新防线进「必跑十三件套」· ⑧ **已恢复**（通用修复移入 `Core/`）· ⑨ 边界诊断已删（离线体检接管）· ⑩ 诊断块清理完成 · ⑪ 通用修复归位 `Core/`。**无遗留待办。**

**暂缓（写清等什么，不占本步）**
- `BattlePowerCalculationGuardPatch`（打法：战斗部署时战力查询缺键崩溃；等 **T4 自建兵种数据**做完后复核兵源来源，再定退役） · `CharacterCreationCultureVisualFallbackPatch`（打法：建号选文化时文化大图缺失空屏；等 **T4 文化图素材**配齐） · `AreaMarkerTagGuard` / `IssuesSettlementGuardPatch`（等**读档崩二分排查定案**——这两个补丁目前被临时注释着，定案后决定恢复还是退役）

## 决策表：每个环节按「织丰做了什么」分类（2026-09-08 用户裁定维度）

> 实施纪律：再遇到「缺 X」类问题，先反编译织丰（Shokuho.dll + spcultures/）拿到它是"做了/不做/怎么做"的证据，再定我们的动作。

| 环节 | 织丰证据 | 分类 | 我们的动作 |
|---|---|---|---|
| BackstoryCampaignBehavior（卡拉迪亚前史） | Shokuho.dll:99600 实锤：`[HarmonyPatch "RegisterEvents"]` + Prefix false | **A 织丰做+我们必做** | ✅ 完成（`BackstoryCampaignBehaviorPatch` 同款） |
| neutral_culture 文化 | 织丰**有定义**（spcultures/shokuho_main_cultures.xml：basic/elite=guard 复用、militia_template、encounter_mesh、roster×2、banner 武器——最小 7 字段） | **A**（引擎 fallback 消费点 32472/32477/47645 等 4 处 + 织丰证词 = 自定义世界需要它存在） | 待做（照织丰最小字段集） |
| PartyTemplate 引线（militia/villager/caravan/elite_caravan/rebels/vassal_reward 6 个） | 织丰有 militia_template | **A**（世界 tick 一跑商队/守军就消费） | 待做（T3 进图前） |
| 兵种（militia melee/ranged/elite） | **织丰做派 = basic=elite=guard 单兵复用**（不造多兵种树） | A | 待做（同做派最少化：guard 1 + militia 2） |
| C 组路人 40 职业（townsman/townswoman/villager/blacksmith…） | 织丰有完整 sho_* NPC 体系 | **B 织丰做+我们最小集不做**（后续 T4 数据层）；但最小 4 个（guard/villager/townsman/townswoman）场景生成必撞 → 提前到 A | 核心 4 个随 Party 线做，其余 T4 |
| gear_practice_dummy_<culture>（43826）/ nervous_caravanmaster | 织丰有 | B（触发 = 进练习场/商队事件——当前 v0 触发面小） | 留 T3 观察/随场景线 |
| 未成年变体 / tournament_master / notary / 舞女 / beggar | 织丰有 | **C 都不做**（v0-T4 不再评；T4 数据层重审） | 不做 |
| 日式素材（mesh/face/banner_key 日本风） | 织丰有（encounter_sho_lord 等） | B/T5 | 先抄官方值（能跑），T5 日化 |
| 卡拉迪亚 LRS 文本（world_lore_strings 注释块） | 织丰 n/a | C | 不做（注释态） |

## 织丰经验索引（2026-09-08 系统盘点）

> **完整版见 `Knowledge/织丰自定义世界观经验.md`（65 条世界工程补丁全清单 + 数据做派方法论 + 证据行号）。发现新经验 → 先更新 Knowledge 文档，再同步本表。**
> 用法：到 T3/T4 环节先回查 Knowledge 对应面，按织丰同款适配。

| 面 | 织丰做法 | 采纳时机 |
|---|---|---|
| 建号/捏脸 | FaceGen 种族名收窄（否则 CC 显示 Imperial/Aserai 出戏） | **T2 可用，建议现在抄** |
| 建号阶段 | 原版 CC 框架 + 自加 ClanNamingStageView（家族命名阶段） | T2+（可选第 3 阶段——太阁5 玩家家名对味） |
| 遭遇-战斗全链 | PlayerEncounter×6 + Encounter 菜单 + MapEvent×5 + BattleEndLogic 等 | T3（进图后第一战前回查 Knowledge §5.2） |
| 城镇体验 | PlayerTownVisit×5 + Barber×3 + SettlementMenuOverlay | T3（§5.3） |
| 地图视觉 | SettlementNameplate×5 + PartyVisual×4 + 天气/相机 | T3（§5.4） |
| 战争层 | 攻城机械×8 + SiegeAftermath + CustomBattle×6 | T4（§5.5） |
| 政治 UI | KingdomManagementVM/Encyclopedia/BannerEditor 系列 | T4+（§5.6） |
| 玩法扩展 | Diplomacy 系 TPatch | ❌ 不采纳：LWN 走自家玩法线 |
| 母本工程 | GameManager 6 步/CC 框架/主线接线/出生点 | ✅ 已采纳 |

## 📦 历史快照（T1 时代，2026-09-07）——**已过时**，现状看开头「一句话现状」

**太阁5 还原工程的自定义战役模式（TaikouCampaign）已过"建世界事件链"第 3 颗雷（Companion NRE，根因=拷贝文件残留原版引用×2 层）**——DLL+数据已就位，**下一站 = 跑游戏 → 建号界面（T2）**。
（⚠️ T2/T3 早已完成并归档；本段仅留作演进轨迹，勿据此判断当前进度。）

## 🔴 三大架构裁定（用户拍板，已入 CLAUDE.md / memory）

1. **三单元**：LivingWorldNpcs = 通用基座（功能只写 LWN）；Taikou = 纯数据包（1.2.12 机 `MB2_Version/MB2_1.2.12/.../Modules/Taikou`，**不依赖织丰**）；ShokuhoTaikouExpansionPack = **已归档**（织丰城池不合格）；未来三国走同通路
2. **双模式开关**：LWN 启动时 `ModuleHelper.GetModuleInfo("Taikou")?.IsSelected`——**不行！判据 = `LivingWorldNpcs.ModuleActivationHelper.IsModuleEnabled`（既有轮子，判据 = `TaleWorlds.Engine.Utilities.GetModulesNames()` 启用列表）**。勾选 Taikou → 主菜单接线战役模式；否则纯功能包
3. **GameType 匹配键 = 战役类名**（实证：官方 `new Campaign()`→"Campaign"；织丰 `ShokuhoCampaign`；我们 `TaikouCampaign`）——`IncludedGameTypes` 与 `GetType().Name` 对比（`MBObjectManagerExtensions.LoadXML` cs 实锤）

## 反编译实证结论（贵，下 session 别重挖）

| 主题 | 结论 | 出处 |
|---|---|---|
| 织丰完整建号链 | `ShokuhoCampaignGameManager : MBGameManager`（DoLoadingForGameManager 6 步）+ `ShokuhoCampaign : Campaign`（namespace `Shokuho.ShokuhoCustomCampaign`）+ 主菜单接线（剔 SandBoxNewGame/StoryModeNewGame/ContinueCampaign + AddInitialStateOption）+ CC = `CharacterCreationContentBase`（namespace `TaleWorlds.CampaignSystem.CharacterCreationContent`）| /tmp/shokuho.txt 已丢（一次性），结论在本表 |
| 沙盒官方初始化 | `SandBoxGameManager`（SandBox.dll）case0-5 与我们的实现一字不差；`SandBoxManager.Initialize` = 99 个 Default* 模型清单（含 `DefaultCharacterDevelopmentModel` @cs12:41112）；`InitializeSandboxXMLs` load 顺序 = NPCCharacters→Heroes→Kingdoms→Factions→WorkshopTypes→LocationComplexTemplates→Settlements | |
| 出生点 | `Campaign.DefaultStartingPosition` **非 virtual**（硬编码 685.3,410.9；**1.3.15 起已移除**，基类引用它会挂 1.5.x 编译）→ 出生坐标由内容包自己写 `MobileParty.MainParty.Position2D`。**🔴 写入时点 = 世界创建期**（`OnNewGameCreatedPartialFollowUp` i==0）：引擎 `InitializeMainParty`（放默认坐标）→ `OnNewGameCreated`（我们写）→ 建号；地图相机初始目标在主队坐标上，且其读取早于建号完成回调 | 2026-09-10 实机验证 |
| 地图相机 | 相机初始目标 = **主队运行时坐标**（`MapCameraView.Initialize` 读 `MainParty.Position2D` + 地形高度）；场景**唯一**的相机输入 = `border_min/border_max`（只钳制不设定）；`camera_top` 是**死实体**（全库 0 引用）；`ResetCamera(true,·)` 兼管**缩放复位**（构造默认 2.5 → 正常 15），原版建号同款调用 | 2026-09-10 实机验证 |
| LoadBasicFiles id 清单 | Monsters/SkeletonScales/ItemModifiers/ItemModifierGroups/CraftingPieces/WeaponDescriptions/CraftingTemplates/BodyProperties/SkillSets（TaleWorlds.Core.Game 436-447） | |
| `CreateMergedXmlFile` 越界 | 空 toBeMerged → `toBeMerged[0]` 越界 = 某 id 在当前 GameType 下 0 段匹配 | objsys:837 |
| Culture 必备组 | lord_templates + rebellion_hero_templates + **notable_and_wanderer_templates**（缺 = `CompanionsCampaignBehavior` NRE）；`default_party_template` 缺 = CalculateAverageWage NRE | |
| navmesh | 战役图必须 `navmesh.bin`（RNM1 v3）或 `nav_mesh_auto_generated_="true"`；缺失 = native AccessViolation | |
| 场景实体绑定 | 实体 name=settlement StringId + `<tags><tag name="town"/></tags>`（子标签非属性）+ 世界坐标=XML posX/posY（0.02m 实证）+ 外层 `campaign_icon_capsule_NN` 惯例（Z=20 哨兵，93 组 261 据点）| |

## 📦 已落盘改动清单（**T1 时代快照，2026-09-07**——现状看开头 TODO；本节仅留作演进轨迹）

> ⚠️ 本节描述的是**T1 阶段**的落盘状态（当时只有 19 个段、只 town_kyoto 一个据点）。**现行状态**：段 30 个（含 9 个 GameText 文本段）、据点 2 个（京 + 退休据点）、`CampaignMode/` 补丁已清到 3 个在役 + 2 个通用修复在 `Core/`。别拿本节当现状读。

**A. LWN（ExampleModVS/ExampleMod/ExampleMod/CampaignMode/）**：
`CampaignModeActivator.cs`（双模式开关+主菜单接线）/ `LivingWorldCampaign.cs`（:Campaign，含 dump 诊断）/ `LivingWorldCampaignGameManager.cs`（:MBGameManager，DoLoadingForGameManager 全链）/ `LivingWorldCharacterCreationContent.cs`（CC 2 阶段 + OnCharacterCreationFinalized 出生点）/ `TaikouCampaign.cs`（thin，`TaikouStartingPosition=(973,421)`）——csproj 已加 Compile；MySubModule.cs 已调 TryActivateCampaignMode。

**B. Taikou 数据包（1.2.12 机 Modules/Taikou/）**：
- `SubModule.xml`：**19 个 TaikouCampaign 段**（世界数据 8 + 基础 11：BodyProperties/SkillSets/EquipmentRosters/Concepts/CraftingPieces/MusicInstruments/MusicTracks/GameText×2/…）
- `ModuleData/taikou_*` 文件：items（目录 10 文件）/bodyproperties/skill_sets/equipment_sets/concepts/crafting_pieces/location_complex_templates/music_instruments/music_tracks/module_strings/world_lore_strings/heroes（最小 7 条）
- `spcultures.xml`（Culture.ikoku：default_party_template + notable_and_wanderer 5 引用 + lord/rebellion 模板）
- `spkingdoms.xml`（kingdom_oda + kingdom_imagawa 已删——今川精简实验）
- `spclans.xml`（clan_oda + player_faction；clan_imagawa 已删）
- `settlements.xml`（只 town_kyoto，pos=969.424/421.563，**已闭合**，XML parse OK）
- `spnpccharacters.xml`（main_hero=官方全量版 + 织田 2 + 今川 2）
- `partyTemplates.xml`（main_hero_party_template）
- `SceneObj/Main_map/`：scene.xscene（nav_mesh_auto_generated_="true"+town_kyoto 实体 969.4/421.6/z4.6+tags）+ **navmesh.bin 已生成（3.58MB RNM1 ✅）**

## 排雷链记录（每颗一行：根因→修）

1. 主菜单点 NewGame → `CreateMergedXmlFile` 越界 = BodyProperties/SkillSets/… 段缺失（GameType 过滤后 0 段）→ 补 4 段
2. 同点再崩 = CraftingPieces 0 段 → 补
3. 同点再崩 = Heroes/LocationComplexTemplates 0 段 → 补
4. 同点再崩 = MusicInstruments/MusicTracks（SandBoxSubModule.OnRegisterTypes 无条件调）→ 补
5. `LoadBasicFiles` 仍崩（误判）→ 实际 = Items（SandBoxCore 物品库被过滤 + 自身空壳）→ 拷贝官方物品目录全量 + GameText 补 2 段
6. `CalculateAverageWage` NRE = Culture.ikoku 缺 default_party_template → 补
7. `Campaign.InitializeGamePlayReferences` NRE（PlayerTroop=main_hero null 链）→ main_hero 条目换官方全量版
8. `Clan.ValidateInitialPosition` NRE（**真凶 = 我们删村庄时把 settlements.xml 弄坏了（缺 `</Settlements>`）→ 首装失败 → Settlement.All=0**）——修复+全量 XML 校验（教训：脚本改 XML 必 parse）
9. 同点再崩（误判今川）→ 今川全删（最小世界实验，设计保持）
10. `CompanionsCampaignBehavior.InitializeCompanionTemplateList` NRE = culture 缺 notable_and_wanderer_templates → 补（引用世界内存在的 5 角色）
11. 同函数再崩 → **结案（见 T1 结案）**：真因 = 拷贝文件 1835 处原版文化引用 → GetPresumedObject 创 8 裸文化桩；+ 模板引用 5 角色只定义 3（今川真空）+ 幽灵 id（peasant_farmer 实为织丰角色 / lord_template_empire_male = 自编 id）→ 修复三层（数据清洗/数据自洽/代码加固）+ 校验脚本
12. `BackstoryCampaignBehavior.OnNewGameCreated` NRE（原版卡拉迪亚前史硬编码 8 领主+town_V6）→ **结案：织丰同款屏蔽**（CampaignMode/BackstoryCampaignBehaviorPatch 打 RegisterEvents prefix false，实证 Shokuho.dll:99600）
13. `Kingdom.OnNewGameCreated` NRE（Leader=null 链）→ **结案**：英雄带装备+neutral/party/roster 数据 → 置位 partial-followup（王都=京，反射 private setter）；⚠️ 两个坑记录：①OnInitialize 置位被引擎 OnNewGameCreated 洗白（时点必须 partial-followup）②**GetObjectTypeList<T> 在 partial 时点返回 null**（Kingdom 被吞教训——改 Campaign.Current 合集）
14. `Settlement.SpawnMilitiaParty` NRE（Culture.MilitiaPartyTemplate 空）→ Party 引线全挂（militia_template 复用）
15. `WorkshopsCampaignBehavior.BuildWorkshopForHeroAtGameStart` NRE（城镇无 Notables → ChooseWeighted(空)）→ **根因 = 文化 N&W 模板池只有 Lord**（CreateHeroAtOccupation 从 N&W 挑职业）→ N&W 补 merchant/artisan；**证据三连：名流非 XML 预定义**（heroes.xml 0 商人/settlement 0 Notables/Hero cell 8 属性）
16. `CaravanPartyComponent.InitializeCaravanOnCreation`（First(CaravanGuard&&Lv26&&ikoku) 零命中）→ **根因 = occupation 枚举非法**（12+ 个非 33 成员）→ **枚举解析失败 = NPCCharacters 段静默截断** → occupation 全合法化 + caravan_guard 等级 1→26
17. `Workshop.InitializeWorkshop`（type null）→ spworkshops 空壳 + SubModule 未注册 WorkshopTypes 段 → 拷官方 14 类型 + 注册
18. `TownMarketData.GetPrice` NRE（分类表空）→ 根因 = 29 物品 merchandise=0 + Outputs 38 分类无匹配 → 6 资源物品转 ikoku+merch + Outputs 裁 9 类
19. `AlleyCampaignBehavior.OnNewGameCreated` DivideByZero（`settlement.Alleys.Count=0` → `i % 0`）→ **Alleys 来源 = `<CommonAreas><Area>` 子节**（Settlement.Deserialize 145801 实锤）→ town_kyoto +2 巷；+`gang_leader` 模板（occupation=**GangLeader**——IsGangLeader 判定→黑帮名流 SetOwner 另一 `% source.Count()` 防炸）
20. CC 启动崩 `CharacterCreationCultureStageVM.SortCultureList`（`Single(x=>CultureID.Contains("vlan"/"stur"/...))`——原版六大文化摆拍排序器）→ **LWN 补丁 prefix 跳过**（`CharacterCreationCultureStageSortPatch`——自定义世界无原版文化）
21. `RecruitmentCampaignBehavior.FindTotalMercenaryProbability` NRE（`Culture.BasicMercenaryTroops` 空节→GetRandomElementInefficiently null）→ +`mercenary_ikoku`（occupation=Mercenary）→ Culture 挂 1 引用
22. `NameGenerator.GenerateClanName` NRE（Culture 名字池空——clan_names/male_names/female_names 空节）→ **织丰日式名池植入**（21/390/214——对味）——崩在我们自己的 OnCultureSelected（生成家名）
23. CC 文化界面**没翻译/没大图/按钮小字**（2026-09-08）→ 三合一：①**文本** = `CharacterCreationCultureVM` 构造时 `FindText("str_culture_rich_name"/"str_culture_description", Culture.StringId)` variation=ikoku——织丰做派 = module_strings.xml 加 `id="str_culture_rich_name.ikoku"`（实锤 Shokuho module_strings.xml:193-207；Native 同构 5947 起）→ Taikou/taikou_module_strings.xml 补 2 词条 + 新建 `ModuleData/Languages/CNs/taikou_culture_CNs.xml`（照 Shokuho_CNs 格式）；②**大图** = `CharacterCreationCultureVisualBrushWidget.SetCultureVisual(id)` → 4 层 `Culture.Banner.Layer.1..4` 每文化一个 `<Style Name=id>`（Native 仅 7 官方 Style——brush XML 实锤 Style 集）→ 无 ikoku state = 空图只留边框；**修法 = LWN `CampaignMode/CharacterCreationCultureVisualFallbackPatch`**（Prefix：id 在 Layer.1 Style 集里不存在 → 换官方兜底 `empire`——判据数据驱动，不硬编码内容包文化；1.2.12/1.5.2 同名同参同私方法已验证，csproj 新引用 TaleWorlds.MountAndBlade.GauntletUI.Widgets）；③**按钮小字** = ShortenedNameText 解析失败残留——补文本自动消失。织丰对照：织丰把文化选择界面整套自建（Sho*View + 地图选文化艺术 + ShoCulture.MapButton.Nankai 自家 brush），v0 我们 = 引擎线 + 官方 art 兜底，T5 日化时再定自建/自美术。**实测通过**（截图：大图/Japanese/描述/无 ERROR）
24. 选完文化进 **Generic 属性阶段即崩**：`CharacterCreationGenericStageVM` 构造 → `CharacterCreationOnInit(0)` → `CharacterCreationMenus[0]` 越界 = **内容套餐列表空**。链路实锤：GenericStageView 按菜单序号 0..MenuCount-1 逐页消费；菜单由 ContentBase.`OnInitialized(CharacterCreation)` 里 `AddNewMenu` 建造（原生 SandboxCharacterCreationContent / 织丰 ShokuhoCharacterCreationContent.OnInitialized 6 菜单全链——织丰 = Family/Childhood/Education/Youth/Adulthood/AgeSeletion 全包 + 11 职业）；我们没实现 OnInitialized = 0 菜单 = 崩溃。**修法**：LivingWorldCharacterCreationContent 加 OnInitialized → v0 最小「出身」菜单（1 菜单 3 选项：武士从者/商人之子/浪人——技能+属性加成，引擎选项自带 ApplySkillAndAttributeEffects；General 世界 2 选项兜底）。彩蛋：Culture 阶段不需要菜单（CultureVM 不查菜单）——所以文化阶段能过、Generic 才崩
25. Generic 阶段 **Tick NRE（`_playerOrParentAgentVisuals` null）**：模型数据 FaceGenChars 为空——填入方 = FaceGen 阶段（`CharacterCreationFaceGeneratorView` 完成时 `new FaceGenChar(...)` → `ChangeFaceGenChars` 实锤）；我们的阶段链是 [Culture, Generic, Review] **缺 FaceGenerator**。原版顺序（1.2.12 SandboxCharacterCreationContent 实锤）= Culture → FaceGenerator → Generic → BannerEditor → ClanNaming → Review → Options；织丰同款（+ RacesPatch 收窄种族）。**修法**：阶段链插入 `CharacterCreationFaceGeneratorStage`（在 Culture 与 Generic 之间）；模型预览场景 = Native character_menu_new（引擎硬编码，任何自定义战役共用）
26. FaceGen 阶段崩 **`MBGlobals.GetActionSet` NRE**：真凶 = `MBGlobals._actionSets` 静态词典 **从未初始化**（`InitializeReferences()` 没人调 → null.TryGetValue 崩）——不是动作库资源缺失（Native 模块永远在，你的原则没错）。必调点实锤：SandBox `EditorSceneMissionManager.DoLoadingForGameManager` case0（紧跟 ModuleData 加载）；织丰自家 GameManager 同样补调（Shokuho.dll:121150）。**我们漏因**：LivingWorldCampaignGameManager.DoLoading 状态机 case0 与母本分叉时跳过了该行。**修法**：case 0 加 `MBGlobals.InitializeReferences()`（幂等：_initialized 守卫；任何内容包战役共通）
27. FaceGen 阶段崩（26 修后新 NRE）——**捏脸模板段被 GameType 白名单过滤**：探针三连（InitBodyGenerator/OpenScene/AddCharacterEntity 全过）→ 死点在 ctor 尾部模板盘查段：`GetObject<BasicCharacterObject>("facgen_template_test_char_0").GetBodyProperties()` NRE——`facgen_template_test_char_0..9` 定义在 **SandBoxCore `spnpccharactertemplates.xml`（NPCCharacters 段），只注册 Campaign/CampaignStoryMode/CustomGame/EditorGame**，TaikouCampaign 下为 null。织丰做派 = 自家副本+自家 GameType 注册（Shokuho SubModule.xml:417-423 实锤）。**修法**：①拷 SandBoxCore 模板文件进 Taikou 模块 ②sanitize 文化引用→ikoku（140 处）③SubModule.xml 注册 `<XmlName id="NPCCharacters" path="spnpccharactertemplates"/>`（仅 TaikouCampaign）④⚠️ 纠偏记录：模板引用 120 件官方物品——我此轮曾全量恢复官方物品库（1759 件）违背"**模仿织丰做自建物品体系**"裁定，被用户当场纠正 → **回滚**（prune 重跑回 69 件精选集）+ **模板装备槽/升级线清空**（887 处）——模板 = 容貌样板，T4 自建物品后再挂自家装备；物品库裁剪真源 = prune_taikou_items.py 不可绕过。checker：0 悬空 0 未知
29. **进图后解除 pause 即崩 `PartyVisual.RefreshPartyIcon` KeyNotFound**（2026-09-08）：`gateBannerEntitiesWithLevels[wallLevel]`——`GetWallLevel()` = 城防建筑（Fortifications）**当前等级**（1.2.12 `Town.GetWallLevel` 实锤；官方场景门旗组（`banner_pos` placeholder + `banner_l1/l2/l3` 墙旗实体）按 {1,2,3} 建组）——**京 town_comp 配了 `level="4"` 超出官方体系上限 3** → wallLevel=4 → dict[4] 缺 key 崩。**修法**：`town_comp_kyoto level 4→3`（数据；checker pass）。织丰对照：织丰场景只有 banner_pos（无 l1/2/3——他们城墙等级天生 ≤3 且未触发 4 级城）。**教训**：官方城墙等级体系上界 = 3；内容包"4 级城"需要自己做第 4 层门旗实体（T4 布局表时决定要不要）
30. **进图后相机看不到角色**（2026-09-08 用户实测）：CC 完成落场时大地图默认机位 = 官方地图坐标（camera_top/默认锚点），与玩家出生点脱节。织丰母本实锤做法 = `OnCharacterCreationFinalized` 里 `MapState.Handler.ResetCamera(true,true) + TeleportCameraToMainParty()`（Shokuho.dll 反编译实锤；织丰场景无 camera_top 实体——他们"看得见角色"靠的就是这段）。**修法**：LivingWorldCharacterCreationContent.OnCharacterCreationFinalized Taikou 分支出生点设置后照抄（编译 0 错）
    - 🔴 **2026-09-10 更正**：本条的「默认机位 = camera_top 锚点」**是错的**——`camera_top` 全游戏 2 万个文件 0 引用（死实体）。相机初始目标 = **主队运行时坐标**（`MapCameraView.Initialize` 读 `MainParty.Position2D`），当时的"看不见"真凶是**场景缺 border 实体**（雷 34）。见文末「兜底治理第 3 批」。
31. **进图每小时 tick 崩 `RetirementCampaignBehavior.CheckRetirementSettlementVisibility` NRE**（2026-09-08）：`_retirementSettlement = Settlement.Find("retirement_retreat")`（SandBox.dll 硬编码）——官方退休据点 `retirement_retreat`（RetirementSettlementComponent + map_icon bandit_hideout_b + gui_bg_village_battania + retreat_complex + scn_retirement 全官方资源）我们世界没有 → null → tick NRE（相机 WASD 失灵 = 崩在每 tick 的连带效果）。织丰做派 = 自建整套退休体系（ShokuhoRetirementCampaignBehavior+RetirementEncounter+OpenRetirementMission——KCD 水准）。**修法（v0 选 A：补数据，不屏蔽）**：官方最小条目追加进 settlements.xml（position 放京边 1090/500；culture 洗 ikoku；checker 0 悬空）——退休菜单/对话随组件自动注册；T4 布局表再统一管位置
32. **无法存档 `SaveFailed: Could not find type definition of type: LivingWorldNpcs.CampaignMode.TaikouCampaign`**（2026-09-08）：战役类（Campaign 子类）进存档必须有类型注册——机制实锤 = `SaveableTypeDefiner` 派生类由引擎启动自动发现实例化（StoryMode `SaveableStoryModeTypeDefiner` base=320000 注册 CampaignStoryMode id=1；织丰 `ShokuhoSaveableTypeDefiner` base=3564814 注册 ShokuhoCampaign id=69——两者均无显式 new 调用点 = 自动发现实证）。**修法**：新增 `CampaignMode/LivingWorldSaveableTypeDefiner.cs`（base=4455667；注册 TaikouCampaign=1 + LivingWorldCampaign=2——通用战役同样需要）
33. **相机不框住玩家**（2026-09-08 用户实测三轮）：OnCharacterCreationFinalized 里 teleport 时序不生效——CC 完成回调时 ActiveState 仍为建号状态、MapState 未推入 → 织丰同款代码被 `if (val != null)` 空检查跳过。织丰等效做法 = 自建 MapView（`ShokuhoMapView`，AddMapView 注入）初始化后再拉相机。**修法（轻量等价）**：`CampaignMode/MapScreenCameraPatch.cs`——`MapScreen.OnInitialize` Postfix（地图就绪时刻，即织丰 MapView 初始化时机）→ `Handler.ResetCamera(true,true)+TeleportCameraToMainParty`。出生点同步外移（973,421→985,428：原坐标落城圈内贴塔，视线遮挡；新坐标 = 京门前一箭地）
    - 🔴 **2026-09-10 更正（本条判定推翻）**：反编译实锤 `FinalizeCharacterCreation` 是**同步**顺序——先 `CleanAndPushState(MapState)`（含 MapScreen 构造 + Handler 赋值）、**后**调 Content 回调 → 回调时 MapState 已就位、teleport 本来就执行了（织丰在同一位置还无保护地写 AddMapView，若时序真不成立他们开新档必 NRE = 反证）。真正卡住相机的是 **border 缺实体**（雷 34）。`MapScreenCameraPatch` 因此属重复劳动，**已退役删除**（见文末第 3 批）。教训：症状归因前先问"有没有更简单的解释"。
34. **日本图相机「空气墙」：到京都（x≈969）以东就动不了**（2026-09-08 用户实测，T3 TODO②反转实锤）：相机目标位置每帧被钳进 `[Campaign.MapMinimumPosition, MapMaximumPosition]`（SandBox.View.dll `ComputeMapCamera` 反编译实锤），而这两个值来自 `SandBox.MapScene.GetMapBorders` = **读场景里 border_min / border_max 两个命名实体**。Taikou Main_map 克隆时主体地形+脚本实体都搬了、但**两个边界实体没带**：
   - **v1.2.12**：引擎对缺失**有兜底**——min=(0,0)、max=(**900,900**)、height=670（SandBox.dll 反编译实锤）→ 相机墙在 x=900 / y=900；京都(969,421) 恰好落在墙外一点 → 症状 100% 吻合
   - **v1.5.x**：`GetFirstEntityWithName("border_min").GetGlobalFrame()` **无 null 保护** → 缺实体 = 进图直接崩（比空气墙更狠，1.5.2 机必撞）
   - 织丰对照：Shokuho Main_map **有** border_min=(87,105,-7.98) / border_max=(2100,2100,1000)
   - **修法（数据）**：scene.xscene `<entities>` 顶部插入 `border_min`(0,0,0) / `border_max`(2048,1280,1000)——地形实体规格 = 16×10 节点 × 128m = 2048×1280（scene `terrain` 节点+`physics_world_max`+`flora_bounding_rect` 三处相互印证）；z=1000 取织丰值（控制最大缩放距离+远裁剪面，1.2.12/1.5.2 通用）——已落盘（1.2.12 游玩库与 1.5.2 主环境两份 xscene 实为同一文件），minidom parse 通过
   - **修法（日志）**：`CampaignMode/MapBorderDiagnosticPatch.cs`——`GetMapBorders` Postfix 打一行 `[MapBorder]`（一次/局），识别引擎兜底信号（(0,0)/(900,900)/670 = 场景又缺实体），PatchAll 自动生效，1.2.12/1.5.2 同名方法二进制 grep 双命中
   - **教训**：蓝图自查表（本表 → 知识库）：**地图场景克隆必带实体 = border_min/border_max + 12 脚本实体清单（见 28）；缺 border 实体 1.2.12 静默降级成 900×900 相机墙、1.5.x 直接崩**——两种版本都要出图前 grep scene.xscene `border_min` 确认
35. **找织田信长对话崩溃 `conversation_lord_introduction_on_condition` NRE**（2026-09-09 用户实测）：领主介绍句条件（TaleWorlds.CampaignSystem.dll `LordConversationsCampaignBehavior`）查文本 `str_comment_liege_introduces_self` 等——`FindMatchingTextOrNull` **文本键不存在返回 null**（反编译实锤，无空保护）→ `textObject.SetTextVariable` NRE。**根因 = SandBox `comment_strings.xml`（含全部 str_comment_*）注册带 GameType 白名单**（SandBox SubModule.xml:227-231：仅 Campaign/CampaignStoryMode）→ TaikouCampaign 下整文件被过滤 → 文本缺失。素材旁证：玩家开场台词（module_strings 系）正常显示、到领主介绍即崩。织丰对照：自备全套文本（ShokuhoLordConversationBehavior + 自家 str_comment_*）。**修法两层：①数据**——**全量盘底（2026-09-09 二次扩充）**：SandBox 全部 **9 个 GameText 文本段**均带白名单（module_strings/world_lore_strings/companion_strings/wanderer_strings/comment_strings/comment_on_action_strings/trait_strings/voice_strings/action_strings——扫描实锤；Native 侧仅 multiplayer_strings 白名单）→ 9 文件官方原样拷贝进 Taikou ModuleData + SubModule 全部注册（TaikouCampaign 段，parse 验证过）；**后续开战新闻错误（str_factions_declare_war_news，用户 2026-09-09 截图）= action_strings 被过滤的同因**，本轮一并排掉 ②代码（LWN 通用兜底）`Debug/LordIntroConditionGuardPatch.cs`——同条件前缀：文本缺失/Hero.Clan 缺失/任一城镇 OwnerClan 缺失 → 跳过该句（对话不崩并打 `[LordIntroGuard]` 日志）；类型+方法名双字符串运行期解析（跨版本静默跳过防线）。parse/build 均过。**教训（进 pitfalls）**：自定义 GameType 下，引擎注册带类型白名单的 SandBox 文本/任意 XmlName 段会被过滤——内容包查"某句话没出现/崩在文本"先看该文本所在文件的 SubModule 注册白名单；**新建战役后扫一眼运行日志的 Text 报错**（运行期才炸）
36. **遭遇敌遇「攻击！」进战斗崩 `BattlePowerCalculationLogic.CalculateTeamPowers` KeyNotFoundException**（2026-09-09 用户实测，后实机验证通过）：部署阶段战术决策 → TeamQuerySystem 战力查询 → 惰性 Evaluate → `dictionary[agentTeam]` 缺键。**根因（[BattlePowerGuard] 日志实锤）**：键桶错位——第一遍按队伍自身 Side 登记字典，第二遍按循环侧取桶；**敌方侧（side=0）枚举里混入 `IsUnderPlayersCommand=true` 的兵源 → `Mission.GetAgentTeam` 首分支返回玩家队（Team 1）→ 玩家队登记在桶 1、查表查桶 0 → KeyNotFound**（PlayerEnemyTeam 非 null，"空键"方向已排除）。自定义世界兵源组建/两军构造顺序差异所致（vanilla 同条件不触发）；兵源具体身份待 T4 自建兵种数据时复核。**修法**：`CampaignMode/BattlePowerCalculationGuardPatch.cs`——前缀替换完整重实现（逻辑与官方一致）+ 缺键兜底（键未登记 → 补登记 0 战力继续打）+ `[BattlePowerGuard]` 诊断；类型+方法名双字符串解析（1.2.12 命中；1.5.x 无此类 = 静默跳过）。**⚠️ 幂等标记教训（2026-09-09 当日二次修正）**：`IsTeamPowersCalculated` 是 auto-property（get; private set;）——初版用 `AccessTools.Field(..., "IsTeamPowersCalculated")` 未命中原名 backing field（`<...>k__BackingField`）→ 标记永远 false → 每次战术 QueryData 过期（5s）都重算 + 每算一次记一行 → **日志刷屏**（实机 10:54 二十+行）。修法 = 反射走 `PropertyInfo.SetValue`（私有 setter 全信任可调）+ 显式 backing field 兜底 + 注入失败打警示行。**教训（进 pitfalls）**：①引擎"先按队 Side 登记、后按循环侧查桶"模式 = 异世界键错位就炸 ②dotnet build 必须显式 MB2_PATH 指 1.2.12（Bash 环境快照坑，2026-09-09 本会话踩）③跨版本类型 = 接口 + 反射（IMissionAgentSpawnLogic 两版有、GetAllTroopsForSide 不在公共接口，禁引具体类）④**auto-property 反射注入：AccessTools.Field 按属性名找字段不可靠，认准 `<PropertyName>k__BackingField`；改私有 setter 用 PropertyInfo.SetValue**
37. **进城（TownCenter）场景加载 NRE：`TroopRoster.AddToCountsAtIndex`（`TaleWorlds.CampaignSystem\Roster\TroopRoster.cs` 第 368 行，`character.IsHero` 处）**（2026-09-09 用户实测）：链路 = `TownCenterMissionController.AfterStart` → `MissionAgentHandler.SpawnLocationCharacters` → `CampaignEvents.LocationCharactersAreReadyToSpawn` → `AlleyCampaignBehavior.LocationCharactersAreReadyToSpawn` → `DefaultAlleyModel.GetTroopsOfAlleyInternal` → `troopRoster.AddToCounts(_thug, n)`。**根因 = gangster_1/2/3 不存在**：`_thug/_expertThug/_masterThug` = lazy 属性 `MBObjectManager.Instance.GetObject<CharacterObject>("gangster_1/2/3")`（1.2.12/1.5.2 两版反编译实锤同名）；官方定义在 **SandBoxCore 段 `spnpccharacters.xml`**，其 SubModule IncludedGameTypes = 仅 Campaign/CampaignStoryMode/CustomGame/EditorGame → TaikouCampaign 过滤 → GetObject null → `AddToCounts(null)` → `data[i].Character` null → `character.IsHero` NRE。织丰做派 = 自家副本 + 自家 GameType 注册（Shokuho spnpccharactertemplates.xml 定义 gangster_1/2/3）。**修法（数据三层，checker 0 悬空）**：①新文件 `taikou_gangsters.xml`（官方定义结构副本：level 6/11/16、occupation=Gangster、traits Smuggler/Thug、升级链、face=fighter_empire、skill_template=level6/11/16 均有；**物品 = Taikou 自有 38 件**——vanilla Item.empire_mace_1_t2 等全部悬空，防呆：物品集外一律禁引）②`taikou_equipment_sets.xml` 加自证民用套装 taikou_civil_gangster_t1/2/3（EquipmentSet equipmentType=Civilian，culture=ikoku）③SubModule 注册 taikou_gangsters 段（TaikouCampaign）。**实测验证**：进城不崩 + 巷内出现地痞（3 人一组 × owner.Power 档）。**旁证**：清单雷 19（AlleyCampaignBehavior DivideByZero——城有 CommonAreas 就创建黑巷，黑巷进城必撞本雷）；雷 38 的 LWN Tag 兜底只挡 CommonAreaMarker null（不同层）。✅ 2026-09-09 实机：顺利进城镇中心（与雷 41 一道收尾）
38. **进城（TownCenter）第二颗：`MissionAgentHandler.SpawnHorses` NRE（`ItemRosterElement(null)`）**（2026-09-09 用户实测 1.2.12 机，雷 41 对位）：`TownCenterMissionController.AfterStart` 第 24 行 → SpawnHorses 扫 `FindEntitiesWithTag("sp_horse")` → `item.Tags[1]` = 物品 id → `GetObject<ItemObject>` → null → 构造 ItemRosterElement NRE。**根因同族（引擎硬编码消费的原版资源不在自定义世界）**：官方 town 场景（`empire_town_a`，Taikou town_complex center 直接引用官方场景——太阁无自有城市场景）内 sp_horse_merchant 实例 → prefab 定义（Native/Prefabs/editor_spawnpoints.xml，1.2.12 本机实锤 7 个 sp_horse prefab = aserai/khuzait/empire/vlandia/battania/sturgia 马 + 重复）Tags[1] 引用马物品 → GameType 过滤未装载（太阁物品 38 件，仅 vlandia_horse/charger/mule/sumpter_horse）→ null → NRE。织丰对照：织丰自有场景 + 自有 sp_horse_kiso prefab + 自家马物品 → 从不踩。**修法两层**：①数据（治本）——1.2.12 官方 5 匹马定义（aserai/battania/empire/khuzait/sturgia_horse）原样拷贝入 `taikou_items/horses_and_others.xml`，culture 洗 ikoku（checker 0 悬空 ✓）②代码（LWN 通用兜底）`Debug/HorseSpawnNullGuardPatch.cs`——前缀替换 SpawnHorses：Tags 缺项/物品 null → 跳过 + `[HorseSpawnGuard]` 日志（不崩，马匹降级缺失）；类型+方法名字符串双目标（1.2.12 `SandBox.Missions.MissionLogics.MissionAgentHandler` + 1.5.2 `SandBox.SandBoxHelpers.MissionHelper`，后者 1.5.2 SpawnMonster 5 参 `#if MB2_GE_140` 分支）。**环境实锤（本次更正）**：1.2.12 完整客户端就在本机 `H:\SteamLibrary\steamapps\common\MB2_Version\MB2_1.2.12\...`（注册表 MB2_PATH 当前指向它，2026-09-09）；Taikou/LWN 模块 = 双客户端 junction 同源，数据改动一处双端生效。✅ 2026-09-09 实机：顺利进城镇中心
39. **黄圈尺寸 / 拾取范围手感（2026-09-09~10 用户实测两轮，纯 XML 调）**：①**圈太大**（截图目测）→ 手动缩 3/5：`town_circle_decal` scale 1.56 → 0.936（世界 1.87m）；②**圈外很远就触发 tooltip**（截图红点）——**根因：拾取与视觉是两个独立组件**——`bo_town`（bo_sphere_collider，仅碰射线碰撞体，雷 37 加）控制鼠标拾取，`map_settlement_circle` tag 的 decal 控制视觉圈；bo_town 原 scale 3.5（×父 2.0 = 世界 7.0）远大于圈 → 收到 1.0 与圈同量级。**官方惯例实锤**（SandBox Main_map 50 城抽样）：两个值的 local scale 各配各的（bo 1.7~7.2 / decal 2.6~9.1，无固定比例）——**官方也是场景作者手调的**。**智能方案否决记录**：曾写 `MapSettlementCircleAutoSizer`（运行时按模型包围盒自动调圈）——用户裁定「纯 XML 静态值不需要代码实时调整」→ 代码已删（同批删掉的还有只读诊断 `MapSettlementMetricsDiag`）。**教训**：场景实体尺寸类问题先查「哪些实体参与该行为」（拾取=bo_*、视觉=decal/mesh）再动手，别只调看得见的那个。
40. **流程雷：csproj 显式 Compile 清单——新增 .cs 不登记 = 静默不编译（2026-09-10 查处）**：本项目 csproj 为旧式显式清单（无通配），`dotnet build` **成功 ≠ 新文件被编译**——9-09 新增的 `HorseSpawnNullGuardPatch.cs` 与 `MapSettlementCircleAutoSizer.cs` 均漏登记，所谓「编译 0 错」是假象（编译器根本没看这两个文件；进城成功全靠数据修复，兜底从未生效）。**纪律**：新增 .cs 文件后第一件事 = 在 csproj `<Compile Include="..."/>` 登记 + build 一次看真编译产物；文件删除时同步删登记行。`HorseSpawnNullGuardPatch` 现保持未登记（数据已治本，待读档二分定案后决定启用）。
41. **僵尸修复遗留 + merchandise 误诊更正（2026-09-10 用户追问查处）**：`taikou_produce_items.xml`（雷 18 的"方案 A"产物：造 ikoku_* 商品）——状态 = **三重死亡**（11 个 `<Itemid=` 全坏 / 未注册 SubModule / 对着旧 23 分类生成）；游戏跑通靠的是"方案 B"（转 ikoku 文化 + 裁工坊分类）。**深入反编译后更正**：①`ItemObject.is_merchandise` **缺省即商品**（`NotMerchandise` auto-property 默认 false；XML 只显式 false 才排除）——雷 18 记录里"merchandise=0"是误诊，**真根因 = 文化不匹配**（`TownMarketData.IsItemPreferredForTown`：`item.Culture == town.Culture` 才可用；官方拷入物品带 empire 等文化）→"转 ikoku"那半是唯一有效修复；②市场分类表筛选 = `!NotMerchandise` + 文化匹配 → 本包 43 件（除 5 件显式 false）**天然都是商品**，市场/工坊路径本来就健康。**处置**：僵尸文件 + 生成器 `gen_taikou_workshop_items.py` **已删**；**连带真隐患补救**：`prune_taikou_items.py` 判据漏"场景消费物品/EquipmentSet 引用"（重跑会剪掉 5 马与地痞套装）→ 已补（`SCENE_CONSUMED_ITEMS` + 泛化扫描）+ `default_stealth_equipment_roster` 保守保留；dry-run 全绿（43→43/11→11）。**教训**：①生成物判据必须跟着"新消费面"升级（场景消费/装备集引用都要进保留集）②改生成物前先 `--dry-run`③"当年诊断"要经反编译复核再用（merchandise 误诊传了三轮）。

28. **进大地图首步崩 `MapScreen.OnInitialize` NRE**（2026-09-08，CC 全链通过后的下一站=T3 第一雷）：`PrefabEntityCache = _mapScene.GetFirstEntityWithScriptComponent<CampaignMapSiegePrefabEntityCache>().GetFirstScriptOfType(...)` 无 null 守卫——**我们的 Main_map 缺官方 12 个地图脚本实体（官方 37906 实体：CampaignMapSiegePrefabEntityCache/MapColorGradeManager/SceneLeveler/SettlementPositionScript/Town Entity Manager/Town Scene Manager/river_generator/water_body/path_converger/sound_emitter/VolumeBox——我们只保住了 ReflectionCapturer）。修法（v0 一次补基建 5 + 京胶囊）**：①`CampaignMapSiegePrefabEntityCache`（引擎硬查询无守卫——NRE 元凶）②`MapColorGradeManager` ③`SettlementPositionScript`（地图锚点）④`Town Scene Manager` ⑤`SceneLeveler`——全部以官方原样空对象块插入 Taikou Main_map xscene；⑥京 = 官方 capsule 结构重拼：`campaign_icon_capsule_1`（Z=20 + Town Entity Manager，与官方 capsule_36 同构）→ children `town_kyoto`（转相对坐标 0/0/-15.399）。**未搬（刻意）**：river_generator/water_body（卡拉迪亚河/水，日本图上出现=出戏）、path_converger（路网）、sound_emitter（卡拉迪亚声）、VolumeBox——留 T3 配日本风。**教训**：地图场景 = 实体+脚本的"内容原型"，从 bigmap 基底克隆地形图时必须带官方脚本实体清单（写进 knowledge——地图场景脚本实体清单是内容包造图的必查表）；xscene 实体插入后 XML parse 必验（已过）。⚠️ navmesh 未受影响（地形未动，实体变化不要求重生成）

**🔴🔴 为"生成器真源"教训加强（16 雷两踩，不再犯）**：gen_taikou_culture_full.py 重跑会覆盖对 spcultures/spnpccharacters 的一切手改（N&W 增补被覆盖/佣兵被覆盖——两现场）。**铁令：凡改 Culture/NPC 数据 → 先改生成器 → 重跑 → checker**；生成器幂等判断的"已存在"检查要**覆盖所有新增 id**。

## 🔴🔴 不再犯清单（16 雷沉淀——改动前逐条自查）

1. **生成器唯一真源**：Culture/NPC 数据增补 = 先改 `gen_taikou_culture_full.py` → 重跑 → checker。**禁止直接改 spcultures/spnpccharacters 再指望存活**（两种死法：手改被下次重跑覆盖〔N&W 增补、佣兵——双现场〕；生成器幂等检查漏新 id〔gang_leader〕）。新增模板后立即把 id 纳入幂等判断。
2. **枚举字段必须过全集校验**：occupation 等枚举值对照引擎枚举全集（Occupation=33 成员）；写错 = NPCCharacters 段**静默截断**（不报错、只少加载——探针才知道）。
3. **每次数据改动后必跑**：`check_taikou_xml_references.py`（0 悬空）+ parse 所有改过 XML（minidom）。
4. **脚本/程序改 XML 必 parse**（09-07 未闭合教训 + 本次两处）。
5. **裁剪前建引擎白名单**（物品 51 / roster 5 / neutral 文化——prune 脚本常量；将来扩展内容包先查）。
6. **时点坑**：Kingdom.InitialHomeLand 置位必须 partial-followup（OnInitialize 会被洗白）；partial 时点 `GetObjectTypeList<T>()` 返回 null——用 `Campaign.Current.*` 合集。
7. **行为链数据依赖一次给全**：N&W 池（名流生成）/名字池（家名生成）/basic_mercenary_troops（雇佣兵）/Alleys（黑巷）/工坊 Outputs 分类——这些组从雷链里学到的"文化必备组"，新内容包照清单配。

## 纪律提醒（这轮踩过的）

- **脚本/程序改 XML 必 parse 验证**（11:30 的未闭合 = 15 轮空跑的导火索）——改完立即 minidom.parse
- 世界数据「半成品型 NRE」模式：**每次 = 某 id/属性没给它就炸**——排雷方法 = 反编译错误函数看 null 点 → 补数据，不该猜
- 数据变动 = 无需编译；DLL 变动 = dotnet build 验语法（本机 MB2_PATH=1.2.12 = 真验证）+ VS2022 最终编译
- LWN 模块 = **1.2.12 环境经 junction 与主环境共享**（Modules/LivingWorldNpcs），改哪边都生效；**只在 1.2.12 编译目标下验证**
- 铁律 22（生成物免手改）：settlements 等数据现在是手写档，**接入布局表生成器后**转为生成物（重跑生成）
