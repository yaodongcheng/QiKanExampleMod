# 已造轮子速查（索引）

> **总纲：加功能前先查本表。命中就复用，不要重写。**
> 本文件是**索引**：先按任务定位域 → 打开 `wheels.d/<域>.md` 读细则（每卷 20–670 行）。
> **只加载命中域的卷，不要整卷全读**——避免把几十万 token 全塞进上下文。
> 路径均相对 `ExampleModVS/ExampleMod/ExampleMod/`。签名为核实后的真实签名。

| 域 | 分卷 | 涵盖内容 |
|----|------|---------|
| 配置 / 双配置(MCM) / 版本兼容 | [wheels.d/config.md](wheels.d/config.md) | Settings 单例与世界观、MCMSettings（MCM 排序渲染坑）、DisabledInteractionMissionModes、设计数据 CSV、Emotion↔台词模板一致性、日志、控制台调试指令、Settings 新增开关、VersionCompat 三锚点（含 **InventoryManager→InventoryScreenHelper 改名表**）、🔴 **模块启用判据（**2026-09-07：内容包勾选判定唯一走 `Utilities.GetModulesNames()`；GetModuleInfo/IsSelected 均为坑，见分卷尾部**）** |
| 演出 / 过场相机 | [wheels.d/camera.md](wheels.d/camera.md) | 🔴 **弹簧臂相机（2026-09-22 登记）**：`Camera/SpringArmCameraView.cs` 三个入口 —— ① 静态机位 `UseCameraTemlate(模板, speaker, listener, anchor)`（只算一次，角色一动就出画）② 🔴 **`ApplyFollowFromEngineCamera(agent, 秒数)` = 表演镜头默认**（照抄开演那一刻的引擎机位，**方向冻住只跟位置** —— 视角不变、跟着人平移）③ `ApplyFollowTemplate(模板, agent, 秒数)`（方向按模板、**跟角色转**，`ArmYaw=0` = 角色正后方）；后两者都是每帧重算、到点自动渐变归还 / `StopFollowCamera()` 立刻还；机位数据 = `ModuleData/DesignData/Camera.csv`；数学 `SpringArmMath` **与飞行相机共用**；🔴 **接管 = 必须接管"看"**（`CustomCamera` 一挂，引擎鼠标 look 整段早退、朝向冻结）+ **进出场只渐臂长/FOV 不渐方向** + **引擎机位方向换算走 `Vec3.RotationZ/RotationX`（`atan2(y,x)` 差 90°，飞行踩过）** |
| Agent 行为 / 警戒 / 移动 | [wheels.d/agent.md](wheels.d/agent.md) | AgentControlHelper 动作走位、🔴 **引擎蹲姿（SetCrouchMode = 玩家 Z 键同机制，非 SetPose）**、🔴 **移动目标分派（找 agent → FollowAgentAction，坐标点 → MoveToPositionAction）**、AgentBrain 行为队列、儿童 human_child、SetPartyAiAction、SetScriptedPosition、战斗回调职责、警戒值系统与字段、AlertTypes、NpcSightSystem 清理 / **感知目标注册链（🔴 追加行为拿不到 OnBehaviorInitialize 的坑）**、🔴 **通用动画状态机（注册制，2026-09-22：定义/运行时分家，别的运动系统可复用；两条硬纪律 = 命中即定 + Force 必带 agent）**、🔴 **配对表演（2026-09-22：两个 agent 同帧起播配对动画 = 角色朝向定位 + `SetMovementDirection` 转身 + NPC 侧必走 `ForcePlayAction`；范本 `custom.exec_pair`）**、🔴 **玩家冻结 + 机身转向（2026-09-21 飞行用，实机验证）：冻结 = `Controller=AI` + `SetIsAIPaused(true)`（三条死路：清零移动输入 / `IsDisabled` / 只切 AI 不暂停）+ 转向 = `SetMovementDirection(Vec2)`（`LookDirection` 实测转不动）** |
| 🔴 法术 / 施法框架 | [wheels.d/spells.md](wheels.d/spells.md) | 🔴 **通用施法框架（2026-09-23 登记）**：法术 = **四段拼装**（起手/瞄准/投送/结算），每段一个接口 + 一张注册表，**加形态 = 在某一段加实现**；三条契约（瞄准产出**意图表** / 投送**有生命周期** / 结算**只认单次命中、节流归投送**）；数据 = 内容包 `AssetRegistry/Spells.xml`（**身份 = 用哪个弹药物品**，族预设给默认）；🔴 **投射物走自管实体**（引擎导弹约 110 米必剔除网格）—— 造实体/挂拖尾/引擎射线判命中（取近的那个）/手搓 `Blow` 走 `AgentDamageHelper.CastBlow` 落地；法印拦截补丁六条纪律；诊断 `custom.spell`（cast/tilt/probe/verbose）；**粒子 XML 属 soln 体系**（不挂 mbproj 静默不加载） |
| NPC 互动意图 | [wheels.d/intents.md](wheels.d/intents.md) | 意图引擎（Evaluate/Goal/…）、IntentBase 新 API（NPC 平权）、BuildForNpc、IntentRegistry、Intent Tactic × ActionParam、NPC 主动意图、InteractionOptionType、SettlementHonorStore |
| 对话 / 叙事注入 | [wheels.d/dialogue.md](wheels.d/dialogue.md) | 对话注入铁律、v2 新模型（Transition 路由）、CrimeDialogueBuilder（辅助方法/自包含/拦截）、AckNode 与 Transition 检定纪律、KCD2 轮次对话、NpcSpeechResolver（XML 模板台词）、PlaceholderResolver、大世界对话接入、延迟操作、🔴 **对话入口禁止普适重定向到 party leader（主队成员→换玩家自己→vanilla 效忠死循环，2026-08-28）** |
| 🔴 对话会话三件套 | [wheels.d/dialogue-session.md](wheels.d/dialogue-session.md) | 🔴 **双层边界与混合在场**（§0 必读：管道看渠道/认知共用/表现按在场裁剪）、**SpeechChannel 说话并联**（优先级/队列/闸门/线程安全/战斗喊话，全量收编 25 处直接 AgentSay）、**PersuadeSlot 说服会话**（agree 公式/兑现/打断/plan_decision 回流）、**SessionDialogueTemplates**（分类×职业×档位 key 回落链 + 无 LLM 完整模板会话）、🔴 **CampaignSession 已删除（2026-08-16：私聊劝说/群聊动议 AI 扩张产物——问句误判动议 + 表态回包必丢弃，用户裁决删除）**、ApplyPlan 合并修复、旁观插嘴 |
| 世界事件 / 犯罪 / 赔偿 | [wheels.d/worldevent.md](wheels.d/worldevent.md) | 事件架构（模拟器→数据库→导演）、出生点寻路验证、通知防刷屏、新婚事件姿势、事实派生 API、ActionRecord 记账、赔偿消费点清单、🔴 **GameMenu 多动态选项文本机制（实例级 SetTextVariable，全局表串名/args.Text 替换无效——赎回菜单两连 bug 实录）** |
| Story 命令引擎 | [wheels.d/story.md](wheels.d/story.md) | JSON 脚本剧情演出（CommandManager + StoryEngine） |
| 偷窃 / 动物 | [wheels.d/stealth.md](wheels.d/stealth.md) | 动物识别/偷动物/持久化/价格修正、扒窃盲盒、撬锁片数、储物道具（四件套/复合键/黑名单）、**战利品挑选共享管线（LootFlowSession）**、浮标五色条、双动体、子弹时间、玩家输入冻结 |
| Gauntlet UI / HUD | [wheels.d/ui.md](wheels.d/ui.md) | 双版本 XML 兼容、VM↔XML 同步铁律、HUD 五元素与显隐、AgentHudVM、性能距离分级、原生弹窗面板、富文本、NinjaNotification 书信流、**GauntletLayer 层序表（原生层序实测）**、🔴 **摘层守卫三件套（HasLayer + !LayerFinalized + try/catch——已 Finalize 层禁止任何引擎操作：1.2.12 的 HandleFinalize 无 IsFinalized 防护，二次摘层 = 二次 OnFinalize = EventManager.OnFinalize foreach null 字典 NRE，实机 2026-08-22）**、🔴 **全屏 UI / ESC 菜单统一检测（UiFullScreenHelper：TopScreen 类型名 + 层名字符串判定不引强引用；ESC 两路径层序实锤 Mission 50 / Campaign 4400；显示+激活分治三处共用）**、🔴 **补丁目标必须精确到实际调用链的类（MissionScreen override OnFrameTick 不走基类，ScreenBase 补丁 Mission 内永不触发；Mission 兜底 = MissionScreen.OnFrameTick 独立补丁类）**、**贴内容气泡（CoverChildren+MaxWidth）**、**贴底滚动语义（Bottom 对齐聊天流：val=MaxValue 才是贴底 + 锁定态防漂移）**、🔴 **ScrollablePanel 滚动三件套纪律（holder 必须兄弟节点 + AutoHideScrollBars 超限才显 + 单 TextWidget 可驱动滚动 + 脚本替换 XML 编辑纪律）**、**长文本 UI 纪律（有界预览+只读摘要+布局刷新节流，BannerlordTalk ManagerTextPreviewPolicy 范本）**、🔴 **Brush 素材 9 宫格（Extend 即开关无 Type 属性，缺 Extend = 整图拉伸 = 按钮拼接缝；上下 Extend ≤ 按钮高/2）**、🔴 **往原版屏动态插入按钮（2026-08-29：版本无关插入点=上溯最近 ListPanel + 反射数据桥（CharacterID/CharStringId）+ 英雄门只注英雄行 + 可见性跟随 + 关屏再开 SetPendingSecretLetter + hover 屏激活门控/3s 重发；「不可见子节点无布局盒」「插入普通 Widget=渲染原点叠目标」「ShowHint 寿命淡出+销毁窗口旧矩形」三条引擎坑见 pitfalls）**、🔴 **立绘/头像 Sprite 按需加载 + 内容包契约（2026-08-31：SpriteAssetsManager（LRU 按需 partial load）+ PortraitRegistry（StringId→阶段卡）；内容包契约=tpac+GUI/*SpriteData.xml+ModuleData/AssetRegistry/*.csv；接入=GetOrLoad→icon.Sprite 两行；坑=ImageWidget 半透明必须 bg.Brush.Color 非 Widget.Color、热重载不缓存 Sprite）**、**tpac 打包链（TpacToolCLI makepack/inspect 命令 + 内嵌 BC3 编码器 + 回解验证闭环；纹理定版 DXT5 无 mip）**、🔴 **「一个屏、两个 prefab 互切」+ 承载时点判据（2026-09-11：静态表→可放主菜单阶段 PushScreen / 读活世界→只能世界建好后挂 TopScreen + 每帧重试）** |
| 记忆 / 叙事迁移 | [wheels.d/memory.md](wheels.d/memory.md) | 记忆系统三件套、QuestManager 硬编码字串清理、🔴 **RecordDynamicMemory 改道（2026-08-21：短期记忆只收 LLM 总结产物，裸事件走 AddHistory("system") 进对话历史）**、🔴 **记忆容量分档（热度 Hot≥10/Normal≥3/Cold 每日 -1 → RecentHistory 40/20/8 + 动态 8/5/2 + 永久 500/300/100；2× 触发总结；RecentHistory 单容器混装，私聊无独立容量）**、🔴 **记忆调试面板 + SeqId 调试编号 + [NPCInfo-Mem]/[ImChatStore] 诊断日志** |
| 存档诊断 | [wheels.d/save.md](wheels.d/save.md) | 🔴 **存档三合一防线 `Debug/SaveGuard.cs`**（2026-08-18 合并）：①字符串超长防护（救档+监控+裁剪）②错误诊断（`SaveErrorReporter`/`SaveSerializeDiagPatch`，新增 Saveable 后遇存档问题的第一取证入口）③**只读属性防护（`SaveFileReadOnlyGuard`：写盘前清 ReadOnly，治 PlatformFileHelperFailure/Access denied）**、🔴 **记忆存档读档周期（2026-08-21：SerializeSlot 双源写回 + 读档清双字典 + OnGameLoadedEvent 只能清 active 不能清 pending——SyncData 之后触发，清 pending = 读档记忆全丢；save_inspect --keys 大小显示不可靠用 --dump）**、🔴 **新档清理（2026-08-23：OnNewGameCreatedEvent = IMbEvent\<CampaignGameStarter\> 三锚点实锤 → ResetAllCampaignState 清 18 组 static——判定标准「static + 只在 IsLoading 恢复 = 必须清」，新增 SyncData 条目必须同步补 ResetAll）** |
| LLM | [wheels.d/llm.md](wheels.d/llm.md) | LLMService（重试/HttpClient 复用）、**连接失败五原因诊断 + 统一展示（ClassifyFailure/ShowConnectionMessage）**、PromptBuilder（静态 prompt 工厂）、**prompt 静态文本单一事实源（LWN_plan_* XML，py/C# 同源，改 prompt 只改 XML）**、🔴 **【目之所及】在场采样器（楼层聚类 + 分层配额 + 优先级采样 + 己方/主公标记 + 视角/人称纪律）**、🔴 **分兵近况段（BuildSplitPartyAwareness：分兵随从自己的队伍位置/AI 行为/兵力，J3 裁剪只裁主队信息；NearestSettlementName 参数化重载）**、🔴 **职务认知注入（V.GetPartyRoleKeys 版本兼容职位查询 + 职位/国王/总督注入；引擎坑：1.4.x MobileParty 不继承 PartyBase、role 文本组无 CN 翻译、GetHeroPartyRoles 仅 1.4.0+）** |
| IM 传讯 / 群聊 | [wheels.d/im.md](wheels.d/im.md) | 群聊回复管线（延迟调度 + 三层丢弃纪律）、**群聊记忆方案 B（参与度写入）**、回应模式人格化（trait/画像/hash 加权）、事件广播线程模型（🔴 async-over-sync 死锁教训 + 三段式）、选人增强（@提及候选/bigram 相似度/沉寂补偿/随机+保底）、🔴 **决策卡片统一结构**（ImButtonVM/CardButtons 数据驱动按钮行、UpdateCardAnchors 单锚点规则；计划卡片/闲聊动作卡片/NPC 提议/群聊动作同构，含双路径分离 + 防死循环纪律）、🔴 **闲聊动作空间模型（ActionSpace 三态：执行人×目标 Mission 内外裁决）**、🔴 **defender 场景优先解析 + 执行期目标解析同口径**、🔴 **多消息分时投递（SpeechPauseFor 字数挂钩间隔 + 延迟队列）**、🔴 **卡片按钮可见性时序（先 IsCardAnchor 后 AnchorCard + 构建后强制广播）**、🔴 **墙钟驱动（后台任务禁止依赖 CampaignEvents.TickEvent——暂停停发；挂 ImChatView.Tick 双端入口 + WorldBackgroundBehavior 生成任务范式：状态机/快速首检低频重试/指纹失效判定）**、🔴 **手柄导航（光标锚定止血补丁/光标跟随焦点 A 键点击语义/自绘焦点准星 + SetMouseToWidget/HideNavCursor 纪律/软键盘取消链双层补丁/0.5s 键窗硬锚）**、🔴 **mask 缓存跨层生命周期残留（Close 必须重置 _lastCompactMask——不重置则第二次打开 SetInputRestrictions 被跳过、新层默认光标隐藏，实机 2026-08-19）**、🔴 **呼出按钮驱动矩阵（Campaign/Mission 两套 tick 隔离：Campaign=OnScreenFrameTick / Mission=InteractionMissionView 同 InteractArea tick / ESC 暂停=MissionScreen.OnFrameTick 独立补丁兜底；ESC/全屏 UI 门控三件套 = 显示 ShouldShow + 激活 ModInput 门控 + 打开 CanOpen）**、🔴 **私聊频道上下文（2026-08-21：GetPrompt_RespondContext includeChannelRows——群聊跳过防重复、私聊包含才能接住"刚在频道里说的话"）+ 频道段频道名标注（BuildChannelRecentSection 首行"（这里是{频道名}）"）+ 私聊显示 = 记忆 im_user/im_npc 行同源（无独立容量）**、🔴 **计划卡片真实目标行（2026-09-13 事故：批准面必须渲染 C# 解析后的目标——LLM 写「您身旁的目标」而步骤填 player = 主公本人，确认门被骗过 → 随从打晕主公）+ 澄清判定改用 LLM 目标（代词命令走玩家原话必然假阴性）** |
| 密谋命令系统 | [wheels.d/planner.md](wheels.d/planner.md) | LLM 计划生成 + PlanExecutor 确定性执行（四件套：语法/世界状态/执行器/ReactiveAgent）、Plot 玩法行、plan_debug、Replan、执行摘要 HUD、🔴 **检定成功率公式（d20 风格：掷点≥门槛，ratio 式 + 模板 Level 估算属性）**、执行期目标解析（快照五层匹配）、🔴 **击晕单管线（KnockoutFlow：玩家/NPC 平权范本，判定+结算共享、壳留节奏与播报）**、🔴 **免确认瞬时动作（RequiresConfirm=false 白名单 + crouch/stand 范本）**、扒窃绕背走位、🔴 **Agent.Index 目标唯一标记（[#N] prompt 标记 + TryResolveIndexedTarget 解析链 + 计划轮【目标指认】段）**、🔴 **Party 动作 IsValid/Execute attacker 侧 dual-check（IM 语境 defender 恒为玩家——执行者是队伍成员自己的动作必须补 attacker 侧，范本 gather_to_player/party_patrol 归队巡逻 + move_to 玩家目标跟随兜底）**、🔴 **目标解析失败必须中止（2026-09-13 事故：`target = Agent.Main` 兜底 = 把失败伪装成"玩家自己要求的"）+ 共享管线差异点必须参数化（铁律 18 补强：主语/计数两问，禁在管线内写死一侧）** |
| 按键映射 / 输入 | [wheels.d/input.md](wheels.d/input.md) | 输入三件套、当前映射表（改键唯一入口）、🔴 **设备检测自监测（TickInputSource：按键>持续输入、同帧按键胜、两污染源过滤——跨帧模拟点击 0.25s 窗 + 摇杆模拟移动 padHeld 过滤，替代引擎 IsGamepadActive 粘性判定）**、UI 按键提示接入范式、🔴 模态门控（IM 面板/弹窗打开期间玩法行暂停）、🔴 **Mission 面板键占用拦截（IsDown+IsPressed 双漏斗/物理键判拦改绑免疫/B ESC 单发）**、🔴 **缩略半模态聚焦门控（2026-08-23 更新：无焦点 A/D-pad 全归面板——A 进聚焦沿被吞，「还给游戏」废止）**、🔴 **ESC/B 统一（HotKey.IsReleased 吞 ToggleEscapeMenu 原版全入口 + 同帧吞窗；🔴 手柄 ↓ 蹲 native 直喂玩家 Agent 拦不到——「可移动」与「屏蔽蹲」互斥，引擎限制）**、🔴 **输入「按下沿」必须只活一帧（tick 开头无条件清——否则跨帧滞留，2026-09-21 飞行二段跳踩到）**、模态 UI 打开自动关 IM、🔴 **IME 组合态吞键（VK_PROCESSKEY 三信号：按键消费 150ms 宽限 + WM_IME 消息 + IMM32；WndProc 子类化全窗口 + 委托保活；EditableTextWidget.HandleInput 前缀吞退格/Enter；🔴 哨兵 int.MinValue 溢出误判坑）**、🔴 **Steam Deck 软键盘三件套（点击聚焦弹浮动键盘 set_FocusedWidget postfix + IM 提交回填 GauntletLayer 放行/UIContext SetAllText + 直通退格 \b 吞键 HandleInput Prefix，门控 Steamworks IsSteamRunningOnSteamDeck 反射 + 竞态重试，全类 #if MB2_GE_130；五坑：GauntletLayer 拦回填/controller 恒 true/检测竞态/双信号双删/显式接口实现补丁崩 PatchAll）**、🔴 **KbDiag 软键盘诊断链路（Settings.KbDiagEnabled 统一开关默认关 + Steamworks 直连探测——反射调 ShowGamepadTextInput 拿 Steam 亲口返回值，绕过引擎桥/PlatformServices；2026-08-24 实锤：Deck 桌面模式恒 false = Steam 客户端无键盘服务，mod 无解；🔴 .NET 字符串字面量 UTF-16LE 编码，mod DLL 字符串验证必须 UTF-16LE 搜索）** |
| 🔴 废弃系统（别碰） | [wheels.d/deprecated.md](wheels.d/deprecated.md) | **旧对话 UI（StoryDialogVM/DialogChoice）+ 旧切磋 UI（DuelMissionView）**——已废弃勿加功能；IM 弹窗确认回调禁调 `_vm.Close()`（触发旧链 OnDialogClosed → GenerateEventAsync 必崩，实机 11:13:37）；现行对话 = 原版对话流 + IM/AgentSay，切磋 = CombatManager |
| 🔴 词表/注册表生成器 | [wheels.d/data-registry.md](wheels.d/data-registry.md) | **外部语料 → 我们侧词表的翻译纪律（2026-08-27 登记）**：**扣除法自检**（复制语料→扣掉能解释的→残渣即漏洞，原语料只读 + 残渣 exit 1）/ **词汇表单一来源**（一套词只在一处定义，其余表派生加前缀）/ **孪生词表检测**（同词异名生成期报错 + TWIN_EXEMPT 白名单）/ **侧名塌缩断言**（同域内两个源词落到同一个我们侧字段 = 报错；含「不算塌缩别去修」三种）/ **万能接收器识别**（兜底规则让自检形同虚设 → 三分类拆分范本）/ 三步复跑与冗余检查清单、🔴 **织丰表数据源纪律（2026-08-28：太阁5 数据一律读 `Knowledge/太阁5/骑砍2织丰角色ID对应/csv/` 镜像，xlsx 只作上游导入源——`xlsx_to_csv.py` 是唯一解析 xlsx 的脚本；镜像 CSV 禁手改，人填数据进生成器映射表；含三动作用表与回归证据）**、🔴 **生成器自检两级口径（2026-09-11：硬错误=生成器 bug / 警告=数据缺口，别把缺口当 bug 报否则逼人放宽标准）**、🔴 **实体 ID 体系与别名表纪律（2026-09-11：多套编号先定基准（低段重合不能外推高段）/ 「同一实体两行」禁止只查 ID 撞号（改用「另一套编号相同」「外键列相同且非模板行」）/ 「同名」可能是父子袭名（判据：名字在对面日志另有槽）/ 别名表撞名闸门两级判据——别名键撞车 exit 1、年代名跨行重名只警告）** |
| 🔴 地形/地图资产工具 | [wheels.d/terrain.md](wheels.d/terrain.md) | **ExportHeightMatMap**（素材→高度图+材质图管线：元素语义分层/富士蓝白雪≠水雪帽检测/PEAKS 地标表/小素材尺度标定 MASTER_W/材质四通道 R草G林B沙A雪；不同分辨率素材→同一世界尺寸 2048×1280m）+ **OpenTrf**（.trf 网格 Blender 导入/导出：rfver4 结构、导入/导出器严格对称往返验证、MyMapTest TRF 路径约定）+ 🔴 **场景黑/白诊断（atmosphere.xml 的 time_of_day=22 深夜全黑、UI 无此属性只能改 XML、原版大气≠白天大气——BigMapLearn 实机 2026-09-06）** + 🔴 **地图场景必备实体 border_min/border_max（2026-09-08：大地图相机边界=场景两实体坐标，底层反编译实锤；缺实体 v1.2.12 静默兑底 900×900 相机墙 / v1.5.x 进图崩；取值=地形 node_dimension×node_size，z=相机缩放+远裁剪面；[MapBorder] 诊断补丁——内容包造图必查表）** + 🔴 **定居点实体组 4 娃（2026-09-09 雷 37：bo_town 只碰射线碰撞体【地图拾取唯一入口，缺=无黄圈/无hover/点不动/进不了城且探针全绿极具迷惑】/ town_circle_decal 黄圈贴花 / gate_position 门 / banner_pos 旗占位+墙级门旗组——与 border/12 脚本实体同属造图必查清单）** |
| 🔴 **3D 资产**（外部模型→骑砍） | [wheels.d/assets.md](wheels.d/assets.md) | 🔴🔴 **§十六 模型导入核对表（2026-09-19）—— 任何 FBX 进编辑器之前逐条核：材质名三者同源 / 贴图三件套+`_s` 通道约定 / PNG 过 `png_for_editor` / 材质勾 Bumpmap+Skinning（重导必回看）/ 导入设置 / Publish 模块外 / 关卡 1 / 目录两态** +  **外部绑定模型 → 骑砍骨架的重定向**（一行公式；四条裁定：🔴 **正面相反用 Y 轴镜像不用旋转**（旋转会连左右一起翻；镜像是反射，**做完必须反转面绕序**）/ 🔴 **旋转只给手臂链推**（源侧骨盆骨最长子骨是腿根 → 差 147.7°；骑砍 `matrix_local` 骨轴不沿肢体）/ 🔴 **手臂缩放拆「径向/沿骨轴」**（各向同性会把手臂也拉长 → 籠手超出拳头 16cm；🔴 **已退役 2026-09-17** —— 头/甲/兜整装改走**逐角色共用源变换 T**，见卷内 §九）/ 比例差用两个 R 吸收不做逐骨 λ）+ **骨映射按关节坐标不按名字**（骑砍名后缀=引擎骨索引；上臂前臂各拆两根→映射前一根；布料骨走父链兜底）+ **部位识别**（单显渲染 + UV 采样贴图颜色聚类；排武器件看材质 `mat_w_*` 不看子网格号）+ 🔴 **编辑器侧六件事**（只勾 Import meshes / **材质勾 Bumpmap+Skinning 漏了=不跟骨架动** / `tex[0]/[2]/[4]`=三贴图 / Publish 目标选模块外 / tpac 拷进 AssetPackages / `Assets` 必须改名否则遮蔽全部 tpac）+ 🔴 **资产命名关系**（`_geo`/`_mtl`/`_tex` 是**文件名后缀**，内部名=你输入的；网格与材质本来就同名）+ **通用工具**（tpaccli 全家 / 二进制 grep 定位 / fbx_probe 关卡 1 / 硬链接单文件加载 / png_for_editor）+ **验证手法四条**（🔴 **底色当自发光渲染**=判断「贴图坏没坏」第一件事 / hide 非目标 LOD / 包围盒自动取景 / 两个诊断脚本）+ **坑表 11 条按症状查** + 🔴 **换「默认脸」= xslt 覆盖 Native 皮肤**（按文件名自动加载、无需注册；**六条纪律** —— **匹配必须锚定 race**、**配置必须放在一定加载的模块**（启动参数 `_MODULES_` 决定加载列表，会覆盖启动器勾选）、**后处理是终点（白编译≠可用）**（`skin[@name='man']` 是跨 race 的，会连专属 race 一起改掉）/ 改网格必须同时改贴图且**条数不能变** / mouth·eyebrow·deform_keys 照原版 / bat 化切换 + 唯一真源；含离线回归脚本）、🔴 **武器（2026-09-15）**：**静态网格体、不要骨架/不勾 Skinning**；**碰撞走 `body_name`（引擎内置 PhysicsShape）、不走网格**（`WeaponComponentData` 无形状字段）——三层分离（视觉 mesh / 物理 body_name / 手感 weapon_length+class）；网格约定 = **原点握持点 · 长轴+Z · 刃宽X · 刃厚Y**；近战可用物理体就十来个（`bo_spear_b`/`bo_sword_one_handed`/`bo_knife_a`/`bo_mace_a`/`bo_axe_longer_a`），按语义挑、**别全用 `bo_mace_a`**；T-pose 源模型的武器是横躺在手上的，要刚体变换 | **含 🔴 换头「该并哪一块」三则判据（2026-09-15 登记）** + 🔴 **自建头装包后处理三件套（2026-09-16 登记：`morphfix→skinfix→metaparts --clearflags`，清 flag 必须排最后否则眼睛/嘴糊成一片）** + **引擎认脸部件的两条规则（按顺序/数量、且顺序分性别：女 脸→嘴→眼→睫 / 男 脸→眼→嘴）** + **`metaparts` 零成本验证通道（只动元数据，不必重导/不开编辑器）** + **离线渲染做判断的三个前提（形状键归零/别删骨架/只信「几何在不在」）** + 🔴 **逐角色共用源变换 T（2026-09-17 登记，取代旧的「径向/沿骨轴拆 R」）**：`src_transform.py` 是**唯一实现**（`s = 1.569 ÷ 源模型头骨 bone_11 高`，`T(v) = (s·x, −s·y, s·(z − z_sole))`），逐角色表 `out/srcT.json`（28 武将 + 23 兵种），三个 builder 都吃 `--t-s/--t-z-sole`；三条硬事实 —— **Y 轴镜像（保 x、翻 y）不能绕 Z 转 180°**（反射行列式 −1 → 必须反转面绕序）/ 🔴 **口径锚头骨不锚整体身高**（含发/兜口径把家康压小 36%）/ **最低顶点不能当脚底**（归蝶鞋底画到 −11.26） + 🔴 **从源模型抠「某个部件」的双判据**（几何：薄/不深/够高/绕轴角向覆盖 **+ 肤色**：碎片 UV 平均色 vs 参照色；🔴 教训 = **下巴带被胡子污染、手部被籠手污染，只有颧骨/鼻梁带干净**；`build_head.carve_neck_part`；**不是每个人都有**=脸壳自带颈部属正常）+ 🔴 **甲侧去重复 `--drop-coincident <头 FBX>`**（甲按**骨骼**剔头骨族会漏掉绑 **`bone_9`** 的脖子皮 → 与头的 `_neck` 件**完全重合**（实测 6 人 222 顶点）→ 按**几何**（KDTree ≤1mm）从甲侧删；计量 `Debug/offline/_neck_dup_count.py`）+ 🔴 **拼装闸门 `check_assembly.py`**（已进一键体检）：硬判 = **一圈最长连续露缝弧 ≤60°**（共面/共点/共边算重叠）+ **单档 gap ≤30mm** + **不穿模** + **无孤立浮片（>50mm）**；🔴 **闸门与管线必须同源**（参照色的 face 组照 `parts_table.face_idx()` 拼，否则两边抠出的件名单不一致）；旧口径 9 过/19 不过 → 新口径 20 过/8 不过 | 🔴 **§十三 两个「产物被自己的补丁毁掉」的坑（2026-09-17）**：① **薄片补背面 ↔ 颈部翻面互斥** —— `flip_inward_neck_faces` 必须豁免「**反平行**孪生面」（面心 0.3mm 内 + `法线·法线<-0.5`，KDTree），否则把双面副本翻成同向 = z-fighting + 薄片丢背面；**闸门报的「朝内面」有大批是副本=误报，先甄别再改，别用「抬 z_top」当解法** ② **`--pick-idx` 挑中的身体件没消费完 → 源子网格泄漏进 FBX**（28/28 全中，每人 5~9 件 `model_0_submesh_*` 源空间 z 0~186cm 的脏资产；修法 = pop 出来的源对象没被用上的一律删、仍被别的角色引用的留；🔴 判据禁用 `_ob.name` 查存在性——已删对象取 `.name` 抛 `ReferenceError`，半藏当场构建失败） | 🔴 **§十四 `AssetSources` 两套布局**：镜像布局（与 `Assets\` 同目录同名）才是编辑器自动同步入口，`sw2\<键>\` 只是管线落点（**写完=没交付**）；同步只对「ModKit 开着期间」的改动有效；交付判据 = `Assets\…\*_geo.tpac` 时间戳 + **装机包量出来的数字**（CLAUDE.md 铁律 31） + 🔴 **BodyProperties 三件事实**（身高 = `KeyPart8` 位 19..24，`AgentScale=(0.9+0.2h)×baseScale` 全量程 ±10% / **weight·build 不在键里**，是动态浮点 + **骨架骨缩放**（甲/头会跟着被拉）/ **引擎没有 race 级默认 BodyProperties 读取点** → 要"选 race 套默认身型"只能写补丁，范本 `RaceDefaultBodyPatch.cs`） + 🔴🔴 **自制动画进游戏（2026-09-19 登记，卷十五）**：五环链路（trf → clip 元数据 → 三件套接线 → 自定义 `item_usage` → 沙箱改名交付）；**四颗雷** = ① trf 写「增量当绝对」（人趴地；判据 = 坏文件首帧 28 根骨齐刷刷全是单位四元数）② **位置轨不能写绝对高度**（人整体抬高 ≈6cm —— 引擎动画要适配不同身高，位置存的是增量；判据 = 首帧应 ≈0 不是 0.9）③ 自定义 `item_usage_set` **不能用 `base_set` 继承**（能拔枪但流程起不来 → 逐字抄原版整块 + 只改 id 与 `reload_action`，并对账「改回两处应与原版逐字节相同」）④ **clip 元数据必须整组抄原版**（`Continue to action` / `Param 2` / `Priority` —— **能播 ≠ 流程认**；装填是两段式，链断 = 弹药永不进膛）；**排查手法 = 二分实验**（把 `reload_action` 临时指回原版动作，一行改动一次重启就能定性；引擎不打印这些流程、日志没用）；交付 = 中转沙箱编译 → **改名**拷进内容包（与内容包自有 `pack0.tpac` 同名会覆盖它） + 🔴 **§十七 捏脸系统两件套（2026-09-19）**：**拉杆错位 = 位移场被前后(Y)镜像**（判据 `tpaccli morphmap` 看帧质心 y；闸门 `check_chan_anatomy.py`；**通道源别再用 `head_tifa_a_v10`，dump `head_xxfemale_a` 当源**；蒂法/萨菲罗斯/亨利/战无2×28 全中招）+ **脸比身体更吃色 = 贴图底色太"成品"**（`渲染色=贴图×肤色乘子+环境光`；原版/xxFemale 是淡底图亮度≈188、亨利≈97；🔴 **"摆动幅度"与"整体明暗"是同一个旋钮，一张贴图修不到两头同时对**；工具 `tint_face_texture.py` + 🔴 **`custom.face_tex` 运行时换贴图已实测不可信**（任何档位只把脸变蓝 = `Texture.CreateFromMemory` 没把像素喂对；调档一律走离线 `texreplace` 循环）+ 🔴 **运行时路径两个 API 坑**（`PlatformDirectoryPath.ToString()` 是相对路径走进程工作目录 / `PlatformFileType.Application` 指向 ProgramData 不是游戏根 → 游戏根只认 `BasePath.Name`） + 🔴 **§十八 动画「带位移」= clip 的 `displacement` 字段（2026-09-20 登记，实机验证通过）**：**引擎不认动画自带的位移轨**（TRF 根骨位置轨只动骨骼、不动 agent 世界坐标 —— 实测播完位置一位没动）→ 要角色真被带走**必须填 clip 的 `displacement` 向量**：口径 = **角色本地水平面、单位米**（**+X 右手侧 / +Y 正前方 / Z 恒 0 / endProgress 0.4~1.0**，由原版 337 条带位移 clip 四对同名对照反推 + 实机正面验证，位移量与向量模长差 0.016%、方向差 0.04°）；值从 TRF 根骨位置轨量（`Debug/offline/trf_root_travel.py`）；两条写入路径 = **ModKit 的 `clip_usage_data` 折叠区**（🔴 不在 Flags 列表里）/ 离线 `tpaccli clipset`；🔴 **改 tpac 元数据三坑**（`AssetPackage.Save` 把每资产 8 字节字段**硬写 0** → 判据是 roundtrip **逐字节零差异** / 别信"改对象再序列化"（版本 6 被写成 5、vec4.w 丢）→ 走**字节级就地替换** / `TypedAssetFactory` **漏注册 `AnimationClip`** → 包里 clip 对代码隐形） + 🔴 **§二十 网格贴图动画（2026-09-22 登记）**：**翻页**（`use_animated_texture_coords`，缩放+偏移，需**图集**）vs **漂移**（`use_texture_sweep`，平移 UV，需**可平铺**）—— 两条路**互斥**，且与 `self_illumination` **三个抢同一个 `Vector Argument 1`**（`.x/.y/.z/.w` 三种读法）；🔴 **硬限制 = 漂移会把贴图里的空间遮罩一起漂走** ⇒ "圆形遮罩 + 会动" **只能走翻页**；`USE_SUNLIGHT` **只有粒子 shader 有**、网格拿不到；可平铺判据（**名字带 `_Tile` ≠ 无缝**）；Material Editor 字段↔界面名↔面板对照 + 可抄的最小配方；全文见 [Knowledge/骑砍2网格贴图动画_引擎能力与实现.md](../../Knowledge/骑砍2网格贴图动画_引擎能力与实现.md) |
| 🔴 plan 写作/审查 | [wheels.d/plan-audit.md](wheels.d/plan-audit.md) | **plan 审计清单（2026-08-26 登记）**：人话五问 / 矛盾核对锚点（README 准绳 + 单所有权文件）/ 剧本纪律七条核对 / 高发坑清单（flag 双义、重复键、口头写入者、自查 vs 自曝、编号错位、计数口径）/ 审计分工法 + 修复派单格式 |
| 🔴 自定义战役/内容包自给 | [wheels.d/campaign-mode.md](wheels.d/campaign-mode.md) | **内容包数据自给校验（2026-09-08 登记）**：交叉引用闭合检查（图→字典判定：自给定义 ∪ 引擎基础层（Native 无过滤段）∪ 别名边 Faction→Clan；未知前缀人工隔离）+ **GetPresumedObject「引用创建裸对象」NRE 谱系**（拷贝官方文件残留引用 = 根因；sanitize 配套清洗）+ ~~CultureTemplateNullFix~~（文化模板列表 null→空 反射兜底，**已退役 2026-09-10**；反射手法与 private setter 的 `GetSetMethod(true)` 用法仍可复用）+ 同族兜底战线索引（AgentDamageModelCultureNullFix / CharacterCultureBackfill / HorseSpawnNullGuardPatch）+ 🔴 **官方场景消费清单（2026-09-09 雷 40/41 总根：场景不过滤、场景内 prefab 引用的对象被过滤——进城 SpawnHorses/黑巷地痞 NRE；排查三步法 = 扫场景 prefab → 读 Prefabs/editor_spawnpoints.xml tags[1] → 缺则拷官方定义入自家库；已知消费面 gangster_1/2/3 + 马 6 种 + 动物 5 只）** + 🔴 **版本警示（2026-09-08）：战役模式 = 1.2.12 独有 API 重灾区（InitialHomeLand/CreateHeroAtOccupation 等 v1.3 移除）——改 CampaignMode/ 必须 1.5.2 机编译验证** + 🔴 **文本/语言线体检三件套（2026-09-10 登记，雷 45/47/48/50/51）**：`check_culture_text_variants.py`（官方无 `.default` 的文化变体族必须自备）+ `check_language_registration.py`（语言文件**清单式**加载/双向登记/emoji/自有键中文覆盖「C#+XML 双源」/声明后夹注释）+ `gen_taikou_english_strings.py`（英文层生成器）；含**语言目录两层结构**（根级英文 + `CNs/` 中文，各带 language_data.xml） + 🔴 **引擎「盲读子节点」元素禁塞注释（2026-09-10 雷 52：`clan_names` 里一条注释 → 文化反序列化 NRE → 雷 11 当场复发；防线 = 列表污染体检，**必须用 `insert_comments=True` 解析器**——ET 默认丢注释查不出、正则会把注释里的字面标签当标签）** + 🔴 **出生点与地图相机（2026-09-10 实机验证）**：相机初始目标 = **主队运行时坐标**（`MapCameraView.Initialize` 读点**早于建号完成回调**）→ 出生点必须在**世界创建期**写（`StartingPosition` 虚属性 + `OnNewGameCreatedPartialFollowUp` i==0，零 Harmony）；border 实体只**钳制**不设定；`camera_top` 是死实体；`ResetCamera` 兼管缩放复位**不能删** + 🔴 **离线地图场景体检 `check_scene_entities.py`（2026-09-10）**：border 与地形规格对账 / 5 个引擎脚本实体 / navmesh / **有地图组件的据点必须有同名实体（缺 = 雷 53 家宅选不中；服务性据点如退休据点官方亦无实体＝豁免）** / 城镇交互链三件套——替代运行期 [MapBorder] 日志，已进「数据改动必跑八件套」 + 🔴 **离线体检全家桶（2026-09-10，卷九）**：`run_all_checks.py` 一键跑 13 项；新增 5 个 checker（引擎硬编码 38 条 id / 必填字段与取值合法 / 段注册与 csproj 漏登记 / 距离缓存一致 / 官方拷贝原样）。**纪律：清单里每条检查都必须有脚本兜底（禁止只有文字）**；写规则前先拿官方数据验、写完必做负面测试 + 🔴 **运行期护栏 ↔ 数据化已逐条排除（卷十，2026-09-10 收官）**：「能靠 XML + 离线检查替代的运行期补丁已清零」，剩下的三类（给别人 mod 兜底 / 拦引擎硬编码 / 引擎自身 bug·时序）都转不了——**新护栏出现时按卷十的三问法决策，别再重复论证**、🔴 **`AssetRegistry/` 运行期数据契约（2026-09-11：生成器→表→LWN 通用读取器，四条纪律含「内容包数据禁用基座键前缀」）**、🔴 **「三处同键」检查器（模板↔画像表↔立绘表↔选人目录，集合同源防漂移）**、🔴 🔴 **六代世界段生成 + 时代段注册迁移 + 距离缓存（卷十一，2026-09-12）**：`gen_taikou_era_world.py`（四段族×6 代；在场判据「Appear 空+有家族=推定在场」救回 33 位女性／商家不立国做独立家族／模板按代切段／每个英雄必带 faction 的收容家族／名字键按「英雄×年代」）· `migrate_era_registration.py` 三坑（判共用段按 path 不按 GameType 数／幂等用文件路径／XmlName 取窗口最后一个）· 距离缓存**断点续传**（85~95% 是两两距离，截断+`-1` 即格式合法，实测省 6 小时）· **体检防 1e30 哨兵**（雷 104：报走不通的对数 + 孤立据点；`CheckPositions` 不是可选项、缓存用场景实体坐标） + **「家臣团 / 势力」派生管线（2026-09-11：从游戏运行时日志反推世界骨架——势力存在性=该年有当主 / 家族=家臣团 R1~R6 / 据点归属=城主家族；含确定性、同名带类型消歧、首领泛用 NPC→实名替补、合并重验+自证断言、按距离合并、改名按最大那年命名六条纪律）**、🔴 **部队模板与兵种最小集（卷十二，2026-09-13 实机验证：文化 8 个部队模板属性的语义 / 占位值会让全图部队变成「主角」副本 / 兵种 `TROOPS` 生成段 / 三条工具链约束 = 小写 `<equipment>` · 民用集双属性 · 官方拷贝源版本）**、🔴🔴 **引擎按「规则/后缀」查定义——缺一个就在该路径上崩（卷十三，2026-09-14）**：①**自建 race（换头/换脸）必须整族复刻 Monster**（Native `human` 一族 5 个 id：`human`/`_child`/`_settlement`/`_settlement_fast`/`_settlement_slow`；只造基础 id → 进据点领主大厅 `Mission.SpawnAgent` NRE 且**栈无下层帧**；`GetMonsterWithSuffix` 缺变体**不回落**）②**引擎零守卫直送原生 → 在唯一收口处封堵**（大地图 AV 段错误、日志无异常直接断；**定位法 = 完整反编译列全部调用点 + 全 DLL 二进制 grep 确认只有「调用方+声明方」** → 单点封堵覆盖全部入口；范本 `MapDistanceInvalidFaceGuardPatch`，含「日志即清单」与「兜底值必须有界、禁返回 null」两条纪律）③🔴 **自建 race 开新人物外观完整清单（卷十三 §三）**：**换头型网格 = 必须开新 race**（`face_meta_mesh` 写在 skin 上、skin 按 race×性别×年龄段选，没有"按角色"的口子；**一个 skin 一个 `face_meta_mesh` ⇒ 一个头型一个 race**）；四处接线（skins.xml 走 `project.mbproj` / monsters.xml 走 SubModule XmlNode / NPCCharacter `race=` / SubModule 注册，**两套加载体系别搞混**）；四个不崩但会坏的坑（skin 年龄段没覆盖 / `face_textures` 条数≠参照物 → 索引越界 native AV / 换网格没换 deform_keys / 被别人的 `skins.xslt` 删 race）；🔴 **验证三纪律**（先留肉眼标记 / 症状二分法"静默回落 vs 刷人时崩" / **验证场景必须覆盖据点领主大厅**——只在大地图看会漏雷）；成本模型（新 race 的 XML ≈ 0，真正贵的是每个头网格一次编辑器编译 → 只换脸池/捏脸/装备的角色**不需要 race**）+ 🔴🔴 **卷十四 旗帜与家纹（2026-09-16 实机验证通过）**：城池旗**听城主家族**（统治家族则听王国旗 → 家族表与势力表都要配）；`banner_icons.xml` **三条硬检查**（声明与 `<base>` 之间、`<base>` 与 `<BannerIconData>` 之间**禁夹注释**，雷 50 同款，违者开局 `TWXmlLoadException` 崩）；**1.2.12 按文件名扫 vs 1.5.x 走注册表+合并**（双端可用就照注册，且**别写 IncludedGameTypes**）；图标 id **先到先得**（原版 1..535 / 织丰 536..839 / Taikou 840..1159）；图标**不需要网格**（运行时拼方块 + 4×4 图集硬编码 UV）；🔴 **几何必须固定模板**（继承借来的底旗 → 55 种几何 → 名牌按旗形裁剪把图标裁掉）；三件套 = `taikou_mon_atlas.py` / `gen_taikou_banner_icons.py` / `import_shokuho_mon_column.py`，人手维护面 = CSV 的 `Mon` 列 + 🔴 **文化「显示名」两层（2026-09-16 登记，雷 132/134）**：**建号卡片读 GameText `str_culture_rich_name.<文化id>`（引擎 `CharacterCreationCultureVM` 写死，不读 `Culture.Name`）**，世界内名读 `spcultures.xml` 的 `name=` 且口径跟 `Culture.csv` 的 **Name 列**——🔴 **改表 Name 列必须同步改语言层 `TAIKOU_culture_<id>`**（两表无任何自动同步）+ **生成器头注释必须幂等**（雷 133：写前先剥自己上次那行，正则要写 `^(?:\s*<!--`，`^<!--` 匹配不上；验收 = 连跑三次 diff 不变） + 🔴 **武将装备接线（2026-09-17 登记，卷十五）**：三个落点（身份档表 `HeroEquip.csv` / 武将专属列 `TaikouHero.csv` 的 `Race`·`Weapon`·`Ammo` / 兵种表）+ 三条口径（**「专属武将」判据 = `Race` 列有值** / **他们身上只带自己那套、不从身份档表补盾** / **远程必带弹药**；非专属武将照旧按身份档表） + 🔴 **「自建 race」判据的唯一入口 `CampaignMode/CustomRaceHelper.cs`（2026-09-17 登记，卷十三 §三 F）**：前缀 `lwn_` = 「给某一个角色做的 race」= 有专属形象（含 `_child`/`_settlement*` 后缀变体；原版 human 族 5 个 id 不算）；🔴 `CharacterObject.Race` 是 **int 索引不是字符串**；消费者 = 捏人「选 race 套默认身型」（雷 139）与**百科角色页立绘补头盔**（雷 142：原版 `SetEquipment(EquipmentIndex.NumAllWeaponSlots, default)` 写错枚举，该名**数值 = 5 = `Head`** ⇒ 本想清武器、实际把全世界的兜摘了；修 = 只给自建 race 补回，原版 race 保持原版行为） |

**登记新轮子**：进 `wheels.d/` 对应域文件追加条目（解决什么问题 + 关键签名 + 调用范例 + 文件路径，与现有格式一致）。域归属拿不准时先问，或就近放入最相关的卷。
