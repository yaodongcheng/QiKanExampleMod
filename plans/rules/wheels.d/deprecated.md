# 废弃系统速查（🔴 尽量别碰）

> **原则**：这些系统已废弃。**不要在它们上面加功能**；修 bug 前先确认该路径是否还在被调用——废弃路径上的「修复」往往只是给死代码续命。
> 现行对话 = **原版对话流（vanilla ConversationManager）+ 新版 IM chat + AgentSay**（DialogueComponent 统一台词管线，见 [dialogue.md](dialogue.md)）；切磋/打架 = **CombatManager**（StartFight 多队模型）。
> 登记时间：2026-08-11（用户裁定）；2026-08-14 增补剧本生成管线、传闻广播管线。

| 废弃系统 | 关键文件/入口 | 替代方案 | 已知坑 |
|---------|--------------|---------|--------|
| **旧对话 UI：StoryDialogVM / DialogChoice.xml** | `InteractionMissionView._dialogueLayer/_dialogueVM`（Mission 启动即创建）、`InteractionController._vm`、事件链 `_vm.Close()` → `OnDialogClosed` → `OnDialogueEnded` → `GenerateEventAsync` | 原版对话流 + IM chat + AgentSay | 🔴 **IM 弹窗确认路径（ATTACK/DUEL/KNOCKOUT/STEAL 的 confirmFight）不得调 `_vm.Close()`**——`_memory` 只在 `StartInteraction` 赋值，无当面对话时 Close 触发 OnDialogClosed → `GenerateEventAsync` 第 2600 行 `_memory.DynamicMemories` → **NullReferenceException**（实机 2026-08-11 11:13:37 崩溃实录）。确认回调只做现行事（`ChatActionFlow.TryExecute` → CombatManager） |
| **旧切磋 UI：DuelMissionView / DuelUI（DuelVM）** | `Combat/DuelMissionView.cs`（Layer 100，StartDuelUI） | `CombatManager.StartFight`（拆队模型：队2 玩家侧/队3 敌方/队4 切磋；`_originalTeams` 恢复） | 无人调用（控制台遗留），勿接新功能 |
| **剧本生成管线（AIStoryGenerator → 导演梗概 + 详细脚本）** | `WorldEvent/SocialEventManager.cs:491`（**已注释**）、`Story/AIStoryGenerator.cs`、`LLM/PromptBuilder.cs`（BuildDirectorPrompt / BuildShowPrompt）、`Story/StoryEngine.cs`（演出端，仅控制台命令 `custom.testAIStoryEvent` 可启动） | 无（未接入常规玩法） | 2026-08-14 用户裁定**停用**：事件广播原本自动触发两次 LLM 调用（导演+脚本），结果仅控制台能播 → 白烧 token。🔴 **已知坑（恢复时必补两守卫）**：无嫌疑人案件（`suspect=none`）→ `ScreenPlayOutline` 构造器 early-return 留半截对象（Accused/Accuser 为 null）→ `BuildDirectorPrompt` 守卫返回 `"ERROR: 无法生成剧本，关键角色缺失。"` 但 `GenerateTaskAsync` 不检查、继续调 `BuildShowPrompt` → 第 1588 行 `outline.Accused.Name` **NullReferenceException**（实机 2026-08-14）。恢复 = ①`GenerateTaskAsync` 检测 ERROR 前缀短路 ②`BuildShowPrompt` 开头加 null 守卫。另：`ScreenPlayOutline` 构造器失败是静默 return，建议加 `IsValid` |
| **传闻广播管线（SocialEventManager / NewsSpreadSystem / ActionTransactionSystem）** | 入口已注释：`WorldEvent/WorldEvent.cs` ProcessEmerging（原调 `SocialEventManager.BroadcastWorldEvent`）、`Core/AgentControlHelper.cs`（ApplyDivorceMarriage/ApplyMarriageLogic/OnPlayerSelect_MarryNewLover 内 4 处 `ActionTransactionSystem.*`） | 无（替代渠道：AlertTypes 警戒 / AgentBrain 质问 / IM 聊天） | 2026-08-14 用户裁定**停用**（久未维护：`CreateDivorceEvent` 返回 null 半成品、ScreenPlayOutline 半成品、文案旧味）。链路：BroadcastWorldEvent / ActionTransactionSystem → `NewsSpreadSystem.BroadcastEvent` → `ProcessHeroRecursively` → `memory.ReceiveNews()` 写 `KnownEvents`。🔴 **影响面**：①NPC 不再经此链"听说"犯罪/婚姻事件（`KnownEvents` 停止写入）②消费端 `AttitudeSystem.ComputeStance`（InvestigationEngine/AgentBrain/PlaceholderResolver 现行调用）与 IM 聊天人格 prompt（PromptBuilder.cs:220）读 `KnownEvents` 有 `?.` 兜底 → 退化为默认立场，不崩 ③结婚/离婚功能本身（MarriageAction.Apply、好感、公告）**不受影响**——只停了新闻广播部分。恢复 = 取消注释入口即可 |

**通用检查**：改到这些文件/字段（`InteractionMissionView._dialogueVM`、`InteractionController._vm`、`StoryDialogVM`、`DuelMissionView`、`OnDialogueEnded`、`GenerateEventAsync`）前，先确认调用点是否还在现行路径上——IM 弹窗确认、IM 闲聊动作、CombatManager 之外的调用一律视为死代码，不修不扩。
