# ============================================================
# LivingWorldNpcs .gitignore
#
# 🔴 书写纪律：一律用「整目录」或「通配」，禁止写死单个文件名。
#    点名 = 新产物漏网（实测教训：Debug/prompt_dump_*.txt、compat_log_*.txt、
#    plans/**/tools/_probe_*.txt 21 个，全因规则写死了别的文件名而漏进库）。
#    详见 CLAUDE.md「目录归口与 git 收纳政策」。
# ============================================================

# ── Visual Studio ──
.vs/
*.suo
*.userosscache
*.sln.docstates
*.user

# ── 构建输出（任意深度）──
[Bb]in/
[Oo]bj/

# ── NuGet ──
packages/
*.nupkg

# ── 发布打包输出 ──
release/

# ── Python ──
__pycache__/
*.pyc

# ── 通用临时/日志/备份 ──
*.log
*.tmp
*.temp
*.sav
*.bak
*.bak_*

# ============================================================
# 🔴 Debug/ = 模块根唯一产物根（运行时 + 离线），默认全忽略
#    白名单只留 C# 读的开发夹具 PlanExamples/（PlanDebugCommands.cs）
#    离线/一次性产物统一放 Debug/offline/，被本兜底规则覆盖，无需单独写规则
# ============================================================
Debug/*
!Debug/PlanExamples/
!Debug/PlanExamples/**

# ── `_` 前缀 = 改完即弃的临时物（项目政策，全局生效）──
#    Scripts/_*                        ：_tmp_* 等临时脚本
#    plans/**/tools/_*                 ：_patch16b*（16b 落点裁定表补丁）+ _probe_*.txt 等
Scripts/_*
plans/scenario-campaign-mode/tools/_*

# ── 规则空档补漏 ──
.codemaker/

# ── UE 拆解详情树：可由 tools/ue-dissect/detail_tree.py **完全重生成**（镜像 UE 工程目录，410 页）──
#    重生成：python tools/ue-dissect/detail_tree.py（--no-pins = 薄版）
Knowledge/FCS详情解析/

# ── 工具目录：源码入库 + 产物整目录忽略（本仓库的正确范例）──
tools/loading_pipeline/gen/
tools/video_tools/out/
tools/ExportHeightMatMap/SourceMap/
tools/ExportHeightMatMap/Output/
tools/ExportHeightMatMap/NativeExample/
tools/face-pipeline/**/bin/
tools/face-pipeline/**/obj/
tools/face-pipeline/data/
tools/face-pipeline/dist/
tools/face-pipeline/*.png
# FreeUV 外部依赖（内嵌 git 仓库）：管线已改为 LLM 生成 UV 图，不再需要，整目录不进库
tools/face-pipeline/external/FreeUV/

# armor-pipeline（盔甲构建工具链；产物整目录忽略，规则用整目录不用文件名——铁律 26）
tools/armor-pipeline/out/

# anim-statemachine（动画状态机工具链：XML -> 可视化页 / 编辑器 + 自检三件套）
#   代码与文档进库；两个页面（生成物）与 _shots/ 截图全在 out/ 下，整目录忽略。
tools/anim-statemachine/out/

# anim-retarget（Blender 动画重定向工具链）
#   代码 / 文档 / 数据集元数据进库；大数据面在 D:\BrainMaker\骑砍2动画重定向\（见 tools/anim-retarget/README.md）。
tools/anim-retarget/docs/**/*.mp4
#   viewer：页面与脚本、dataset.json（含手调的 11 组站位参数）进库；
#   烘出来的 GLB（137MB，可由 input/source 的 FBX 重生成）与 serve.py 的滚动备份不进库
tools/anim-retarget/viewer/datasets/*/assets/
tools/anim-retarget/viewer/datasets/*/_backup/
#   下面几条是【防御性兜底】：这些目录本不该出现在工具目录里（数据都在 D 盘），
#   防的是将来有人在工具目录里跑出产物、或误把数据拷进来。
tools/anim-retarget/input/
tools/anim-retarget/output/
tools/anim-retarget/UEAnims/
tools/anim-retarget/_legacy/

# particle-pipeline（粒子特效工具链：UE/手写 spec -> 骑砍 XML -> 离线自包含预览页）
#   同样的两根设计：代码进库、数据在 D:\BrainMaker\骑砍2粒子特效复刻\（见 tools/particle-pipeline/README.md §1），
#   两边用 junction 连成一份。所有产物（XML / 预览页 / 静帧）都落数据根，本目录本不该有产物。
tools/particle-pipeline/out/
#   下面是【防御性兜底】：防的是将来有人在工具目录里误跑出产物、或把数据拷进来
tools/particle-pipeline/**/out/

# tpactool（tpac 读写/编译工具链：粒子资产离线编译、贴图替换、网格元数据就地改）
#   工具产物（编译出来的 tpac）一律不进库 —— 与上面几条同理。
tools/tpactool/out/
tools/tpactool/**/out/

# ── 不同步 AIInfluence 子项目 ──
ExampleModVS/ExampleMod/ExampleMod/AIInfluenceProject/

# ── 音频资产（太阁 BGM / 菜单主题——2026-09-06 已迁往内容包 Modules\Taikou，勿再入库）──
music/
ModuleSounds/

# ── 参考素材与研究工作区（太阁5 图片素材、地图研究中间产物，不入库）──
refs_koei/

# ── 剧本工程：转化器生成产物（tk5_to_json.py / stage_inject.py 随时可重生成）──
#    跟踪的只有 plan 文档 + py 脚本；审核记录.md 是文档，保留跟踪
plans/scenario-campaign-mode/story_event_json/*
!plans/scenario-campaign-mode/story_event_json/审核记录.md

# ===== TK5 分析工程（_analysis）：只保留最终成果 csv + 关键解析脚本，中间产物不入库 =====
Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/*
# --- 关键解析脚本（生成链入口 + 核心解析器）---
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/build_v2_era.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/build_viewer.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/build_era_init_v1.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/extract_affiliations.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/gen_city_era.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/rebuild_city_bridge.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/pair_cities.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/cluster_cities.py
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/migrate_aliases.py
# --- 关键输入事实表（城名官方名单 / 编辑器 S-box 表）---
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/names_180.txt
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/editor124/
Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/editor124/*
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/editor124/*.rtg
# --- 最终成果目录（每年代 CSV + 总览 + HTML 成品）---
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/decoded/
Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/decoded/*
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/decoded/era_v2/
!Knowledge/太阁5/骑砍2织丰角色ID对应/_analysis/decoded/era_v2/**
